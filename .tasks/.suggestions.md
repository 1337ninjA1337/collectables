## Upgrade suggestions

### Chat DB (2026-05-16)

- **Cursor pagination for `fetchMessagesForChat`** — it currently `select=*&order=created_at.asc` with no `limit`, so a long-lived conversation pulls the entire history into memory on every hydrate. Add `&limit=50` + a `created_at` cursor (or PostgREST `Range` header) and load older pages on scroll-up. Bounds memory and free-tier egress.

- **Realtime reconnect backfill** — while the WebSocket is down, `subscribeToInbox` misses INSERTs and only `refreshFromCloud` (mount-time) closes the gap. On `SUBSCRIBED` after a drop, fetch messages `created_at=gt.<last-known>` per active chat so no message is silently lost during a flaky connection.

- **First-class conversations table** — replace the free-text `chat_id` with a `conversations(id uuid, user_a uuid, user_b uuid, UNIQUE(user_a,user_b))` table and a `chat_messages.conversation_id uuid REFERENCES conversations(id)`. A real FK + unique participant pair is stronger than the derived-string CHECK and unlocks per-conversation metadata (muted, archived, last_message_at) without re-deriving from messages.

- **Functional idempotency test with a mocked fetcher** — the new ignore-duplicates path is only covered structurally. Add a `__tests__/supabase-chat-idempotency.test.ts` that injects a fake `fetcher` returning `200 []` and asserts `sendMessage` resolves to the synthesized message (not `null`), plus a fake returning a populated row for the happy path.

- **`chat_messages.client_id` + server uuid split** — keep the server `id uuid DEFAULT gen_random_uuid()` authoritative and add a separate indexed `client_id uuid` (the composition-time key) with a UNIQUE constraint for idempotency. Decouples "who owns the primary key" from "idempotency key", which is the canonical chat-storage pattern and avoids ever trusting a client-supplied PK.

- **SPA fallback regression guard** — Add a CI step that runs `npm run build` then asserts (a) `dist/404.html` contains `location.replace`, (b) `dist/index.html` contains `data-spa-restore`. This catches a future contributor accidentally reverting the post-build script (e.g. swapping back to `cp dist/index.html dist/404.html`) before the change reaches production iOS users.

- **Service worker for instant deep-link load** — Even with the sessionStorage redirect, the user sees a flash of the redirect page on iPhone. Registering a service worker (Workbox `NetworkFirst` + `NavigationFallback` to `/collectables/index.html`) would let cached deep links resolve to 200 directly and remove the redirect entirely on repeat visits.

- **Sentry breadcrumb for SPA URL restore** — Drop an `addBreadcrumb("spa-redirect", { from, to })` inside the restore script (gated on `__sentry_loaded__`) so Sentry sessions show exactly which deep-link URLs hit the 404 fallback. Useful for ranking which routes deserve a real static export.

- **Static export of common collection IDs** — Expo Router could pre-render the top-N most-shared collections at build time (e.g. by reading from Supabase) so their URLs return real 200s without the redirect dance. Reduces flicker and helps SEO for public collections.

- **Migrate baseUrl to a single source** — `app.json` declares `expo.experiments.baseUrl`, `lib/url-helpers.ts` infers it at runtime, and `scripts/build-spa-fallback.ts` re-reads it from app.json. A single `lib/base-url.ts` exporting a const + a helper that all three import would eliminate the drift risk if the deployment path ever changes (e.g. moving off the `1337ninja1337.github.io/collectables` prefix).

### Power BI starter template (2026-05-17)

- **Generate `.pbit` measures from `lib/analytics-events.ts`** — `STARTER_MEASURES` in `lib/powerbi-template.ts` is hand-maintained and only parity-checked against `measures.dax`. A small codegen that derives per-event count measures (`Count_listing_created`, …) directly from the typed `ANALYTICS_EVENT_NAMES` union would keep the model exhaustive automatically as new events are added, instead of silently missing them.

- **CI round-trip unzip check** — the test re-implements a central-directory parser. Add a CI step that shells `unzip -t docs/powerbi/Collectables-Starter.pbit` and asserts all 8 entries + zero CRC errors, so a future ZIP-writer regression is caught by a real archiver, not just our own reader.

- **Properties jsonb auto-expansion** — the embedded partition M leaves `properties` as text; the connection doc tells users to expand it by hand. Add a second template variant (or an M step behind a `ExpandProperties` boolean parameter) that `Json.Document`-parses and expands the documented keys per event (`method`/`provider`/`language`, `mode`/`hasPrice`, …) so funnel-by-property slicing works with zero Power Query authoring.

- **Wire `build:powerbi` into a verify gate** — `npm run build:powerbi` is manual. Add it to `lint:ci` (or a pre-commit hook) so the committed `.pbit` can never drift from the source; today only the test catches staleness and only if someone runs it.

- **Ship a default Power BI theme part** — the report omits `resourcePackages` to avoid a missing-theme dependency, so visuals render with Power BI's stock palette (not the app's warm-brown `#261b14`/`#d89c5b`). Add a `Report/StaticResources/SharedResources/BaseThemes/Collectables.json` theme part matching the app palette and reference it from the layout config for an on-brand starter dashboard.

### trackEvent rate limiter (2026-05-17)

- **Share one limiter primitive** — `lib/sentry.ts` and `lib/analytics.ts` now carry near-identical sliding-window code (`recentEvents`/`recentAnalyticsEvents` + `*RateLimitAllow`). Extract a pure `lib/rate-limit.ts` `createSlidingWindow(max, windowMs)` factory both import, so the window logic is tested once and a future fix can't drift between the two stacks.

- **Per-event-name caps** — the limiter is global across all event names; a runaway `chat_opened` debounce regression would also starve a legitimate `signup_completed` in the same minute. A small per-`AnalyticsEventName` bucket map (e.g. 50/min each) would isolate a noisy event from suppressing rare high-value ones.

- **Drop-count breadcrumb** — when the limiter sheds events the loss is silent. Emit a single throttled `addBreadcrumb("analytics-rate-limited", { dropped, windowMs })` (gated on Sentry being ready) the first time the window saturates, so the drop is visible in crash sessions and we can spot the runaway loop that caused it.

- **Dedupe identical rapid events** — beyond the hard cap, collapse byte-identical `(name, props)` pairs fired within ~250ms into one capture. A re-render storm usually emits the *same* event repeatedly; deduping preserves signal while cutting volume far more than a blunt count cap.

- **Make the cap config-driven** — `MAX_ANALYTICS_EVENTS_PER_WINDOW` is a hard-coded literal. Reading an optional `EXPO_PUBLIC_ANALYTICS_MAX_PER_MIN` (via the existing `readAnalyticsEnvFromProcess` inlining helper) would let the cap be tuned per environment without a code change if real traffic shows 200/min is too tight or too loose.
