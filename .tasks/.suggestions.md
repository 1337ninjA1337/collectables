# Upgrade Suggestions

- Make the `VIEWER_PROFILE_TTL_MS` constant configurable via an env var (`EXPO_PUBLIC_PROFILE_CACHE_TTL_MS`) so it can be tuned per-environment (e.g. shorter in staging to catch stale-data bugs faster) without a code change.
