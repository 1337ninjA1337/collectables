## The green check that meant "queued" and said "build" (2026-09-21)

<!-- Fifth bullet of "After the shared walk floor" (2026-09-19), and the same bullet in the two lists before it: "The EAS job reports success for queueing, and the job should say so — `--no-wait` is a well-argued choice, but the green check means 'a build was queued' and reads as 'the native app builds'. One line in the job summary naming the Expo dashboard removes a false signal that has been up for months." -->

- [DONE: 2026-09-21] [est: 28000] **A check named "EAS iOS preview build" has been going green for having queued one** — `--no-wait` is the right trade and nothing here argues with it: waiting would hold a runner for a twenty-minute native build whose result nothing in this repository reads. What it leaves behind is a green check making a claim about building that nobody measured, on the one target `npm run verify` does not cover — and `npm run bundle:native` currently fails, so the optimistic reading is not merely unsupported, it is wrong.

  **The summary says which of the two it is, in the place a reader lands.** "A build was QUEUED. It has not been built" — then why the check is green (EAS accepted the job), and where the outcome actually is. The build page EAS prints is captured out of the CLI output and linked; when the output format changes the dashboard is named instead, so a changed format costs a convenience rather than the whole signal.

  **Three paths, not one.** `if: always()`, because the runs whose colour is hardest to read are the ones with no build behind them: a green from a skipped `EXPO_TOKEN` probe now says nothing was queued and no minutes were spent, and a red from a refused queue says the step log is the whole story because nothing reached the dashboard. A summary written only after a successful queue would be missing from exactly those two.

  **The doc makes the same point for the other audience.** The summary is read by whoever opens the run; README-DEPLOY.md is read by whoever is deciding whether the native target is covered by CI. Both had been given the same green to misread.

  4 new cases (11 in the workflow suite), 9589 in the suite. 0 new keys, 0 bundle cost — the workflow ships to nobody. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds — `EXPO_TOKEN` is read from the repository secret, as before.

## The suggestion that would have undone the constant (2026-09-21)

<!-- First bullet of "After the shared walk floor" (2026-09-19): "`npm run remeasure-floors` now prints a number three entries no longer spell — the tool suggests a `minimum` per walk, and for the three on `RUNTIME_CODE_DIRS` the edit it implies is to one constant rather than to three entries. Nothing in its output says so, and the next person to run it will paste 174 back into a row." -->

- [DONE: 2026-09-21] [est: 42000] **The tool's advice pointed at the shape the constant had just removed** — `RUNTIME_CODE_WALK_FLOOR` took 174 out of three entries two days earlier. `remeasure-floors` walks a floor at a time, so today's tree (315 files against 174, 45% deletable) produced the drift note three times, each ending "Re-measure to 236 when convenient" with no hint that the three numbers are one number. A reader following it lands on three `minimum:` fields, and the obvious edit — paste 236 into each — un-shares the constant one row at a time.

  **`SHARED_FLOOR_CONSTANTS` names the value and the walk; membership is derived.** Roots are half the key deliberately: an unrelated floor measured at the same number over a different walk is a coincidence, and telling its reader to edit a constant their entry does not mention is worse than saying nothing. `check-console-swap` — `RUNTIME_CODE_DIRS` plus `scripts/` — is the live example of a near-miss that stays out. Members are read back out of `SCANNED_FLOORS` by `sharedFloorFor()`, the way `floorWalks()` already derives the walk list, so an entry joins by taking the constant and there is no second list to drift.

  **Two places say it, and only where a number is actually suggested.** Each row that prints a suggestion now names the constant, the rows that move with it, and the instruction that keeps the consolidation: edit the constant, because pasting the number here moves one row and un-shares all 3. The run summary counts EDITS beside its count of rows — "check-inline-hex, check-latest-ref and check-reduced-motion take their number from RUNTIME_CODE_WALK_FLOOR in lib/scanned-floor.ts — that is 3 row(s) and one edit." A healthy row says nothing: a constant named beside a row nobody is being asked to edit is the line that gets skipped on the run where it matters.

  **The join helper was written without a length test, which the plural sweep found first.** `joinNames` began as `if (names.length <= 1) return names[0] ?? ""` and `__tests__/plural.test.ts` went red — it sweeps `lib/` for modules deciding something by comparing a count against 1, and the fix an exemption list would have bought is a name in a list. Slicing does the branching instead: an empty head means there was nothing before the last name, and the sweep has nothing to see.

  12 new cases (43 in the floor-walks suite), 9585 in the suite. 0 new keys, 0 bundle cost — the tool ships to nobody. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## One number instead of three, and the two fixtures that noticed (2026-09-19)

<!-- First bullet of "After the runtime-roots constant" (2026-09-19): "Three guards now share a walk and three `SCANNED_FLOORS` notes each explain the same alignment to the reader… The roots are one statement now; the NUMBER is still three." -->

- [DONE: 2026-09-19] [est: 13000] **The roots were consolidated an hour earlier and the number was not** — `check-inline-hex`, `check-reduced-motion` and `check-latest-ref` each carried `minimum: 174`, and each carried a sentence in its `note` explaining to the reader why it matched the other two. Three copies of a number, plus three defences of the copying. `RUNTIME_CODE_WALK_FLOOR` is that argument stated once.

  **What it is NOT, said in the constant's own header.** Not a bound on how much code this tree may hold and not a number anybody should feel pressure from. The property that actually matters — a walk that came back healthy-looking with `app/` missing — is carried by each entry's `roots` and asserted directly, so this number's only remaining job is the walk that came back implausibly small with every root present: a narrowed glob, a broken extension filter, a widened skip list. At 174 against 311 files it is loose, deliberately: tightening it would mean re-taking it every time the tree grows, for a property that does not need a tight number.

  **Two fixtures caught the change, both correctly, and one of them had been caught by this before.** `lint-guard-invalid-floor` and `guard-fixture-refusals` both rewrite `minimum: <number>` to prove a guard refuses an unusable premise — and the right-hand side stopped being a number. The first had already broken once when a floor was re-measured, and had been taught to read the current value out of the table; that is why it broke a second time, because the new spelling is not a value at all. Both match a TOKEN now, which survives either. The failure was loud rather than silent only because one of them asserts that the rewrite changed something: a patch that matches nothing produces a perfectly valid entry and a case that fails claiming the guard would not refuse, which is a puzzling failure rather than a named one.

  **And a suggestion from the same list, checked and dropped.** "`check-console-swap` is `RUNTIME_CODE_DIRS` plus `scripts/` and could say so as a set" cannot be built: `__tests__/helpers/guard-scan-list.ts` parses each guard's `SCANNED_DIRS` as a LITERAL array, deliberately, and its header already records why — a `[...SOURCE_DIRS]` spread would satisfy the one-copy rule while defeating the pin that keeps every guard's scan list readable from its own file. The suggestion was a one-line refactor wearing the clothes of a design this repository has already decided against.

## The third rule to write out the same narrowing (2026-09-19)

<!-- First bullet of "After the latest-ref guard" (2026-09-19): "Two guards arrived today and both narrowed to `app` + `components` + `lib` with the same floor of 174 — `check-inline-hex` makes three… the roots that hold code that runs are a fact about the tree, and three rules have now stated it separately." -->

- [DONE: 2026-09-19] [est: 12000] **Three rules, three copies of one fact** — `check-inline-hex` (a colour literal is a token decision only where something is drawn), `check-reduced-motion` (an animation has to consult the setting only where it can run) and `check-latest-ref` (a per-render ref sync needs a render) all narrow to `app` + `components` + `lib`, and all three wrote the narrowing out. The second and third arrived within hours of each other this morning, which is the only reason it was visible: one copy is a choice, two is a coincidence, three is a fact about the tree that nowhere states.

  **`RUNTIME_CODE_DIRS`, named for what it MEANS.** The same move `MARKUP_DIRS` made one level narrower, and the naming argument is the one that file already records: "the roots the three code rules scan" is a list to re-justify every time one of them changes, and "the roots that hold code that runs" is a fact a rule wanting something else has to argue against.

  **`NON_RUNTIME_REASONS` ships in the same commit, which `MARKUP_DIRS` did not manage.** That constant landed as a narrowing with no statement of what it excluded and needed a follow-up commit to say so; this one is the pair from the start. And `scripts/` is the entry that has to be said carefully — it RUNS, on a developer's machine and in CI, so a rule about "code that runs" catches it on a careless reading. What these three rules are about is code on a USER's device, which is Metro's decision, and Metro never resolves `scripts/`.

  **The guards keep their own literal `SCANNED_DIRS`.** `guard-scan-dirs.test.ts` parses it out of each wrapper precisely so a guard's scope is readable from the guard's own file, and the suite asserts the three lists EQUAL the constant. One statement plus a checked agreement, rather than three lists that happen to match — which is what they were doing until this morning.

  **The claims about the constant live beside the claims about its sibling, not in a second home.** The partition (every source root is runtime or has a sentence), the minimum-length rule on each sentence, the non-empty check on each root, and the nesting — `MARKUP_DIRS` ⊆ `RUNTIME_CODE_DIRS` ⊆ `SOURCE_DIRS` — all went into `source-files-helper.test.ts` next to the markup versions of the same four. The first draft put them in the guard suite, where they would have been a second place to look for one kind of question.

## The rule that was a test case (2026-09-19)

<!-- First bullet of "After the latest-ref consolidation" (2026-09-19): "The no-render-body-assignment rule is a test case and could be a lint guard — it lives in `use-latest-ref.test.ts` and walks `app/` + `components/` + `lib/` exactly as `check-inline-hex` does." -->

- [DONE: 2026-09-19] [est: 14000] **A rule about a shipped-code idiom, enforced only by a suite** — the sweep that found the eleven hand-written ref syncs stayed where it was written, inside the hook's own test file. That is enforcement (it runs in `npm test`, a gate leg) and it is the wrong shape: the twelfth copy would be reported by a suite about `useLatestRef`, under a case name nobody grepping for a lint failure would find, with no annotation on the diff that introduced it. `lint:latest-ref` is the 23rd guard, beside the 22nd, which arrived this morning.

  **The narrowness IS the design, and it took six negative cases to say so.** Ordinary ref usage is almost the same text: `timerRef.current = null` is a reset, `timerRef.current = setTimeout(...)` stores a handle, `mountedRef.current = false` is a lifecycle flag — `app/chat/[id].tsx` alone has six, every one of them correct. So the name must be the SAME identifier either side, with the `Ref` suffix carried by a backreference rather than by convention. And the statement must sit at exactly two spaces: four or more means inside an effect, a handler or a branch, which is where `use-transition-event` writes its own `prevRef` correctly and where a rule that argued would simply be wrong.

  **Indentation is a weak signal and it is the honest one available.** Deciding "is this statement in the component body" properly means parsing the function, and this repository has `check-jsx-walk` precisely because of what it thinks of hand-rolled parsers that are almost right. The two-space rule under-matches — a component nested inside another function is missed — and under-matching is the direction a style rule should fail in.

  **The half it refuses to attempt is the more interesting one.** The other side of `useLatestRef`'s contract is that the ref must not be READ during render, which is a real React violation rather than a preference, and the hook makes that mistake one line shorter to write. Telling a render-body `.current` read from a callback's is a question about scope, not about text, and the shapes that separate them — an arrow in a prop, a function declared and called later, a `useMemo` body — are indistinguishable to a scan. A rule catching nine of ten would be worse than none, because the tenth would read as cleared. A case asserts the module does not grow one quietly.

  **No subject floor, and the absence is reasoned rather than forgotten.** `check-reduced-motion` refuses a run that matched no animation, because its rule is about things that exist. This one's healthy state is zero findings and there is nothing in the tree for it to count, so the equivalent hazard — the pattern silently ceasing to match after a formatter change or a rename — is demonstrated against fixtures in its suite instead. That is the only place a negative rule can show it still recognises its subject, and its floor entry says so.

## Eleven copies of one assignment (2026-09-19)

<!-- "Three refs in one component now exist for one hazard", carried on two suggestion lists: "`activeRef`, `tabsRef`, `onChangeRef`, `translateRef` and `useReducedMotionRef` are all in `components/swipe-tabs.tsx` for the same reason… A `useLatestRef(value)` hook is four lines; the alternative worth pricing first is rebuilding the responder." -->

- [DONE: 2026-09-19] [est: 17000] **`const ref = useRef(x); ref.current = x;` in eight files, for three different reasons** — a `PanResponder` built once inside `useRef(...).current` and never rebuilt (`swipe-tabs`, four of them); a callback whose identity changes every render and must not restart a timer (`toast-host`, `use-transition-event`, `use-visibility-refresh`, `use-entry-currency`, `use-dwell-time`); and render state read by a handler that outlives the render (`app/index.tsx`, `app/collection/[id].tsx`). Three sentences, one mechanism, and each file arguing for it in its own words.

  **The second line is the whole hook, and it is the line that gets forgotten.** Not because it is hard but because it reads as bookkeeping and sits below the declaration it belongs to. The failure when it is missing is the reason this is worth a hook rather than a comment: nothing crashes, the ref simply keeps the FIRST value, and the component is correct until the value changes — which in four of these sites is a language switch, a second toast or a reorder, none of which the first render can see. That is a bug that ships.

  **The one site that was already right is what suggested the other ten.** `useReducedMotionRef` was written two days earlier with the assignment INSIDE it, and its header said why: "rather than by a line each caller has to remember". It is now `useLatestRef(useReducedMotion())` — the signal plus the hook, with nothing of its own left.

  **The sweep found an eleventh site the suggestion's grep had missed.** `lib/use-dwell-time.ts` was below a `head -30`, which is how a hand-run survey misses one. The case that found it stays as the floor: no `xRef.current = x` at the top level of a component body anywhere under `app/`, `components/` or `lib/`, with the name on both sides required to match so an assignment inside an effect or a handler — a different thing — is not caught.

  **Mutable on purpose, which is one deliberate concession.** `use-transition-event` keeps a second ref it writes from inside its own effect; a `readonly` return type here would push that one caller back to a hand-rolled `useRef`, and one holdout is exactly how an idiom stays two idioms.

  **The behaviour of a one-line hook is RUN rather than read.** The load-bearing property is that the ref object is the same one across renders — a hook returning a fresh object every render would satisfy every naive case and none of the actual call sites, because the `PanResponder` holds the object it was handed on render one. So: identity pinned across three renders, a first-render capture asserted to see the third render's value, `undefined` and `null` carried as values rather than treated as absent, and the mutability exercised. Five source-text assertions in other suites moved to the new spelling; one of them had been asserting the assignment line itself, which no longer exists anywhere.

## The native bundles nothing had ever built (2026-09-19)

<!-- "Put a size line on the native bundles", carried on four suggestion lists in a row, most recently: "`metro.config.js` is shared, so iOS and Android got the same 115 KiB back and nothing measured it. The EAS preview build already runs in CI; a line reporting its bundle size would make the native output as visible as the web one." -->

- [DONE: 2026-09-19] [est: 19000] **The job every list pointed at cannot supply the number** — `eas build --platform ios --no-wait` QUEUES a build on Expo's infrastructure and exits, which is what `--no-wait` is for; the workflow then reports success for having queued something. Four lists in a row proposed reading a size off it. So `npm run bundle:native` exports the platform locally and measures what comes out, which is the only way this repository can answer the question at all.

  **And the first run of it failed, for a reason nothing here could have seen.** A SECOND COPY of react-native — 0.86.0 — sits at `node_modules/react-native/node_modules/react-native`, put there by npm to satisfy `@react-native/virtualized-lists`' `react-native: *` peer, which it resolved to the newest published version instead of deduping to the 0.81.5 already at the root. Metro resolves a private component (`VirtualViewExperimentalNativeComponent`) out of the nested copy and the root copy's codegen cannot parse it. Confirmed both directions: move the nested directory aside and the export succeeds at 7643.0 KiB of Hermes bytecode; put it back and it fails again.

  **The web build never resolves that file, which is why every signal in this repository says fine.** `npm run verify` is green, all nine CI legs are green, and the EAS job is green for having queued a remote build whose outcome nothing here reads. Three green lights over a platform that does not build. That is not a gap a guard could have caught, because no check in this tree had ever asked Metro for a native bundle — the failure was not hiding from the checks, it was outside the set of things any of them look at.

  **The diagnosis is the report's real output.** Metro's own message names a component nobody working on this app has heard of and points at a path six directories deep; that is precisely why this went unexamined. `diagnoseNativeExportFailure` requires BOTH halves of the signature — a nested react-native path AND a codegen parse error — and returns nothing for anything else, because a diagnosis offered for every failure is one that will eventually be attached confidently to a failure it is wrong about.

  **A report, not a tenth gate leg, and the distinction is the point.** `npm run verify` chains its nine steps fail-fast, so a leg that cannot pass today would turn every commit red for a dependency-tree problem no commit caused. `bundle:composition` is the standing precedent: gates nothing, run when a question comes up. This one is run when the question is "how big is the native bundle", and today it answers "there isn't one, and here is exactly why".

  **The fix is left to a human on purpose.** `overrides` — both the plain form and the nested `@react-native/virtualized-lists` form — did not dislodge the copy; npm kept the resolution from the existing lock. Forcing it would mean hand-editing `package-lock.json` and shipping a dependency tree whose only real verification is an EAS build this session cannot run. Writing down a reproduction that takes one command is worth more than a lockfile edit nobody can check.

## Four settings nobody had opened, opened (2026-09-19)

<!-- "Audit the rest of Metro's transformer config the same way", carried on the suggestion lists of 2026-09-17 (twice) and 2026-09-19: "`reduce_funcs: false`, `mangle.toplevel: false` and `toplevel: false` are three more that were chosen by somebody else for some other app. Each is worth one build to measure, and the answer may well be that they are all correct." -->

- [DONE: 2026-09-19] [est: 16000] **The answer is that Metro is right about all of them, which is worth knowing once rather than never** — `ascii_only` was one option in a preset with no reader and it was costing 3% of the bundle, which is what made the rest of that preset a fair question. One build each, against a 3,718,641-byte bundle: `compress.reduce_funcs: true` saves 33 bytes, `mangle.toplevel: true` saves nothing, `toplevel: true` saves nothing. Nothing in `metro.config.js` changes.

  **The two zeroes are structural, and that is the whole finding.** Not "nearly nothing" — ZERO, byte for byte across all seven chunks, which is not how a conservative minifier behaves and is not luck. Metro wraps every module in a `__d(function (global, require, …) { … })` factory, so a Metro bundle has no meaningful top level: every name is already function-scoped and already mangled. Whatever those two options are for, this bundle SHAPE cannot reach it, and that is a better answer than a number because it will still be true after the next dependency bump.

  **33 bytes is 0.0009%, and React Native turns that option off on purpose.** `reduce_funcs` inlines single-use functions, which moves work off the wire and onto startup — a cost that lands where nothing in this repository measures. The interesting part is not the risk assessment: it is that the number is small enough that nobody has to do one.

  **A result like this is re-derived every few months unless it is written down.** So it is, twice over: `lib/minifier-audit.ts` holds the table (path, the value Metro sends, the measured delta, why the current value is right) and `metro.config.js` holds the summary, because the next person to ask this question will open the config and not a lib module. The suggestion list asked it three days running, which is what a finding with nowhere to live looks like.

  **And the premise gets a check, which is the part that will still be earning its keep in a year.** A measurement is only as good as the config it was taken against, and a dependency bump can move a default silently: the bundle stays correct, every guard stays green, and those paragraphs become fiction with nothing to say so. That is the same failure mode that put `evaluateBundleCharset` inside `check-bundle-smoke` rather than leaving it as a comment. The suite loads the preset through `createRequire` anchored at `metro.config.js` — the build's resolution, not the suite's — and compares it in BOTH directions: a changed value is named with the date its number is now stale against, and a setting the toolchain ADDS is reported as unpriced. The second direction is the one a table of known options cannot see by itself, and it is exactly how `ascii_only` survived unexamined for as long as it did.

## The tab change nothing said out loud (2026-09-19)

<!-- First bullet of "After the reduce-motion scanner" (2026-09-19), and of the three lists before it: "Say the tab change out loud, now that the pager may not show it" — carried four times, which is itself the finding. -->

- [DONE: 2026-09-19] [est: 18000] **On native there are no tab buttons at all** — `components/swipe-tabs.tsx` renders a `<Text>` and a row of dots for its header, and the swipe is the only way to change tab. So a commit produced no spoken feedback of any kind: no role, no selected state, no sentence. On WEB the same component renders `<Pressable>`s carrying `accessibilityState={{ selected }}`, which the platform announces on press — which is why the gap was invisible to anyone testing in a browser, and why the fix could not go where it first looked like it belonged.

  **The sentence belongs to the GESTURE, not to `commitTo`.** Both routes into a tab change pass through that function, and a version wired into it would say the same thing twice on web and nothing on the native route that has no buttons. `spoken` defaults to FALSE and only the two pan-release call sites opt in, rather than the other way round: a third caller added later is quiet until somebody decides it should not be. And it fires AFTER `onChangeRef.current(targetKey)` inside the completion callback, because the `!finished` branch immediately above returns without changing anything — a sentence spoken for a gesture the pager then abandoned is a lie the listener has no way to check.

  **Not conditional on reduced motion, though that is what raised it.** The suggestion framed this as a reduced-motion gap: under the setting the commit is instantaneous, so the only sign a swipe did anything is the content changing. True, and the narrower half. A screen-reader user perceives the 220ms slide exactly as well as they perceive its absence, which is not at all, so the sentence is owed on every swipe commit — a version firing only under the setting would be a feature for the smaller group.

  **`announcedPosition` gets a second caller rather than a second derivation.** "Position N of M, or nothing" is one rule, already written for the reorder announcements with six cases behind it, and re-deriving it here would have been a second set of edge cases to get right. Both halves of the sentence are required: a blank label says ", tab 2 of 3" — a position attached to no subject — and a position out of range says a number that is simply wrong. A `findIndex` miss resolves to `-1`, which is exactly that case, and it is refused rather than read aloud.

  **The translator is read through a ref, like everything else the PanResponder touches.** The responder is built inside `useRef(PanResponder.create(...)).current`, which runs once, so a `translate` captured in that closure is the one from the first render for the life of the component — the announcement would keep speaking the language the app was STARTED in after the user changed it. That is the same hazard `useReducedMotionRef` was written for two days earlier, in the same file, and the third ref in a row this component has needed for it.

  **Two modules, because `@/lib/announce` is a platform pair.** It pulls react-native (the announcement API is an empty function on web, so the web half builds an ARIA live region), and anything importing it is unreachable from a node suite. So `lib/tab-sentence.ts` holds the DECISION and `lib/tab-announcement.ts` holds the saying of it — the same split `lib/drag-reorder.ts` and `lib/reorder-announcement.ts` already have, for the same reason: the interesting cases here are the ones that produce nothing, and a function whose only effect is a side effect can be tested only by watching for the absence of one. The case that pins the split asserts on the IMPORTS rather than the text, because the module's own header explains the split by naming the module it must not pull.

## The four filenames that were holding the line (2026-09-19)

<!-- Last bullet of "After banking the saving" (2026-09-17): "Promote the animated-surface list into a real scanner" — the reduce-motion sweep's last case names four files by hand, so a fifth animated surface joins unchecked. -->

- [DONE: 2026-09-19] [est: 20000] **A sweep's floor was a list of four filenames somebody typed** — the toast, the skeleton, the pager and the wishlist sheet were taught to consult `AccessibilityInfo.isReduceMotionEnabled` two days earlier, and what stopped a fifth surface from joining without asking was an array in a test: green on the day it was written and silent about every file added after it. The setting is not a preference about taste — vestibular disorders are why it exists — so "nobody remembered" is the failure mode worth spending the 22nd guard on. `check-reduced-motion` walks `app/` + `components/` + `lib/` and refuses any `Animated.timing/spring/decay/loop` without `@/lib/reduced-motion` in scope.

  **The rule is the IMPORT, and that is a decision rather than a shortcut.** There are three right answers and the module's header is about which is which: a `motionDuration` of zero for a transition whose completion callback commits state, a `setValue` for a spring with nothing waiting on it, an early return for a loop. A text scan cannot tell which one a surface needed, and a guard that guessed would be wrong on two thirds of this tree. What it CAN say is that the author was handed the question — which is the claim that catches the surface written by somebody who has never opened the header.

  **Three narrowings, each with a case behind it.** Comments are blanked first, and this rule is the reason that matters: its own header names three of the four drivers, `lib/reduced-motion.ts` names two, and `components/skeleton.tsx` quotes `Animated.loop` to explain its early return — a rule that read prose would report every file that documents itself. The combinators are OUT (`sequence`, `parallel`, `stagger` only arrange drivers, so a file holding one holds a driver too) and `loop` is IN despite being one, because it has its own answer. And `lib/` is in the walk rather than `MARKUP_DIRS`, because twelve context providers live there and render JSX — "animation lives where the markup does" is a sentence this tree has already falsified once.

  **A second premise, beside the count floor.** `assertScannedWalk` proves files were read; nothing proved the rule still RECOGNISES an animation. Every pattern here is a spelling of one library's API, and the day this app moves to Reanimated's `withTiming` all of them stop matching at once: a clean report over a tree full of unguarded motion, which is exactly the failure every floor in this repository exists to catch. One animated file is the floor — the number of animated surfaces is a product decision and the guard is not the file that should have an opinion about it.

  **The repository's own meta-guards named the four registries the guard had to join, in one run.** `guard-registration.test.ts` reported all three gaps at once — a partial-root fixture, a scan-dirs exclusion table, a planted offender — rather than one per run, which is the shape it was built for and the first time it has been exercised by a guard arriving after it. The planted offender is the one entry in that table written LITERALLY rather than assembled: `__tests__/` is the source root this rule deliberately does not walk, and a fixture has to be able to spell a driver out.

  **And the bug that fell out of deriving the list.** `components/skeleton.tsx` returns early under the setting and its cleanup is `loop.stop()`, which freezes the animated value wherever it had got to. Turning reduce-motion on MID-SWEEP is not the exotic case — it is the ordinary one, since it is turned on BECAUSE something animated — and it parked the highlight band across the middle of the box, permanently, on the surface that was supposed to go quiet. The comment above it promised "the box keeps its base colour and sits still"; `anim.setValue(0)` is what makes that sentence true, and it is the `setValue` shape rather than a timing back to zero because nothing is waiting on it.

## The saving, banked (2026-09-17)

<!-- First two bullets of "After the UTF-8 bundle" (2026-09-17): "Re-take the composition baseline" and "Spend some of the 127.9 KiB deliberately, or lower the budget to keep the pressure". -->

- [DONE: 2026-09-17] [est: 14000] **The baseline had written the diagnosis down and left it as a fact about the world** — "`lib/i18n/ru.ts` is the heaviest module a page load still fetches, at 92 KiB, because Metro escapes non-ASCII as `\uXXXX` and Cyrillic costs six bytes a letter" was in `lib/composition-snapshot.ts` since 2026-09-13, as a description of the bundle rather than of a setting somebody had chosen. Re-taking it after the fix: `lib/` 554,338 → 442,000 bytes and `data/` 8,725 → 5,603, the two buckets that hold Cyrillic, with no code removed.

  **The mirror image of the round before it.** That one changed which CHUNK every Sentry bucket was in and moved the totals by 1.1 KiB; this one changes no bucket's membership at all and takes 113 KiB out of two rows. A report watching only totals would have called the first a no-op; one watching only rows would have nothing to say about the second beyond "the copy got smaller". That is why it prints both, and why `(lazy)` is a marker rather than a bucket.

  **The budget comes down in the same commit, because the repository's own rule says so.** 3.67 → 3.57 MiB, 25.5 KiB of headroom — still under `SMALLEST_GUARDED_SDK_BYTES`, so a statically-imported SDK trips on its own commit. Left at 3.67 the gate would have had 128 KiB of slack, which is not a loose guard but no guard at all: four rounds of ordinary work would have spent it without anybody deciding anything. `BUDGET_HISTORY` gains the second row that goes DOWN, and both were found by the composition report — which is what the count now carries: the first was not a fluke, it is what that report is FOR.

  **The measurement records that had to move with it, and the one case that made me find them.** `LAST_MEASURED_LAZY_BYTES`, `SMALLEST_MEASURED_LAZY_CHUNK_BYTES`, the `TODAY` fixture and three report strings were all taken from the previous export. The pairing case in `bundle-composition.test.ts` — "is measured against the same bundle the budget is", within 20 KiB — is what refused the re-taken baseline until the budget snapshot followed it. A baseline that could be updated alone is a baseline that drifts from the number it is supposed to be comparable with.

## 115 KiB of backslash-u (2026-09-17)

<!-- "Watch the bundle budget", said on four suggestion lists in a row, most recently: "This is the fourth list to say so, which is itself the finding: the next feature of any size crosses it. Either move the budget with a sentence saying what grew, or take out the largest avoidable weight." -->

- [DONE: 2026-09-17] [est: 22000] **The headroom was 12.5 KiB and the answer was not in any feature** — the budget had been flagged four days running and the two honest options were always "raise it with a sentence" or "spend a diff getting under it". The composition report said where to look: `lib/i18n/be.ts` shipped as 94 KiB and `ru.ts` as 97, against 28–33 KiB for English, Polish, German and Spanish — the same 554 keys, three times the bytes. That is not a translation being longer. It is `output.ascii_only: true` in Metro's terser preset, which rewrites every character above U+007F as a six-byte escape: a Cyrillic letter costs two bytes in UTF-8 and six as an escape, and there were 13,386 of them in the Belarusian chunk alone.

  **One line of `metro.config.js`, and 115 KiB.** Headroom 12.5 KiB → 127.9 KiB; the EAGER entry chunk alone dropped 56 KiB, because the default Russian locale lives in it. Safe because every consumer declares UTF-8 — `dist/index.html` carries `<meta charset="utf-8">` and GitHub Pages serves `.js` with `charset=utf-8` — and a script tag with no charset on the response inherits the document's anyway. Metro's other three output options are SPREAD rather than replaced: `quote_style` and `wrap_iife` are its own and the rest of the toolchain expects them, and assigning a fresh `output` object is exactly how this edit goes wrong.

  **A setting nothing reads is a setting that comes back.** A dependency bump or a config rewrite restores it silently: the bundle would still be correct, still pass every other check, and be 115 KiB heavier — the budget would absorb it and the headroom would be spent without anybody deciding to spend it. So `evaluateBundleCharset` joins the token sweep and the privacy pages inside `check-bundle-smoke`, over chunks it has already read. No tenth gate step, which the gate's own suite would have had something to say about.

  **The rule is a comparison, not a count, and both halves are load-bearing.** "No escapes at all" is wrong — a source file may legitimately escape a zero-width or control character, and the bundle carries 50 such today. "Some literals" alone is too weak: a stray `©` would satisfy it in a bundle escaping every translation. So it is a floor AND `literals > escapes`, and the second is what carries the meaning: with `ascii_only` on, literals go to roughly zero while escapes go to tens of thousands, which is the reverse of what this asserts and cannot be reached by accident. The floor is argued rather than measured — 1000 against ~30,000 today — so deleting a language does not fail it.

## Four animated surfaces, none of which had ever asked (2026-09-17)

<!-- "Give the toast stack a reduced-motion path", carried on the suggestions list since 2026-09-10 and restated three times since: "Every other animated surface in the app has the same gap, which makes it a sweep rather than a toast fix." -->

- [DONE: 2026-09-17] [est: 20000] **Nothing in this tree had ever read `AccessibilityInfo.isReduceMotionEnabled`** — the toast entrance slides 24px over 220ms, the skeleton shimmer loops forever, the swipe pager translates a full screen width and the wishlist sheet springs. All four ran unconditionally, on every platform, for every user. The setting is not a preference about taste: vestibular disorders are why it exists, and for some people a sliding panel or a looping shimmer means nausea, dizziness or a migraine. iOS, Android and every desktop browser expose the answer and React Native forwards it.

  **`duration: reduced ? 0 : ms` is the naive fix, and it is wrong in two of this app's three shapes.** A transition whose completion callback commits a state change — the pager's tab change, the sheet's dismissal — must still RUN, so zero is right there and skipping it would strand the user mid-gesture. A spring back to rest has nothing waiting on it, so the instant equivalent is `value.setValue(rest)`: a zero-duration spring is still a spring. And a LOOP must not start at all, because `Animated.loop` of a 0ms timing is not a still image, it is a frame callback that never stops — a battery drain handed to the person who asked for less. So `lib/reduced-motion.ts` exports the SIGNAL and the arithmetic, and each surface picks; one "make it instant" helper would have had to know which of the three it was called on.

  **The subscription is the half that matters most.** A value read once at mount would leave every already-mounted surface animating until the screen was navigated away from and back — and the user turning this on mid-session is turning it on BECAUSE something in this app made them ill, which is the moment it most needs to be heard. It defaults to ALLOWING motion when the platform cannot answer: an app that read a missing API as "no motion" would silently drop every animation on a platform whose only fault is not implementing the query.

  **`useReducedMotionRef` exists because two of the four cannot read state.** Both pan responders are built inside `useRef(PanResponder.create(...)).current`, which runs once: a `reduced` captured in that closure is the first render's value for the life of the component. The ref is updated by the hook rather than by a line each caller has to remember.

  **Fifteen cases, and one of them is the floor under the other five.** The arithmetic is called; the hook is mounted and driven through a mocked `AccessibilityInfo` — first answer, a change in each direction, an unmount that must remove its listener, and a platform whose promise REJECTS, which would otherwise be a crash on mount of every animated surface. The last case names all four files and fails if any of them animates without importing the module, so a fifth surface cannot join quietly.

## The tag list nobody had ever checked (2026-09-17)

<!-- First bullet of "After the tag-colour rebalance" (2026-09-17): "Rebalance the tags arriving from storage and from a peer, not only the ones deleted here." -->

- [DONE: 2026-09-17] [est: 18000] **Two functions enforce the tag rules and neither is on the way IN** — `addTagToList` refuses an empty label and a case-folded duplicate, and `removeTagAt` rebalances the hues. Both sit on the path through the forms, which is not how most tag lists in this app arrive. A row pulled from Supabase was written by another device, possibly a build that predates the label hash entirely; a blob read back from AsyncStorage was written by whatever this app was months ago. Two "sealed" tags in different casings, a label that is one space, a set of hues derived by the old rotation and meaningless on every other item: all of it reached the screen unchecked.

  **`normalizeTagList` is the form's rules, not new ones.** No empty label; the FIRST of a case-folded duplicate wins, matching the add rule because the casing that arrived first is the one the user typed; colours re-derived so a label means the same hue here as on an item created today. It is idempotent and identity-preserving, which is the property that lets a product rule sit on a read path at all.

  **Where it runs, and where it deliberately does not.** `mergeItemsFromCloud` — the seam a peer's edit crosses — and `normalizeItemTags` over the hydrated list, beside the three passes already there (`normalizeOwnItemIds`, `dedupeItems`, `applyTombstones`), written the same way for the same reason. NOT in `coerceItemRow`: that is a defensive validator, it says only that the shape is a `{label, color}[]` — which is true of every list this rejects — and a product rule inside it would rewrite hues on a path with no comparison to decide whether anything actually changed. The first attempt put it there and the validator's own suite said so.

  **A colour is a function of the label now, so it stopped being data.** A delta row that differs only in a derived hue says nothing the local row did not, and the merge's no-op contract — return `local` by reference, or pay a re-render and two storage writes on every pull — now holds for it. One case says that directly; two existing fixtures carried a hue this app no longer writes and were asserting the contract against a row that legitimately changed.

  **A suggestion that turned out to rest on a false premise, recorded rather than built.** "Reuse the label hash for the tag FILTER chips" has been carried since 2026-09-10; there is no tag filter. `lib/item-filters.ts` has query, quick chips and sort, and no mention of tags. The chips it describes do not exist, so the suggestion is a feature request for a tag filter wearing a one-line refactor's clothes.

## The freed hue that stayed free (2026-09-17)

<!-- First bullet of "After the label-derived tag colour" (2026-09-10): "Rebalance an item's hues when a tag is deleted, not only when one is added." -->

- [DONE: 2026-09-17] [est: 15000] **A borrowed tag colour was borrowed for good** — `tagColorForLabel` gives a label its own hue unless that hue is already on the item, in which case the rotation picks a free slot. It reads the visible set at ADD time and never looks at it again, so the set decays the moment anything is removed. "sealed" and "holo" hash to the same slot: add both and "holo" takes a rotation colour, delete "sealed" and the slot is free while "holo" keeps the borrowed one — for good, on every card that tag appears on. That is precisely the label-consistency the hash was introduced to provide, lost silently on a screen where nothing looks wrong.

  **`rebalanceTagColors` re-applies the rule to the whole list, left to right.** The direction is the decision: the order is the one the user sees, and an earlier tag changing colour because a later one was deleted is the more surprising of the two — the tag they did not touch is the tag that should not move. The list comes back BY IDENTITY when no colour changed and each unchanged tag keeps its own object, because the callers write it straight into state and a fresh array on every delete would re-render the row for nothing.

  **`removeTagAt` carries it, so no screen has to remember.** Both forms wrote `tags.filter((_, j) => j !== i)` inline — the same duplication `addTagToList` was extracted to end, one operation down. Removal is exactly where the colours go stale, so the rebalance rides in the remove rather than being a second call; a delete that left the hues alone is the bug above, and it would be invisible on the screen that did it. By index and not by label, so a list carrying two casings from storage or a peer's edit loses one row rather than two.

  **The colliding pair in the suite was found by running the hash, and a case fails first if they ever stop colliding.** On labels that never collide, a rebalance and a plain filter produce identical output — every case about a freed hue would pass on the bug, vacuously. Fourteen cases; 9400 in the tree, all green.

## The departure the browser does send (2026-09-17)

<!-- First bullet of "After the bounded toast" (2026-09-17): "Release the hold when the app loses focus, not only when the pointer leaves." -->

- [DONE: 2026-09-17] [est: 15000] **A ceiling is a backstop, and this one was about to become the ordinary path** — the toast holds its dismissal timer on `onHoverIn`, and a browser does not promise the `onHoverOut` that releases it. Alt-tab away mid-hover, switch tabs, drop a drag over the overlay: no pointer event is ever sent to that element again. `toastHoldCeilingMs` bounds the consequence at four windows, which is the difference between a nuisance and a permanent banner — but a tab switched away from mid-hover is ordinary, and every one of them would have spent the full ceiling.

  **`useAppAway` is the signal the browser DOES send in that case.** `lib/use-app-away.ts` listens for two web events, because they are two different absences: `visibilitychange` for a hidden tab, and `blur` for a window that lost focus while its tab stayed visible — the alt-tab-to-another-app case, which `document.hidden` reports as `false` and which a hook listening only for the first would miss entirely. On native it is `AppState`, and every state that is not `"active"` counts: iOS's `"inactive"` is a notification shade or an incoming call, which is as much "the user is not looking" as a full background.

  **Away only, deliberately.** There is no "back" callback. Every caller wants to CANCEL something a departure invalidates, and a return that re-armed it would be guessing that the user is once again doing whatever they were doing before — which, for a hover, they are not. `visibilitychange` fires both ways and carries no direction, so the handler reads the flag; a case asserts the return trip reports nothing.

  **`fake-dom.ts` grew a second registry, and that is the part that could have been faked wrong.** `window` now has its own `addEventListener`/`removeEventListener` and a `fireWindow`, rather than aliasing the document's. A merged registry would let a hook listening on the wrong object pass every case — including the leak case, which is the one no assertion about callbacks can see: ten cases run, and two of them say only that neither listener is still registered after the tree is down.

## A toast that nothing can dismiss, bounded at both ends (2026-09-17)

<!-- Two suggestions carried since 2026-09-10, both listed twice: "Cap how long a held toast can live" and "Extract the hold decision so it can be tested by being called". -->

- [DONE: 2026-09-17] [est: 16000] **The hold had no ceiling and the queue had no depth** — `<ToastView>`'s effect opened `if (held) return;`, so a held toast scheduled NO timer at all. That is correct while a pointer really is resting on the toast and wrong the moment the `onHoverOut` never arrives: a drag that ends over the overlay, a window that loses focus mid-hover, a phone reporting a stuck hover. `held` stays true with nobody there and a `zIndex: 9999` banner sits over the top of every screen for the rest of the session. The queue had the matching hole — `show()` appended, and the only thing that ever removed an entry was that entry's own dismissal timer, which a held toast does not have.

  **The fix is two pure functions, which is the other half of the task.** `nextToastDeadline({ held, hasAction, elapsedMs })` in `lib/toast-timing.ts` and `capToastStack` in `lib/toast-stack.ts`. Both rules used to be lines inside a React module that `tsx --test` cannot import, so the four cases covering them matched the effect's SOURCE — a `return`, a dep array, the name of a ref — which is green on a cleanup that never runs and on a ceiling off by a factor of a thousand. Fourteen cases call them now, and the two render cases that asserted `timers.live()` was EMPTY on hover assert the ceiling instead, which is the behaviour that changed.

  **Held life is capped at four windows from when the toast appeared; a release still restarts the full window.** So the worst case is ceiling + window rather than the ceiling exactly, and that is the trade on purpose: the failure being fixed is UNBOUNDED life, and a release is a real signal from a real pointer — the one thing a stuck hover never sends. `elapsedMs` comes from a `shownAt` ref captured once, not recomputed per re-run, or every hover would reset the ceiling and the bug would survive its own fix.

  **The stack keeps three and drops the OLDEST.** The direction is the whole decision: the newest toast describes what just happened and is the only one whose action the user is still reaching for. A list that already fits is returned by identity rather than copied — a fresh array on every `show()` would re-render the stack, restart the entrance animations, and reset the very timers the cap exists to bound.

## captureTimers moves to helpers/, and the debounce gets run (2026-09-14)

<!-- First bullet of "Fired-handler follow-ups" (2026-09-14): "`captureTimers` is in one suite and every provider suite wants it — all of them either await real time or assert on source. It is twenty lines beside `helpers/render.ts`, and the `finally`-restore discipline is the part a copy would leave out." -->

- [DONE: 2026-09-14] [est: 14000] **The twenty lines that answer "what did this schedule?" were private to the suite that happened to want them first** — `captureTimers` was written inside `toast-host-render.test.ts` yesterday, and everything else in this tree that reasons about a timer does one of the two worse things: awaits real time (a margin that passes on a fast machine and flakes on a loaded one) or matches `setTimeout` in the source (an assertion about spelling, green on a callback that is wrong and on a cleanup that never runs).

  **`withCapturedTimers` is the form that ships, not `captureTimers`.** The stubs go on `globalThis`, so a case that threw halfway and left them installed would break whatever file `tsx --test` ran NEXT — with a message about that file's timers, not about this one. The `finally` is inside the helper now instead of in each of the six bodies that remembered to write one; `captureTimers` stays exported for a capture that has to outlive one body, and says so.

  **A fired timer stops being live.** The original kept a `cleared` flag that only `clearTimeout` set, so an entry that had already run still reported itself as armed — which makes "and nothing is left over" pass on a component that re-armed and fail on one that did not. `run()` retires its own entry; `scheduled` keeps everything, because "one timer per keystroke was armed" and "one is still standing" are different questions and the burst case asks both.

  **The second caller is the debounce, whose suite said the harness did not exist.** `use-debounced-value.test.ts` opens by explaining that the hook can only be pinned structurally because "the repo has no React mounting harness" — true when written, false since `helpers/render.ts` landed, and the half it was standing in for is the entire point of a debounce. Eight cases mount it: a burst of five keystrokes leaves ONE timer standing and not five, the previous one is CLEARED rather than superseded, unmount clears the pending settle (a claim the hook's own doc comment has made since it was written and nothing had ever checked), a zero delay schedules nothing at all rather than a `setTimeout(fn, 0)` that settles a tick late, a NaN or a negative reaches `setTimeout` already folded to the default, and a changed delay re-arms at the new one.

  **Proved by mutation.** Deleting `return () => clearTimeout(timer)` from the hook turns four of the eight red. The helper's own suite asserts the part no caller can: that the globals come back when the body returns, when it throws, and when an await inside it rejects.

  18 new cases (9360 in the suite). 0 new keys, 0 bundle cost — nothing here ships. `npm run verify` green: typecheck, 21/21 guards, 9360 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The hold window, run rather than read (2026-09-14)

<!-- First bullet of "Rendered-toast follow-ups" (2026-09-14): "The hold/release window is the toast's most interesting behaviour and it is still only read, not run — the harness fires `onPress`; nothing fires the other four handlers." -->

- [DONE: 2026-09-14] [est: 12000] **`onHoverIn`, `onHoverOut`, `onFocus` and `onBlur` hold a toast's dismissal open, and three source-text matches were standing in for all four** — the feature stops an undo expiring under the cursor reaching for it, which is the whole reason the timer lives in the view rather than in the queue. The harness could fire `onPress` and nothing else.

  **`fire(node, handler, ...args)` is the generic form**, added because the first component moved into this harness on purpose turned out to hang its most interesting behaviour on events `press` cannot reach.

  **A captured `setTimeout`, not a fake clock.** Nothing here needs time to pass — only to see WHAT was scheduled and whether it was cleared — so the cases read the delay and the cleared flag: the plain window for a reporting toast, the longer one for a toast with an action, none standing after a hover, and the FULL window again on leave rather than the remainder. That last one is the deliberate choice the component's comment argues for and nothing had ever checked.

  **Focus gets the same window as hover**, asserted on the action button, which is `focusable` and a tab stop on the web — a toast that expired while its button was focused is the same bug with a keyboard.

  **And the other end of the timer**: running the callback dismisses the right toast by id, which no source match can say.

  **Proved by mutation.** Deleting `if (held) return;` from the component turns exactly two of these red; restored, all seventeen pass. Every case restores the real globals in a `finally`, because a suite that left a stubbed timer installed would break every later file in the same process.

  6 new cases and a harness method (9342 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9342 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The toast overlay, rendered (2026-09-14)

<!-- First bullet of "Toast-move follow-ups" (2026-09-14): "The toast host is scanned by four rules and mounted by no test — every case about it reads its SOURCE, because `lib/toast-context.tsx` pulled react-native and could not be transformed under `tsx --test`." -->

- [DONE: 2026-09-14] [est: 13000] **The reason every case about this markup was a text scan stopped being true when the markup moved** — `lib/toast-context.tsx` pulled the provider, the queue and the announcement seam, so nothing could import it under `tsx --test`. `components/toast-host.tsx` is presentational and takes its list as a prop, which is exactly the shape `helpers/render.ts` mounts.

  **An empty queue draws NOTHING, which a source scan cannot say.** The host is `position: absolute` with a `zIndex` of 9999, so a stray wrapper over an empty list would sit on top of every screen swallowing presses at the top of it. `root.children` is empty; a regex looking for `if (toasts.length === 0) return null` would pass on a component that returned an empty `<View>` from somewhere else.

  **Dismissal is by ID, and a case proves it with two toasts.** A host that closed "the first" would close the wrong one every time the second was tapped, and every source scan in this repository would have been happy with it.

  **The action runs BEFORE the dismissal**, asserted by the ORDER two handlers pushed into a list rather than by the order two statements appear in the file — which is the difference between reading the code and running it.

  **The palettes are read off the rendered nodes**, with a floor under them: the success and error tokens have to be different values, because a lookup that had collapsed to one entry satisfies either colour case written on its own.

  **The timing cases stay where they are.** `toastDisplayMs`, the hold/release pair and the ref the timeout reads are about how the component is WRITTEN, and a harness with no real timers has nothing to say about them.

  11 new cases (9336 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9336 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The toast overlay moves under the rules (2026-09-14)

<!-- First bullet of "Toast-gap follow-ups" (2026-09-14): "The toast host is unscanned by four rules and it is the surface every error in this app lands on — moving it to `components/toast-host.tsx` would put it inside all four and is a one-file move with one import to follow." -->

- [DONE: 2026-09-14] [est: 18000] **`components/toast-host.tsx` — the markup every error in this app lands on, finally inside the rules that have opinions about it** — two `<View>`s, two `<Pressable>`s and three `<Text>`s lived in `lib/`, where the a11y, clarity, empty-state and heading sweeps do not look, because `lib/` is where modules return values. That was true of twelve context providers and false of this one. `check-a11y-jsx` scans 72 screen files now instead of 71, and reports the host clean — which is a fact rather than the hope it was yesterday.

  **The split is queue-here, drawing-there.** `ToastProvider`, the api and the announcement stayed: a toast is announced when it is QUEUED, whether or not the host ever draws it. The dismissal window went with the view, because a toast the user is reading has to hold its own timer and a countdown owned by the queue cannot be paused by the toast it is counting down.

  **`ToastItem` is a type-only export**, so the two files do not form a runtime cycle — the host imports the shape, the context imports the component.

  **The rule that found this got its subject right on the second try.** "Any capitalised element outside the markup roots" flags a file for rendering `<ToastHost />`, a component whose own file is scanned and which no rule has anything to say about as a tag. What the five rules are about is PRIMITIVES, so an element now counts only if the file imported it from a library — derived from the imports rather than from a list of names, so a library this repo has not used yet is covered the day it arrives.

  **`MARKUP_OUTSIDE_MARKUP_DIRS` is empty**, and the list is kept rather than deleted: "the list is empty" is a stronger statement than "there is no list", and the next such file will want somewhere to be named.

  8 cases move with the markup, 2 new (9325 in the suite). 0 new keys, +0.2 KiB for the module boundary — 15.4 KiB of headroom left. `npm run verify` green: typecheck, 21/21 guards, 9325 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The lib/ sentence was false, and one file proves it (2026-09-14)

<!-- First bullet of "Single-mechanism follow-ups" (2026-09-14): "`NON_MARKUP_REASONS` claims `lib/` renders nothing and names two exceptions inside its own sentence — a rule that no `lib/` module declares an element would make the sentence a fact; nothing does." -->

- [DONE: 2026-09-14] [est: 11000] **"Modules that return values" was written yesterday about a directory where TWELVE files render JSX** — the suggestion asked for the claim to be checked and the check failed it on the first run. Eleven are context providers whose whole markup is `<Ctx.Provider>{children}</Ctx.Provider>`, which is why nobody had noticed and which no accessibility or design rule has an opinion about. The twelfth is a real gap.

  **`lib/toast-context.tsx` draws the toast host** — two `<View>`s, two `<Pressable>`s and three `<Text>`s that a screen reader meets on every error this app reports — and the a11y, clarity, empty-state and heading rules all stop at `app/` and `components/`. It is a NAMED entry in `MARKUP_OUTSIDE_MARKUP_DIRS` now rather than a clause inside a sentence, so a twelfth file that renders a `<View>` joins the list with a reason or moves into `components/`.

  **The exception list is held honest in both directions.** An entry that has stopped rendering anything is dropped, because an exemption nobody needs is one that will excuse the next thing to land in it.

  **"Eleven providers" is a number, not a memory** — a case counts them, so the sentence has to be updated rather than quietly drifting. And a case pins that a tag in a string or a doc comment does not count, which is what every lint module in `lib/` is full of.

  **Deliberately a text rule rather than `walkJsx`.** The question is whether a file declares any element at all, and a walk that gave up on the first unreadable tag would answer "no" for the wrong reason — the exact failure mode this week's other rounds were about.

  5 new cases (9323 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9323 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## Two mechanisms for one fact, down to one (2026-09-14)

<!-- First bullet of "Partition follow-ups" (2026-09-14): "`unionOfScanDirs` reads two guards' sources to derive an answer a case now pins to a constant — two mechanisms for one fact, and the parse is the one that can fail for a formatting reason." -->

- [DONE: 2026-09-14] [est: 6000] **The reach floor derived its scope by parsing two guards' `SCANNED_DIRS`, for a fact `guard-scan-dirs` already pins** — that suite asserts both markup guards EQUAL `MARKUP_DIRS`, so the parse was a second route to the same answer and the only one of the two that can go red because somebody reformatted an array. It takes the constant.

  **Three spellings of one scope in three commits**, which is the shape worth recording: hand-typed, then derived from the guards, then the constant the guards are themselves held to. Each step was an improvement on the last and only the third has one statement.

  **`unionOfScanDirs` is deleted**, having had exactly one caller for one commit. The `declaredScanDirs` parse stays in the helper — `guard-scan-dirs.test.ts` is its only reader again, but the file's other export has readers on both sides of the split, so moving it back would just trade one import for another.

  0 new cases (9318 in the suite, unchanged — this removes a path to an answer, not an answer). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9318 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The narrowing says what it leaves out (2026-09-14)

<!-- First bullet of "Markup-dirs follow-ups" (2026-09-14): "`MARKUP_DIRS` is a SUBSET of `SOURCE_DIRS` and nothing says so — it slipped in beside a constant that has a partition check, with neither." -->

- [DONE: 2026-09-14] [est: 7000] **`MARKUP_DIRS` landed as "app and components" with no statement of what it excludes** — which is exactly the shape `NON_APP_TS_DIRS` exists to prevent one level up, written by the round that had just finished praising that shape. `NON_MARKUP_REASONS` is the other half: `data/` holds seed literals with no elements declared in them, `lib/` returns values (and the two modules that touch JSX read it as TEXT), `scripts/` never reaches Metro.

  **A sixth source root can no longer join the tree and be silently outside every JSX rule.** The partition is asserted — markup roots plus excused roots equals `SOURCE_DIRS`, with neither list claiming a directory twice — which is the same check `assertSourceDirsCoverTheTree` makes about the level above it.

  **These do NOT replace the per-guard exclusion sentences, and the doc says why.** "No unnamed control to find" and "no lib module renders a TextInput" are what a particular rule would have been looking for, which is the more useful sentence and the one a reader checks when a directory changes character. Collapsing five of them into three would lose it.

  **A stale entry is caught too**: every markup root has to actually hold `.tsx`, the same honesty check `SOURCE_DIRS` carries, because a list naming a directory that renders nothing scans nothing while looking maintained.

  3 new cases (9318 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9318 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## MARKUP_DIRS — the roots that hold markup, said once (2026-09-14)

<!-- First bullet of "One-constant follow-ups" (2026-09-14): "`JSX_SUITE_SCAN_DIRS` and the guards' `SCANNED_DIRS` are the same two strings in two places — the same split `lib/source-dirs.ts` was created to resolve for the walk itself. A third entry there, named for what it is, is the version with one statement instead of two that agree." -->

- [DONE: 2026-09-14] [est: 7000] **Five rules narrow to `app/` + `components/` and each one wrote the narrowing out** — `check-a11y-jsx` and `check-clarity-input-mask` from `scripts/`, `check-empty-state-wrappers` and the two heading sweeps from `__tests__/`, and then a sixth copy when the reach floor arrived yesterday afternoon to measure how much of that markup the shared walk reads. `lib/source-dirs.ts` is where this repository already resolved that split for the walk itself; `MARKUP_DIRS` is the same answer for the subset.

  **Named for what it MEANS, not for who uses it.** "The roots the JSX rules scan" is a list that has to be re-justified every time one of them changes; "the roots that hold markup" is a fact about the tree — `data/` holds seed literals that declare no elements, `lib/` returns values rather than JSX — and a rule that wants something else now has to say why.

  **The two script guards keep their own literal**, because `guard-scan-dirs.test.ts` parses `SCANNED_DIRS` out of each wrapper so that a guard's scan list stays readable from its own file. The agreement is asserted instead: both must EQUAL `MARKUP_DIRS`, with a message that names the two ways out (widen the constant, or state why this rule differs). Until this commit they matched by coincidence.

  **`JSX_SUITE_SCAN_DIRS` is an alias rather than a bare re-export**, and the doc says why: the day a suite-side rule legitimately reads a root with no markup in it, that is where it stops being an alias and the diff says so.

  1 new case (9315 in the suite), one two-element list deleted. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9315 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## An assertion about values, not about spelling (2026-09-14)

<!-- First bullet of "Derived-scope follow-ups" (2026-09-14): "The three suite-side roots are pinned by a STRING MATCH on the call site — `readSuite(...).includes('tsxFiles(\"app\", \"components\")')` is an assertion about spelling: a reformat, or the same list built from a constant, turns it red while nothing changed." -->

- [DONE: 2026-09-14] [est: 7000] **The pin added one commit earlier could fail for a reason nobody cares about, and pass for one they would** — matching the literal text `tsxFiles("app", "components")` in three suites goes red on a reformat and green the moment somebody builds the same list from a constant. It was the cheapest thing that caught a widening, and cheap in exactly the way this repository keeps writing down as a mistake.

  **`JSX_SUITE_SCAN_DIRS` is the list, and the three suites read it.** `empty-state-wrapper-audit` and the two heading sweeps had three copies of one answer at three call sites; widening a rule is now an edit to one constant, which is what lets the reach floor follow it instead of watching for it.

  **The floor compares VALUES**: the suite-side roots minus the roots it covers, asserted empty, with a message saying what an uncovered root means — a directory whose markup is watched by nothing.

  **And a floor under that**, because a constant compared against itself proves nothing if the suites have stopped reading it: each of the three has to still spread it. That one IS a source match, and it is the right place for one — it is about whether a file shares a list, which is a fact about its text.

  1 new case, three copies of a list removed (9314 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9314 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The reach floor takes its scope from the rules (2026-09-14)

<!-- First bullet of "Reach-floor follow-ups" (2026-09-14): "The reach is watched over `app/` + `components/` and the rules read more than that." The premise turned out to be wrong — all four read exactly those two — which is the finding, and the fix is to stop restating it by hand. -->

- [DONE: 2026-09-14] [est: 7000] **The floor hand-typed `("app", "components")`, which is a copy of an answer two guards already state** — and a copy is right only until one of them widens. `unionOfScanDirs("check-a11y-jsx", "check-clarity-input-mask")` reads their own `SCANNED_DIRS`, so a rule that grows a third root grows the floor with it rather than leaving a directory whose markup nothing watches.

  **The suggestion that asked for this was WRONG and the round is worth having anyway.** It said the rules read more than the floor does; they read exactly the same two roots, because `lint:jsx-walk`'s six-root walk is a TEXT scan and not a JSX one. What was true underneath is that the agreement was a coincidence nothing checked.

  **The parse moved out of `guard-scan-dirs.test.ts`** — it had been the only reader since three guards were caught declaring one list under three names — into `helpers/guard-scan-list.ts`, on the round it gained a second. The literal is still what gets parsed, deliberately: a `[...SOURCE_DIRS]` spread would satisfy the one-copy rule and defeat the pin that keeps every guard's scan list readable from its own file.

  **The two suite-side rules cannot be read the same way**, because they name their roots at the call site rather than in a script, so a case pins that `empty-state-wrapper-audit` and the two heading sweeps still walk this set — which is where a widening there would say so.

  1 new case, one parser de-duplicated (9313 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9313 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## How much of the markup the rules actually read (2026-09-14)

<!-- First bullet of "Unparseable-tag follow-ups" (2026-09-14): "The walk STOPS at an unparseable tag and every rule silently loses the rest of the file — the honest output is 'read 40 of 180 tags in this file'. The primitive knows where it stopped and tells nobody." -->

- [DONE: 2026-09-14] [est: 12000] **`jsxReach` — the walk says where it gave up, and nothing in this tree gives it the chance** — four scanners reported a clean tree this morning while one of them was reading NOTHING for `components/nav-tab.tsx`. `walkJsx` stops at a tag with no end because it has nowhere to resume from, and it stopped silently: "no findings" and "no reading" print the same line. That is the failure every floor in this repository exists to catch, and the one they could not see, because they count FILES WALKED rather than markup understood.

  **Two assertions over `app/` + `components/`, and each is useless without the other.** No file truncates the walk — which a walk yielding nothing satisfies perfectly — and the tree yields at least 1300 opening tags against the 1625 measured across 71 files today. The same 20% slack the walk floors keep.

  **The failure message names the four rules that would have gone quiet**, because the answer to a truncation is never "look at this suite", it is "go and look at that tag" — and `unreadAt` is an offset into the comment-blanked source, so the line number printed is the real one.

  **The apostrophe has a case of its own, on RAW source.** `stripComments` would hide it, which is the right thing for the rules and the wrong thing for a regression guard: the case asserts the primitive survives the shape, so undoing this morning's `skipStringLiteral` fix turns it red rather than staying green behind a blanking step.

  **`onUnreadable` is optional on `JsxWalk`.** The two rules that inherit something have no use for it; `jsxReach` is what a caller that wants the number takes, and it reports the FIRST offset, because a render-prop recursion can give up inside a tag the outer walk then gives up on too.

  8 new cases (9312 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9312 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## What each rule does with a tag it cannot read (2026-09-14)

<!-- First bullet of "Fail-closed follow-ups" (2026-09-14): "`check-clarity-input-mask` is the only guard here whose unparseable input is a FINDING, and the others were never asked — the distinction is worth one sentence per guard rather than one decision per author." -->

- [DONE: 2026-09-14] [est: 8000] **Four rules walk this JSX and three of them had inherited "skip it" from whoever wrote the walk** — the fourth was caught failing open earlier today, which is what made the question visible. Nobody had decided; three of the answers happened to be right and none of them was written down.

  **The split is clean and it is about what the rule is LOOKING FOR.** A rule about a SHAPE — is this icon button unlabeled, does this wrapper set a card background — is hunting an offence, and a tag it cannot read is not evidence of one; reporting it would make a half-typed file fail a run about design tokens. A rule about a REQUIRED ATTRIBUTE — does this input carry the privacy mask — is hunting an absence, and an unreadable tag is exactly where the attribute might be missing and nobody can tell, so a skip is an unearned pass.

  **Each of the four is now pinned to its side by a case**, with a floor under each skip: a scanner that returned nothing for every source would satisfy "reports nothing about an unparseable tag", so each one also has to still report the tag it CAN read, on the line before.

  **The walk stops rather than resuming.** `walkJsx` has nowhere to pick up from after a tag with no end, so everything below an unparseable tag is outside the walk too — the fact all four decisions are about, asserted on its own so a future reader does not have to infer it.

  8 new cases (9304 in the suite), and the decision in the module header where the next scanner will read it. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9304 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The privacy guard that failed open (2026-09-14)

<!-- First bullet of "Loop-rule follow-ups" (2026-09-14): "`check-clarity-input-mask` failed OPEN and still does — `endOfJsxTag` returns `source.length` for a tag that never closes, so a malformed file is treated as one enormous tag and a single `clarity-mask` anywhere in it satisfies every input below." -->

- [DONE: 2026-09-14] [est: 9000] **One unclosed tag made every `<TextInput>` below it read as masked** — `endOfJsxTag` answered `source.length` for a tag it could not parse, with a comment explaining the choice: "treat the rest of the file as the tag so the mask check still sees every attribute". Every attribute, and every other input, and any string that happens to contain `data-clarity-mask`. For a rule about whether user keystrokes reach a session recorder, that is the worst failure available: the guard reports a clean tree.

  **It reads the tag or it says it could not.** `openTagEnd` answers -1, which is information; the tag is now reported with a hint naming the real problem rather than the mask advice, because "add data-clarity-mask" is not what to do about a tag that never closes.

  **The quoted-`>` fix has a case now, which it did not when it landed this morning.** `<TextInput placeholder=">" data-clarity-mask />` was argued from the code — nothing in this tree carries a quoted bracket before its mask — so the improvement was a claim. It is a fixture.

  **The unclosed-tag case pins the old behaviour by construction**: an unclosed tag on line 1 and a masked input on line 2, where the old reader found the marker below and reported nothing at all.

  3 new cases (9296 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9296 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The walk written as a loop, in a shipping guard (2026-09-14)

<!-- First bullet of "Anchored-wildcard follow-ups" (2026-09-14): "The guard still cannot see the walk written as a LOOP — both rules match a regex literal or a string search, so a hand-rolled `for` with a brace counter passes, and that is what `check-empty-state-wrappers` would have become if somebody had 'fixed' its regex by hand." -->

- [DONE: 2026-09-14] [est: 13000] **`char === ">" && depth === 0` is the whole of `openTagEnd`, and two files had it written out** — the guard's two rules matched a regex literal and a string search, so the walk spelled as a character loop went straight past. It is the shape the previous round's suggestion named as the honest limit; it turned out to be occupied.

  **One of the two is a SHIPPING guard, and its copy has the documented bug.** `check-clarity-input-mask` counted braces and did not skip string literals — the exact half `lib/jsx-open-tag.ts`'s header records an earlier copy leaving out, written again independently months later. A `<TextInput placeholder=">" data-clarity-mask />` ends at the quoted bracket, so the mask attribute is invisible and a masked input reports as unmasked. The escape-hatch `return source.length` meant it failed OPEN rather than closed.

  **The other had reimplemented `openTagsNamed` around it** — brace depth and string skipping, twenty-five lines, missing the backslash escape (`placeholder="a \" >"` closes early) and blind to a `<Pressable>` rendered through a render prop.

  **The rule is the PAIR, not either half.** A `>` comparison alone is ordinary text handling — the privacy-page reader tests for a markdown blockquote — and a depth counter alone is how `declared-shape` and `check-platform-pairs` read TypeScript type text. Together, and adjacent, they mean "the opening tag ends here". Adjacency is what keeps `char === ">" && code[i - 1] !== "="` out: that one closes a generic, and a case pins it.

  5 new cases for the rule, 2 copies deleted (9293 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9293 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The wildcard that reaches the `>` a few characters later (2026-09-14)

<!-- First bullet of "Empty-span follow-ups" (2026-09-14): "Sixteen suites assert a negative over a slice they cut by hand — those blocks are cut by a named regex rather than by a walk, so there is no shared helper to hang a floor on." -->

- [DONE: 2026-09-14] [est: 42000] **`lint:jsx-walk` matched a wildcard IMMEDIATELY followed by `>`, and forty call sites reached it through a couple of anchoring characters** — `/<FlatList[\s\S]*?windowSize=\{5\}[\s\S]*?\/>/` and `/<Pressable[\s\S]*?\n {6}>/` are the two commonest shapes here; the second finds an element's end by its INDENTATION. Same walk, same bug, invisible to the rule as it shipped this morning. A short lazy tail between the wildcard and the `>` turned nine findings into forty, and all forty are migrated.

  **`<FlatList[\s\S]*?prop[\s\S]*?\/>` reads as "the FlatList sets this prop" and means "somewhere after SOME `<FlatList` there is this text before SOME `>`".** `app/collection/[id].tsx` renders three of them — the viewer list's `windowSize={5}` and the selection list's `{7}` are the distinction those cases exist to hold, and one wildcard spanned both. It also crosses into the nested `<RefreshControl … />`, so the props being tested were partly another element's. `__tests__/helpers/jsx-element-props.ts` asks the element instead, and `elementTagWith` refuses TWO matches rather than taking the first: a marker that has stopped identifying anything is not a marker.

  **A shipping guard had the same bug.** `check-empty-state-wrappers` tracked `<View>` nesting with `/<View\b([^>]*?)(\/?)>/`, so a wrapper carrying `onLayout={() => measure()}` pushed a truncated attribute string — a `backgroundColor` after that point was invisible and the guard reported a clean tree — and the unmatched `>` desynchronised the open/close pairing for the rest of the file. It is a `walkJsx` with an inherited View stack now, which also reaches a wrapper rendered through a render prop.

  **An APOSTROPHE in a comment inside an open tag returned NOTHING for the whole file.** `skipStringLiteral` let a `'` open a string that never closed, `openTagEnd` ran off the end and returned -1, and `walkJsx` stops at a tag that does not close — so `components/nav-tab.tsx` walked to an empty list, silently. A quoted string cannot contain a raw newline in JavaScript; it ends at the line break now, and a template literal still spans lines because it legally does.

  **Two exclusions the widening needed, both found by running it.** `flushPendingQueue<ChatMessage>(pending, …)` is a type argument, told from JSX by the identifier character before the `<`. And a tag whose own `>` the author wrote (`/<I18nProvider>[\s\S]*?<DiagnosticsProvider>/`) is a nesting assertion with no wildcard finding anything — tested in code rather than as a `(?!>)`, which let the NAME backtrack and duly reported `<I18nProvide>`.

  15 new cases for the helper, 9 for the guard and the primitive, 40 call sites migrated across 21 files (9288 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9288 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The sheet reader asks the body, not the tag shape (2026-09-14)

<!-- First bullet of "One-meaning follow-ups" (2026-09-14): "`modalSpans` still decides for itself what a self-closing `<Modal />` means — it skips them, `elementSpan` now answers with an empty span, and both are right. Two statements of one rule is the shape that drifts." -->

- [DONE: 2026-09-14] [est: 7000] **`<Modal></Modal>` was a real span over nothing, and the skip that should have caught it was about the wrong thing** — `modalSpans` skipped `tag.selfClosing`, which is a second statement of what a self-closing element means now that `elementSpan` says it with an empty body. It skips an empty BODY instead, which is the rule this function actually has: a Modal with nothing in it wraps no sheet, however it was written.

  **The paired-and-empty case is what the old test on the tag shape let through.** `<Modal></Modal>` produced a span with a start and an end and nothing between them, so a sheet written after it could land in the gap between two spans and be reported as drawn outside a Modal — a message whose reason nobody would guess from reading it.

  **"Empty" has to mean empty, not short.** A case pins that a Modal whose only child is a whitespace-wrapped `<View />` still covers it, because `trim()` is doing the work and a rule that discarded small spans would be the same bug wearing the fix's name.

  2 new cases (9265 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9265 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## One meaning for `end` (2026-09-14)

<!-- First bullet of "Falsifiable-window follow-ups" (2026-09-14): "`elementSpan` gives a self-closing tag `end === tagEnd`, which is the `>` and not the `/` — two different meanings for one field, defensible for each and stated nowhere; the first caller that slices a mixed list will find out by rendering half a tag in an error message." -->

- [DONE: 2026-09-14] [est: 8000] **`elementSpan.end` is exclusive, like every other end offset a `slice` takes** — it shipped this morning as "the offset of the close tag" for a paired element and "the offset of its `>`" for a self-closing one. `code.slice(start, end)` therefore returned the element minus its close tag in the first case and the element minus its final bracket in the second, and a caller holding a mixed list of both gets one of each with nothing at the call site to say which.

  **Neither meaning was wrong; having two was.** The fix is the one a reader already expects from an `end`: just past the element's last character, so the slice is the whole element in both shapes. A case asserts exactly that, over one of each, because it is the property the field exists for and the one that was silently false.

  **Every reader of it got more correct rather than less.** `<BulkBar>` "after the Profiler closes" now means after the whole element instead of after the `<` of its close tag, and the sheet-span containment is unchanged because an `accessibilityRole="none"` cannot sit inside a close tag.

  **One day between the bug and the fix, which is the argument for the suggestion file.** The field had three callers and all three were green: nothing was going to surface this except somebody reading the type, and the note that said so was written by the round that introduced it.

  1 new case (9263 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9263 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## A window that shrank to nothing is a finding, not a pass (2026-09-14)

<!-- First bullet of "Flat-walk follow-ups" (2026-09-14): "Three suites assert a NEGATIVE over a slice that could be empty — a reader that answered -1 everywhere would make the first pass vacuously; the migrations are green and nothing says they are still reading anything." -->

- [DONE: 2026-09-14] [est: 11000] **`elementSpan` — where an element closes AND what is inside it, because the second half is what makes the first falsifiable** — nine call sites moved onto the shared JSX walk this morning and three of them assert that something is NOT in a window. A negative is true of an empty string: `entry-currency-notice` would have passed with a close tag at offset 0, reading nothing, and its only guard was `!== -1`. The window now has to contain the line it is about before it is asked what it does not contain.

  **`closeTagIndex(code, tag.tagEnd + 1, tag.name)` had been written out three times in a day**, and the `+ 1` is the part a fourth copy gets wrong — `tagEnd` is the `>` itself, so a body counted from there opens with the tag's own last character. One function, and a case that pins the offset.

  **The two ordering assertions were vacuous in the other direction.** `<BulkBar>` after `</Profiler>` holds just as well when the close came back too EARLY, and `<StorageNotice />` before `</ToastProvider>` holds when it came back at the end of the file. Both are stated over the body now: the Profiler has to wrap a `<FlatList>`, and the provider's body has to contain the mount. A broken reader cannot satisfy either.

  **A self-closing tag is a complete element with nothing inside** — `""` and its own `>`, not null. A caller asking what is inside `<HeroBanner />` has an answer, and looking for its close tag would find the next element's, which is the span-swallowing bug the sheet reader was fixed for yesterday. A missing close tag is null rather than -1, so a caller cannot slice with it by accident.

  6 new cases (9262 in the suite), 4 call sites shortened and 3 of them made falsifiable. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9262 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The flat walk says so once (2026-09-14)

<!-- First bullet of "Refused-fourth-walk follow-ups" (2026-09-14): "`openTagsNamed` returns TEXT and two of the four migrations wanted offsets — both dropped to `[...walkJsx(code, { seed: null, inherit: () => null })]` and a `.find`, which is the same six words in two files." Second bullet, folded in because it is the same change: "`{ seed: null, inherit: () => null }` is now written in five places." -->

- [DONE: 2026-09-14] [est: 9000] **`jsxTags` and `tagsNamed` — the argument that reads the same either way, said at the call site instead** — `walkJsx(code, { seed: null, inherit: () => null })` was written in five places within a day of the walk being extracted: four suites and `openTagsNamed`. None of them has an ancestor question; they want the tags, and the phrase that says so is fourteen tokens of noise on every line.

  **The two callers that DO inherit something still say so**, which is the point of separating them: the a11y guard tracks "an ancestor has hidden this subtree" and the heading sweeps track "a `<Modal>` is above this node", and those are the lines where a reader should stop and read the callback. The other five were carrying the same weight for nothing.

  **`jsxTags` yields a plain `JsxTag`.** The `inherited` field of a walk that inherits nothing is a null every caller has to ignore, and one of the five was already destructuring around it.

  **`tagsNamed` is the offsets half and `openTagsNamed` is now one line over it** — two suites had written `[...walk].find((tag) => tag.name === "Profiler")` on the same afternoon, which is the same six words for the same question. They read `const [profiler] = tagsNamed(block, "Profiler")` now, and a case pins that a name nothing renders comes back as an empty array rather than a throw, because a destructure is how both of them read it.

  4 new cases (9256 in the suite), 4 call sites shortened. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9256 tests, audit baseline, build, bundle secrets, bundle size (15.6 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The fourth JSX walk is refused rather than discouraged (2026-09-14)

<!-- First bullet of "Last-copy follow-ups" (2026-09-13): "Three walks were merged today and nothing stops a fourth — the next sweep that needs 'every tag in this file' will write the tag stack again, because the reason not to is a paragraph in a module header rather than a rule." -->

- [DONE: 2026-09-14] [est: 25000] **`lint:jsx-walk` — and it found NINE more copies on the day it was written** — three walks were merged into `lib/jsx-open-tag.ts` on 2026-09-13, two of them carrying the same render-prop bug and fixed an hour apart. What stopped a fourth after that was a paragraph in a module header, and a paragraph is not a rule: the guard ran green on nothing and red on nine, in seven suites, every one of them passing at the time.

  **Two shapes, because there are two ways to fake the walk.** `open-tag-regex` is `/<HeroBanner[\s\S]*?>/` — the exact failure that module's header opens with, where the wildcard ends at the first `>` in the tag, which in this codebase is the one inside `onPress={() => close()}`. `close-tag-search` is `code.indexOf("</Modal>")` — the first close tag belongs to the INNERMOST element of that name, not the one being asked about.

  **Two of the nine were live bugs, not style.** `link-aschild-style-flatten` matched `<Link\b[^>]*\basChild\b[^>]*>`, so a link with a handler prop was never found at all — a sweep reporting a clean tree while reading none of it — and then ended the CHILD's tag at its first `>`, so `<Pressable onPress={() => …} style={[…]}>` had the array style cut off the end of the window. That ordering is the exact web crash the file exists to prevent.

  **Uppercase only, which is the JSX convention rather than an exemption list.** This tree genuinely parses HTML in three places — the built page's `<h1>`, the `<head>` a CSP meta is injected into, the `</head>` the SPA marker must precede — and none of `jsx-open-tag`'s premises hold there. Nothing has to be maintained as those three files change.

  **A precise regex is still allowed.** `/<Profiler\s+id="selection-flatlist"\s+onRender=\{…\}\s*>/` spells out the props it expects instead of wildcarding past whatever is there, and a case pins that it passes: a rule that fired on every match against markup is a rule somebody turns off.

  **The suite assembles every fixture from pieces**, because it sits inside the tree the guard walks — the same predicament `check-comment-terminators` records about its own terminator — and the last case asserts the assembly worked, so a literal fixture is caught here rather than as a confusing finding in `lint:all`.

  21 new cases for the guard, 4 for the new `openTagsNamed` primitive, 9 call sites migrated (9252 in the suite). 0 new keys, 0 bundle cost — the guard ships to nobody. `npm run verify` green: typecheck, 21/21 guards, 9252 tests, audit baseline, build, bundle secrets, bundle size (15.6 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.
