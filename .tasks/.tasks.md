## A skip that exits 0 and leaves no mark (2026-09-04)

<!-- First bullet of "Skip-reason follow-ups": "A skip still exits 0, so a week of registry outages is a week of green runs — the reason is printed now and nothing reads it. The gate is the one leg allowed a live feed and the only one that can decline to answer without leaving a mark any later reader would find." -->

- [DONE: 2026-09-04] [est: 11000] **The audit gate's skip is a workflow annotation, so a run that did not check advisories does not look like one that did** — the two entries before this one stopped npm's failure being read as an empty tree, and made the skip say which failure it was. Both wrote to stdout. A skip exits 0, and a log line on a green run is read by whoever is already reading the log, which on a green run is nobody. A week of registry outages would have been a week of green runs.

  **`::warning title=check-audit-baseline skipped::…`**, carrying npm's own sentence plus "The advisory baseline was NOT checked on this run." It lands on the run summary, which is where a later reader looks.

  **`lib/github-annotations.ts`, because the escapers were private to the other producer.** `check-inline-hex` had them — a five-replacement rule for property values, a three-replacement one for messages, and the difference between them is what keeps a title with a colon in it from cutting the annotation in half. Writing that a second time is how two copies of one rule stop agreeing, and this repository has four entries this week about exactly that.

  **The move is proved against the OLD output.** The hex scanner's 39 cases pass untouched, and a new case writes out the annotation it produced when the escapers were private and compares — so the extraction is checked against what it replaced rather than against itself.

  **`annotation()` omits an absent property rather than writing it empty** (`file=` with nothing after it is a property GitHub tries to resolve), and `runningUnderActions` compares the exact string: another CI system setting `GITHUB_ACTIONS=1` would otherwise get raw workflow commands printed at it.

  **Shown, not assumed:** run under `GITHUB_ACTIONS=true` with npm's ENOTFOUND payload, the gate prints the sentence and the annotation — which is what the two runs that failed this morning should have printed.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7575 tests (+11), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## A skip nobody can tell from a pass (2026-09-04)

<!-- First bullet of "Audit-soft-skip follow-ups": "The soft skip is silent about WHY it skipped — one line says "no parseable audit report (registry unreachable?)" for four different payloads, and npm's error object carries a `code` and a `summary` that would name the actual failure. A skip nobody can distinguish from a pass is how run 1565 was read as the registry moving." -->

- [DONE: 2026-09-04] [est: 9000] **The audit gate says which failure it skipped for, in npm's own words** — the entry before this one stopped npm's error payload from being read as an empty tree. It skipped instead, and printed the sentence the old reader printed for everything: "no parseable audit report (registry unreachable?)". Four different problems with four different fixes, one line between them, and a line that guesses in parentheses.

  **`readAuditPayload` decides and explains in one place.** It owns the parse, so a caller cannot skip for one reason and report another — the arrangement `networkTool` and the refusal verbs were both moved to. The script keeps the `execFileSync` and nothing else; the four branches, and the sentences for them, are pure and tested.

  **npm's `code` and `summary` are quoted when it gave them**, and the quote is bounded to 120 characters: this goes to a CI log, and npm's non-JSON output on a bad day is a stack trace. A skip line nobody reads is the thing being fixed.

  **Six cases, including the one that keeps the quote short** and the one relating `isAuditReport` to the reader by BEHAVIOUR rather than by a line of source — the pin the previous entry wrote broke the moment the line it named was replaced, which is the shape this file keeps retiring.

  **CI on `main` is green again** (run 1579). Honest limit: that run's audit step took 49 seconds, against 5m35s and 7m on the two that failed, so the registry answered normally and the new skip path was not exercised there. What the run shows is that the failure is gone, not that the skip fired.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7564 tests (+6), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## npm failing is not npm reporting nothing (2026-09-04)

<!-- Not from `.suggestions.md`: found by reading the CI failure that the previous entry's merge produced on `main`. -->

- [DONE: 2026-09-04] [est: 10000] **The audit gate's soft skip asked whether npm's payload PARSES, and npm's registry failures parse** — CI run 1573 spent five and a half minutes in `npm audit` and then failed with all four baseline entries "no longer reported", on a commit whose diff was four test files. Nothing about the tree had changed. `npm audit --json` answers a registry failure with `{"error":{…}}`: valid JSON, no `vulnerabilities`, and no findings is exactly what "every accepted advisory has been withdrawn" looks like from inside `evaluateAudit`.

  **The red it prints is an instruction to delete the baseline**, which is the one edit that would let a real advisory through in silence. A gate whose failure mode is "remove your exemption list" is worse than a gate that skips.

  **`isAuditReport` is the line, and the line is the `vulnerabilities` key.** An `error` present means npm is telling you why it has no answer; the key absent means whatever this is, the answer to "which advisories are open?" is not in it. A CLEAN tree is the case that must still get through, and npm writes it `"vulnerabilities": {}` — key present, empty — so the staleness it produces is real.

  **The soft skip had no test at all.** Seven cases now: npm's error payload, an error beside findings, a payload with no key, `null`/`undefined`/`0`/`""`/`true`/`[]`, a clean tree, a report with findings, and one that measures the consequence — the error payload handed to `evaluateAudit` returns the whole baseline as stale with nothing unexpected beside it, which is the red CI printed, character for character. A last case pins the one line in `readAudit` that turns the payload away, since reverting it would leave the other six green.

  Found by reading a CI failure rather than from `.suggestions.md`: the same failure had already gone red on `main` for run 1565 in an earlier session, and been read as the registry moving.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7558 tests (+7), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The verb that tells nothing apart (2026-09-04)

<!-- First bullet of "Verb-ambiguity follow-ups": "The pair loop is quadratic over the distinct verbs and asserts nothing about the ONE that matters most — that a verb is non-empty. `""` is a prefix of everything with a space in it, and it would be registered, matched and reported as a verb by every reader here." -->

- [DONE: 2026-09-04] [est: 7000] **A registered verb has to be words the sentence can carry** — the entry before this one ruled out one verb being read as another, pair by pair. It says nothing about the verb that is read as nothing: `""` is registered, matched and reported like any other. The message becomes `A test tried to  curl`, the expected prefix becomes `A test tried to  `, and the two agree perfectly — while the pair loop asks whether some other verb opens with `" "` and finds that none does.

  **The shape is words, not merely non-empty.** The sentence puts exactly one space on each side of the verb, so an edge space or a doubled one shifts the prefix the population case builds by a character it did not intend — the same failure with a different spelling. `/^\S+(?: \S+)*$/` is all three at once.

  **Proved on three trees, and what matters is the case that stays GREEN.** Empty, doubled-space and trailing-space verbs each turn only this case red among the registry's readers; the population case that reads the verb passes on all three, which is the hole rather than an argument about it. (The behavioural http case goes red on each too, for the sentence it pins rather than for this.)

  `npm run verify` green: typecheck, `lint:all` 19/19, 7551 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A prefix test is only as strong as the verbs are unambiguous (2026-09-04)

<!-- First bullet of "Verb-registry follow-ups": "The verb is asserted with `startsWith`, so a refusal whose verb is a PREFIX of another's would pass for both — "fetch" and "fetch and follow" are one rewording apart, and the three verbs being mutually non-prefixed is a property nothing states or checks." -->

- [DONE: 2026-09-04] [est: 8000] **The registry's verbs are asserted unable to be read as one another** — the entry before this one made every point answer with the verb its own surface installs, matched with `startsWith` because the verb sits inside one sentence and there is nowhere to cut it out of. That match holds exactly while no verb is another's opening words: `"fetch"` reads `A test tried to fetch `, and a refusal saying `"fetch and follow"` starts with precisely that. One rewording away, and the check that replaced the too-weak prefix would be the too-weak prefix again.

  **The space is the whole property.** `"fetching"` is not a hole — the character after the shorter verb is `i`, and the match requires the space. So what is asserted is that no verb, followed by a space, opens another; a narrower statement than "no verb is a prefix of another" and the one that is actually true of the match it defends.

  **Equal verbs are left alone, deliberately.** `http` and `https` share one because both refuse identically, so a point of one registered against the other is not a fault worth a sentence. What the case rules out is two DIFFERENT statements one of which can be read as the other.

  **A floor, because one verb everywhere makes both cases vacuous.** With a single distinct verb there are no pairs to check and reading the verb tells a caller nothing the shared prefix did not.

  **Proved on the two-part tree the case describes**: the http verb reworded to open with `fetch`, and `http.get` registered under the fetch verb — the misregistration the population case exists to catch. That case stays GREEN, which is the hole demonstrated rather than argued, and the new case is the only thing that names it. (The behavioural http case also goes red on that tree, for the reworded sentence rather than for this.)

  `npm run verify` green: typecheck, `lint:all` 19/19, 7551 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Some refusal is not this point's refusal, again (2026-09-04)

<!-- First bullet of "Every-point-registry follow-ups": "The population case calls every point with `"curl"` and one bound, and three surfaces read that argument three different ways — a spawn refuses it as a tool, the http clients as an unparseable host, `fetch` as an unparseable URL. They agree today because the refusal fires before any of them parses anything; the day one point stops refusing, what the call does next is a different function's business and the bound is the only thing standing between the case and a real `curl`." -->

- [DONE: 2026-09-04] [est: 9000] **The refusal a registered point answers with is matched against the verb its own surface installs** — the entry before this one made every point prove it refuses, with `/A test tried to/`. That is the prefix all four refusals share, so what it proved was that SOME refusal fired. The registry is eleven names over three surfaces and the whole reason it exists is that a point can end up holding something other than what its name says; the one fault it was built for reads identically under a shared prefix.

  **This is the `spawnToolOf` argument, one level up.** Four entries back a probe was proved to let SOME refusal through, and the fix was to name the tool its own call earned. A point registered against another surface's wrapper is that same shape, and the registry made it possible in a way it had not been.

  **The verb is one expression, not two statements.** `FETCH_VERB`, `HTTP_VERB` and `SPAWN_VERB` are each passed to `refuseNetwork` and to `installRefusal` at the same site, so `refusalVerbAt` cannot drift from the sentence a caller actually meets — the arrangement `networkTool` was exported for, applied to the other half of the message.

  **Proved against a tree where only the registry is wrong.** `http.get` stays correctly patched on the module and its entry points at the spawn wrapper: the outermost check compares that wrapper against itself and passes, the behavioural http case reads the real module and passes, 26 of 27 stay green, and the new assertion is the only thing that sees it — "`http.get` answered with \"A test tried to spawn curl…\" — that is not the refusal this surface installs".

  `npm run verify` green: typecheck, `lint:all` 19/19, 7550 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Only the spawn methods were asked (2026-09-04)

<!-- First bullet of "Outermost-wrapper follow-ups": "Only the spawn methods are asked — `globalThis.fetch` and the four http client methods are installed by the same bootstrap and nothing remembers what it put there, so a wrapper around the fetch refusal is exactly as invisible as a spawn wrapper was before this. The suites stub `fetch` constantly, which is why it is harder, and also why it is the likelier one to acquire a layer." -->

- [DONE: 2026-09-04] [est: 11000] **The bootstrap remembers all eleven refusals it installs, and the outermost check asks about every one** — the entry before this one taught it to keep the wrapper it put on each spawn method, and stopped there. `globalThis.fetch` and the four http client methods come out of the same module body and were remembered by nothing, so a layer over the fetch refusal was exactly as invisible as a spawn wrapper had been the morning before. `fetch` is the point the suites stub in dozens of places, which makes it both the likeliest one to acquire a layer and the one that had none of this.

  **A point is a value and a closure that reads the current one**, because the three surfaces are not the same shape — `fetch` on `globalThis`, two client methods on each of two module objects, six on a third. `refusalAt(point)` hands back what a call site would get, so a reader asking "does this still refuse?" does not rebuild a second copy of which surface each point lives on.

  **`rewrappedRefusals()` replaces `rewrappedSpawnMethods()`** rather than sitting beside it: two readers of one registry, one of them a filter of the other, is the arrangement half this week's entries are about closing.

  **The identity check is vacuous against an empty registry**, so a second case calls every point it names and requires a refusal — read live, one argument (`"curl"` is a network tool to a spawn and not a URL to the other two), the probes' bound riding along because a spawn point that had stopped refusing would otherwise run it. It also asserts the four surfaces are all still represented.

  **A guard that matched the bootstrap's source went red about a rewording.** `verify-gate-script.test.ts` proved the http refusal existed with `/\[http, https\]/` — a literal in the loop this entry rewrote. It reads `refusalPoints()` now, plus `networkTool("curl")` for the marker that is about the tool rather than the call, so it asks the bootstrap what it patched instead of how it is spelled.

  **Proved against two broken trees, each isolating one case.** A helper that wraps `globalThis.fetch` after the bootstrap: 26 of 27 green, only the outermost case sees it, and the population case passes because the layer still refuses. The http points left unregistered: the outermost case passes (nothing to differ) and only the population case goes red.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7550 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Present is not outermost (2026-09-04)

<!-- First bullet of "Instance-count follow-ups": "The count says the body ran once, not that the refusal is one layer deep — they are the same thing only because the body is the only thing that wraps. A helper that re-wrapped `execFileSync` for its own reasons would leave the count at one and the stack two deep, which is the state the case describes in its message." -->

- [DONE: 2026-09-04] [est: 9000] **The bootstrap keeps the wrapper it installed on each spawn method, and a case asks whether it is still there** — `bootstrapInstances()` answers how many times the module body ran, which is a different question from how deep the refusal is stacked. Anything that wraps a spawn method after the bootstrap leaves the count at one and puts a layer between a caller and the refusal. That layer still refuses, so nothing goes red; it also gets to see, change or drop the call first, and `optionsPassedBy` in the same file is proof that code here does exactly that kind of swapping.

  **`rewrappedSpawnMethods()` names the methods that are no longer the bootstrap's own wrapper.** A suite that swaps and restores correctly is invisible to it, which is the point: what it answers is whether the swap is still in place.

  **Proved against a helper that wraps `execFileSync` and never restores it.** The instance count stays at one, the refusal still fires, 25 of the file's 26 cases stay green, and the new one is the only thing that sees the extra layer.

  **Also fixed: a doc comment orphaned by the previous entry.** `NETWORK_TOOLS`' comment — the one explaining why `npm` and `npx` are in the set — had the instance-count block inserted between it and its declaration, so it read as documentation for the counter. The counter block moved above it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7549 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A second bootstrap would have been invisible to every case in the file (2026-09-03)

<!-- First bullet of "Bootstrap-import follow-ups": "A suite now imports the bootstrap, and that the module body runs once was proved by hand and not by anything in the tree — the demonstration was a `console.error` added and removed. A second bootstrap instance would re-wrap every spawn method and re-assign `fetch`, and the identity cases would compare against the wrappers they found, so all of them would still pass." -->

- [DONE: 2026-09-03] [est: 9000] **The bootstrap counts its own evaluations and a case reads the count** — the entry before this one had a suite import `test-globals.ts` for `networkTool`, which turned "the same instance the `--import` evaluated" from a property nobody could break into one a specifier could. It was proved once by putting a `console.error` in the module body and counting lines. That is not a thing the tree can do again.

  **The count lives on `globalThis` under a registered symbol**, not in a module-scope `let`: two instances would each have their own `let` and each answer one, which is the answer that hides the problem.

  **Measured on a real second instance**, made the way a mis-resolved specifier would make one — the module deleted from the require cache and required again, so the body runs twice for real. Every spawn method ends up wrapped around its own wrapper and `fetch` is re-assigned. **24 of the file's 25 cases stay green**; the new one is the only thing that notices. The refusal still fires, and every identity check compares against whatever was installed by the time it read the module, which is exactly why nothing else could see it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7548 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The case reduced a spawn to a tool name, and so did the refusal (2026-09-03)

<!-- First bullet of "Tool-naming follow-ups": "`spawnToolOf` re-implements the bootstrap's reduction from a path or command line to a tool name — first token, then `basename`. The bootstrap does the same thing privately, and the two agreeing is what makes the assertion mean anything; nothing relates them, which is the `NETWORK_TOOLS` arrangement one level down." -->

- [DONE: 2026-09-03] [est: 9000] **`networkTool` is exported from the bootstrap and the case asks it** — the entry before this one taught the case to name the tool a probe's spawn earns a refusal for, and wrote a second reduction to get it: first token, then basename. Correct the morning it was written, and load-bearing only while it agreed with the reduction the refusal actually uses, which lived privately in `test-globals.ts`. That is the two-copies arrangement four entries this week are about, reintroduced by the entry that closed one.

  **The suite imports the bootstrap it is already preloaded with, and the module body runs ONCE** — measured, not assumed: a `console.error` in the module body prints one line under `--import ./__tests__/test-globals.ts`. The import resolves to the same instance, so nothing is patched twice.

  **`undefined` is a different failure and says so.** A probe whose spawn names nothing the refusal covers would never have been refused at all, so the case says that rather than asking which refusal it let through.

  **Proved against a tree where the refusal stops covering `npm`.** Both versions go red; what differs is the diagnosis. This one: "a probe that spawns names `npm`, which the bootstrap does not count as a network tool — it would run rather than be refused". The private reduction: "the `after-recording` probe did not let through the refusal for spawning `npm`", blaming a probe that behaved perfectly. In that run the probe's spawn was real and `BOUNDED` is what held it, which is the case that exists for exactly that.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7547 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Some refusal is not this spawn's refusal (2026-09-03)

<!-- First bullet of "Refusal-measurement follow-ups": "The behavioural case matches `/A test tried to/`, which is the prefix of all four refusals — it proves the probe let SOME refusal through, not that the one it met was its own spawn's. A probe whose body fetched instead would satisfy it identically, and the marker/probe pairing that would notice is a different case reading a different thing." -->

- [DONE: 2026-09-03] [est: 10000] **The refusal a probe lets through is matched against the tool its own call names** — `/A test tried to/` is the prefix every refusal in the bootstrap shares, so the case proved a refusal reached the caller and not that it was the one this probe earned. A probe that swallowed its spawn's throw and then fetched satisfied it exactly.

  **The tool is read off the recorded call.** `spawnCallOf` is the recorder `optionsPassedBy` was built around, returned whole now, because two readers want different arguments out of one call: the bound is the last, the tool is the first. `spawnToolOf` reduces that first argument the way the refusal does — `exec` takes a command line, `execFile` and `spawn` take a path — so the assertion is runtime against runtime, with no word from the source in it.

  **`spawnCallOf` takes whether to re-raise the probe's own failure.** The restore cases need it thrown; that path is their subject. A reader that only wants the arguments does not, and the one probe that throws deliberately after its spawn would otherwise have to be caught at every call site. The recorded call is complete either way — the probe had already made it.

  **Proved on one broken tree, run twice.** The probe eats its own spawn's refusal and calls `fetch` instead, so a refusal still reaches the caller. With `/A test tried to/` the case is GREEN; with the tool named it is RED.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7547 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The rule written to close a spelling hole was itself checking spelling (2026-09-03)

<!-- First and second bullets of "Swallow-rule follow-ups": "The rule matches the identifier `RECORDER_STOP` in the source, which is the shape the bound rule was retired for — a probe mentioning it in a comment, or comparing `error === RECORDER_STOP` and swallowing anyway, satisfies it." / "`PROBES` and `THROWING_PROBES` are merged with a spread, so an id in both silently collapses to one probe — the marker parity case reads `PROBES` alone and would not see it." -->

- [DONE: 2026-09-03] [est: 11000] **A swallowing probe is handed a real refusal now, not just read for the right word** — the swallow rule matched `RECORDER_STOP` in a probe's source, which is the shape `assert.match(probe.toString(), /\bBOUNDED\b/)` was retired for three entries ago, reappearing in the rule written to close a different instance of it. A probe comparing against the stop and swallowing anyway reads exactly the same.

  **The measurement is a call.** Each probe that both spawns and catches is invoked directly — no recorder installed, because `optionsPassedBy` is not in that call — so its spawn meets the bootstrap's refusal, the error a bare `catch` ate. The probe has to let it through. Nothing is spawned; the refusal is why.

  **The two probe tables are asserted disjoint.** `{ ...PROBES, ...THROWING_PROBES }` keeps ONE probe when an id is in both, and the one it keeps is the throwing one — a marker's probe could be shadowed out of the rule that judges it while the parity case, which reads `PROBES` alone, went on passing. `allProbes()` says so, and `probesThatCatch()` is the population with its floor inside it, where the filter and its anti-vacuous check used to be inline.

  **Proved against two broken trees.** A probe whose `catch` reads `void RECORDER_STOP;` — the identifier present, everything swallowed — leaves the source rule green and turns only the new case red. An id added to both tables turns both population readers red while the marker parity case passes, which is the silence the disjointness assertion was written to end.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7547 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The hole was fixed in the probe and nothing in the tree said so (2026-09-03)

<!-- Fifth bullet of "Sentinel follow-ups": "The broken tree that proves this case had to be run twice, against two versions of the probe — the demonstration lives in a `.tasks/` entry and nothing in the tree records that a bare `catch` here is a hole. The next probe that needs to swallow a spawn is one `catch {}` away from reopening it." -->

- [DONE: 2026-09-03] [est: 12000] **A probe that swallows anything has to swallow the recorder's stop BY IDENTITY, and that is a rule now** — the bare `catch` was found by reading a diff, fixed in the one probe that had it, and recorded in a `.tasks/` entry. That is the arrangement four entries this week are about: the next probe that needs to swallow its spawn is one `catch {}` away from reopening a hole where a real `npm audit` starts and the case still passes.

  **The throwing probes are a named population.** Both lived as closures inside case bodies, where no rule can read them. `THROWING_PROBES` keys them by the path they fail on — `before-spawning`, `after-recording` — and the two restore cases take theirs from it, so the source rule has something to judge and a third path gets a probe rather than a third closure.

  **The rule reads the source, like the marker and spawn rules.** Any probe whose body catches must match `CATCH_BINDING` (a `catch` that binds what it caught — `catch {}` cannot tell a stop from a refusal) and must name `RECORDER_STOP`. It is a population with an anti-vacuous floor, not the two bodies that exist today.

  **The premise case carries the third shape.** `sourcePremiseControl` grew a binding `catch`, its doc comment says three readers instead of two, and the case asserts `CATCH_BINDING` fires on it — so a runner that minified is named there rather than reported as every probe catching nothing.

  **Proved against two broken trees, each isolating the new rule**: the bare `catch` the probe actually had, and a `catch` that binds but compares `instanceof Error` — a comparison that swallows the bootstrap's refusal just as thoroughly. Only the new case goes red on either; every neighbour stays green, which is what says the rule is not a restatement of one of them.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7546 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A bare `catch` in the probe would have swallowed a real spawn's refusal (2026-09-03)

<!-- First bullet of "Post-spawn-throw follow-ups": "The new probe swallows the stop with a bare `catch {}`, which would swallow a real spawn's error identically — if the recorder were ever not installed, the probe would spawn `npm audit` for real, swallow whatever came back, and still throw `boom`, so the case would pass while a child had started. `BOUNDED` is what keeps that harmless, and it is the second case in this file whose safety rests on the bound rather than on the assertion." -->

- [DONE: 2026-09-03] [est: 9000] **The recorder's stop is one named sentinel, so a probe can swallow THAT and nothing else** — it was created fresh inside `optionsPassedBy`, which is all that function needs: it compares by identity against the one it just made. A probe cannot. The post-spawn-throw probe had to swallow its spawn's throw to reach its own, had nothing to compare against, and used a bare `catch` — which swallows the bootstrap's refusal exactly as quietly. With no recorder installed that probe would have spawned a real `npm audit`, dropped whatever came back, and still thrown `boom`: the case green while a child had started.

  **`RECORDER_STOP` is now a describe-scope constant** the recorders throw and the probe compares against, so the swallow reads "the recorder's stop, and nothing else" and anything else re-raises.

  **Proved on one broken tree, run twice.** The tree: no recorder installed over `execFileSync`, so the probe's spawn reaches the real function. With the old bare `catch` the case is GREEN — the hole, demonstrated rather than argued. With the identity comparison it is RED. (Two neighbouring cases go red on that tree either way; this one was the blind spot.)

  Nothing was spawned in either run: the bootstrap's refusal is what the probe now re-raises, and `BOUNDED` rides on the call regardless.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7545 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The restore was proved on the half of its input where nothing had been recorded (2026-09-03)

<!-- Fourth bullet of "Restore-population follow-ups": "The re-raise path is still exercised by a hand-written closure that never spawns — the throwing-probe case proves the `finally` survives a probe that fails BEFORE the recorder is reached. Nothing exercises a probe that throws after it, which is the failure a real probe would have, and the derived population is not used there at all." -->

- [DONE: 2026-09-03] [est: 10000] **A probe that throws AFTER a recorder has fired now has a case** — the existing throwing-probe case never reaches a spawn, so the state it leaves `optionsPassedBy` in is recorders installed and nothing recorded. That is not the state a real probe fails in. A probe holding its own `try` around its spawn swallows the recorder's stop, keeps going, and throws something of its own: recorders installed, one call recorded, and the re-raise path taken. Both halves of what the `finally` has to survive were being proved on the same half of its input.

  **Proved against a broken tree aimed at exactly that gap**, not a blanket break: the restore is skipped when the probe re-raises AND something was recorded. Only the new case goes red — the pre-spawn throwing case and the every-probe loop are green against it, which is the demonstration that they do not cover this state.

  **The three restore comparisons are one function now.** `spawnMethods()` snapshots the module and `assertRestored(before, why)` makes the name-by-name comparison, where three copies of one loop differing only in the sentence used to be — the arrangement that lets a fourth reader be written slightly wrong.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7545 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## One probe was checked for restoration and two probes spawn (2026-09-03)

<!-- First bullet of "Module-restore follow-ups": "The identity snapshot runs once, on `npm-audit`, and generalises to every probe by assuming they share `optionsPassedBy` — the throwing-probe case is the only one that exercises the re-raise path, and neither asserts the restore holds across the OTHER spawn probe. Two probes spawn; one is checked for restoration." -->

- [DONE: 2026-09-03] [est: 9000] **The restore case reads the derived spawn population instead of naming one probe** — it called `optionsPassedBy(PROBES["npm-audit"])` and asserted the module came back identity-clean. That is a claim about one probe read as a claim about the function: the other spawn probe puts a different body through the same seven-method swap, and "they share `optionsPassedBy`, so one is enough" is the exact assumption a case about a SHARED module should not be making.

  **The population is `probesThatSpawn()`**, the same derivation the bound case reads, so a third spawning probe is covered by existing rather than by somebody remembering to add it here — the arrangement the `BOUNDED` hand-application was already retired for.

  **The snapshot stays outside the loop.** Each probe is asserted back to the wrappers the bootstrap installed, not to whatever the probe before it left behind — a per-probe snapshot would take a recorder as the baseline and pass. The failure message names the probe, because "left `execFileSync` replaced" without one sends a reader through the whole table.

  **Proved against a broken tree** with the shape the follow-up named rather than a blanket break: the `finally` restores unless the probe's source mentions `npx`, so the first spawn probe is put back and the second is not. Exactly this case goes red; the old one-probe version is green against it, and so is every other case in the file — including the two that run AFTER it with recorders installed, which is how quiet the failure is.

  **Also on this branch: `npm update fflate`** (0.4.8 → 0.4.9, lockfile only). The audit gate turned the branch red on a `GHSA-px8p-9vwx-vf98` a lockfile bump clears, and the gate's own message is that such a finding is not a triage decision and belongs on the branch that meets it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7544 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The restore was the one thing keeping later suites refused, and nothing watched it (2026-09-03)

<!-- First bullet of "Bound-identity follow-ups": "`optionsPassedBy` mutates the module every suite shares and only a `finally` puts it back — an assertion that threw between the loop and the restore would leave a recorder installed, and every later suite in the process would silently stop being refused. The `finally` is correct today; nothing asserts the module is the same afterwards, which is the one check that would survive somebody rearranging the function." -->

- [DONE: 2026-09-03] [est: 8000] **`optionsPassedBy` is asserted to leave `node:child_process` exactly as it found it** — it replaces every spawn name on the live module and restores them in a `finally`. That restore is the only thing between this suite and every one that runs after it: a recorder left installed does not call through, so a later spawn hits `throw stop` instead of the bootstrap's refusal, and fails with this file's name nowhere in it.

  **Two cases, because the restore has two paths to survive.** One snapshots the module, runs `optionsPassedBy` on a spawn probe, and asserts every method is identity-restored — then measures the property that stands in for end to end: a spawned `curl` still throws the refusal afterwards. The other hands `optionsPassedBy` a probe that throws BEFORE it spawns, the error the function re-raises rather than swallows, and asserts the module is restored anyway — a probe with a bug must not take the refusal down with it.

  **The identity check is not vacuous.** `SPAWN_NAMES` has a floor of six proved a case away, `before` is captured while the methods are the bootstrap's wrappers, and `optionsPassedBy` really does assign recorders over them — so a dropped or moved restore leaves recorders in place and the comparison fails.

  **Proved against a broken tree**: the `finally` body replaced with `void saved` turns both cases red, one on the identity comparison and one on the throwing-probe path.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7544 tests (+2), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Matching the word proved the word was there (2026-09-03)

<!-- Third bullet of "Probe-population follow-ups": "Matching the identifier `BOUNDED` proves the word is in the source, not that the options were passed — a probe mentioning it in a comment, or passing it as the wrong argument, satisfies the rule. The property is that a spawn cannot outlive the case, and the only thing that measures it is the control spawn." -->

- [DONE: 2026-09-03] [est: 11000] **The bound is read off the call now, and compared by identity** — `assert.match(probe.toString(), /\bBOUNDED\b/)` measured that the word was in the text. `execFileSync("npm", ["audit"], { timeout: 1 }) // BOUNDED` satisfied it and would still have started a child nothing could interrupt, which is the one thing the rule exists to prevent.

  **`optionsPassedBy` replaces every name in `SPAWN_NAMES` on the live module with a recorder that throws instead of calling through**, so the probe stops before a child exists and the case sees the argument the call actually made. Restoring is in a `finally`: a recorder left behind would disarm the refusal for every suite that runs after this one.

  **The import had to change to reach the object.** `import * as childProcess` compiles to a namespace COPY under `tsx` — measured, not assumed: a named-import call site does not see a mutation made through it. The default import is the live module the bootstrap patches, and the comment above the import says which and why.

  **Identity, and the last argument.** Every probe here spawns synchronously and a sync spawn's options are its final argument; an async one would end in a callback and fail, which is correct, because what bounds an async child is a different question. `{ ...BOUNDED }` fails too — there is one bound object and the probes share it.

  **`probesThatSpawn` is one population with the floor inside it**, read by the bound case, where the filter and its anti-vacuous `>= 2` used to be inline.

  **`BOUND_APPLIED` is gone**, along with the premise case's third assertion: the source premise now underwrites two readers, not three, and the doc comments say two.

  **Proved against a broken tree** with exactly the shape the follow-up named — the bound moved into a comment. The old rule matches that probe's source; the new one does not.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7542 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The spawn detector was six names and the module knows seven (2026-09-03)

<!-- Second bullet of "Probe-population follow-ups": "The spawn detector lists six `child_process` function names by hand — the module has more ways to start a process (`fork`, and `spawn` reached off a default import), and the list was written from the two probes it had to cover. Same shape as `NETWORK_TOOLS` being two names for the property 'a tool that reaches the network'." -->

- [DONE: 2026-09-03] [est: 8000] **`SPAWN_NAMES` is asked of `node:child_process` now** — the detector's alternation was typed out from the two probes it had to cover that morning, and `fork` was not among them. A probe that forked would have read as not spawning at all: no bound demanded, and the case that catches a missing refusal back to hanging on a real process. What a suite can call and what the module exports are the same list, so the list is not the thing to write down.

  **Two exports are not a way to start a process, and each says so in its own name.** `ChildProcess` is the class a spawn RETURNS, told apart by node's convention that a constructor is capitalised; `_forkChild` is the entry point node calls INSIDE a child it already made, told apart by the underscore. The filter is those two conventions and nothing else — no third list of names.

  **The six it replaced are the floor, not the rule.** A derivation that quietly dropped one would leave a probe reading as not spawning, which is exactly the silence the hand list produced for `fork` — so each of the six is asserted present, `fork` is asserted present as the thing the omission cost, and the two non-spawns are asserted absent.

  **Sorted longest-first**, so `execFileSync` is not matched as `exec` and then rejected for the `F`. Alternation backtracking finds it either way; the ordering means a reader does not have to know that.

  **Proved against a broken tree** three ways, one per assertion: a filter that keeps only `exec`/`spawn` prefixes loses `fork`, a filter with no case rule admits `ChildProcess`, a filter excluding `execSync` breaks the floor.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7542 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Three rules read a probe's source and none of them read it on purpose (2026-09-03)

<!-- First bullet of "Probe-population follow-ups": "Three rules in this suite now read a probe's transpiled source and each spells the dependency differently — the pairing case runs marker patterns over it, the bound case matches spawn names, the bound case also matches the identifier `BOUNDED`. All three break the same way under a runner that minifies, and each states the risk in its own comment. Nothing asserts the one premise they share: that `probe.toString()` still looks like the source somebody wrote." -->

- [DONE: 2026-09-03] [est: 9000] **The premise is asserted once, by a function written to be read** — `patternPerformedBy`, the spawn detector and the bound match all rest on `Function.prototype.toString` handing back what somebody typed. Each said so in its own comment and none of them said it to the runner. `sourcePremiseControl` is never called: its body carries one of each shape the three readers look for, and the three assertions are labelled with the rule each one underwrites.

  **The diagnosis is the deliverable, not the detection.** A `toString` that stopped answering already turns the tree red — `patternPerformedBy` returns nothing for every probe and the spawn floor of two goes unmet. What it does NOT do is say why: the messages blame the probes, so a contributor meets "the `fetch` probe is not the shape its pattern reads" and starts reading a diff that is fine. Each assertion here names the rule downstream of it instead.

  **An exact answer no single probe produces.** The control performs two of the four shapes, so `patternPerformedBy` is asserted to return `["npm-audit", "fetch"]` — a pattern that had stopped firing and a pattern that fired on everything both show up, where a one-marker control would only catch the first.

  **Two regexes stopped being written twice.** `SPAWN_CALL` and `BOUND_APPLIED` are named consts the bound case and the premise case both read; the transpiler paragraph moved onto `SPAWN_CALL`, where the pattern it explains lives.

  **Proved against a broken tree** three ways, one per assertion: the literals changed, the call name renamed the way a minifier would, the identifier renamed. Each is named by its own message.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7541 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The bound was applied to the probes that existed that morning (2026-09-03)

<!-- Second bullet of "Bounded-spawn follow-ups": "`BOUNDED` is applied to the two spawn probes by hand and nothing keeps a third one covered — a fifth marker that is also a spawn gets a probe written next to two that carry the options, which is exactly how the first four probes came to be keyed by prose. The population of 'probes that spawn' is knowable from the probe source the pairing case already reads." -->

- [DONE: 2026-09-03] [est: 7000] **A probe that spawns is found rather than remembered** — `BOUNDED` went onto the two spawn probes that existed the hour it was written, and a third would be written next to two that carry it and inherit nothing. That is the same arrangement as keying four probes by prose, filed against the same file two changes earlier, and it does not need a list: a probe that spawns says so in the source the pairing case already reads.

  **The reading found nothing on the first attempt, and the reason is worth having written down.** `tsx` compiles `execFileSync(…)` to `(0, import_node_child_process.execFileSync)(…)`, so the name is no longer against its own bracket — a closing paren counts too. Same transpiler dependency `patternPerformedBy` runs on, now stated in a second place with the evidence beside it.

  **A floor of two, because a detector that matched nothing would agree with every probe there is** — the vacuity that turned up as a real failure here rather than as a hypothetical.

  **Proved against a broken tree**: the `expo-install-check` probe with its options taken away is named by the failure.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7540 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The case that guards the refusal would have failed by hanging (2026-09-03)

<!-- Third bullet of "Marker-id follow-ups": "Two of the four probes spawn a real `npm`/`npx` and rely on the refusal firing before the process starts — if `NETWORK_TOOLS` ever stopped covering them the case would run a real `npm audit` in a test, which is slow, network-dependent, and exactly what the whole file exists to prevent. The failure mode is a hang rather than a red assertion." -->

- [DONE: 2026-09-03] [est: 8000] **A spawn probe cannot perform the spawn it is probing for** — two of the four probes hand a real `npm` and a real `npx` a real command and rely entirely on `NETWORK_TOOLS` refusing them before the process starts. Take either name out of that set and the probe does not fail: it RUNS. `npm audit` against the registry, inside `npm test`, on the leg whose whole subject is that no suite reaches the network. The case that guards the refusal would have reported its absence by being slow.

  **A killed spawn turns a hang back into an assertion.** `BOUNDED` gives the child a millisecond and SIGKILL, so `execFileSync` throws for the wrong reason and `assert.throws` reports a message that is not the refusal's. It is load-bearing only in the failure: while the refusal works the probe never reaches a spawn at all.

  **The control is a spawn the refusal does not touch.** A node process that would never exit on its own, run through the same options — if the bound were wrong this is the case that hangs, and it hangs on a spin loop rather than on a suite quietly asking npm's registry a question. Time is asserted as well as the throw, because an `execFileSync` that threw after ten seconds would satisfy the throw and not the property.

  **Measured with the refusal removed**, which is the only way to see the shape this is about: `NETWORK_TOOLS` cut to `curl` and `wget` turns the run red in 2.8 seconds with two assertion failures, where the same edit before this change had the suite spawning npm and npx for real.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7539 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Freeing the prose to be reworded also freed it to be deleted (2026-09-03)

<!-- Second bullet of "Marker-id follow-ups": "`why` is now the only field a failure prints and no rule keeps it a sentence — the id took over the identifier job, which frees the prose to be reworded and also to be shortened to the id. A marker whose `why` reads 'fetch' would print a failure message that says less than the pattern does." -->

- [DONE: 2026-09-03] [est: 9000] **A marker's `why` is held to being a sentence, because it is the entire failure message** — `networkMarkerHit` answers with `why` and nothing else, so every finding this scan reports is that string beside a filename. Giving markers an `id` the day before took the identifier job off the prose, which was the point; what it also did was make shortening the prose to the id free. `why: "fetch"` would have printed a finding that says less than the pattern a reader could have looked at instead, and nothing would have gone red.

  **Two rules reading one regex from opposite directions.** `MARKER_ID_SHAPE` is exported now rather than written into the id case: every `id` matches it, no `why` does. A single regex asserted in both directions is the whole statement that the two fields have different jobs — two copies of it could drift into agreeing.

  **Three ways a sentence collapses, not one.** Shaped like an id; fewer than three words; the id with its dashes swapped for spaces, which is the same collapse spelled so the first rule misses it. Plus distinctness, since two markers printing one sentence leave a reader unable to tell which shape was found — the id is what tells them apart in the wiring and the reader never sees it.

  **Measured on the real tree, where the scan already has findings.** The scripts outside the gate are the population the anti-vacuous case leans on — `db:find-duplicates` and `sentry:check` call `fetch`, `check-expo-install` asks the registry about versions — so what a reader of those findings is handed is asserted rather than inferred from a return type, and it is TWO different sentences, which is the distinctness rule measured instead of stated.

  **Proved against a broken tree.** `why: "fetch"` planted on the fetch marker turns two of the three cases red, including the one that reads the real scripts.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7538 tests (+3), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A key relates two things by name, and nothing related them by meaning (2026-09-03)

<!-- First bullet of "Marker-id follow-ups": "The id is checked for shape and nothing relates it to the pattern it names — `fetch` could be the id of the http-client marker and every case would pass, because the parity comparison is between two key sets and the probe lookup follows the same key. The pair that must agree in MEANING is `pattern` and probe, and that is still asserted by nobody." -->

- [DONE: 2026-09-03] [est: 12000] **A probe is judged against its marker's `pattern`, not only against its `id`** — giving markers a short id the day before fixed the reword problem and left the parity arrangement resting entirely on a string. The population case compares two key SETS, the refusal case looks a probe up by the same key, and the shape case asks the id to be lowercase and distinct. Swap the `fetch` probe with the http-client one and all three stay green: both throw, both throw `A test tried to`, and neither is asked what it actually performs.

  **The one text both halves can be judged against is the probe's own source.** `pattern` describes what the shape looks like in a script's text and the scan reads exactly that; `probe.toString()` is a text that either is the shape or is not. `patternPerformedBy` returns the markers whose pattern fires on a probe, and the case demands it return the probe's own marker and NOTHING else — a probe filed under the wrong marker names the marker it really performs and goes red.

  **Two probes were not the shape they were filed under, which is the finding.** `fetch` called through `installed`, the captured refusal — invisible to `/\bfetch\s*\(/`, and to any scan, which is the whole point of the marker. It reads `globalThis.fetch` at call time now, which is both what the pattern reads and what a real suite does; the cases that need the pre-stub capture still have it. `http-client` is the marker whose pattern reads an import SPECIFIER, and a probe cannot import at call time, so it takes the client by that specifier through `httpClient("node:https")`. Its doc says what that does not prove: nothing here claims an import of `node:https` is refused, and nothing here needs to.

  **Exactly-one is self-controlling and there is a planted offender anyway.** A `patternPerformedBy` that matched everything or nothing fails the deepEqual for every marker rather than passing quietly — but a rule nobody has watched refuse anything reads as passing whether it works or not, so a case exchanges the two probes that used to be indistinguishable here and asserts each is named by the other's marker.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7535 tests (+2), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The probes were keyed by a sentence written to be reworded (2026-09-03)

<!-- First bullet of "Probe-parity follow-ups": "The probes are keyed by the marker's prose label, so a reworded label is a red run about nothing — 'an `npm audit` of the registry' is a sentence written to be read in a failure message, and it is now also an identifier. Renaming it for clarity breaks the parity case, and the fix is a copy-paste that teaches nobody anything. A short id beside the label would separate the two jobs." -->

- [DONE: 2026-09-03] [est: 7000] **A marker has an `id` and a `why`, because the two have opposite lifetimes** — the parity case that pairs a runtime refusal with every scan marker keyed its probes by the marker's prose. That prose exists to be read in a failure message, which is exactly the thing somebody rewords for clarity — and doing so turned into a red run about nothing, whose fix is a copy-paste that teaches nobody anything.

  **`NETWORK_MARKERS` is objects now**, `{ id, why, pattern }`, where it was a `[label, pattern]` tuple. `networkMarkerHit` still answers with the sentence, so every reader of the scan is unchanged; `PROBES` is keyed by `npm-audit`, `expo-install-check`, `fetch`, `http-client`, and the refusal case still REPORTS the sentence — the failure names the shape a reader recognises and the wiring names something stable.

  **A third case, because an id can drift back into being one.** Distinct, and `^[a-z][a-z0-9-]*$` — two markers sharing an id would hide a missing probe behind a passing key comparison, which is the failure the parity case exists to prevent.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7533 tests (+1), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The shared reader gave the ESM walk an edge node cannot have (2026-09-03)

<!-- Third bullet of "Shared-specifier follow-ups": "`repoLocalSpecifiers` now follows a relative `require` and the reporter graph is ESM-only — node's default loader is what the guard models, and a `require` in that graph would not load at all. The shared reader returns it, the filter keeps it, and the walk would report a file that node never asked for as missing rather than as impossible." Also its second bullet: "`kind` is returned and no caller reads it." -->

- [DONE: 2026-09-03] [est: 6000] **The reporter graph drops a `require`, and that is what `kind` is for** — making the four import scanners one reader gave `repoLocalSpecifiers` a fifth shape it had never had. The guard models node's OWN loader, which loads that graph as ESM: a `require` there is a call somebody built with `createRequire`, resolved at runtime and absent from the module graph. The text walk would have claimed the edge, node's measured graph would not have carried it, and `missingFromWalk` would have reported the difference as a missing import rather than as the impossible one it is.

  **The first caller to read `kind`.** It was filed the same day as a field nothing consumed — carried, asserted in one case, load-bearing nowhere. The rule that needed it is the one whose subject is a specific loader, which is exactly the distinction a specifier string cannot carry.

  **Three cases pinning what the shared reader changed for this walk**, since the two differences were argued in a doc and asserted nowhere: a `require` ignored, a commented-out import ignored (it was followed before, and node's loader never saw it), a single-quoted specifier read (the old pattern's blindness there was a rule about prettier, not about node).

  `npm run verify` green: typecheck, `lint:all` 19/19, 7532 tests (+3), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## Four rules read the module graph and no two agreed what an import is (2026-09-03)

<!-- Fourth bullet of "Import-address follow-ups": "Three modules now extract module specifiers with three different patterns — `check-analytics-imports.ts`, `check-problem-phrasing-imports.ts` and now `reached-from.ts`, each with its own idea of which spellings count. `lib/check-reporter-graph.ts` has a fourth for relative imports only. Nothing relates them, which is the shape `source-files.ts` was extracted for after eighteen copies." -->

- [DONE: 2026-09-03] [est: 16000] **`lib/import-specifiers.ts`, and the two rules that were quietly checking less than they claimed** — every structural rule here about the module graph eventually writes the same regex, and by the fourth copy no two agreed. The differences were never decisions: each pattern described the shapes its author's tree happened to hold that day, which is exactly how the eighteen source-tree walks came to disagree about `node_modules`.

  **Two of them were wrong, and the shared reader is what says so.** `check-analytics-imports` matched `from`, `require(` and `import(` and NOT the side-effect `import "@/lib/analytics-events"` — a UI file could drag the taxonomy and its description strings into the bundle without naming a binding, invisible to the guard whose whole subject is that. `check-reporter-graph` matched double quotes only (a rule about this repository's prettier config, not about node) and stripped no comments, so a commented-out import was an edge the text walk followed and node's loader did not.

  **Records, not strings, because two callers need more than the specifier.** `kind` (static / side-effect / dynamic / require) and `index` — an offset into the source as passed in, which comes free from `stripComments` blanking to spaces, and is what lets the analytics guard keep printing the real `file:line` and the offending line's text. `specifierIsPackage` and `specifierEndsWithModule` are the two membership rules the callers had written by hand, one by prefix and one by suffix.

  **`check-problem-phrasing-imports` stays where it is**, deliberately: it needs the BINDINGS a clause introduces, not the specifier, which is a different question with a different answer shape.

  **The suite that broke was asserting a proxy.** `strip-comments.test.ts` held four guards to importing the stripper DIRECTLY, as a stand-in for "not through the bundling module"; a guard that reaches it through the shared scanner satisfies the property and failed the proxy. It states the property now — not via `env-inlining` — and accepts either sanctioned address.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7529 tests (+11), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The address was checked by a substring, and only half of it was checked (2026-09-03)

<!-- Second and third bullets of "Reachability-address follow-ups": "'Still names the package' is a substring match on a file's text — `source.includes("nanoid")` passes on a comment, a variable called `nanoidLike`, or an import that was left behind after the call was deleted." and "Nothing checks that `reachedFrom` names ALL the call sites, only that the ones named exist — an entry could list one file while the package is imported in six, and the acceptance would read as argued." -->

- [DONE: 2026-09-03] [est: 14000] **A named call site has to IMPORT the package now, and has to be all of them** — `reachedFrom` is what an entry that ships is held to instead of the bundle, and the thing holding it was `source.includes(pkg)`. That passes on a doc comment about the removal, on a variable called `nanoidLike`, and on an import left behind after the call it served was deleted — the dead address reading as live, which is the one shape the check exists to catch.

  **`importsPackage` asks the question the substring was standing in for.** Comments blanked first (`stripComments`, so nothing shifts), all five spellings of taking a module read by one pattern, and the specifier held to being the package or a subpath of it — so `nanoid-esm` is somebody else's package rather than a match. A type-only import counts: a call site that got narrower is still one the reader should be sent to.

  **The direction nothing asked at all.** The old case walked the LISTED paths, so an entry naming one file while the package was imported in six read as fully argued — the five unread call sites being exactly the ones where the argument might not hold. `evaluateReachedFrom` sweeps the source tree and reports both directions from one pass: a listed path that does not import (`dead`, a deleted file included) and an importer that is not listed (`unlisted`). An entry that has drifted usually has both, which is the case the suite pins.

  **What it cannot say, in the module's own doc.** That the argument at those addresses is right — a call site passing the package attacker-controlled input reads identically to one passing it a constant. And the ways out of any text match: a runtime-built specifier, a re-export elsewhere, a bundler alias. All three surface as an entry naming a path the sweep does not, never as a silent pass.

  **Vacuous today, like the rule it sharpens.** No `shipsToClient: true` entry exists, so every case is a fixture; `nanoid` was the only one there has ever been and it was fixed rather than re-argued.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7518 tests (+15), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The half of the column that shipped (2026-09-02)

<!-- Third bullet of "Ships-to-client follow-ups": "The `shipsToClient: true` half is examined by nothing, and it is the half that shipped — `nanoid` is the one entry that ever reached a browser, and entries like it are skipped here because their argument is about reachability from this app's call sites. That argument is exactly the kind nobody re-derives either, and it is the one where being wrong means shipping a vulnerability rather than mis-labelling a build tool." -->

- [DONE: 2026-09-02] [est: 16000] **An acceptance that ships has to name where it was argued** — `absentFingerprint` made "build-time only" an assertion the build can refute, and skipped every entry claiming it DOES reach the client. That skip was correctly reasoned (no grep settles reachability, and an entry that says it ships would not be refuted by finding it) and its effect was that nothing asked those entries anything at all — which is exactly the state the whole column was in before the fingerprints. It is also the half where being wrong means shipping a vulnerability rather than mislabelling a build tool.

  **The argument is not checkable; its ADDRESS is.** `reachedFrom` lists the call sites the acceptance was argued from, required of every `shipsToClient: true` entry and meaningless on the others. A case holds each path to still existing and still naming the package: an acceptance pointing at a file that was refactored away reads exactly like one somebody re-read this morning.

  **Four findings, not one.** An entry that ships and names nothing (`unargued`); an entry that says build-time-only and names call sites anyway (`misplaced`, because the two fields answer different questions and both being set means one is wrong); plus the two the guard already had. `reachedFrom: []` is treated as no list at all — an empty array is what a shape rule satisfied to satisfy the compiler looks like.

  **The OK line only mentions reachability when there is some.** "0 reachability claims" on every green run is the noise the audit gate's OK line was rewritten to stop printing, and a case asserts the green path says nothing about it on today's tree.

  **Vacuous today, deliberately.** The baseline has no `shipsToClient: true` entry — `nanoid` was the only one there has ever been, and it was fixed rather than re-argued on 2026-09-02. The rule is here before it is needed, so the day one is added its call sites are asked for by a red run rather than by a reviewer noticing. The behaviour is pinned by fixtures, which is the only way a rule with no live subject can be pinned at all.

  SECURITY.md's ships-to-client section states the "Yes" half beside the "No" half it already documented.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7503 tests (+7), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## Two lists describing one property, related at last (2026-09-02)

<!-- First bullet of "Marker-parity follow-ups": "Two lists of network markers now exist and nothing relates them — `NETWORK_MARKERS` in `helpers/gate-legs.ts` is what the scan reads in a script's TEXT, and the bootstrap's three patches are what the suites are refused at RUNTIME. They agree today because one was written from the other by hand; a fifth marker added to either is invisible to the other." -->

- [DONE: 2026-09-02] [est: 10000] **The runtime refusal's population comes from the scan's marker list now** — one list says what a read outside the tree LOOKS LIKE in a gate script, the other says what a suite is refused at runtime, and they agreed because somebody copied one into the other that morning. A fifth marker added to either was invisible to the other, which is the arrangement four `.tasks/` entries this week are about.

  **A probe per marker, keyed by the marker's own label.** `PROBES` maps each `NETWORK_MARKERS` entry to a call that must throw, and a case asserts the two key sets are EQUAL — a marker with no probe is a shape the suites can still perform, a probe with no marker is a refusal nothing asks for. The population is derived; only the probe body is written by hand, which is the half a person has to write anyway.

  **Parity forced the refusal to grow.** Two of the four markers are spawns — `npm audit` asking the registry about advisories, `npx expo install --check` asking it about versions — and the bootstrap refused neither. `npm` and `npx` join `curl` and `wget` in `NETWORK_TOOLS`; no suite spawns either, and the guard fixtures run `node --import tsx` directly, which is a read of this tree rather than of a registry.

  **The gate's own leg is the one marker that stays allowed.** `check-audit-baseline` performs the `npm audit`; what is refused is a SUITE performing one, which is a different claim about a different process, and the note the whole arrangement protects is about the gate.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7496 tests (+2), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The other three ways out of the tree (2026-09-02)

<!-- First bullet of "Network-refusal follow-ups": "`fetch` is refused and `node:http`, `node:https` and a spawned `curl` are not — the audit gate's own marker list names all four, and the runtime refusal covers one. Nothing in the suites uses the other three today, which is exactly the argument that was made about `fetch` before the refusal existed." -->

- [DONE: 2026-09-02] [est: 12000] **All four of the audit scan's markers are refused at runtime now, not one** — the scan that measures "the one check here whose answer can change while the repository does not" reads four shapes: an `npm audit` (the gate that is allowed to), a `fetch`, an http/https client, a spawned tool. The suites' refusal covered the second and left the other two exactly as invisible as `fetch` had been that morning — which was the argument for the refusal in the first place.

  **`http.request`, `http.get` and their https twins, patched on the module object.** Not a loader hook: node's own runner and `tsx` load these too, and a refusal that took the module out from under them would fail a run for a reason that is not a test's.

  **A NAMED import is covered, and that is a fact about this runner rather than about JavaScript.** `tsx` transpiles the suites to CommonJS, so `import { request } from "node:https"` compiles to a property read at CALL time and sees the patch; under a native-ESM runner it would be a binding made when the builtin was evaluated, and would not. A case pins that shape, so the day it changes the suite goes red instead of quiet.

  **A spawned `curl` or `wget`, by tool name only.** Refusing spawning outright would break the guard-fixture suites, which run `node`, `tsx` and `git` dozens of times — and those are reads of THIS TREE, which is the thing the whole rule is about. A case asserts an ordinary `node -e` spawn still goes through.

  **7494 tests ran against all three refusals with no finding**, which is the measurement rather than the hope: no suite in this tree opens an http client or shells a network tool today, and now nothing can start to without saying so.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7494 tests (+4), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The leg with the most code in it was the one nothing looked at (2026-09-02)

<!-- First bullet of "Hermetic-leg follow-ups", filed twice: "The suites are a leg of the gate and the scan skips all of them — `npm test` runs 1412 files and the rule covers the 23 CLI wrappers only, because the suites stub `fetch` by name in dozens of places and a marker scan cannot tell a stub from a call. So a suite that really made a request is exactly as invisible to this rule as it was yesterday, and `npm test` is the leg most likely to acquire one." -->

- [DONE: 2026-09-02] [est: 15000] **A test that reaches the network fails now, instead of being invisible** — `PUBLISHED_ELSEWHERE_NOTE` tells every contributor the audit gate is "the one check here whose answer can change while the repository does not", and the scan that measures that claim reads 27 CLI wrappers and skips 1420 suite files. The leg with the most code in it, and the one most likely to acquire a real request, was the leg the claim rested on and nothing examined.

  **It was filed twice as a gap in the SCAN, and it is not a scan problem.** Whether a call goes out is a runtime fact. `__tests__/test-globals.ts` is already preloaded into every test process by `--import`, before any suite loads, so it replaces `globalThis.fetch` with something that throws — naming the URL, saying what to do, and naming the file that installed it.

  **Stubbing still works, and that is the design.** A suite that assigns its own `globalThis.fetch` has said what the response is and nothing leaves the process either way. What the refusal catches is the call NOBODY stubbed: a helper that grew a request, a module that fetches on import, a stub installed for one case and reached from another — the shapes no reader spots in a diff.

  **Throws rather than rejecting**, because a rejected promise is something a `.catch` swallows and half the value is the failure landing in the case that made the call. It reads the URL out of all three shapes `fetch` takes (string, `URL`, a request object) — "a request" would send the reader through 1420 files looking for the caller.

  **Installed at module scope, never in a `beforeEach`**, and a case pins that: a per-test reinstall would overwrite the stub a suite deliberately put in place in its own `before`. A case also asserts all three test scripts still preload the bootstrap — a bootstrap a suite has to import itself only covers the suites that remembered.

  **7490 suites ran against it with no finding**, which is the measurement: the claim about the `test` leg was true, and was true by nobody's action until today.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7490 tests (+7), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The fourth tree, and the read nobody had classified (2026-09-02)

<!-- Fifth bullet of "Ships-to-client follow-ups": "The guard reads `node_modules` from a test and no rule says whether that is allowed — `sourceFiles`, `suiteFiles` and `GUARD_SCANS` all describe walks over the repository's own code; this one walks an installed dependency, which is neither source nor `dist/`. It works and it is unclassified, which is how the source-directory walk got written eighteen times before somebody named the rule." -->

- [DONE: 2026-09-02] [est: 17000] **`node_modules` is a named tree now, with one door into it** — three trees the suites read had a name and a helper each: the app's source, the suites, and `dist/`. The installed tree had neither, and the difference is not filing: the first three are facts about THIS REPOSITORY, where the same commit answers the same next year, and this one is a fact about what npm resolved on the machine the run happens on. A lockfile bump changes it with no diff in anything else a suite reads, which is the sentence a contributor debugging a red `ships-to-client` fingerprint needs before they start reading their own diff.

  **Two known reads, and the sweep found three more.** `ships-to-client.test.ts` (a package's own code) and `check-comment-terminators.test.ts` (`.bin/tsc`) were the ones the follow-up named. Writing the rule turned up `guard-fixture-refusals.test.ts` twice — an installed package root and the installed tree's own root — and `rnw-modal-premise.test.ts`, which spelled it as a `["node_modules", …]` array spread into `repoPath`. Five reads, four suites, three spellings, every one decided independently to be fine.

  **`helpers/installed-packages.ts` is the door**: `installedPackagePath` (scoped names split themselves), `installedPackageFiles` (the package's OWN JavaScript, nested dependencies skipped), `readInstalledFile`, `installedBin` for npm's shim directory, and `installedRoot` for the caller whose subject is the directory. No walk of the whole installed tree: nothing needs one, it is the slowest read available here, and offering it would make "scan every dependency" a one-liner for a suite that should be asking something narrower.

  **The rule is the CONJUNCTION, not the word.** Joining `node_modules` onto the repository root — `repoPath("node_modules"` or `path.join(REPO_ROOT, "node_modules"`. Six suites mention the directory for reasons that are not a read: a `skipDirs` entry so a walk does NOT enter it, a fake resolution chain under `mkdtemp`, a KEY in package-lock.json, a nested path in a fixture. Banning the word would have taken ten exemptions, which is a list rather than a rule; four cases pin those four shapes as things the rule must stay silent about.

  **Two exemptions, both files that plant the shape without performing it** — `lint-guard-premise.test.ts` proving its own tsx-bin sweep fires, and this suite proving this rule does. A pattern nobody can demonstrate firing is one nobody can trust.

  **The repo's own rules caught the first draft twice.** `source-files-helper.test.ts` reads a `readdirSync` beside a source-directory literal as a hand-written source walk, and this suite's `path.join("lib", "css-syntax-error.js")` was one — a path inside `postcss`, not a directory here. The file names that fixture by basename now. And the first draft's own `repoPath("node_modules")` sat under an exemption whose stated reason was "plants the shape in a string", which it was not.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7483 tests (+12), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The half of the green line that records a human decision (2026-09-02)

<!-- Third bullet of "OK-line follow-ups": "`stillPresent` is still a bare count on the OK line while `majorOnly` got a vocabulary — '4 accepted high/critical advisories' says nothing about which packages they are or whether the exemptions still make sense, and it is the half of the line that represents a human decision rather than npm's. The half nobody argued about got the redesign." -->

- [DONE: 2026-09-02] [est: 14000] **The OK line names the packages it is still accepting** — the major-only half was regrouped by upgrade the day before and the accepted half stayed "4 accepted high/critical advisories", a number with nothing to look up. Every key behind it is an advisory somebody READ, wrote a `why` for and accepted; the count is the one thing about it that is not a decision.

  **Grouped by the VULNERABLE package, where the other half groups by the upgrade, and the difference is the point.** `majorOnlySummary`'s reader is about to run `npm update`, so it names what npm would move; this reader is about to re-read an argument in SECURITY.md, which is indexed by the package the advisory is IN. The same advisory is `postcss` here and `expo` there, and the line now says both — before: `4 accepted high/critical advisories`; after: `4 still accepted, in image-size (2), postcss (2)`.

  **Bounded by the baseline, which is the only list in this report a person curates.** `majorOnly` is as long as npm's answer today; this is as long as somebody has been willing to write `why` sentences for, which is the bound the previous OK-line task went looking for.

  **Three clauses, one per question a green run answers, separated by semicolons.** Is anything new, what am I still accepting, what would clearing the rest cost. Two of the three END in a comma-separated package list, and a comma joining the clauses put a list's last entry and the next clause in the same punctuation — which is how a reader loses track of which list they are still in.

  **`groupByPackage` is shared rather than written twice.** Not for the six lines: the two clauses are halves of one sentence read on the same run, and a grouping that drifted between them would print two shapes for one idea — the drift the OK-line rewrite exists to stop.

  **The ordering case builds the verdict by hand**, because no report produces a group order that would expose "widest first, then by name" — the second time this suite has had to, and for the same reason.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7471 tests (+4), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The sentence about the gate's size, in five places and derived in none (2026-09-02)

<!-- Fourth bullet of "Ships-to-client follow-ups": "Adding a ninth `verify` leg made four prose copies stale and a grep found them, not a check — CLAUDE.md's step list, SECURITY.md's 'eight legs', and two suite headers. `verify-gate-script.test.ts` derives the step list from ci.yml and so grew silently and correctly; every sentence ABOUT the gate is hand-written and did not. That is the fifth filing of this pattern." -->

- [DONE: 2026-09-02] [est: 20000] **The gate's leg count is counted now, wherever it is written down** — `verify` has nine legs, and that number is stated in prose in five places: CLAUDE.md's Commands section, SECURITY.md's "a red gate here may not be your branch's fault", the doc comment above `PUBLISHED_ELSEWHERE_NOTE`, and two suite headers. Every one was written by hand on the day its author added a leg. `lint:ships-to-client` became the ninth on 2026-09-02 and four of them said EIGHT until somebody thought to grep.

  **`__tests__/helpers/gate-legs.ts` is what they are checked against.** It expands `verify`'s npm-script chain to its leaves — a script that runs a command rather than delegating — so `typecheck` is a leg and `lint:ci` is not, which is the population the prose means and the one a contributor watches go past. `pre*` hooks are spliced in where npm runs them and a script reached twice keeps its FIRST position: `typecheck` arrives through `lint:ci` and again through `pretest`, and it is one leg that runs once.

  **Three numbers, all derived, plus the ordered list.** The count ("nine legs"); the HERMETIC count, which is the count minus the legs a network-marker scan fires on, and is what "the other eight read the tree" means; and the NEXT position, where prose names one ("a tenth leg that shelled out to a registry would make this sentence false"). CLAUDE.md's arrow list is built from `GATE_LEG_LABELS` rather than compared against a copy of itself, so a leg added with no name fails rather than printing its npm script.

  **Three sentences were already stale and the rule found them.** All three said "a ninth leg" — written when there were eight, true for one day, wrong since `lint:ships-to-client` landed. `lib/audit-baseline.ts`, `verify-gate-script.test.ts` twice, CLAUDE.md once. That is the fifth filing of this pattern and the first where the copies were found by a check instead of by a grep somebody remembered to run.

  **The markers moved into the helper rather than being written twice.** `verify-gate-script.test.ts` owned `NETWORK_MARKERS` and its own `scripts/*.ts` walk; the hermetic count needs the same scan, and two copies would have let the two rules disagree about which legs are hermetic — the exact drift both files exist to catch.

  **Proved against a broken tree twice.** `SECURITY.md` edited to say "eight legs" names that file and that phrase; a tenth leg spliced into `verify:dist` turns five claims red at once and reports the unnamed leg separately. The anti-vacuous half is a floor of six claims in four files, because a table of patterns that stops matching agrees with every document it can no longer read.

  **A leg that reads across a wrapped line was invisible to the first draft.** `lib/audit-baseline.ts` wraps "A\n * ninth leg", which reads as `A * ninth leg` until the JSDoc continuation asterisks go — four of the five copies matched and the fifth did not, silently. `prose()` strips comment furniture and joins lines, and a case pins both wrapped sentences by name.

  **`.tasks/` is deliberately not swept**: its entries are dated records, and "the eight legs" in a 2026-09-01 entry is correct history.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7467 tests (+11), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. The audit gate was red on an advisory published overnight (`@xmldom/xmldom#GHSA-6gmq-8vp8-gcm6`, fixable in range); `npm update @xmldom/xmldom` cleared it and the lockfile is in this commit. No DB migration, no creds.

## The column that was a sentence (2026-09-02)

<!-- Third bullet of "Every-severity-fix follow-ups": "`shipsToClient` is still a sentence nobody re-derives, and the tooling to check it now exists twice over ... A guard that greps `dist/` for each accepted package's fingerprint would make the column a measurement; the honest difficulty is that a minified bundle carries no package names, which is exactly why `image-size`'s entry has to explain that the string in the bundle is an icon name." -->

- [DONE: 2026-09-02] [est: 22000] **`shipsToClient` is checked against the build now** — it is the half of every exemption that makes a high advisory acceptable rather than urgent ("the vulnerable code never runs where a user's input can reach it"), and nothing re-derived it. It was written by whoever added the entry, on the day they added it, and stayed true by nobody's action. The one entry that ever DID ship was `nanoid`, found by a person reading call sites on a list the gate had no opinion about.

  **A fingerprint, because the name cannot work.** A minified bundle carries no package names — that is what minification is — so grepping `dist/` for `postcss` answers nothing. It carries STRINGS: a minifier renames every symbol and rewrites no string literal. Each `shipsToClient: false` entry names one literal out of its own package (`CssSyntaxError`; `invalid invocation. input should be a Uint8Array`) and `check-ships-to-client` greps all twelve chunks for it. `image-size` is the case that proves the point — the bundle contains `image-size-select-actual`, an icon name, and its entry has had to explain that in prose since the day it was written.

  **What it can and cannot say, in the module doc and in SECURITY.md.** A hit is not proof of the package; a coincidental literal reads the same, so the report prints the string it found and leaves the chunk to a person. A miss is weaker still: it says this fingerprint is absent, not that no code from the package shipped. The claim moves from "somebody wrote this down" to "the build does not contradict it", and nothing here pretends to more.

  **An entry with nothing to look for FAILS rather than passing quietly** — that shape is exactly the state the whole column was in, and it is the one an entry added next week will be in by default.

  **The half the guard cannot check about itself is a case.** A fingerprint that is not really in the package can never appear in a bundle either, so the guard would pass forever while reporting the claim as measured; the suite reads `node_modules/<pkg>` (its own code, its nested dependencies excluded) and fails if the string is not there.

  **Proved against the real bundle, not only fixtures.** Pointing the evaluator at the exported chunks with `image-size-select-actual` planted as the fingerprint produces the finding and names the chunk — so the pass on the real fingerprints is a measurement rather than a scan that finds nothing.

  **The ninth leg made four prose copies stale and they were found by grep, not by a check.** CLAUDE.md said EIGHT steps and listed them, SECURITY.md said "`verify`'s eight legs", and two suite headers plus the audit note said "the other seven read the tree". All updated; that nothing relates one copy to another is the fifth time this has been filed.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7456 tests (+15), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## How much a passing run is allowed to say (2026-09-02)

<!-- Fourth bullet of "Every-severity-fix follow-ups": "Widening the fix rule widened `majorOnly` from 4 advisories to 8, and that list is printed on every green run — it is informational and it is now half the OK line ... The line is already long enough to wrap; the next widening makes it noise, and noise on the green path is how the previous version of this gate stopped being read. Nothing decides how much a passing run is allowed to say." Taken together with the third bullet of "Update-target follow-ups": "The `majorOnly` line still names vulnerable packages while the fix line names roots." -->

- [DONE: 2026-09-02] [est: 16000] **The OK line says UPGRADES now, and that is what bounds it** — it listed every major-only advisory by `package#GHSA` and severity. Four names when the fix rule read high/critical, EIGHT the day it widened to every severity, and the growth had no ceiling: the line's length was set by how many advisories the tree carried. It wrapped. Half of it was moderates the baseline deliberately does not triage, printed on a run where nothing is wrong.

  **Grouping by the update package answers both open bullets at once.** The count is bounded by how many packages would clear the advisories rather than by how many advisories there are — the real tree's eight over five vulnerable packages are two upgrades — and the green path stops speaking a different language from the red one, which has named npm's own fix target since earlier today. Before: 8 keys and 8 severities. After: `expo (7, up to high), expo-router (1, up to moderate)`.

  **Count and worst severity, because those are what a reader acts on** — "is any of this high?" and "how much does that one upgrade buy?". The advisory ids are what a FAILING run prints; a case asserts no `GHSA-` appears on the green path at all, because the id list was the noise.

  **The worst severity is derived, not read off position 0.** It is first today only because `majorOnly` arrives sorted, which is luck the summary must not depend on. No report can produce the disordered group, so that case builds the verdict by hand — the one place in this suite where that is the honest fixture rather than a shortcut.

  **`severityRank` came out of `bySeverityThenKey` rather than being written twice.** Two orderings now read the same function, including its decision that a severity npm has not used yet sorts to the bottom instead of throwing.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7441 tests (+7), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## The sentence about the other seven legs (2026-09-02)

<!-- Fourth bullet of "Live-registry-note follow-ups": "Eight `verify` legs and no list of which ones are hermetic — the note's claim ('the one check here whose answer can change while the repository does not') is true today and is checked by nothing ... a ninth leg that shelled out to a network would make the sentence false, silently, in the message that exists to be trusted." -->

- [DONE: 2026-09-02] [est: 15000] **`PUBLISHED_ELSEWHERE_NOTE`'s claim about the other legs is measured now, not remembered** — it rides every failure of the audit gate telling a contributor this is "the one check here whose answer can change while the repository does not". That was true when it was written, nothing checked it, and the cost of it going stale falls on whoever meets a red audit gate next rather than on whoever added the ninth leg.

  **The scan is over what the gate RUNS, derived twice.** `resolveScript("verify")` already expands the npm-script chain, so the `scripts/*.ts` paths come out of the expansion; `lint:all` spawns its guards one at a time, so `LINT_GUARDS` supplies the rest. Twenty-three files, and a case asserts the population is real — a resolver that stopped matching would satisfy "nothing reaches the network" perfectly.

  **The markers are the REMOTE read, not the shell-out.** `npx tsx` is how `lint-all` spawns all nineteen guards and `tsx` is a devDependency, so spawning is not the property. `npm audit` asking the registry about advisories, `expo install --check` asking it about compatible versions, a `fetch`, an `http`/`https` import: four markers, each named so the failure says what it found rather than which pattern matched.

  **Two controls, because a marker set that fires on nothing passes everything.** `db:find-duplicates` and `sentry:check` both call `fetch` and neither is reachable from `verify`, so the same scan over all of `scripts/` must find them — proof the markers work, without being findings. And the rule was run against a deliberately broken tree: a `fetch` added to `check-inline-hex.ts` turns the case red and names the file.

  **Pinned against the note itself rather than a paraphrase.** A rewrite that dropped "the one check here" would otherwise leave the scan asserting a property nothing depends on; the note's doc comment now points back at the case.

  **The first draft walked `scripts/` by hand and the repo's own guard caught it** — `source-files-helper.test.ts` bans `readdirSync` beside a source-directory literal, which is exactly what it was. It goes through `sourceFiles("scripts")` now.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7434 tests (+5), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## The command that ran clean and fixed nothing (2026-09-02)

<!-- Second bullet of "Every-severity-fix follow-ups": "`npm update <pkg>` is the command the gate prints and it was wrong for one of the three — `esbuild`'s fix needed `npm update tsx` ... a contributor following the printed instruction literally will see it no-op and have to work out why. npm's own `npm audit fix` knows the answer; nothing here reads it." -->

- [DONE: 2026-09-02] [est: 18000] **The audit gate prints a package `npm update` can actually move** — the fix rule's first run in anger demanded three updates and one of them was wrong: `esbuild`'s in-range fix was `npm update tsx`, because `tsx` pinned `esbuild@~0.27.0` while the fix was `>=0.28.1`. `npm update esbuild` exits 0 having changed nothing, which is the worst thing a fix instruction can do — a contributor who follows it literally has no way to tell it did not work except by re-running the gate.

  **The report answered twice over and neither was read.** `fixAvailable`'s object shape carries the `name` of the install npm would perform, and the field was read for `fixKind` with its name thrown away. Where npm names nobody — the `esbuild` case, where it said a bare `true` — `effects` lists the dependents the advisory reaches and `isDirect` says which the manifest declares. `fixPackage` reads npm's name first, walks `effects` second, and falls back to the vulnerable package third, which is the direction `fixKind` already takes on a shape npm has not used yet.

  **Measured on the tree it was written against**, not only on fixtures: eight major-only advisories across five vulnerable packages resolve onto two roots, `expo` and `expo-router`, where the old rule would have named all five. `fixCommandPackages` now dedupes across roots as well as within one.

  **The walk terminates on the cycle this tree really has.** `metro` and `metro-config` list each other in `effects`; without the visited set the gate hangs rather than fails, which is a worse failure than the one being fixed. Breadth-first and sorted at each step, so a package two chains reach gets the same answer on every run and the nearest direct dependent wins — a case pins both orderings of the same diamond.

  **The redirect is printed only when there is one.** `(fix in tsx)` rides the finding whose fix moved and nothing else; on every line it would stop being read on the one line where it is the whole answer, which is how the previous version of this gate stopped being read.

  **SECURITY.md had the finding written down as prose** — "npm's in-range fix is not always an update of the package the advisory names", worked out by hand on 2026-09-01 while the gate went on printing the wrong command. It now records that the gate reads it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7429 tests (+10), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## The run that fails is not the run that caused it (2026-09-02)

<!-- Third bullet of "In-range-fix follow-ups": "The gate is the eighth `verify` leg and the only one that changes its verdict when nothing in the repo changed — an advisory published overnight turns a green tree red with no commit in between, which is the point and is also a new failure shape for contributors ... Nothing in the message says so, and 'your PR did not do this' is one sentence the failure could carry." -->

- [DONE: 2026-09-02] [est: 14000] **Every failure of the audit gate now ends by saying it reads a live registry** — seven of `verify`'s eight legs read the tree, and the same commit gives them the same answer next year. This one asks npm what the world knows about these packages TODAY, so an advisory published overnight, a fix published overnight, or an advisory withdrawn turns a green tree red with no commit in between. That is the point of the gate and it is also a failure shape nobody here had met: every other red in this repository means "your diff did this", which is exactly how a contributor will read this one.

  **One sentence is the whole difference** between hunting your own diff and reading the finding. `PUBLISHED_ELSEWHERE_NOTE` is exported rather than inlined, so the suite asserts the text a contributor sees rather than a paraphrase of it.

  **On all three failing paths, not just the untriaged one.** An advisory published makes `unexpected`, a fix published makes `fixableInRange`, an advisory withdrawn makes `stale`. Filing the note under "new advisory" would have left the two failures this gate produces most often without it — and the widening earlier today made an overnight red MORE likely, not less, by taking the fix rule from two severities to five.

  **It must not read as permission to merge past the gate**, which is the direction "your PR did not do this" slides in on its own. The sentence ends with the fix belonging on the branch that met it — the tree is only green when it is green today — and a case pins that clause, because a note that excused the red would be worse than no note.

  **Position is the difference between context and an excuse.** It is the LAST line, after the findings and after the instruction that fixes them, and a case asserts both: the note is last, and the finding is printed above it. A reader who stops at the first line sees what is wrong.

  Absent from a passing run, where it would be noise on the green path — which is how the previous version of this gate stopped being read.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7419 tests (+5), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## The strongest evidence in the migration lived in a terminal (2026-09-02)

<!-- Fifth bullet of "Derived-locale follow-ups": "The seventh-language control had to be run by hand, by editing `lib/i18n-context.tsx` and reverting it — the strongest evidence in this task exists only in a terminal. A fixture picker with seven languages, fed to the same helpers, would make it a case; the helpers already take a source string (`locales(TWO_LOCALES)` is tested that way), so nothing structural is in the way." -->

- [DONE: 2026-09-02] [est: 17000] **The seventh language is a case now, not something somebody did once and reverted** — the whole argument for deriving the locale list from the picker rests on what happens when a seventh arrives, and the proof of it was a terminal session: `lib/i18n-context.tsx` was edited to add an `it` row, nineteen migrated suites were run, the edit was reverted. Nothing in the tree recorded that it had happened, so the claim was back to being a sentence in a doc comment.

  **The fixture edits the REAL picker, because that is what was measured.** The helpers take source text, so the hand-edit is a value: `withSeventhLanguage` inserts the row after the last one and asserts it found it, so a picker whose shape changes fails loudly rather than silently returning the six-language source and passing.

  **The assertion is the property the messages share, not either wording.** Seven helpers throw on the missing map and they do not agree on the sentence — `localeBodies` and `localeValuesOf` say "no translation map for 'it'", `localeKeys` says "no `const it` translation map in the source". Matching either one would have been a case about a string. It asserts instead that the message names the language AND says the map is what is missing, which is exactly the difference from the count's "expected 6, got 7" — a message that reads as the new locale being wrong.

  **The green half is what makes the red half mean anything.** `withSeventhLocaleMap` adds an `it` map that declares the key for itself, and the same helpers pass across seven locales. Without it, a helper that simply refused any list longer than the one it was written against would satisfy the redness.

  **Two assertions were written the way the guards forbid and the guards said so.** A first draft asserted `locales(source).length` was 6, which `COUNTS_LOCALES` reports as the retired habit — correctly, in the one file whose subject is that habit. Both are derived now (`[...locales(source), "it"]`), and the negative control asks `!locales(source).includes("it")` rather than counting.

  **The shared key is read out of the file rather than named.** A hardcoded key would put this case on the copy's critical path, which is the failure the migration was about; it picks the first key every locale declares for itself.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7414 tests (+4), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## Three roots npm could fix and nobody was allowed to ask (2026-09-02)

<!-- First bullet of "In-range-fix follow-ups": "The rule reads high/critical only, and THREE moderate/low roots have an in-range fix available right now — `dompurify`, `esbuild` and `undici` ... reporting them is a filter change rather than a mechanism; making them BLOCKING is the separate decision, and the reason this task did not just do it." This took the decision. -->

- [DONE: 2026-09-02] [est: 38000] **The fix rule now reads every severity, and the three roots it could not see are fixed** — the rule shipped the day before asking npm "can you clear this?" of high and critical only, because it was written inside a high/critical baseline and inherited its filter without anybody choosing it. Measured the next morning: three roots had an in-range fix waiting and not one of them was high. `dompurify` (low + moderate), `undici` (three moderates), `esbuild` (low). Ten of the tree's fourteen distinct advisories were moderate or low and nothing was asking about any of them.

  **The bullet said "report them"; the measurement says block.** `postcss` is the argument: moderate when it was first triaged in June, high by August, on a lockfile nobody had touched. A moderate with a published fix is a high with a published fix that has not been re-scored yet, and the run that would have noticed is the one where it was still one command. Reporting is what the previous version of this gate did for two months while thirteen advisories accumulated.

  **Two severity scopes, and the split is the whole design.** Needing a triage sentence is expensive, so `unexpected`/`stillPresent`/`stale` stay high/critical — widening those would demand a `why` paragraph for ten advisories nobody has argued about, which is the exemption-list-as-paperwork failure this file already carries a scar from. Needing a lockfile bump costs one command, so `fixableInRange` reads all five. SECURITY.md states it as a two-row table because "it only cares about highs" is the wrong reading that cost the month.

  **One walk, two populations.** `observedAdvisoryDetails` is the severity-blind traversal and holds the four conditions that decide what is observable; `observedAdvisoryFixes` is it filtered to the triaged severities. The filter moved OUT of the walk on purpose — it is a fact about the exemption list, not about the audit, and leaving it inside is exactly how the fix rule got a filter it never asked for.

  **`npm update esbuild` moved nothing, and npm was still right.** `tsx` pins `esbuild@~0.27.0` and the fix is `>=0.28.1`; npm reported `fixAvailable: true` because `tsx@4.23.13` is inside the root's own `^4.21.0`. So npm's in-range fix is not always an update of the package the advisory names — written into `fixCommandPackages`' doc comment and into SECURITY.md, because the command the gate prints is a starting point rather than a guarantee.

  **Findings are ordered most-severe-first with a key tiebreak.** Without it the order is `Object.entries` order, which is lockfile order, and a findings list that reshuffles between runs is one nobody can diff. An unknown severity sorts to the bottom rather than throwing — the same direction `fixKind` takes with an unrecognised fix shape.

  Lockfile: `dompurify` 3.4.11→3.4.14, `undici` 6.27.0→6.28.0, `tsx` 4.21.0→4.23.13 (pulling `esbuild` 0.28.2 and its platform binaries). Nothing else moved. All four remaining advisories are `expo@57`/`expo-router@5` majors, so the gate is green on a tree with zero in-range fixes at any severity.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7393 tests (+14), audit baseline (4 accepted, 8 major-only reported at two severities), build, bundle secrets, bundle size (18.3 KiB headroom, unchanged), bundle smoke. No DB migration, no creds.

## The exemption was defended by a claim about English (2026-09-02)

<!-- First bullet of "Comment-span follow-ups", filed one task earlier: "The JSX exemption is a two-character shape and nothing checks it stays the right one ... The exemption is defended by 'no paragraph starts that way', which is a claim about English rather than about the tree, and no case can plant a counter-example without inventing one." -->

- [DONE: 2026-09-02] [est: 48000] **A second rule now stands behind the JSX exemption, so the skip does not have to be right** — the terminator guard skips a comment opened straight after a `{` whose trailing text starts with `}`, because that is the wrapped JSX comment container and there are twelve of them in this tree. The defence of that skip was a sentence about English: no orphaned paragraph begins with a brace. Nobody can write the counter-example without inventing it, and inventing it is the same thing as nobody being able to check it.

  **The counter-example exists and the first rule is blind to it.** A comment container whose body writes the glob out, wrapped, leaves prose starting with a brace after the terminator — reproduced against the working tree before anything was written, and the early rule returned an empty list. The case now asserts that emptiness FIRST, so a change that stopped reaching the exemption would say so instead of quietly testing a different path.

  **What was added is not a better exemption, it is a rule that does not need one.** A comment that ends early leaves its ORIGINAL terminator standing in what is now code, and a terminator in code is a thing no valid program contains — the compiler rejects it wherever it lands. `findOrphanTerminators` looks for exactly that. The broken container is named by its wreckage, and the exemption can be as wrong as it likes.

  **The literals had to become spans for it to be honest.** `scanComments` reported comments; the complement it left was code AND every literal, so a scan for the two characters would have reported this suite's own fixtures, the strings in `SECURITY.md`-style prose, and any pattern holding them in a character class. `scanSpans` reports all six kinds now — line, block, single, double, template, regex — from the one state machine that was already tracking every one of them for its own reasons, and `scanComments` is a FILTER over it rather than a second walk. `codeOffsets` is the complement as a lookup.

  **Cause and symptom, never both.** An early terminator is the reason its file's orphans exist, so `orphansWithoutCause` drops them: one offence reported twice, once by what broke it and once by the wreckage, is one report too many, and only the first is actionable. What survives is exactly the case the second rule was added for. The two carry different advice for the same reason — a reader looking at an orphan needs to be told to look UP, not to stop writing globs — and a case asserts the orphan report does not contain the other sentence.

  **The literal-span extension found nothing wrong and stated three things that were only true by luck.** An unterminated `'` or `"` is cut by a newline and is now `closed: false` with the newline outside it; a template literal is the one quote allowed to span lines and stays closed; a regex the scanner opened on what turned out to be a division ends unclosed at the newline, which is the same boundary rule a line comment gets. All three were behaviours of the machine with nothing asserting them.

  Measured across 812 files: zero findings from either rule.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7386 tests (+17), audit baseline (4 accepted, all major-only), build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## Nineteen suites asked about six of seven languages (2026-09-02)

<!-- First bullet of "Guard-registration follow-ups", generalised: "every registry-shaped suite here has the same blind spot". The audit that bullet asked for found the pattern was NOT widespread in the guard fleet — and was everywhere in i18n. -->

- [DONE: 2026-09-02] [est: 44000] **Nineteen suites restated the language list, and `helpers/i18n-locales.ts` was written to end exactly that** — its own doc comment says why: "the six was a literal. A seventh language turns every one of these cases red at once with 'expected 6, got 7', which reads as the new locale being wrong rather than as twenty assertions needing their number bumped." It fixed the call sites it replaced and did not stop new ones appearing. Nineteen files carried the list again, seventeen of them iterating it against the very source the picker is read from.

  **The failure is silence, not redness.** A seventh language would have left every one of them asking about six of seven: not red, not skipped, just quietly incomplete. Proven rather than argued — a seventh picker entry (`it`) added to `lib/i18n-context.tsx` turns the migrated suites red immediately, naming the missing map ("no `const it` translation map in the source"), and they were green on the same edit before the migration.

  **The audit that started this found the OPPOSITE of what the suggestion claimed.** The bullet generalised from one guard-fleet bug to "every registry-shaped suite has the same blind spot". A scan of all 124 table consts in `__tests__` found the guard fleet already sound and the real cluster somewhere else entirely. Writing the over-general claim down is what made it checkable.

  **A helper is a thing people can use; a rule is what makes not using it visible.** `i18n-locale-list-restated.test.ts` sweeps every suite for the codes written out, matched on the SET rather than a sequence — the picker's order and the alphabetical order most of these used are the same mistake, and a third ordering would be too. It carries its own positive control (the shapes it must catch) and a negative one (an ordinary two-string tuple it must not).

  **Four exemptions, each a different reason, and the two that matter are pins.** `i18n-source-file.test.ts` and `i18n-locales-helper.test.ts` assert the real picker yields exactly those codes — without them the whole fleet would derive from a picker nothing checks, agreeing perfectly with a list that had lost a language. A case re-reads both and fails if either stops pinning. `i18n-source.test.ts` feeds literals to the parser, so its codes are a fixture's contents. `privacy-languages.test.ts` writes out `PrivacyPageLanguageCode` — a DIFFERENT population that happens to hold the same six today, which is exactly why it needs saying.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7379 tests (+5), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## Four registries a guard must join, discovered one failure at a time (2026-09-02)

<!-- Third bullet of "Comment-terminator follow-ups": "Four conformance registries now need an entry when a guard lands, and none of them is reachable from the others". -->

- [DONE: 2026-09-02] [est: 33000] **`guard-registration.test.ts` names every registry a guard is still missing from, in one failure instead of four** — landing `lint:reporter-graph` and then `lint:comment-terminators` each cost four red runs, one per registry, in whatever order the concurrent suites happened to fail in: a `SCANNED_FLOORS` entry, a partial-root fixture, a `GUARD_SCANS` entry, a planted-offender case. Each message was clear and each arrived ALONE, so the checklist existed only as a sequence of failures — you learn the fourth item by fixing the third.

  **It replaces nothing.** Each owning assertion knows things this cannot and its message is better than a list; this answers a different question ("which registries does THIS guard still need") and answers all of it before the first specific failure is fixed.

  **Membership is a condition, not a blanket.** A guard that reads two named files has no scan list and no walk whose base could be wrong, so each registry carries the predicate that decides who belongs — read off the guard's own wrapper, deliberately duplicated rather than shared, because a shared predicate that drifted would take both the specific check and this one with it. A case asserts at least two predicates still distinguish somebody, or the file would be five copies of "is it mentioned anywhere".

  **The registries are read as TEXT, and that is the right question.** They are consts inside `*.test.ts` files, so importing one would register that suite's describes a second time under this file's name. A guard NAMED in an exemption list has been decided about, which is what this asks; a stricter check would have to know each registry's escape hatches and would go stale as they changed.

  **It found two pre-existing gaps on its first run.** `check-console-swap` and `check-a11y-jsx` both declare scan lists that `guard-scan-dirs.test.ts` never pinned — because that suite iterates its own registry and so structurally cannot notice a guard absent from it. `check-a11y-jsx` has the IDENTICAL walk to `check-clarity-input-mask`, which is pinned; the case reading "the only `.tsx`-only scan" named one of the two. Both have entries with real exclusion reasons now, and that case names both.

  Also closes the fifth bullet of the same batch: `SUITES_DIR` (for the guards) and `SUITES_REL` (for the suites) are the same string exported from two modules that cannot import each other, both load-bearing since the comment-terminator guard, and nothing said they were the same directory. One assertion does now.

  Rebased onto a `main` that moved three PRs while this waited — #479, #485 and #486, the last of which rewrote the comment-terminator rule to read spans. Restored clean, no conflicts.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7374 tests (+27), audit baseline (4 accepted, no new), build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## The rule could not see the paragraph it was written for (2026-09-02)

<!-- First bullet of "Comment-terminator follow-ups", filed one task earlier: "The rule sees one line at a time, so a terminator on a line that does NOT begin with a star is invisible to it ... Deciding it properly needs a scanner that tracks comment state, which is a parser, which is the cost this rule was written to avoid". -->

- [DONE: 2026-09-02] [est: 55000] **`lint:comment-terminators` reads comment SPANS now, and the parser it was told it could not afford already shipped** — yesterday's guard was a per-line text match: a line beginning with a star, carrying a terminator with text after it. Every continuation line of a doc comment in this repository is one, so it caught the case that motivated it. A block comment whose continuation lines are UNPREFIXED — legal, and what a pasted-in paragraph looks like before anything formats it — could carry the two characters anywhere and the rule saw nothing.

  **The follow-up priced the fix wrongly, and the price had been paid for months.** `lib/strip-comments.ts` has tracked comment, string and regex state for eleven callers since long before this guard existed. What it lacked was any way to say WHERE a comment ended, because blanked text cannot: a comment and the spaces around it are the same characters. `scanComments` is that state machine, EXTRACTED rather than rewritten — `stripComments` is now it plus a blanking rule, and its 14 existing cases passed unchanged on the first run.

  **The extraction is asserted, not assumed.** One case drives both functions over a real file and checks that every character `stripComments` changes is inside a span `scanComments` reported and every character it keeps is outside one. That is the property that makes "the stripper is the reader plus blanking" a fact rather than a description.

  **The new rule and the one exemption it needs.** A block that opens on one line and closes on another must not have code after its terminator; an inline block comment with code after it on the same line is untouched, because the line break is exactly what the two shapes differ by. The measurement then found twelve legitimate findings in one shape: the braced JSX form, the only way to write a comment in JSX, wrapped across lines by prettier, with the container's own brace after the terminator. The exemption checks the character BEFORE the comment too, so a stray terminator inside one is still reported — the paragraph it leaves behind does not begin with a brace, and a case pins that.

  **The honest limit, pinned rather than hidden.** Everything after an early terminator is CODE, so whether a file's SECOND broken comment is seen depends on what that code does: an orphaned terminator read as a regex resynchronises at the newline and the next block is found, while a first offence that opens a template literal swallows it. The old text match reported both by not knowing the difference — and its second "finding" was inside a template literal, not a comment at all. The case now asserts the resynchronising shape and says in words what the other one costs: the run that fixes one reports the other.

  **A third nanoid advisory arrived mid-task and was fixed rather than accepted.** GHSA-xwg4-73v4-xw9w (integer wraparound below 3.3.12) turned the blocking audit gate red. `nanoid` was the ONLY accepted root that ships to the client, accepted on the argument that no bundled call site passes the argument shape its two advisories need — an argument that would have needed rewriting for a third time. An `overrides` pin to `^3.3.18` clears all three in the same major with no other package moving, so the entry left the baseline instead of growing. The task below reached the same verdict about the same package from the other direction, on the same day; the pin stays, so the fix survives a lockfile regeneration rather than depending on one.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7355 tests (+16), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## Seven exemptions npm could already fix, and nothing ever asked (2026-09-01)

<!-- Not a filed follow-up: `npm run lint:audit-baseline` went red on the working tree with two new `browserslist` advisories, and fixing them exposed the larger gap. -->

- [DONE: 2026-09-01] [est: 47000] **The baseline asked "is this advisory new?" and never once asked "is it still unfixable?"** — `npm audit` reported a fix available WITHIN THE INSTALLED RANGES for four of the six accepted roots. `nanoid`, `brace-expansion`, `js-yaml` and `tar`, seven GHSAs between them, all cleared by a single `npm update` on those four. `nanoid` ships to every user and had been sitting on an exemption list for a day short of a month, being read as triage.

  **It was found by accident, which is the finding.** Two new `browserslist` advisories (GHSA-73wf-gq98-2v4g, GHSA-c83g-rgw3-j3cx) turned the gate red on the working tree; 4.28.7 was already published, so the fix was `npm update browserslist`. Asking whether the same was true of the accepted list took one command and returned four packages. Nothing had asked in a month, and SECURITY.md had said to "prefer `npm audit fix` (no breaking changes)" the whole time — guidance, which is what this repository keeps rediscovering is not enforcement.

  **`js-yaml` is the row worth reading twice.** Its `why` said the fix was `react-native@0.86`, a breaking major. That was true of the direct dependency and false of the tree, which carried 3.15.2 and 4.3.2 in range. A `why` sentence is a claim about the dependency graph on the day somebody wrote it, and nothing re-checked one for two months.

  **`fixKind` reads npm's own `fixAvailable`, and the three shapes are not interchangeable.** A bare `true` means npm resolves the fix inside the ranges the manifest already declares — the shape that cost seven exemptions, because from anywhere except that field it is indistinguishable from `false`. An object is the upgrade npm would perform and only `isSemVerMajor` separates `npm update` from a migration. Everything else reads as "none": the gate fails on in-range only, so an unrecognised shape lands on the side that cannot invent a red run out of a field npm changed.

  **A major-only fix is reported, never failed.** `expo@57` is a migration, not a gate's call, and a gate demanding a major upgrade on every PR is a gate somebody switches off. What changed is that the claim each `why` makes is now restated by npm on every run instead of resting on an author's sentence: the green line names the four advisories nothing short of a major will clear.

  **`stale` joined the failing side, and because of this change.** Fixing an in-range advisory is exactly what MAKES its baseline entry stale — all seven went stale the moment they were fixed. Leaving staleness advisory-only would have meant every fix this gate now demands leaves the accepted list describing a tree that no longer exists, and green. `isClean` is the one predicate the CLI and the suites both read, so a finding cannot be printed by a step that exits 0.

  **One walk, not two.** `observedAdvisoryFixes` keeps npm's verdict beside each key and `observedAdvisories` is now its key list. "Observed" is a definition with four conditions in it (an advisory OBJECT, high or critical, with an identity, keyed per package) and a second copy would be a second chance to disagree about which advisories the gate is looking at.

  Positive control end to end: the guard was run against the PREVIOUS lockfile and named all nine — the seven accepted plus the two new — with the exact `npm update brace-expansion browserslist js-yaml nanoid tar` to run, then green against the new one. Accepted list is 6 roots/11 advisories down to **2 roots/4 advisories**, both fixed only by `expo@57`.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7353 tests (+14), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## Six syntax errors pointing at English (2026-09-01)

<!-- Fifth bullet of "Measured-graph follow-ups", filed one task earlier: "A doc comment containing the `include` glob silently turned a paragraph into code, and only tsc noticed". -->

- [DONE: 2026-09-01] [est: 45000] **`lint:comment-terminators` — the nineteenth guard, and the widest walk in the registry** — a doc comment in `scripts/record-loaded-files.ts` said what `tsconfig.json`'s `include` was and wrote the glob out. A path glob of that shape ends in the two characters that close a block comment, so the comment ended mid-sentence and the rest of the paragraph became code. `tsc` reported six syntax errors, every one of them pointing at prose, the first saying "Expression expected" at a column inside a noun phrase. The cause is invisible from all six.

  **The typecheck already CATCHES this. What it cannot do is say why.** That is the whole value, and the case that pins it is the honest one: the guard's suite asserts tsc still fails the fixture AND that none of its messages mentions a comment — so the day TypeScript starts diagnosing this directly, the case goes red and says the guard is redundant. A contributor whose only change was a paragraph currently gets a list of complaints about code they did not write.

  **The rule is narrow because a wider one would be turned off.** Only lines beginning with a star — the continuation lines of a doc comment, where every line is one — may not carry a terminator with text after it. `foo(/* flag */ true)` is legitimate and untouched. Measured before it was written: zero findings across 809 files, and it flags the real case.

  **Widest walk in the registry, and that is the point.** Six roots: `SOURCE_DIRS` entire plus the suites. The hex and radius rules are about code that SHIPS and stop at `lib/`; this one is about prose, the suites hold more of it than everything else put together, and the file that demonstrated the failure was in `scripts/` — which neither of those walks reads. Floor of 600 against a measured 810.

  **Three files could not write the two characters out, which is the rule working on its own authors.** The lib module, the suite and the planted-offender fixture all build the terminator from parts, each saying why beside it. The one file that has to spell the source-dir list literally — the guard's wrapper, because `guard-scan-dirs.test.ts` reads scan lists by PARSING for a literal array — is named in that suite's one-copy exemption with the structural reason, and held honest by `assertExemptionsHonest`. `SUITES_DIR` joined `lib/source-dirs.ts` so the floor entry could name the roots through the constants rather than restating them.

  Fleet conformance took four rounds and every one of them was the fleet working: a missing partial-root fixture, a missing planted-offender case, a second copy of the source-dir list in the wrapper, and a third in the floor entry. None was found by reading.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7339 tests (+37), audit baseline, build, bundle secrets, bundle size (18.3 KiB headroom), bundle smoke. No DB migration, no creds.

## The guard was checking its own guess, and node knew better (2026-09-01)

<!-- Second bullet of "Reporter-graph-guard follow-ups", filed one task earlier: "`repoLocalSpecifiers` is a regex over source text and will miss what a parser would not". -->

- [DONE: 2026-09-01] [est: 38000] **`check-reporter-graph` reads the graph twice now, and only one of the two readings is a guess** — the guard shipped this morning walked the reporter's imports with a regex and reported "the 3 file(s) it reaches". That number was the regex's opinion. A file reached by a shape the scan does not know — a computed dynamic import, a side-effect `import "./x.ts"` with no `from` to anchor on — was simply absent from it, and the guard said 3 with the same confidence either way.

  **`scripts/record-loaded-files.ts` is a module-customization hook that prints every file node resolves.** The guard registers it, imports the entry the way the test runner does, and reads the markers back: the file list is a MEASUREMENT now. `missingFromWalk` compares the two readings and refuses when node loaded something the walk never reached — not because the tree is broken (it loaded fine) but because the walk's findings are incomplete without saying so, which is how a guard starts quietly checking less than it reports.

  **Neither half is redundant.** The walk is the only one that can name every broken import in a single run — node's loader stops at the first — and the only one that works on a tree that does not load at all. The measurement is the only one that can answer the erasability question, and now the only one that decides which files are under the rules.

  **The recorder cannot import anything, and that is a rule with a reason.** A hook module is loaded on the loader thread before the graph it watches, so anything it pulled in would appear inside its own measurement as a member of the graph it is supposed to be outside of. `LOADED_FILE_MARKER` is therefore spelled out twice — once as a constant, once as a literal in the recorder — with a case asserting the copies agree and a second asserting the recorder has no imports at all.

  **`spawnSync`, not `execFileSync`, and the difference is load-bearing.** The recorder writes on stderr, which `execFileSync` hands back only on the throwing path: the first draft could have measured the graph only when it was broken.

  **It caught its own author within the hour.** The recorder's doc comment said `include` is the double-star `.ts` glob, which ends in the two characters that close a block comment — the paragraph became code, tsc reported six syntax errors in prose, and the guard reported `ERR_INVALID_TYPESCRIPT_SYNTAX` on a file that had been fine a minute earlier. The glob is described rather than written now, with the reason beside it.

  Both new behaviours were reproduced against the working tree before this was called done: a computed `import()` of a probe module produced "node loaded lib/reporter-graph-probe.ts, which the text walk never reached", and the side-effect import shape has a unit case of its own. The regex also gained that shape, so it is a smaller gap as well as a reported one.

  `npm run verify` green: typecheck, `lint:all` 18/18, 7302 tests (+11), audit baseline, build, bundle secrets, bundle size (18.4 KiB headroom), bundle smoke. No DB migration, no creds.

## The constraint that kills the whole run, and nothing enforced it (2026-09-01)

<!-- First bullet of "Failure-reporter follow-ups", filed one task earlier: "The reporter's import graph is read by node's native type stripping, and nothing checks it stays erasable". -->

- [DONE: 2026-09-01] [est: 41000] **`lint:reporter-graph` — the eighteenth guard, and the only one whose subject cannot be tested by the test suite** — node imports a custom test reporter through its DEFAULT loader, so `scripts/test-failure-reporter.ts` and everything it reaches are read by native type stripping rather than by tsx's transform. Two rules follow, and neither is visible from inside the files they bind: every repo-local import names its `.ts` extension (stripping infers none), and every file in the graph is erasable (no `enum`, no `namespace`, no parameter property). Break either and the run dies at LINK TIME — `ERR_MODULE_NOT_FOUND` or `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`, before a single suite starts.

  **Which is exactly why it could not be a test.** `test-failure-report.test.ts` spawns the real command, the strongest check available, and it is unreachable in precisely the case it exists for: the run it belongs to is already dead. `lint:all` runs before `npm test` in `lint:ci` and in ci.yml, so the guard gets to say what happened while there is still a run to say it in.

  **The trap is real, not hypothetical.** `lib/thrown-value.ts` is in that graph and has two other callers who know nothing about any of this. Yesterday's task put a constraint on a shared helper and documented it in a comment; a comment is not an enforcement.

  **`node --check` is not the oracle, and the suite says so.** It parses a `.ts` file with an `enum` in it quite happily and exits 0 — a guard built on it would be green on the exact change it exists to catch. The only reliable answer is node's real loader, so the guard spawns one `import()` of the entry with no tsx anywhere near it, which is what the test runner does. `check-reporter-graph.test.ts` pins that asymmetry directly: `--check` accepts the enum, the import rejects it. The child's env is stripped of `NODE_TEST_*` — the guard is itself spawned from `lint-guard-premise.test.ts`, and an inherited `NODE_TEST_CONTEXT` makes a child believe it is part of a test run.

  **The graph is walked, not listed.** A hardcoded set of three files would go stale silently in the direction of checking too little, the moment an import here changed. `SCANNED_FLOORS` declares only the ENTRY as an input for the same reason `check-sentry-version`'s note gives about resolved version strings: declaring `lib/thrown-value.ts` would turn "the reporter stopped needing that helper" into a premise failure about a file nothing reads any more.

  Both failure modes were reproduced against the working tree before the guard was called done: an `enum` appended to `lib/thrown-value.ts`, and the `.ts` tidied off one specifier. Full fleet conformance — empty root, partial root, empty input, floor lookup, premise report — passes without an exemption.

  **One follow-up was closed instead of filed.** The guard had two independent definitions of "the reporter" — a constant here, a `--test-reporter=` flag in package.json — with nothing comparing them, so pointing the script at a different file would have left this guard checking the old one, greenly. That is one assertion, and a guard whose subject has quietly moved is worse than no guard, so it went in rather than into the suggestions.

  `npm run verify` green: typecheck, `lint:all` 18/18, 7291 tests (+37), audit baseline, build, bundle secrets, bundle size (18.4 KiB headroom), bundle smoke. No DB migration, no creds.

## The run said a test failed and would not say which (2026-09-01)

<!-- First bullet of "CI-observability follow-ups" (2026-08-31): "A test failed once in CI and could not be named" — the `test` job reported `# fail 1` of 7213 on PR #473, passed on re-run, and reproduced green five times locally. -->

- [DONE: 2026-09-01] [est: 34000] **`npm test` now names every test that failed, in the last lines of the log** — tap prints `not ok <n> - <name>` inline at the moment of failure and summarises in COUNTS, so on a 146-second, 7253-test run the one line carrying the name sits thousands of lines above the end and every log reader that returns a tail returns the trailer instead. The only diagnosis available on #473 was "re-run it", which passed, which ended the investigation without answering the question.

  **A second reporter, not a different one.** tap's inline detail is worth keeping and CI's annotations read it, so `--test-reporter=tap --test-reporter-destination=stdout` stays and `scripts/test-failure-reporter.ts` is added on **stderr**: silent for the entire run, then the file, the `describe › describe › it` path and the first line of the thrown value for each failure. `lib/test-failure-report.ts` is the fold (events in, report out, no I/O); the script is the twelve lines that hand it node's stream. Wired in `package.json` on `test`, `test:only` and `test:sentry` — all three, because the two parity guards already require it — rather than in ci.yml, so the same command behaves the same way on a laptop.

  **The ancestry has to be rebuilt from `test:start`.** A suite's own `test:fail` arrives AFTER its children's, so at the moment a case fails nothing has yet said which `describe` it is in. And the stack is keyed BY FILE: node runs suites in concurrent child processes and their events interleave in the parent, so one shared stack would file a case under a `describe` from another file — a wrong answer, not a missing one. Aggregate failures (`failureType: "subtestsFailed"`) are dropped so a nested failure is named once; the filter is on the failure REASON and not on `details.type`, because a `describe` whose own body throws is a suite that nothing below will ever report.

  **Node imports a custom reporter through its DEFAULT loader — tsx's hooks are never consulted.** The first draft died at link time with `ERR_MODULE_NOT_FOUND` on an extensionless import, before a single suite ran. That graph is read by node's native type stripping, which infers no extensions, so `scripts/test-failure-reporter.ts` and `lib/test-failure-report.ts` name their repo-local imports with `.ts` and `tsconfig.json` gained `allowImportingTsExtensions` so tsc agrees. `--disable-warning=MODULE_TYPELESS_PACKAGE_JSON` suppresses the four lines node prints when it sniffs those files, and nothing else — `--no-warnings` would have hidden whatever a suite legitimately warns about.

  **The proof is a spawned run, because the loading bug is invisible to a text scan.** `test-failure-report.test.ts` builds the real command out of `package.json` (a hand-copied flag list is the thing that goes stale), writes a fixture with one passing and one failing case, and asserts the failing one is named in stderr, the passing one is not, and a green fixture writes nothing at all. That case failed the first time for its own reason worth keeping: this suite runs inside node's test runner, which marks its children with `NODE_TEST_CONTEXT`, and the inherited variable told the spawned runner it was a child — it swapped to the v8 reporter, ignored the `--test-reporter` flags and exited 0 on a failing suite. A green assertion about a run that never used the reporter. The env is stripped now.

  **It found its own first customer during this task.** The full-suite run went red on `suite-files-helper.test.ts` — this new file named `"__tests__"` literally instead of importing `SUITES_REL` — and the name was the last line of a 100-second run rather than somewhere in the middle of 7253 results.

  **And then it named the flake the follow-up said nothing was looking for.** The next full run went red on `guard-fixture-refusals.test.ts` — "the refusal left 1 scratch root(s) in /tmp" — for a directory it never created. `assertRefusesWithoutLeaking` proves a refused fixture spec leaves nothing behind by diffing `os.tmpdir()` across the throw, which is the only way to see it from outside (the helper throws instead of returning the path). It filtered on the bare `lint-guard-partial-` literal; SIX suites build fixtures under that name and node runs each of them in a concurrent child process, so another file's directory appearing between the two snapshots was read as this refusal's leak. One failure in thousands, naming an innocent case, surviving no re-run — the exact shape of the anonymous `# fail 1` on #473. `PARTIAL_ROOT_PREFIX` carries the pid now (trailing dash so `…-123-` cannot prefix `…-1234-`), which also stops a crashed run's leftovers counting. The regression case creates a decoy with another pid INSIDE the assertion, and it fails against the old literal.

  `npm run verify` green: typecheck, `lint:all` 17/17, 7254 tests (+30), audit baseline, build, bundle secrets, bundle size (18.4 KiB headroom), bundle smoke. No DB migration, no creds.
## Which documents are supposed to carry an audit was three names and no rationale (2026-08-31)

<!-- Third bullet of "Audited-plan follow-ups", filed one task earlier: "`.analytics-setup.md` and `.sentry-setup.md` are the two `.tasks/` documents still unaudited ... deciding that explicitly is cheaper than a fourth run rediscovering the question. `PLANS` in the test names three files and nothing says why those three." -->

- [DONE: 2026-08-31] [est: 8000] **The two remaining documents needed a decision, not an audit** — `.analytics-setup.md` and `.sentry-setup.md` are provisioning walkthroughs: create a PostHog project, paste a DSN into GitHub Secrets. The steps happen in third-party dashboards, so nothing in this tree can be read to decide whether they are "done" and a STATUS AUDIT table would be a fabrication. That is a different answer from "nobody has audited them", and the two look identical from outside — which is precisely the confusion that let three finished plans read as queues for months.

  **So the classification is the deliverable, and it is enforced.** Every `.tasks/*.md` is exactly one of three things, by the test of whether "is this done?" is a question about this repo: a PLAN (repo work — audited), a RUNBOOK (operator steps outside the tree — reason recorded per file), or a QUEUE (live loop state). A document in none of them fails the completeness case, which forces the decision when somebody ADDS a file rather than whenever a later run notices. Planted a scratch document to confirm it fails, and removed it.

  Deliberately a NEW test file rather than an edit to `status-audit-paths.test.ts`: another session has that file open in #477, and having just flagged cross-session conflicts on shared files as a problem I was contributing to, editing one would have been inconsistent. The two rules are also genuinely separate — one checks that cited paths are real, the other that every document has been classified at all.

  `npm run verify` green: typecheck, `lint:all`, 7229 tests (+5), audit baseline, build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## Three plans, three finished, and a guard so the audits cannot rot the same way (2026-08-31)

<!-- Second bullet of "Stale-plan follow-ups", filed one task earlier: "nothing keeps a status audit true — the tables name files, so a rename makes them wrong silently, exactly like the text they replaced". -->

- [DONE: 2026-08-31] [est: 11000] **`.design.md` is the third `.tasks/` plan audited today and the third found complete** — Syne/DM Sans loaded through `lib/fonts.ts` with every one of the sixteen listed screens carrying `fontFamily` styles, the gradient hero in `components/hero-banner.tsx` with no plain `#261b14` hero left, `components/skeleton.tsx`, the masonry grid in two screens, the minimal bottom nav. Three for three is no longer a hypothesis about these documents: they are historical briefs, and every one of them reads as a queue.

  **A table of paths is a claim about the tree, and it rots exactly the way the prose it replaced did.** Shipping three of them with nothing checking them would have been replacing a stale document with a stale document, which is the joke this task would otherwise be. `status-audit-paths.test.ts` reads the `> ## STATUS AUDIT` block out of each plan and requires every backticked token that LOOKS like a repo path to exist — narrow on purpose: it says nothing about whether the evidence convinces, only that it is real.

  **It failed on its first run, against my own two audits from earlier today.** They cited bare filenames — `ci.yml`, `20260625_subscriptions.sql`, `_shared/assert-caller.ts` — which a reader cannot open and which name nothing unambiguously. Eleven citations across the three tables are now repo-relative. That is the guard doing the one job it was written for, on the author who needed it.

  Carries the two floors this repo has learned to want: an audit block shorter than 200 characters is not a table, and fewer than twelve path citations across the three means the reader has stopped matching rather than that the tree is clean.

  `npm run verify` green: typecheck, `lint:all`, 7224 tests (+5), audit baseline, build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## The backend plan was finished and said so nowhere (2026-08-31)

<!-- Fifth bullet of "Advisory-baseline follow-ups", filed two tasks earlier: "`.tasks/.security-upgrade.md` needed a status audit because 13 of 16 checked items were done and unrecorded — the same is likely true of `.be-suggestions.md`". It was. -->

- [DONE: 2026-08-31] [est: 9000] **Thirty-odd BE items, every one checked implemented, and the document records none of it** — `.be-suggestions.md` was written when the backend was aspirational and reads today exactly as it did then. A run picking work off it top-down re-derives the state from the code, which is what two passes of this session did to `.security-upgrade.md` before that one was audited. A stale plan is worse than no plan: it costs a cycle at best and invites re-implementing something finished at worst.

  Verified against the tree rather than assumed: the base schema and its parity test, the six constraint/index/default migrations, RLS plus its pgTAP leak test, the sync engine and cursors and cloud import, realtime replica identity, seven Edge Functions covering BE-20 through BE-26, retention sweeps, the write rate limiter, the kill-switch envs, the three CI migration guards, and the injected-fetcher suites. The audit table names a file for each.

  **Two items are honestly marked not-audited rather than assumed done**: BE-4 is an operator action on a throwaway Supabase project and leaves no repo trace, and BE-28's cursor pagination is present in `lib/supabase-marketplace.ts` but was not confirmed for every list path. Guessing on those would have made the table exactly the kind of document it replaces.

  No code changed. `npm run verify` green: 7219 tests, audit baseline, build and the three `dist/` guards, on a tree that also carries the other session's #473. No DB migration, no creds.

## The blocking gate would have cried wolf on a lockfile reshuffle (2026-08-31)

<!-- First bullet of "Advisory-id follow-ups", filed one task earlier: "`brace-expansion` carries nine ids for three distinct advisories ... a lockfile reshuffle that changes the path count changes the id set, turning the gate red for a tree-shape change rather than a security one". -->

- [DONE: 2026-08-31] [est: 10000] **npm reports one `via` object PER DEPENDENCY PATH, so 11 advisories arrived as 18 ids** — `brace-expansion`'s three are nine, and `js-yaml`'s one is two. Keyed on `via[].source` the baseline was a function of the dependency GRAPH rather than of the advisories: adding or dropping a path changes the id set, and the gate — blocking since the task before last — would have failed on a tree-shape change with no security content. A gate that cries wolf on churn is a gate somebody switches off, which would have cost the whole mechanism.

  The GHSA is the advisory's own name: stable across paths, across lockfiles, and the thing a person actually reads. `advisoryIdentity` takes it off `via[].url` and falls back to `source` when there is no GHSA — a worse, path-sensitive key, and still better than dropping an advisory silently from a gate whose entire job is to have no silent holes.

  **Three versions of the key, each wrong in its own way, and worth writing down together**: the package NAME accepted every future CVE in an accepted package; the per-path `source` id churned with the lockfile; the GHSA is neither. That progression is now in the module header and in SECURITY.md, because the two failures are opposite and a fourth version would otherwise rediscover one of them.

  `npm run verify` green: typecheck, `lint:all`, 7218 tests (+3), audit baseline (11 accepted, none new), build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## The signature the reader measured from was sometimes the return type (2026-08-31)

<!-- First two bullets of "Arrow-form follow-ups" ("`bodyBrace` scans forward for `{` on the function form and refuses to on the arrow form ... reads the ANNOTATION as the body" and "the arrow branch finds `=>` with `indexOf` and a return type can contain one"), plus the third ("three of the four `signalsInSource` refusals are now proven by a needle and the fourth is not"). -->

- [DONE: 2026-08-31] [est: 14000] **A return-type annotation can contain both tokens the reader used to find the body, and `indexOf` cannot tell them from the real ones** — `: { reason: StorageFailureReason } {` puts a brace before the body's, so the reader would have scanned the ANNOTATION and reported its contents as the classifier's signals; `: ((e: unknown) => StorageFailureReason) => {` puts an arrow before the body's, so the reader would have refused an ordinary declaration for not having `{` where it looked. One is a wrong answer wearing a successful parse and the other is a false refusal, and both were one `indexOf` call.

  **Depth is the distinction, and it only needs three brackets.** `atDepthZero` walks from the parameter list counting `(`, `[` and `{` and skipping string literals with `balanced-source`'s own `QUOTES`/`endOfString`, so the annotation's arrow — inside its parens, at depth 1 — is walked past and the signature's is found. Angle brackets are deliberately NOT counted: `>` is the second character of `=>`, so a counter tracking them goes negative on the very token it is looking for, and a generic argument cannot hold a top-level `=>` or `{` without opening one of the three that are counted.

  **Depth alone does not settle the object return type, because its brace IS at depth 0.** So the guard asks what a body's brace may FOLLOW — `)` or the last character of a type name — and refuses anything else by name. `: {` was the shape that went wrong first and a `:` check would have missed `Promise<{ … }>`, whose brace follows `<`; both are needles now.

  **Every `assert` in the reader has a needle.** Three of them — the unclosed parameter list, the unclosed body, and a `const` binding that is not a function — were branches nothing exercised, which is indistinguishable from working until the day one of them is all that stands between a rewrite and a wrong answer. `UNREADABLE_PLANTED_CLASSIFIERS` goes 2 → 7 and `functionTypedReturn` joins the readable side, pinned to its exact signals rather than to "did not throw".

  `npm run verify` green: typecheck, `lint:all` (17/17), 7213 tests (+1), audit baseline, build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. Test-only change: no `lib/` edit, no DB migration, no creds.

## The baseline accepted an advisory nobody had read (2026-08-31)

<!-- Second bullet of "Advisory-baseline follow-ups", filed one task earlier: "the baseline keys on package NAME, not advisory id — a second, unrelated CVE in `nanoid` would land under an accepted name and pass silently". It was not hypothetical. -->

- [DONE: 2026-08-31] [est: 12000] **`nanoid` was already carrying TWO high advisories the day the baseline was written** — 1138811 (a negative `size`) and 1139427 (a custom generator). The entry's reasoning covered the first. The second was accepted because the PACKAGE was on the list, which is precisely the failure a baseline exists to prevent, reproduced one layer inside the baseline. Keys are `package#advisoryId` now, and every entry lists the ids it has actually read: a name covers every future CVE in that package, an id covers the one somebody looked at.

  The second nanoid advisory does hold under the same reasoning — expo-router calls the default generator, so neither a negative size nor a custom generator is reachable — so nothing was exploitable. What was wrong is that the list said "read and understood" about something nobody had opened.

  **Severity moved to the advisory too.** npm reports a package at the highest severity among its advisories, so `postcss` shows "high" while two of its four are moderate; a gate reading the package's number would have accepted those two unread, by the same blanket-licence mechanism. 18 advisories across 6 roots, from 13 high ENTRIES — the other seven are Expo/metro packages carrying no advisory of their own.

  `isAdvisoryRoot` went with the change: `observedAdvisories` does the job and a second exported reader for the same question is the dead export #468 was about. The report's `advisor(y|ies)` pluralisation went too — it read like a leaked regex in output written for a person.

  `npm run verify` green: typecheck, `lint:all`, 7215 tests (+3), audit baseline (18 accepted, none new), build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## SEC-8: the audit step was red every run, so nobody could read it (2026-08-31)

<!-- Not from .suggestions.md. `.tasks.md` had no open items, so I audited `.tasks/.security-upgrade.md` — the only severity-ordered queue in the repo — against the code. SEC-1/2/3/4/6/7/9/10/11/12/14/21/22 are all implemented and unrecorded (the doc marks only SEC-1). SEC-8 is the one that had genuinely regressed. -->

- [DONE: 2026-08-31] [est: 26000] **`npm audit --audit-level=high` was `continue-on-error: true`, and it had to be** — accepted advisories make a bare audit red on every run, and a step that is always red reports the same red for an advisory somebody triaged and one nobody has ever seen. SECURITY.md recorded **0 high/critical** on 2026-06-28; the same lockfile audited at **27 advisories, 13 of them high**, on 2026-08-31. The step failed dutifully through all of it, under a green-looking PR list.

  **A baseline turns "always red" into "red when it changed".** `lib/audit-baseline.ts` holds the six triaged advisory ROOTS (the other seven high entries are Expo/metro packages that merely depend on them — that kind churns whenever the tree is reshaped and says nothing about exposure), and `evaluateAudit` fails on a high/critical root not on the list. It reports the other direction too: an accepted entry the audit has stopped naming is stale, because a list nobody prunes stops describing the tree and then starts covering something somebody would have wanted to see.

  **The documented reasoning was wrong, not just the number.** SECURITY.md said the remaining advisories "live entirely in dev/build-time tooling". `nanoid` is high and ships to every user, via `@react-navigation/routers`' route keys. It is still accepted — the DoS needs a negative `size` and every bundled call site is `nanoid()` with no argument — but that is a different sentence, so the table now answers "ships to client?" per package instead of summarising in prose. `image-size` looked like a second one and is a false positive: the `image-size-select-actual` in the bundle is an icon name.

  Blocking now, which is the point of being quiet, so it had to join `npm run verify` — CLAUDE.md's own rule, and the eighth step its text anticipated. The exclusion list in `verify-gate-script.test.ts` had `npm audit` written into its rationale as `continue-on-error`; that reasoning is gone with the step. The 2026-06-28 resolved table is kept: a security record is not tidied away.

  `npm run verify` green: typecheck, `lint:all`, 7210 tests (+14), audit baseline, build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## The reader's refusal was a rule the codebase never agreed to (2026-08-31)

<!-- Fourth bullet of "Refusing-reader follow-ups" ("the reader refuses an arrow const and the app has no lint rule preventing one ... a red suite with a message explaining a reader's limitation rather than a problem with the code") and the third ("the partition case counts three readable and two unreadable against five entries, and counting is all it does"). -->

- [DONE: 2026-08-31] [est: 9000] **`signalsInSource` refused `const classifyStorageError = (…) => {…}`, and nothing in this repo forbids writing one** — so the first contributor to reach for the arrow form would have got a red suite whose message explained a reader's limitation rather than a problem with their code. The refusal was the right call the day it was added, when the alternative was reporting an unreadable declaration as "the signal count dropped to 0"; it was never a rule anyone agreed to. The reader follows both shapes now: `DECLARATION_FORMS` carries the two openers and a flag for where the body starts, and `arrowConst` moves from the refusal table to the needles that must parse, pinned to its exact signals rather than to "did not throw" — the failure worth catching is a reader that finds the declaration and then scans the wrong span.

  **Teaching it the arrow form introduced an absence the function form does not have.** An expression-bodied arrow has no body span at all, and `indexOf("{")` answers that with the next brace ANYWHERE later in the file — a neighbouring function's, whose signals would be returned as this classifier's. A wrong answer, which is worse than the refusal it replaced. So `bodyBrace` requires the very next non-space thing after `=>` to be `{`, and the `conciseArrow` needle carries a trailing `elsewhere()` containing a `"neighbour"` signal so the case fails on the misread rather than on a technicality.

  **The partition case counted and counting was all it did.** `readable.length + unreadable.length === total` balances a needle a list names but `PLANTED_CLASSIFIERS` no longer holds against a needle no list names, and says nothing about which. Both directions are asserted now — every needle in exactly one list (naming which of "both" or "neither" it is), and every listed name a real key — which is also what makes moving a needle between the lists legible rather than arithmetic.

  `npm run verify` green: typecheck, `lint:all` (17/17), 7198 tests (+2), build, bundle secrets, bundle size (19.1 KiB headroom), bundle smoke. Test-only change: no `lib/` edit, no DB migration, no creds.

## "Found nothing" and "nothing to find" were the same value (2026-08-31)

<!-- Fourth bullet of "Planted-needle follow-ups" ("`signalsInSource` finds the function by `indexOf(...)`, so a classifier declared as a `const` arrow would read as absent") and the first ("`PLANTED_CLASSIFIERS` holds source strings and nothing checks they still PARSE"). -->

- [DONE: 2026-08-31] [est: 9000] **The reader returned `{codes: [], phrases: []}` for a file it could not read, which is the same answer as a classifier with no signals** — and the reader is the only layer that knows which of the two it is holding. `const classifyStorageError = (error) => …` is a legal rewrite the `indexOf("function classifyStorageError(")` does not match, so the whole body goes invisible and the floor two layers up reports "the signal count dropped to 0" — true, and about the wrong file. It refuses now, with two messages: the name is absent, or the name is there in a shape this reader cannot follow. `parameterList` in `declared-shape.ts` refuses an overload set out loud for the same reason.

  **The needles had the same hole, one level down.** They are source strings nothing else compiles, so a typo in one degraded to "the reader found nothing" and its own case passed for the wrong reason — `deadSubstring` and `bothKinds` assert exact signal lists and were pinned, `unsampledSignal` was not. Every needle is now in exactly one of two lists (parses, or is refused with a named message), and a case asserts the two lists partition the table, so a needle added later that is in neither is checked by nothing and says so.

  `arrowConst` joins the table as the nastier absence: the identifier IS present, so a reader that only asked "is the name here?" would be satisfied while every signal in the body is invisible.

  `npm run verify` green: typecheck, `lint:all`, 7196 tests (+1 net), build, bundle secrets, bundle size (19.1 KiB headroom), bundle smoke. No DB migration, no creds.

## The guard's teeth were shown in a terminal and not in the repo (2026-08-31)

<!-- Fifth bullet of "Substring-signal follow-ups" ("the planted-clause check was run by hand and left no trace ... this one is the only guard whose evidence lives in a session transcript rather than in the repo") and the first ("the numeric ones are excluded by a `/^\d+$/` filter"). -->

- [DONE: 2026-08-31] [est: 11000] **A guard that only ever runs over source that satisfies it is green for two indistinguishable reasons** — the tree is clean, or the reader found nothing. The substring rule was proven by editing `lib/report-storage-failure.ts` by hand, watching the case fail and putting the file back: correct, unrepeatable, and gone the moment the session ended. Every other sweep in this tree carries its needle (`guard-fixture.ts`, the split needles in the suite-import rule); this one did not, and it is the one whose entire value is that it fails on a specific input.

  `classifierSignals()` splits into `signalsInSource(source)` — pure over text — and a one-line wrapper that reads the real file. `PLANTED_CLASSIFIERS` holds four `classifyStorageError` bodies as SOURCE STRINGS rather than committed `.ts` files, because a deliberately broken classifier under `lib/` is exactly what every other guard in this tree is looking for.

  **The reader also stopped flattening.** It returned one list and the substring rule filtered it with `/^\d+$/` — a second place that had to know a `code === N` renders as a bare number, and one that would have turned `"22"` inside `"1022"` into a false offender the day the reader tagged its codes. `{codes, phrases}` makes the distinction the reader's, and the substring rule takes `phrases`.

  Four cases: the dead pair really parses and the rule really throws on it; an added signal is seen and is not in the samples; the two kinds stay apart; and a renamed-away function yields NOTHING — which is the failure mode a derived rule has and a hand-written list does not, and the reason the floor exists.

  `npm run verify` green: typecheck, `lint:all`, 7195 tests (+4), build, bundle secrets, bundle size (19.1 KiB headroom), bundle smoke. No DB migration, no creds.

## The second spelling could never be the one that answered (2026-08-31)

<!-- Second bullet of "Derived-sample follow-ups" ("`\"database or disk is full\"` is dead source ... it is checked after `includes(\"disk is full\")`, which every string containing it also satisfies"). -->

- [DONE: 2026-08-31] [est: 8000] **Two spellings of one signal, and the longer one was unreachable** — `haystack.includes("disk is full") || haystack.includes("database or disk is full")` reads as thoroughness about SQLite's wording and is a tautology: every string containing the second contains the first, so the right-hand clause could never decide. It shipped, so it was also bytes in every bundle. It survived because nothing ever put the two clauses side by side — they are one `||` apart and both look like they are doing something. The task before this one derived the signals from the classifier's body and printed them as a list, which is where the pair became obvious.

  Deleted, with SQLite's actual wording moved into the doc comment, which is where it was doing its work. `KNOWN_CLASSIFIER_SIGNALS` drops 6 → 5, exercising the floor in the direction it was written for (a signal removed), and the sample that existed only for the dead clause goes with it.

  **The generalisation is the case worth having.** A substring pair is a shape, not an incident: any two string signals where one contains the other make the longer clause dead. The new case takes the string signals off the classifier's own body and requires that none contains another — numeric `code` signals excluded, because those are compared rather than searched. Planted the old clause back to confirm it fails, and it names both spellings in the message.

  `npm run verify` green: typecheck, `lint:all`, 7191 tests (+1), build, bundle secrets, bundle size (19.1 KiB headroom, 0.1 KiB better), bundle smoke. No DB migration, no creds.

## Two samples of one input space, and neither knew the space (2026-08-31)

<!-- First bullet of "End-to-end-notice follow-ups" and first of "Fourth-read follow-ups" — the same observation filed twice ("the eleven inputs are now written out in TWO suites, and neither list is derived from the classifier ... a fifth signal added to `classifyStorageError` gets an input in neither, and both stay green"). -->

- [DONE: 2026-08-31] [est: 14000] **Three cases stated a property over "everything the classifier reads" and each read a hand-written list** — `report-storage-failure.test.ts` had twelve inputs and `storage-notice.test.ts` eleven, differing by a quota spelling nobody chose, and neither had an edge back to `classifyStorageError`. A seventh signal added to the classifier would get an input in neither list, so the branch nothing exercised would sit behind three green cases each claiming to cover the space it is in.

  **The samples could not live beside the classifier, which is what the two earlier filings assumed.** `lib/report-storage-failure.ts` SHIPS — a fixture table there is bytes in every user's bundle, against 19 KiB of headroom — and a helper cannot import it for its value either, because it reaches `@/lib/sentry` and would load the real wrapper before any suite's shim registers. So the edge is a source read: `classifierSignals()` takes the body of `classifyStorageError` and returns the literal signals it tests (`code === N`, `haystack.includes("…")`), and the coverage case requires a sample carrying each. `import type` is erased, which is the exemption `helper-lib-imports.test.ts` states.

  Two cases in place of one: every sample classifies to the reason it declares (which is what lets the other suites read the reason off the sample instead of restating it), and every signal the classifier's own body reads has a sample carrying it — with a floor on the signal count, because a parse that finds nothing reads as a clean tree rather than as a broken reader. Six signals found today, thirteen samples.

  `npm run verify` green: typecheck, `lint:all`, 7190 tests (+1 net — one hand-rolled case replaced by two derived ones), build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## The sweep that closes leaks was hiding one (2026-08-31)

<!-- Third bullet of "Swept-ratio follow-ups" ("`swept` is `[...liveTrees]` and the loop can only ever end trees that were in it at entry ... a cleanup that RENDERS adds to `liveTrees` mid-sweep, and the new tree is neither swept nor counted, then `liveTrees.clear()` drops it from the registry while it is still mounted"). -->

- [DONE: 2026-08-31] [est: 9000] **`liveTrees.clear()` was erasing evidence, not tidying up** — `unmount()` removes a tree from the registry BEFORE it runs a single cleanup, so every tree the sweep touched was already gone from the set and the clear had nothing left to do for them. What it did reach was a tree mounted DURING the sweep: a cleanup is arbitrary code and may render — a teardown probe, a fallback tree put up as a subscription closes — and that tree joins `liveTrees` after the snapshot the loop iterates. The clear dropped it with its effects live, and `__mountedTreeCountForTests()` then reported zero to the case that would have caught it. The one leak this registry exists to expose was the one it could not see.

  The loop re-reads the registry until it is empty instead of sweeping one snapshot, which needs a terminator: a cleanup that mounts a tree whose cleanup mounts another never settles, and without a cap that is a process hanging inside an `afterEach` — a runner that appears to have stopped rather than a component that will not let go. `SWEEP_PASS_LIMIT` is 5, and giving up pushes ONE more cause into the same report rather than throwing separately, so the broken cleanups found on the way out are still in the message.

  Two cases: a cleanup that mounts a `Latecomer` and requires both cleanups to run and the count to end at 0, and a `Runaway` whose cleanup remounts itself — asserting the cap in the message, exactly five mounts, and an empty registry, because the alternative is handing the runaway to the next case too.

  `npm run verify` green: typecheck, `lint:all`, 7189 tests (+2), build, bundle secrets, bundle size, bundle smoke. No DB migration, no creds.

## The count said how many trees threw in the words of how many there were (2026-08-31)

<!-- Third bullet of "Tagged-aggregate follow-ups" ("the tree count is the number of trees that THREW, not the number swept ... `across 2 tree(s)` after a sweep of five means two were broken, which is the useful number and is not what the phrase says"). -->

- [DONE: 2026-08-31] [est: 6000] **`across 2 tree(s)` was one number answering two questions** — the clause was added a task ago to separate "three failures in one tree" from "three failures in three", and it counts the trees that THREW. After a sweep of five that prints `across 2 tree(s)`, which a reader takes for the size of the registry: the number that tells you two components are broken is spelled like the number that tells you the sweep found two trees, and the three healthy trees that came down are nowhere in the sentence. `in 2 of 5 tree(s)` carries the ratio and costs one variable — `swept` is the array the loop already materialises, so the count is `swept.length` rather than a second traversal.

  `trees` goes from `number` to `{broken, swept}`, which is also the shape that makes the omission impossible: a caller can no longer pass one number and leave the reader to guess which.

  Three cases: the two that pinned the old phrase now pin `in 1 of 1` and `in 2 of 2`, and a new one sweeps three trees with the broken one in the MIDDLE — the only arrangement where the ratio and the bare count differ — asserting `in 1 of 3 tree(s)`, that both healthy cleanups ran, and that the registry emptied.

  `npm run verify` green: typecheck, `lint:all`, 7187 tests (+1), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The unwrap could not tell its own aggregate from a component's (2026-08-31)

<!-- Second bullet of "Collected-teardown follow-ups" ("the flattening reads `error instanceof AggregateError` and takes `.errors`, which is also true of an `AggregateError` a COMPONENT'S cleanup threw ... tagging the harness's own aggregate would make the unwrap say what it is unwrapping") and the first ("the causes name the component's message and nothing names the tree"). -->

- [DONE: 2026-08-31] [est: 7000] **`instanceof AggregateError` was the wrong question, asked one task ago** — `unmountAllTrees` flattens the per-tree aggregates so a case can count causes, and it recognised them by the base class. A destructor that rethrows a `Promise.any` rejection satisfies that too, so its causes would have been spliced into the tree-level list: one broken component reported as two, with the count in the message growing to match. `CleanupFailures extends AggregateError` costs a line, makes the unwrap say what it is unwrapping, and keeps every case written against `instanceof AggregateError` working.

  **The message names the tree count, which was the missing dimension.** The causes carry the component's message and nothing carries the tree it was in, so `3 effect cleanup(s) across 1 tree(s)` and `across 3 tree(s)` are the two different diagnoses a reader was previously left to guess between. Passed only by `unmountAllTrees`, because it is the only caller whose failures span more than one tree.

  Two cases: the flatten one now asserts the tree count, and a new one hands a component's own `AggregateError` to a cleanup and requires it to arrive as ONE cause, identical to the object thrown.

  `npm run verify` green: typecheck, `lint:all`, 7186 tests (+1), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## Two reads one character apart, and the path with nothing to classify (2026-08-31)

<!-- Fifth bullet of "Suite-import-rule follow-ups" ("`suiteCode()` flattens whitespace and `suiteSource()` here does not, and both are one call away from each other — a `suiteLines()` in `helpers/suite-files.ts`, named for what it preserves, would put the distinction where the next caller reads it") and the second of "End-to-end-notice follow-ups" ("the join is proven for the WRITE half and not for the gate half"). -->

- [DONE: 2026-08-31] [est: 8000] **`suiteLines()` is the fourth read, and it exists because the third one cost a false green an hour earlier** — `suiteCode` flattens whitespace, which is what a shape sweep wants and the opposite of what a line-anchored rule wants; the two are one call apart. The suite-import sweep reached for the flattened one, its `/^import .../gm` matched at most once over a one-line file, and the rule reported a population of zero — which reads as a clean tree rather than as a broken parse. The distinction now lives in `helpers/suite-files.ts` beside the read it is easy to confuse, cached like the other three, with the incident written into its doc comment; `helper-lib-imports.test.ts` drops its local copy. One case pins what it preserves and how it relates to `suiteCode` (the flatten and nothing else — `suiteText` is the one that keeps comments), plus the identity check the other three already have.

  **The gate half had no report assertion at all.** `useStorageNotice(refusing)` is the third path through the same latch and the one with no error to classify: a refused hydrate means the store answered when it was READ, before anything was written, so it passes `"unavailable"` as a constant and captures nothing. That was right and assumed. A regression that started capturing there would send Sentry an event with no error in it, once per launch, on every device with the gate shut — and every existing case would stay green, because they all assert about toasts.

  `npm run verify` green: typecheck, `lint:all`, 7185 tests (+2), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The toast and the crash report were proven separately and never joined (2026-08-31)

<!-- Fifth bullet of "Agreement-property follow-ups" ("the equality property runs inside `report-storage-failure.test.ts`, where the observer is a local subscription rather than the app's ... Nothing joins them end to end") and the fourth ("Nothing asserts the reverse for the loud half — that a data write reports AND toasts"). -->

- [DONE: 2026-08-31] [est: 11000] **"The toast and the crash report agree" was two claims, proven in two files, about two different observers** — the agreement property lives in `report-storage-failure.test.ts` and its observer is a local subscription in that file. `lib/storage-notice.ts` is the real consumer — the one that decides WHICH SENTENCE the user reads off `event.reason` — and it is not in that suite's graph at all. A notice that read the reason backwards satisfied both halves. The new case runs the real `reportStorageFailure` past the real `useStorageFailureNotice`, mounted, and reads both outputs from one call: for each of eleven classifier inputs, exactly one toast, exactly one report, the report's `extra.reason` equal to `classifyStorageError`, and the toast's key equal to `storageNoticeMessageKey(reason, "web")`.

  **Three resets per input, each for different once-per-session state**: the toast latch (the user hears it once), the Sentry budget (a site/keyspace pair reports once), and the spy arrays. Without them every iteration after the first asserts over empty lists, which is how a loop like this passes without running.

  **The input list has to reach both answers or the property is one branch eleven times.** The sentences seen are collected and asserted to be exactly the two, so a list that drifted to eleven quota spellings turns red instead of quietly narrowing.

  **The loud half now reports as well as toasting.** "Still REPORTS the quiet writes" proved the quiet half is quiet only to the user; nothing said the reverse, so a regression that stopped reporting the sites that DO toast left every case green — the toast was asserted and the report assumed. `expectStorageReport` pins scope, keyspace and reason, and the keyspace is the masked `collectables-items-v1-{id}`, which is the account id being stripped on the way out.

  `npm run verify` green: typecheck, `lint:all`, 7183 tests (+2), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The eager-import rule was checked for helpers and not for the suites (2026-08-31)

<!-- Second bullet of "Helper-boundary follow-ups": "The same rule applies to the SUITES and is checked only for helpers — a top-level suite that statically imports a `lib/` module before its `mockModule` calls has exactly the failure this guard describes, and every mounted suite avoids it by hand, in a header comment. The walk is written; pointing it at `topLevelSuites()` with an exemption for the modules a suite legitimately loads eagerly is the same case with a different seed." -->

- [DONE: 2026-08-31] [est: 16000] **A suite's own import list can defeat its own mocks, and only the helpers were being checked** — ESM caches a module the first time it evaluates, so a static `import { x } from "@/lib/y"` at the top of a suite that later mocks something in `lib/y`'s closure registers the mock over a module nobody will load again. Every mounted suite avoids this by hand, in a header comment. `externalReach` was already written; this points it at `topLevelSuites()`.

  **The rule is NARROWER than the helper rule, and that is why it needs no exemption list.** A helper is imported by suites it knows nothing about, so the only safe property is absolute: its closure reaches nothing but `node:` builtins. A suite knows exactly what it replaces, so the rule is the precise one — an eager `lib/` value import may reach anything except a specifier THIS suite mocks. The suggestion asked for "an exemption for the modules a suite legitimately loads eagerly"; under the precise rule there are none to exempt, because `@/lib/balanced-source` beside an AsyncStorage mock is not an offence.

  **A mock a helper registers counts as the suite's.** `providerHarness` and `installSpyAsyncStorage` mock on the suite's behalf and a suite that delegated is no less broken for it. The helper→specifier map is derived by reading `helpers/*.ts` for their own `mockModule` calls, so a helper that starts mocking a fifth module extends the rule with nobody editing it.

  **Zero offenders, so the teeth are four planted cases**: a transitive hit (`lib/report-storage-failure` → `lib/storage-keys` → AsyncStorage, and the message names `storage-keys` or the reader opens the wrong file), a suite importing the very module it mocks, a mock reached only through a helper, and the two that must stay quiet — a type-only import and a pure module.

  Two bugs found while writing it, both in the sweep rather than in the tree. `suiteCode()` flattens whitespace, so the line-anchored import regex matched at most once per file and the first draft reported a population of zero — it reads `stripComments(readSuite(…))` now, with the reason written beside it. And the planted sources spelled two shapes other sweeps search their suites for, which made this file an offender under two unrelated rules; the lines are assembled from parts instead, including in the prose, because one of those sweeps reads the file with its comments.

  `npm run verify` green: typecheck, `lint:all`, 7181 tests (+6), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The status stub was `{}`, and a provider that branches on it took the falsy path (2026-08-31)

<!-- Third bullet of "Helper-boundary follow-ups": "`INERT_SENTRY` returns `{}` from `getSentryStatus`, and the real one returns a status object with six fields — a provider that reads `.ready` off it gets `undefined` rather than a crash, which is the improvement; a provider that BRANCHES on it takes the falsy path silently. The honest stub is the real shape with inert values, and the case that derives the name list cannot check shapes." -->

- [DONE: 2026-08-31] [est: 13000] **A missing NAME crashes; a missing FIELD does not, which is the worse of the two** — `getSentryStatus: () => ({})` gave a provider reading `.ready` an `undefined` that is falsy in the same direction a not-ready Sentry is, so the case passed and proved nothing. `reason` is the field with no falsy member at all: a `switch` over the six-way union falls through a default the app never expects to reach. `INERT_SENTRY_STATUS` is now the full nine-field `SentryStatus` with the values the real module returns before `initSentry` runs — which is the state a mounted provider suite is actually in — and it is TYPED as `SentryStatus`, so a tenth field is a compile error in the helper.

  **A fresh object per call**, because the real one builds its snapshot each time and a shared literal lets a case that mutates what it read change what the next case sees.

  **The shape is derived from the TYPE, not from the app's reads.** The name sweep beside it reads the app's imports on purpose — a helper nobody calls needs no stub. A status FIELD is the opposite: the whole object crosses the boundary in one return value, so a field the app has not read yet is one `status.x` away from being read, and a subset stub is one that happens to work today.

  **`declaredMembers()` is the new half of `helpers/declared-shape.ts`.** `assertRequiredMember` answers about a member the caller already names; a stub has to ask the other question — what is the full set — because a per-name assertion is green over a type that has grown a field, which is exactly how a stub goes stale. Nested braces and string literals are blanked before the read, so an inline object type's members belong to it and a `reason: "ready" | "not-initialised"` union contributes no member called `not-initialised`. Four cases over a fixture type carrying all three shapes, plus the refusal for a type that is not declared — an empty list would make a stub with no fields correct.

  `npm run verify` green: typecheck, `lint:all`, 7175 tests (+6), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The sweep that ends every leaked tree stopped at the first broken one (2026-08-31)

<!-- Fourth bullet of "Lifetime-sweep follow-ups" ("`unmountAllTrees()` runs cleanups, and a cleanup that throws now fails the case that comes AFTER the one that leaked"), the second bullet's "nothing checks that a suite's trees are actually GONE between its cases", and "Nothing bounds how many cleanup failures one message carries" from "Cleanup-failure follow-ups". -->

- [DONE: 2026-08-31] [est: 12000] **`unmountAllTrees()` had the bare loop that `unmountAll()` was fixed for, one level up** — `for (const tree of liveTrees) tree.unmount()` propagates the first tree's `AggregateError`, so every tree BEHIND it stays mounted and `liveTrees.clear()` never runs. The failure that proves a component is broken therefore reopened the exact leak this registry exists to close, and from the `autoUnmount` hook it meant one throwing cleanup handed its live subscriptions to the next case — which is the misattribution the bullet describes, arriving one case later than the tree that caused it. The loop collects now, ends every tree, and reports once.

  **The per-tree aggregates are flattened, not nested.** `unmount()` has already aggregated one tree's failures; an `AggregateError` whose `errors` are `AggregateError`s buries the causes a case counts. Three broken cleanups across two trees are three causes, and a case asserts no cause is itself an aggregate.

  **The message stops at five causes and says how many are left.** `describeThrown` caps each cause at 200 characters and nothing capped the count, so fifty broken components produced a fifty-clause sentence before the reader reached `error.errors` — where all of them still are, uncapped.

  **`__tests__/auto-unmount-hook.test.ts` is the half no sweep can see.** The lifetime sweep reads `autoUnmount()` as a token; the harness suite proves `unmountAllTrees()` on trees it rendered on purpose. Neither says a suite calling `autoUnmount()` at module scope gets its trees taken down BETWEEN its cases — a `node:test` guarantee sixteen suites rely on and nothing pinned. One case cannot observe its own `afterEach`, so the proof spans two: one drops two trees on the floor, the next finds the registry empty and both cleanups run, in order. A fourth case renders and throws, and the fifth reads the registry — the early-return case is precisely the one an `unmount()` at the end of every case never covers. Its own file, because everything in it is about state carried across cases and a suite testing anything else would be one added `render()` away from proving nothing.

  `npm run verify` green: typecheck, `lint:all`, 7169 tests (+8), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The lifetime rule was stated in five places and enforced in none (2026-08-30)

<!-- First bullet of "Tree-lifetime follow-ups", filed minutes earlier this run: "Five suites call `autoUnmount()` and roughly forty others render without it — the opt-in was the right first step and it means the rule is now stated in five places and enforced nowhere. A sweep asking that every suite calling `render(` also calls `autoUnmount()`, with an exemption list ... is the same shape as the AsyncStorage-double sweep next to it." -->

- [DONE: 2026-08-30] [est: 11000] **Sixteen suites mount a tree, not forty — so the adoption was one pass rather than a migration** — the estimate in the filed bullet was wrong in the useful direction: `render(` appears all over the tree in prose and in unrelated identifiers, and the suites that actually build a tree are sixteen. Ten adopted `autoUnmount()`, five already had it, and the whole suite stayed green, which is the answer to the "does any suite carry a tree across cases?" question the opt-in was hedging against: none does.

  **The sweep is the point, and it is the same shape as the AsyncStorage-double sweep beside it.** A suite "mounts a tree" if it imports `render` from `helpers/render` or `providerHarness` from `helpers/mount-provider` — the two ways a tree comes into existence here — and one that does without an `autoUnmount()` is an offender. A companion case asserts the sweep found at least fifteen suites, because a parse that matched nothing would make the rule green over an empty room.

  **`render-harness.test.ts` is the one exemption and it is held honest.** Three of its cases render trees deliberately and assert on the live count, including one that leaves two mounted to prove `unmountAllTrees()` ends them; an `afterEach` would mean the harness's contract was being checked through the thing it is a contract about. `assertExemptionsHonest` keeps the hole alive only while that suite still calls `unmountAllTrees()`.

  `npm run verify` green: typecheck, `lint:all`, 7161 tests (+3), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## A tree a case drops on the floor stays mounted for the rest of the process (2026-08-30)

<!-- Second bullet of "Unmount-phase follow-ups", filed earlier this run: "Nothing runs the sweep when a suite simply drops a tree on the floor — a case that renders, asserts and returns leaves every effect mounted for the rest of the process, which is how a subscription from one case reaches the next one's assertions. `resetStorageNotice` exists in four suites for exactly that reason. A `renderScoped()` that unmounts in an `after` hook ... would replace the per-registry reset helpers with one rule." -->

- [DONE: 2026-08-30] [est: 15000] **`unmount()` made teardown possible and nothing made it happen** — a case that renders, asserts and returns leaves its effects live, so the subscription an effect opened is still in whatever module-level registry it joined and the NEXT case's assertions see it. Four provider suites carry a `resetStorageNotice`-shaped `beforeEach` for exactly that reason: clearing the registry the leak lands IN, rather than ending the tree that leaked into it, which works for the registries somebody thought of and for no others.

  **`autoUnmount()` is one line at module scope and covers the cases that FAIL.** `node:test` takes a root-level `afterEach` from anywhere in the file, so the helper registers one — and unlike an `unmount()` at the end of every case, it also ends the trees of cases that returned early, which are precisely the ones that leave a subscription behind for the next assertion to read.

  **Opt-in rather than automatic.** A suite that renders in one case and asserts in the next is doing something this would break, and whether any does is a question about that suite rather than about the harness. Adopted in the five suites whose registries the reset helpers were papering over.

  **`liveTrees` is a module-level registry with a count seam**, because the outcome cannot see this: a leaked tree and a torn-down one render identically, and the difference is a closure that is still subscribed. `__mountedTreeCountForTests()` is what lets a case assert the count rather than the symptom.

  **Three new cases**: a rendered tree is live until it is unmounted, `unmountAllTrees()` ends two dropped trees and runs both cleanups in order, and it is safe over a tree the case already ended (a cleanup must not run twice).

  `npm run verify` green: typecheck, `lint:all`, 7158 tests (+3), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The capture spy answered for one name, and helpers could load what suites mock (2026-08-30)

<!-- Third and fourth bullets of "Capture-spy follow-ups", filed earlier this run: "`installSpyCapture` mocks exactly one export unless a caller says otherwise, so a provider that starts calling `addBreadcrumb` gets `undefined is not a function`", and "`CaptureContext` is now imported by a test helper ... nothing checks that it stays erased. A future edit turning `import type` into a plain `import` would pull the real `@/lib/sentry` into every suite that mocks it, and the failure would look like a mock that stopped working." -->

- [DONE: 2026-08-30] [est: 14000] **The double replaces the WHOLE module, so a name it does not answer for is `undefined` at the call site** — the failure is `x is not a function` several frames inside a provider, which reads as a bug in the provider. `DiagnosticsProvider` was the only suite that had noticed, and it had noticed by writing three stubs by hand. `INERT_SENTRY` now answers for all eight names the app imports for their value, with `initSentry` returning a promise because every caller awaits or chains it.

  **Inert rather than recording, and the caller still wins.** The point is that a provider reaching for one of these gets a function; a suite that wants the CALLS passes its own recorder through `extraExports`, which is spread after the inert set and over the recorder's own `captureException` in turn.

  **The list is derived in a case rather than trusted.** `mount-provider-harness` parses every `import { … } from "@/lib/sentry"` in `lib`/`components`/`app` and compares, so a ninth name is one red case naming the file to edit rather than a confusing crash in whichever suite reaches it first.

  **`__tests__/helper-lib-imports.test.ts` is the second half, and the rule it states is not "no lib imports".** A helper is evaluated before any suite's mocks register, so anything it pulls in is resolved for real and permanently. A helper may therefore import a `lib/` module for its VALUES only if that module's transitive `lib/` closure imports nothing outside `node:` builtins; `import type` is exempt because it is erased, which is the whole reason `storage-failure-report.ts` may take `CaptureContext` from `lib/sentry` at all.

  **The closure is WALKED, not assumed.** A pure module that grows a dependency turns the guard red at the import that became unsafe, and the message names the edge three modules in rather than the seed: confirmed against a planted value import, which reported `lib/report-storage-failure … reaches "@react-native-async-storage/async-storage" through lib/storage-keys`.

  Two parser bugs found and written down beside the regexes that had them: a `[\s\S]*?` middle matched from the FIRST import in a file to the lib import several statements later (reading every type-only import as a value one), and a doc comment quoting `from "…"` was read as a dependency called "did not look there".

  `npm run verify` green: typecheck, `lint:all`, 7155 tests (+4), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## Two properties that were standing on one example each (2026-08-30)

<!-- Fourth bullet of "Report-helper follow-ups" ("the equality case proves the observer and Sentry agree for ONE error — a property over the classifier is what the comment actually claims") and the second of "Write-filter follow-ups" ("a case proves a failed language write raises no toast; none proves it still reports"). -->

- [DONE: 2026-08-30] [est: 9000] **The observer and the crash report now have to agree for every input the classifier reads** — the existing case reports one quota error and compares the two reasons, under a comment claiming the property ("a second call site would let them drift"). An example cannot say that: a drift introduced for the quota spellings alone — a `name` read at one site and a `message` at the other — passes it. The new case runs all twelve documented inputs (six quota spellings, a `SecurityError`, a bare `Error`, `null`, `undefined`, a thrown string, a bare `{code: 22}`) through both consumers and compares each against `classifyStorageError` itself.

  **A budget reset per input, because the reporter reports a site/keyspace pair once per session** — a shared key would leave every iteration after the first asserting on an empty array, which is the way a loop like this passes without running.

  **The quiet half of the write filter is now quiet only to the USER.** One case proved a failed language write raises no toast; none proved it still reaches Sentry, and the difference is the whole diagnosis for three sites. A regression moving `losesUserData` from the observer into `reportStorageFailure` would have passed the existing case — correctly silent — while silently ending the reports for a language, a currency and a cached FX table. The new case walks `PREFERENCE_WRITE_SITES` and asserts the captured scopes are exactly those three.

  `npm run verify` green: typecheck, `lint:all`, 7151 tests (+2), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## One broken cleanup was hiding the leak in every other component (2026-08-30)

<!-- Fourth bullet of "Unmount-phase follow-ups", filed earlier this run: "A cleanup that throws now takes the whole unmount down with it — `unmountInstance` calls destructors in a bare loop, so the first throw skips every remaining cleanup, parent included, and surfaces as a failure in whatever line called `unmount()`. React logs and continues. A `try` per cleanup, collecting into an `AggregateError`, would keep one broken component from hiding the leak in five others." -->

- [DONE: 2026-08-30] [est: 13000] **A destructor that throws no longer decides whether the rest of the tree comes down** — the loop was bare, so the first throw skipped that component's remaining cleanups AND every later instance in the sweep, parent included. One broken component left live subscriptions behind in five healthy ones, and the failure named only the first of them. React logs and continues; this collects and reports, which is the same guarantee with the evidence kept.

  **The collection spans the whole unmount, not one instance.** `sweep()` and `unmountAll()` each own the list and throw once at the end, because the failure being prevented is a LATER instance being skipped — a `try` inside `unmountInstance` alone would have fixed the smaller half and left the parent unmounted.

  **An `AggregateError` even for a single failure.** The count is the thing a reader needs, and a shape that changes with it hides it: a matcher written against the one-error form would go green-then-confusing the day a second component broke the same way. The message carries every cause inline through `describeThrown`, so `assert.throws(..., /unsubscribe/)` still finds what it is looking for, and `error.errors` holds them intact for a case that wants to count.

  **The cell is cleared BEFORE the call**, so a throw does not leave the destructor armed for a second unmount — which `unmount()`'s idempotence would otherwise re-run.

  **The same bare loop was one function away and went with it.** The StrictMode double-invoke tears every strict effect down and mounts it again; a throw there aborted the rest and then the `mount()` re-ran an effect whose previous instance was still subscribed — a duplicated subscription surfacing as a cleanup failure.

  **Five new cases**: a parent's cleanup runs after a child's threw, two failures are reported together rather than the first, a thrown cleanup is not re-run, a closed conditional branch — an unmount that happens inside a render pass rather than from `unmount()` — reports the same way, and a strict subtree tears down its healthy sibling whatever the broken one did.

  `npm run verify` green: typecheck, `lint:all`, 7149 tests (+5), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## "Free up space" is native advice, and web is the build we deploy (2026-08-30)

<!-- Fourth bullet of "Failure-reason follow-ups", filed earlier this run: "`storageFullMessage` says 'freeing up space usually fixes it' on a platform where the user cannot see this app's storage — on web the quota is per-origin and the fix is the browser's site-data screen, not the phone's storage settings. One sentence covers both because `Platform.OS` is not consulted anywhere in the notice, and the web build is the one this repo actually deploys." -->

- [DONE: 2026-08-30] [est: 16000] **`"full"` names one error class and two situations, and the notice now tells them apart** — on a phone the store is the device's disk, the number is in Settings, and "free up space" is a thing the user can go and do. On web the quota is PER-ORIGIN: this site ran out of its slice, the phone's storage screen does not list this app at all, and the native sentence sends somebody deleting photos over a limit that has nothing to do with them. GitHub Pages is what this repo deploys, so web was the platform getting the wrong sentence.

  **The web sentence deliberately does NOT say "clear this site's data."** That is the browser's own remedy for a full origin and it is the one action that destroys exactly what the toast is warning the user they might lose. "Freeing up space on your device" is the honest half — every engine sizes the origin quota against free disk — so it is both true and safe. A case holds that in all six locales, matching the word for "clear" in each of them rather than trusting the English.

  **`storageNoticeMessageKey(reason, os)` takes the platform as an argument.** The test harness's `react-native` stub reports `Platform.OS === "web"` and cannot report anything else, so a hook reading the module directly would have left the NATIVE sentence unreachable from any case — the one branch this repo's own CI would never run.

  **`storageFullWebMessage` is written in all six locales**, so every row rose 501 → 502 with nothing newly inheriting from English, and the coverage report's published number moved with it.

  **Three new cases**, plus three existing toast assertions that moved to the web wording because the harness is a browser: the platform split, the unchanged sentence for a store that is merely blocked on every platform, and the "never tell them to clear this site's data" rule over all six translations.

  `npm run verify` green: typecheck, `lint:all`, 7144 tests (+3), build, bundle secrets, bundle size (19.0 KiB headroom), bundle smoke. No DB migration, no creds.

## The rule that keeps `extra` safe now fails instead of explaining (2026-08-30)

<!-- Third bullet of "Reported-reason follow-ups", filed earlier this run: "`reason` is now in `extra`, where `scrubPII` still does not look ... The rule that keeps `extra` safe is 'every value here is a literal or a label', and it lives in a comment rather than in anything that fails: a future field built from a key, a title or an email would pass every case in the suite." -->

- [DONE: 2026-08-30] [est: 12000] **`extra` is the one Sentry payload no sweep read, and it is the one nothing strips** — `scrubPII` walks `user` and `request`; `toSentryCaptureContext` forwards `context.extra` verbatim beside them. So every rule keeping user content out of a crash report stopped at the object a caller assembles by hand, and the only thing between an auth id and Sentry was that the two call sites happened to pass labels. `reportStorageFailure` says so in a comment.

  **Two halves, because a key allow-list alone is not the rule.** The first case holds every `extra` key to `{keyspace, reason}` and to `isPiiPropKey`, which is the shape the `addBreadcrumb` sweep already uses. The second pins the VALUE form to shorthand identifiers: `{ keyspace: key }` passes the allow-list while sending the raw key, and `` { label: `${item.title}` } `` sends a title under a key no token in `PII_PROP_TOKENS` matches. Both were confirmed red against a planted `itemTitle: key`.

  **The object is read with `balancedInner`, not a regex body.** `[^{}]*` stops at the first nested brace and silently checks half an object; a greedy read to the last `}` swallows what follows the call. An `extra:` that is not an object literal — a variable, a spread, a call — comes back `null` and is reported rather than skipped, because that is the form no key sweep can rule on.

  **The premise is now checked too.** A case in `sentry-scrub-policy-parity` asserts `scrubPII` returns `extra` untouched. A scrubber that grew an `extra` arm would make the new sweep's stated reason false while every case in both files stayed green.

  `npm run verify` green: typecheck, `lint:all`, 7141 tests (+3), build, bundle secrets, bundle size (21.6 KiB headroom), bundle smoke. No DB migration, no creds.

## One capture spy for the nine suites that were each writing their own (2026-08-30)

<!-- First bullet of "Report-helper follow-ups", filed minutes earlier this run: "every suite that spies on `captureException` still declares its own `captured` array and its own `mockModule` ... an `installSpyCapture()` beside `installSpyToast` would remove the last copied block". Plus the second bullet (the hand-written `reason` union) and the fifth (the `index` parameter nobody passed). -->

- [DONE: 2026-08-30] [est: 18000] **`installSpyCapture()` replaces nine hand-written `@/lib/sentry` doubles, in three spellings that differed only by accident** — four suites DISCARDED the call (`captureException: () => undefined`), three pushed into an array typed `{ scope?: string }` because that is the only field they read, and two typed the context `unknown` because they compare the whole object. Every one of them is the same spy, and the `unknown` half is why `expectStorageReport` had to take a context it could not describe.

  **Recording is the default, including for the four that ignore the array.** A discarding double answers "was anything reported?" with silence, which is the same answer a broken report path gives; keeping the events costs one array and leaves the question askable.

  **`extraExports` comes FIRST in the spread, and a case says why.** `diagnostics-context` drives `initSentry`, `setSentryOptOut` and `shutdownSentry` through the same module, and a second `mockModule` call for one specifier REPLACES the first rather than merging — so the extra exports had to become an argument. Spread after `captureException` instead of before, and a caller with a key of that name would silently displace the recorder.

  **Both helper types are now imported rather than restated.** `reason` was a hand-written `"full" | "unavailable"` and `context` was `unknown`, because a helper that pulled `lib/` in at module scope would evaluate the very modules its suites mock. `import type` is erased before the runner sees it, so `StorageFailureReason` and `CaptureContext` are the app's own — a third reason reaches these suites through the compiler instead of leaving them checking a copy.

  **The `index` parameter finally has a caller.** `use-persisted-blob` provokes two reports from one hook and asserted the first; the second — the `items-user-1` keyspace, which is what proves the per-keyspace budget treats two blobs independently — was the one nothing was reading.

  **Four new cases.** The adoption sweep (no top-level suite may contain `mockModule("@/lib/sentry"`), its own exemption held honest by `assertExemptionsHonest` because a guard cannot search for a string without containing it, the spy records rather than discards, and the two type imports are real.

  `npm run verify` green: typecheck, `lint:all`, 7138 tests (+4), build, bundle secrets, bundle size (21.6 KiB headroom), bundle smoke. No DB migration, no creds.

## One strict assertion for the storage report, in one place (2026-08-30)

<!-- First two bullets of "Reported-reason follow-ups", filed minutes earlier this run: "three cases assert the whole `extra` object and a fourth field will move all three again ... the pressure that puts on the next contributor is to loosen the assertion", and "nothing asserts that the reason in the event equals the reason the observer got". -->

- [DONE: 2026-08-30] [est: 8000] **`expectStorageReport(captured, {scope, keyspace, reason})` replaces four hand-written whole-object comparisons in two suites** — the strictness is the point (a report that grows a field is a decision somebody should see, and a `.keyspace` lookup would let one through) and it was also the cost: adding `reason` an hour earlier moved three cases in two files, which is the pressure that ends with somebody loosening the assertion instead of updating it. The comparison now lives once, in `__tests__/helpers/storage-failure-report.ts`, with the reason it must stay strict written beside it.

  **The helper asserts the WHOLE context, not the `extra`.** Two of the four sites checked only the `extra` and passed no scope; folding them in cost one line each and made every site check the field that says which write broke.

  **One new case pins the property the refactor cannot**: the observer's `reason` and the Sentry event's `extra.reason` are the same value. They cannot differ today — the classification runs once and is passed to both — which is exactly why nothing would notice if a second call site appeared and the toast and the crash report started disagreeing about the same failure.

  `npm run verify` green: typecheck, `lint:all`, 7134 tests (+1), build, bundle secrets, bundle size (21.6 KiB headroom), bundle smoke. No DB migration, no creds.

## The guess the user was shown now reaches the crash report (2026-08-30)

<!-- Second bullet of "Failure-reason follow-ups", filed minutes earlier this run: "Sentry gets the error and the scope but not the reason the user was shown ... a `reason` in the captured `extra` would let somebody ask how often the guess says `full`, which is the only way to find out whether the quota spellings cover the devices this app actually runs on." -->

- [DONE: 2026-08-30] [est: 9000] **`reason` rides in the Sentry `extra` beside the keyspace** — `classifyStorageError` is this app's OPINION about an error rather than something the error states, and until now it existed only in a toast that vanishes: the event carried the raw error and the scope, so "how often does the guess say full?" and "which devices does the spelling table miss?" were unanswerable. One field closes both.

  **Computed once, used twice.** The classification already ran per report for the observer; it now also goes to `captureException`, which keeps the observer and the crash report saying the same thing by construction — a second call site would let them drift the day somebody adds a spelling to one path.

  **Three expectations moved with it**, and each one is a case that asserts the whole `extra` rather than a field of it: the PII case in `report-storage-failure`, the language-key case beside it, and `use-persisted-blob`'s. That is the right shape — a fourth field added silently is exactly what these are for — so the update is the cost of the check working.

  **One new case pins the pair end to end**: a `SecurityError` reported from `premium-context.setItem` lands in Sentry as `{keyspace, reason: "unavailable"}`, which is the half a classification unit test cannot see.

  `npm run verify` green: typecheck, `lint:all`, 7133 tests (+1), build, bundle secrets, bundle size (21.6 KiB headroom), bundle smoke. No DB migration, no creds.

## "Restart the app" is useless advice on a full disk (2026-08-30)

<!-- Fifth bullet of "Write-filter follow-ups", filed minutes earlier this run: "the toast's own sentence still describes only the loud half's cause ... it explains a broken device, not a full one, and 'Restarting the app usually fixes it' is false for a disk that is out of space. The diagnosis that could tell those apart is in the Sentry event." -->

- [DONE: 2026-08-30] [est: 22000] **The notice reads the cause off the error and offers the fix that can actually work** — one sentence covered two opposite situations: a store behind a privacy setting (restart) and a device with no space left (delete something). Whichever half the copy chose, it was wrong for the other, and the information needed to choose was already in the `error` the reporter hands Sentry.

  **`classifyStorageError(error)` is the guess, and it is deliberately a conservative one.** Web spells the quota `QuotaExceededError` (code 22), Firefox `NS_ERROR_DOM_QUOTA_REACHED` (1014), and the React Native backends surface `SQLITE_FULL: database or disk is full` or `Errno 28: No space left on device` with no `name` at all — so name, code and message are all read. Anything unrecognised is `"unavailable"`, because that is the sentence that does not send somebody deleting photos over a `SecurityError`.

  **The reason rides on the observer event beside the scope and the keyspace**, so the React-free call sites (`lib/tombstones.ts`, `lib/sync-cursors.ts`) get the right sentence without knowing there is one. The gate half passes `"unavailable"` explicitly: a refused hydrate is a READ that failed before anything was written, and nothing about it says the device is out of space.

  **One title, two messages.** `storagePersistRefusedTitle` ("Changes aren't being saved") is what HAPPENED and is true either way; the new `storageFullMessage` is what to DO. Written in all six locales, so every row rose 500 → 501 with nothing newly inheriting from English, and the coverage report's published number moved with it.

  **Seven new cases.** Six spellings of the quota classify as `full`; a `SecurityError` and a bare `Error` do not; `null`, `undefined`, a thrown string and a bare `{code: 22}` are all values a `catch` can really receive. End to end: a quota-rejected write raises `storageFullMessage`, a blocked one raises the restart sentence, and `premium-provider-mounted` reads the full-disk sentence because its `writeError` names a quota.

  `npm run verify` green: typecheck, `lint:all`, 7132 tests (+6), build, bundle secrets, bundle size (21.6 KiB headroom), bundle smoke. No DB migration, no creds.

## A language that would not save is not "changes aren't being saved" (2026-08-30)

<!-- Third bullet of "Listener-split follow-ups", filed earlier this run: "the write half no longer knows whether a user is signed in, and the string still says 'your changes' ... a language preference that could not be saved is not 'your changes aren't being kept', and the write filter has no equivalent of the read filter's judgement". -->

- [DONE: 2026-08-30] [est: 14000] **The write filter makes the judgement the read filter always made** — the observer ignored a failed READ because "a currency preference that could not be read costs the user a default, not their data", and then raised "Changes aren't being saved" for a failed WRITE of that same currency. Three of the fifteen write sites hold a preference or a derived cache: `i18n-context.setItem` (language), `locale-helpers.setItem` (currency), `currency-rates.setItem` (a cached FX table with a TTL). None of them costs the user anything but a default at the next launch and a refetch.

  **`losesUserData(scope)` replaces `isWriteFailure(scope)` at the one call site, and `PREFERENCE_WRITE_SITES` is the list.** The default is LOUD: a site added to `STORAGE_FAILURE_SITES` without a decision raises, because the failure mode of guessing wrong in that direction is a toast nobody needed rather than silent data loss.

  **`DATA_WRITE_SITES` is derived from the site list rather than written beside it**, so the case that pins the split reads the real enumeration: the two halves must together cover every `.setItem` exactly once, and be disjoint. A sixteenth site turns it red until somebody says which half it is in — which is the whole point, given that the quiet half is a claim about what the user can afford to lose.

  **Four new cases.** A language that could not be saved raises nothing; a rejected `chat-context` write still does; the halves cover and do not overlap; `losesUserData` answers false for both halves of a read pair rather than for a whole module.

  `npm run verify` green: typecheck, `lint:all`, 7126 tests (+4), build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke. No DB migration, no creds.

## The harness had no unmount phase (2026-08-30)

<!-- First bullet of "Listener-split follow-ups", filed minutes earlier this run: "the only thing that would notice a leak is a seam a suite calls by hand ... a component that subscribed without returning its cleanup would accumulate one observer per remount, every one of them would work". The seam stays; what it now has is a real unmount to measure. -->

- [DONE: 2026-08-30] [est: 24000] **`render()` grew an unmount phase, so "does this component clean up after itself?" is finally a question a case can ask** — effect cleanups ran only when a dep changed, which meant a probe dropped from the tree kept its instance, its state AND its subscription. Every suite that cared about a teardown worked around it by calling the subscription API by hand and asserting on the registry, which tests the registry rather than the component: the storage-notice unsubscribe case said so in its own comment.

  **The instance map now knows what the pass did NOT render.** `runComponent` records each key it visits, and the sweep after the walk tears down every instance missing from that set — child before parent, which is reverse insertion order and the order React destroys a deletion in. So a closed conditional branch is an unmount now: its effects clean up, and a re-added branch gets fresh state instead of the state it had before it disappeared.

  **`tree.unmount()` is the whole-tree form.** Idempotent, and `rerender`/`press` throw afterwards with a message that says why rather than a `TypeError` three frames down.

  **Cleanups are found through a per-instance `effectCells` set rather than by type.** An effect stores its cleanup in its own hook cell, and `useCallback` stores a function in one too — "every cell holding a function" would have called a memoised press handler as a destructor.

  **Nine new cases: seven on the harness contract, two on the notice.** The storage-notice unsubscribe case is now a real unmount (`observer count 1 → 0`, and a write reported afterwards raises nothing), and the hand-rolled registry version it replaces is gone. On the harness: teardown order, idempotence, the refusal to re-render, the closed branch, fresh state for a re-added one, an effect that returns no cleanup, and the dep-change re-subscribe that pins the existing behaviour the sweep must not disturb.

  `npm run verify` green: typecheck, `lint:all`, 7122 tests (+9), build, bundle secrets, bundle size (24.2 KiB headroom), bundle smoke. No DB migration, no creds.

## One listener for a registry that reaches the whole tree (2026-08-30)

<!-- First bullet of "Storage-notice follow-ups", filed the run before: "five providers each mount their own observer and the registry now holds five entries doing the same work ... one `<StorageNotice />` beside `ToastProvider` in `app/_layout.tsx` would subscribe once". The trade it named — the gate half would need the flag out of five context values — is the one this did NOT take: the gate half stays where it is. -->

- [DONE: 2026-08-30] [est: 21000] **The write half of the storage notice subscribes once, from the app shell, instead of five times from the providers** — `useStorageNotice` did two jobs with two different multiplicities: a per-provider gate predicate, and a subscription to a MODULE-LEVEL registry that already carries every failing write in the tree. The second one needs one listener; it had five, and the latch made the extra four invisible from every assertion about what the user sees.

  **`useStorageFailureNotice()` is the split, and `components/storage-notice.tsx` is its only caller.** It renders `null` and is mounted directly under `ToastProvider` in `app/_layout.tsx` — ABOVE the auth gate on purpose, because the providers below it unmount around a sign-out and an account switch, and a store that fills up during one is exactly when the user needs telling. Both hooks raise through the same module-level latch, so a device that fails both ways still says it once.

  **The cost of the split is stated in a case rather than discovered later.** A provider that reports a rejected write now reaches nobody unless the listener is mounted, so the mount IS the adoption requirement: four cases hold it — exactly one module in the tree calls the hook, `app/_layout.tsx` renders `<StorageNotice />`, the tag sits between `<ToastProvider>` and its close, and it sits above `<AuthProvider>`.

  **`__storageFailureObserverCountForTests` exists because the outcome cannot see this.** Five subscribers and one subscriber raise the same single toast; the difference is five closures and five `Set` entries. The new cases assert the COUNT: two gated probes add zero observers, the listener adds exactly one, and a re-render does not add a second.

  **`mountStorageNoticeListener()` in the shared mount harness keeps the end-to-end fact end-to-end.** `premium-provider-mounted` asserted that a rejected `setItem` reaches the user; that assertion now mounts the shell's listener beside the provider — and so does its silent twin, which would otherwise pass because nobody was listening rather than because the session was healthy.

  `npm run verify` green: typecheck, `lint:all`, 7113 tests (+9), build, bundle secrets, bundle size (24.2 KiB headroom), bundle smoke. No DB migration, no creds.
