## One sentence per thing the archive did (2026-09-12)

<!-- Third bullet of "Archive-row follow-ups" (2026-09-12), carried through four rounds since "Bulk-outcome follow-ups". -->

- [DONE: 2026-09-12] [est: 8000] **`archiveActionDoneListingRemoved` — a purpose-written sentence for an outcome the bulk path had learned to compose** — the item screen said "Item archived and listing removed" in six languages; the bulk bar said `itemsArchived` and then `bulkListingsRemoved`, one clause per thing the act did. Two shapes for one distinction, and the single-item key was simply the older one.

  **The composed shape is the one that scales.** A third thing an archive also did (a reminder cancelled, a share revoked) is another clause in the bulk path and a fourth hand-written sentence in the other. `${t("archiveActionDone")} ${t("bulkListingsRemoved", { count: 1 })}` reads "Item archived · 1 listing taken down" — a count of one where a purpose-written sentence would not have had a number, which is the cost, and the reason the suggestion carried four rounds as a decision rather than a chore.

  **No `count > 0` gate on this branch, unlike the bulk one.** `listedOutcome()` on `app/collection/[id].tsx` exists because a selection may contain no listings; this branch only runs when `isOpenListing(existingListing)` is true, so there is exactly one and the conditional would be dead code arguing that it might not be.

  **The removal is asserted in both directions.** `lint:orphan-i18n` catches a key nothing reads; `assertNoLocaleDeclares` catches the other half — a removal that reaches `en` and misses `ru` is invisible at runtime, because the inheriting locales serve English either way. The claim the retired key carried is restated as a property of the composition: every locale's suffix is a sentence rather than decoration, so the listed toast still says materially more than the plain one.

  **The item-detail family did not shrink.** `i18n-complete-families.test.ts` counts 50 before and after — a key left the base vocabulary and the screen still renders a sentence about the listing, now the one the bulk bar already paid for. That is what a screen family counts and a per-key diff would not show, and it is the entry that says why the total went DOWN for the first time in the coverage log.

  2 new cases and 2 restated in `__tests__/archive-retires-listing.test.ts` (8734 in the suite). 6 keys removed, 0 added; 542 keys × 6 locales. `npm run verify` green: 8734 tests, 20/20 guards, 13.6 KiB of headroom — 0.7 KiB back, which is what six sentences weigh. No DB migration, no creds.

## The bulk bar cannot be handed nothing to do (2026-09-12)

<!-- Second bullet of "Archive-row follow-ups" (2026-09-12), carried through four rounds since "Bulk-outcome follow-ups". -->

- [DONE: 2026-09-12] [est: 7000] **`<BulkBar count={n} onCancel={f} />` type-checked and rendered a selection count over an empty button row** — a selection mode a user can enter and cannot resolve. The four actions became optional the day the archive screen mounted the second copy of the bar, which is what lets that screen state its refusal of a "delete all" by omission; the same change made the one combination no screen could mean a legal call.

  **A mapped union, not four written-out variants and not a discriminated one.** `AtLeastOneAction` is `{ [K in keyof BulkBarActions]-?: Required<Pick<BulkBarActions, K>> & Omit<BulkBarActions, K> }[keyof BulkBarActions]` — one member per action, generated. A discriminated union would have to name a variant per screen, which is a second place to edit for a third screen offering Move and Archive and no Delete; this names none. The suggestion that carried this asked for "a case asserting at least one action" as the cheap half, and the union is the same size in lines and is checked on every build instead of on every run.

  **The enforcement is five `@ts-expect-error` declarations and they fail in BOTH directions.** `tsconfig.json` includes `**/*.ts`, so `__tests__` is type-checked: each suppression is an assertion tsc makes, and if the union ever stops rejecting an action-less bar the directive becomes unused and the typecheck leg goes red on the comment itself. That is the half a runtime case cannot do, because the bad call never renders in a suite.

  **`onRestore: undefined` is rejected too**, which is the shape a screen reaches by spreading a half-built props object rather than by choosing it — and the one an `onRestore?: () => void` alone would have accepted.

  **The source half is a sweep, because a type says what is expressible and not what the screens pass.** Both call sites are matched on `<BulkBar\s+count=` rather than a bare tag: both files also name `<BulkBar>` in prose, and the first draft's lazy match ran from a comment to the next self-closing tag and found four call sites where there are two.

  9 new cases in `__tests__/bulk-bar-actions.test.ts` (8734 in the suite). 0 new keys, 0 bundle cost — the whole change is erased at compile time. 1 existing doc block restated: "every action is optional and Cancel is not" was the whole truth for a day, and a reader who stopped there would write the call the union now rejects. `npm run verify` green: 8734 tests, 20/20 guards, 12.9 KiB of headroom. No DB migration, no creds.

## The archive row is a memoized component (2026-09-12)

<!-- First bullet of "Bulk-restore follow-ups" (2026-09-12), written the same day the selection mode that made it matter shipped. -->

- [DONE: 2026-09-12] [est: 7000] **The screen built a `selectedById` map and then rendered inline** — which is the map without the latch that makes it worth building. The collection screen pairs its map with `<SelectableItemRow>`, a `React.memo` row, so a toggle re-renders two rows; the archive screen re-rendered every visible one, on a window of forty rows that each mount a remote image.

  **`<ArchiveRow>` takes a `collectionName` string, not the collection**, and reads the theme and `t` itself rather than being handed them — so the props that decide a re-render are the item and its two booleans. A theme object passed down would be a fresh reference on every parent commit and the memo would skip nothing, which is the failure mode that makes a memo look like it works.

  **`ARCHIVE_ROW_HEIGHT` moved with the markup and the screen imports it.** The number is a fact about the row's own styles, and "declared once" survives the move only if the list reads the component's copy — a constant left behind in the screen is exactly the drift the original constant was extracted to prevent.

  **6 existing pins restated in `archive-screen.test.ts` and 2 in `bulk-restore.test.ts`**, each split along the same line: what a row RENDERS is read from the component, what the screen DOES from the screen. The Restore case now reads both halves — the row calls `onRestore(item.id)`, the screen passes `handleRestore` — because the split is the claim.

  0 new cases, 0 new keys; `components/archive-row.tsx` is named by a suite through the pins that moved onto it. `npm run verify` green: 8725 tests, 20/20 guards, 12.9 KiB of headroom. No DB migration, no creds.

## The archive screen restores a selection (2026-09-12)

<!-- Second bullet of "Bulk-outcome follow-ups" (2026-09-12), carried in every group since the bulk-archive round and the oldest unactioned item in that file. -->

- [DONE: 2026-09-12] [est: 9000] **One gesture puts thirty rows in the archive and thirty taps got them back** — `archiveItems` shipped on the bulk bar and the archive screen took its rows out one at a time. That is not a missing convenience: the whole argument for archiving over deleting is that the way back is cheap, and it stops being true at exactly the scale the other direction was built for.

  **`unarchiveItems` is the mirror, minus the clock.** `archiveItems` stamps ONE timestamp for a selection so a single act does not scatter across the top of the archive list; this clears a field, so there is nothing to stamp and nothing to order — every row it touches leaves the list. It clears with `null` and not `undefined`, which is the whole of `unarchiveItem`'s doc block at thirty times the size: `updateRemoteItem` writes a field only when the key is PRESENT in `updates`, so an `undefined` would clear thirty local flags, leave thirty cloud rows archived, and let the next sync put the selection straight back in the trash.

  **The interesting part of the screen half is what it does NOT pass.** `app/archive.tsx` has refused a "delete all" since it was written — a place people reach after a mistake is the worst place for a one-tap way to make a bigger one — and mounting a bar that can render one is the first thing that could have quietly retired that argument. So `<BulkBar>`'s actions became optional and the archive screen states its answer by omission: `onRestore`, `onCancel`, nothing else, with a case failing on any of the other three appearing at that call site.

  **`rowSelected` changes two colours and no geometry.** `getItemLayout` promises every row is exactly `ROW_HEIGHT`; a selection style that added a 2px border would make that promise false for precisely the rows a user has touched, which reads as a scroll bug and only after you interact. Its own case, listing the properties that would grow a row.

  **No confirm in front of the bulk restore**, for the reason the single-row restore has none: nothing it does is destructive, and the way to undo it is the action that filled this screen. The destructive resolution stays one row at a time behind the confirm it has always had.

  24 new cases in `__tests__/bulk-restore.test.ts` (8725 in the suite); 1 new key in all six locales. 3 existing pins restated: the archive screen's context destructure (now a membership check, since `unarchiveItems` joined a line that wraps) and its footer (a fragment now, so the case reads the button rather than the prop) — and `bulk-archive.test.ts`'s five body slices, which were `indexOf("archiveItems: async")` and would have silently grown to cover `unarchiveItems`, a name that CONTAINS that string. `npm run verify` green: 8725 tests, 20/20 guards, 13.6 KiB of headroom, +13.1 KiB drift of which +4.1 KiB is copy. No DB migration, no creds.

## The bulk toasts count the listings they withdrew (2026-09-12)

<!-- First bullet of "Archive-polish follow-ups" (2026-09-12), carried since the bulk-archive round. -->

- [DONE: 2026-09-12] [est: 7000] **Both confirms counted listings and both outcome toasts counted only rows** — so a seller who archived thirty items and took four listings down was told about the thirty. That is the half they least need to have seen, because the other half is the one other people can see: four standing offers withdrawn from every buyer's device, reported as nothing.

  **`bulkListingsRemoved`, composed onto the existing counted message** rather than a second pair of keys — the same shape the confirms already use, and one string instead of four. A case asserts it is a different sentence from `bulkListedWarning` in every locale: one is a confirm asking about something that has not happened and the other reports what did, and a locale that reused the warning would tell a seller their listings "will be taken down" after they already were.

  **Read before the selection is cleared.** `selectedOpenListings` derives from `selectedIds` and `exitSelectionMode` empties it two lines below the toast, so the outcome is composed into a local after the await and before the exit. Its own case, on both bulk paths, asserting the ordering rather than the presence.

  4 new cases (33 in the suite); 1 new key in all six locales, declined in the three Slavic ones. 2 existing counts restated. `npm run verify` green: 8698 tests, 20/20 guards, 17.1 KiB of headroom, +9.7 KiB drift of which +3.5 KiB is copy. No DB migration, no creds.

## The archive screen finishes its list (2026-09-12)

<!-- Third and fourth bullets of "Budget-history follow-ups" / "Archive-list follow-ups" (2026-09-12): the missing `getItemLayout` and the missing pull-to-refresh. -->

- [DONE: 2026-09-12] [est: 6000] **Two things every other list screen has** — `getItemLayout`, without which FlatList measures every row as it mounts and cannot skip to an offset; and pull-to-refresh, which this was the only list screen without, so reconciling an archive across two devices meant leaving the screen and coming back.

  **`ROW_HEIGHT` is declared once and read by both the stylesheet and the layout.** A `getItemLayout` whose number disagrees with the style places rows where they are not, which looks like a scroll bug and is a constant. A 56px thumbnail in 12px of padding, and the text column is shorter than the thumb — a 15px title and two 12px meta lines come to roughly 50 — so the thumb sets the height and nothing in the row can grow past it.

  **Which is the property that makes a fixed layout honest**, and it holds because both text fields are capped at one line and the date is a ten-character ISO prefix. Its own case, counting the caps: a row whose text could wrap would need the measuring back.

  3 new cases (41 in the suite). 0 new keys. 1 existing pin restated — `refresh` joined the context destructure, which is not what that case was claiming. `npm run verify` green: 8694 tests, 20/20 guards, 18.4 KiB of headroom. No DB migration, no creds.

## The budget keeps its own history (2026-09-12)

<!-- Fifth bullet of "Page-size follow-ups" (2026-09-12), carried eight times and the oldest unactioned item in that file. -->

- [DONE: 2026-09-12] [est: 10000] **Four raises, each argued against the one before it, in prose** — `lib/bundle-size.ts`'s doc block is four paragraphs doing a table's job, and a reader wanting the RATE had to reconstruct it by reading all four. Every build now prints `budget raised 3 times since 2026-09-10 (+61.4 KiB in total)`.

  **`BUDGET_HISTORY`, newest first, and the head IS the snapshot.** A raise adds a row rather than editing one, which is what makes the trend accumulate instead of being overwritten by the measurement meant to extend it. Each row carries the budget it justified — without that a row is half an argument, since how much was spent is only meaningful against how much was bought — plus the date and one line on what spent it, with a case rejecting a `because` under forty characters.

  **`translationsBytes` is `null` on the three rows that predate the copy measure.** The honest answer for a number nobody took, and better than back-filling it from today's file, which would describe a translations module nine keys larger. `formatCopyDriftLine` returns nothing for a null baseline rather than subtracting from zero, which would report the whole translations module as one round's growth.

  **The trend line says nothing for a one-row history**, because "raised once" is not a trend, and it points at the rows rather than restating them — four `because` lines in a build log would be the prose the history replaced, printed on every commit.

  **Two rules the repo already had, applied.** `plural(raises, "time", "times")` rather than a hand-rolled ternary, and `lib/bundle-size.ts` named in `lib/plural.ts`'s importer paragraph — which is worth the sentence it got: a build-log line nobody translates is still a sentence with a count in it, and the alternative is a second rule for the places the first was judged not to matter. The `if (history.length < 2)` guard is the FOURTH entry in `plural.test.ts`'s documented if-shaped hole, and the same kind as the other three: a bounds check on how many rows a claim needs, in a module that does import the rule for its actual inflection.

  11 new cases (17 in `budget-snapshot.test.ts`). 3 existing pins restated. `npm run verify` green: 8691 tests, 20/20 guards, 18.7 KiB of headroom. No DB migration, no creds.

## One page size stops doing five jobs (2026-09-12)

<!-- Second bullet of "Archive-list follow-ups" (2026-09-12), carried since the load-more rounds. -->

- [DONE: 2026-09-12] [est: 8000] **`DEFAULT_CHUNK_PAGE_SIZE` was 20 and every one of the five windows took it** — across rows that differ by an order of magnitude in what they mount: a collection card with a full-bleed remote cover against an archive row with a 56px thumbnail and two lines of text. The export existed to be overridden and nothing overrode it for as long as the hook existed.

  **`CHUNK_PAGE_SIZE_CARDS = 12` and `CHUNK_PAGE_SIZE_ROWS = 40`** — the two shapes the app actually has. Twelve collection cards is twelve remote images decoded before the user has scrolled anywhere, which is the cost the hook exists to bound and the one the shared default was never chosen against; forty compact rows stops a list of thirty asking for a Load-more press for no reason.

  **The numbers are argued, not measured, and both the module and the suite say so.** Nobody has profiled either row on a device; 12 and 40 come from what the rows CONTAIN. What is defensible is the split — one number could not be right for both — and the shape that makes a measured answer possible later. A case asserts that disclaimer is still in the doc block, because a reader who takes them for profiler output would be reluctant to change them, which is the opposite of the point.

  **The default stays, bracketed by the two.** A sixth window whose row shape nobody has decided about should get a working default rather than a guess dressed as a decision — and the bracketing is what says one number could not have been right.

  12 new cases in `__tests__/chunk-page-sizes.test.ts`, including one that fails on any future window taking the bare default. 8 existing pins restated across 6 suites: each spelled `useChunkedList(x)` with no size, which was never what any of them was claiming. `npm run verify` green: 8680 tests, 20/20 guards, 18.7 KiB of headroom. No DB migration, no creds.

## The archive screen recycles its rows (2026-09-12)

<!-- Fifth bullet of "Approximate-marker follow-ups" (2026-09-12), carried through six rounds since "Archive-window follow-ups". -->

- [DONE: 2026-09-12] [est: 7000] **The window bounded the start and nothing bounded the ceiling** — the screen mapped `visibleItems` into a `<View>` inside a `<ScrollView>`, so the chunked window capped how many rows were MOUNTED FIRST and nothing recycled them: five presses of Load more held a hundred rows and a hundred remote cover photos. On the one list in the app with no ceiling of its own — nothing prunes an archive, every sale adds to it, and one gesture on the bulk bar can put thirty rows in it.

  **`<Screen scroll={false}>` with the list owning the scroll**, the pattern `lib/flat-list-styles.ts` describes and collection detail established. The hero is the header, the empty state is `ListEmptyComponent` — the same claim as the `length === 0` ternary it replaces, made by the thing that actually knows whether the list is empty — and the Load-more button is the footer.

  **The window stays, and so do both ways of growing it.** `onEndReached` for a scroll that reaches the end, `<LoadMoreButton>` for the platforms and gestures where that never fires, and `remaining > 0` as the single gate for both.

  4 new/restated cases (38 in the suite). 0 new keys. `npm run verify` green: 8668 tests, 20/20 guards, 19.0 KiB of headroom — the change cost 0.3 KiB, which is what swapping a mapped View for a FlatList already in the bundle looks like. No DB migration, no creds.

## The app says what the PDF was taught to say (2026-09-12)

<!-- First and second bullets of "Total-caveat follow-ups" (2026-09-12): "`approximate` is rendered in one place" and "The stats screen renders `skipped` and not `approximate`". -->

- [DONE: 2026-09-12] [est: 11000] **Three in-app surfaces showed a raw sum of three currencies as though it were one number** — `approximate` shipped for the export, where the claim is permanent, and the collection cards, the collection-detail summary and the stats screen said nothing. The argument for their silence was that they re-render when the rates land, and that is an argument for a QUIETER marker than a sentence rather than for silence: a user reads the figure in the meantime, and "1,500 USD" for items priced in three currencies is a claim the app is making.

  **The cards take the `≈` they already print.** `<CostBadge>`'s item mode has marked a converted cost with that symbol since it was written, and the two questions are genuinely different — item mode asks whether a conversion changed the currency, raw mode whether there was a rate table at all — which is why the flag is a prop rather than a shared computation. They happen to print the same symbol because they mean the same thing to a reader.

  **The stats screen gets the words, and the worse claim wins.** It is the surface whose entire job is one total, and it was saying "excludes N items" about the smaller problem while staying silent about the larger. A figure summed with no rate table is in no single currency; being short of a few items is not the same thing, and a case asserts the branches are in that order.

  **`<CollectionCard>` takes one total instead of a pair.** Four call sites spread `amount` and `currency` by hand from the one object the provider hands them, and `approximate` would have made it three props — the prop-pair smell an earlier round flagged, collected by the feature that would have made it worse.

  **Both summary-card branches are marked.** Owners get a tap-to-swap `Pressable` and non-owners a plain `View`, with the badge written out in each; a case counts two, because the realistic slip is marking one.

  14 new cases in `__tests__/approximate-total-marker.test.ts`; 1 new key in all six locales. 2 existing pins restated — including a `<CostBadge>` adoption case that matched the prop list exactly, which made it a pin on the arguments rather than on the adoption it is about. `npm run verify` green: 8666 tests, 20/20 guards, 19.4 KiB of headroom, +7.4 KiB drift of which +2.5 KiB is copy.

## The exported total says when it is not what it looks like (2026-09-12)

<!-- First and second bullets of "Export-money follow-ups" (2026-09-12): "The export prints a partial total with nothing saying it is partial" and "The no-rates path prints raw amounts under a converted label, in the one artifact that is kept". -->

- [DONE: 2026-09-12] [est: 13000] **`approximate`, and a caveat under the PDF's total** — two ways a total can be less than it appears, and the document stated neither.

  **The first was on the shape and unrendered.** `skipped` counts items in a currency the rate table did not hold; they are simply absent from the sum, and nothing but the stats screen had ever rendered the field.

  **The second was invisible even to a caller that wanted to know.** With no rate table at all, `collectionTotalCost` summed raw amounts and reported them as fully `converted` — indistinguishable from a total that really had converted everything. That is a fine lie for a card which re-renders two seconds later, and it is not fine for the one output a user keeps, mails and files: a PDF exported during a cold start states a sum of three currencies as a figure in one, permanently, with nothing on it saying so.

  **So the flag exists for the consumers that cannot re-render**, and it is a genuinely different question from `skipped`: `approximate` is about the TABLE, `skipped` about the currencies in it. A total can be exact-but-partial, which is the commoner case. Its own case, both directions.

  **`emptyCollectionTotal` is not approximate.** Zero of zero items is exact in every currency, so there is nothing a rate table would have changed — and a case keeps it equal to what `collectionTotalCost([])` answers with rates and without.

  **The unconverted sentence wins over the partial one** when both could apply, because a total summed with no table at all is the worse claim. Resolved on the screen, since the builder takes strings and the counted sentence needs a number — and the screen reads the total ONCE, because two accessor calls could straddle a rate table landing and print a caveat about a figure that is no longer on the page.

  17 new cases in `__tests__/export-total-caveat.test.ts`; 2 new keys in all six locales. 23 existing expectations gained the field, and 2 case titles were restated: "sums raw amounts and reports them as converted" was true, and was the bug. `npm run verify` green: 8652 tests, 20/20 guards, 20.3 KiB of headroom, +6.4 KiB drift of which +1.9 KiB is copy — the first round in which the copy line has printed at all, and it says a third of the growth was sentences.

## Deleting a collection takes its listings with it (2026-09-12)

<!-- Second bullet of "Departure-path follow-ups" (2026-09-12): "`deleteCollection` deletes its items and is not in the record". -->

- [DONE: 2026-09-12] [est: 12000] **The record caught its own blind spot on the first day** — the previous round built a list of every path that touches an item and derived it from mutations whose PARAMETER was an item id. That is the shape of the paths that were already known, so it found eight and missed six — among them `deleteCollection`, which removes a collection AND every item in it and is the largest departure path in the app. Deleting a collection of thirty left thirty standing offers on every buyer's device, each pointing at an item that no longer exists anywhere.

  **The sweep had the same blind spot as the rounds it was written to catch, one level up**, which is the finding worth more than the bug. It asks `does this write setLocalItems?` now — a question about what a mutation DOES rather than what it takes, and one a parameter cannot dodge. Fourteen paths instead of eight, and the six it had been missing are their own case so a narrowing cannot pass quietly.

  **No new strings.** Deleting a collection IS the bulk delete with the selection implied, so its confirm takes `bulkListedWarning` — the same counted sentence, already declined in three Slavic locales. A separate key would have been the same warning translated twice.

  **The six new verdicts are mostly exemptions, and each says why.** `addItem` and `addWishlistItem` are arrivals; `reorderItemsInCollection` writes `sortOrder` and a listing has no notion of position; `transferItemToBuyer` is the buyer's copy of a thing they just bought. `deleteUserContent` is the interesting one: an admin dropping another user's content from THIS device, where the listings belong to that user's account and are not the viewer's to withdraw — one device deciding what another person is selling.

  3 new cases (13 in the suite), 6 new verdicts, 0 new keys. `npm run verify` green: 8635 tests, 20/20 guards, 23.2 KiB of headroom, +3.6 KiB drift and no copy line. No DB migration, no creds.

## Every path that touches an item has a listing verdict (2026-09-12)

<!-- First bullet of "Snapshot follow-ups" (2026-09-12): "`moveItems` is the one bulk resolution that still ignores listings, and four rounds have now declined to say why". -->

- [DONE: 2026-09-12] [est: 11000] **`LISTING_RULE_BY_ITEM_PATH` — the answer to a question four rounds carried, and the list that would have caught the class** — two bugs this week were the same bug: a path that touches an item and does not ask the question the others ask. The archive round taught `archiveItem` to retire an open listing and did not think about `deleteItem`; the round that fixed `deleteItem` did not think about the bulk pair. Each was found by writing a suggestion afterwards rather than by anything in the tree, because the failure is always a path nobody remembered — and a rule stated only in the paths that follow it cannot name the one that does not.

  **`moveItems` is EXEMPT, and that is a decision rather than an omission.** A listing carries `itemId` and nothing about the collection, and `app/listing/[id].tsx` resolves its item with `getItemById(listing.itemId)` alone — so moving an item between collections is invisible to the marketplace, and retiring a listing there would withdraw a live offer for no reason. Three cases hold that reasoning against the code it depends on: the listing screen's resolution, the absence of `collectionId` from the `MarketplaceListing` shape, and the verdict's own wording. Four rounds carried this as an open question and the answer turned out to be a sentence no amount of copying the archive path would have produced.

  **The expensive half is the case that finds a NINTH path.** The eight entries are derived from the provider's own context type rather than listed twice, so a mutation added to the type and not to the record is red until somebody decides which kind it is. That is what turns "somebody has to remember" into "the build says so".

  **Every verdict carries an argument, enforced.** A case rejects a reason under sixty characters, because "EXEMPT" alone is exactly the state the four `moveItems` rounds were already in.

  **A verdict of "retires" is a claim about a CALL SITE.** The providers cannot see each other, so the rule is enforced by screens — a real limitation, and the premise of the whole record. A case asserts the provider itself touches neither helper, so the record cannot quietly start meaning something else.

  10 new cases in `__tests__/item-departure-paths.test.ts`. `npm run verify` green: 8632 tests, 20/20 guards, 24.6 KiB of headroom, +2.2 KiB drift and no copy line — the round added no strings. No DB migration, no creds.

## The budget's two measurements become one (2026-09-12)

<!-- First bullet of "Copy-measure follow-ups" (2026-09-12): "Two recorded measurements now have to move together and nothing enforces it". -->

- [DONE: 2026-09-12] [est: 8000] **`BUDGET_SNAPSHOT` — a trap the previous round built, closed before anybody fell into it** — `LAST_MEASURED_BUNDLE_BYTES` in one module and `LAST_MEASURED_TRANSLATIONS_BYTES` in another, and the pair is a single measurement: the bundle at the commit that last moved the budget, and the translations module at that same commit. The report subtracts one from today's bundle, the other from today's copy, and prints the second as a share OF the first. A raise that updated one and forgot the other would report a copy delta measured against the wrong baseline — silently, with a plausible number, on the one output somebody reads when deciding whether to raise again.

  **A suite could not have caught it.** The tolerance that keeps a drift case from going red on ordinary work is exactly the size of the error it would need to catch. One object instead, so forgetting half is not expressible rather than merely detectable.

  **It imports nothing at all, and that is the point.** `lib/translations-footprint.ts` reaches the i18n parser and `lib/bundle-size.ts` must not; a module both import has to be one that cannot carry anything into either. The strongest form of the rule and the cheapest to read — the same argument `lib/translation-status.ts` makes, asserted the same way.

  **`takenOn` is on the object.** A raise argues about a rate — four moves in three days — and a rate needs the date. A case rejects one in the future, which is what a typed number rather than a measured one looks like.

  8 new cases in `__tests__/budget-snapshot.test.ts`. 2 existing pins restated: both shipped an hour earlier asserting the bundle figure was DECLARED in `lib/bundle-size.ts`, which was right for an hour — what they were claiming is that the number has one home, which is what they ask now. `npm run verify` green: 8622 tests, 20/20 guards, 26.6 KiB of headroom, +0.1 KiB drift. No DB migration, no creds.

## The drift says how much of it was sentences (2026-09-12)

<!-- First bullet of "Bundle-drift follow-ups" (2026-09-12): "The drift is one number and the doc block asked for two". -->

- [DONE: 2026-09-12] [est: 10000] **`translationsFootprint` — the other half of what `lib/bundle-size.ts` asked for** — the drift line shipped this morning saying how much of the last raise a build has spent, and the doc block's request was "KiB per round, split code from copy". That split is the distinction the four raises actually turned on and nobody could state: one round cost 17.9 KiB with four counted strings in six languages in it, the next cost 0.2 KiB with none.

  **It reads SOURCE bytes, and the line says so in its own words.** `lib/i18n-context.tsx`'s size is not that module's share of the minified bundle — the bundler renames locals, strips types and drops comments — and the line sits directly under a figure in bundle KiB, where the obvious misreading is that it is part of that total and the obvious next step is to subtract it. What survives minification almost untouched is the string literals themselves, so the DELTA between two measurements tracks what the bundle sees far better than either absolute number does. Its own case, on the wording rather than the arithmetic.

  **`declarations`, not just `baseKeys`.** A key translated into six languages is six strings in the bundle and a key only `en` declares is one, so a round adding four keys everywhere spends roughly six times what an English-only round does — invisible in a vocabulary count, and the number that actually costs bytes. A case separates the two by deleting one translation from a fixture.

  **Silent when the copy has not moved.** A build that added no sentences should not print a line about sentences: the drift line above it already says the growth was code. This round is its own first example — verify prints the drift and no copy line.

  **`translation-status.test.ts` gains an entry, with the reason.** The rule is that nothing the app loads reaches the i18n parser, and this module reaches it: it runs under tsx from the check script after the build. The reason recorded is that it is the likeliest of the three to be imported by mistake, because what it measures — the app's sentences — is a thing a screen could plausibly want to show.

  15 new cases in `__tests__/translations-footprint.test.ts`, including one asserting the module never touches the filesystem and one that the copy line prints before `process.exit(1)` rather than after. `npm run verify` green: 8614 tests, 20/20 guards, 26.6 KiB of headroom, +0.1 KiB drift. No DB migration, no creds.

## The budget says how much of itself has been spent (2026-09-12)

<!-- Fifth bullet of "Archive-window follow-ups" (2026-09-12), carried from "Bulk-archive follow-ups" and asked for by `lib/bundle-size.ts`'s own doc block. -->

- [DONE: 2026-09-12] [est: 9000] **Four raises in three days, each argued against the one before it** — 4.53 → 4.55 → 4.57 → 4.59 MiB, because nothing recorded what was buying the space. Every build prints it now: `+17.9 KiB since the last budget move, of the 26.8 KiB it bought`.

  **The measurement had three readers and was owned by the one that cannot print it.** `MEASURED_BUNDLE_BYTES` was declared inside `bundle-size.test.ts` while the doc block argued from it in every paragraph and the report could not reach it at all. It is `LAST_MEASURED_BUNDLE_BYTES` beside the budget now, with the reasoning for why it is a MEASUREMENT and never a re-measurement: reading `dist/` for it would make every claim depend on whether somebody had built, and turn a real regression into a number that re-derives its own expectation. Three cases pin that — including one asserting the module never reaches for the filesystem.

  **The zero case says "none of it spent" rather than printing `+0.0 KiB`.** A build at the recorded measurement is the one that just moved the budget, and reading that as growth is how a raise gets argued twice. A shrink signs with a minus, because deleting a screen did that twice this week and a drift that could only be positive would report it as nothing.

  **The second line is the rule the suite held and the tool never said.** Below 8 KiB of headroom the gate fails on ordinary feature work rather than on an accidental SDK, which is not what it is for — and a build sat at 3.7 KiB this morning reporting "OK": green, and one ordinary diff from a red CI that would have named the wrong cause. It now says so on the build that is actually close, and asks for an argument rather than a bigger number, which is the failure mode the budget's own doc block names. Not printed over budget, where there is a louder thing to say.

  16 new cases in `__tests__/bundle-drift.test.ts`, including the exclusive boundary at the floor itself. `npm run verify` green: 8599 tests, 20/20 guards, 26.6 KiB of headroom, +0.1 KiB of drift. No DB migration, no creds.

## The archive screen mounts a window (2026-09-12)

<!-- Fourth bullet of "Bulk-archive follow-ups" (2026-09-12), carried through six rounds since "Archive-screen follow-ups". -->

- [DONE: 2026-09-12] [est: 6000] **Six rounds carried this and the bulk archive is what made it real** — the screen mapped over `archivedItems` and mounted a remote cover photo per row, on the one list in the app with no ceiling: nothing prunes it, every sale adds to it, and one gesture on the bulk bar can now put thirty rows in it. The reason it shipped unwindowed was that it was empty for almost everybody, which was an argument about timing, and the timing changed.

  **`useChunkedList` and `<LoadMoreButton>`, both already used by four screens.** No new keys: the button owns its three, which is why it exists. `remaining` is the only number — the component renders nothing at zero, so the gate and the label are the same fact rather than a `hasMore` asked beside a subtraction. A case asserts `hasMore` appears nowhere on this screen.

  **The window takes the provider's memoised array, and that is what makes it work.** `useChunkedList` resets when the array's REFERENCE changes, so a screen that filtered inline would snap back to one page on every render and turn `loadMore` into a no-op. `archivedItems` is memoised on `localItems` alone; a case pins both halves — the memo in the provider, and the absence of a re-derivation on the screen.

  **A restore resetting the window is correct, not a bug.** The row left the list, so a window the user had grown is describing a list that no longer exists.

  3 new cases in `__tests__/archive-screen.test.ts`. 0 new keys. `npm run verify` green: 8583 tests, 20/20 guards, 26.6 KiB of headroom — the change cost 0.2 KiB, which is what reusing two components instead of writing a third looks like. No DB migration, no creds.

## The bulk bar gains a way back, and learns about listings (2026-09-12)

<!-- First and third bullets of "Open-listing follow-ups" (2026-09-12): "`deleteItems` still leaves every open listing standing" and "Nothing archives in bulk", fifth round carrying the latter. -->

- [DONE: 2026-09-12] [est: 17000] **`archiveItems` + `openListingsForItems` — one bug and one gap that are the same machinery** — `deleteItems` was the bar's only way to make a selection go away, so somebody retiring thirty sold items either deleted them, permanently, or opened thirty screens. And it asked nothing about listings, so those thirty rows left storage while thirty standing offers stayed on every buyer's device with the confirm saying nothing. The single-item archive and the single-item delete had each been fixed for exactly that this morning; the bulk path is where nobody would have noticed.

  **One `archivedAt` for the whole selection.** The archive screen sorts on that field, and thirty timestamps a millisecond apart would scatter a single act across the top of the list in an order nobody chose. Its own case, counting the `new Date()` calls.

  **`openListingsForItems` returns the listings, not a count or a boolean**, because both callers need all three answers: how many to warn about, which ids to remove, and whether to bother. It walks the SELECTION's order rather than the listing store's, so the warning a user reads and the removals that follow agree with the order the selection was made in — and it takes the first match per item, which is what `findListingByItemId` does, so a duplicate pair resolves the same way here as on the item screen.

  **The bulk archive asks only when a listing would go with it.** The single-item action takes no confirm for an unlisted item — archiving is reversible from the item's banner and from the archive screen — and the bulk version keeps that rule rather than inventing a stricter one for the same act done thirty times. The listing removal is the part that is not reversible, and that is exactly when it asks. Not styled destructive, and Archive sits between Move and Delete: a reversible action past the permanent one reads as the more serious of the two.

  **The three Slavic locales decline the counted nouns.** `i18n-agreement` caught the first draft, where the number was followed by "из них" / "з іх" / "z nich" and the declined noun came later — correct prose the guard cannot read, which is the same thing as a rule it cannot enforce. Rewritten so the agreed word sits beside the number, which is also where a bulk confirm's reader looks.

  **4.57 → 4.59 MiB, the FOURTH raise in three days, and the trend is now written into the doc block as the finding rather than a footnote.** Each raise buys about 25 KiB and four rounds of ordinary work spends it, so the number records how fast the app grows and has never said stop. The shape is still right — headroom is chosen against Clarity's ~30 KiB, so a statically-imported SDK trips on its own commit — and this round cost 17.9 KiB of which the copy is a real share. The paragraph asks for KiB-per-round, split code from copy, before a fifth raise.

  29 new cases in `__tests__/bulk-archive.test.ts`; 4 new keys in all six locales. 3 existing counts restated. `npm run verify` green: 8580 tests, 20/20 guards, 26.8 KiB of headroom. No DB migration, no creds.

## Deleting an item takes its listing too (2026-09-12)

<!-- First bullet of "Listing-retirement follow-ups" (2026-09-12): "`deleteItem` still leaves a live listing standing". -->

- [DONE: 2026-09-12] [est: 10000] **The archive round fixed one half of this and left the worse half standing** — an open listing is a standing offer to strangers, it lives on every buyer's device, and nothing the seller does to the item reaches it. Archiving at least left a row somebody could restore and look at; deleting removes it from storage entirely, so the buyer who claims the surviving listing gets a purchase pointing at nothing the seller can even open.

  **`shouldRetireListingOnArchive` is `isOpenListing` now.** The delete path is what made the narrower name wrong: the two callers differ in what they do NEXT — an archive is reversible and asks about the listing it is about to withdraw; a delete is reversible by nothing, so its existing confirm gains a sentence rather than a second dialog — and never in what they need to know. Named for the listing's state, with a case asserting the old name is gone.

  **A sold listing survives both.** A delete does not un-sell a thing that was sold, and the buyer's purchase list, the transfer log and "recently sold" all read that record.

  **The sentence appears only when there is a listing to lose.** A confirm naming only the item describes half of what the button does on the one action in this family nothing can undo; a confirm mentioning a listing for every item is noise on the overwhelming majority that have none. Both platforms take the same body — the web branch builds its own string and the native branch passes it to `Alert.alert`, and the realistic slip is changing one and leaving the other saying less. Its own case.

  **Removal before deletion**, for the same reason as the archive path, with the same ordering case — plus one asserting the navigation away still happens, because the realistic slip in adding a branch above a `router.replace` is to return early out of the whole function.

  13 new cases in `__tests__/delete-retires-listing.test.ts`; 1 new key in all six locales. 2 existing pins restated: the rename, and the "leaves delete exactly as it was" case that had pinned the confirm's exact arguments when what it was claiming is that delete still ASKS while archive does not. `npm run verify` green: 8551 tests, 20/20 guards, 12.5 KiB of headroom. No DB migration, no creds.

## Archiving takes the listing down with it (2026-09-12)

<!-- First bullet of "Archived-listing follow-ups" (2026-09-12): "Archiving an item that is currently listed leaves the listing standing". -->

- [DONE: 2026-09-12] [est: 12000] **`shouldRetireListingOnArchive` — the previous round locked the door the other way round** — it stopped a listing being CREATED for an archived item and said nothing about the reverse order, which is the likelier one: a seller agrees a sale off-platform and archives afterwards. `archiveItem` touched nothing in the marketplace, so the listing outlived the archive and stayed in `activeListings` for every buyer, while the item's own screen showed it as listed underneath a banner reading "this item is archived".

  **A SOLD listing stays.** The sold-listing prompt archives the item as the final step of a sale that already happened, and the listing is that sale's record — the buyer's purchase list, the transfer log and "recently sold" all read it. Removing it there would delete the history the archive exists to keep, which is the argument `archivedAt` itself is built on. So the question is exactly "is there a listing, and is it still open?", asked on `soldAt` — the same field `activeListings` filters on, with a case asserting the two agree.

  **A function, not an `if` on a screen.** The previous round's rule lives in a component, which the next caller (a bulk archive) would not inherit; this one is in `marketplace-helpers.ts` where it can be tested on values and reached.

  **The listed branch asks first and offers no undo.** Archiving an unlisted item is private; archiving a listed one withdraws something other people can see, and `addListing` mints a new id and a new `createdAt`, so nothing can put the listing back. An undo that silently restored half of what it took would be worse than none — so the confirm carries the warning instead, and a case asserts each locale's body actually says the listing does not come back rather than only asking "are you sure".

  **The removal goes before the archive.** An item archived first is one whose listing removal can be lost. Its own ordering case, as is the one on the earlier round's `addListing` guard.

  19 new cases in `__tests__/archive-retires-listing.test.ts`; 3 new keys in all six locales. 1 existing pin restated: last round's "takes no confirm" is now about the unlisted BRANCH, which is what it was always claiming. `npm run verify` green: 8538 tests, 20/20 guards, 14.1 KiB of headroom. No DB migration, no creds.

## An archived item cannot be put up for sale (2026-09-12)

<!-- Fourth bullet of "Item-archive follow-ups" (2026-09-12): "An archived item is still listable, tradeable and reactable". -->

- [DONE: 2026-09-12] [est: 11000] **The marketplace block under the archived banner was completely unchanged** — "List on marketplace" rendered as normal, the sheet opened, and `addListing` accepted the id. So a seller could offer an item the app itself was describing, two inches higher on the same screen, as one they no longer have. The archived items likeliest to be re-listed are exactly the ones that got there through the sold-listing prompt — rows whose entire history is "this was already bought".

  **This is the one affordance in the family worth closing.** The share sheet, the reaction bar and the edit form all act on the seller's own data. A listing creates an expectation in somebody ELSE, who claims it and then waits for a thing that was sold last month.

  **No CTA rather than a disabled one.** A greyed-out "List on marketplace" invites a press that explains nothing; the sentence says what to do instead, and Restore is the button directly above it.

  **Two locks, because hiding a button is not closing a door.** The sold-listing prompt fires on a realtime update, on whatever screen the seller is looking at — including this one with the listing sheet already open. `handleSubmitListing` checks before it calls `addListing`, and a case asserts the ordering rather than only the presence.

  **The feed is not where this could have been fixed.** Items are local-first: a buyer's device holds the listing and not the seller's `archivedAt`, so no filter on the browse page can see the state. The lock has to be on the device that knows.

  **Taking a listing DOWN still works on an archived item.** That is the direction that reduces the obligation rather than creating one, and an item archived while listed is exactly the case that needs it. Its own case.

  12 new cases in `__tests__/archived-not-listable.test.ts`; 2 new keys in all six locales, with a case that the standing hint and the refusal toast are not the same sentence — a hint that reads like an error alarms where nothing went wrong, and an error that reads like a hint does not say the press was rejected. 2 existing counts restated. `npm run verify` green: 8519 tests, 20/20 guards, 17.5 KiB of headroom. No DB migration, no creds.

## Archiving reaches the screen the item is on (2026-09-12)

<!-- Second and third bullets of "Archive-screen follow-ups" (2026-09-12): "Archiving is still reachable from exactly one place, and it is a marketplace prompt" and "Nothing tells a user an item IS archived". -->

- [DONE: 2026-09-12] [est: 12000] **An Archive action and an archived banner on `app/item/[id].tsx` — the archive family's last two holes** — `archiveItem` had exactly one caller, the sold-listing prompt, so the only route into the archive was selling something through the marketplace. A collector who wanted to retire an item they still own — broken, loaned, lent to a show — could only delete it, which is the one action in this family nothing can undo. And an archived item rendered on this screen exactly like a live one: no banner, no restore, nothing saying why it had disappeared from every list.

  **This screen is where an archived row is met by accident.** The deep link and the marketplace transfer log both land here, and `getItemById` does not filter archived rows — so it was already reachable and already silent. The banner answers the three questions somebody arriving that way actually has, which is why it is a title AND a hint: "Archived" alone says where it went, not whether it is gone or whether it can come back. A case asserts the hint is neither a copy of the title nor shorter than it, in each of the six locales.

  **No confirm on the archive, and an undo instead.** Archiving is reversible in two places now — this screen's own banner and `app/archive.tsx` — so a confirm would be an obstacle in front of a reversible action, and the toast covers the tap that was a mistake. The same shape the sold prompt uses, down to capturing the id before the await: `activeItem` is recomputed from `getItemById` and the row leaves that list the moment it is archived.

  **The action disappears once the item is archived.** A second archive would restamp `archivedAt` and move the row to the top of a list sorted by it — a confusing no-op rather than a destructive one — and a button offering it beside a banner saying "this is archived" reads as a contradiction either way.

  **`isArchived`, never `activeItem.archivedAt`.** The screen asks the shared predicate and a case asserts the field is not read directly, which is the gate that keeps a seventh spelling of "is this in the trash" from being written on the screen most likely to grow one.

  18 new cases in `__tests__/item-archive-actions.test.ts`; 4 new keys in all six locales. 2 existing counts restated — the item-detail family rose by SEVEN for a four-key round, because the screen also picked up three keys the archive screen had already paid for, which is exactly what a screen family counts and a per-key diff would not show. `npm run verify` green: 8507 tests, 20/20 guards, 20.0 KiB of headroom. No DB migration, no creds.

## The archive is a place you can walk into (2026-09-12)

<!-- First bullet of "Unarchive follow-ups" (2026-09-12): "The restore is reachable for about eight seconds and then never again". -->

- [DONE: 2026-09-12] [est: 16000] **`archivedItems` + `app/archive.tsx` — the other half of the morning's restore** — `unarchiveItem` shipped with one caller, the Undo on the sold-listing prompt's toast, which covers the accidental archive and nothing else: eight seconds after the fact there was still no way to see what had been archived or to get any of it back. The list, the screen, and a home-screen row that appears the moment something lands in the trash.

  **Sorted by `archivedAt`, not `createdAt`.** What somebody opens this screen looking for is the thing they archived a minute ago, which may well be the oldest item they own. Its own case, built so the two orderings disagree.

  **`localItems`, not the merged list.** An archive is a thing you did to your OWN rows; a friend's archived item is neither yours to restore nor yours to be shown. And `isArchived` alone, with no `!isWishlist`: an archived want is exactly as unreachable as an archived holding, and a filter that excluded it would leave it with no way back at all.

  **Restore has no confirm and Delete does.** Restoring is reversible by archiving again; deleting is reversible by nothing, and this is the screen people arrive at having already made one mistake. For the same reason there is no "empty the archive" — a screen whose whole purpose is recovering from a mistake is the worst possible place to put a one-tap way to make a bigger one. Both are their own cases, including the negative one.

  **The banner is conditional.** An archive is a recovery path, not a feature to advertise: a permanent row pointing at an empty screen is noise on the surface every session starts on, and a row that appears when the trash gains something is how somebody learns the trash exists at the one moment they need to. `dashboard-banner.test.ts` counts three rows now and asserts the third is the conditional one, because "bump the number to 3" would have been a different claim about the dashboard.

  **4.55 → 4.57 MiB, re-measured and re-argued as the doc block requires.** Four rounds today took the bundle from 4634.4 to 4655.5 KiB and the headroom from 24.8 to 3.7 — below the 8 KiB floor `bundle-size.test.ts` holds precisely so the gate fails on an SDK and not on a screen. The raise buys 24.2 KiB, still under Clarity's ~30 KiB, so a statically-imported SDK still trips on its own commit.

  33 new cases in `__tests__/archive-screen.test.ts`; 11 new keys in all six locales. 4 existing counts restated (the banner count, the home-screen family size, the coverage denominator, the measured bundle). `npm run verify` green: 8489 tests, 20/20 guards, 24.2 KiB of headroom. No DB migration, no creds.

## An archive you can come back from (2026-09-12)

<!-- First bullet of "Live-item-predicate follow-ups" (2026-09-11): "An archived want can be created and nothing can un-archive it". -->

- [DONE: 2026-09-12] [est: 15000] **`unarchiveItem` — the safe answer was the irreversible one** — the sold-listing prompt offers Archive beside Delete and labels it "keep it for stats and audit history", and an item that took it left every listing, total, count, recent-items strip and search in the app. No screen lists archived rows and there was no `unarchiveItem` anywhere in the tree, so the row sat in storage and kept syncing: not gone, not reachable, counted by nothing. The archive was one-way in both directions.

  **`null`, not `undefined`.** `updateRemoteItem` writes the column only when the key is PRESENT in `updates` (`"archivedAt" in updates`), and the row coercer reads `null` for "not archived". Clearing the field with `undefined` would restore the item locally and leave the cloud row archived, so the next sync would put it straight back in the trash — a worse bug than the one being fixed, and silent. Its own case, plus one pinning the `in updates` contract it depends on.

  **The undo goes where the archive happened.** `toast.show` rather than `toast.success` (a success toast has no action slot), and the id is captured before the await: `item` is recomputed from the head of the notification queue, which the next line dismisses, so an undo closing over it would restore whatever the NEXT notification is about. The restore shows no toast of its own, so it announces — same argument as the reorder undo.

  **A second bug, found while reading the first, in five places.** `archiveItem`, `updateItem`, `updateCollection`, `promoteWishlistItem` and `moveItems` learned what they had changed by assigning to a `let` from inside a `setLocalItems` updater and reading it on the next line. React computes a `useState` update eagerly only while the hook has no work queued, so the pattern holds right up to the moment something else has queued one — a landing realtime row, a second tap, a sync flush — and then the variable is still `null`, the `if` is skipped, and the cloud write never happens. Local state changes, the remote row does not, the next sync hands the old value back: an edit that reverts itself, only under load. `deleteCollection` in the same file had always read `localItems` directly; `resolveLocalItem` is that shape with a name on it.

  19 new cases in `__tests__/unarchive-item.test.ts`. 2 existing counts restated (the marketplace family's size, the coverage denominator). `npm run verify` green: 8456 tests, 20/20 guards, 15.1 KiB of headroom. No DB migration, no creds.

## The PDF prints money, not numbers (2026-09-12)

<!-- Third and fourth bullets of "Cost-gate follow-ups" (2026-09-11), carried through four rounds: "The export's total is summed but never converted or labelled" and "A collection's `currency` override reaches the totals and not the PDF". -->

- [DONE: 2026-09-12] [est: 13000] **`ExportMoney` — the one output a user keeps was the one place that converted nothing** — `buildCollectionExportHtml` summed the raw `cost` fields itself, printed the result as a bare number beside a "Total cost" label, and printed each item's cost as `${item.cost}`: no currency, no separator. A collection holding items in EUR and USD filed one figure in no unit at all, and a 1500-euro item printed "1500" beside a 1500-dollar one with nothing in the document able to tell them apart. `collection.currency` — the override that reaches every screen in the app — reached this document not at all, because the builder never read the field.

  **The builder takes the ANSWERS, not the rate table.** The conversion needs the USD rates, which live in the provider behind a fetch and a cache; taking them would make a pure module's signature about where rates come from instead of about the document, and would be a second conversion implementation to keep in step with the screen's. Taking `{ total, itemCost }` gets something better than two implementations that agree today: the PDF prints the figures the screen printed, because they are the same values — `getCollectionTotalCost(collection.id)` and the `convertItemCost(item, collection.currency)` every `<CostBadge>` on that screen already calls.

  **The `≈` is emitted by the builder rather than taken as a label**, because the marker is a symbol and not a word: all six locales spell `itemValueApprox` as exactly `≈ ${amount} ${currency}`. A case pins that equivalence rather than trusting the comment — if a locale ever writes it differently, the export has to start taking a label, and that case is what says so. What it marks is worth marking in a document somebody files: a figure derived from a rate table on the day it was printed is not the price that was paid.

  **The cost cell is escaped now.** The amount is a number, but the code beside it comes from `costCurrency`, which is stored text nothing validates on the way out of the database — and it was being interpolated into the document raw. Its own case.

  21 new cases in `__tests__/export-pdf-money.test.ts`. 10 existing call sites in two suites restated onto the new signature, one of them the assertion that the printed total is "42" — it is "42 USD" now, which is the round in one character. `npm run verify` green: 8437 tests, 20/20 guards, 16.1 KiB of headroom. No DB migration, no creds.

## The stats screen's total is a real total (2026-09-12)

<!-- First bullet of "Cost-gate follow-ups" (2026-09-11), carried through three rounds: "`app/stats.tsx` is the one screen whose totals never convert". -->

- [DONE: 2026-09-12] [est: 14000] **`portfolioTotalCost` + `ownedTotalCost` — the one total in the app that never converted and never said what it was in** — the stats screen reduced every owned item into `sum + i.cost` and rendered it with `toLocaleString()` under a label reading "Total value". A collector holding items priced in EUR, GBP and USD read the sum of three different units, printed with no currency beside it, on the same screen as collection cards that had converted those exact figures correctly. Three rounds carried this bullet.

  **The property worth the most is the one a user checks by hand: the portfolio total is the collection cards added up.** That is why `portfolioTotalCost` resolves an item with no `costCurrency` through its COLLECTION's override before falling back to the viewer's display currency — `collectionTotalCost` already does this, because for a card the target IS that collection's override. A plain `?? displayCurrency` passes every other case in the new suite and silently disagrees with the cards for exactly the collections that set one. Its own case, in both directions (50 with the override, 40 without).

  **One `sumEntries`, because the no-rates branch is a judgement call.** Summing raw amounts while the rate table loads is a deliberate lie of convenience the module header argues for; a second copy of a judgement call is how two screens end up disagreeing about what an unconvertible item is worth. Both totals delegate, and a case counts the delegations.

  **`skipped` is rendered for the first time since the field existed.** A single collection is usually one currency, so a missing rate there is pathological; a PORTFOLIO spans currencies, which makes it ordinary — and a headline figure that silently drops those items is wrong rather than approximate. `statsTotalValuePartial`, translated in all six locales: a hint saying a sum is incomplete, served in English under somebody else's flag, is the same defect one layer up.

  **Built from `itemsByCollection`, not by re-filtering `items`.** The index is already live-item filtered (the predicate the screen used to get via `selectOwnedActiveItems`), and walking owned collections skips every friend's, subscription's and share's items rather than testing each one. Memoised in the provider on the five things it depends on.

  30 new cases in `__tests__/portfolio-total.test.ts`. 1 existing pin re-stated: `has-finite-cost.test.ts` listed stats as a caller that reads the shared gate, and the right claim now is that the screen touches no `.cost` at all. `npm run verify` green: 8416 tests, 20/20 guards, 16.4 KiB of headroom. No DB migration, no creds.

## A price that is not a number stops being printed as one (2026-09-11)

<!-- Second bullet of "Fan-out follow-ups" (2026-09-11), carried from the two rounds before it: "`app/stats.tsx` sums with `typeof i.cost === \"number\" ? i.cost : 0`". -->

- [DONE: 2026-09-11] [est: 11000] **`hasFiniteCost` reaches the six callers that were asking a weaker question** — four of them spelled it `typeof item.cost === "number"`, which is true of `NaN` and of `Infinity`. One such item made the stats screen's headline "total value" read `NaN`, put `NaN` in the total of an exported PDF, and printed it in that PDF's cost cell. `lib/item-filters.ts` had the whole finite check written out verbatim beside the helper that is the finite check, which is the shape that makes a shared gate look optional.

  **No entry path can produce one today, and that is the argument for doing it now rather than against.** The forms parse through `parseCurrencyValueDetailed`, which rejects a non-finite number; cloud rows go through `coerceNumberOrNull`, which does the same. So this is a property of the paths rather than of the code, and the weak check gives the wrong answer — silently, inside a total — the day one more importer, paste handler or migration disagrees. Written into the gate's header rather than left for the next reader to rediscover.

  **The gate is a type guard now, which is what makes it the easy thing to reach for.** `items.filter(hasFiniteCost)` narrows to `cost: number`, so four call sites sum or print the field with no cast — including `collectionTotalCost`, which had an `as number` from the round that extracted it. A helper that leaves the caller casting is a helper the next author writes around.

  **The PDF is the case worth the most.** It is the one output a user keeps, mails and files, and a `NaN` in it outlives every re-render that would have fixed a screen. Its two cases feed a `NaN` and an `Infinity` through the real builder and assert neither word reaches the document.

  15 new cases in `__tests__/has-finite-cost.test.ts`: 5 on the predicate (zero and a negative are prices; missing, null, `NaN` and both infinities are not), 1 on the narrowing, 2 through the PDF builder, and 7 sweeping the callers for the weak spelling and the hand-written finite check. `npm run verify` green: 8386 tests, 20/20 guards, 18.1 KiB of headroom. No DB migration, no creds.

## One failed request stops discarding the five that worked (2026-09-11)

<!-- Fourth bullet of "Collection-total follow-ups" (2026-09-11), carried unchanged through three rounds: "Two hand-written item-count fetches still use `Promise.all`". -->

- [DONE: 2026-09-11] [est: 13000] **`lib/fan-out.ts` — six all-or-nothing fan-outs, each written out and each wrapped in a catch that hid the loss** — `Promise.all(list.map(fetchOne))` rejects on the FIRST rejection, so one collection deleted between the list and the fetch, one permissions change, one dropped connection on request nine of ten threw away the nine answers that had already arrived. Every call site then swallowed the error, so what reached the user was a screen of zeros the app had the real numbers for, with nothing anywhere saying a request had failed.

  **Partial is the right answer because every caller is a cache fill.** The app reads from AsyncStorage and these fetches top it up: nine collections' items beat none, and the tenth arrives on the next refresh. A fan-out that genuinely needs all-or-nothing — a write that must not half-apply — keeps `Promise.all` and says why, which is what the surviving ones in the provider do: three storage reads and a cursor pair, fixed tuples with nothing to partially lose.

  **`fetchSettledRows` because four of the six immediately flattened.** Each looped its results into one array with its own nested `forEach`; the flattening belongs with the fan-out. The friend block lost eleven lines and two intermediate arrays.

  **`inputs.map((input) => fetchOne(input))`, not `inputs.map(fetchOne)`.** `map` passes the index and the array as second and third arguments, and a fetcher whose second parameter is an options object would silently be handed a number — a bug that works fine on the first element. Its own case.

  **Two bugs on the profile screen, not one.** Its `loadItemCounts` was the sixth copy, and it takes an `isActive` guard that the pull-to-refresh caller never passed — so a refresh resolving after the user navigated away wrote state onto an unmounted screen. Slow connections are when a refresh resolves late, which is also when somebody stops waiting and leaves. A mounted ref, and the focus effect keeps its own `active` flag.

  15 new cases in `__tests__/fan-out.test.ts`, including the two a rewrite would break silently: results come back in INPUT order rather than completion order, and the requests overlap rather than serialising (a `for … await` rewrite passes every other case and turns ten parallel fetches into ten round trips). `npm run verify` green: 8371 tests, 20/20 guards, 18.2 KiB of headroom. No DB migration, no creds.

## Each collection's total, once per change instead of once per card (2026-09-11)

<!-- Second and third bullets of "Live-item-predicate follow-ups" (2026-09-11): "`getCollectionById` is a `collections.find` and `getCollectionTotalCost` opens with another one" and "The index made the total's input cheap to find and left the work on it alone". -->

- [DONE: 2026-09-11] [est: 15000] **`lib/collection-total.ts` + `collectionTotals` — the expensive half of the previous round** — the per-collection index made a total's INPUT cheap to find and did nothing about the conversion, which is a currency conversion per priced item, run by every `<CollectionCard>` on every render. The provider memoises a `Map<collectionId, CollectionTotalCost>` off four things that do not change on a render (the item index, the rate table, the viewer's display currency, the collections and their overrides), and the accessor is a lookup.

  **`collectionsById`, the same move one type up.** `getCollectionById` was a `collections.find` and the total opened with a second one to read the collection's `currency` override, so every card that showed a total scanned the whole collection list and every screen resolving a collection by id scanned it again.

  **A `NaN` cost no longer poisons a collection.** The sum filtered on `typeof item.cost === "number"`, and `NaN` and `Infinity` are both numbers — one item carrying either made the whole total `NaN` and the card rendered it. `hasFiniteCost` has been the app's shared gate for "this item has a renderable cost" since it was written; the collection total was the one place still asking the weaker question.

  **The arithmetic is testable on values now.** It lived inside the accessor, so the only way to pin it was to match this provider's characters — and three suites did: the shape of a `sumConverted(` call, a `const target = …` line, two returned object literals. What those cases actually claim is about numbers, and they say so now: 20 new cases in `__tests__/collection-total.test.ts` cover conversion, the no-rates raw sum, an unconvertible currency counted as skipped rather than as zero, zero and negative costs kept, `NaN`/`Infinity`/`null`/absent dropped, and the empty total agreeing with the fallback the accessor answers with.

  **`emptyCollectionTotal` keeps the override.** A collection holding nothing priced has no entry in the map, and the realistic slip is for that path to reach for `displayCurrency` alone — an empty collection reading "0 USD" under a card labelled in EUR. Its own case.

  20 new cases plus 6 structural ones on the provider; 8 existing pins re-stated across three suites (`currency-rates`, `collection-currency-override`, `marketplace-sold-prompt`) onto the wiring they are actually about. `npm run verify` green: 8356 tests, 20/20 guards, 18.5 KiB of headroom. No DB migration, no creds.

## A live item is one rule, and the wishlist stops ignoring the trash (2026-09-11)

<!-- First and second bullets of "Live-item-index follow-ups" (2026-09-11): "The live-item rule is now stated twice in one file, forty lines apart" and "`wishlistItems` filters on `isWishlist` and not on `archivedAt`". -->

- [DONE: 2026-09-11] [est: 12000] **`isLiveItem` / `isLiveWishlistItem` — four spellings of one question, and the fifth one had it wrong** — "does the collector have this?" was decided in four places and no two wrote it the same way: a three-clause filter in `selectOwnedActiveItems`, its De Morgan negation as a `continue` guard in `groupItemsByCollection` forty lines below it in the same file, and two early returns in the search overlay. They agreed. Checking that they agreed meant negating one of them in your head, which is the state the fifth copy gets written in.

  **The fifth copy already existed and was a bug.** The provider's `wishlistItems` memo asked `item.isWishlist` and nothing else, so an archived want was in the trash on every screen in the app — collection listings, counts, totals, recent items, stats, search — except the wishlist screen it was added on. Whether `archiveItem` can reach a wishlist row today looks like no, which made this the cheap moment: the fix is the predicate, not a patch on the path that would have found it.

  **A pair, not one function.** `isLiveItem` and `isLiveWishlistItem` split on the wishlist flag and share `!isArchived(item)`, so neither side can quietly drop the half the memo dropped. A case asserts they are a genuine split: every un-archived row is a holding or a want and never both, and an archived row is outside both.

  **`isArchived` returns a boolean, not the timestamp.** `archivedAt` is a nullable ISO string and legacy rows carry `null`, so the question is truthiness — and a predicate that hands back a string is one somebody eventually compares with `===`.

  17 new cases in `__tests__/live-item-predicate.test.ts` (12 behavioural on the three predicates, 5 structural on the four callers, both directions of the old spelling). 1 existing pin re-stated: the wishlist suite's case is about the SEPARATE memo, so it asks for that and the dep rather than for the filter body. `npm run verify` green: 8335 tests, 20/20 guards, 19.0 KiB of headroom. No DB migration, no creds.

## One pass over the items, not two per card (2026-09-11)

<!-- First bullet of "Feed-deletion follow-ups" (2026-09-11): "The home screen asks the item list two full questions per card, and the first one SORTS what it only counts". -->

- [DONE: 2026-09-11] [est: 14000] **`groupItemsByCollection` — three accessors stop re-walking the whole item list** — `getItemsForCollection` filtered the merged array, `getCollectionTotalCost` filtered it again with the identical three-clause predicate, and a screen that wanted a COUNT got one by taking `.length` of the first one's SORTED result. A `<CollectionCard>` asks two of them, so a home screen with twenty cards walked every item the viewer can see — their own, their friends', their subscriptions', their shares' — forty times per render, and sorted twenty lists whose order the `.length` threw away. One `Map<collectionId, items>` built per change of `items`, and the accessors do a lookup.

  **The predicate is the part worth more than the passes.** A wishlist entry is a want, an archived item is in the trash, and that rule was written out three times. Three copies is three chances for a card's count to include what the total beside it excludes — two numbers on one card disagreeing about one collection. It is one statement now, with behavioural cases rather than a source regex: two suites had pinned "skips archived items" by matching `!item.archivedAt` inside each accessor's body, and both were re-stated onto the index they now read.

  **`countItemsForCollection`, because `.length` of a sort is the shape that caused it.** The home screen's three renderers and the profile screen's card list all wanted a number; `getItemsForCollection` stays for the one caller that renders the items in order. A case sweeps all three screens for `getItemsForCollection(...).length`.

  **The owner's row asked for its total twice** — `getCollectionTotalCost(id).amount` and then `getCollectionTotalCost(id).currency`, which is a second currency conversion of every priced item in the collection to read one string off the result. The other two renderers hoisted it into a `const` and this one never did.

  **`[...(index.get(id) ?? [])].sort(...)`, not `index.get(id)?.sort(...)`.** `sort` is in place: sorting the entry would reorder what every other reader of that collection sees, from a getter that looks like it only reads. A collection with nothing live in it is ABSENT from the map rather than present with an empty array — `get` answering `undefined` and `?? []` at the call site are the same answer, and an entry per collection would need the collection list passed in to say nothing.

  19 new cases across `__tests__/items-by-collection.test.ts` (8 behavioural on the helper, 6 structural on the provider, 4 on the screens) and 1 in `marketplace-sold-prompt.test.ts`; 2 existing pins re-stated there. `npm run verify` green: 8318 tests, 20/20 guards, 19.2 KiB of headroom. No DB migration, no creds.

## The screen nobody could open is gone (2026-09-11)

<!-- First bullet of "Remaining-count follow-ups" (2026-09-11), carried unchanged through four rounds: "`app/collections-feed.tsx` is STILL a route nothing links to". -->

- [DONE: 2026-09-11] [est: 16000] **`app/collections-feed.tsx` deleted — four rounds of shared-code work had been reaching a screen no user could open** — no `Link`, no nav entry, no `router.push`, nothing in the tree pointing at it; reachable only by typing the URL. What it rendered was the home screen's "Friends' collections" and "Subscribed" tabs: the same `selectFriendCollections` over the same two lists, the same `useChunkedList` window, the same `<CollectionCard>`, the same two empty states, the same `<LoadMoreButton>`. It was the oldest unactioned item in the suggestions file and the only one that was not a code question.

  **The entry point was the worse of the two options.** "See all" leading to the same twenty cards behind the same Load-more button is not a destination; linking it would have made the duplication permanent rather than ended it. Nothing user-facing leaves with the file — a route no user can reach has no users to lose, and git keeps it if the product call ever goes the other way.

  **The one thing it did that the home screen does not is NOT ported.** It fetched per-collection item counts from Supabase for the cards in its window. The collections provider already fetches items for friend, subscribed and shared-with-me collections, and `getItemsForCollection` — what the home screen's cards count — reads the merged result. Porting it would have added a round trip per visible card for a number the screen already has. Said in the suite, because "they forgot" is the obvious reading of an absence.

  **Three i18n keys had exactly one reader each and left with it** — `collectionsFeed`, `collectionsFeedTitle` and `tabFriendCollections`, in all six locales: eighteen strings translated for a screen nobody could open. `collectionsFeedSubtitle` stayed, because the home screen's subscribed tab renders it, and deleting a prefix family wholesale is the realistic mistake — it would have blanked a line of the home screen. The translation floor moves 511 → 508 and takes its first FALL, with the reason written into the list beside the additions.

  5 new cases in `__tests__/collections-feed-retired.test.ts` — the file, any navigation pointing at a route that is not there, the home screen still owning both tabs, the three dead keys, and the fourth that must survive. One suite deleted with its screen; 8 existing suites re-stated (the file was named in a screen list, a token sweep, a spacing table, a hero-banner consumer list, a Load-more sweep and three prose headers — a module header naming a path that is not on disk is its own guard). `npm run verify` green: 8299 tests, 20/20 guards, 19.3 KiB of headroom — 3.6 KiB of it bought back by the deletion. No DB migration, no creds.

## The window says how much is left, and the button gets rendered (2026-09-11)

<!-- Second and third bullets of "Load-more-button follow-ups" (2026-09-11): "`<LoadMoreButton>` is pinned structurally and has never been rendered by a test" and "Three screens now compute `total - visibleItems.length` and hand it over". -->

- [DONE: 2026-09-11] [est: 12000] **`remaining` on `ChunkedList`, and six cases that actually mount the button** — extracting the button moved the duplication rather than ending it: each of the three screens still wrote `total - visibleItems.length` at its call site, and each got `total` from a different expression — one of them a ternary picking between two lists. The hook has both numbers. `hasMore` is `remaining > 0` now, derived rather than computed separately, which is what made it possible for a screen to gate on one and label with the other in the first place.

  **`renderLoadMore` lost its second parameter** and collection detail's memo lost two deps for one. Nothing at a call site knows the subtraction any more, which a case asserts across all three screens.

  **The one claim the regexes could not check is now checked.** Thirteen structural cases read the component's source; `return null` and a node styled to nothing look identical there and are not identical in a layout — an empty pressable still takes its 14pt of vertical padding, which on the home screen is a gap under the last card that nobody can explain. Six rendered cases on the existing harness: nothing in the tree at 0 and at -3, the labelled button at 7, the press reaching `loadMore`, and 1 rendering as the same sentence with a 1 in it rather than a special case.

  **Process note, not a code change:** the Pages deploy for the previous round failed with "in progress deployment" — two commits pushed 33 seconds apart raced each other, and the older one lost. The site was never wrong (the winning deploy was a descendant carrying everything), and the cause was splitting a round across two commits, which this repo's history does not do. One commit per round from here, this one included.

  10 new cases, 10 existing pins re-stated across five suites. `npm run verify` green: 8305 tests, 20/20 guards, 15.7 KiB of headroom. No DB migration, no creds.

## One Load-more button, not three (2026-09-11)

<!-- Second bullet of "Windowed-tab follow-ups" (2026-09-11): "The Load-more CTA is now written out on three screens" — flagged by the round that added the third copy. -->

- [DONE: 2026-09-11] [est: 15000] **`components/load-more-button.tsx` — the third copy was added and retired the same day** — collection detail's drag-mode fallback, `app/collections-feed.tsx` and the home screen's two tabs each carried the same `<Pressable>`, the same three i18n keys, the same two style rules and the same accessibility props. Each copy arrived when that screen got its window, which is how a button becomes three buttons without anybody deciding it should.

  **`remaining` replaces the pair of questions.** Every copy gated on the window's `hasMore` and then computed `total - visibleItems.length` for the label — the same fact asked twice, since `hasMore` IS `total > visibleItems.length`. The component renders nothing at zero or below, so "Load more (0 remaining)" stops being expressible, and `hasMore` and `t` both left collection detail's memo deps with the markup.

  **`<=`, not `=== 0`.** A caller whose total and window disagree for one render — a list that shrank under a window that has not reset yet — would otherwise render "Load more (-3 remaining)".

  **The three keys moved files, not apps.** Same movement as `reorderActionProps`: the component takes `t` from the context rather than its labels from the caller, so the home and collection-detail screen families each shrank by three (29 → 26, 50 → 47) with the reason written into the entry. `lint:orphan-i18n` still reaches all three keys.

  13 new cases in `__tests__/load-more-button.test.ts` — the zero and negative guards, the three strings, and a sweep asserting that none of the three screens kept a copy. 7 existing pins across five suites re-stated, including the one whose a11y assertions now point at the component rather than at the screen. `npm run verify` green: 8296 tests, 20/20 guards, 15.7 KiB of headroom. No DB migration, no creds.

## The home screen stops mounting every card it was handed (2026-09-11)

<!-- Found while closing the previous round: `app/collections-feed.tsx` has windowed these two lists since it was written, and the screen every user lands on had not. -->

- [DONE: 2026-09-11] [est: 13000] **`useChunkedList` on the friends and subscribed tabs** — both `.map`ped the whole array, so every friend collection and every subscription mounted a `CollectionCard` — and its remote cover image — the moment the home screen rendered, whether or not that tab was the visible one. That is precisely the iOS memory hot-path `useChunkedList` was written for, and `app/collections-feed.tsx` has used it over these exact two lists since it was written. The screen every user lands on was the one still unbounded, which is the shape of an optimisation that got applied to the second screen and never the first.

  **Found by looking for the entry point to the screen the previous round fixed.** `app/collections-feed.tsx` is a route with no `Link`, no nav entry, and nothing in the tree pointing at it — reachable only by typing the URL. Whether it should be linked or deleted is a product call and is a suggestion, not this diff; what it turned up is that its bounded rendering is the thing the home screen needed.

  **"Mine" stays unwindowed and the code says why.** It is a drag-to-reorder list, and a window would let the owner drag a row toward a position that is not mounted. It is also the one of the three that cannot grow without the user doing the growing.

  **The empty check still asks the FULL length.** `visibleItems.length > 0` is the same answer today and the wrong question: an empty window over a non-empty list would show "no friends yet" to somebody who has plenty.

  **Three i18n keys joined the screen already translated** — `app/collections-feed.tsx` paid for `loadMoreItems` and its two a11y siblings in all six locales, over the same two lists. The family size moved 26 → 29 with the reason written down, because a screen gaining keys without a translation round is exactly what that number exists to catch.

  11 new cases in `__tests__/home-chunked-tabs.test.ts`, including the one that keeps the unwindowed list deliberate. `npm run verify` green: 8286 tests, 20/20 guards, 15.1 KiB of headroom. No DB migration, no creds.

## Two screens, one label, one list (2026-09-11)

<!-- Fifth bullet of "Home-derivation follow-ups" (2026-09-11), carried into "Friend-Set follow-ups": "`app/collections-feed.tsx` renders `friendCollections` from the CONTEXT and the home screen derives its own". -->

- [DONE: 2026-09-11] [est: 14000] **`selectFriendCollections` — the feed's "Friends" tab was a strict subset of the home screen's** — two lists with one name, built from different inputs by different rules, on two screens a user moves between. The home screen filtered the merged `collections`; the feed rendered the context's `friendCollections`, which holds only what `fetchPublicCollectionsByUserId` returned for each friend. So the feed's tab was missing every seeded friend collection, everything shared directly with the viewer, and — until the fetch landed, or at all on a device with no Supabase — everything. A user opening one screen and then the other got two answers to one question with nothing saying which was meant.

  **The merged rule wins because it is the one that can see every source.** The context's `collections` already holds the cloud fetch, the seeds and the shares; the context's `friendCollections` is one of three inputs to that merge and is still the right thing for the merge to consume. It was the wrong thing for a tab to render.

  **The shared-with-me arm survives and the header says it is not obviously right.** A collection a stranger shared appears under a label that says "friends". It is there because there is nowhere else — no screen renders `sharedWithMeCollections` on its own — so dropping the arm would hide those collections rather than move them. The arm goes when that surface exists, and the comment says so rather than leaving the next reader to guess it was an oversight.

  11 new cases in `__tests__/friend-collections-selector.test.ts` — which sources the rule can see, since that is the whole disagreement — plus two structural ones that stop the feed quietly going back to its own list. 3 home pins re-stated and the `depsOf` helper taught to read a single-expression memo; 1 import pin loosened to ask for a name rather than a whole import line. `npm run verify` green: 8275 tests, 20/20 guards, 15.9 KiB of headroom. No DB migration, no creds.

## The self-pair is stopped at the door, and the rule has a name (2026-09-11)

<!-- First, second and third bullets of "Self-request follow-ups" (2026-09-11): the filtered copy, the three spellings of one rule, and nothing rejecting a self-pair on the way in. -->

- [DONE: 2026-09-11] [est: 11000] **`toFriendRequests` — the one door `friendRequests` comes in through** — the previous round put the self-pair guard in the derivation, which is two layers downstream of where the bad row arrives: it still entered state, was still persisted, and every future reader had to know to skip it. Both entry points (the hydrate and the realtime refetch) carried their own copy of the same `map`, which is exactly how two doors come to enforce different rules about what a row may say. One mapper, the filter in it, and the derivation gets to trust what it is handed — the filtered copy it allocated on every change is gone with it.

  **`isSelfPair` is the rule stated once.** It was written three times before it was named: a `&&` on each arm of `collectHandshakes`, an early return in `addFriend`, and the derivation's filter. Three spellings of one sentence is how the fourth author writes a fourth. The header says what it guards and what it does not — the DB rejects the row and nothing composes one deliberately; the path it covers is a row arriving anyway, from a hand-edited cache or a server that stops enforcing its constraint.

  **The mounted cases did not change and now prove a different mechanism.** They set a self-pair on the fetch and assert the viewer is not their own friend; they passed against the derivation's filter yesterday and pass against the door today, which is what an end-to-end case is for.

  6 new cases (4 for `isSelfPair` including the arm `collectHandshakes` carried, 2 structural for the door), 1 hydrate pin re-stated — the gate it guards is unchanged, the name inside it was not. `npm run verify` green: 8265 tests, 20/20 guards, 16.1 KiB of headroom. No DB migration, no creds.

## A request naming one person on both sides is not a relationship (2026-09-11)

<!-- First, second and third bullets of "Request-direction follow-ups" (2026-09-11): the self-request hole, the `friendIds` alias, and `incomingRequestUserIds` still filtering the raw list. -->

- [DONE: 2026-09-11] [est: 9000] **One comparison closes a hole that predates the rewrite** — a row `user-a → user-a` lands in `outgoing` AND `incoming`, so the intersection holds the viewer's own id: their name in `friends`, a `fetchProfileById` for a profile the provider already holds, and a count one too high. `getRelationship` answers `"self"` before it looks at either set, so nothing on screen would ever have said so — which is what makes it the kind of hole that is unreachable right up until a locally-created request skips the path the DB constraint guards. The old `hasRequest` derivation had it too, so the round that rewrote the derivation neither introduced nor closed it; this one does, in the filter that feeds both passes.

  **`incomingRequestUserIds` reads `incoming` minus `mutual`.** It was filtering the raw list — a fourth pass over something three sets are already derived from — for the same answer in the same order, since a Set iterates in insertion order and `incoming` was filled by walking that list. One difference, and it is a fix: two rows from the same person are one inbox entry rather than two identical rows. `requestDirections` is now the only reader of `friendRequests`.

  **The `friendIds` alias says why it is a `const` among memos.** It reads as the one derivation somebody forgot to memoise, and the next author to "fix" it would add a `useMemo` that recomputes on exactly the renders the memo above it already does.

  5 new cases. `npm run verify` green: 8259 tests, 20/20 guards, 16.1 KiB of headroom. No DB migration, no creds.

## The request list is walked twice, not once per asker (2026-09-11)

<!-- First bullet of "Friend-Set follow-ups" (2026-09-11): "`getRelationship` scans `friendRequests` twice per call and the people browser calls it once per row". -->

- [DONE: 2026-09-11] [est: 12000] **`requestDirections` — three questions about one flat list, answered in two passes** — `friendRequests` is a list of directed pairs and `hasRequest` is a `.some` over all of it. It was called twice per `getRelationship`, which `app/people.tsx` calls once per row of a twenty-five-profile page: fifty scans to render one page, and the page re-renders on every follow, every accept and every keystroke in the search field. The friend derivation was worse — a `hasRequest` inside a `forEach` over the same list, which is quadratic in a list the realtime subscription refetches.

  **Two linear passes: the directions, then the mutual ones.** `outgoing`, `incoming`, and `mutual` as the intersection — which is what a friendship IS here, and what `friendIds` now points at rather than deriving a second time.

  **`friends` walks the list rather than intersecting the sets, which looks redundant and is not.** The ORDER of that array is the order the requests arrived in, and it is what the profile fetch and the home screen's count render from; intersecting would order it by whichever direction happened to be recorded first. A case pins it with a fixture whose two orders disagree.

  **`addFriend` and `removeFriend` read the sets too.** Four more scans per tap, asking the render's `friendRequests` — the exact list the sets are derived from. The one `hasRequest` left is the duplicate-pair guard INSIDE the `setFriendRequests` updater, and it stays a scan deliberately: `current` is the live state, which a concurrent update may have moved past the render the directions came from. The comment says so and a case counts the call sites.

  9 new cases (all five relationship values off one mount, a handshake between two strangers that must reach none of the three sets, and the ordering property), 3 existing wiring pins re-stated for the new seam — including the one whose argument order decides whether a decline is reported as a cancellation. `npm run verify` green: 8254 tests, 20/20 guards, 16.1 KiB of headroom. No DB migration, no creds.

## "Is this person a friend" is one Set, not one per caller (2026-09-11)

<!-- First bullet of "Home-derivation follow-ups" (2026-09-11): "`friends` is an array in the context and every consumer that asks 'is this person a friend' scans it". -->

- [DONE: 2026-09-11] [est: 13000] **`friendIds` on the context — the Set is built where the array is** — the previous round gave the home screen a `new Set(friends)` inside its memo, which fixes one caller and leaves the shape for the next one to rediscover. There were four more: the provider's own `incomingRequestUserIds` filter (requests × friends), the two visibility filters (collections × friends, and again × `following` × the tombstone list), and the profile screen. Each one is a scan of a list that changes when somebody accepts a friend request, and each author who noticed built their own Set.

  **The derivation already built a Set and threw it away.** `friends` was `[...uniqueIds]` at the end of the memo; it is now `friendIds` returned whole, with `friends` as its spread in a memo of its own. Both shapes, one pass, and `friends` cannot hold a duplicate because the Set is no longer discarded before the array is made.

  **`following` and `deletedProfileIds` got the same treatment and stayed INSIDE the provider.** Both are persisted as JSON arrays and asked membership questions per row — the tombstone list against every profile the app knows, `following` against every social collection — and nothing outside the provider scans either. Exposing three sets where one is asked for would be widening the context's surface for no reader.

  **The two visibility getters became one memo each.** `getVisibleItems` re-ran `getVisibleCollections`'s filter to get the ids, so one evaluation of the collections provider's `collections`/`items` memos walked `seedSocialCollections` twice, scanning three arrays per row on each pass. The getters hand back a memo now. That makes the returned arrays stable between graph changes — a change in kind, since callers used to get a fresh array per call — and both consumers only read: a `forEach` and a push into `merged`.

  **A brittle pin retired on the way past.** `home-derivations.test.ts` matched `}, [collections, sharedWithMeCollections, friends]);` as a literal, which is a question about formatting: reordering deps is a no-op for React and turned the suite red, while a fourth dep landing elsewhere in the file passed silently. It parses the dep array into a set of names now — the second bullet of the same suggestion round, closed by the round that tripped it.

  15 new cases in `__tests__/social-friend-ids.test.ts`, mounted rather than read (the claim is about what the context HANDS OUT: that the Set and the array name the same people, and that the filters that changed shape still decide the same way), plus a structural describe for the half a mount cannot see — that no scan came back. 2 cases in the home suite rewritten, 1 added. `npm run verify` green: 8250 tests, 20/20 guards, 16.3 KiB of headroom. No DB migration, no creds.

## The home screen stops re-deriving its friends list (2026-09-11)

<!-- Fifth bullet of "Fresh-read follow-ups" (2026-09-10): "`useMemo` on `ownedCollections` fixed a re-derivation and left three beside it". -->

- [DONE: 2026-09-11] [est: 10000] **`friendCollections` is memoised, and the friend lookup stopped being a scan** — `ownedCollections` got a memo because a ref had to point at a stable array; the three derivations beside it were flagged in the same round and left, since none of them was blocking anything. That is the shape of a performance note that never gets done — it is nobody's bug until somebody's list is long.

  **The real cost was inside the filter, not around it.** `friends.includes(collection.ownerUserId)` ran once per collection, so the work was friends × collections: a user with forty friends and two hundred visible collections paid eight thousand string comparisons for one render of a list that only changes when somebody accepts a friend request. A `Set` makes it linear, and the memo makes it once per input rather than once per render — on a screen that re-renders on every tab swipe, every pull to refresh, and every state change in the provider tree above it.

  **`sharedWithMeIds` folded into the memo that is its only reader.** It was a screen-level `const` with one consumer, rebuilt beside the filter that used it.

  **Hoisted above the `!ready` early return**, for the reason `ownedCollectionsRef` was: a hook below a conditional return runs on some renders and not others, which React treats as a different component.

  **`myProfile` is deliberately left alone, and the code says why.** `getMyProfile` is built inside the context value object, so a memo keyed on it would recompute on exactly the renders it already does; what it costs is one `Map.get`. A sweep that counted un-memoised derivations would have demanded that one too and been wrong, which is why the reason is written down rather than the memo added.

  6 new cases in `__tests__/home-derivations.test.ts`, structural like the screen's other pins, including the one that keeps the deliberate omission deliberate. `npm run verify` green: 8234 tests, 20/20 guards, 16.3 KiB of headroom. No DB migration, no creds.

## A blip is not an outage (2026-09-11)

<!-- First bullet of "Reconnect-notice follow-ups" (2026-09-11): "A flapping socket is now a pill that never settles". -->

- [DONE: 2026-09-11] [est: 11000] **`OFFLINE_GRACE_MS` — a drop is not news until it has lasted** — the round before this one gave the connection a second sentence, and a polite live region QUEUES rather than replaces. So a phone on a train wrote two sentences per drop/recover cycle into something that reads all of them, and the user was read a paragraph about a connection they can do nothing about. The pill flashing on and off is the same fact for somebody who can see it — the regression was introduced and flagged in the same round, and it is closed in the next one.

  **The two timers point in opposite directions, and that is the design.** Going down waits 1.5s; coming back is immediate, because by then the user has already been told something is wrong and is waiting to hear it is not.

  **Both halves of a blip are silent, and the second one falls out of the first.** A recovery inside the window cancels the drop before it was ever said, and `"reconnected"` can only follow an `"offline"` that was actually reached — so a connection nobody was told had broken has nothing to come back from. No second rule, no second flag.

  **A screen that mounts disconnected waits too.** The notice starts at nothing rather than at `"offline"`, so opening a screen during a 200ms blip flashes nothing.

  **The pill suite stopped timing the notice and started being handed one.** WHEN each notice appears is the hook's suite, under its own short windows; WHICH sentence each notice renders is the pill's. Driving the real hook there would have meant waiting out a 1.5s grace per case to assert a string — and it let three new cases ask what the pill passes IN, including that `idle` is not offline.

  4 new cases and 5 rewritten. `npm run verify` green: 8228 tests, 20/20 guards, 16.4 KiB of headroom. No DB migration, no creds.

## The connection says when it comes back, not only when it goes (2026-09-11)

<!-- Fourth and fifth bullets of "Status-region follow-ups" (2026-09-11): "The offline pill says 'Offline · reconnecting' and never says 'back online'" and "app/chat/[id].tsx still renders its own inline copy of the offline pill". -->

- [DONE: 2026-09-11] [est: 18000] **`lib/connection-notice.ts` — the pill vanishing is not a sentence** — the offline pill told the user the socket had dropped and then went quiet when it returned. On screen a pill disappearing reads as "fixed", which is why nobody noticed; in a live region text becoming empty is announced by nothing at all. So the user who most needed telling was told the connection broke, was never told it worked again, and had a message they had just sent with no way to know whether it had gone.

  **A reconnection is a TRANSITION, which is why it needs a decision function and a timer rather than a boolean read off the socket.** `"offline"` while it is down, `"reconnected"` for four seconds after it comes back, nothing the rest of the time. A session that starts online has not reconnected: the first render is not a transition, and greeting every user with news about a connection that was never broken is worse than saying nothing.

  **The clear is its own function because the timer outlives what it was started for.** The socket can drop again inside those four seconds, and a timer that blindly wrote `null` would erase an `"offline"` the user is currently living through. The newer fact wins, in both the reducer and the effect cleanup.

  **The chat screen's own pill was the last silent surface.** It reads its own socket rather than the shared registry, so it keeps its own source and its own styling and gains the region and the notice — it is the screen where a dropped connection matters most, and it was the one the previous round did not reach.

  **`CONNECTION_NOTICE_KEYS` is a table because `lint:orphan-i18n` was right about the ternary.** Written as `t(notice === "offline" ? "chatOfflinePill" : "chatBackOnlinePill")`, the guard stopped seeing the first key: `lib/i18n-key-usage.ts` counts a literal in a record value as a read and the consequent of a `?:` as an expression, and its header says the fix for a form it cannot see is to widen the rule or write the key somewhere it can. A table does the second and retires a ternary that was about to be written twice.

  16 new cases in `__tests__/connection-notice.test.ts` (the reducer, the stale clear, and the hook driven through a real tree, because "a mount at online is not a reconnection" is a claim about the first render); 1 new i18n key in all six locales; 2 pill cases rewritten. `npm run verify` green: 8224 tests, 20/20 guards, 16.6 KiB of headroom. No DB migration, no creds.

## The two pills that appeared and vanished in silence (2026-09-11)

<!-- Fourth bullet of "Announcement-door follow-ups" (2026-09-10): "Every other silent state change in the app is still silent — the sync pill, the storage notice, a rejected write, the offline banner". -->

- [DONE: 2026-09-11] [est: 14000] **The sync pill and the offline pill are polite live regions now** — one says a write is parked and the other says the socket has dropped, and both appeared, sat there and vanished with nothing reaching the accessibility tree as a CHANGE. A user who cannot see them found out by going looking for a node that may not be there. (The storage notice and the rejected write, the other two on that list, already go through the toast, which has been announced since 2026-09-10.)

  **A region, not `announceMessage`.** The app's one announcement channel is assertive: it exists so a keyboard reorder can cut off the position the user was at three presses ago. Cutting somebody off mid-sentence to say three changes are queued is the wrong trade, and a status is worth waiting for a pause.

  **The wrapper is the whole mechanism.** A live region announces a CHANGE to text it already had; a node carrying `aria-live` that is inserted with its text already inside it is usually not announced at all — the lesson `lib/announce.web.ts` opens with and the reason `ensureLiveRegion` exists. So the region is mounted from the first render and the pill moves in and out of it. Empty, it carries no style and costs no layout.

  **Both spellings of the prop**, because the platforms disagree on the name: react-native-web forwards `aria-live` to the DOM node, Android reads `accessibilityLiveRegion`, and iOS has no live regions at all — which nothing here can change and which the header says rather than implies.

  **The realtime pill's outer `null` stays outside the region.** A screen with no realtime subscription has no connection to have an opinion about, which is not the same fact as a connection that is up.

  10 new cases in `__tests__/status-pill-live-regions.test.ts` — rendered rather than read off the source, because "the region is in the tree BEFORE the pill" is the one claim a regex cannot check. `npm run verify` green: 8207 tests, 20/20 guards, 18.4 KiB of headroom. No DB migration, no creds.

## Two things happening at once are both said out loud (2026-09-11)

<!-- Second bullet of "Announcement-door follow-ups" (2026-09-10): "Two callers now announce and neither knows the other just did". -->

- [DONE: 2026-09-11] [est: 15000] **`lib/announce-queue.ts` — one caller can no longer silence another** — the door has five call sites now through three paths (the keyboard reorder, the blocked-by-sort notice, the sort restore, every toast, the pick-up), and two of them landing in one tick meant the second overwrote the first. On web both writes reached one node inside one task, so a reader observed a single mutation and read a single sentence; on native the second `announceForAccessibility` cut the first off mid-word. A toast arriving with a keyboard move silenced the move, and nothing said so.

  **The module already claimed otherwise.** `lib/announce.web.ts`'s header said "the write clears the region first, which is also what makes two announcements in quick succession both land", and the case beside it — "keeps the last message when two announcements race" — pinned the losing. One of them was wrong about what this app wants, and it was the case.

  **One held message and one pending slot.** The first sentence is said SYNCHRONOUSLY, because it is about something the user just did and a reader that answers a keypress a tick late is worse than one that answers now — which is why this is a hold and not a debounce. It then keeps the channel for 150ms, which is what makes the next sentence a separate mutation rather than a correction to this one. A sentence arriving inside that window goes into a slot holding exactly one, and a third replaces the second: two callers in one tick are both heard, and five keyboard moves in a second are heard as the first and the fifth. The three in the middle were stale before they could be read aloud, and a queue that spoke them all would leave the user a second behind their own hands.

  **Both spellings of the pair drive it**, so the fix is not web-only: the native channel interrupts itself just as readily. `__resetAnnouncementsForTests` is exported from both — the channel is module state, and a suite that announces was otherwise arming a timer that fired into the next suite's fake DOM.

  13 new cases in `__tests__/announce-queue.test.ts` under mock timers; the two web cases that pinned the old behaviour rewritten to pin the new. `npm run verify` green: 8197 tests, 20/20 guards, 18.5 KiB of headroom. No DB migration, no creds.

## The card that fades is the card under the finger (2026-09-11)

<!-- First bullet of "Fresh-drag follow-ups" (2026-09-11): "`activeIndex` is still the long press's index and it is what dims a row". -->

- [DONE: 2026-09-11] [est: 8000] **The dim follows the row, because the commit already does** — the previous round re-resolved the dragged row at the drop and left the DISPLAY reading the long press's index. So a merge landing mid-gesture put the dim on whatever row had since taken that slot, while the drop marker was measured against the new list: the same bug as before, with the user watching. They see the wrong card fade and the marker land somewhere it does not belong.

  **`active` holds the row, not its index.** The index is a fact about `data`, and `data` changes without the gesture doing anything — so it is derived on every render through the same `indexOfDraggedRow` the drop uses, which makes one answer where there were two. -1 is a row that has left the list mid-drag: it dims nothing and marks nothing, which is the answer `onUp` gives it too.

  2 new cases — the row the merge pushed down, and the row the merge deleted — on the harness the previous round built. `npm run verify` green: 8183 tests, 20/20 guards, 19.2 KiB of headroom. No DB migration, no creds.

## The drag moves the row the finger is holding (2026-09-11)

<!-- First bullet of "Two-way-anchor follow-ups" (2026-09-11), and of the two rounds before it: "`onDragEnd` sends `from` and `planDragCommit` re-derives it from `data[to]`". -->

- [DONE: 2026-09-11] [est: 17000] **`components/DraggableList.web.tsx` re-resolves the dragged row at the drop** — the flag was "a second source of truth", raised three rounds running, and it pointed one layer above where the disagreement actually was. `planDragCommit` reading `data[to]` is right: `from` indexes the list BEFORE the move and `to` indexes `data`, which is that list with the row moved, so they are answers about different arrays and only one is in the same frame as the rest of the function. The stale index was in the shim, and there it was a bug rather than a redundancy.

  **`beginDrag(index)` captured the index at the long press and `onUp` spliced it out of the list as it was at the DROP.** The ref beside it exists because "a drag that starts before a cloud merge lands and ends after it must reorder the list as it is at the drop" — and only half of that shipped: `rows` was re-read and `from` was not. A merge landing mid-gesture then means `moveItem(current, from, to)` lifts out whatever row has since taken that index. The user drags Alpha and Beta moves, the commit is a well-formed whole-list order, and `planDragCommit` downstream re-plans the wrong row faithfully — it reads `data[to]`, and `data` is the array that line built. The same half-fix the keyboard route had before `identify`, on the route that produced the argument for it.

  **The row is recognised by key, not by reference.** A merge rebuilds the objects, so the same row comes back as a new object with the same id and `indexOf` would find nothing on every merge — the case `identify` was written against. `keyExtractor` is destructured for the gesture and handed straight back to the `FlatList`; a list rendered without one falls back to the row itself, which is right for a list whose rows are not being rebuilt underneath it.

  **A row that LEFT the list commits nothing.** Deleted on another device, moved to another collection, filtered out — there is no move to make, and making one moves whatever is standing where it was.

  **`to === from` stopped being the question "did the finger move".** Once `from` is re-resolved, a list that shrank above the dragged row can make a genuine landing index collide with the index the gesture started from, and reading that as "never moved" drops a drag the user made. An explicit `moved`, set before the marker's dedup check rather than after it — a pointer resting on the index it was already over is still a pointer that resolved.

  6 new cases in `__tests__/draggable-list-web.test.ts`, on a harness that re-renders the shim with new `data` mid-gesture and re-attaches the row refs — the two things the real `FlatList` does when a merge lands under it. `npm run verify` green: 8181 tests, 20/20 guards, 19.2 KiB of headroom. No DB migration, no creds.

## The tap the user took back never leaves the device (2026-09-11)

<!-- First bullet of "Reaction-sequence follow-ups" (2026-09-11): "Two taps now send two writes and nothing collapses them". -->

- [DONE: 2026-09-11] [est: 19000] **`lib/reaction-write-queue.ts` — the settled direction is the only one the cloud hears** — the double tap was fixed on screen and left alone on the wire: on-then-off in one gesture was an INSERT and a DELETE for a row the server created and dropped microseconds apart. Two round trips, a write made to satisfy a user who had already changed their mind, and on a flaky connection two chances for a rollback to fire for a tap that had been undone. A trailing debounce keyed on (targetType, targetId, emoji) — the `lib/identify-scheduler.ts` shape, for the same reason: only the settled value is worth sending.

  **The collapse compares directions, it does not count taps.** Parity gives the same answer only while taps strictly alternate, which is true of a finger on one emoji and not true of a re-read landing in the middle of the window. A pending "add" meeting a "remove" drops both, because the list is back where the server already is and the correct number of writes is zero.

  **The key is the whole triple.** Two reaction bars are mounted at once whenever an item sits over the collection it belongs to, and one emoji's settled direction says nothing about the other target's.

  **One write per key at a time.** A tap arriving while that key's write is in flight cannot cancel it, so it waits and the window re-arms when the write lands — an INSERT and a DELETE for one row racing to the server is the one way this module could leave the cloud disagreeing with the screen. And the unmount flushes: the window is a batching delay, never a way to lose a tap the user made on their way out.

  **Related bug found and fixed: the rollback could not fire on the failure it was written for.** `fetch` resolves for every status the server manages to answer with, and neither `addReaction` nor `removeReaction` looked at one — so an RLS denial arrived as a 401 that read exactly like a success, and the optimistic row stayed on screen stating a fact the server had refused. Both writers assert the response now. The hook's suite mocked them, so the rollback had only ever been asserted against a dropped connection. `removeReaction`'s `user_id` filter was also the one caller-supplied segment of that URL going in unencoded.

  22 new cases in `__tests__/reaction-write-queue.test.ts` under mock timers (the boundaries, the in-flight serialisation, the flush), 4 more in the hook's own suite for the gesture end to end, 4 in `supabase-profiles-shapes.test.ts` for the refusal. `npm run verify` green: 8175 tests, 20/20 guards, 19.5 KiB of headroom. No DB migration, no creds.

## Two taps on one emoji are on and then off (2026-09-11)

<!-- Fourth and fifth bullets of "Optimistic-write follow-ups" (2026-09-06): "`toggle` depends on `reactions`, so a double tap before the re-render reads a stale list" and "the optimistic row's id is `tmp-${Date.now()}`". -->

- [DONE: 2026-09-11] [est: 16000] **`lib/reaction-toggle.ts` + a ref the writer updates itself — the double tap stops being two inserts** — `toggle` closed over `reactions` and listed it as a dep, so two handlers running in one pass both read the list from the last render, both found no row of their own, and both inserted: two hearts on screen and two INSERTs sent to a table with a unique constraint on (user, target, emoji). The emoji are 44 points apart and a double tap on one of them is a single thing a finger does, so this is the ordinary gesture, not a contrived one.

  **A `setReactions` updater cannot fix it.** The updater sees the real previous list, but the DIRECTION of the tap decides which network call to make and has to be known synchronously — before any updater runs. So the writer keeps its own copy and updates it ahead of the re-render; every mutation including the fetch goes through one `apply`, or the ref becomes the stale answer instead of the fresh one.

  **`toggle` also stops changing identity per tap.** `reactions` was a dep purely to read the list, so every count change minted a new handler and a new prop for `components/reaction-bar.tsx`. Same root cause, and the case that pins it is one line.

  **The optimistic id is a uuid.** `tmp-${Date.now()}` gave two taps in one millisecond the same id, and rolling back either then removed both — the exact class of bug `lib/item-id.ts` documents having cost this app duplicated rows once already.

  **The three decisions are a module now** because the failure they guard is a SEQUENCE — tap, tap, reject — and the rollbacks are written against the list as it is when the write fails rather than a snapshot from when the tap happened, which no single-tap case can tell apart. 16 cases in `__tests__/reaction-toggle.test.ts` for the sequences, 5 more in the hook's own suite for the gesture end to end. `npm run verify` green: 8144 tests, 20/20 guards, 21.0 KiB of headroom. No DB migration, no creds.

## The drop is read from whichever neighbour survived (2026-09-11)

<!-- Third and second bullets of "Drag-commit follow-ups" (2026-09-11): "anchors on the row ABOVE the drop and never on the row below it" and "The anchor walk is O(to × rows)". -->

- [DONE: 2026-09-11] [est: 9000] **`anchoredTarget` looks down as well as up** — the anchor rule shipped one-sided because one side covered every case that had been written: the nearest surviving row above the drop, else the top. "Else the top" is the part that was wrong. A drop near the top whose two or three predecessors were deleted on another device resolves to position 0 — a real position the user did not ask for — while the row they dropped ONTO sat there the whole time, one place below. The fallback walks down instead and lands the row before the first neighbour that survived; the top is now only for a list holding nothing the snapshot had, where there is genuinely nothing to be relative to.

  **The downward walk is a fallback, not a tiebreak.** A drop with both neighbours intact must still land after the one above it — otherwise every ordinary drag moves one place — and the case that says so is the one that would catch a reordering of those two loops.

  **One lookup per row instead of one scan per candidate.** The walk can visit every row in `data` before it finds a survivor, and each step was a `findIndex` over the whole list: a drop at the bottom of a 2000-item collection after a merge that trimmed the rows above it was a hundred full scans, landing after the gesture had already ended. A key→index map built once makes it linear. First key wins, so a duplicate key resolves the same way `findIndex` did.

  4 new cases (3 for the new branch, 1 pinning that it stays a fallback). `npm run verify` green: 8123 tests, 20/20 guards, 21.6 KiB of headroom. No DB migration, no creds.

## The drag commits the list it is looking at, not the one it drew (2026-09-11)

<!-- Second bullet of "Row-identity follow-ups" (2026-09-10): "The drag route has the same staleness and none of the fix". Also the first bullet of that section, "Both screens pass the same `keyOf` and it is `(row) => row.id` twice". -->

- [DONE: 2026-09-11] [est: 17000] **`planDragCommit` — the route that produced the staleness argument was the one route still committing a stale array** — the keyboard actions stopped trusting the render three commits ago (`rows` read at action time, the row found by `identify`); `onDragEnd` kept handing `commitItemOrder` / `reorderOwnedCollections` its `data`, which is the list's reordered copy of what it DREW. A cloud merge or a `loadMore` landing between the finger going down and coming up is then persisted away: rows that arrived mid-gesture are dropped from the order, rows deleted elsewhere are written back, and the user who dragged one card renumbered a list nobody was looking at. Flagged in three separate suggestion rounds and untouched by both extractions.

  **The drop is read as "after this row", not as an index.** `to` indexes `data`, and an index into a list that has changed names a different card — so the nearest predecessor in the snapshot that still exists is the anchor, and the row lands immediately after it. A drop at the top has no predecessor and lands at the top. When nothing changed underneath this reproduces `data` exactly, which is the property the 20-case suite opens with and then proves over every from/to pair on a four-row list: the 99% must not pay for the 1%.

  **Three gestures now commit nothing at all** — a row deleted mid-drag (committing `data` would resurrect it), a `to` that is not a position in the snapshot, and a drop that resolves to where the row already sits. The last one is the "it moved" / "it was already there" ambiguity `availableReorderActions` refuses to create for the keyboard, arriving at the pointer route for the same reason: the announcement reads `plan.to` and `plan.rows.length`, so what is spoken is the order that was actually written.

  **`byId` retires the fourth hand-written `(row) => row.id`.** Two `identify`s and two `keyOf`s, all meaning the same thing, none of them saying so — and a key that is not stable is exactly how a drag commits against a row it matched by name.

  20 new cases in `__tests__/plan-drag-commit.test.ts`; five existing source pins re-stated for the new seam, one of them gaining a case for the silent-refusal branch. `npm run verify` green: 8120 tests, 20/20 guards, 21.8 KiB of headroom. No DB migration, no creds.

## The sort chips are one choice, not seven buttons (2026-09-10)

<!-- First bullet of "SORT_OPTIONS + sort chip a11y" (2026-08-07): "Give the sort chips a `radiogroup` semantic instead of three independent buttons". -->

- [DONE: 2026-09-10] [est: 7000] **`radiogroup` + `radio`, because that is what the row is** — seven chips, one of which is on, and choosing another turns the previous one off. `accessibilityState={{ selected }}` (added 2026-08-07) says which one is active and nothing said they were alternatives, so a screen-reader user heard seven unrelated buttons with no reason to expect that picking one changes the others and no count of how many there were. The row carries the group role and its label; each chip carries `radio` with both state spellings — RN maps `selected` on native, react-native-web renders `aria-checked` from `checked`, which is the state a radio actually carries.

  The union grew from three modes to seven since that suggestion was written, which only sharpens it: seven independent buttons is a worse thing to hear than three. 1 new case; three existing pins re-stated. `npm run verify` green: 8098 tests, 20/20 guards, 22.4 KiB of headroom. No DB migration, no creds.

## A tag is the same colour everywhere, and Add says when it refuses (2026-09-10)

<!-- Second and fourth bullets of "TAG_COLORS rotation + nextTagColor" (2026-08-07): "Persist the tag colour by label" and "Surface a 'tag already added' hint". -->

- [DONE: 2026-09-10] [est: 14000] **`lib/tag-input.ts` — one implementation of adding a tag, and a hue that belongs to the label** — `nextTagColor` only ever looks at the tags on the item in front of it, so "sealed" was amber on one card and sage on the next, which defeats the point of colour-coding a label that means the same thing everywhere. The colour is derived from the lowercased label now (the `placeholder-color.ts` deterministic-palette idiom the repo already uses for avatars).

  **The label's hue is a preference, not a rule.** Two labels can hash to one slot, and two same-coloured tags side by side on one item is what a user reads as a mistake — so when the derived hue is already on the item, the old rotation picks the first free slot and consistency is traded away only where keeping it would be visibly wrong. Existing `tag.color` values stay authoritative: no migration, only new tags derive.

  **Pressing Add on a tag the item already has said nothing** — a bare `return`, in both forms, which is indistinguishable from a broken button. `addTagToList` reports `empty` and `duplicate` separately because only one of them is worth saying out loud, and the screens toast `tagsDuplicate` (six locales). The existing casing wins on a duplicate: the user typed that one first, and rewriting it under them would be a silent edit of data they can see.

  **Both screens now hand the list to one pure function** — the two hand-written `addTag()`s had the same three rules and different variable names, which is the shape that drifts.

  14 new cases in `__tests__/tag-input.test.ts`; the three existing rotation pins and two design-token pins re-stated for the new seam. `npm run verify` green: 8097 tests, 20/20 guards, 22.5 KiB of headroom. No DB migration, no creds.

## An active-looking control says it is active (2026-09-10)

<!-- Second bullet of "SORT_OPTIONS + sort chip a11y" (2026-08-07): "Sweep the other chip rows for the same missing `selected` state". -->

- [DONE: 2026-09-10] [est: 11000] **Three surfaces were styled selected and announced as plain buttons** — the app says "this one is active" with a style suffix (`chipActive`, `templateCardActive`, `filterButtonActive`), which is invisible to a screen reader. The filter button read the same whether its sheet was open or not (`expanded`, not `selected` — it opens a sheet; the dark treatment means filters are on, which the label already says as a count); the collection templates were a row of identical buttons with nothing saying which one was applied; the share sheet's per-friend toggle changed its label on press but announced no state on focus.

  **A sweep, because the failure mode is never difficulty.** It is one prop, and it goes missing when a new chip is added beside five that have it. The rule asks one question over `app/`, `components/` and `lib/`: does every `<Pressable>` whose style names an `*Active` variant declare an `accessibilityState`? What that state SAYS stays the author's call — a rule that guessed would be wrong about a third of the cases and would train people to write the wrong one to keep it quiet.

  **The scanner is parsed, not regexed.** A JSX opening tag is full of `>` characters that do not end it: every arrow function in an `onPress` has one. The first draft named seven files, four of which already had the prop below the arrow where the regex gave up — so the sweep tracks brace depth and string literals, and four of its five cases are about the scanner itself.

  5 new cases in `__tests__/chip-a11y-state.test.ts`, walking through the shared `helpers/source-files.ts` rather than a hand-rolled `readdirSync`. `npm run verify` green: 8083 tests, 20/20 guards. No DB migration, no creds.

## Every toast is heard, and its action can be reached (2026-09-10)

<!-- First, second and fourth bullets of "After the holdable toast" (2026-09-10). -->

- [DONE: 2026-09-10] [est: 13000] **`toastAnnouncement` — the app's main "something happened" channel stops being visual-only** — `<ToastHost>` is an overlay at the top of the tree, so a screen reader reaches it only if it happens to walk into that subtree, which may be after the toast has gone. Two callers had hand-written `announceMessage` calls beside their `toast.show(…)` for exactly that reason and every other caller was silent. The sentence is derived from the toast and said once, by the provider, through the app's ONE live region — a second region would interrupt the reorder announcements and the user would hear half of each.

  **The action label stays out of the sentence.** "Undo" read aloud with no way to say how to reach the button is a worse sentence than the message alone; the reachable version of that button is a focus concern, and it is the other half of this task.

  **The action is focusable now**, so a keyboard can reach it at all — and so the focus-hold added with the timer has something to hold ON. It was wired to `onFocus` on an element nothing could focus.

  **The hold covers the whole toast.** The accent bar moved inside the dismiss target: a five-pixel stripe that released the timer when the pointer crossed it is a hover gap nobody can see or explain.

  The clear's hand-written announcement is gone with the general rule in place; the undo keeps its own, because it shows no toast. 12 new cases. `npm run verify` green: 8078 tests, 20/20 guards, 23.6 KiB of headroom. No DB migration, no creds.

## An undo you can actually reach (2026-09-10)

<!-- Second and fifth bullets of "After the reorder/sort handoff" (2026-09-10): "Pause the dismissal timer on hover and on screen-reader focus" and "Say the undo out loud when it happens". -->

- [DONE: 2026-09-10] [est: 12000] **The toast holds its window open while somebody is reading it** — the dismissal timer was started by `show()` and nothing could extend it, so the 6.5s window ran whether or not the user had noticed the toast, was reading it in their second language, or was moving a cursor toward the Undo button. A timer owned by the provider cannot be paused by the toast it is counting down, so it moved into `<ToastView>`: a pointer over the toast or focus inside it holds it, leaving restarts the full window (a 300ms stub after looking away is indistinguishable from being ignored). The per-toast dismiss handler is read through a ref, or its identity — new on every render of the host — would restart the timer by itself.

  **And both halves are said out loud.** The clear discards a choice and the undo puts it back, and a toast is an overlay a screen reader reaches only if it happens to walk into it. `sortClearedForReorder` was already written; `sortRestored` is new, in all six locales, because a user who presses Undo and hears nothing has only the list they cannot see as evidence that it worked.

  4 new cases in `__tests__/toast-action.test.ts`. `npm run verify` green: 8071 tests, 20/20 guards, 24.1 KiB of headroom. No DB migration, no creds.

## The bundle budget, re-measured and re-argued (2026-09-10)

<!-- Fourth bullet of "After the reorder/sort handoff" (2026-09-10): "Raise the bundle budget deliberately, or spend a diff getting under it". -->

- [DONE: 2026-09-10] [est: 6000] **4.53 → 4.55 MiB, with the argument the doc block asks for** — three features took the bundle from 4619.4 to 4634.4 KiB and the headroom from 19.3 KiB to 4.3, so the next diff of ordinary size would have gone red on nothing an SDK did. The raise buys 24.8 KiB, still below Clarity's ~30 KiB, so a statically-imported SDK trips the gate on its own commit — which is the whole point of the number and the thing a comfortable raise would have given away.

  **The test's measured bundle was stale too.** `bundle-size.test.ts` reasons about the RELATIONSHIP between the budget and a measured size, and it was still measuring 4609.1 KiB from two budget moves ago — so both of its assertions were true about a bundle that no longer exists. Re-measured to 4634.4 KiB, with the rule stated: it moves when the budget moves, and only then.

  The two prose copies of the number (`ci.yml`'s step comment, the CLI's header) moved with it. `npm run verify` green: 8068 tests, 20/20 guards, 24.8 KiB of headroom. No DB migration, no creds.

## Reorder clears the sort, and the toast can undo it (2026-09-10)

<!-- First bullet of "Reorder-blocked-by-sort notice" (2026-08-07): "Auto-reset the sort when the owner enters reorder mode, instead of explaining why it's off". -->

- [DONE: 2026-09-10] [est: 20000] **Entering reorder mode does the thing the owner asked for** — the notice fixed the "broken button" feel, but the owner still has to make two taps to reach the state they requested: tap Reorder, read why it did nothing, tap Reset. Clearing the sort on the way IN turns a dead end into a direct path, and now that the sort persists across visits the notice is landing on a choice made weeks ago that the owner may not remember making. The clear needs an undo, which the toast cannot express yet — so the toast learns an action, and this is its first consumer. The notice stays for the case it was written for: a sort re-applied WHILE reorder mode is on.

  **The toast learned an action.** `ToastAction = { label, onPress }`, rendered as a labelled button, running the handler BEFORE the dismissal — dismissing first would swallow a handler that threw, and leaving the toast up invites a second press on an undo that already happened. The label arrives translated: the module sits below the i18n context, like every other string it renders.

  **An actionable toast outlives a reporting one** — 6.5s against 3.2s. An undo the user cannot reach in time is worse than no undo, because it shows them the way back and then takes it away. The rule is `lib/toast-timing.ts` so it can be asserted; `toast-context.tsx` pulls React Native and its old literal could not be.

  **`applySort(mode)`** replaces `resetSort`'s body and is now the single writer: functional updater (so it cannot clobber an active query), remembers the mode, one dep. The undo, the notice's reset and the restore effect all go through it.

  12 new cases in `__tests__/toast-action.test.ts`; two new i18n keys in all six locales; five existing pins re-stated. `npm run verify` green: 8068 tests, 20/20 guards, bundle 4.3 KiB under budget — the headroom is getting thin, and the next feature-sized diff should expect to raise it. No DB migration, no creds.

## The collection remembers how you like to read it (2026-09-10)

<!-- Fifth bullet of "SORT_OPTIONS + sort chip a11y" (2026-08-07): "Persist the chosen sort per collection". -->

- [DONE: 2026-09-10] [est: 18000] **`useItemSortPref` — the sort survives leaving the screen, and nothing else does** — `itemFilters` reset to `EMPTY_FILTERS` on every mount, so a user who prefers A→Z re-picked it on every visit, two taps behind a sheet. Only the sort persists: a query or a tag filter is the question the user is asking right now, and restoring one would hide items with no visible cause.

  **Per collection, keyed by id in one blob** (`collectables-item-sort-v1`). The choice does not generalise — the cost sort means nothing in a collection whose items carry no prices — and one blob is one read per open instead of a key per collection.

  **`"default"` deletes the entry.** "I cleared it" and "I never picked one" behave identically, so they are one state; storing the default would grow the blob for every sheet the user opened and backed out of.

  **A retired mode is dropped on read.** The blob outlives the build that wrote it, and a mode no longer in `SORT_OPTIONS` would sort nothing while showing no chip — a picker that looks broken. Validated against `SORT_OPTIONS` itself, so a mode retires from storage the moment it retires from the UI; one bad entry costs that collection's sort, never the whole blob.

  **Applied once, and never over the user's own pick** — the read is async, so a sort chosen while it was in flight would otherwise be replaced by the stored one.

  **Related bug found and fixed:** the reorder notice's "reset sort" only changed local state, so the sort the user just turned off came back on the next visit. All three surfaces that can change the sort now remember it.

  23 new cases in `__tests__/item-sort-prefs.test.ts`; three existing pins re-stated for the new dep lists. `npm run verify` green: 8056 tests, 20/20 guards. No DB migration, no creds.

## The collection order the user dragged, on every device (2026-09-10)

<!-- Second bullet of "After the dense-run reorder writer" (2026-09-10): "Give `reorderOwnedCollections` the planner treatment too". -->

- [DONE: 2026-09-10] [est: 16000] **`byOwnedCollectionOrder` + `planCollectionReorder` — the home list reads back the order it writes** — dragging a collection wrote `sortOrder` to the cloud and reordered the local array, and the home screen rendered the ARRAY. So the number was written, synced, and never read: on a second device the rows came back in whatever order the merge produced and the user's manual order was gone, even though it was sitting in the `sort_order` column the whole time. The `collections` memo now sorts through the comparator, so the number is the order.

  **Two of the three item rules, and the third is deliberately absent.** Dragged outranks undragged, and two dragged rows sort by `sortOrder` with `id` breaking a cross-device tie. `Collection` carries no `createdAt`, so two undragged rows compare 0 and the stable sort keeps them newest-first — a local fact rather than a total one, and the honest answer until the row gains a timestamp.

  **The writer lays a dense run**, `planItemReorder`'s rules over the owned collections: unnamed ones renumbered too, unknown and repeated ids skipped, rows the user does not own untouched — their `sort_order` is their owner's business.

  **A new collection is stamped one below the lowest index**, but only once a manual order exists. Without it rule 1 would drop every newly-created collection to the BOTTOM of the home screen on the first drag — one row write instead of renumbering the whole list on every create.

  21 new cases in `__tests__/owned-collection-order.test.ts`. `npm run verify` green: 8034 tests, 20/20 guards, bundle 8.7 KiB under budget. No DB migration (`collections.sort_order` already exists and is already synced), no creds.

## A reorder writes a dense run over the WHOLE collection (2026-09-10)

<!-- Second bullet of "After the collection-order extraction" (2026-08-28): "Give `reorderItemsInCollection` a duplicate-index assertion of its own". -->

- [DONE: 2026-09-10] [est: 15000] **`planItemReorder` — the writer refuses to persist a run with holes** — `reorderItemsInCollection` renumbered only the ids it was handed, so an `orderedIds` that misses part of the collection left the rest holding their old indices, which collide with the freshly-written ones. `byCollectionOrder`'s id tiebreak renders that deterministically, which is damage control, not prevention: the collision stays in storage and the next reorder writes on top of a run that already has duplicates. The planner lays the collection's items out as a dense 0..N-1 permutation — named ids first in the order given, the rest after in their rendered order — so a partial call cannot be persisted badly.

  **The id list is a request, not gospel.** An id naming no member of the collection is skipped rather than consuming an index (it would push every real row down one), and a repeated id counts once, at its first appearance. Items in other collections keep both their number and their place in the array.

  **Only what moved gets written.** `changed` holds the rows whose number actually differs, so the drag that ends where it started syncs nothing and the plan returns `items` by reference — the same no-op contract `mergeItemsFromCloud` states, so the caller skips the `setState` and the AsyncStorage rewrite behind it.

  **Related bug found and fixed:** both reorders collected their sync list by pushing into a closure array from INSIDE a `setState` updater, then read it synchronously on the next line. React runs that updater when it renders, not when the setter is called, so the cloud write could be skipped entirely (or, under StrictMode's double-invoke, sent twice). Both now decide from the state this render is holding.

  15 new cases in `__tests__/plan-item-reorder.test.ts`. `npm run verify` green: 8013 tests, 20/20 guards, bundle 9.5 KiB under budget. No DB migration, no creds.

## Which row the user actually picked (2026-09-10)

<!-- First bullet of "Fresh-read follow-ups" (2026-09-10): "`index` is still the render's answer while `rows` is now the action's". -->

- [DONE: 2026-09-10] [est: 14000] **`identify` — the row is found by id at action time, not assumed to be where it was drawn** — reading the rows fresh was half a fix. Re-checking whether a move EXISTS at the old index says nothing about whether the row is still sitting there: a cloud merge that reorders the list leaves the index pointing at a different card, so the user asks to move Alpha and Beta moves. Silent, and correct as far as every other check is concerned — the commit is a well-formed whole-list order, it just reorders the wrong thing.

  **By key, not by reference.** A merge rebuilds the objects: the same collection comes back as a new object with the same id, so `indexOf` would find nothing and the action would do nothing on every merge. `identify: { row, keyOf }` and a `findIndex` over the key is what survives that.

  **A row the key cannot find has LEFT the list** — deleted, filtered out, moved to another collection — and the action does nothing. Falling back to the render's index is the one thing that must not happen there, because it is exactly the bug this closes.

  **Optional, and the default is the old behaviour.** A caller whose list cannot be reordered underneath it passes nothing and keeps the render's index; a case pins that, so the fallback is a decision rather than an accident.

  5 new cases in `__tests__/reorder-actions.test.ts`: the row that stayed put, the row the list reordered around (which would have moved its neighbour), the row that left, the caller that identifies nothing, and the merge that rebuilt the objects. `npm run verify` green: 7998 tests, 20/20 guards, bundle 9.9 KiB under budget. No DB migration, no creds.

