## The drop you can see, on a screen you drag with a finger (2026-09-10)

<!-- First and third bullets of "Web-drag follow-ups" (2026-09-10): "A drag on a touch screen still fights the page scroll" and "Nothing shows where the row will land". -->

- [DONE: 2026-09-10] [est: 9000] **A touch drag no longer loses to the page scroll** — the gesture shipped this morning worked with a mouse and died on a phone, which is most of a GitHub Pages audience. A long press is a finger held still, so at `drag()` the browser has not yet chosen between scrolling and giving the page the gesture; the first `touchmove` is where it chooses, and until now nothing voted. The shim registers a non-passive `touchmove` for the length of the gesture and calls `preventDefault` on it — only when the event is cancelable, since an uncancelable one is a scroll the browser has already committed to and preventing it is a console warning and nothing else.

  **Per gesture, not `touch-action: none` on the rows.** The list IS the page on both screens that use it, so a permanent `touch-action` would cost the user the ability to scroll past the collection they are looking at.

  **A mouse drag no longer selects the text it crosses** — `document.body.style.userSelect` for the length of the gesture, restored to whatever it was. `body` is optional-chained: the node-run suites install a fake document that has none, and a `TypeError` thrown inside a pointer handler is caught by nothing.

- [DONE: 2026-09-10] [est: 8000] **The dragged row says where it will land** — a 2pt amber line on the edge the row would drop onto: below the target when dragging down, above it when dragging up, never on the row being held (where "it will land where it already is" is true and is not information).

  **Absolutely positioned, not a border.** A 2pt border grows the row and pushes everything below it down on every pointer move — a visible twitch, and worse, it changes the very measurements the next move is resolved against. `View` is `position: relative` by default on both platforms, so `left: 0 / right: 0` is the row's width for free.

  **Two holders of one number, argued rather than tidied away.** The landing index is a closure variable for the commit and component state for the marker. Reading it back out of state inside a document handler would read the value from the render that attached the listener — the drag's first frame, which never moves.

  Eight new cases: both directions, no marker on the held row, the marker gone after the drop, `position: absolute` pinned as the anti-twitch property, the `touchmove` vote in both cancelable states, and a drag through a document with no `body`. `npm run verify` green both times: 7855 tests (+15 across the two), `lint:a11y-jsx` 19/19 after the marker got its `aria-hidden` to go with `accessibilityElementsHidden`, bundle 15.3 KiB under budget. No DB migration, no creds.

## Four modules nothing imports, ten days after they arrived (2026-09-10)

<!-- Not a `.suggestions.md` bullet: the loop's step 7, run as the inverse of the census. The census asked which product modules no SUITE names — all five of those now have suites. This asks which have no CALLER, which is a different question and had never been put. -->

- [DONE: 2026-09-10] [est: 7000] **Measured, and deliberately not acted on: `components/premium-gate.tsx`, `components/qr-code.tsx`, `lib/supabase-data-export.ts` and `lib/use-masonry-columns.ts` are imported by nothing** — not by a screen, not by another module, not by a script. 182 lines between them. Two are named in prose elsewhere (`premium-upsell-sheet.tsx` describes a premium gate, `data-export.ts` names the supabase wrapper in its header) and neither reference is an import; the other two are not mentioned outside their own suites.

  **All four were added on 2026-08-31 and none has been touched since.** One day, four modules, no callers, ten days — which reads as scaffolding staged for features that have not landed rather than as code that lost its last caller.

  **Why this stops at the measurement.** Deleting them is a decision about somebody's unlanded work, and the evidence cannot tell staged from abandoned: a QR code component and a premium gate are both plausible next features and both plausible dead ends. What would settle it is knowing whether those features are still coming, which is not in the tree.

  **Why it is not a case either.** The obvious shape — pin the four, turn a fifth into a red run — is the shape `#536` was, and that PR was closed without merging. The census it carried has since been made moot the better way: every module it named now has a suite, and one of those suites found a leaked interval in `useVisibilityRefresh`. The finding travelled; the case did not need to.

  No code changed. `npm run verify` not re-run: nothing to verify.

## The reorder mode the deployed build could not run (2026-09-10)

<!-- First bullet of "Web-list follow-ups" (2026-09-10): "Drag-to-reorder does nothing in the build that ships — `components/DraggableList.web.tsx` stubs `drag` as a no-op and pins `isActive: false`, so `onDragEnd` never fires; `app/collection/[id].tsx` still renders a 'reorder items' toggle and `app/index.tsx` still long-presses collection cards, both of which are inert in a browser." -->

- [DONE: 2026-09-10] [est: 16000] **Drag-to-reorder works on web now** — the gap the census suite pinned four hours earlier, closed rather than documented. Metro serves the `.web` spelling to GitHub Pages, and its `drag` was `() => {}` with `isActive` hardcoded `false`, so `onDragEnd` could not fire: the "reorder items" toggle on a collection and the long-press on an owned collection card were both dead controls on the only platform this app is deployed to.

  **The shim runs the gesture itself.** A row's `drag()` attaches `pointermove` / `pointerup` / `pointercancel` to `document`; each move re-measures the rows through refs on a wrapper `View` and remembers which index the cursor is over; the release hands `onDragEnd` the reordered array. It does not animate and it does not shuffle rows under the cursor — the dragged row dims, and the list re-renders once from the persisted order after the drop.

  **The two decisions moved out to `lib/drag-reorder.ts`** — `targetIndexForPointer` (which row a pointer is over) and `moveItem` (what the array becomes). Both are total: an unmeasurable list, a pointer that is `NaN`, a fractional index and a `from` past the end all return something usable, because every one of them is reachable at the end of a gesture the user has already finished. The midpoint rule is the swap point, not the row boundary, so a row does not jump the moment the cursor leaves it.

  **`measureRows` is all-or-nothing on purpose.** `targetIndexForPointer` walks its array by index, so a windowed list missing its third row would rank the fourth as the third and drop the row one place short. A missing ref means no measurement, which means no commit.

  **Nothing regresses where there is no DOM.** `draggableDocument()` returns null and `drag()` is the no-op it was — including, importantly, not leaving a row marked active with no way to clear it.

  **Ten new cases drive the real wiring**, not a source scan: `setupFakeDom`'s listener registry fires the pointer events, and React 19's `ref`-as-a-prop lets a case hand a row a stand-in whose `getBoundingClientRect` returns the geometry it wants. Committed drops, drops that must not commit (never moved, never measured), the active-row style, listeners removed on release and on unmount, and a second long press replacing the gesture instead of stacking one. Fifteen more cover the arithmetic, including a monotonicity sweep over the whole list height.

  `npm run verify` green: typecheck, `lint:all`, 7840 tests (+25), audit baseline, build, bundle secrets, bundle size (+1.8 KiB, 16.1 KiB headroom left), bundle smoke, ships-to-client. No DB migration, no creds.

## The last census exemption was the file the browser actually runs (2026-09-10)

<!-- First bullet of "Optimistic-write follow-ups" (2026-09-06): "One census exemption left, and it is the file the deployed build actually serves — `components/DraggableList.web.tsx` adapts the library's `renderItem` params and stubs `drag`, and `helpers/render.ts` mounts components." -->

- [DONE: 2026-09-10] [est: 11000] **`components/DraggableList.web.tsx` has a suite, and the census has no exemption list left** — the sixth and last entry to close, and the one that ships: Metro picks the `.web` spelling for the bundle GitHub Pages serves, so the shim is the list adapter every browser user runs while the native re-export beside it is the half nobody does.

  **Closing one closed both, and not by choice.** The census counts a module as named when a suite contains its path without the extension, and `components/DraggableList.web` CONTAINS `components/DraggableList` — there is no way to name the shim that does not also name its twin. So `draggable-list-web.test.ts` answers for both: three parity cases hold the native file to still being nothing but re-exports of `react-native-draggable-flatlist`, and to publishing the same value exports as the web spelling. That is the claim the twin's exemption comment made ("a re-export of the native list library") with nothing checking it.

  **`UNNAMED` is gone rather than empty** — `assertExemptionsHonest` refuses a list with no entries ("delete it instead"), so the six-entry history and the terms for re-adding a list are prose in the header now, and the first case simply asserts no module in `app`/`components`/`data`/`lib` is unnamed.

  **Fifteen cases, mounted rather than scanned.** The two shims are exercised through the same loop: forwarded props, `onDragEnd` and `activationDistance` absent from the `FlatList` (absence of the KEY, since `{...rest}` with an undefined value still reaches the DOM through react-native-web), `renderItem` adapted to `{item, drag, isActive, getIndex}`, `getIndex()` answering per row rather than from a capture, and `renderItem` left undefined when none was given. Plus the two shims having separate component identities, `NestableScrollContainer` being RN's `ScrollView` by reference, and `ScaleDecorator` rendering its child with no wrapper node of its own.

  **The gap the shim has is now pinned, not folklore.** `drag` is a no-op and `isActive` is always `false`, so `onDragEnd` can never fire and drag-to-reorder does nothing in the web build — which is what `app/collection/[id].tsx`'s reorder mode and `app/index.tsx`'s owned-collections list offer there. The case that asserts the shim drops `onDragEnd` is the case that goes red the day somebody makes web dragging work.

  `npm run verify` green: typecheck, `lint:all`, 7815 tests (+15), audit baseline, build, bundle secrets, bundle size (17.9 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The reaction the server refused and the screen kept (2026-09-06)

<!-- First bullet of "Visibility-poll follow-ups" (2026-09-06): "Two census exemptions left, and both are the reachable half of a pair — `components/DraggableList.web.tsx` is what GitHub Pages actually serves, and `lib/use-reactions.ts` is a hook over `supabase-profiles` that needs the mock shapes `supabase-profiles-shapes.ts` already describes." -->

- [DONE: 2026-09-06] [est: 13000] **`useReactions` has a suite, and the optimistic write now rolls back** — the fourth census entry to close and the second in a row that was covering for a bug. A tap adds the reaction to local state and then tells the cloud; both writes ended `.catch(() => {})`, so a rejected insert left the emoji on screen, counted, and marked `mine`. The UI stated a fact the server had refused and nothing corrected it until a remount. The removal direction lost a reaction the same way.

  **The catch is where the rollback lives now**, not where the failure is dropped: a refused insert filters the optimistic row back out, and a refused delete puts the original back — guarded on it not having returned already, because a refetch may have landed while the write was in flight.

  **The second bug is smaller and older.** An empty `targetId` returned from the effect before `setLoading(false)`, and `components/reaction-bar.tsx` renders nothing but its spinner while `loading` is true — so a bar mounted on the first pass of a detail screen, before the route param resolves, spun for the life of the mount.

  **No new harness.** A probe component that calls the hook and renders `null`, `settle()` for the fetch, and `mockModule` for `@/lib/auth-context` and `@/lib/supabase-profiles` — the same three pieces the last two entries used.

  **What is read from the state and what from the mocks is a decision, not a habit.** The counts, `mine` and `loading` come off the returned value because that is what the bar renders from; the calls come off the mocks because "optimistic" is a claim about ORDER, and only a mock that holds its promise open can be asked whether the state changed first.

  **Twelve cases**, and the three that name the bugs were red before the fix and green after: the refused insert, the refused delete, and the spinner with no target.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7785 tests (+12), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The poll that outlived every tree that started it (2026-09-06)

<!-- First bullet of "Mounted-provider follow-ups" (2026-09-06): "Three census exemptions left and all three are components or hooks, not providers — `components/DraggableList.web.tsx`, `lib/use-reactions.ts`, `lib/use-visibility-refresh.ts`. `providerHarness` does not fit any of them: two are hooks with no provider to wrap, and nothing in `helpers/` mounts a bare hook today." -->

- [DONE: 2026-09-06] [est: 14000] **`useVisibilityRefresh` has a suite, and it was hiding a leak** — the third census entry to close and the first that was covering for a bug. The web branch with no `document` returned `() => {}` as its cleanup, having started an interval two lines above. Nothing cleared it: the tree unmounted, `refreshFn` kept firing every `intervalMs` for the life of the process, and every closure it captured stayed reachable — in a hook whose entire purpose is to stop polling when nobody is looking.

  **The path is not hypothetical.** `Platform.OS === "web"` with no `document` is a web bundle evaluated outside a browser — the static export `scripts/build-spa-fallback.ts` produces, any prerender pass — and it is also the environment the suites run in, which is why the case costs nothing once the module can be loaded at all. The fix is `return pause`, the function the other branches already use.

  **No new helper was needed to mount a bare hook** — a probe component that calls it and renders `null` is the harness, and `render()` has had `unmount()` since the leak family landed. What was missing was a DOM that could be sent an event: `helpers/fake-dom.ts` grew `hidden`, `addEventListener`/`removeEventListener` and `setHidden`, which flips the flag BEFORE dispatching because that is the only ordering a browser produces. Four cases in `fake-dom.test.ts` pin the new surface, including a handler that removes itself mid-dispatch — which is exactly what an effect cleanup running inside a handler does.

  **`react-native` is mocked rather than stubbed**, because the stub pins `Platform.OS` to `"web"` and this hook has two branches. A mutable `Platform` object makes the native half reachable in the same process, and the `AppState` mock records its listener and whether `remove()` was called, so "removes the subscription on unmount" is a fact rather than an absence.

  **Eleven cases over four properties**: the immediate refresh on mount, the interval, the ref that keeps the LATEST callback without restarting anything, and the two pause/resume protocols. A redundant `visibilitychange` must not stack a second interval; iOS's transient `inactive` counts as away; native registers no DOM listener even when a document is present.

  **Proved against four broken trees**: the empty cleanup (the bug itself, one case red before the fix), the `resume()` guard removed, `inactive` treated as active, and the mount refresh deleted (ten red).

  `npm run verify` green: typecheck, `lint:all` 19/19, 7773 tests (+15), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The provider that needed no mocks at all (2026-09-06)

<!-- First bullet of "Deep-link follow-ups" (2026-09-06): "Five modules are left in the census exemption list and four of them are one harness away — `helpers/render.ts` mounts a component, `helpers/mount-provider.ts` a provider, and `lib/nav-animation-context.tsx` is a provider with a hook and 34 lines. It is the same 'smallest of the remaining' argument that picked `lib/deep-link.ts`, one step down." -->

- [DONE: 2026-09-06] [est: 8000] **`NavAnimationProvider` is mounted in a suite, and the census is down to three** — its exemption comment said "a provider; needs the mount harness" and that was the entire cost: this provider imports nothing but React, so the file contains no `mockModule` call. It is the only one of the app's providers with that property, which is what made it next.

  **What it decides is written by one subtree and read by another on the same pass.** `components/bottom-nav.tsx` calls `setAnimation("slide_from_left")` before navigating left along the tab bar; `app/_layout.tsx` reads `animation` into the `Stack` options. A value that does not propagate is a screen sliding in from the wrong side with nothing thrown, which is why the propagation case mounts rather than reads the source.

  **Two of the three properties are identities, and neither is the state.** `setAnimation` is `useCallback`'d with an empty dependency list because `bottom-nav` closes over it; the context value is `useMemo`'d because every consumer re-renders when it changes. Both exist for the CALLERS, and both are asserted from outside — by comparing the object a probe received across two passes — rather than by matching `/useMemo</` against the source the way `use-analytics-hook.test.ts` does, which holds until somebody writes the memo with the wrong dependency list.

  **The memo needs both directions or it is half a claim.** Stable when nothing changed, and NEW when the direction did: a memo with an empty dependency list passes the first and pins every consumer to the direction the app started with.

  **Proved against three broken trees**: `useMemo` deps emptied (three cases red), the setter's deps widened to `[animation]` (one), and the memo removed entirely (one).

  `npm run verify` green: typecheck, `lint:all` 19/19, 7758 tests (+7), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The census's first finding was product code nobody had ever loaded (2026-09-06)

<!-- Second bullet of "Module-census follow-ups" (2026-09-06): "`lib/deep-link.ts` is thirteen lines and the smallest of the six — one exported function, one branch on `getAppBaseUrl()`, one `expo-linking` import that a module mock covers. It is live product code reached from an item screen and a share sheet, and closing it is the census's own first finding rather than a suggestion about the census." -->

- [DONE: 2026-09-06] [est: 9000] **`buildDeepLink` runs in a suite now, and the exemption that named it is gone** — every share surface in the app builds its URL here, and the module had never been imported by anything but the app. It was the sixth entry in the census landed an hour earlier, its comment said "needs a module mock", and that is exactly what closing it took: two `mockModule` calls and the stub installer that has been in `helpers/render.ts` since it landed.

  **The seams are mocked and the subject is not.** `getAppBaseUrl` is behind a mock because the value it answers is a module-scope `const` read from `process.env` at import time — one value per process — and both branches are the point. What is real is what the module does: strip the caller's leading slashes, branch, join.

  **The strip is not cosmetic and the two branches would have broken it in opposite directions** — `${base}//item/1` on web, `collectables:///item/1` from `createURL` on native, an empty authority and a link that resolves to nothing. Callers pass both spellings, so a case asserts the three of them agree.

  **The join's correctness rests on a fact two modules away.** `${base}/${clean}` is only right because nothing upstream can hand it a trailing slash, and that is `lib/url-helpers.ts`'s guarantee. The last case asks the REAL helpers for it, over both ways a base is produced — a configured URL with trailing slashes stripped, and an origin plus the first path segment, which is the GitHub Pages shape this app deploys to.

  **`import { Platform } from "react-native"` was dead** — imported, never read, and the only thing making the module load `react-native` directly. Removed.

  **Proved against two broken trees**: `const clean = path` (two cases red) and `createURL(path)` for `createURL(clean)` (one).

  `npm run verify` green: typecheck, `lint:all` 19/19, 7751 tests (+5), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The list of untested modules was a script somebody ran once (2026-09-06)

<!-- First bullet of "Unreached-module follow-ups" (2026-09-05): "Nothing in the tree measures that list — it was produced by a one-off script in a session, the way the `if`-hole census was before it became a case. A walk that named the modules no suite mentions would turn a sixth into a red run rather than a discovery." -->

- [DONE: 2026-09-06] [est: 13000] **The census is a case now, and it found the sixth while being written** — "five modules are still mentioned by no suite" went into `.suggestions.md` as a measurement with nothing behind it: a script run inside one session, its output pasted into prose. The bullet beside it said so. `__tests__/suite-named-modules.test.ts` is that walk, kept.

  **The list was already wrong.** `lib/deep-link.ts` is the sixth — `buildDeepLink`, imported by `app/item/[id].tsx` and `components/collection-share-sheet.tsx`, live product code with no suite. The one-off matched on a bare word, and four suites use "deep-link" in prose about share links, so it read as covered.

  **The rule is "named", not "covered", and the weak half is the point.** A suite naming a module is the floor below which no coverage claim can be true; being swept by `plural.test.ts` is not being tested. What a red run says is narrower and still worth a failure — nobody has ever imported this module, read its source, or walked past it with its name in hand.

  **Two spellings, because the tree writes a module reference two ways.** `@/lib/use-now` in an import, and `readRepoFile("lib", "supabase-cloudinary.ts")` where the path is split across arguments. The first spelling alone reported `lib/supabase-cloudinary.ts`, `lib/supabase-data-export.ts` and `lib/use-debounced-value.ts` as unnamed, and all three have suites named after them.

  **`scripts/` is out of the walk on purpose** — a guard wrapper is reached through `LINT_GUARDS`, not by anybody typing its path, so nine of fifteen would be offenders and they are the most heavily checked files here. **Comments are stripped** because a mention in a paragraph is the mention this rule must not accept, and **the file excludes itself** for the reason `header-prose-names.test.ts` does: an inventory of absent names, written as string keys, is code that resolves its own exemptions.

  **The loose half is measured rather than argued about.** A file-name match answers for two modules that share a basename; `[id].tsx` is the only repeated one and all five are reached by their stems, so a case pins the hole empty instead of a sentence claiming it.

  **Proved in both directions**: an unnamed `lib/` module turns the census red naming it, and a planted import of `@/lib/deep-link` turns the exemption case red for an entry that no longer needs its hole.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7746 tests (+4), audit baseline, build, bundle secrets, bundle size (18.0 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The comparison that reads like a seventh spelling and is a different question (2026-09-05)

<!-- Second bullet of "Spelling-shape follow-ups": "`!==\s*0` is the operator-shaped seventh spelling and it is not swept for — the case proves a comparison propagates through the wrappers, and `!== 0` is a real inflection shape ('no items' against 'one item') that the six alternatives do not carry. Nothing states whether its absence from the rule is a decision or an omission." -->

- [DONE: 2026-09-05] [est: 3000] **A comparison against zero is a disclaimed near miss with a reason, not a gap** — the previous entry used `!==\s*0` as an operator-shaped seventh spelling precisely because it is not one of the six, and left the interesting half unstated: whether it is absent by decision. It is, and the reason is the one this module was written around.

  **`n !== 0 ? many : none` picks a sentence for an EMPTY collection**, and the form on the other side of that branch is still the plural. "0 items" is the number `plural` is `=== 1` rather than `< 2` for — the case three hundred lines up exists for exactly it. Nothing is agreed, so there is nothing for the rule to own; a screen that wants "no items" asks `t()` for a second key.

  **Both spellings are in the disclaim list** — `n !== 0 ? many : none` and `count === 0 ? empty : some` — so the sweep is watched refusing them rather than assumed to, and the argument sits where a reader meets the rule instead of in `.tasks/`.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7735 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The PDF export had no suite because nothing could load it (2026-09-05)

<!-- Not a `.suggestions.md` bullet: the loop's own step 7 ("scan the codebase for related bugs"), pointed at product code because every open bullet at the top of the file is being worked by another session. Measured first: six modules in `lib/` and `components/` are mentioned by no suite in the tree, all of them live in screens. -->

- [DONE: 2026-09-05] [est: 11000] **The export document is a module a test can reach now, and it has one** — `lib/export-pdf.ts` imports `expo-print`, `expo-sharing` and `react-native`, so no node suite could require it, and the half worth checking was never the printing. It was the document: HTML escaping of user text, the header totals, and which fields an item contributes. None of it had ever been run by a case, and the reason was reachability rather than anybody deciding a print document does not need checking.

  **The split is the one this repository already argues for twice.** `lib/powerbi-template.ts` says it "mirrors the lib/spa-fallback.ts + scripts/build-spa-fallback.ts split so the .pbit assembly is unit-testable in CI"; `lib/export-pdf-html.ts` is the same move. What is left in the wrapper is the print engine, the share sheet and the web popup.

  **The clock came out of the builder.** The footer called `new Date()` inside, so the output differed on every call and could not be asserted against a fixed string. It is a parameter with today's date as its default, and a case pins that two calls agree.

  **What the escaping cases assert is the property, not the four replacements** — that user text cannot end an attribute or open a tag. A photo URL is the only user string that reaches an attribute, and `x.png" onerror="alert(1)` is the case that says so. `'` is deliberately not escaped and the module says why: every attribute in the template is double-quoted.

  **The inline-hex exemption moved with the CSS.** `check-inline-hex.ts` named `lib/export-pdf.ts` for its standalone-document palette; the palette is in the new module and so is the exemption, with the wrapper left clean.

  **Proved against three broken trees**: dropping the `"` replacement, `if (item.cost)` for `typeof item.cost === "number"` (which loses a recorded zero), and putting `new Date()` back inside the builder. One case each.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7742 tests (+7), audit baseline, build, bundle secrets, bundle size (18.0 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A seventh spelling that is shaped like the six (2026-09-05)

<!-- Third bullet of "Fragment-reader follow-ups": "`\bunity\b` is the seventh spelling and it is a word, where all six real alternatives are operators — the case proves the plumbing carries an alternative, not that it carries a COMPARISON. A widening the fragment could not survive (an unescaped `(`, a stray `|`) would fail differently and is untested." -->

- [DONE: 2026-09-05] [est: 4000] **The propagation proof runs a comparison as well as a word** — the widened-fragment case used `\bunity\b`, which is a word, and every real alternative is built out of `!`, `=`, `<`, `>` and `\s*`. A word exercises none of that: it proves the plumbing carries an ALTERNATIVE, which is a weaker claim than the one the case is named for.

  **`!==\s*0` is the shape a seventh spelling would actually be written in** — an escape, two metacharacters and a quantifier — and it is not one of the six, so the disclaim half stays honest: the un-widened readers must still refuse `n !== 0 ? one : many`.

  **Both spellings, both directions, three readers.** The offenders are built per reader from the token rather than typed per reader, so a third alternative costs one entry instead of three strings.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7735 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

  **One bullet filed an entry ago is withdrawn rather than implemented**: "three scans read `plural.test.ts` by its own filename" measured wrong — there is ONE `suiteCode` call feeding both scans, and `readSuite` throws on a missing file rather than returning an empty string, so a rename is a red run and not a silent pass.

## The census read a naming convention as though it were a type (2026-09-05)

<!-- First bullet of "Self-reading-count follow-ups": "The declaration count keys on the NAME suffix `Reader` and nothing enforces it — a fourth builder called `switchPattern` or `countRule` is declared, used, and invisible to the census, which reads a naming convention as though it were a type. The three that exist agree by having been written together." -->

- [DONE: 2026-09-05] [est: 4000] **The reader population is counted twice, once by name and once by what makes a function a reader** — the entry below this one replaced a literal `3` with a scan for `const \w+Reader = `, which closed the hole it was about and opened a narrower one in the same line: the scan reads a NAMING CONVENTION. A fourth builder called `switchPattern` takes the alternation, is a reader of it, and is invisible to a pattern looking for the suffix. The three that exist agree because they were written in one commit, which is not a reason.

  **The parameter is the thing that makes a function a reader**, so `\(comparison: string\)` is counted too and the two counts have to agree. A builder named off the convention shows up on one side; a `…Reader` that is not built from the fragment shows up on the other. Neither pattern can match its own source — the parentheses are escaped in one and bare in a declaration, and `\w` cannot read the backslash after `const ` in the other.

  **Proved in the failing direction**: `switchPattern` declared beside the three turns the case red with both numbers in the message — 4 taking the alternation, 3 named for it — where the name-only count was green.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7735 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The population was a number typed beside the list it counted (2026-09-05)

<!-- First bullet of "Fragment-reader follow-ups": "The reader population is a literal `3` in an assert beside a three-entry array — the array's own length is compared against a number typed next to it, which is a tautology that only stops being one because a human has to edit the number. Nothing relates the count to the rules the file actually builds, so a fourth reader added WITHOUT a fourth array entry is invisible." -->

- [DONE: 2026-09-05] [est: 4000] **The reader count is read out of the file rather than compared against a literal** — the case that hands a widened alternation to all three readers guarded its own population with `assert.equal(readers.length, 3)`, three lines under a three-entry array. That holds only while somebody remembers to edit the number, which is "a list in prose is a list that goes stale" written as an assert — the thing this suite refuses one describe up, where the caller list is walked instead of read.

  **What it counts now is the declarations.** `const \w+Reader = ` over the suite's own comment-stripped source, compared against the array the case iterates. A builder declared and left out of the array — the exact shape a fourth reader with the alternation inlined would take — is red with both numbers in the message.

  **The pattern cannot match its own source**, which is the hazard every self-reading scan in this tree has met: after `const ` the regex literal has a backslash, and `\w` does not read one. No exemption, no path filter, nothing to keep in step.

  **Proved in the failing direction**: a `switchReader` declared beside the other three and left out of the array turns the case red, where under the literal `3` it was green.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7735 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The fragment's own claim, asked instead of asserted (2026-09-05)

<!-- Second bullet of "App-tree sweep follow-ups": "`COUNT_COMPARISON` has three readers now and no case proves an edit reaches any of them — the ternary rule, the `if` rule and the app census are all built from the fragment, and the only proof that a seventh spelling propagates is a probe run by hand and written into `.tasks/`." -->

- [DONE: 2026-09-05] [est: 6000] **A seventh spelling is handed to all three readers in one case** — the alternation was extracted so one edit would reach every reader of it, and the proof was a `SEVENTH` alternative dropped into the fragment by hand, both readers asked, the result written into an entry. That is the arrangement four entries closed for `network-refusal.test.ts` and one closed for the rule's own six alternatives, owed here and cheaper than in any of them: the readers are three lines and the offender is a string.

  **The three wrappers are functions of the alternation now, not of the constant.** `ternaryReader`, `ifReader` and `censusReader` each take the fragment and wrap it; the three rules are one call apiece. A `RegExp` built from a constant keeps no link back to it, so nothing could hand the readers a WIDENED alternation until they took one.

  **The seventh spelling is `\bunity\b`**, which no comparison operator can spell and no file in this tree contains. Each reader must read it in the shape that reader wraps — a ternary, an `if` head, a ternary with its operand — and the REAL reader must refuse the same string, which is the half that says the widening did it rather than something already in the pattern.

  **The way the claim actually goes wrong is a FOURTH reader with the six comparisons inlined**, which passes every case in the file because every case in the file is about the three that exist. So the population is asserted too, and each live rule's `source` is checked to carry the fragment verbatim — a reader that quietly stops being built from it is red rather than silent.

  **Proved in the failing direction**: `censusReader` rewritten to ignore its parameter and inline the alternation turns the case red by name, while every other case in the suite stays green — which is exactly why the proof had to be in the tree.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7735 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The half of the tree the grammar ships from, never swept (2026-09-05)

<!-- Fifth bullet of "Hole-measurement follow-ups": "The walk is `lib`, `scripts` and `__tests__/helpers`, and the app renders every count through `t()` — the hole was measured in three directories out of a tree where the plural rule matters most in the sixth one. `app/` has never been swept for either shape." -->

- [DONE: 2026-09-05] [est: 7000] **Both plural sweeps now read `app/`, `components/` and `data/`** — the ternary rule and the `if`-shaped hole were each measured over `lib/`, `scripts/` and `__tests__/helpers/`, which is where the six copies were found. `lib/plural.ts` opens with fifty lines about the six locales and the sixteen count-bearing keys, and nothing had ever read the sixty-nine files those keys are rendered in. The tooling was swept twice; the half the grammar ships from, not once.

  **It is clean, and structurally rather than by luck.** A screen holds a KEY, not a word: every count-bearing string comes out of `t()`, and the agreement happens behind it in `lib/i18n-context.tsx`. Four expressions in the app tree compare a count against one, in eight places, and not one inflects anything — a pager (`item.photos.length > 1`), a disabled "previous" button (`page <= 1`), an owner filter (`owners.length > 1`), and one that picks which `t()` key to ask for (`photoCount > 1`).

  **The near miss is the interesting one and it is named rather than exempted.** `components/item-card.tsx` does choose between two strings by a count — both of them out of the translation map, so what it picks is which SENTENCE to say, not which form of a word. That is the distinction the pattern cannot make and a reader can.

  **Pinned by EXPRESSION, not by path, which is where this sweep is sharper than the two above it.** `item-card` is the likeliest file in the tree to grow a real inflection — it already compares a photo count against one four times — and a sanctioned PATH would have swallowed the next one silently. Proved: `n === 1 ? "photo" : "photos"` planted in that file is red under the census and would have been GREEN under the path form, which is how the first draft of this case was written. The `if` half uses `assertNoOffenders` rather than an empty `expected`, because "nobody does this" is the claim and a sanctioned list of none reads as an oversight.

  **What it still cannot see is written down**: a fifth ternary on an operand already in the list, since repeats are collapsed on purpose — the same render condition written twice is not a second copy of the rule. The branches would say which it is, and the rule's own comment refuses to read them, because a copy that picks between two variables is the one worth catching most.

  **Built from `COUNT_COMPARISON` rather than from a third copy of the six comparisons**, which is the entry below this one landing an hour earlier and earning its keep immediately: the census is the fragment's third reader, and a seventh spelling now reaches all three from one edit.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7734 tests (+2), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Two rules, one alternation (2026-09-05)

<!-- Second bullet of "Hole-measurement follow-ups": "The two rules read the same six comparisons and are written twice — the ternary pattern and the `if` pattern share an alternation and differ in what wraps it. A third shape (a `switch`, an `&&` chain) would be a third copy of the same six." -->

- [DONE: 2026-09-05] [est: 6000] **The six comparisons are a fragment both rules are built from** — they were written out twice, once inside the ternary pattern and once inside the `if` pattern, which is two places to add a seventh spelling and one of them to forget. Two entries ago this file was about a rule read off the offenders in front of it; a rule written twice is the same failure with a shorter fuse.

  **What differs between the two readers is only what wraps the alternation**, and that is now all each one spells: `${COUNT_COMPARISON}\s*\?` and `if\s*\([^)]*${COUNT_COMPARISON}\s*\)`.

  **Proved by adding a seventh spelling in the one place and asking both readers.** A `SEVENTH` alternative put into the fragment alone is matched by the ternary reader and by the `if` reader, which is the property the duplicate could not offer.

  A third shape — a `switch`, an `&&` chain — now costs one alternative rather than three copies.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7732 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The if-shaped hole, measured instead of described (2026-09-05)

<!-- Third bullet of "Rule-proof follow-ups": "The disclaim list pins `if (n === 1)` as a hole and nothing measures how big it is — no walk counts two-line `if` inflections in the tree, so 'the sweep cannot see them' is stated and the number is unknown. A count would say whether the hole is theoretical." -->

- [DONE: 2026-09-05] [est: 8000] **Three modules write an `if` comparing a count against 1 or 2, and none of them is an inflection** — the entry above pinned `if (n === 1) { return one; } return many;` as the shape a single-line rule cannot read, and left the size of the hole unstated. It is empty: `list.length < 2` (a group needs two rows to be a duplicate), `value.length <= 1` (a trailing separator cannot be trimmed off an empty string), `starts.length > 1` (a marker that no longer identifies one section).

  **Which is also why widening the rule would be wrong.** A condition-only pattern over `if` fails those three and puts their names in an exemption list — the arrangement the rule's own comment refuses, arrived at from the other direction.

  **So the lookalikes are pinned rather than the rule widened.** A real two-line inflection now arrives as an unsanctioned module WITH ITS PATH, and whoever writes one has to decide what the sweep should do rather than never hearing about it.

  **Proved in both directions.** A `claimWord` helper with a two-line `if`, planted in the module the ternary was removed from, is reported as unsanctioned by path; rewriting `guard-root`'s bounds check so it no longer compares against 1 reports it as a sanctioned entry that no longer matches — the half an exempt list cannot see.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7732 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The proof that the rule catches six spellings was a paragraph (2026-09-05)

<!-- Third bullet of "Spelling-breadth follow-ups": "Four broken trees were run by hand and none of them is in the tree — the proof that each spelling is caught lives in a `.tasks/` entry, which is exactly the arrangement `network-refusal.test.ts` spent four entries closing for its own rules." -->

- [DONE: 2026-09-05] [est: 8000] **The sweep's rule is asserted against every spelling it claims and the near misses it disclaims** — the widening was proved on four broken trees planted by hand, once, in a module that had just had the rule taken out of it, and the proof was a paragraph in `.tasks/`. A rule nobody has watched refuse anything reads as passing whether it works or not; that argument cost `network-refusal.test.ts` four entries, and here the offenders are strings, so it costs one case.

  **Both halves, because a rule that matched everything would pass the first loop.** Six spellings plus an unspaced one (`count===1?one:many`, since the sweep reads whatever the formatter left) must match; `=== 2`, `> 2`, a bounds check and a `for` header must not.

  **The known hole is pinned rather than implied.** `if (n === 1) { return one; } return many;` says the same thing across two lines and no single-line pattern reads it. It sits in the disclaim list, so a reader meets the limitation where the rule is, not only in `.tasks/`.

  **Proved in both directions, and the interesting part is what stayed green.** Narrowing the rule back to `=== 1 ?` and over-widening it to any comparison against 1 or 2 each turn the new case red — while the sweep itself passes both times, which is exactly why the proof had to be in the tree.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7731 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The third producer of annotations, and the one nothing had ever run (2026-09-05)

<!-- Fifth bullet of "Classifier-caller follow-ups", filed the same day: `scripts/check-comment-terminators.ts` built annotations and no suite had ever asked the classifier about them. -->

- [DONE: 2026-09-05] [est: 7000] **`earlyTerminatorAnnotations` and `orphanTerminatorAnnotations` moved into `lib/check-comment-terminators.ts`** — both were private functions in the script, which is the shape every other gate's decisions moved OUT of: a function only a process can reach is checkable by reading the file for an exact expression and no other way, which is a check on the spelling of the code rather than on what it produces. These two had never been run at all. This was the third module in the tree emitting workflow commands and the only one whose output had never been past `isAnnotationLine`, so nothing said its marks would reach a run summary.

  **It is the guard whose annotation matters most.** The compiler's own errors for a comment that ends inside its own body land on the lines BELOW the real one, so a reviewer reading the diff sees complaints about code nobody wrote and nothing at all on the paragraph that caused them. The mark is the only thing that points at the prose, and until now it was the only part of this check with no case behind it.

  **Five cases, on the properties the script could not be asked about**: one mark per finding with the finding's own file, line and column; both messages carrying their own advice sentence and not the other's, which is the reason there are two builders rather than one; a path carrying `:` and `,` escaped, because an unescaped separator truncates a command rather than failing it; and the classifier agreeing these are marks while the human-readable reports printed beside them are not.

## The second suite that reads annotations, and a level that could not be emitted (2026-09-05)

<!-- Second and fourth bullets of "Line-classification follow-ups": `check-inline-hex.test.ts` matched the annotation format by hand and did not switch to the classifier, and nothing checked that a fourth `ANNOTATION_LEVELS` entry would be reachable. -->

- [DONE: 2026-09-05] [est: 5000] **`check-inline-hex.test.ts` asks the classifier too, and the levels are held to a shape the format can carry** — `isAnnotationLine` was written so `audit-baseline.test.ts` would stop telling marks from log lines with `startsWith("::")`, and the other suite that reads annotation output kept matching `/^::error file=…/` by hand. That was half a migration, and the reason it looked like an oversight is that the difference was real and unwritten: the hex suite asserts the FORMAT — where the level goes, which properties follow, how `%`, `:` and `,` are escaped — which no classifier can check.

  **Both halves are asked now, and the distinction is written down.** A case runs the classifier over the annotations this check emits, including the path carrying every property metacharacter, and — anti-vacuously — over the human-readable report printed beside them, which is the line marks have to be told apart from. A change to the prefix is one fact going red in two suites instead of two unrelated failures.

  **`ANNOTATION_LEVELS` is checked for a shape it can actually be emitted in.** The round trip is driven off the array, so a fourth level is covered the moment it is declared — and nothing covered whether `annotation` could emit it: the level is the one part of a workflow command that is never escaped, so an entry with a space produces `::a level::…` that the classifier reads as prose, one with a colon closes the command early, and one with `%` collides with an escape. Every one of those is a mark that silently vanishes from the run summary rather than a red run, which is the failure this module was split out to prevent.

## Four constructors, and no `kind` written by hand anywhere else (2026-09-05)

<!-- Third bullet of "Gate-constructor follow-ups", and the `answerRead` half the "Union-naming follow-ups" entry left open as "defensible and unstated". -->

- [DONE: 2026-09-05] [est: 8000] **`answerRead`, `isAnsweredRead` and `isSkippedRead` finish the pattern on `AuditRead`** — the entry above gave `AuditGateRun` constructors and guards, and left the union that taught it how to discriminate with neither. A day earlier the missing `answerRead` was filed as "defensible, because a constructor would have one caller, and nothing states it". The argument that produced `skipRead` does not count call sites, and `checkedGate` had just been written for two constructions on exactly that ground, so the asymmetry was one sentence away from being a rule nobody could restate.

  **What one caller buys is that the module has no hand-written discriminator left.** `kind` is now written in six places in `lib/audit-baseline.ts`: two interface pairs declaring it, and four `return`s. A branch that produces an answer where it meant a skip has to go out of its way to do it.

  **And that is measured rather than argued.** A case reads the module comment-stripped — its headers quote the shapes they replaced, so a scan that read prose as code would report `skippedGate`'s own doc comment — counts the object literals opening with `kind:`, and asserts the four are the four. A fifth construction displacing one of these keeps the count and changes the answer, which is why both halves are checked.

  **The guards are driven against the field, over both halves and every declared cause**, with `notEqual(isAnsweredRead, isSkippedRead)` so a third kind answering `false` to both is red rather than quiet; and the narrowing is proved by assignment to `AuditAnswer` and `AuditSkip`, which stops compiling if a guard stops narrowing. `github-annotations.test.ts` built two skips by hand and builds them with `skipRead` now — it was the last file outside the module spelling a discriminator for itself.

## The sweep was reading the spelling in front of it (2026-09-05)

<!-- First bullet of "One-rule follow-ups": "The sweep reads `=== 1 ?` and that is one spelling of the rule — `!== 1 ? many : one`, `< 2`, `length > 1` and a `switch` all say the same thing and none is caught. What it really guards is the spelling that six modules happened to use, which is the shape three entries in `.tasks/` are about." -->

- [DONE: 2026-09-05] [est: 8000] **The one-door sweep matches six spellings of the rule, not the one its offenders used** — it was written from six modules that all happened to write `=== 1 ?`, so a seventh copy spelled `!== 1 ? many : one`, `> 1 ?`, `<= 1 ?`, `< 2 ?` or `>= 2 ?` said the same thing and passed. A rule derived from the offenders in front of it is the shape three entries this week are about, and this one was a day old.

  **The CONDITION is matched, not the branches.** A copy that picks between two variables — `count === 1 ? one : many`, which is exactly what `audit-baseline` had — is the one worth catching most, and a rule keyed on a string literal after the `?` would have missed it while looking tighter.

  **`=== 2` and `> 2` are deliberately absent**, and the comment says so: widening to "any comparison against a small number" sweeps in bounds checks and makes the exemption list the thing that carries the meaning.

  **Proved on four broken trees, one per new spelling**, each planted in `lib/ships-to-client.ts` where the rule had just been removed: all four caught, and all four passed the old pattern.

  No false positives to absorb — the wider rule matches exactly the two modules the narrow one did.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7718 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The last three copies of the one-versus-many rule, and the sweep that says so (2026-09-05)

<!-- PR #528, merged. Two of its five modules had already been converted on main by a later session; these three had not, and the sweep that would have caught them was in the branch. -->

- [DONE: 2026-09-05] [est: 6000] **`ships-to-client`, `provenance-tables` and `oldest-record` are settled, and `plural.test.ts` sweeps for the shape** — `lib/plural.ts` has agreed a word with a number in six languages since the entry that found sixteen ungrammatical keys, and five modules wrote the English half out again anyway. `audit-baseline` and `coverage-floor` were converted on main; the other three were only ever fixed on a branch, so the tree carried five inline ternaries with the rule sitting one import away.

  **`provenance-tables` is the copy worth naming**: it agrees a VERB with a count — `declare`/`declares`, and `is`/`are` beside `stop`/`stops` in the same sentence — which is what shows the rule is about agreement rather than about nouns. A CI message is not a different rule from a UI string: exactly one takes the singular, and zero does not.

  **`lib/oldest-record.ts` keeps its ternary, by name.** It is a documented LEAF — no imports, so any evaluator may reach for it and it can never close a cycle — and `oldest-record.test.ts` asserts that. Two words are not worth trading it for, so the sweep sanctions the module explicitly rather than letting it read as one nobody noticed.

  **A sweep, so the sixth copy is a red run rather than a discovery.** `plural.test.ts` walks `lib/`, `scripts/` and `__tests__/helpers/` for `=== 1 ?` and names the two modules allowed to carry it, read comment-stripped so `i18n-source.ts` — which quotes the shape in prose as the example its scanner cannot see — is not an offender. The doc comment's caller list went from three names to five, which is the direction the same suite already checked.

## A type guard written as an expression, and a colour spelled beside the verdict (2026-09-05)

<!-- First, second and fifth bullets of "Narrowed-run follow-ups": the suite narrowed with inline arrows, no union in the module shipped a guard, and the gate built both of its halves by hand. -->

- [DONE: 2026-09-05] [est: 9000] **`isCheckedRun` and `isSkippedRun` are the narrowing, and `skippedGate`/`checkedGate` are the construction** — the entries above named both halves of `AuditGateRun` so a caller could say what it was holding, and left the caller nothing to hold it with. The suite narrowed by wrapping each run in `((run: AuditGateRun) => run.kind === "checked" ? run : undefined)(…)`, twice — a type guard spelled as an expression because the module that owns the union did not ship one.

  **`checkedGate` has no `clean` parameter, which is the half that was a real hole.** `runAuditGate` returned `{ kind: "checked", lines, clean: isClean(verdict), verdict }`, so the colour was written one expression away from the verdict it is about and a run whose `clean` disagreed with its own printed verdict was a shape the type allowed. The predicate call lives in the constructor now: `clean` is read off the verdict, on every path, and there is no call site that can claim a red verdict passed.

  **`skippedGate` writes `clean: true` once** for the same reason `skipRead` was written for nine constructions — a discriminator written by hand is one that can be written wrong, and `{ kind: "checked", … }` around a report nobody read is a run claiming a comparison that did not happen. Two constructions is not nine and the argument does not count call sites.

  **Three cases, and one of them is exhaustive over the pair.** The guards are driven against `kind` over both halves and every declared skip cause, with `notEqual(isCheckedRun, isSkippedRun)` so a third kind answering `false` to both is a red run rather than a quiet pass; the narrowing case assigns to `AuditGateChecked` and `AuditGateSkipped` directly, so it stops compiling the day a guard stops narrowing — which is the half of a type guard an assertion cannot reach. Both constructors are compared against what the gate returns, built from the gate's own parts.

## The same asymmetry one type up (2026-09-05)

<!-- Second bullet of "Union-naming follow-ups": `AuditRead`'s halves were named and `AuditGateRun`'s, the union that taught it how to discriminate, were still spelled inline. -->

- [DONE: 2026-09-05] [est: 4000] **`AuditGateSkipped` and `AuditGateChecked` are named, and a case narrows onto both** — the entry above named both halves of `AuditRead` and left the older union, the one `AuditRead` was made a discriminated union to match, with two anonymous members. A caller holding a checked run could not say what it was holding.

  **It also makes the skip's `clean: true` reachable.** That literal type has been a stated guarantee — a skip has no other outcome available to it, because availability of a third party must not decide whether this repo's tests can run — and the only caller reads `run.clean` for both kinds without narrowing, so nothing could benefit from it. A case narrows on `kind` now and asserts the half's `clean` and its absent `verdict`, which is the first time the guarantee is somewhere other than the type.

## One pair, two argument orders, in two files read together (2026-09-05)

<!-- Second, fourth and fifth bullets of "Skip-construction follow-ups": one union half named and the other not, a builder that flipped the order it wrapped, and a case comparing a value against itself. -->

- [DONE: 2026-09-05] [est: 5000] **Both halves of `AuditRead` are named, the suite builds skips with the exported constructor, and the bypass case proves the sentence** — `AuditSkip` was named an hour earlier because two functions returned exactly it and could not say so, and the answered half stayed spelled inline. One side of a two-sided union carrying a name and the other not makes the sides look different in a way they are not, and a reader who has met `AuditSkip` goes looking for `AuditAnswer`.

  **The suite's `skippedWith(skip, cause)` wrapped `skipRead(cause, skip)`** — the builder predated the constructor and flipped the order in the wrapping, so one pair had two orders in two files that are read together. It is gone; `skipRead` is exported and is what the module itself builds skips with, which makes it the right thing for a case to build one with.

  **And the "nothing bypasses the constructor" case no longer compares a value against itself.** It asserted `deepEqual(killed, skipRead("abandoned", killed.skip))` — reading the sentence off the answer and feeding it back in, so the one field carrying what happened was compared to itself and a reader returning the wrong text under the right cause passed. Both sentences are written out now.

## Nine hand-built skips, and a type that could not name its own half (2026-09-05)

<!-- Fifth and first bullets of "Read-kind follow-ups": every skipped read spelled `kind: "skipped"` for itself, and the two functions returning exactly that half were typed as the whole union. -->

- [DONE: 2026-09-05] [est: 6000] **`skipRead(cause, skip)` is the one construction, and `AuditSkip` is the half it returns** — nine skipped reads were built by hand in `lib/audit-baseline.ts`, each spelling the discriminator for itself. A branch writing `"answered"` where it meant `"skipped"` is caught only because the two other fields contradict it, which is a guarantee resting on a shape somebody could reasonably tidy away.

  **The cause comes first, matching `auditSkipHeadline` beside it.** Every one of these sentences is printed under a headline the cause chooses, and putting them in the same order at the call site is what makes a mismatched pair look wrong. `SKIP_HEADLINES` already assumed the two travel together; nothing put them in one call.

  **`AuditSkip` exists because two functions returned exactly it and could not say so.** `auditInvocationSkip` and `skipRead` both produce a skip and both were typed as the whole `AuditRead`, so a caller holding one still had to narrow before asking which cause it was.

  **What did NOT change, having been examined: the `report?: undefined` mirrors.** A suggestion filed an hour earlier called them vestigial now that `kind` discriminates. They are not — removing them turns forty-five assertions in the suite into type errors, because they let a caller ask either question of an un-narrowed read. The reason is written into `AuditSkip`'s doc comment so the next person to notice the asymmetry does not spend the same hour.

  **And three cases run the constructor**, including one that drives both readers and asserts their answers are indistinguishable from a built one — the property that makes "nothing bypasses it" a fact rather than a convention.

## Two unions for one fact, discriminated two different ways (2026-09-05)

<!-- Third bullet of "Single-reader follow-ups": `AuditGateRun` said which half it was by `kind` and `AuditRead` by whether `report` happened to be present. -->

- [DONE: 2026-09-05] [est: 7000] **`AuditRead` says which half it is, in the vocabulary `AuditGateRun` already used** — one module, one fact (npm answered, or it did not), and two unions announcing it two ways: the newer by `kind`, the older by testing a field for `undefined`. The second is the shape `AuditGateRun` was made a union to get away from a week earlier — `clean: true` meaning both "passed" and "was never asked" — reintroduced one type up.

  **`"skipped"` is spelled identically in both, because it is one fact travelling.** A read that skipped makes a run that skipped; the positive halves differ (`"answered"` there, `"checked"` here) because they are different claims — npm said something, versus the baseline was checked. A case drives the mapping over every declared cause rather than leaving it to the prose: a gate returning `kind: "checked"` for a read that skipped is a run reported as having checked a baseline nothing was read for.

  **The suite builds reads instead of spelling them.** Sixteen literals would have had to grow the field, which is sixteen chances to write the wrong one; `answeredWith(report)` and `skippedWith(skip, cause)` say which half a case wants, and the field is written once.

## Five copies of half of the annotation format (2026-09-05)

<!-- Third bullet of "Gate-run follow-ups": the gate's lines mix log lines and workflow commands, and the only thing telling them apart was a `::` prefix the cases matched for themselves. -->

- [DONE: 2026-09-05] [est: 5000] **`isAnnotationLine` is where the prefix is known, and `ANNOTATION_LEVELS` is where the levels are declared** — a gate returns its whole output as one `readonly string[]`, log lines and workflow commands together, because the caller prints them all. Five cases in `audit-baseline.test.ts` told them apart with `startsWith("::")`, which is a copy of half of `annotation`'s decision: where the level goes, and whether a property list follows it before the closing `::`. Two spellings of one format, kept in step by nobody, in a module written because "a second copy of a rule with five replacements in it is how the two stop agreeing".

  **Anchored on the levels rather than on `::` alone.** A bare-prefix rule also matches a TypeScript type annotation in a line of source, and these guards quote source back at people constantly — a reported offender classified as a mark would be suppressed on a terminal run, which is the one place the offender is the whole point.

  **`ANNOTATION_LEVELS` is the array and `AnnotationLevel` is derived from it.** The other way round leaves a fourth level as something `annotation` can emit and the classifier cannot recognise; the case that drives the classifier iterates the array, so a fourth is covered by arriving.

  **And one case pairs the two ends.** The gate emits a log line and a mark for the same skip, so the classifier's split is compared against the one `underActions: false` already makes — a run with no marks and a run whose marks this cannot see look identical from outside.

## The tidy that turned a guard off, and the case that now notices (2026-09-05)

<!-- Second bullet of "Bounded-reader follow-ups": the options moved into `lib/` and the command did not, which looked like an oversight and is a constraint. -->

- [DONE: 2026-09-05] [est: 8000] **The audit command stays in the gate's own wrapper, and the scan that depends on that says so when it stops being true** — moving `["npm", ["audit", "--json"]]` into `lib/audit-baseline.ts` beside the bound reads as obviously correct: the bound travelled and the thing being bounded did not, so a second caller gets the timeout right and the arguments wrong. It was tried, and it turns `verify-gate-script.test.ts` off. That scan reads each gate leg's OWN wrapper for a remote read, and the claim it measures — `PUBLISHED_ELSEWHERE_NOTE`'s "the one check here that reads a live feed" — rests on the read being legible there. With the command one import away the marker stopped matching and the scan reported a hermetic `verify` while `npm audit` ran on every leg-run.

  **Following the script's `lib/` closure does not rescue it.** That was the second attempt, and it finds three more legs: `build` reaches `lib/spa-fallback.ts`, which contains a service worker's source text with a `fetch` handler in it; `lint:all` reaches `lib/cloudinary.ts`, the app's uploader; `lint:ships-to-client` reaches `lib/audit-baseline.ts` for one exported note. None of the three calls anything. A text marker cannot tell a module that CONTAINS a network call from one that makes it, which is exactly why the scan read one file to begin with.

  **So the bound lives where it is argued and the command lives where it is visible.** Both `AuditSpawn`'s doc comment and the gate script's header carry the argument, because the next person to see the asymmetry will reach for the same tidy.

  **And the case that would have caught it is split in two.** `reachOut(GATE_SCRIPTS)` was asserted against the whole expected list, so a run where the audit gate had stopped being seen at all failed with "a second leg of `verify` now reads something outside the tree" — the opposite finding, wearing the wrong sentence. Seeing the gate is its own case now, with a message that says the call moved somewhere the scan cannot see.

## A gate that took a reader, and a reader nobody could run (2026-09-05)

<!-- Second bullet of "Single-reader follow-ups" and the fifth of "Header-and-stub follow-ups": the bound lived on the far side of the boundary from the argument for it, and the thirty lines applying it had no header and no test. -->

- [DONE: 2026-09-05] [est: 10000] **`auditReader` takes the spawn, applies the bound, and is run by nine cases** — `AUDIT_TIMEOUT_MS` states the case for three minutes from three measured runs (a 49-second healthy answer against 5m35s, 7m and past 13m on a degraded registry), and the four literals applying it were typed beside an `execFileSync` in `scripts/`. `runAuditGate` takes a reader, so a caller handing it one without a timeout got a gate with no bound and no complaint — the whole argument one module over from the code that needs it.

  **`AUDIT_SPAWN_OPTIONS` is the bound, the kill signal, the buffer and the stdio, stated where the decision lives.** Each was an inline comment inside a function whose own doc comment did not exist: `maxBuffer` is 32 MiB because the default cuts a whole-graph report into unparseable JSON, and that failure arrives looking like npm answering badly rather than like a buffer this repo chose; `stdio` drops stderr because npm writes progress there; SIGKILL rather than SIGTERM because what is being given up on is a process waiting on a socket.

  **The three ways a read can end are each driven now.** An answer, a throw carrying the report (which is EVERY real run — `npm audit` exits non-zero whenever it finds anything at or above the default level, so a reader trusting the status would skip every run and the gate would be green for a year), and a throw carrying nothing. The order of the two throw paths has its own case: a killed process's stdout is whatever had been flushed, and here it is valid JSON with no findings — which is exactly what "every accepted advisory has been withdrawn" looks like, so salvaging before asking whether it was killed prunes the baseline over a timeout.

  **Three source reads became three executions.** `read: readAudit,`, `readAuditPayload(raw)` and `timeout: AUDIT_TIMEOUT_MS` were matched in the script's text; the first two are now the reader compared against `readAuditPayload` over the same payload, and the third is a spawn stub that captures the options it was handed. A reader with no bound at all could have been written to keep every one of those spellings.

  **And the strict header rule counts what the file imports.** The gate's header names `execFileSync`, which is neither ours to export nor ours to declare and is findable from the file the reader already has open. A rename still fails: `tsc` rewrites the import to the new name and leaves the one in the paragraph resolving to nothing.

## Three ways the header sweep was load-bearing on something it typed itself (2026-09-05)

<!-- Second, third and fifth bullets of "Header-sweep follow-ups", plus the fourth: a filename typed into the file it names, a floor eleven names below the measurement, two findings in one sentence, and three walks where one would do. -->

- [DONE: 2026-09-05] [est: 6000] **The sweep asks the runtime where it is, reports its two findings separately, and extracts each header once** — `SELF` was the literal `"header-prose-names.test.ts"`, a filename typed into the file it names. A rename leaves it pointing at a suite that is not there, the exclusion stops excluding anything, and every entry in `FOREIGN` quietly resolves itself again — the failure the exclusion exists to prevent, restored by an edit that has nothing to do with it. `path.basename(new URL(import.meta.url).pathname)` is the spelling two helper suites already use to ask the same question.

  **The two failures are two cases now.** A file somebody MOVED and a function somebody RENAMED are different findings, and one sentence covered both with a `(no such file)` marker on half the rows as the only thing telling them apart.

  **The floors were eleven names below the measurement.** 257 headers and 311 names, guarded at 200 and 300 — a tripwire on somebody rewriting one paragraph in plain words, and a red run about prose style is a red run nobody keeps. Both sit at roughly half the tree now, which is what they were for: a shape rule tightened until it claims nothing must not pass in silence.

  **And `HEADERS` is built once.** Three cases wanted the same 257 headers and each walked and re-extracted for itself; `readSource` is cached and the extraction is not, so the half that costs something was the half being paid three times.

## The probe that found two stale headers and then stopped existing (2026-09-05)

<!-- First bullet of "Prose-name follow-ups": the rule guarded one header, and the sweep that found the other two lived for the length of one run. -->

- [DONE: 2026-09-05] [est: 11000] **Every module header in the source tree is checked against the code it points at** — `classifyProseName` was run over all 257 of them while the gate's own case was being written, and it named two headers that were already wrong: `lib/deployment-env.ts` described a `normaliseEnvironment` renamed weeks earlier, and `lib/check-profile-id-pii.ts` said `toProfileRow` writes `public_id` back to the cloud when the writer has been `upsertProfileBody` since. Both were fixed by hand and the probe was thrown away, which means the next one is found the next time somebody happens to run the same thing.

  **The rule is looser here than the gate's, deliberately.** Resolution is "the name appears somewhere in this tree's code" rather than "somebody exports it". The strict rule is right for one header making one claim and wrong for 257 making every kind — source headers name parameters, JSX props, object fields and env vars, none of which is an export and every one of which a reader can find; demanding exports reports 68 files, almost all of them correct prose. What survives the loosening is the failure that actually happens, and both real finds are of exactly that shape.

  **Three more paths were wrong and are fixed.** `lib/check-reporter-graph.ts` pointed at `__tests__/test-failure-report.ts` (it is `.test.ts`), and `lib/strip-comments.ts` twice named `helpers/…` for files that live under `__tests__/helpers/`.

  **The index walks the suites, and excludes this one file.** A header naming `SCAN_ROOTS`, `balancedParens` or `nanoidLike` points at a spelling that survives in the suite covering the change, which is where a reader asking "what was this called before?" should land — walking only source calls five of those missing. The exclusion is not tidiness: `FOREIGN` is a list of names the tree does NOT contain, written as object keys, which are identifiers. Index them and every exemption resolves itself, the allow-list cancels the allow-list, and the rule is green because it is broken. That was the first version, and a case states the exclusion now rather than leaving it to the walk's filter.

  **`FOREIGN` is six names quoted from outside the repository** — two Node loader error codes and four ECMA-262 lexical-grammar productions — each carrying where it comes from, on the same terms every allow-list here is held to: compared against a separate literal so widening it means finding both, and every entry asserted to still be quoted by some header AND still absent from the tree, so an exemption that stopped doing the thing is red rather than a hole standing open.

## A reader that counted one way, and eight options objects for one call (2026-09-05)

<!-- Third and fifth bullets of "Header-and-stub follow-ups", plus the last of "Single-reader follow-ups": the stub caught an over-read and not an under-read, and every case built the same options object by hand. -->

- [DONE: 2026-09-05] [est: 7000] **The gate's runner accounts for its reads in both directions, and stages them in one call** — `staged()` failed on a call PAST the answers it held and said nothing about answers left unused. Every case in the block states how many times the registry is asked, in the length of the list it stages, and half of every one of those statements was decoration: a case staging two reads and getting one passed silently. That is the direction the single-reader change was made to catch — a gate that stopped asking again is a gate that prunes the baseline over one read's silence, which is the incident rather than a regression in a message.

  **`gateRun(reads, options)` stages, runs and reconciles the count.** The equality is asserted by the runner rather than offered to the case, because a `drained()` a case has to remember to call is the same bug one screen later. Staging and running are one function for the same reason: eight cases were building the same `runAuditGate` options object by hand — five fields, four identical in every one — and a case reaching for the raw call could still stage nothing.

  **The two unbounded readers are gone.** `read: () => ({ report: short })` answered forever, which said "the registry is still degraded" and could not say how many times it was asked; two staged copies of the same short report say both, so "a short report is worth re-asking about" is now something those cases prove rather than something their comment claims.

  **And the runner is run.** Two cases drive `gateRun` against the one gate whose read count is known either way: a stale-only red asks twice, so staging one is refused; a clean report is not worth asking about, so staging two is refused. Without them the accounting is a mechanism nothing exercises, which is what the accounting was added to stop.

## A header pointing at code, with nothing checking the code is there (2026-09-05)

<!-- First bullet of "Header-and-stub follow-ups": three module names in prose, in the file whose whole subject this week has been prose going quietly wrong about code. -->

- [DONE: 2026-09-05] [est: 9000] **The gate script's header names things that exist, and the extraction both headers need is a helper** — `scripts/check-audit-baseline.ts` opens by saying every decision is `runAuditGate` in `lib/audit-baseline.ts` and that `readAuditPayload` tells a report from a registry failure that parses like one. That paragraph is the only thing telling a contributor where the gate's decisions live, and it is prose: `tsc` rewrites the import on a rename and leaves the sentence pointing at a function that is no longer there — the exact failure `lib/plural.ts` grew a case for when its "who else asks" paragraph fell a caller behind.

  **`__tests__/helpers/module-doc.ts` is the half both headers need.** `moduleDoc` was inside `plural.test.ts`, written for the one invariant that wanted it; a second header reading its own names is how the second copy gets written. It gained the case the gate scripts need — a `#!/usr/bin/env tsx` shebang is skipped for the same reason a licence header is — and `backtickedNames` is bounded to one line, because a lone backtick in prose would otherwise hand the caller a "name" the width of the header.

  **What is claimed is decided by SHAPE, and most of the header is not claimed.** `classifyProseName` takes a camel, Pascal or SCREAMING_SNAKE identifier and a slashed path with an extension; `npm audit --json`, `check-expo-install`, `SIGKILL`, `postinstall` and `pull_request` are left alone, because a rule demanding npm's lifecycle hooks and the kernel's signals resolve to something in this tree would be red on correct prose, which is the rule somebody exempts and then ignores. Two measured floors keep that honesty from degrading into a check of nothing: a header reworded until it names no code has stopped pointing anywhere, and that is a finding.

  **Resolution is exports, not "appears somewhere".** The claim the sentence makes is that a reader can go and find `runAuditGate`, so a dead name surviving as somebody's local is not the sentence being true. Both export forms are read — the declaration and the `export { }` list — and a fourth case derives the entry point from the script rather than restating it: the gate calls exactly one function with an options object, and the header has to name that one.

  **Two headers were already wrong, found by running the rule over the tree.** `lib/deployment-env.ts` said the two configs each carried an identical `normaliseEnvironment`; the function has been `normaliseDeploymentEnv` for weeks and the old name is nowhere in the repository. `lib/check-profile-id-pii.ts` — and the case in its suite — said `toProfileRow` writes `public_id` back to the cloud; the writer is `upsertProfileBody` in `lib/supabase-profiles-shapes.ts`. Both are the incident the rule describes, sitting in the tree.

## Sixty lines arguing for decisions the file no longer makes (2026-09-05)

<!-- Fifth bullet of "Gate-run follow-ups" and the first of "Single-reader follow-ups": the script's header outlived the code it described, and the suite had two stubs for one job. -->

- [DONE: 2026-09-05] [est: 6000] **The gate's header describes the gate's file, and the argument moved to the module that makes the decisions** — `scripts/check-audit-baseline.ts` was 110 lines, 60 of them a doc comment: the two severity scopes, the soft skip, `isAuditReport`, `reportCompleteness`, the second read and which runs pay for it. Every one of those now lives in `lib/audit-baseline.ts`, so the header was five paragraphs to keep in step with code the file no longer contains — which is the staleness a dozen entries this week are about, in the one place a contributor reads first.

  **What is left says what the file is**: the two things only a process can do, plus the reader that carries the bound its spawn needs, and a pointer to where the decisions are argued. The three-lines-of-defence paragraph — loud failure, quiet failure, and asking again — moved into `runAuditGate`'s doc comment, beside the code it describes, in the order of how loudly the registry fails.

  **And the suite's two reader stubs became one.** `once(x)` was `inTurn(x)` with a different failure message, four lines apart, and the distinction they encoded was carried by which one a case picked. `staged(...)` is both: a case stages the answers its run should get, and a call past them fails with the count — so "a healthy run does not pay for a second `npm audit`" and "the second read got this" are the same mechanism.

## Two readers for one registry, and a pass that could not be told from a skip (2026-09-05)

<!-- First and second bullets of "Gate-run follow-ups": the signature let the two reads come from different trees, and `clean: true` meant both "passed" and "never asked". -->

- [DONE: 2026-09-05] [est: 7000] **`runAuditGate` takes ONE reader, and its result says which kind of run it was** — the entry above moved the gate's decisions where a test could run them, and did it with the shape `main` happened to have: a first read already made, plus a reader for the second. Both calls ask the same registry about the same tree, so that signature allowed a state no caller wants — a first read of one tree reconciled against a second read of another — and it made "this run did not pay for a second `npm audit`" a property of the arguments rather than of the run.

  **One `read: () => AuditRead`, called once and again only where `worthAsking` says so.** The cases stage their answers instead of splitting them: `once()` fails the case on a second call, so the healthy path proves it spent one call rather than asserting a count somebody could stop maintaining, and `inTurn()` hands over a first and second read in order and refuses a third.

  **`AuditGateRun` is a union now**, for the reason `AuditRead` is one. `clean: true` was both "the baseline is clean" and "the baseline was never checked" — the same ambiguity `stale: []` had before `completeness` travelled beside it — with a `verdict` that might or might not be there to tell them apart. `kind` is `"skipped"` or `"checked"`, the skip's `clean` is the literal `true` (a skip has no other outcome available to it), and a caller reaching for the verdict has to say which run it is holding first.

## The half of the gate that decides whether the second read happens (2026-09-05)

<!-- Fifth bullet of "Runnable-decision follow-ups", with the third folded in: `main` still held the first read, and the second read's skip printed one sentence for four failures. -->

- [DONE: 2026-09-05] [est: 12000] **`runAuditGate` is the whole gate minus the registry and the exit, and the skips both say who gave up** — the entry above made the SECOND read runnable and left `main` holding the first: its skip, the headline that says which failure it was, the annotation that keeps a skip from reading as a checked run, the under-report warning and the exit decision. That is the half deciding whether a second read happens at all, and five cases were still establishing it by matching expressions in the script — `if (!isClean(verdict)) process.exit(1)`, `skipping (${headline})`, `verdict.completeness.underReported.length > 0 && runningUnderActions()`. Every one of them passes over a `main` rewritten to print nothing.

  **The script is now the two things only a process can do**: run `npm audit --json` and exit. It hands `runAuditGate` the first read, a reader for the second, the check's name and whether it is under Actions, prints every line it gets back, and exits 1 when the run is not clean. `AuditGateRun` carries `clean` true for a SKIP as well as a pass — availability of a third party must not decide whether this repo's tests can run — and the verdict is absent when the first read produced none, which is the one state that used to be an early `return`.

  **What the five source reads became.** A clean report prints the OK line and nothing else; a skip exits 0, names its cause and marks the run; the same skip locally prints one line and no workflow commands; an untriaged advisory is red; a stale-only red asks again and goes green on the entries both reads mention; a short report warns on the summary with the same two numbers its log line carries. The skip case runs every declared cause, so a fifth joins the assertion rather than waiting to be typed into it.

  **And the second read's skip carries the headline the first read's has.** It printed the bare sentence for all four failures — a second read abandoned after three minutes says the bound may be too tight, one the registry refused says the bound is beside the point — which is the exact confusion three entries were spent removing from the first read's skip, reintroduced one function away.

## Six source reads standing in for one execution (2026-09-05)

<!-- Fifth bullet of "Clause-builder follow-ups": every fact about the gate's second read was established by matching an exact expression in its source. -->

- [DONE: 2026-09-05] [est: 12000] **The second-read decision moved into `lib`, and the cases RUN it** — `answer()` lived in `scripts/check-audit-baseline.ts` with a `readAudit` closed over from the module and a `console.log` in each branch, so nothing could call it. Every fact about it was pinned by reading the file for an exact expression: the `worthAsking` guard, the `evaluateAudit` of the second payload, the reconcile, the account, the annotation `if`, and the count of format calls. Six spelling checks in a file whose recurring bug is that a check reading for a spelling is checking the spelling — and every one of them passes over a function that has been rewritten to do nothing, as long as the words are still there.

  **`answerWithSecondRead` takes the reader and returns the lines.** The gate passes its own `npm audit --json` and `runningUnderActions()`, then prints what comes back; a case passes a stub and asserts what the decision did. All three paths are reachable that way — never asked, asked and the registry stopped answering, asked and answered — and the "never asked" case now COUNTS the reader's calls, so "a healthy run does not pay for a second `npm audit`" is measured rather than inferred from an `if`.

  **What that bought immediately.** Five cases that could not have been written before: the announcement precedes the account; a skip between the calls keeps the first verdict and annotates nothing; a disagreement reconciles to the entries both reads mention; the summary mark carries the SAME account string the log line printed, escaped; and a run whose reads agreed is left unmarked whether or not it is under Actions. One source read remains, and it is about delegation rather than behaviour: that the gate calls this and prints every line it hands back.

## The case read the account with a regex it typed itself (2026-09-05)

<!-- Second bullet of "One-comparison follow-ups": four phrasings written into the assertion, matching prose the same file asserts the exact wording of. -->

- [DONE: 2026-09-05] [est: 8000] **The disagreement clauses come from the formatter, so the case compares strings rather than patterns** — the entry above put one `secondReadDifference` behind the printed account and the annotation predicate, and held them together with a case that asked whether the sentence "names a difference" using `/short of its own totals|the reads disagree about|…/`. Four phrasings typed into an assertion, over prose three cases in the same file assert the exact wording of. Rewording a clause turns that check OFF rather than red: a pattern that stops matching and a run with nothing to report are indistinguishable from inside the assertion, and the invariant would have gone on passing over a sentence it no longer read.

  **`secondReadDisagreements` is the clause list, empty exactly when the reads agreed.** The six clause builders are one private function each — the decider, the shared tally, the disputed entries, the settled staleness, and the two one-sided finding clauses — and both `formatSecondRead` and the disagreement list call them, so a reworded clause moves in both at once. The case now asserts the predicate against `disagreements.length === 0` and asserts every clause it names appears verbatim in the printed line: a clause that is measured and never printed is a failure, which no regex could have said.

  **A shortfall both reads share is not a disagreement.** Two reads that could not answer answered the same way, so the tally clause splits: `deciderClause` when exactly one read carried its totals, `sharedTallyClause` when both did or neither did. The second is in the account (it is what the run cost) and out of the disagreement list (nothing differed), and a case pins both halves of that on one degraded pair.

## One question asked twice, by the sentence and by the mark beside it (2026-09-05)

<!-- First bullet of "Second-read-account follow-ups": the account and the annotation predicate compared the same two reads separately. -->

- [DONE: 2026-09-05] [est: 9000] **`secondReadDifference` is the one comparison, and both readers take it** — the entry above printed what the second read did and annotated the runs where the two reads disagreed. Those are two jobs asking one question, and they asked it separately: `formatSecondRead` built its clauses out of the two verdicts and `secondReadAgreed` folded three comparisons of its own into a boolean, in the same module, over the same pair. A log line reading "both reads carried npm's own tally; every baseline entry was reported" printed beside a summary mark saying the reads disagreed was one function's edit away — the two-copies arrangement most of this file's history is about, reintroduced by the entry that closed one.

  **Three fields, because three things can differ between two calls a minute apart.** Whether one read carried npm's own tally and the other did not; which baseline entries exactly one VOTING read failed to mention; and which findings only one of the two calls reported, in both directions. `stillPresent` and `majorOnly` are derived from those, so a difference in either is already in the list. An abstaining read is left out of the staleness comparison, or the whole baseline would read as disputed on every degraded run.

  **The predicate is now the ABSENCE of what the sentence names**, rather than a second opinion about it, so a run cannot be annotated for a disagreement its own log does not describe. Held by a case that runs eight pairs of reads through both and asserts they agree: two clean reads, a staleness list that moved, either read failing to carry its totals, both failing, a finding only one call saw in each direction, and the same live advisory twice. Two reads that BOTH answered nothing agree — a shared shortfall is not a disagreement, and that pair is in the table for the same reason as the rest.

## The second call announced itself and then said nothing (2026-09-05)

<!-- Third bullet of "Second-read follow-ups", with the second bullet's information loss folded into it: the log said the gate was asking again and never said what came back. -->

- [DONE: 2026-09-05] [est: 11000] **The gate says what the second read did, and marks a run where the two reads disagreed** — the entry above taught this leg to ask npm twice when the first answer rested on what npm did NOT report. What reached the log was one line before the second call and the verdict after it, with nothing in between. Whether the second read landed, agreed, or was the one that changed the answer — which is most of what the second call exists to establish — was readable nowhere, on the one leg of `verify` whose answer can change while the tree does not.

  **`formatSecondRead` is three measurements, never a judgement.** Who could answer the staleness question (both reads, one of them and which, or neither, with the shortfall in npm's own numbers); what happened to the staleness list (the two reads agree, or they disagree about named entries that therefore stay on the baseline); and which findings only one of the two calls saw — kept either way, because a finding is something npm SAID and one read failing to repeat it is not a withdrawal. Only the reads that VOTED are compared: an incomplete read abstained, and counting its silence as disagreement would print the whole baseline as disputed on every degraded run.

  **It also carries the sentence `reconcileAudit` is right to drop.** The reconciled verdict holds the completeness of the read that DECIDED staleness, so a first read short of its own totals disappears entirely once a sound second read exists — the correct answer, printed by a run that met a degraded registry and says so nowhere. A case asserts that absence from `formatAuditVerdict` alongside its presence here, so the two cannot quietly become one.

  **`secondReadAgreed` decides an annotation, not the exit code.** The answer printed is sound; what a contributor loses without a mark on the run summary is the only evidence their branch was judged by a registry that changed its mind mid-run — and a log line on a green run is read by nobody, which is the argument the skip's annotation already made. A `notice`, the quietest level, because nothing here is wrong. It compares completeness, staleness and findings before the reconciliation, because a reconciled verdict cannot be asked whether it came from two answers: its whole job is to look like one.

## The one leg whose answer can change, and it could not check (2026-09-04)

<!-- Fifth bullet of "Audit-report-completeness follow-ups", restated as the fifth bullet of "Report-completeness follow-ups" and the fifth of "Two-scope tally follow-ups": the gate has no way to re-ask. -->

- [DONE: 2026-09-04] [est: 12000] **The audit gate asks npm a second time when the first answer was about what npm did NOT say** — eight of the nine `verify` legs read the tree and give the same answer next year. This one asks the registry, so a green tree goes red with no commit in between, and until now it was also the only leg with no way to check: a red here was indistinguishable from a red the branch caused until somebody re-ran the step by hand. That is exactly what happened on 2026-09-04, twice, nine minutes apart, and both times the second answer was different. Two entries above turned that wrong answer into a WITHHELD one, which is better and is still not an answer.

  **`worthAsking` decides which runs pay for it.** A verdict red only on `stale` is a claim built entirely out of silence; a report short of its own totals is the same claim caught one layer earlier. Nothing else qualifies, and the reasons differ: a red on `unexpected` or `fixableInRange` was caused by something npm positively reported, so a second read can only agree or say less; a skip already exits 0, so there is nothing to win, and an `"abandoned"` skip has already spent three minutes not being answered. A healthy run never reaches the second call, so the gate costs on passing runs exactly what it did.

  **`reconcileAudit` unions the findings and intersects the staleness.** A finding is something npm SAID, so either read having said it is enough and no union can invent one. Staleness is something npm did not say, so it takes both reads failing to mention an advisory before the baseline is told to drop it. An INCOMPLETE read abstains rather than voting empty: intersecting its empty `stale` with a sound read's would let the degraded answer decide, which is the failure the whole mechanism undoes.

  Exercised against the live registry: a baseline entry for an advisory that does not exist triggers the second read, both reads agree it is absent, and the finding survives — with one call on every healthy run, as before.

## A registry that could forget the wrapper it was written to remember (2026-09-04)

<!-- Second and third bullets of "Refusal-registry follow-ups": "`installRefusal` takes a holder and a key, so nothing stops a second call overwriting an entry" and "`installedRefusalNames()` is asserted to CONTAIN seven names and never to equal them." -->

- [DONE: 2026-09-04] [est: 9000] **The refusal registry refuses a duplicate address, and its population is compared against the surfaces the bootstrap patches** — `installRefusal` was a `Map.set`, so registering one address twice kept the LAST entry. That is the failure the registry exists to catch, reproduced inside it: the first call's wrapper is still on the surface, still between a caller and the refusal, and no longer remembered by anything — so `rewrappedRefusals()` compares the second wrapper against itself, finds them equal, and reports a clean process. It throws now, in a module body `--import` runs before any suite, so the process fails at load rather than one case going red downstream of a registry that has already lied.

  **And nothing stated the population.** The existing case reads the registry's MEMBERS — each one refuses, each names a known surface — and both faults it cannot see are the same fault: a point registered for a surface nobody patches passes every assertion in it, and a surface patched with no registration is invisible to all of it. `HTTP_SURFACES`, `HTTP_METHODS` and `SPAWN_METHODS` are exported now, the point names are built from them, and the case builds the set it expects from the same lists — so a method joining `SPAWN_METHODS` moves both sides and a name written by hand moves only one. `globalThis.fetch` is the hand-written registration and is what the case is really about.

  **The prose lost its number.** The header said "ALL ELEVEN POINTS, not the six spawn methods" — two counts that go stale the day a method is patched, in the file whose subject is two populations drifting. It names the lists instead, and the derived case is where the population is stated.

  Both guards were run against the fault they describe: commenting out the `globalThis.fetch` registration turns the new comparison red, and a second registration of it fails the process at load.

## Two scopes for one question, and only one of them was being asked (2026-09-04)

<!-- First and third bullets of "Report-completeness follow-ups", plus the fourth bullet of "Audit-report-completeness follow-ups" (what `evaluateAudit` does with no `vulnerabilities` key). -->

- [DONE: 2026-09-04] [est: 11000] **The completeness check reads npm's whole tally, and says so where it withholds nothing** — the entry above compared `metadata.vulnerabilities` against the entries at high/critical, because staleness is the verdict it protects and the baseline is a list of high/critical roots. The FIX rule reads all five severities, and it was left with the same blind spot the staleness half had just lost: a report that dropped its nineteen moderates is "complete" by the triaged question, `fixableInRange` under-reports over a population it cannot see the size of, and the run prints OK.

  **`reportTally` is now the one walk and `reportCompleteness` folds it.** One row per severity either half of the report names, `total` skipped because it is the sum and not a severity, an unrecognised severity kept rather than dropped, and the no-claim rule applied PER ROW — an npm that stops emitting one severity's tally is not an npm that stopped emitting totals. `complete` still reads the triaged two and still withholds `stale`; `underReported` reads all five and withholds nothing, because under-reporting can only make a finding list quieter. Two scopes, stated as two fields, each with the argument for its own scope.

  **And the sentence had a `1` in it.** "9 high/critical roots were counted" reads "1 high/critical root were counted" at one — `counted` inflects the noun and cannot reach a verb three words later. It is "npm counted 1 high/critical root and reported 0" now, where the number governs nothing downstream, with a case at one and a case at nine.

  **Plus the behaviour nobody had pinned.** `evaluateAudit({})` reads the whole baseline as stale — a decision that falls out of an optional key and a `?? {}`, that no case stated, and that no reader would predict from the type. It is the right one, because `isAuditReport` is what stands in front of it, and there is a case saying both halves now.

## A report that said nothing was read as a report that said "all clear" (2026-09-04)

<!-- First two bullets of "Audit-report-completeness follow-ups": "A registry answer that is well formed and INCOMPLETE reads as 'everything was fixed'" and "`stale` is the one verdict that fails on the ABSENCE of data, and absence is what a degraded read produces." Third bullet, the note's wrong event, in the same change. -->

- [DONE: 2026-09-04] [est: 14000] **The audit gate checks npm's report against npm's own totals before reading an empty findings set as four withdrawals** — `isAuditReport` draws the line at "npm answered" and `readAuditPayload` at "the answer is readable", and both were written for a registry that fails LOUDLY: an error object, a timeout, a truncated stream. On 2026-09-04 it failed quietly instead — a well-formed, parseable report whose high/critical entries were simply missing. Every predicate said yes, `observedAdvisories` came back empty, every baseline entry read as `stale`, and CI went red demanding the removal of four advisories that were all still live. Two calls nine minutes apart disagreed; a third agreed with the baseline again.

  **The report says so itself.** `metadata.vulnerabilities` is npm's own tally over the entries it emitted, so a report short of its totals has definitely lost entries — measured on this tree's real audit: `high: 9` in the totals, nine entries at `"high"` in the map. `triagedCompleteness` compares the two and `evaluateAudit` withholds `stale` when they disagree, carrying `completeness` on the verdict so an empty list still says which of its two meanings it has.

  **Withheld, not skipped, and the asymmetry is why.** `unexpected` and `fixableInRange` need a FINDING to fire, so a report that says less makes them quieter — the safe direction, and the reason a partial report is still worth evaluating. `stale` is the one verdict that fires on the ABSENCE of data, so it fires harder the less the report says, and it is on the failing side of `isClean`. The gate prints the withholding on the green path too, and annotates the run summary: a run that passes because a question was not asked is not a checked run.

  **And the note that rides every failure named the wrong event for one of them.** "May have been published since the last green run" was printed on `stale` as well, where nothing is published — an entry lands there when an advisory is WITHDRAWN. It says "published, or withdrawn" now, which is true on all three paths.

## A floor with no reason, next to the helper that exists to ask for one (2026-09-04)

<!-- First two bullets of "Header-floor follow-ups": "The header floor is 2000 characters and the header is 3400 — a MEASURED floor would be 3400 and this is neither that nor argued: it was picked as 'clearly longer than a function's JSDoc'." and "`doesNotMatch(doc, /export function plural/)` pins the first export's name … the property wanted is 'ends before the code', and the evidence is one identifier." -->

- [DONE: 2026-09-04] [est: 6000] **The header is compared against the longest function doc in the same file, derived at run time** — the entry above floored the extracted block at 2000 characters, a number that was neither measured (the header is 4285) nor argued, written in the one file whose whole subject this week has been telling those two apart. The first draft of the fix argued it and quoted "971 characters" in the reason, which is a third number to keep in step with the prose around it.

  **So it derives both sides instead.** Every `/**` block in the file, the first one against the longest of the rest: an anchor on `/**` can land on either, and the header is the one the caller list lives in. No number is written down, and editing the prose cannot make it wrong.

  **And the second bullet, in the same case.** `doesNotMatch(doc, /export function plural/)` proved the block ends before the code by naming one identifier; the property wanted is "stops at the source", so it is `/^export /m` now — a rename is no longer the thing that would notice.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7608 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## Two bullets that were wrong, and the one under them (2026-09-04)

<!-- "Module-doc follow-ups", first three bullets. The first two are marked EXAMINED in place with what the measurement said; the third is the one implemented: "The three cases run on synthetic strings and the case that matters runs on the tree — nothing connects them: `moduleDoc(readRepoFile(\"lib/plural.ts\"))` could stop returning the block it was proved to return on a fixture, and only a caller falling out of the list would notice." -->

- [DONE: 2026-09-04] [est: 6000] **The extraction is now proved on the file it is actually run on** — three cases proved `moduleDoc` on synthetic sources, and the case that matters runs it on `lib/plural.ts`. An extraction that quietly returned the whole file passes all three fixtures and keeps the caller list green, because the names are in there somewhere. The fourth case asserts the return begins at an opener, clears a measured floor of 2000 characters, and stops before `export function plural`.

  **The first bullet of that section was wrong twice and is recorded as such.** "Three suites do this by hand" was written rather than measured — the tree has one. And promoting it to a helper would ship a wrong answer: it anchors on the first `/**`, which is a module's header only for a file that opens with one, and dozens under `lib/`, `scripts/` and `__tests__/helpers/` open with imports. A shared `moduleDoc` would hand those callers the first JSDoc of a function.

  **The second was a shape this tree does not have.** No file under those three directories opens with `/*!`, and a module whose header is not a JSDoc block genuinely has no doc comment to read — which is what the refusal says.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7608 tests (+1), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## The doc comment was whatever came before the first `*/` (2026-09-04)

<!-- First bullet of "Caller-list follow-ups": "The case reads the doc comment as `source.slice(0, indexOf(\"*/\"))` — the leading block, by position rather than by parse. A file that grew an `import` above its doc comment, or a `/* eslint */` line, would silently narrow the text the caller names are looked for in, and every path would read as unnamed at once." -->

- [DONE: 2026-09-04] [est: 7000] **The caller list is looked for in the first `/**` block, closed from its own opener** — the entry above compares `lib/plural.ts`'s doc comment against the import graph, and got the doc comment by taking everything up to the first `*/`. That is the doc comment only while the doc comment is the first thing in the file. A licence header or an `/* eslint … */` line above it cuts the search text down to that line, and all three callers then read as unnamed at once.

  **The failure it would have produced is the expensive kind: a red run about the extraction wearing a message about the paragraph.** Whoever met it would go and re-check a doc comment that was right.

  **`moduleDoc` anchors on `/**` and closes from there**, so a comment above it is skipped and the block ends at its own terminator rather than at the file's last one. A file with no doc comment is refused: an empty search text agrees that nothing is named, which is the wrong answer given confidently.

  **Three cases, on synthetic sources rather than on the tree.** The comment-above-the-doc shape that broke it, a `// lib/impostor.ts` line after the close that must not be read as inside it, and the no-doc-comment refusal. Synthetic because the tree has exactly one file with this shape today and the point is the shapes it does not have yet.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7607 tests (+3), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## A caller list in prose, one entry from being wrong (2026-09-04)

<!-- First bullet of "Marker-refusal follow-ups": "The doc comment now names its three callers and nothing keeps that list live — a fourth module importing `lib/plural` leaves the header describing two, which is the same staleness the 'who else asks' paragraph was written to fix. The importers are greppable and no case reads them." -->

- [DONE: 2026-09-04] [est: 8000] **The paragraph naming `lib/plural`'s callers is walked and compared against the import graph** — the entry above fixed a header that had gone stale by two callers, and fixed it in prose, which is the same material the staleness was made of. The importers are a `grep`; nothing was reading it.

  **Three callers, named rather than counted.** `lib/i18n-context.tsx`, `lib/audit-baseline.ts`, `__tests__/helpers/coverage-floor.ts`. The case walks `lib/`, `scripts/`, `app/`, `components/` and `__tests__/helpers/` for an import of the rule and fails on any importer the doc comment does not mention by path.

  **Suites are excluded and the exclusion is the interesting half.** A case importing `plural` to TEST it is not a caller in the sense the paragraph is about; a helper that builds a message somebody reads is. So `*.test.ts` is dropped and `helpers/` is kept, and the walk carries a measured floor of three so a broken import pattern cannot pass as "nobody imports it".

  **Proved by unnaming one.** With `lib/audit-baseline.ts` reworded to "the audit gate" in the doc, the case goes red naming that module; the other fourteen in the file stay green.

  **A repo guard caught the first draft, which is what it is for.** `file !== "lib/plural.ts"` inside the walk is a path spelled at the point of use, and `inline-exclusion.test.ts` fails that shape by name — the exclusion is a named const now.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7604 tests (+1), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## A rule read by three callers and documented for one (2026-09-04)

<!-- First bullet of "Plural-rule follow-ups": "`lib/plural.ts` now has a caller that is not the app, and its doc comment does not know — every sentence in it is about the six languages and the sixteen i18n keys, and two of its readers are a build-time gate and a test helper counting probes." -->

- [DONE: 2026-09-04] [est: 6000] **`lib/plural.ts` says who else asks, and the marker refuses a form it cannot resolve** — the entry above pointed two new callers at a module whose every sentence is about translation maps. A reader arriving from the audit gate or the floor helper meets fifty lines about `предмет`/`предмета`/`предметов` and none about why a gate's English is here, which is the header that gets a fourth copy written by whoever reads only the header.

  **What the doc now states is the part that generalises.** `plural` knows no vocabulary, which is exactly what makes it the two-form rule for a failing gate as much as for `en`, `de` and `es`; `slavicPlural` has no caller outside the maps and wants none, because this tree produces no Russian failure messages.

  **And the second bullet was a live wrong answer, fixed with it.** The marker split on the first `|` and dropped everything after a second, so `"(a|b|c)"` typechecked, inflected to "a" or "b", and lost "c" with nothing said — a silent wrong answer in the one string a reader is meant to believe. It throws now, naming the count of forms and the two shapes that work.

  **The throw lands on the GREEN path, which is where it belongs.** `assert.ok(x, m)` builds its message whether or not it fires, so a phrase the rule cannot resolve fails the first run that reaches the line rather than the first run that goes red.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7603 tests (+1), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## Three copies of "exactly one takes the singular" (2026-09-04)

<!-- First bullet of "Counted-phrase follow-ups": "Two helpers now solve the same agreement problem two different ways — `coverage-floor.ts` inflects a `(s)` marker and `offence-sweep.ts` puts the count behind a label … `lib/audit-baseline.ts` has a third shape, `${count} ${count === 1 ? one : many}`, in production. Three answers to 'a number and its noun' in one tree, and nothing relates them." -->

- [DONE: 2026-09-04] [est: 10000] **The rule lives in `lib/plural.ts` and the other two ask it** — the bullet counted three shapes and undercounted: the FOURTH was the one the app has spoken through since the day sixteen i18n keys read "1 items" in six languages. `plural(count, one, other)` is that module, argued at length for the six locales, and the entry above added a fifth comparison in a test helper while `lib/audit-baseline.ts` was already carrying `count === 1` privately.

  **`lib/audit-baseline.ts` no longer decides agreement.** Its `plural` was a rule plus a count in front of a word; what is left, `counted`, is only the count — the rule comes from `lib/plural`, and the `"it"`/`"them"` ternary two lines down goes through it too. Every message it prints is byte-identical, which is the point: this bought one rule, not new behaviour.

  **The marker is now a spelling of the two forms rather than a rule of its own.** `"probe(s)"` splits into "probe" and "probes" and `plural` picks; `(s)` is short for `(|s)`, and the general form takes both — `"advisor(y|ies)"` — so the irregular nouns the last entry told a reader to rephrase around are simply writable. The type widened with it.

  **What relates them is a case, not a comment.** The helper's output is compared against `plural`'s answer at 0, 1, 2, 3, 11, 21 and 100, so a copy of the rule that agreed at the two obvious numbers and diverged anywhere else is red. The audit gate's stale line gained the case that would have caught its own singular: three words in that sentence move with the count and none of them had a case at one.

  **Not folded in: the offence sweep.** Its fix put the count behind its label, so it has no agreement to get wrong and nothing to delegate. Recorded as a difference rather than made uniform for the sake of it.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7602 tests (+3), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## "only 1 probes", in six suites at once (2026-09-04)

<!-- Third bullet of "Floor-helper follow-ups", the first in that section with a proportionate fix — the two above it are marked EXAMINED (one wants a 106-site migration, one describes the design): "The helper's message reads 'only 1 probes' — five suites and now six carry the ungrammatical singular, which is the sort of thing nobody fixes because it lives in a shared string and every call site would have to be re-read." -->

- [DONE: 2026-09-04] [est: 9000] **The count and its noun agree, because the phrase carries `(s)` and the type demands it** — both floor messages open `only ${count} ${what}`, and `what` was a bare plural. The count a floor of one or two fails with is most often ONE, so the message a reader was likeliest to meet read "only 1 probes read as spawning".

  **Two call sites had already worked around it and left the workaround in the output.** `"module(s) matched the provenance pattern"` and `"translated page(s) — the cases below would prove little"` printed the parens and let the reader inflect. That spelling is now the INPUT: `(s)` is dropped at a count of one and becomes `s` otherwise, so the same phrase prints "1 module matched" and "0 modules matched".

  **The marker is required by the type, not by a convention.** `CountedPhrase` is `` `${string}(s)${string}` ``, so `measuredFloor(1, 2, "probes read as spawning")` does not compile — checked against a probe module before the migration, which is the only way to prove a compile-time guard is one. Four call sites moved to the marker; "probes both spawn and catch" became "probe(s) both spawning and catching", since a singular head noun would have disagreed with its verbs too.

  **And the same defect one helper over, where a case was pinning it.** `assertOnlyTheseMatch` ended `${unexpected.length} unsanctioned ${subject} ${what}` with `subject` a plural noun forty call sites write plural, and `offence-sweep.test.ts` asserted the output `/1 unsanctioned modules use the sanctioned form/`. Migrating forty sites to the marker is a sweep this did not do; putting the count behind its label — "unsanctioned modules that use the sanctioned form: 1" — needs no agreement at any count and touched one line.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7599 tests (+6), audit baseline (`no new high/critical advisories; 4 still accepted, in image-size (2), postcss (2)`), build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## Ninety-odd floors, a helper for them, and four written longhand (2026-09-04)

<!-- Fifth bullet of "Branch-sample follow-ups", taken ahead of the first because the first has no cheap fix (a prose key cannot be related to an `if` statement) and this one is a helper the repository already has: "This is the fourth suite to grow a named population with an anti-vacuous floor — probes, refusal points, skip causes, and now skip inputs. The shape is stable enough to be a helper and is written out longhand in all four." -->

- [DONE: 2026-09-04] [est: 7000] **The four anti-vacuous floors in `network-refusal.test.ts` go through `coverage-floor.ts`, which existed the whole time** — the suggestion said the shape was "stable enough to be a helper". It was already a helper: `measuredFloor` and `arguedFloor`, used by five other suites, with a doc comment about the one distinction that matters and a deliberate design (they build MESSAGES, not assertions, so a failure reports at the case's line rather than the helper's).

  **The distinction is the point, and none of the four stated it.** Measured at runtime rather than guessed: spawning probes 2 with a floor of 2, catching probes 1 with a floor of 1, spawn-and-catch probes 1 with a floor of 1 — all three MEASURED, the floor IS the count. Distinct verbs 3 with a floor of 2 — ARGUED, and now carrying the argument: two is the fewest that lets a verb tell one refusal from another.

  **What a reader gets is the diagnosis.** Break the spawn detector and the failure now reads "only 0 probes read as spawning — the floor of 2 is MEASURED … so this means the pattern stopped matching rather than that the tree shrank below a target". The bespoke sentences each said some of that; none said which kind of floor it was, which is the thing that decides what to do about it.

  **Not the first bullet of its section, deliberately.** That one asks for a row key to be related to the branch it names, and a prose key cannot be related to an `if` statement by anything short of coverage instrumentation this repository does not have. It is recorded and left.

  **And the registry came back.** This entry's `npm run verify` got a real answer for the first time in an hour: `no new high/critical advisories; 4 still accepted, in image-size (2), postcss (2)`. Those are the same four entries two CI runs this morning demanded be deleted as "no longer reported" — so the diagnosis behind the last six entries is confirmed from the other side: they were live all along, and the gate was reading a failure as an empty tree.

  `npm run verify` fully green, audit gate included: typecheck, `lint:all` 19/19, 7593 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client.

## A sample nobody could audit had quietly shrunk (2026-09-04)

<!-- First bullet of "Cause-population follow-ups": "The produced set is built from six hand-written inputs — a reader that grew a seventh branch returning a NEW cause would fail to compile (the record has no headline for it) but a reader that stopped returning `refused` for every input would only be caught if one of those six still reaches it. The inputs are a sample, and nothing says what they are a sample of." -->

- [DONE: 2026-09-04] [est: 6000] **The skip-cause sample is one named input per branch, and each row is asserted live** — the entry above compared the declared causes against the produced ones, from six expressions in an array literal. What they were a sample OF was written nowhere, and because of that the seventh branch was simply missing: a JSON payload that parses to something which is not an object had no input at all, and nothing in the tree could have said so.

  **Naming the branch is the whole of the fix.** `SKIP_INPUTS` is keyed by what each row exercises — "killed, reported as a signal", "npm printed JSON that is not an object" — so an eighth way to skip is still not covered automatically, but the omission is visible as a branch with no row rather than as an array that looks complete.

  **A row that stops producing a skip is now its own failure.** It would otherwise drop out of the set silently and the comparison would go on passing, as long as some other row still reached the same cause.

  **Stated narrowly, because the trees say so.** Deleting that branch outright turns four cases red — it makes `readAuditPayload("null")` throw — and mislabelling its cause turns exactly ONE red, and that one is `blames nobody when the output is not a report`, which existed before this entry. So what this bought is an auditable sample and a liveness floor, not a hole that was letting a bug through; the branch's labelling was already covered.

  `npm run verify`: typecheck, `lint:all` 19/19, 7593 tests (+1), build, bundle secrets, bundle size, bundle smoke, ships-to-client green; the audit gate skipped (`abandoned`) — the registry has not answered from here for an hour.

## A list a type could have supplied, written twice (2026-09-04)

<!-- First bullet of "Skip-cause follow-ups": "`auditSkipHeadline` is a `switch` over three literals and the test derives its population from the same three, written again — a fourth cause added to the type would be a compile error in the switch and an untested headline, because the case's list is a literal tuple rather than anything the type produces." -->

- [DONE: 2026-09-04] [est: 7000] **The skip causes are a `const` array the type comes FROM, so nothing writes them twice** — the entry above gave a skip three causes, spelled as a bare union. A union cannot be enumerated at runtime, so the suite wrote the same three literals a second time to loop over them: a fourth cause would have been a compile error in the headline `switch` and a silently untested headline in the case that checks they are distinguishable. Two lists for one population, in the one place a type could have supplied the list.

  **`AUDIT_SKIP_CAUSES` is the door and `AuditSkipCause` is `(typeof …)[number]`.** The headlines moved from a `switch` to a `Record<AuditSkipCause, string>`, so a cause added to the array without a headline does not compile, and the suite maps over the array rather than over a copy of it.

  **A door onto nothing is what that would otherwise buy.** A cause could be declared, given a headline, looped over by every case here, and returned by no reader for any input. So the declared set is compared against the PRODUCED one, from real inputs — a killed call, npm's error object, and three payloads that are not reports.

  **Proved against a fourth cause that nothing produces.** `"stalled"`, declared and given a headline: the tree TYPECHECKS — the compiler has nothing to say about a member no code path returns — 93 of 94 cases stay green, and the set comparison is the only thing that sees it.

  `npm run verify`: typecheck, `lint:all` 19/19, 7592 tests (+2), build, bundle secrets, bundle size, bundle smoke, ships-to-client green; the audit gate skipped (`abandoned`), the registry still not answering from here.

## Three skips in a row that read identically (2026-09-04)

<!-- First bullet of "Audit-bound follow-ups": "A skip now has two causes and one sentence shape, and only one of them is npm's fault — a killed call and a registry error both print `skipping — …`, but a kill means the bound may be too tight while an error means the registry spoke. Nothing distinguishes \"we gave up\" from \"it gave up\" for whoever reads three skips in a row." -->

- [DONE: 2026-09-04] [est: 8000] **A skip says WHO gave up, which the sentence alone never carried** — the four entries above this one taught the gate to skip rather than answer wrongly, to say which failure it was, to annotate it and to bound it. All four skips print the same shape, and they call for opposite responses. This gate abandoning the call means the registry may be perfectly fine and three minutes may be too few. npm reporting an error means the registry was reached and refused, and the bound is beside the point. Output that is not a report means neither of them said anything and npm itself has changed.

  **Three causes on the skip arm of `AuditRead`**: `abandoned`, `refused`, `unreadable`. Both timeout shapes are `abandoned` — that is the whole point of the label, since the registry never said anything in either.

  **`auditSkipHeadline` puts it at the front of the line and in the annotation title**, which is what a run summary shows. The one distinction the thing exists for is asserted directly: only `abandoned`'s headline begins "we".

  **The success arm carries no cause and the type says so**, so a reader that switches on it has to handle the report case first.

  **Shown for all three**, under `GITHUB_ACTIONS=true`: a real hang against `sleep 600`, npm's ENOTFOUND payload, and `npm ERR! code E401`. The annotation title's colon comes out `%3A`, escaped by the shared property escaper two entries ago — which is the first time that extraction has been load-bearing for something other than itself.

  **And demonstrated without asking, again.** This entry's `npm run verify` hit the 180s bound and printed `skipping (we stopped waiting) — npm audit was killed with SIGKILL after 180s`. Before this change that line would not have said which of the two it was. Advisories were NOT checked on that run.

  `npm run verify`: typecheck, `lint:all` 19/19, 7590 tests (+6), build, bundle secrets, bundle size, bundle smoke, ships-to-client all green; the audit gate skipped, annotated, cause `abandoned`.

## The gate had no bound, and a registry that never answers took the run (2026-09-04)

<!-- Fifth bullet of "Audit-soft-skip follow-ups", promoted ahead of the queue because the failure it describes was happening while the entry above it was being written: "Nothing measures how long `npm audit` is allowed to take — the failing run sat in it for 5m35s, which is the signal that the registry was failing, and the gate has no timeout, no bound and nothing that would turn a slow answer into a skip rather than a wrong one." -->

- [DONE: 2026-09-04] [est: 10000] **`npm audit` gets three minutes, and a kill is a skip rather than a partial read** — the three entries above this one taught the gate to tell npm's failure from an empty tree, to say which failure it was, and to annotate it. None of them bounded the call. What that costs is measured rather than imagined: a healthy run answers in 49 seconds, and three runs on `main` today sat in `npm audit` for 5m35s, 7m and past 13m against a degraded registry. The first two produced a WRONG answer — every baseline entry read as withdrawn — and the third was still running when this was written.

  **A bound that is hit is cheap on purpose.** It produces a skip: a run that did not check advisories and says so on the summary, not a red one and not a wrong one. Three minutes is a little under four times the healthy run, so being slightly too tight costs one annotated skip, where having no bound cost the run and, twice, the answer.

  **A killed process's output is not an answer and is not npm's account of one either.** `auditInvocationSkip` is what separates a kill from the ordinary non-zero exit `npm audit` makes whenever it finds anything — that exit carries the whole report on stdout, and treating it as a kill would skip every run of a tree that has advisories in it, which is every run.

  **Measured on a real hang**, with `npm` replaced by `sleep 600` and the bound stood down to three seconds: killed, skipped, annotated, exit 0.

  **And then measured without asking.** The `npm run verify` for this entry hit the real 180s bound — the registry was degraded from here too while this was being written — and printed `check-audit-baseline: skipping — npm audit was killed with SIGKILL after 180s`. So that run did NOT check advisories, which is exactly what the last three entries were built to make visible, and it is the one leg of the nine that did not answer.

  `npm run verify`: typecheck, `lint:all` 19/19, 7584 tests (+6), build, bundle secrets, bundle size, bundle smoke, ships-to-client all green; the audit gate SKIPPED, annotated, for the reason above.

## The guard against a copy was checking the copy's punctuation (2026-09-04)

<!-- First bullet of "Copy-guard follow-ups": "The escape-table guard looks for `"%3A"` and would miss a copy that spelled it `'%3A'` or `\u003A` — it names one character in one quoting style, which is the spelling-check shape this repository keeps retiring, applied to a rule that genuinely has no runtime handle." -->

- [DONE: 2026-09-04] [est: 7000] **The escape-table guard reads for the replacement TEXT, in any quoting** — it read for `'"%3A"'`, and the quotes in that string were the guard's own. A fourth copy written with single quotes or a template literal walked straight past the rule that exists to stop it, which is the spelling-check shape four entries this week are about, reintroduced by the entry that closed one.

  **It is still a source read, and that is defensible here.** A percent-escape has to APPEAR in the source to be produced — there is no computing `%3A` from a colon, only writing it — so the replacement text is the one handle a read gets on a rule with no runtime surface. What was wrong was reading the punctuation around it.

  **All five sequences, because a copy is not always a whole table.** The message escaper is three replacements and the property escaper is those three plus two, so a copy of the smaller one names none of the two the old rule looked for.

  **Proved on two trees the old rule was blind to**: a single-quoted copy and a template-literal one, each added to `check-inline-hex`. Both turn the guard red and it names the file; under the old rule both were green.

  **And a case that the pattern does not fire on prose** — the scan reads code with comments stripped, and three doc comments in the tree describe this escaping in words.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7579 tests (+1), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The third copy the entry did not predict (2026-09-04)

<!-- First bullet of "Annotation-module follow-ups": "Three scripts decide when to annotate and only one of them asks `runningUnderActions` — `check-inline-hex` and `check-comment-terminators` still write `process.env.GITHUB_ACTIONS === "true"` inline, which is the copy the module was created to end, left in the two places it already existed." -->

- [DONE: 2026-09-04] [est: 9000] **Both remaining producers go through `lib/github-annotations.ts`, and a case fails the fourth copy** — the entry before this one extracted the escapers when a second producer arrived, and left the two older scripts spelling the Actions check themselves. What it predicted was one duplicated comparison. What was actually there was a THIRD copy of the escape table: `check-comment-terminators` had both escapers written out again, five replacements and three, sitting fifteen lines from a `check-inline-hex` copy nobody had noticed either.

  **Byte-identical, measured rather than argued.** The old template and `annotation()` were run against the same three inputs — a plain one, a file name containing `:` and `,`, and a message with `\r\n` in it — and agreed on all three. The case that pins it uses the awkward one, because a file name with no colon in it cannot tell the property escaper from the message one.

  **Two guards, not two migrations.** `"%3A"` outside the module means an escape table has been written again; `GITHUB_ACTIONS ===` outside it means a script is deciding on its own. Both read code with comments stripped, because three doc comments quote the variable. Proved by putting the comparison back into `check-inline-hex`: 13 of 14 green, and the guard names the file.

  **One neighbouring case updated rather than worked around.** `check-inline-hex.test.ts` pinned the literal `GITHUB_ACTIONS` in the script; the property it was written for — the annotations are gated so local runs stay quiet — is what `runningUnderActions()` now says, and pinning the old spelling would have fought the guard that ended the duplication.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7578 tests (+3), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## A skip that exits 0 and leaves no mark (2026-09-04)

<!-- First bullet of "Skip-reason follow-ups": "A skip still exits 0, so a week of registry outages is a week of green runs — the reason is printed now and nothing reads it. The gate is the one leg allowed a live feed and the only one that can decline to answer without leaving a mark any later reader would find." -->

- [DONE: 2026-09-04] [est: 11000] **The audit gate's skip is a workflow annotation, so a run that did not check advisories does not look like one that did** — the two entries before this one stopped npm's failure being read as an empty tree, and made the skip say which failure it was. Both wrote to stdout. A skip exits 0, and a log line on a green run is read by whoever is already reading the log, which on a green run is nobody. A week of registry outages would have been a week of green runs.

  **`::warning title=check-audit-baseline skipped::…`**, carrying npm's own sentence plus "The advisory baseline was NOT checked on this run." It lands on the run summary, which is where a later reader looks.

  **`lib/github-annotations.ts`, because the escapers were private to the other producer.** `check-inline-hex` had them — a five-replacement rule for property values, a three-replacement one for messages, and the difference between them is what keeps a title with a colon in it from cutting the annotation in half. Writing that a second time is how two copies of one rule stop agreeing, and this repository has four entries this week about exactly that.

  **The move is proved against the OLD output.** The hex scanner's 39 cases pass untouched, and a new case writes out the annotation it produced when the escapers were private and compares — so the extraction is checked against what it replaced rather than against itself.

  **`annotation()` omits an absent property rather than writing it empty** (`file=` with nothing after it is a property GitHub tries to resolve), and `runningUnderActions` compares the exact string: another CI system setting `GITHUB_ACTIONS=1` would otherwise get raw workflow commands printed at it.

  **Shown, not assumed:** run under `GITHUB_ACTIONS=true` with npm's ENOTFOUND payload, the gate prints the sentence and the annotation — which is what the two runs that failed this morning should have printed.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7575 tests (+11), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## A skip nobody can tell from a pass (2026-09-04)

<!-- First bullet of "Audit-soft-skip follow-ups": "The soft skip is silent about WHY it skipped — one line says "no parseable audit report (registry unreachable?)" for four different payloads, and npm's error object carries a `code` and a `summary` that would name the actual failure. A skip nobody can distinguish from a pass is how run 1565 was read as the registry moving." -->

- [DONE: 2026-09-04] [est: 9000] **The audit gate says which failure it skipped for, in npm's own words** — the entry before this one stopped npm's error payload from being read as an empty tree. It skipped instead, and printed the sentence the old reader printed for everything: "no parseable audit report (registry unreachable?)". Four different problems with four different fixes, one line between them, and a line that guesses in parentheses.

  **`readAuditPayload` decides and explains in one place.** It owns the parse, so a caller cannot skip for one reason and report another — the arrangement `networkTool` and the refusal verbs were both moved to. The script keeps the `execFileSync` and nothing else; the four branches, and the sentences for them, are pure and tested.

  **npm's `code` and `summary` are quoted when it gave them**, and the quote is bounded to 120 characters: this goes to a CI log, and npm's non-JSON output on a bad day is a stack trace. A skip line nobody reads is the thing being fixed.

  **Six cases, including the one that keeps the quote short** and the one relating `isAuditReport` to the reader by BEHAVIOUR rather than by a line of source — the pin the previous entry wrote broke the moment the line it named was replaced, which is the shape this file keeps retiring.

  **CI on `main` is green again** (run 1579). Honest limit: that run's audit step took 49 seconds, against 5m35s and 7m on the two that failed, so the registry answered normally and the new skip path was not exercised there. What the run shows is that the failure is gone, not that the skip fired.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7564 tests (+6), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## npm failing is not npm reporting nothing (2026-09-04)

<!-- Not from `.suggestions.md`: found by reading the CI failure that the previous entry's merge produced on `main`. -->

- [DONE: 2026-09-04] [est: 10000] **The audit gate's soft skip asked whether npm's payload PARSES, and npm's registry failures parse** — CI run 1573 spent five and a half minutes in `npm audit` and then failed with all four baseline entries "no longer reported", on a commit whose diff was four test files. Nothing about the tree had changed. `npm audit --json` answers a registry failure with `{"error":{…}}`: valid JSON, no `vulnerabilities`, and no findings is exactly what "every accepted advisory has been withdrawn" looks like from inside `evaluateAudit`.

  **The red it prints is an instruction to delete the baseline**, which is the one edit that would let a real advisory through in silence. A gate whose failure mode is "remove your exemption list" is worse than a gate that skips.

  **`isAuditReport` is the line, and the line is the `vulnerabilities` key.** An `error` present means npm is telling you why it has no answer; the key absent means whatever this is, the answer to "which advisories are open?" is not in it. A CLEAN tree is the case that must still get through, and npm writes it `"vulnerabilities": {}` — key present, empty — so the staleness it produces is real.

  **The soft skip had no test at all.** Seven cases now: npm's error payload, an error beside findings, a payload with no key, `null`/`undefined`/`0`/`""`/`true`/`[]`, a clean tree, a report with findings, and one that measures the consequence — the error payload handed to `evaluateAudit` returns the whole baseline as stale with nothing unexpected beside it, which is the red CI printed, character for character. A last case pins the one line in `readAudit` that turns the payload away, since reverting it would leave the other six green.

  Found by reading a CI failure rather than from `.suggestions.md`: the same failure had already gone red on `main` for run 1565 in an earlier session, and been read as the registry moving.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7558 tests (+7), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The verb that tells nothing apart (2026-09-04)

<!-- First bullet of "Verb-ambiguity follow-ups": "The pair loop is quadratic over the distinct verbs and asserts nothing about the ONE that matters most — that a verb is non-empty. `""` is a prefix of everything with a space in it, and it would be registered, matched and reported as a verb by every reader here." -->

- [DONE: 2026-09-04] [est: 7000] **A registered verb has to be words the sentence can carry** — the entry before this one ruled out one verb being read as another, pair by pair. It says nothing about the verb that is read as nothing: `""` is registered, matched and reported like any other. The message becomes `A test tried to  curl`, the expected prefix becomes `A test tried to  `, and the two agree perfectly — while the pair loop asks whether some other verb opens with `" "` and finds that none does.

  **The shape is words, not merely non-empty.** The sentence puts exactly one space on each side of the verb, so an edge space or a doubled one shifts the prefix the population case builds by a character it did not intend — the same failure with a different spelling. `/^\S+(?: \S+)*$/` is all three at once.

  **Proved on three trees, and what matters is the case that stays GREEN.** Empty, doubled-space and trailing-space verbs each turn only this case red among the registry's readers; the population case that reads the verb passes on all three, which is the hole rather than an argument about it. (The behavioural http case goes red on each too, for the sentence it pins rather than for this.)

  `npm run verify` green: typecheck, `lint:all` 19/19, 7551 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A prefix test is only as strong as the verbs are unambiguous (2026-09-04)

<!-- First bullet of "Verb-registry follow-ups": "The verb is asserted with `startsWith`, so a refusal whose verb is a PREFIX of another's would pass for both — "fetch" and "fetch and follow" are one rewording apart, and the three verbs being mutually non-prefixed is a property nothing states or checks." -->

- [DONE: 2026-09-04] [est: 8000] **The registry's verbs are asserted unable to be read as one another** — the entry before this one made every point answer with the verb its own surface installs, matched with `startsWith` because the verb sits inside one sentence and there is nowhere to cut it out of. That match holds exactly while no verb is another's opening words: `"fetch"` reads `A test tried to fetch `, and a refusal saying `"fetch and follow"` starts with precisely that. One rewording away, and the check that replaced the too-weak prefix would be the too-weak prefix again.

  **The space is the whole property.** `"fetching"` is not a hole — the character after the shorter verb is `i`, and the match requires the space. So what is asserted is that no verb, followed by a space, opens another; a narrower statement than "no verb is a prefix of another" and the one that is actually true of the match it defends.

  **Equal verbs are left alone, deliberately.** `http` and `https` share one because both refuse identically, so a point of one registered against the other is not a fault worth a sentence. What the case rules out is two DIFFERENT statements one of which can be read as the other.

  **A floor, because one verb everywhere makes both cases vacuous.** With a single distinct verb there are no pairs to check and reading the verb tells a caller nothing the shared prefix did not.

  **Proved on the two-part tree the case describes**: the http verb reworded to open with `fetch`, and `http.get` registered under the fetch verb — the misregistration the population case exists to catch. That case stays GREEN, which is the hole demonstrated rather than argued, and the new case is the only thing that names it. (The behavioural http case also goes red on that tree, for the reworded sentence rather than for this.)

  `npm run verify` green: typecheck, `lint:all` 19/19, 7551 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Some refusal is not this point's refusal, again (2026-09-04)

<!-- First bullet of "Every-point-registry follow-ups": "The population case calls every point with `"curl"` and one bound, and three surfaces read that argument three different ways — a spawn refuses it as a tool, the http clients as an unparseable host, `fetch` as an unparseable URL. They agree today because the refusal fires before any of them parses anything; the day one point stops refusing, what the call does next is a different function's business and the bound is the only thing standing between the case and a real `curl`." -->

- [DONE: 2026-09-04] [est: 9000] **The refusal a registered point answers with is matched against the verb its own surface installs** — the entry before this one made every point prove it refuses, with `/A test tried to/`. That is the prefix all four refusals share, so what it proved was that SOME refusal fired. The registry is eleven names over three surfaces and the whole reason it exists is that a point can end up holding something other than what its name says; the one fault it was built for reads identically under a shared prefix.

  **This is the `spawnToolOf` argument, one level up.** Four entries back a probe was proved to let SOME refusal through, and the fix was to name the tool its own call earned. A point registered against another surface's wrapper is that same shape, and the registry made it possible in a way it had not been.

  **The verb is one expression, not two statements.** `FETCH_VERB`, `HTTP_VERB` and `SPAWN_VERB` are each passed to `refuseNetwork` and to `installRefusal` at the same site, so `refusalVerbAt` cannot drift from the sentence a caller actually meets — the arrangement `networkTool` was exported for, applied to the other half of the message.

  **Proved against a tree where only the registry is wrong.** `http.get` stays correctly patched on the module and its entry points at the spawn wrapper: the outermost check compares that wrapper against itself and passes, the behavioural http case reads the real module and passes, 26 of 27 stay green, and the new assertion is the only thing that sees it — "`http.get` answered with \"A test tried to spawn curl…\" — that is not the refusal this surface installs".

  `npm run verify` green: typecheck, `lint:all` 19/19, 7550 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Only the spawn methods were asked (2026-09-04)

<!-- First bullet of "Outermost-wrapper follow-ups": "Only the spawn methods are asked — `globalThis.fetch` and the four http client methods are installed by the same bootstrap and nothing remembers what it put there, so a wrapper around the fetch refusal is exactly as invisible as a spawn wrapper was before this. The suites stub `fetch` constantly, which is why it is harder, and also why it is the likelier one to acquire a layer." -->

- [DONE: 2026-09-04] [est: 11000] **The bootstrap remembers all eleven refusals it installs, and the outermost check asks about every one** — the entry before this one taught it to keep the wrapper it put on each spawn method, and stopped there. `globalThis.fetch` and the four http client methods come out of the same module body and were remembered by nothing, so a layer over the fetch refusal was exactly as invisible as a spawn wrapper had been the morning before. `fetch` is the point the suites stub in dozens of places, which makes it both the likeliest one to acquire a layer and the one that had none of this.

  **A point is a value and a closure that reads the current one**, because the three surfaces are not the same shape — `fetch` on `globalThis`, two client methods on each of two module objects, six on a third. `refusalAt(point)` hands back what a call site would get, so a reader asking "does this still refuse?" does not rebuild a second copy of which surface each point lives on.

  **`rewrappedRefusals()` replaces `rewrappedSpawnMethods()`** rather than sitting beside it: two readers of one registry, one of them a filter of the other, is the arrangement half this week's entries are about closing.

  **The identity check is vacuous against an empty registry**, so a second case calls every point it names and requires a refusal — read live, one argument (`"curl"` is a network tool to a spawn and not a URL to the other two), the probes' bound riding along because a spawn point that had stopped refusing would otherwise run it. It also asserts the four surfaces are all still represented.

  **A guard that matched the bootstrap's source went red about a rewording.** `verify-gate-script.test.ts` proved the http refusal existed with `/\[http, https\]/` — a literal in the loop this entry rewrote. It reads `refusalPoints()` now, plus `networkTool("curl")` for the marker that is about the tool rather than the call, so it asks the bootstrap what it patched instead of how it is spelled.

  **Proved against two broken trees, each isolating one case.** A helper that wraps `globalThis.fetch` after the bootstrap: 26 of 27 green, only the outermost case sees it, and the population case passes because the layer still refuses. The http points left unregistered: the outermost case passes (nothing to differ) and only the population case goes red.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7550 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Present is not outermost (2026-09-04)

<!-- First bullet of "Instance-count follow-ups": "The count says the body ran once, not that the refusal is one layer deep — they are the same thing only because the body is the only thing that wraps. A helper that re-wrapped `execFileSync` for its own reasons would leave the count at one and the stack two deep, which is the state the case describes in its message." -->

- [DONE: 2026-09-04] [est: 9000] **The bootstrap keeps the wrapper it installed on each spawn method, and a case asks whether it is still there** — `bootstrapInstances()` answers how many times the module body ran, which is a different question from how deep the refusal is stacked. Anything that wraps a spawn method after the bootstrap leaves the count at one and puts a layer between a caller and the refusal. That layer still refuses, so nothing goes red; it also gets to see, change or drop the call first, and `optionsPassedBy` in the same file is proof that code here does exactly that kind of swapping.

  **`rewrappedSpawnMethods()` names the methods that are no longer the bootstrap's own wrapper.** A suite that swaps and restores correctly is invisible to it, which is the point: what it answers is whether the swap is still in place.

  **Proved against a helper that wraps `execFileSync` and never restores it.** The instance count stays at one, the refusal still fires, 25 of the file's 26 cases stay green, and the new one is the only thing that sees the extra layer.

  **Also fixed: a doc comment orphaned by the previous entry.** `NETWORK_TOOLS`' comment — the one explaining why `npm` and `npx` are in the set — had the instance-count block inserted between it and its declaration, so it read as documentation for the counter. The counter block moved above it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7549 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A second bootstrap would have been invisible to every case in the file (2026-09-03)

<!-- First bullet of "Bootstrap-import follow-ups": "A suite now imports the bootstrap, and that the module body runs once was proved by hand and not by anything in the tree — the demonstration was a `console.error` added and removed. A second bootstrap instance would re-wrap every spawn method and re-assign `fetch`, and the identity cases would compare against the wrappers they found, so all of them would still pass." -->

- [DONE: 2026-09-03] [est: 9000] **The bootstrap counts its own evaluations and a case reads the count** — the entry before this one had a suite import `test-globals.ts` for `networkTool`, which turned "the same instance the `--import` evaluated" from a property nobody could break into one a specifier could. It was proved once by putting a `console.error` in the module body and counting lines. That is not a thing the tree can do again.

  **The count lives on `globalThis` under a registered symbol**, not in a module-scope `let`: two instances would each have their own `let` and each answer one, which is the answer that hides the problem.

  **Measured on a real second instance**, made the way a mis-resolved specifier would make one — the module deleted from the require cache and required again, so the body runs twice for real. Every spawn method ends up wrapped around its own wrapper and `fetch` is re-assigned. **24 of the file's 25 cases stay green**; the new one is the only thing that notices. The refusal still fires, and every identity check compares against whatever was installed by the time it read the module, which is exactly why nothing else could see it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7548 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The case reduced a spawn to a tool name, and so did the refusal (2026-09-03)

<!-- First bullet of "Tool-naming follow-ups": "`spawnToolOf` re-implements the bootstrap's reduction from a path or command line to a tool name — first token, then `basename`. The bootstrap does the same thing privately, and the two agreeing is what makes the assertion mean anything; nothing relates them, which is the `NETWORK_TOOLS` arrangement one level down." -->

- [DONE: 2026-09-03] [est: 9000] **`networkTool` is exported from the bootstrap and the case asks it** — the entry before this one taught the case to name the tool a probe's spawn earns a refusal for, and wrote a second reduction to get it: first token, then basename. Correct the morning it was written, and load-bearing only while it agreed with the reduction the refusal actually uses, which lived privately in `test-globals.ts`. That is the two-copies arrangement four entries this week are about, reintroduced by the entry that closed one.

  **The suite imports the bootstrap it is already preloaded with, and the module body runs ONCE** — measured, not assumed: a `console.error` in the module body prints one line under `--import ./__tests__/test-globals.ts`. The import resolves to the same instance, so nothing is patched twice.

  **`undefined` is a different failure and says so.** A probe whose spawn names nothing the refusal covers would never have been refused at all, so the case says that rather than asking which refusal it let through.

  **Proved against a tree where the refusal stops covering `npm`.** Both versions go red; what differs is the diagnosis. This one: "a probe that spawns names `npm`, which the bootstrap does not count as a network tool — it would run rather than be refused". The private reduction: "the `after-recording` probe did not let through the refusal for spawning `npm`", blaming a probe that behaved perfectly. In that run the probe's spawn was real and `BOUNDED` is what held it, which is the case that exists for exactly that.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7547 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Some refusal is not this spawn's refusal (2026-09-03)

<!-- First bullet of "Refusal-measurement follow-ups": "The behavioural case matches `/A test tried to/`, which is the prefix of all four refusals — it proves the probe let SOME refusal through, not that the one it met was its own spawn's. A probe whose body fetched instead would satisfy it identically, and the marker/probe pairing that would notice is a different case reading a different thing." -->

- [DONE: 2026-09-03] [est: 10000] **The refusal a probe lets through is matched against the tool its own call names** — `/A test tried to/` is the prefix every refusal in the bootstrap shares, so the case proved a refusal reached the caller and not that it was the one this probe earned. A probe that swallowed its spawn's throw and then fetched satisfied it exactly.

  **The tool is read off the recorded call.** `spawnCallOf` is the recorder `optionsPassedBy` was built around, returned whole now, because two readers want different arguments out of one call: the bound is the last, the tool is the first. `spawnToolOf` reduces that first argument the way the refusal does — `exec` takes a command line, `execFile` and `spawn` take a path — so the assertion is runtime against runtime, with no word from the source in it.

  **`spawnCallOf` takes whether to re-raise the probe's own failure.** The restore cases need it thrown; that path is their subject. A reader that only wants the arguments does not, and the one probe that throws deliberately after its spawn would otherwise have to be caught at every call site. The recorded call is complete either way — the probe had already made it.

  **Proved on one broken tree, run twice.** The probe eats its own spawn's refusal and calls `fetch` instead, so a refusal still reaches the caller. With `/A test tried to/` the case is GREEN; with the tool named it is RED.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7547 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The rule written to close a spelling hole was itself checking spelling (2026-09-03)

<!-- First and second bullets of "Swallow-rule follow-ups": "The rule matches the identifier `RECORDER_STOP` in the source, which is the shape the bound rule was retired for — a probe mentioning it in a comment, or comparing `error === RECORDER_STOP` and swallowing anyway, satisfies it." / "`PROBES` and `THROWING_PROBES` are merged with a spread, so an id in both silently collapses to one probe — the marker parity case reads `PROBES` alone and would not see it." -->

- [DONE: 2026-09-03] [est: 11000] **A swallowing probe is handed a real refusal now, not just read for the right word** — the swallow rule matched `RECORDER_STOP` in a probe's source, which is the shape `assert.match(probe.toString(), /\bBOUNDED\b/)` was retired for three entries ago, reappearing in the rule written to close a different instance of it. A probe comparing against the stop and swallowing anyway reads exactly the same.

  **The measurement is a call.** Each probe that both spawns and catches is invoked directly — no recorder installed, because `optionsPassedBy` is not in that call — so its spawn meets the bootstrap's refusal, the error a bare `catch` ate. The probe has to let it through. Nothing is spawned; the refusal is why.

  **The two probe tables are asserted disjoint.** `{ ...PROBES, ...THROWING_PROBES }` keeps ONE probe when an id is in both, and the one it keeps is the throwing one — a marker's probe could be shadowed out of the rule that judges it while the parity case, which reads `PROBES` alone, went on passing. `allProbes()` says so, and `probesThatCatch()` is the population with its floor inside it, where the filter and its anti-vacuous check used to be inline.

  **Proved against two broken trees.** A probe whose `catch` reads `void RECORDER_STOP;` — the identifier present, everything swallowed — leaves the source rule green and turns only the new case red. An id added to both tables turns both population readers red while the marker parity case passes, which is the silence the disjointness assertion was written to end.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7547 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The hole was fixed in the probe and nothing in the tree said so (2026-09-03)

<!-- Fifth bullet of "Sentinel follow-ups": "The broken tree that proves this case had to be run twice, against two versions of the probe — the demonstration lives in a `.tasks/` entry and nothing in the tree records that a bare `catch` here is a hole. The next probe that needs to swallow a spawn is one `catch {}` away from reopening it." -->

- [DONE: 2026-09-03] [est: 12000] **A probe that swallows anything has to swallow the recorder's stop BY IDENTITY, and that is a rule now** — the bare `catch` was found by reading a diff, fixed in the one probe that had it, and recorded in a `.tasks/` entry. That is the arrangement four entries this week are about: the next probe that needs to swallow its spawn is one `catch {}` away from reopening a hole where a real `npm audit` starts and the case still passes.

  **The throwing probes are a named population.** Both lived as closures inside case bodies, where no rule can read them. `THROWING_PROBES` keys them by the path they fail on — `before-spawning`, `after-recording` — and the two restore cases take theirs from it, so the source rule has something to judge and a third path gets a probe rather than a third closure.

  **The rule reads the source, like the marker and spawn rules.** Any probe whose body catches must match `CATCH_BINDING` (a `catch` that binds what it caught — `catch {}` cannot tell a stop from a refusal) and must name `RECORDER_STOP`. It is a population with an anti-vacuous floor, not the two bodies that exist today.

  **The premise case carries the third shape.** `sourcePremiseControl` grew a binding `catch`, its doc comment says three readers instead of two, and the case asserts `CATCH_BINDING` fires on it — so a runner that minified is named there rather than reported as every probe catching nothing.

  **Proved against two broken trees, each isolating the new rule**: the bare `catch` the probe actually had, and a `catch` that binds but compares `instanceof Error` — a comparison that swallows the bootstrap's refusal just as thoroughly. Only the new case goes red on either; every neighbour stays green, which is what says the rule is not a restatement of one of them.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7546 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A bare `catch` in the probe would have swallowed a real spawn's refusal (2026-09-03)

<!-- First bullet of "Post-spawn-throw follow-ups": "The new probe swallows the stop with a bare `catch {}`, which would swallow a real spawn's error identically — if the recorder were ever not installed, the probe would spawn `npm audit` for real, swallow whatever came back, and still throw `boom`, so the case would pass while a child had started. `BOUNDED` is what keeps that harmless, and it is the second case in this file whose safety rests on the bound rather than on the assertion." -->

- [DONE: 2026-09-03] [est: 9000] **The recorder's stop is one named sentinel, so a probe can swallow THAT and nothing else** — it was created fresh inside `optionsPassedBy`, which is all that function needs: it compares by identity against the one it just made. A probe cannot. The post-spawn-throw probe had to swallow its spawn's throw to reach its own, had nothing to compare against, and used a bare `catch` — which swallows the bootstrap's refusal exactly as quietly. With no recorder installed that probe would have spawned a real `npm audit`, dropped whatever came back, and still thrown `boom`: the case green while a child had started.

  **`RECORDER_STOP` is now a describe-scope constant** the recorders throw and the probe compares against, so the swallow reads "the recorder's stop, and nothing else" and anything else re-raises.

  **Proved on one broken tree, run twice.** The tree: no recorder installed over `execFileSync`, so the probe's spawn reaches the real function. With the old bare `catch` the case is GREEN — the hole, demonstrated rather than argued. With the identity comparison it is RED. (Two neighbouring cases go red on that tree either way; this one was the blind spot.)

  Nothing was spawned in either run: the bootstrap's refusal is what the probe now re-raises, and `BOUNDED` rides on the call regardless.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7545 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The restore was proved on the half of its input where nothing had been recorded (2026-09-03)

<!-- Fourth bullet of "Restore-population follow-ups": "The re-raise path is still exercised by a hand-written closure that never spawns — the throwing-probe case proves the `finally` survives a probe that fails BEFORE the recorder is reached. Nothing exercises a probe that throws after it, which is the failure a real probe would have, and the derived population is not used there at all." -->

- [DONE: 2026-09-03] [est: 10000] **A probe that throws AFTER a recorder has fired now has a case** — the existing throwing-probe case never reaches a spawn, so the state it leaves `optionsPassedBy` in is recorders installed and nothing recorded. That is not the state a real probe fails in. A probe holding its own `try` around its spawn swallows the recorder's stop, keeps going, and throws something of its own: recorders installed, one call recorded, and the re-raise path taken. Both halves of what the `finally` has to survive were being proved on the same half of its input.

  **Proved against a broken tree aimed at exactly that gap**, not a blanket break: the restore is skipped when the probe re-raises AND something was recorded. Only the new case goes red — the pre-spawn throwing case and the every-probe loop are green against it, which is the demonstration that they do not cover this state.

  **The three restore comparisons are one function now.** `spawnMethods()` snapshots the module and `assertRestored(before, why)` makes the name-by-name comparison, where three copies of one loop differing only in the sentence used to be — the arrangement that lets a fourth reader be written slightly wrong.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7545 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## One probe was checked for restoration and two probes spawn (2026-09-03)

<!-- First bullet of "Module-restore follow-ups": "The identity snapshot runs once, on `npm-audit`, and generalises to every probe by assuming they share `optionsPassedBy` — the throwing-probe case is the only one that exercises the re-raise path, and neither asserts the restore holds across the OTHER spawn probe. Two probes spawn; one is checked for restoration." -->

- [DONE: 2026-09-03] [est: 9000] **The restore case reads the derived spawn population instead of naming one probe** — it called `optionsPassedBy(PROBES["npm-audit"])` and asserted the module came back identity-clean. That is a claim about one probe read as a claim about the function: the other spawn probe puts a different body through the same seven-method swap, and "they share `optionsPassedBy`, so one is enough" is the exact assumption a case about a SHARED module should not be making.

  **The population is `probesThatSpawn()`**, the same derivation the bound case reads, so a third spawning probe is covered by existing rather than by somebody remembering to add it here — the arrangement the `BOUNDED` hand-application was already retired for.

  **The snapshot stays outside the loop.** Each probe is asserted back to the wrappers the bootstrap installed, not to whatever the probe before it left behind — a per-probe snapshot would take a recorder as the baseline and pass. The failure message names the probe, because "left `execFileSync` replaced" without one sends a reader through the whole table.

  **Proved against a broken tree** with the shape the follow-up named rather than a blanket break: the `finally` restores unless the probe's source mentions `npx`, so the first spawn probe is put back and the second is not. Exactly this case goes red; the old one-probe version is green against it, and so is every other case in the file — including the two that run AFTER it with recorders installed, which is how quiet the failure is.

  **Also on this branch: `npm update fflate`** (0.4.8 → 0.4.9, lockfile only). The audit gate turned the branch red on a `GHSA-px8p-9vwx-vf98` a lockfile bump clears, and the gate's own message is that such a finding is not a triage decision and belongs on the branch that meets it.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7544 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The restore was the one thing keeping later suites refused, and nothing watched it (2026-09-03)

<!-- First bullet of "Bound-identity follow-ups": "`optionsPassedBy` mutates the module every suite shares and only a `finally` puts it back — an assertion that threw between the loop and the restore would leave a recorder installed, and every later suite in the process would silently stop being refused. The `finally` is correct today; nothing asserts the module is the same afterwards, which is the one check that would survive somebody rearranging the function." -->

- [DONE: 2026-09-03] [est: 8000] **`optionsPassedBy` is asserted to leave `node:child_process` exactly as it found it** — it replaces every spawn name on the live module and restores them in a `finally`. That restore is the only thing between this suite and every one that runs after it: a recorder left installed does not call through, so a later spawn hits `throw stop` instead of the bootstrap's refusal, and fails with this file's name nowhere in it.

  **Two cases, because the restore has two paths to survive.** One snapshots the module, runs `optionsPassedBy` on a spawn probe, and asserts every method is identity-restored — then measures the property that stands in for end to end: a spawned `curl` still throws the refusal afterwards. The other hands `optionsPassedBy` a probe that throws BEFORE it spawns, the error the function re-raises rather than swallows, and asserts the module is restored anyway — a probe with a bug must not take the refusal down with it.

  **The identity check is not vacuous.** `SPAWN_NAMES` has a floor of six proved a case away, `before` is captured while the methods are the bootstrap's wrappers, and `optionsPassedBy` really does assign recorders over them — so a dropped or moved restore leaves recorders in place and the comparison fails.

  **Proved against a broken tree**: the `finally` body replaced with `void saved` turns both cases red, one on the identity comparison and one on the throwing-probe path.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7544 tests (+2), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Matching the word proved the word was there (2026-09-03)

<!-- Third bullet of "Probe-population follow-ups": "Matching the identifier `BOUNDED` proves the word is in the source, not that the options were passed — a probe mentioning it in a comment, or passing it as the wrong argument, satisfies the rule. The property is that a spawn cannot outlive the case, and the only thing that measures it is the control spawn." -->

- [DONE: 2026-09-03] [est: 11000] **The bound is read off the call now, and compared by identity** — `assert.match(probe.toString(), /\bBOUNDED\b/)` measured that the word was in the text. `execFileSync("npm", ["audit"], { timeout: 1 }) // BOUNDED` satisfied it and would still have started a child nothing could interrupt, which is the one thing the rule exists to prevent.

  **`optionsPassedBy` replaces every name in `SPAWN_NAMES` on the live module with a recorder that throws instead of calling through**, so the probe stops before a child exists and the case sees the argument the call actually made. Restoring is in a `finally`: a recorder left behind would disarm the refusal for every suite that runs after this one.

  **The import had to change to reach the object.** `import * as childProcess` compiles to a namespace COPY under `tsx` — measured, not assumed: a named-import call site does not see a mutation made through it. The default import is the live module the bootstrap patches, and the comment above the import says which and why.

  **Identity, and the last argument.** Every probe here spawns synchronously and a sync spawn's options are its final argument; an async one would end in a callback and fail, which is correct, because what bounds an async child is a different question. `{ ...BOUNDED }` fails too — there is one bound object and the probes share it.

  **`probesThatSpawn` is one population with the floor inside it**, read by the bound case, where the filter and its anti-vacuous `>= 2` used to be inline.

  **`BOUND_APPLIED` is gone**, along with the premise case's third assertion: the source premise now underwrites two readers, not three, and the doc comments say two.

  **Proved against a broken tree** with exactly the shape the follow-up named — the bound moved into a comment. The old rule matches that probe's source; the new one does not.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7542 tests, audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The spawn detector was six names and the module knows seven (2026-09-03)

<!-- Second bullet of "Probe-population follow-ups": "The spawn detector lists six `child_process` function names by hand — the module has more ways to start a process (`fork`, and `spawn` reached off a default import), and the list was written from the two probes it had to cover. Same shape as `NETWORK_TOOLS` being two names for the property 'a tool that reaches the network'." -->

- [DONE: 2026-09-03] [est: 8000] **`SPAWN_NAMES` is asked of `node:child_process` now** — the detector's alternation was typed out from the two probes it had to cover that morning, and `fork` was not among them. A probe that forked would have read as not spawning at all: no bound demanded, and the case that catches a missing refusal back to hanging on a real process. What a suite can call and what the module exports are the same list, so the list is not the thing to write down.

  **Two exports are not a way to start a process, and each says so in its own name.** `ChildProcess` is the class a spawn RETURNS, told apart by node's convention that a constructor is capitalised; `_forkChild` is the entry point node calls INSIDE a child it already made, told apart by the underscore. The filter is those two conventions and nothing else — no third list of names.

  **The six it replaced are the floor, not the rule.** A derivation that quietly dropped one would leave a probe reading as not spawning, which is exactly the silence the hand list produced for `fork` — so each of the six is asserted present, `fork` is asserted present as the thing the omission cost, and the two non-spawns are asserted absent.

  **Sorted longest-first**, so `execFileSync` is not matched as `exec` and then rejected for the `F`. Alternation backtracking finds it either way; the ordering means a reader does not have to know that.

  **Proved against a broken tree** three ways, one per assertion: a filter that keeps only `exec`/`spawn` prefixes loses `fork`, a filter with no case rule admits `ChildProcess`, a filter excluding `execSync` breaks the floor.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7542 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Three rules read a probe's source and none of them read it on purpose (2026-09-03)

<!-- First bullet of "Probe-population follow-ups": "Three rules in this suite now read a probe's transpiled source and each spells the dependency differently — the pairing case runs marker patterns over it, the bound case matches spawn names, the bound case also matches the identifier `BOUNDED`. All three break the same way under a runner that minifies, and each states the risk in its own comment. Nothing asserts the one premise they share: that `probe.toString()` still looks like the source somebody wrote." -->

- [DONE: 2026-09-03] [est: 9000] **The premise is asserted once, by a function written to be read** — `patternPerformedBy`, the spawn detector and the bound match all rest on `Function.prototype.toString` handing back what somebody typed. Each said so in its own comment and none of them said it to the runner. `sourcePremiseControl` is never called: its body carries one of each shape the three readers look for, and the three assertions are labelled with the rule each one underwrites.

  **The diagnosis is the deliverable, not the detection.** A `toString` that stopped answering already turns the tree red — `patternPerformedBy` returns nothing for every probe and the spawn floor of two goes unmet. What it does NOT do is say why: the messages blame the probes, so a contributor meets "the `fetch` probe is not the shape its pattern reads" and starts reading a diff that is fine. Each assertion here names the rule downstream of it instead.

  **An exact answer no single probe produces.** The control performs two of the four shapes, so `patternPerformedBy` is asserted to return `["npm-audit", "fetch"]` — a pattern that had stopped firing and a pattern that fired on everything both show up, where a one-marker control would only catch the first.

  **Two regexes stopped being written twice.** `SPAWN_CALL` and `BOUND_APPLIED` are named consts the bound case and the premise case both read; the transpiler paragraph moved onto `SPAWN_CALL`, where the pattern it explains lives.

  **Proved against a broken tree** three ways, one per assertion: the literals changed, the call name renamed the way a minifier would, the identifier renamed. Each is named by its own message.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7541 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The bound was applied to the probes that existed that morning (2026-09-03)

<!-- Second bullet of "Bounded-spawn follow-ups": "`BOUNDED` is applied to the two spawn probes by hand and nothing keeps a third one covered — a fifth marker that is also a spawn gets a probe written next to two that carry the options, which is exactly how the first four probes came to be keyed by prose. The population of 'probes that spawn' is knowable from the probe source the pairing case already reads." -->

- [DONE: 2026-09-03] [est: 7000] **A probe that spawns is found rather than remembered** — `BOUNDED` went onto the two spawn probes that existed the hour it was written, and a third would be written next to two that carry it and inherit nothing. That is the same arrangement as keying four probes by prose, filed against the same file two changes earlier, and it does not need a list: a probe that spawns says so in the source the pairing case already reads.

  **The reading found nothing on the first attempt, and the reason is worth having written down.** `tsx` compiles `execFileSync(…)` to `(0, import_node_child_process.execFileSync)(…)`, so the name is no longer against its own bracket — a closing paren counts too. Same transpiler dependency `patternPerformedBy` runs on, now stated in a second place with the evidence beside it.

  **A floor of two, because a detector that matched nothing would agree with every probe there is** — the vacuity that turned up as a real failure here rather than as a hypothetical.

  **Proved against a broken tree**: the `expo-install-check` probe with its options taken away is named by the failure.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7540 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The case that guards the refusal would have failed by hanging (2026-09-03)

<!-- Third bullet of "Marker-id follow-ups": "Two of the four probes spawn a real `npm`/`npx` and rely on the refusal firing before the process starts — if `NETWORK_TOOLS` ever stopped covering them the case would run a real `npm audit` in a test, which is slow, network-dependent, and exactly what the whole file exists to prevent. The failure mode is a hang rather than a red assertion." -->

- [DONE: 2026-09-03] [est: 8000] **A spawn probe cannot perform the spawn it is probing for** — two of the four probes hand a real `npm` and a real `npx` a real command and rely entirely on `NETWORK_TOOLS` refusing them before the process starts. Take either name out of that set and the probe does not fail: it RUNS. `npm audit` against the registry, inside `npm test`, on the leg whose whole subject is that no suite reaches the network. The case that guards the refusal would have reported its absence by being slow.

  **A killed spawn turns a hang back into an assertion.** `BOUNDED` gives the child a millisecond and SIGKILL, so `execFileSync` throws for the wrong reason and `assert.throws` reports a message that is not the refusal's. It is load-bearing only in the failure: while the refusal works the probe never reaches a spawn at all.

  **The control is a spawn the refusal does not touch.** A node process that would never exit on its own, run through the same options — if the bound were wrong this is the case that hangs, and it hangs on a spin loop rather than on a suite quietly asking npm's registry a question. Time is asserted as well as the throw, because an `execFileSync` that threw after ten seconds would satisfy the throw and not the property.

  **Measured with the refusal removed**, which is the only way to see the shape this is about: `NETWORK_TOOLS` cut to `curl` and `wget` turns the run red in 2.8 seconds with two assertion failures, where the same edit before this change had the suite spawning npm and npx for real.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7539 tests (+1), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Freeing the prose to be reworded also freed it to be deleted (2026-09-03)

<!-- Second bullet of "Marker-id follow-ups": "`why` is now the only field a failure prints and no rule keeps it a sentence — the id took over the identifier job, which frees the prose to be reworded and also to be shortened to the id. A marker whose `why` reads 'fetch' would print a failure message that says less than the pattern does." -->

- [DONE: 2026-09-03] [est: 9000] **A marker's `why` is held to being a sentence, because it is the entire failure message** — `networkMarkerHit` answers with `why` and nothing else, so every finding this scan reports is that string beside a filename. Giving markers an `id` the day before took the identifier job off the prose, which was the point; what it also did was make shortening the prose to the id free. `why: "fetch"` would have printed a finding that says less than the pattern a reader could have looked at instead, and nothing would have gone red.

  **Two rules reading one regex from opposite directions.** `MARKER_ID_SHAPE` is exported now rather than written into the id case: every `id` matches it, no `why` does. A single regex asserted in both directions is the whole statement that the two fields have different jobs — two copies of it could drift into agreeing.

  **Three ways a sentence collapses, not one.** Shaped like an id; fewer than three words; the id with its dashes swapped for spaces, which is the same collapse spelled so the first rule misses it. Plus distinctness, since two markers printing one sentence leave a reader unable to tell which shape was found — the id is what tells them apart in the wiring and the reader never sees it.

  **Measured on the real tree, where the scan already has findings.** The scripts outside the gate are the population the anti-vacuous case leans on — `db:find-duplicates` and `sentry:check` call `fetch`, `check-expo-install` asks the registry about versions — so what a reader of those findings is handed is asserted rather than inferred from a return type, and it is TWO different sentences, which is the distinctness rule measured instead of stated.

  **Proved against a broken tree.** `why: "fetch"` planted on the fetch marker turns two of the three cases red, including the one that reads the real scripts.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7538 tests (+3), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## A key relates two things by name, and nothing related them by meaning (2026-09-03)

<!-- First bullet of "Marker-id follow-ups": "The id is checked for shape and nothing relates it to the pattern it names — `fetch` could be the id of the http-client marker and every case would pass, because the parity comparison is between two key sets and the probe lookup follows the same key. The pair that must agree in MEANING is `pattern` and probe, and that is still asserted by nobody." -->

- [DONE: 2026-09-03] [est: 12000] **A probe is judged against its marker's `pattern`, not only against its `id`** — giving markers a short id the day before fixed the reword problem and left the parity arrangement resting entirely on a string. The population case compares two key SETS, the refusal case looks a probe up by the same key, and the shape case asks the id to be lowercase and distinct. Swap the `fetch` probe with the http-client one and all three stay green: both throw, both throw `A test tried to`, and neither is asked what it actually performs.

  **The one text both halves can be judged against is the probe's own source.** `pattern` describes what the shape looks like in a script's text and the scan reads exactly that; `probe.toString()` is a text that either is the shape or is not. `patternPerformedBy` returns the markers whose pattern fires on a probe, and the case demands it return the probe's own marker and NOTHING else — a probe filed under the wrong marker names the marker it really performs and goes red.

  **Two probes were not the shape they were filed under, which is the finding.** `fetch` called through `installed`, the captured refusal — invisible to `/\bfetch\s*\(/`, and to any scan, which is the whole point of the marker. It reads `globalThis.fetch` at call time now, which is both what the pattern reads and what a real suite does; the cases that need the pre-stub capture still have it. `http-client` is the marker whose pattern reads an import SPECIFIER, and a probe cannot import at call time, so it takes the client by that specifier through `httpClient("node:https")`. Its doc says what that does not prove: nothing here claims an import of `node:https` is refused, and nothing here needs to.

  **Exactly-one is self-controlling and there is a planted offender anyway.** A `patternPerformedBy` that matched everything or nothing fails the deepEqual for every marker rather than passing quietly — but a rule nobody has watched refuse anything reads as passing whether it works or not, so a case exchanges the two probes that used to be indistinguishable here and asserts each is named by the other's marker.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7535 tests (+2), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The probes were keyed by a sentence written to be reworded (2026-09-03)

<!-- First bullet of "Probe-parity follow-ups": "The probes are keyed by the marker's prose label, so a reworded label is a red run about nothing — 'an `npm audit` of the registry' is a sentence written to be read in a failure message, and it is now also an identifier. Renaming it for clarity breaks the parity case, and the fix is a copy-paste that teaches nobody anything. A short id beside the label would separate the two jobs." -->

- [DONE: 2026-09-03] [est: 7000] **A marker has an `id` and a `why`, because the two have opposite lifetimes** — the parity case that pairs a runtime refusal with every scan marker keyed its probes by the marker's prose. That prose exists to be read in a failure message, which is exactly the thing somebody rewords for clarity — and doing so turned into a red run about nothing, whose fix is a copy-paste that teaches nobody anything.

  **`NETWORK_MARKERS` is objects now**, `{ id, why, pattern }`, where it was a `[label, pattern]` tuple. `networkMarkerHit` still answers with the sentence, so every reader of the scan is unchanged; `PROBES` is keyed by `npm-audit`, `expo-install-check`, `fetch`, `http-client`, and the refusal case still REPORTS the sentence — the failure names the shape a reader recognises and the wiring names something stable.

  **A third case, because an id can drift back into being one.** Distinct, and `^[a-z][a-z0-9-]*$` — two markers sharing an id would hide a missing probe behind a passing key comparison, which is the failure the parity case exists to prevent.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7533 tests (+1), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The shared reader gave the ESM walk an edge node cannot have (2026-09-03)

<!-- Third bullet of "Shared-specifier follow-ups": "`repoLocalSpecifiers` now follows a relative `require` and the reporter graph is ESM-only — node's default loader is what the guard models, and a `require` in that graph would not load at all. The shared reader returns it, the filter keeps it, and the walk would report a file that node never asked for as missing rather than as impossible." Also its second bullet: "`kind` is returned and no caller reads it." -->

- [DONE: 2026-09-03] [est: 6000] **The reporter graph drops a `require`, and that is what `kind` is for** — making the four import scanners one reader gave `repoLocalSpecifiers` a fifth shape it had never had. The guard models node's OWN loader, which loads that graph as ESM: a `require` there is a call somebody built with `createRequire`, resolved at runtime and absent from the module graph. The text walk would have claimed the edge, node's measured graph would not have carried it, and `missingFromWalk` would have reported the difference as a missing import rather than as the impossible one it is.

  **The first caller to read `kind`.** It was filed the same day as a field nothing consumed — carried, asserted in one case, load-bearing nowhere. The rule that needed it is the one whose subject is a specific loader, which is exactly the distinction a specifier string cannot carry.

  **Three cases pinning what the shared reader changed for this walk**, since the two differences were argued in a doc and asserted nowhere: a `require` ignored, a commented-out import ignored (it was followed before, and node's loader never saw it), a single-quoted specifier read (the old pattern's blindness there was a rule about prettier, not about node).

  `npm run verify` green: typecheck, `lint:all` 19/19, 7532 tests (+3), audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## Four rules read the module graph and no two agreed what an import is (2026-09-03)

<!-- Fourth bullet of "Import-address follow-ups": "Three modules now extract module specifiers with three different patterns — `check-analytics-imports.ts`, `check-problem-phrasing-imports.ts` and now `reached-from.ts`, each with its own idea of which spellings count. `lib/check-reporter-graph.ts` has a fourth for relative imports only. Nothing relates them, which is the shape `source-files.ts` was extracted for after eighteen copies." -->

- [DONE: 2026-09-03] [est: 16000] **`lib/import-specifiers.ts`, and the two rules that were quietly checking less than they claimed** — every structural rule here about the module graph eventually writes the same regex, and by the fourth copy no two agreed. The differences were never decisions: each pattern described the shapes its author's tree happened to hold that day, which is exactly how the eighteen source-tree walks came to disagree about `node_modules`.

  **Two of them were wrong, and the shared reader is what says so.** `check-analytics-imports` matched `from`, `require(` and `import(` and NOT the side-effect `import "@/lib/analytics-events"` — a UI file could drag the taxonomy and its description strings into the bundle without naming a binding, invisible to the guard whose whole subject is that. `check-reporter-graph` matched double quotes only (a rule about this repository's prettier config, not about node) and stripped no comments, so a commented-out import was an edge the text walk followed and node's loader did not.

  **Records, not strings, because two callers need more than the specifier.** `kind` (static / side-effect / dynamic / require) and `index` — an offset into the source as passed in, which comes free from `stripComments` blanking to spaces, and is what lets the analytics guard keep printing the real `file:line` and the offending line's text. `specifierIsPackage` and `specifierEndsWithModule` are the two membership rules the callers had written by hand, one by prefix and one by suffix.

  **`check-problem-phrasing-imports` stays where it is**, deliberately: it needs the BINDINGS a clause introduces, not the specifier, which is a different question with a different answer shape.

  **The suite that broke was asserting a proxy.** `strip-comments.test.ts` held four guards to importing the stripper DIRECTLY, as a stand-in for "not through the bundling module"; a guard that reaches it through the shared scanner satisfies the property and failed the proxy. It states the property now — not via `env-inlining` — and accepts either sanctioned address.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7529 tests (+11), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The address was checked by a substring, and only half of it was checked (2026-09-03)

<!-- Second and third bullets of "Reachability-address follow-ups": "'Still names the package' is a substring match on a file's text — `source.includes("nanoid")` passes on a comment, a variable called `nanoidLike`, or an import that was left behind after the call was deleted." and "Nothing checks that `reachedFrom` names ALL the call sites, only that the ones named exist — an entry could list one file while the package is imported in six, and the acceptance would read as argued." -->

- [DONE: 2026-09-03] [est: 14000] **A named call site has to IMPORT the package now, and has to be all of them** — `reachedFrom` is what an entry that ships is held to instead of the bundle, and the thing holding it was `source.includes(pkg)`. That passes on a doc comment about the removal, on a variable called `nanoidLike`, and on an import left behind after the call it served was deleted — the dead address reading as live, which is the one shape the check exists to catch.

  **`importsPackage` asks the question the substring was standing in for.** Comments blanked first (`stripComments`, so nothing shifts), all five spellings of taking a module read by one pattern, and the specifier held to being the package or a subpath of it — so `nanoid-esm` is somebody else's package rather than a match. A type-only import counts: a call site that got narrower is still one the reader should be sent to.

  **The direction nothing asked at all.** The old case walked the LISTED paths, so an entry naming one file while the package was imported in six read as fully argued — the five unread call sites being exactly the ones where the argument might not hold. `evaluateReachedFrom` sweeps the source tree and reports both directions from one pass: a listed path that does not import (`dead`, a deleted file included) and an importer that is not listed (`unlisted`). An entry that has drifted usually has both, which is the case the suite pins.

  **What it cannot say, in the module's own doc.** That the argument at those addresses is right — a call site passing the package attacker-controlled input reads identically to one passing it a constant. And the ways out of any text match: a runtime-built specifier, a re-export elsewhere, a bundler alias. All three surface as an entry naming a path the sweep does not, never as a silent pass.

  **Vacuous today, like the rule it sharpens.** No `shipsToClient: true` entry exists, so every case is a fixture; `nanoid` was the only one there has ever been and it was fixed rather than re-argued.

  `npm run verify` green: typecheck, `lint:all` 19/19, 7518 tests (+15), audit baseline, build, bundle secrets, bundle size (18.2 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.
