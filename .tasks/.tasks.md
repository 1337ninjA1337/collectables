## A cursor named max, one microsecond short of it (2026-08-29)

<!-- Not from .suggestions.md: found by the "scan the codebase for related bugs" step, following the same merge path the deduper fix came out of one level up — into the delta pull that feeds it. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-29] [est: 13000] **`maxUpdatedAt` compared milliseconds and stored microseconds, so the delta pull re-downloaded the same rows every sync** — the reducer's own doc block explains why it parses instead of comparing strings: mixed offset formats from PostgREST would sort wrong lexicographically, and that reasoning is correct. What it does not say is that `Date.parse` resolves to MILLISECONDS while `updated_at` is a `timestamptz`, which Postgres keeps to microseconds. `.123456` and `.123900` parse to one number, `ms > bestMs` is false, and the function returned whichever of them the array happened to hold first — a function named `max` that does not return the maximum. **Same-millisecond rows are the ordinary case, not a corner.** `moddatetime` stamps every row touched by one statement with the same `now()`, so any multi-row write — adding several items, the legacy-id re-upsert, a bulk visibility change — produces a batch of them. **The cost lands on the caller, because the cursor is spent as `updated_at=gt`.** A cursor one microsecond short of the batch maximum brings every row above it back on the next pull, and the one after that, until the batch narrows to a single row: it converges silently, having re-spent exactly the egress that `BE-28c`'s narrowed column projection was written to save, and having made the stored cursor depend on row order, so two devices reading identical rows persisted different cursors. The fix is a total order of the shape this tree has been giving its comparators all week: compare the parsed milliseconds, and break a tie on the raw string. The tiebreak is consulted ONLY between two timestamps already known to name the same millisecond, so the prefix through that millisecond is identical and all that remains to compare is the fractional tail — the one place lexicographic order is exactly right, and the sub-millisecond precision `Date.parse` threw away. Two spellings of one instant (`…Z` against `…+00:00`) compare arbitrarily and harmlessly: either is a cursor naming that instant. Three cases, each of which fails against the old reducer or pins a rule the new one must not break — the true maximum out of three same-millisecond rows under three input orders, the cursor never moving BACKWARDS inside a millisecond, and `.099999` still losing to `.100000` where a plain lexicographic max would invert them. The neighbouring keyset pager was checked and is not affected: it carries the raw string into `lte.` and dedupes by id, so nothing there is parsed. 2 files, +3 cases. `npm run verify` green: 6809 tests, 1508 suites, 17/17 guards, build exported. No DB migration, no creds.

## The deduper's own two passes disagreed about determinism (2026-08-29)

<!-- Not from .suggestions.md: found by the "scan the codebase for related bugs" step, reading the merge path the last four runs' comparator work kept citing as the source of id-keyed nondeterminism. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-29] [est: 16000] **`dedupeItems` pass 2 picked its survivor by arrival order, and pass 3 in the same function documents why that is wrong** — pass 3's comment says the survivor is chosen "tie-broken by earliest createdAt then smallest id, so every device — and the MANUAL-TASKS.md DB cleanup — picks the same survivor". Pass 2 makes the same choice over the same rows and had `isUuidV4(item.id) && !isUuidV4(existing.id) ? item : existing` — a rule that decides only when EXACTLY ONE side is uuid-keyed, and otherwise keeps whichever arrived first out of `byId.values()`, which is the id-keyed cloud-merge order. **The tie is the common case, not the exotic one.** The duplicate this pass exists for comes from the BE-5 legacy-id rewrite, which minted a FRESH UUID for every copy it re-upserted — so both rows are uuid-keyed and the guard is false. The two rows agree on the identity key (`collectionId | title | createdAt | createdByUserId`) and on nothing else: description, cost, photos and tags are whatever that copy happened to carry, so two devices holding identical data kept different item bodies, and neither had any way to notice. The fix is one call: pass 2 goes through `pickContentSurvivor`, which is already the total order pass 3 promises. **The second finding is in pass 3's output, and it is why the file's stated invariant was not one.** It grouped survivors into per-contentKey buckets and emitted `for (const bucket of byContent.values()) out.push(...bucket)`, so a pair sharing a content key that `canCollapse` REFUSES — two genuine purchases with distinct timestamps and no shared photo — was pulled together and everything between them pushed back. The list is stored newest-first and `getItemsForCollection` falls through to that order for every item the user never dragged, so the reorder is visible. Survivors are now a flat list in first-seen order with the content map holding indices into it; the `find` returns a stored index that is legitimately 0, so the miss is `undefined` rather than a falsy check. **The order case needed a fourth row to be a test at all** — with nothing collapsing, the function returns the input array by reference and the reordering path is never entered, so the obvious three-row version passes against the bug; an exact-id duplicate in the middle is what makes it reachable. Mutation-checked both ways: restoring the old identity tiebreak turns the permutation case (one input order keeps `UUID_A`, the other `UUID_B`), and restoring the bucket emit turns the order case. The uuid-beats-legacy rule is pinned in both directions so the new tiebreak cannot outrank it. 2 files, +3 cases. `npm run verify` green: 6806 tests, 1508 suites, 17/17 guards, build exported. No DB migration, no creds.

## The carve-out that let one hole through, and the question asked in four places at once (2026-08-29)

<!-- Promoted from .tasks/.suggestions.md, "File-exclusion-ban follow-ups" (2026-08-29), the first entry (a live hole the morning's rule could not see, recorded by hand), the second (`!new Set([…]).has(x)` as the fourth spelling) and the third (three suites hand-writing the same honesty block). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-29] [est: 19000] **`INLINE_WALK_EXCLUSION` — the rule was narrowed on two guesses and one of them was wrong** — the morning's widening decided hole-vs-skip by asking whether the literal names a FILE, and drew one further line: a LONE `!==` is a whole predicate, so it narrows a walk rather than holing a rule, and only the `&& `-conjunct form was banned. Both narrowings turned out to hide the same live hole. `lint-guard-partial-root` builds `SINGLE_ROOT_LOCKS` from `floorWalks().filter((w) => w.checkName !== "check-problem-phrasing-imports")` — a lone `!==`, over a literal that names a guard rather than a path, and a documented cost decision rather than a walk narrowing. **Its reason was written down better than most; the half that was missing is the one prose cannot supply.** Nothing asked whether the name is still IN `FLOOR_WALKS`: drop that guard from the table or rename it and the filter goes on excluding nothing, reading exactly as it does now, in a lock table whose whole job is to be a subset with a reason. Both narrowings are gone. The literal half gains a second class — every guard here is `check-…`, so guard ids are as recognisable as paths and need no judgement — and the conjunct requirement is dropped outright: **measured across the suites it was excusing that one site and this suite's own fixture, so it bought nothing.** `!new Set([…]).has(` joins as the fourth spelling, banned with zero offenders because it is what a reader reaches for once the other three are closed, and narrow enough that all 36 negated set reads in 23 suites — every one over a NAMED set — are untouched. **The other half of the day is a helper that was already there.** Naming the four holes this morning left four suites hand-writing the same two asserts (the entry is in the walk; the entry still offends), in the same order, in nearly the same words — which is precisely what `assertExemptionsHonest` exists to be the one place for. Nothing was stopping them: it takes a list and a one-entry hole is a list, it takes a `walk` and three of the four sweep something other than the suites, and it brings the `expected` tripwire the hand-written copies had no equivalent of. All four adopt it — `analytics-pii`, `check-error`, `guard-scan-dirs` and the new lock-table hole, whose walk is guard NAMES rather than paths and which the helper takes without comment. The `useNativeDriver` sweep gets the control it never had, and needs more now than before: dropping its raw read took its match count in the walk to zero, so it was asserting an absence over a tree where the spelling had become extinct — four modules pass the option, floor three. Mutation-checked: restoring the guard-id exclusion turns the sweep naming that suite. 6 files, +2 cases, -1 (two parametrised cases became one helper call). `npm run verify` green: 6803 tests, 1508 suites, 17/17 guards, build exported. No DB migration, no creds.

## The hole with three ways out, and the skip that had stopped skipping (2026-08-29)

<!-- Promoted from .tasks/.suggestions.md, "Named-hole follow-ups" (2026-08-28), the first entry ("The ban reads `!["a"].includes(x)` and not the three other spellings of the same decision") and the third ("The premise floor is 15 over 24 membership checks, and `web-security-headers` writes 20 of them"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-29] [est: 24000] **`INLINE_FILE_EXCLUSION` — the rule's name promised the decision and its pattern delivered one syntax for it** — yesterday's ban made an inline exclusion a named one, anchored on `!["a"].includes(x)` because that is how the stale metabase hole happened to be written. Three other spellings say exactly the same thing and none of them matched: `file !== "lib/x.ts" &&` as a conjunct in a filter predicate, `if (file === "lib/x.ts") continue;` in a loop, and `… return false;` inside a `.filter` callback. **The widening is narrowed on the LITERAL, not on an exemption list, and that is the whole design.** Ten exclusions in this tree are written in the banned syntax over a VALUE — `if (code === "en") continue;` in three i18n suites, `mode !== "default" &&` in the render helper — and every one is the rule rather than an escape from it, so a rule that caught them would have needed ten holes to excuse ten non-holes. A hole is an exclusion from a WALK and a walk here yields paths, so `FILE_LITERAL` (a path segment, or a source extension) is what tells the two apart: **9 matches in 6 suites, zero false positives, and the ten value skips left alone.** All six are fixed by naming: `TRACK_EVENT_WRAPPER`/`SENTRY_WRAPPER` in `analytics-pii` (the sentry skip had already been COPIED — two loops skipping one module through two independent lines), `HELPER` in `check-error` (one decision in two different spellings), `DECLARING` in `declared-shape` and `guard-scan-dirs`, and `CONTENT_TYPES_PART` exported from `lib/powerbi-template.ts`, where the builder and the suite each excluded the manifest part by spelling a bracketed literal neither reader would compare. **Two live findings came out of it, both of the same class as yesterday's.** `check-error`'s offender sweep skipped `lib/check-error.ts` for building a refusal line by hand, and it does not: `buildsARefusalByHand` asks for a NAME in front of the separator and the helper declares `CHECK_ERROR_PREFIX = ": ERROR — "` bare. The skip was correct against the FIRST version of that rule and was left behind by the narrowing that made it about a built line rather than a mentioned one — excusing nothing, in the one file where a second hand-built line would be least conspicuous. The honesty case written for it is what turned it red. And `web-console-noise` swept RAW source, so a module that merely named `useNativeDriver: true` in prose was an offender — which `lib/animation-driver.ts` does, twice, in the doc block explaining why the constant exists, and its hand-written exclusion was the only thing standing between the guard and a false report about its own subject. Both halves close by reading `sourceCode`: the sweep stops reading prose, the hole has nothing left to excuse and is deleted, and a real `useNativeDriver: true` inside the driver module — the one file that was exempt from the rule it declares — is now an offender like any other. The membership premise was recounted at the same time, per the third suggestion: it was a floor of 15 over 27 occurrences of which `web-security-headers` writes 20, so one file's STYLE was five-sixths of the control and collapsing its twenty checks into a table would have turned the case red with nothing wrong. Six suites, floor four. The second rule gets the matching premise its two halves need — ten value skips in ten suites prove the SYNTAX half still reads, whatever the narrowing then says. Mutation-checked: restoring any one of the six inline exclusions turns the new sweep naming that suite, and the check-error honesty case reproduced the tree's own stale skip before it was removed. 8 files, +7 cases. `npm run verify` green: 6802 tests, 1508 suites, 17/17 guards, build exported. No DB migration, no creds.

## The tied path unpinned while the untied one was pinned (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "After rule 3" (2026-08-28), the "Give `getItemsForCollection`'s `sortOrder` branch a tiebreak too" entry — filed the same day on noticing that the two earlier total-order tasks had pinned the fallback of this comparator while leaving its primary branch untied. -->

- [DONE: 2026-08-28] [est: 14000] **`collection-order-comparator.test.ts` — the comparator every collection screen renders had no test, because it lived where tests cannot reach** — `getItemsForCollection`'s sort was written inline in `CollectionsProvider`, which pulls React Native and cannot be mounted under `tsx --test`, so its rules were pinned only by a source regex asserting the comparator is CALLED. That says nothing about what it does, and it hid a shape that was exactly backwards: the earlier work had made the `createdAt` fallback a total order (`byCreatedAtDescThenId`) while leaving the `sortOrder` branch above it tied — the untied path deterministic and the tied one not. **Duplicate `sortOrder` is reachable, though not common.** One device cannot produce it: `reorderItemsInCollection` writes a dense 0..N-1 run over the whole collection, and the drag handler appends the unrendered tail precisely so items below the `useChunkedList` page boundary keep their places (that hole was already closed, and the check confirmed it). Two devices can: the same owner reordering on phone and web merges per-item by `updated_at`, so one surviving index from each write can land on the same number, and the collision then fell through to the incoming array — the id-keyed cloud-merge order. The fix extracts the comparator to `byCollectionOrder` in `lib/collections-helpers.ts` (the module whose whole premise is "no React, testable in isolation") and ties on `id` in both tiers. **The extraction is the larger half of the value**: three rules that the screen depends on now have one definition and ten real assertions instead of a grep, including that `sortOrder: 0` counts as present — the falsy-check bug would demote the item the user dragged to the very top, the one position users notice. An adoption case asserts the provider delegates and has not re-inlined a copy. Two of the ten fail against the pre-fix comparator. One existing assertion in `collections-helpers.test.ts` was widened: it matched the WHOLE import clause for `userScopedCollectionId`, so a second symbol from the same module broke a test whose actual subject is that the id comes from the helper rather than a template string.

## The rule that said "total" and was not (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Total-order tiebreaks" (2026-08-28), the "Give `applyItemFilters` the same total order" entry — deferred out of the tiebreak task earlier the same day on the grounds that it changes a comparator with its own documented rule set and three test files, so it wanted its own diff. -->

- [DONE: 2026-08-28] [est: 12000] **`item-filters-sort-total-order.test.ts` — a comment claimed the property, the code did not have it** — `applySortMode`'s doc block said "Two rules make the cost/acquired axes **total**", and rule 2 (tie breaks on the collated title) does not do that. **The gap is wider than a reader of that line would guess, because two things compound.** The collator runs at `sensitivity: "base"` on purpose — it is what makes "Écu" collate next to "ecu" for the ru/be/pl/de/es users the axis exists for — but that same setting means "Écu", "ecu" and "ECU" are EQUAL to it, so rule 2 leaves every case- or accent-variant pair tied, not merely the literal duplicates. And the literal duplicates are the common case regardless: two copies of one card, same title, same price, is what a collection app is full of. Tied rows then fell through to the incoming array order, which arrives from the same id-keyed cloud merge as every other list in the app, so one collection rendered in two orders on two devices with no data difference to explain it. Rule 3 — remaining ties break ascending on `id` — closes all three tie sets (the `name` axis, the cost/acquired value tie, and rule 1's both-keys-null branch, which fell through to the title alone and so tied on identical titles too). Ties break ASCENDING under both directions, matching the `ThenId` comparators: the tiebreak is a machine ordering, not a secondary presentation axis, so flipping the primary must not flip it. **`"default"` is deliberately untouched** — there the user's drag order IS the answer, and callers depend on the identity-return for React memo equality. The tests assert the order is total (one output per input set across 12 deterministic shuffles) rather than spot-checking pairs, and four of the eight were confirmed to fail against the pre-fix comparator and pass after — a tiebreak test that passes both ways would be worth nothing. Three further cases pin that the new rule never outranks an existing one: missing keys still sink last in both directions, the title tiebreak still precedes the id, and the primary key still precedes both.

## A consistent comparator is not a pinned one (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Timestamp comparator consistency" (2026-08-09), the "Give `ChatPreview` a stable secondary sort" entry, extended to the whole class after a sweep of all 24 comparator call sites. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. The sibling suggestion in that same block (pin the title collator's `sensitivity`/`numeric`) was found already implemented at lib/item-filters.ts:229 and is not re-filed. -->

- [DONE: 2026-08-28] [est: 21000] **`sort-helpers-tiebreak.test.ts` — the ordering was made consistent, not deterministic** — the 2026-08-09 work fixed comparators that returned non-zero for ties, which made `Array.prototype.sort`'s ES2019 stability guarantee apply. That is only half the property. Stability preserves the **input** order, and it says nothing about what the input order was — and not one list in this app arrives in a meaningful one. `buildChatPreviews` iterates `Object.entries(messagesByChat)`, so a same-millisecond tie resolved to AsyncStorage rehydration order on a cold start and append order on a warm one: the chat list reordered itself across a reload with no data change, which is the symptom the original task set out to kill and did not. The marketplace surfaces sort listings that came through id-keyed `Map` merges, where insertion order tracks whichever rows the last cloud delta happened to return. **The cases where this is user-visible rather than cosmetic are the three that then `.slice(0, limit)`** — `selectRecentItems` (home rail, top-6), `recentlySoldListings` (top-12) and `priceHistoryForTitle` (top-10) — because there a tie AT THE BOUNDARY decides which row a user sees at all, not merely in what order. A fourth, `buildChatPreviews`, reads `lastMessage` off the end of its per-chat sort, so two messages sharing a millisecond put a different preview line under the same chat on each device. The fix is a total order: `tieBreakById(primary, getId)` plus `byCreatedAtAscThenId` / `byCreatedAtDescThenId` in `lib/sort-helpers.ts`, adopted at all ten tie-prone sites across `chat-helpers`, `chat-context`, `home-helpers`, `collections-context` and `marketplace-helpers`. Ties break ASCENDING by id under both directions on purpose — the tiebreak is a machine ordering, not a secondary presentation axis, so flipping the primary must not flip it. **The drag-ordered path is deliberately untouched.** `getItemsForCollection` sorts on `sortOrder` first and only falls through to the timestamp for items the user never dragged; the plain `byCreatedAtAsc`/`Desc` stay exported for any site where the incoming order genuinely IS the fallback the user expects. Verified by permutation rather than by spot check: each of the three top-N surfaces is asserted to return one identical result across 12 deterministic shuffles of its input, which is the assertion shape that would have caught the original bug and the one a case-by-case test cannot make.

## The hole with no name, banned by the machinery it escaped (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Inline-hole follow-ups" fifth entry written today ("Two live stale holes were found today and both had been left by a set that shrank ... Neither was noticed by the suites that own them; both were found by asking a question no case asks. Nothing in the tree runs that question on a schedule") and its second entry (the early-`continue` shape neither audit grep would see). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 6000] **`inline-exclusion.test.ts` — a hole gets a name, because a hole written at the point of use goes stale in private** — the day's two stale exemptions were the same shape from two directions, and only one of them was FINDABLE. The hex-width hole had a name, the name had a reader, and the sweep helpers now hold a rule's holes against the cases policing them. The metabase hole was `.filter((name) => !["item_added_users"].includes(name))` — an array literal inside a call, with no declaration and nowhere for a reader to attach — and it is invisible to every mechanism here and to the reviewer, who reads it as part of the expression rather than as a decision somebody made. Naming does not make a hole honest; it is the precondition for everything in this tree that does. So the shape is banned, by the sweep helpers the whole day went into: one `assertNoOffenders` over all 479 suites, zero offenders. **The un-negated form is deliberately untouched.** `["script-src"].includes(name)` is a membership CHECK — 24 of them are written inline here and every one is correct, `web-security-headers` asking whether a directive is `script-src`, `uuid` asking whether a nibble is one of the four legal variants. Those are the assertion rather than an escape from it; only the negated form is an exclusion, so the rule is anchored on the `!` and a case pins both spellings against it. The suite exempts ITSELF, for the reason `offence-sweep.test.ts` does: its rule is its fixture, and the negative control has to write the banned spelling out to assert the rule reads it. That hole is policed by the same case — it fails the moment this file stops containing an example — and the new suite satisfies, unprompted, both checks shipped earlier today: its rule is a named const, and its exempt list has a reader. The premise case is the third floor: a rule anchored on `.includes(` that stopped matching the call spelling reports a clean tree while the walk is full, which the empty-walk refusal cannot see, so the 24 membership checks are the control that proves the scan still reads. Mutation-checked two ways: re-adding the metabase exclusion turns the sweep naming that suite (the tree's own bug, reproduced a third time), and deleting the negative-control fixture turns the self-exemption's honesty assert. 1 file, +3 cases. `npm run verify` green: 6761 tests, 1493 suites, 17/17 guards, build exported. No DB migration, no creds.

## The hole written inline, and the plural over a list of one (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Allow-list-audit follow-ups" first entry written today ("The audit that found `ALLOWED_LOCAL` was a grep over const NAMES, and a hole named anything else is invisible to it — a list called `SKIP`, `KNOWN`, `LEGACY` or spelled inline in a `.filter` was never looked at"). Completing that audit found the second live stale hole of the day. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 5000] **`NOT_EVENTS` is named, empty, and policed — the inline exclusion it replaces had been excusing nothing for some time** — the first audit read const NAMES, so it saw the six `EXEMPT`/`ALLOWED` lists and none of the holes written at the point of use. Widening it to inline `.filter`/`.includes` exclusions turned up one: `metabase-connection-doc.test.ts` dropped `item_added_users` from its "quoted like an event but not in `ANALYTICS_EVENTS`" check, under a comment about "SQL keywords / non-event tokens" in the plural over a list of one — which is the tell that the set had shrunk and the last entry was left behind. **Measured today, `docs/metabase-connection.md` does not contain the token anywhere, and neither does the taxonomy.** So the exclusion excused nothing while standing open over exactly the name it was written about: quote `'item_added_users'` in that guide as an event that is not in the taxonomy and the check skips the one name it exists to catch. Removing the filter leaves the suite green, which is the proof the hole was empty. It becomes a NAMED empty list rather than a deletion, because a token that genuinely is not an event will turn up again — the query language has plenty — and the version of this hole that goes stale silently is precisely the one written inline at the point of use. The honesty half comes with it: an entry naming a token the guide no longer quotes is a failure. The scan gets the other refusal too, for the same reason the gradients scan did yesterday — a guide whose queries stopped quoting anything passes the loop having read nothing. `WALKS_SOMETHING_ELSE` in `source-files-helper` was the near miss and is properly held by `assertExemptionsHonest`; the four `ALLOWED_TO_*` lists in the helper suites are as well. Mutation-checked two ways: putting `item_added_users` back into `NOT_EVENTS` turns the honesty assert in its own words (the tree's own bug, reproduced), and breaking the quote regex turns the read-nothing assert rather than passing green. 1 file, +0 cases. `npm run verify` green: 6758 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## The allow-list nobody asked about, and the walk that could stop walking (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Per-hole-reader follow-ups" third entry written today ("The stale exemption survived because a hole with no honesty case is invisible to every check in this tree ... a single-call sweep with a bare `exempt` and one reader still passes with a hole nobody asks about"), turned into an audit of every exemption-shaped list in `__tests__`. Sixteen files carry one; seven police it with `assertExemptionsHonest`, five by hand, three are not file exemptions at all, and one had nothing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 5000] **`ALLOWED_LOCAL` is checked to still be a hole, and the gradient scan is checked to have scanned** — the audit that followed yesterday's stale-exemption fix walked every `EXEMPT`/`ALLOWED`/`SANCTIONED` list in the suites and found one with no honesty half at all: `gradients.test.ts` allows `components/skeleton.tsx` to render a local `<LinearGradient colors={…}>` on the reasoning that the shimmer is animation chrome rather than a surface, and nothing asked whether it still renders one. Measured today it does, so this is the class rather than a live bug — and it is the class exactly: the component refactored, renamed or deleted leaves an entry standing open for whoever hand-rolls the next gradient in that file, with nothing about it looking stale. `EAS_EXEMPT` was the near miss worth recording, since it polices itself in the same words ("stale exemption: … drop it from EAS_EXEMPT"); `dashboard-banner` and `hero-banner` do it by hand; the three remaining consts are allowed-VALUE sets rather than file holes. **The second refusal is the one the helper would have given it for free.** This scan cannot go through `assertNoOffenders` — its unit is the element, not the file, which is one of the two shapes recorded in `OffenceRule`'s doc block as needing a different walk — so it carries neither refusal, and a `<LinearGradient` that stopped matching (the import aliased, the element wrapped) empties the offender list and reports a clean tree. That is the failure this whole file exists to refuse, made about itself, and it now says so: at least one coloured gradient element must be found. Mutation-checked two ways: adding a file to the allow-list that renders no gradient turns the honesty assert naming it, and breaking the element regex turns the walked-nothing assert rather than passing green. 1 file, +0 cases. `npm run verify` green: 6758 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## The hole that had stopped excusing anything, and the reader counted once for two (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Call-reader follow-ups" second entry ("The sharing check counts identifier occurrences over the WHOLE suite, not within the case ... The property it can actually see is 'this name has more than one reader'; the property it is for is 'the case policing the hole asks through the rule'"). The tightening found a live stale exemption in `privacy-translated-section.test.ts`, fixed here. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 7000] **Readers must number the holes, and `lib/privacy-translated-section.ts` needed no hole at all** — the sharing check shipped today asked whether a sweep's rule identifier had a SECOND reader, which is a property of the name and not of the hole. `WHOLE_STRING_HEX_RUN` sweeps twice from one const — the source tree and the suites — and only the suite half is held honest, by `assertExemptionsHonest`; the one reader that satisfied the check was that case, and the check then vouched for the source half as well. **The source half was a hole standing open over nothing.** It exempted `lib/privacy-translated-section.ts` on the reasoning that the module DECLARES the width and is the one place allowed to spell it — and measured today, it does not spell it: it builds `^[0-9a-f]{${POLICY_CHECKSUM_LENGTH}}$` from the constant, which is exactly what the rule wants of every module including that one, and the only whole-string hex run left in the file sits in a doc comment that `sourceCode` strips before the rule ever sees it. So the exemption excused nothing while quietly permitting a re-declaration in the single module most likely to make one, and unlike `HEX_WIDTH_EXEMPT` one paragraph above it, it had never been given an honesty case. The `exempt` is gone and the sweep walks the declaring module like everything else — green with no exemption, which is the proof the hole was empty. The systemic half: `shared` is now `readers >= holes` rather than `readers >= 1`, counting the exempt-carrying calls that name each rule, because a reader answers for the exempt list it actually asks about and a count cannot tell which that is. Mutation-checked three ways: re-adding the exemption turns the sharing case naming the suite and the rule (the tree's own bug, reproduced), and the two mutations from this morning — un-sharing `EYEBROW_RECIPE`, sharing `SOLID_HERO` — still turn their own cases under the stricter arithmetic. 2 files, +0 cases. `npm run verify` green: 6758 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## The entry that could excuse nothing, and the third name adoption arrives under (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Call-reader follow-ups" third entry written today ("`POLICED_OTHERWISE` is one entry and it is a string pairing two names ... renaming either half silently empties the exemption and turns the sharing case red with a real-looking failure. Every other exemption list in this tree names a FILE, which the walk itself would refuse if it went missing") and "Conjunct-rule follow-ups" fifth entry ("The adopter floor is 9 and counts suites naming either export; `matchesRule` is a third name it does not count — a suite that imports only `matchesRule` reads as a non-adopter, which is right today and would be wrong the moment a rule is shared between a sweep in one file and an exemption in another"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 6000] **`unseen` refuses a hole around nothing, and the adoption names are checked against the module's own exports** — the sharing check shipped this morning carried one exemption, `"hero-banner.test.ts: SOLID_HERO"`, and it is the only exemption list in this tree that names something the walk would not miss: every other one names a FILE, so a rename turns the walk itself red. This one joins a suite and a rule into a string matched by equality, so renaming the const left an entry excusing a call that no longer existed — and the sweep it was excusing became an ordinary unshared offender, which is a red case saying the wrong thing about the wrong file. `unseen` is the refusal: every recorded entry must name a call this walk actually met, and it is asserted BEFORE the two direction checks so the rename reports itself rather than surfacing as somebody else's failure. **The second half is a floor that could not see a third of the module.** `ADOPTION_NAMES` was two of the three exports, so a suite importing only `matchesRule` — which is exported precisely so an exemption case can ask a sweep's own question — read as a non-adopter, and would have kept reading as one the day a rule is shared between a sweep in one file and the exemption policing it in another. Widening the list by hand fixes today and not the next export, so the list is now checked against the module: every `export function` in `helpers/offence-sweep.ts` must be a name the adoption walk counts, with its own premise floor of three so a regex that stopped reading the helper cannot report the list complete. The widening forced a SPLIT the call-reading cases needed anyway — `sweepingSuites()` (names a sweep export) is not `adoptingSuites()` (uses this module at all), because a `matchesRule`-only suite has no call to find and reporting it as a sweep the reader could not read would be the reader blaming a file for not containing something it never claimed to. Mutation-checked three ways: renaming `SOLID_HERO` turns `unseen` naming the stale entry in its own words, dropping `matchesRule` from `ADOPTION_NAMES` turns the export case naming it, and a temporary `matchesRule`-only suite passes under the split and is falsely reported as an unreadable sweep the moment the call-reading cases walk the wide set. 1 file, +1 case. `npm run verify` green: 6758 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## The reader that knew one argument order, and the sharing nothing checked (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Named-rule follow-ups" first entry ("The named-rule case reads the call with a regex over collapsed source, which is the shape it exists to distrust ... a call written with `files` before `rule`, or built from a spread options object, is invisible to it. The premise floor of nine is what stands between that and a silent pass, and the floor is the current count with no slack") and second entry ("Naming the rule makes sharing POSSIBLE and nothing checks that it happened"), which the "Conjunct-rule follow-ups" second entry states again from the helper's side ("`matchesRule` is exported so an exemption can ask the sweep's question, and nothing makes an exemption use it ... naming the rule is checked, using the name in the exemption assert is not"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 10000] **`sweepCalls` finds calls by their opening bracket, and a hole that can be asked about must ask through the rule** — the named-rule case read every adopter with ONE regex anchored on `rule:` immediately followed by `files:`, so the set of calls it vouched for was the set of calls written in that order. A `files`-first call, a shorthand `read,` before the rule, or a spread options object was not a call it had ever seen, and the only thing between that and a silent all-clear was a premise floor of nine — which was the current suite count with no slack, so the first sweep written in an unknown shape would have taken the walk from nine to nine and passed. **Finding a call and reading its arguments are different reliabilities**, so they are two steps now: `sweepCalls` locates each `assertNoOffenders({`/`assertOnlyTheseMatch({` by the export name plus two characters, bounds the body at the closing `})`, then reads the `rule:` argument out of it — terminated by the next OPTION key in any spelling (`files:`, the shorthand `read,`, a shorthand sitting last) rather than by the next comma, since an inline rule can contain one. A call it cannot read comes back with a `null` rule and is REPORTED rather than skipped. The floor is replaced by two derived equalities: every suite naming either export must be one this walk found a call in, and every call found must be one a rule came out of. The floor stays only where the number means adoption rather than parseability, one case up. **The property the naming was for, checked at last.** Naming a rule makes sharing possible; nothing made it happen. A sweep call that names a HOLE now has to read its rule identifier somewhere else in the same suite — the count of the identifier's uses must exceed one declaration plus every `rule:` argument naming it — because the exemption's honesty case is asking the sweep's own question and two copies of the answer drift the moment one is tightened. Sweeps with no `exempt` list are not held to it: a sweep with no holes has nothing to police. `hero-banner.test.ts: SOLID_HERO` is the one recorded exemption and is the honest kind: its exempt component splits the fill from the radius across two style objects and cannot match a pattern whose subject is the two written together, so sharing the rule there would assert something false — and the entry is itself policed, since an exempt call whose rule IS shared has stopped needing the exemption. Mutation-checked four ways: un-sharing `EYEBROW_RECIPE` turns the sharing case naming the file and the rule, sharing `SOLID_HERO` turns the stale-exemption assert, a spread options object and a call taking a pre-built object each turn the readability case with the shape they broke, and a `files`-first reordering (which turned the old reader red) passes. 1 file, +2 cases. `npm run verify` green: 6757 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## The rule a sweep could not name, and the recipe typed twice (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Adopted-sweep follow-ups" third entry ("Four of the five adopted sweeps now name their rule in a `const` and one still inlines it ... the difference is invisible until someone adds the sixth sweep and picks by whichever neighbour they read first") and the "Conjunct-rule follow-ups" entry written today ("`matchesRule` is exported so an exemption can ask the sweep's question, and nothing makes an exemption use it"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 9000] **Every sweep names its rule, because a rule with no name gets typed a second time** — five adopters passed a regex literal straight into `assertNoOffenders`/`assertOnlyTheseMatch` (both gradients sweeps, the hero eyebrow, the clear-chip fingerprint, the `TAG_COLORS` declaration) while seven named theirs, which read as a style split and was not one. A sweep's rule has a SECOND reader: the case asserting that each exempt file still offends, which is the only thing between a named hole and a hole nobody notices. An inline literal cannot be shared with it, so the recipe gets typed again. **`hero-banner.test.ts` was already there**: the four-line eyebrow recipe (`color: AMBER_LIGHT` / `fontSize: 12` / `textTransform: "uppercase"` / `letterSpacing: 1.2`) written once as the sweep's rule and once in the `assert.match` vouching for `<HeroBanner>`'s exemption, so tightening the sweep would have left the exemption certifying its hole against a rule the sweep no longer had — and nothing about the stale copy would have looked stale. One `EYEBROW_RECIPE` const now, read by both. The other four are named for the same reason before they need it. `offence-sweep.test.ts` gains the case that keeps it: every `rule:` argument in an adopting suite must be an identifier, with the helper's own suite excluded because its rules ARE the fixtures — a case about the `g` flag has to hand a `g`-flagged literal straight to the call. The case carries its own premise (at least nine suites matched the call shape it reads), which is the module's own refusal turned on itself, and it earned that immediately: the first draft anchored on a newline between `({` and `rule:`, `suiteCode` collapses whitespace, and the premise turned red rather than the walk reporting twelve sweeps clean. The capture is anchored on `, files:` rather than the next comma, because an inline rule can contain one — `/colors=\{\[\s*HERO_DARK_4\s*,/` did — and a prefix is not something a reader can go and name. Mutation-checked two ways: re-inlining the scrim rule turns the new case naming the file and the literal, and editing the shared eyebrow const turns the exemption assert that now reads it. 5 files, +1 case. `npm run verify` green: 6755 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## The offence that is a conjunction, and the exemption vouching against the old rule (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Allowlist-sweep follow-ups" third entry ("Three of the four sweeps that could still adopt cannot, for three different reasons — `i18n-locales-helper`'s offence is a compound predicate (an `includes` and a regex) ... A `rule: RegExp | ((source: string) => boolean)` would take the first, at the cost of the g/y refusal being meaningless for half the callers") and "Shared-refusal follow-ups" second entry ("A third sweep shape would have to invent its own two nouns before it could use the refusal"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 11000] **`OffenceRule` takes a list of conjuncts, not the predicate the callers wanted** — `assertNoOffenders` and `assertOnlyTheseMatch` took one `RegExp`, so a sweep whose offence is a conjunction stayed outside them: `i18n-locales-helper`'s habit rule is "the suite reads the translations source AND counts six of something", written as `text.includes(…) && /…/.test(text)` inside a hand-rolled `filter` with both hazards left in prose. The suggestion's own answer was `rule: RegExp | ((source: string) => boolean)`, which takes every such sweep at once and gives up the reason the module exists — a predicate is a closure, and the g/y refusal has nothing in it to read, so those callers would get the refusals in name only. `OffenceRule = RegExp | readonly RegExp[]` with AND semantics takes the conjunction sweeps and keeps every conjunct in view: the refusal loops the list and names the pattern carrying the flag (`carries "g" on worse`) rather than saying "the rule", which is the half a two-conjunct caller needs. A third refusal comes with the shape — an EMPTY conjunct list is true of everything, so it reports the entire walk as offenders, which is loud in the wrong words: the reader gets 200 file names and has to work out that the rule, built from a source that came back empty, is what changed. **The bug the adoption found.** `i18n-locales-helper` wrote its conjunction twice — once in the sweep's `filter` and once as `assertExemptionsHonest`'s `stillNeeded`, word for word — so tightening the sweep left the exemption vouching for its hole against the rule the sweep no longer had, and nothing about the stale half would have looked stale. `matchesRule(rule, source)` is exported for exactly that second reader, and the one `COUNTS_LOCALES` list is now what both ask. The suite keeps its own `suites.length > 200` floor on top of the helper's "walked no files at all", because a walk that came back with three top-level suites is a broken filter rather than a small tree. Adopter floor 8 → 9. Mutation-checked three ways: `.every` → `.some` turns 3 cases (the compound case, the `matchesRule` case and the adopted sweep itself), checking only the first conjunct for `g` turns the flag case, and dropping the empty-conjunct refusal turns its own. The two shapes this still does not take are recorded in the type's doc block — a scan that is per-match rather than per-file, and the two rules that push offenders out of a line loop — because both need a different walk rather than a different rule. 3 files, +4 cases. `npm run verify` green: 6754 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## One builder for every spelling, and the claim only the scanner can make (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Fixture-table follow-ups": the fifth entry ("`consoleSwapFixture` takes a property name and always emits `= () => {};` ... Half the table goes through the shared builder and half repeats the `${CONSOLE}` interpolation, which is the shape 'one trick, stated once' was meant to end"), the second ("the two walks assert the same table with different operators and neither says the other exists ... the only signal on the day the scanner and the pattern disagree — which is the case worth having and the one nothing states") and the third ("the premise case pins 'at least five a side' over a table of 7 and 7 ... the third floor in this tree whose number is 'the current count minus some slack' with the slack unstated"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 10000] **`consoleSwapFixture` takes the access, the table goes in as one file, and two suites stop keeping their own `console`** — the builder emitted the dotted form with a seam-shaped arrow and nothing else, so the four bracket rows of `CONSOLE_SWAP_FIXTURES` could not use it and were written out through the raw identifier: half the table through the shared builder and half restating the very interpolation the builder exists to state once. `consoleSwapFixture(access, value?)` takes a bare string for the dotted form and a `ConsoleBracket` (`{key, spaced?}`) for the other three, with `key` written as it appears between the brackets — `"warn"` with its quotes, a bare `method` for the computed one — so a row says which shape it is in the same characters the source would, rather than through a flag per spelling. All eight assignment rows go through it now, including the two qualified ones and the two local-wrapper innocents (`my${...}`, `fake${...}`), and the six rows that are NOT assignments — a read, a default-seam parameter, three comparisons — take `CONSOLE_IDENTIFIER`, which is the joined literal exported rather than restated. Both suites drop their own private `console` constant with its own paragraph explaining the trick: `check-console-swap.test.ts` had a plain `const C = "console"` and `default-console-seams.test.ts` a `const CONSOLE`, and between them they rebuilt the banned line shape in six places including three per-property loops, all of which now call the builder. A case pins the four spellings the builder emits, because the failure it would NOT catch is a builder that turned every bracket row into the dotted form: those rows still offend, the table walks stay green, and the bracket half of the pattern goes unasserted by a table claiming to cover it. **The two table walks were one claim made twice.** Row by row, against a one-line source, `findConsoleSwaps` IS `CONSOLE_SWAP` — so the guard's walk and the ban's walk turn red together on every mutation and the second failure is noise. Everything the scanner adds happens BETWEEN lines: it strips comments, splits, and numbers what is left. So the new case feeds the whole table in as one file and asserts the exact offender list, line numbers included, derived from `offends` so it cannot drift from the rows the other two walks assert. A scanner that dropped its first line, numbered from zero, or reported a row twice passed both walks and would mis-report every real file it read. Mutation-checked three ways: collapsing the bracket branch of the builder turns 3 cases (the builder case and both table walks), `line: index` turns the whole-file case and the 1-indexed-line case, and a `.slice(1)` on the split turns 7. The five-a-side floor over a table of seven and seven now says where the number comes from — two rows of slack each way, since `npm run remeasure-floors` does not know about floors local to a suite. 3 files, +2 cases. `npm run verify` green: 6750 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## Two refusals written twice, and one parameter under two names (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Allowlist-sweep follow-ups": the first entry ("`assertOnlyTheseMatch` and `assertNoOffenders` share two refusals written twice, word for word apart from one noun ... A private `refuseUnsweepableInput(rule, files, subject)` would say each once; what argues against is that the two messages are the only place each shape's failure is phrased in its own terms"), the second ("the new helper reports both directions in ONE `deepEqual` over a `{unexpected, missing}` object ... the reader meets the structure before the explanation") and the fourth ("`what` and `instead` are the same parameter under two names ... A caller writing their second sweep this week gets it wrong once"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 11000] **`refuseUnsweepableInput`, a flat labelled diff, and `what` in both shapes** — the two sweep exports opened with the same two `assert.ok`s copy-pasted, differing in one noun apiece: a stateful rule "skips offenders" in one and "skips matches" in the other, and an empty walk "would pass against a tree that offends everywhere" in one against "both halves of its claim are about an empty set" in the other. Two copies of a refusal is the shape this module exists to end, one level below where it ends it. `refuseUnsweepableInput` checks both once and takes the two nouns as `skipped` and `vacuously` parameters rather than generalising them away — they are the only place each shape's failure is phrased in its own terms, and a reader meets one of these messages precisely because their sweep is broken in a way they did not anticipate, which is when a sentence about "the sweep" in the abstract is the one they cannot act on. Both messages survive word for word, and a case now asserts each shape still says its own noun in both refusals, because "say it once" is exactly the change that would have flattened them. `assertOnlyTheseMatch`'s two-direction report keeps ONE assert — two would stop at the first and show half the answer on the run where the allowlist is wrong both ways — but the comparison moves off `{unexpected, missing}` onto a flat list whose entries are labelled (`unsanctioned, <what>: a.ts` / `sanctioned, no longer does: b.ts`), so the diff itself says which kind each path is instead of making the reader decode an object and then find the sentence explaining it. A case drives both directions at once and asserts the `actual` array, not just the message. And `instead` is renamed to `what`: they were always one parameter — a verb phrase finishing "these {subject} …" — under two names picked to read well in each message, which cost whoever wrote their second sweep this week one wrong guess; the seven call sites whose phrase ends by naming the fix ("… — render <HeroBanner> instead") keep it, since that is a clause of the description rather than a different argument. Mutation-checked two ways: giving both shapes the same `skipped` noun turns the shared-refusal cases, and dropping the label off the unsanctioned entries turns the both-directions case. 8 files, +3 cases. `npm run verify` green: 6748 tests, 1492 suites, 17/17 guards, build exported. No DB migration, no creds.

## Two suites, one rule, two lists of what it must not match (2026-08-28)

<!-- Promoted from .tasks/.suggestions.md, "Boundary follow-ups": the first entry ("the console rule's negative side is now asserted in two suites and they do not agree on what it covers ... the honest fix is one exported fixture table of must-match/must-not-match lines that both suites walk"), the second ("`\bconsole` still matches `window.console.log = x` ... worth a case saying so, because the boundary case reads as 'the identifier must be exactly console' and the truth is 'the identifier must END at console'") and the third ("four strings say what the rule is and they are written in four files ... the noun phrase could be shared"). Closes the fourth entry of "One-matcher follow-ups" on the way (four files building a console fixture through their own joined literal, each explaining the trick again). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-28] [est: 12000] **`CONSOLE_SWAP_FIXTURES` — one table both suites walk, the qualified global named, and one subject phrase for four readers** — the console-swap rule is enforced in two places (the guard over `app/`, `components/`, `lib/`, `scripts/`; the suite ban over `__tests__/`) and since the pattern was shared they read the same matcher. What was not shared was the answer: each suite kept its own hand-written list of lines the rule must and must not hit — this one pinned the read form, the comparison form and the wrapper form, the ban's pinned three lines chosen to be the ones a widening would break — so a rule edited into matching more could break one list and leave the other green, which is exactly the two-copies failure the shared `CONSOLE_SWAP` had just fixed one level up. `CONSOLE_SWAP_FIXTURES` in `lib/check-console-swap.ts` is 14 rows of `{line, offends, why}` beside the pattern they describe; the guard's suite runs them through `findConsoleSwaps` and the ban's suite through the regex, so the two walks answer for the same claims. A premise case guards the walks from going vacuous — at least five rows a side and no duplicate line, since a table that lost its must-not-match half is a table both suites still pass having asserted nothing about over-reading. Two rows are new and are must-MATCH: `window.console.log = x` and `globalThis.console["warn"] = x`. `\bconsole` READS as "the identifier must be exactly console" and SAYS "the identifier must END at console", so both qualified spellings of the same global are caught (correctly — `window.console` IS the thing the rule is about) while `myconsole.log = x` is not, and the asymmetry is invisible from the pattern alone; a case names all three together. `consoleSwapFixture(property)` is the joined-literal trick stated once — four files had independently invented it and explained it in four comments, because this module is swept by the guard it implements and may not contain the banned form as a literal — and `CONSOLE_SWAP_PROBE` is now built from it rather than being a second copy. `CONSOLE_SWAP_SUBJECT` is the fourth half: the scanner's heading, the wrapper's clean-tree line and the wrapper's probe refusal now interpolate one phrase instead of restating it, and `LINT_GUARDS` deliberately does NOT import it — the registry is a pure data module that depends on no guard, and inverting that for one phrase would grow its imports with the guard count — so a case asserts the blurb still contains the phrase and that the wrapper still says it in both its places. The ordering case that pinned the probe running before the clean-tree line was anchored on the full sentence and is re-anchored on `no assignments to`, since the subject is interpolated now and its first occurrence in that file is the import. Mutation-checked two ways, each turning cases red in BOTH suites rather than one: dropping the `\b` turns 3 cases (the two table walks and the qualified-name case), narrowing the dotted half back to `log|warn` turns 5. 4 files, +5 cases net. `npm run verify` green: 6745 tests, 1491 suites, 17/17 guards, build exported. No DB migration, no creds.

## A rule wider than its own name (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "One-matcher follow-ups": the first three entries (no case pinning what the pattern must NOT match where the ban relies on it; a report still calling it "a global console method" after the pattern widened to any property; and no word boundary, so `myconsole.log = x` matched). Also records that the four-locale translation backlog the 2026-08-22 "Home-screen follow-ups" entry left open is closed — 4 keys still read as English in all four locales and all four are legitimately identical. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 9000] **`\bconsole`, a report that says property, and the negative side pinned where the rule is relied on** — `lib/check-console-swap.ts`, `scripts/check-console-swap.ts`, `lib/lint-guards.ts` wording; `check-console-swap.test.ts` +1 case, `default-console-seams.test.ts` +1 case.

  **Widening the dotted half made an old looseness reachable.** `console\s*\.\w+\s*=` with no boundary matches any identifier ENDING in the word, so `myconsole.log = noop` — a local wrapper being configured rather than the global being replaced — was an offender the report could only describe as one. Nothing in this tree is named that way, which is precisely why the first person to do it would have found it. `\bconsole` now, with a case for both spellings of the false positive and one asserting the global is still flagged on the same line shape.

  **The report kept describing a rule that had moved.** "Assignments to a global console method" was accurate while the pattern named six of them; it matches any property now, so a `console.someFlag = true` would have been refused by a message insisting it was a method. The heading, the clean-tree line, the probe refusal and the `LINT_GUARDS` blurb all say property.

  **A positive control cannot see a widening.** Both halves of this rule are guarded against a pattern that stopped matching — the guard's probe, the suite ban's helper — and a pattern edited into matching MORE satisfies both. So the ban carries the negative side where it relies on the import: reading a console method, comparing one, and configuring a local wrapper are each asserted not to match.

  `npm run verify` green: typecheck, `lint:all` 17/17, 6744 tests (+2), build. No DB migration, no creds.

## The allowlist sweep, written three times as a sorted deepEqual (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Adopted-sweep follow-ups": the third and fourth entries ("the tree holds at least six more file sweeps of the same four lines" / "`sentry-lazy-import`'s sweep cannot adopt without losing what it asserts … The helper has no shape for 'these files must match'"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 14000] **`assertOnlyTheseMatch` — the other question three sweeps ask, and the report that could not name its offender** — new export in `__tests__/helpers/offence-sweep.ts`, adopted by `sentry-lazy-import.test.ts` and `tag-colors-rotation.test.ts`; `sheet-search-row.test.ts` adopts `assertNoOffenders`; `offence-sweep.test.ts` +5 cases and the adopter floor rises to 8.

  **"Nobody does this" and "these files do this and no others" are two questions, and only one had a helper.** The sanctioned static Sentry import, the single `TAG_COLORS` declaration and the one component declaring a row are all the second: a walk, a rule, and a sorted `deepEqual` against a list. Passing the sanctioned files as `exempt` would have kept the half everyone expects (nobody else does this) and dropped the half that rots quietly — an allowlist entry that STOPPED doing the thing, which is a hole standing open with nothing about it looking stale. So it is a second export rather than an option: one name meaning two rules is what the previous helper's note already refuses.

  **A third hazard, which only this shape has.** An `expected` entry naming a file the walk does not contain passes silently, because a claim about a file nobody reads is a claim about nothing — the walk narrows, the allowlist goes on naming a path outside it, and the sweep stays green. Refused by name, beside the stateful-rule and empty-walk refusals the sibling already had. All five failure modes are cases against a fabricated tree, including the sanctioned-file-stopped-matching one that the `exempt` route could not express.

  **A report that told the reader "a consumer" and left them to find which.** `sheet-search-row`'s sweep mapped its consumers to their SOURCES before filtering, so an offender came back as a 900-line string and `deepEqual(declarations, [])` had no file name to print. Through the shared sweep it reports the path, and it picks up the two refusals on the way.

  Floor raised from 5 to 8 and widened to count both shapes, still derived from the imports rather than listed.

  `npm run verify` green: typecheck, `lint:all` 17/17, 6742 tests (+5), build. No DB migration, no creds.

## Two sweeps adopted it and a dozen kept the four lines (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Offence-sweep follow-ups": the first entry ("Two sweeps adopted the helper and the tree holds a dozen more of the same four lines … Each of the others carries the two hazards the helper refuses with nothing saying so. A floor case naming the adopters would at least record that the number is two and rising rather than two and forgotten"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 15000] **Five sweeps through `assertNoOffenders`, a floor counting them, and a chevron rule that could not see the spelling its own component uses** — `gradients.test.ts` ×2, `hero-banner.test.ts` ×2, `dashboard-banner.test.ts` ×1 and the console ban in `default-console-seams.test.ts` adopt the helper; `offence-sweep.test.ts` +1 floor case; the chevron rule widened and pinned both spellings.

  **The hazards were refused in one sweep and written in prose in the rest.** `assertNoOffenders` exists because a rule that grows a `g` flag makes `.test` skip every other file, and because a walk that stops matching satisfies an absence perfectly — two failures that pass green. One pair of sweeps went through it; the others were the same four lines with the hazards left to whoever next edits them. Five now go through it, and the walk-and-rule refusals come with them.

  **A real gap, found by refusing to assume an exemption is a hole in something.** Adopting the helper meant naming each sweep's exclusions as `exempt` entries, and the helper's contract says an exemption should be policed — so each got a case asserting the exempt file still does the thing it is exempt for. Two failed. `components/dashboard-banner.tsx` was exempt from a chevron sweep that could not have flagged it: `<Text style=\{[^}]*\}>` reads `style={styles.arrow}` and stops dead at the nested brace of `style={{ ...styles.arrow, color: theme.meta }}` — which is the spelling the component itself writes, and the spelling a screen hand-rolling the row would copy from it. The rule reads both now; the sweep still finds exactly `dashboard-banner.tsx` and `app/settings.tsx`, and both spellings plus two innocents (`<Text>›</Text>`, a `<View>` carrying the glyph) are pinned as cases so the widening cannot quietly become "any chevron anywhere".

  **The second failure was an exemption whose honesty test had to be a different question.** `<HeroBanner>` splits the solid tone in two — `hero` carries the radius, `heroSolid` the fill — so it cannot match a rule whose whole subject is the two written together, which is what a hand-rolled banner produces. The exemption is real (the component is the declaration) and "does it still offend" is the wrong question to ask of it, so it is policed by what it protects: the component must still be the file declaring the solid fill.

  **A number rather than a memory.** The adopter list is derived — a suite that imports the helper is an adopter — and the floor is 5 with a message naming who they are. A floor rather than an equality, because a rule DELETED is a real outcome and should not fail here, while a rule rewritten back into `walk.filter(…)` + `deepEqual([])` should.

  `npm run verify` green: typecheck, `lint:all` 17/17, 6737 tests (+1 case, five sweeps rewritten), build. No DB migration, no creds.

## A sixth stream, captured and unbanned (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Mapped-stream follow-ups": the fourth entry ("Adding a sixth stream is one edit and nothing tells the person adding it what else to look at … What does NOT follow is the ban's regex in this same suite, which enumerates the five method names in a pattern. A sixth stream would be captured and unbanned"), with the second ("the once-per-name floor counts `"<method>"` with quotes, so it would not see a bare `log:` field reappearing") and the first (three exports, one consumer). Closes two from "Console-swap-guard follow-ups" on the way — the rule enforced twice by two matchers that have to agree, and the one pattern whose two halves disagree about what counts. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 13000] **One matcher for the console-swap rule, and a floor that counts the spelling the regression uses** — `CONSOLE_SWAP` in `lib/check-console-swap.ts` matches any property, `__tests__/default-console-seams.test.ts` imports it instead of restating it, the derivation floor counts words rather than quoted strings, two types un-exported, `__tests__/check-console-swap.test.ts` +4 fixtures.

  **The list was derived and the ban was not, so a sixth stream would have arrived captured and legal.** Yesterday's change made `CAPTURED` the one place a stream name is written in the helper — the swap, the restore, the collectors and both result types follow it. The ban in the suite beside it spelled the five names out in its own pattern. So `trace` was one edit to capture and no edit at all to ban: the helper would have swapped and restored it while the rule that says "go through the helper" went on knowing five, which is a stream a suite could swap by hand with nothing to say it may not. Mutation-checked both ways — with `trace` in `CAPTURED` and the old enumerated pattern, the new case names the stream that is not banned; with the pattern as it now stands, a sixth stream is green in one edit.

  **The rule existed twice and the two copies had already drifted.** `CONSOLE_SWAP` knew `trace`; `SWAPS_A_CONSOLE_METHOD` did not; neither knew any other property of the console. Neither can sweep the other's tree (one walks `__tests__/`, one deliberately does not), but nothing stopped the suite importing the guard's pattern, so it does — one matcher, and the drift it can have is the drift a shared constant has. Each copy of the rule keeps its OWN positive control, the guard's probe and the helper the suite sweep matches, so a pattern edited into matching nothing fails in both places rather than making both sweeps unanimous about a tree they stopped reading.

  **The dotted half and the bracket half were one pattern giving two answers.** `\[[^\]]+\]` matched whatever was between the brackets while the dotted alternative named six streams — so `console.dir = () => {}` was banned when written as a computed key and allowed when written plainly, which is not a rule anybody stated. `console.dir` outlives its swapper exactly the way `console.warn` does. Both halves are `\w+`-wide now, and the enumeration that made a sixth stream a second edit is gone from the last place it lived.

  **A floor that counted quotes could not see the regression it is for.** The hand-written result type it exists to catch spells its members `log: readonly string[]` — no quotes — so five members could have come back with the count sitting at one apiece. It counts each name as a word now, over the comment-stripped source `sourceCode` already returns, which is what lets the prose go on naming the streams it explains while the code names each exactly once. Mutation-checked: one hand-written `log` field turns the case red with the count and both spellings in the message; the old form stayed green on the same edit.

  **Three exports, one consumer.** `CapturedMethod` and `CapturedStreams` were exported on the argument that a mapped type nobody can name is awkward to assert against, and then nothing imported either. Both are module-local now; `CAPTURED` stays exported because the seams suite loops over it, and the two shapes a caller holds still name the local types in their own definitions, so a suite that needs one back is a one-word edit.

  `npm run verify` green: typecheck, `lint:all` 17/17, 6736 tests (+2), build. No DB migration, no creds.

## Ten fields restating one array (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Capture-lifetime follow-ups": the first entry ("`OpenCapture` and `CapturedConsole` declare the same five streams with the same five doc comments, and neither is derived from `CAPTURED` — ten hand-written fields for one array of five names, so a sixth stream is three edits in one file. The existing suggestion said `Record<CapturedMethod, readonly string[]>` would derive one of them; it now saves twice as much"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 8000] **`CapturedStreams` — one list, and a sixth stream is one edit** — `__tests__/helpers/capture-console.ts` maps both result types over `CAPTURED` and builds its collectors from it, `__tests__/default-console-seams.test.ts` +4 cases.

  **Four places for a sixth stream to be added to three of.** `CAPTURED` is the methods the helper swaps; `CapturedConsole` and `OpenCapture` each wrote the same five out by hand, and `install()`'s collector object wrote them a third time as a five-key literal. Both types are `CapturedStreams` now — a mapped type over the list — plus the one field each actually adds (`result`, `restore`), and the collectors come from `Object.fromEntries` over the same list. Adding `trace` is one edit, checked: the whole suite stays green with the sixth name in the array and nothing else touched.

  **The prose that argued against deriving said nothing the method name did not.** Ten doc comments, each some spelling of "everything written through `console.<its own name>`, in order" — which is why the earlier suggestion filed it as a trade. It is not one. The meaning is written once on `CapturedStreams`, and what it says that the ten did not is the part a reader actually needs: arrays rather than a joined string, because a case asserting "warned once" is counting calls.

  **A type cannot see the failure that matters, so a case does.** A method the installer swaps whose writes reach no array looks, from outside, exactly like a stream nobody used — the mapped type is satisfied either way. So the suite writes to every stream `CAPTURED` names and requires each to come back, checks that both result shapes hand back exactly those keys and no others, and puts a floor under the derivation: each method name appears ONCE in the helper, so a return to hand-written fields is red rather than merely verbose.

  Mutation-checked: dropping one stream from the collector table turns 5 cases red, while adding a sixth name to `CAPTURED` alone turns none — which is the change working rather than a gap.

  `npm run verify` green: typecheck, `lint:all` 17/17, 6734 tests (+4), build. No DB migration, no creds.

## A rule two directories could not see (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Capture-lifetime follow-ups": the fifth entry ("The console ban is a suite-only rule, and `scripts/` and `lib/` are not swept — nothing outside `__tests__` swaps a console method today, and nothing says it may not. The guard that contributors hit first is `lint:all`, where this rule does not live; a `scripts/check-console-swap.ts` would put it in the output where the hex and radius messages already appear"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 14000] **`lint:console-swap` — the seventeenth guard, sweeping the four directories the suite ban cannot reach** — new `lib/check-console-swap.ts` + `scripts/check-console-swap.ts`, registered in `LINT_GUARDS` / `SCANNED_FLOORS` / `package.json`, new `__tests__/check-console-swap.test.ts` (14 cases) and three meta-suites adopt it.

  **A tree that is clean, versus a tree that is kept clean.** `__tests__/default-console-seams.test.ts` has banned the hand-rolled console swap since the capture helper landed, and it sweeps `__tests__/` because that is where the adopters live. Nothing in `app/`, `components/`, `lib/` or `scripts/` swaps a console method — and nothing said it may not, so the first one to land would have landed unopposed. The rule now prints in `lint:all` beside the hex and radius messages, which is the output a contributor reads before a test run rather than after one.

  **What a swap costs, in each of the two places it could land, is written where the rule is.** In the app `console` is process-global and lives as long as the session: a component quietening one noisy dependency silences every other warning including the ones a crash report would have carried, and a React tree has no `finally`. In a guard script the swap outlives the function that made it and the next guard's report goes into somebody's array — `lint-all.ts` runs guards in child processes today, which is an implementation detail rather than a promise. Reading a method is untouched, because `const write = console.log` is the seam the rule exists to push people towards and banning it would ban the fix.

  **The guard carries its own positive control, and runs it before it vouches for anything.** A ban is satisfied by a pattern that has stopped matching at all, and a clean tree looks identical either way; the suite sweep controls for that against the helper that legitimately does the banned thing, and there is no such file outside `__tests__/`. So `CONSOLE_SWAP_PROBE` is an offender the scanner must flag before the wrapper prints a clean count — and the probe is a JOINED literal rather than a written one, so the declaring module does not trip its own rule. The alternative was exempting `lib/check-console-swap.ts` from a sweep that walks `lib/`: a hole in the shape of a whole file, and the one place a real swap could then hide.

  **Three meta-suites demanded registration and got it.** A planted offender in `guard-report-paths`, a partial-root fixture in `lint-guard-partial-root`, and a floor in `SCANNED_FLOORS` declaring all four roots — so a walk that loses `scripts/` refuses by name rather than by arithmetic. The new suite builds every fixture through a `"console"` constant, because the suite-wide ban has no exemption list and a literal here would have made this file the offender it is planting.

  Mutation-checked two ways: a planted swap in `lib/` fails the guard with the file, the line and the seam that replaces it; a pattern that no longer matches `console` fails on the probe rather than reporting 279 clean files, and turns 5 cases red.

  `npm run verify` green: typecheck, `lint:all` 17/17, 6730 tests (+36), build. No DB migration, no creds.

## The field two suites vouched for and no line read (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Unknown-key follow-ups": the first entry ("`unknownLanguages` is carried through two result types and read by nothing that ships ... a field nobody consumes is the shape three sweeps in this tree call 'a way to pass dishonestly'. The consumer that would earn it is a pass line that refuses to render while the list is non-empty"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 11000] **`unknownKeysRefusal` — the pass line will not publish an age it measured over a subset** — new refusal in `lib/oldest-record.ts`, both formatters consume `unknownLanguages`, `__tests__/oldest-record.test.ts` +8 cases.

  **A field carried through two result types and read by nobody.** `unknownKeys` was added so a caller that lost its shape half would have something to refuse on, and then no caller refused. The safety stayed exactly where the walk found it — a property of the CALLERS, asserted in two suites and enforced in none — which is the shape three sweeps in this tree already call "a way to pass dishonestly". The formatters read it now: a non-empty list means the age was measured over the entries the order knows while the table holds one it does not, so `Oldest record 2026-01-02 (en)` would be a fact about a subset printed as a fact about the table. The pass line does not render at all; the refusal names the table, the order and every key nobody walked.

  **Unreachable, and that is the claim rather than the caveat.** Both evaluators fail an unknown key in their shape half before a pass line is built, so nothing in this repository reaches the new branch — and a case asserts exactly that, over both real tables, so an evaluator that stops checking its keys turns red beside the refusal it just made reachable. The difference from the erased `unparseable` prose one module over is that this one is reachable from OUTSIDE: the formatters take a plain result, so a case builds one and proofreads the words. The message says which half should have caught it first, because a reader meeting it is looking at a broken guard rather than a broken age.

  **The refusal is the body only, and `lib/oldest-record.ts` is still a leaf.** `checkError` belongs to the callers — the module imports nothing so that any evaluator may use it and it can never be the one that closed a cycle — and the case that pins the missing prefix pins the no-imports property in the same breath, since the two are one decision.

  Mutation-checked two ways: dropping the refusal from the baselines formatter turns 1 case red, and making the refusal unconditional turns 4 (the null answer, the negative control, and both shipped pass lines, which is the parity suite catching it).

  `npm run verify` green: typecheck, `lint:all` 16/16, 6694 tests (+8), build. No DB migration, no creds.

## The decision was only ever written in a test file (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Registry-decision follow-ups": the first entry ("`HARD_FAILERS` is now read by two cases and is still a hand-written set of one id in a suite ... somebody reading `lib/provenance-tables.ts` to decide whether their new entry may stop the run finds `stopsTheRun` and no statement of who is allowed to set it"). Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-27] [est: 10000] **`HARD_FAILURE_IDS` moves beside the registry and `provenanceRegistryRefusal` enforces it on every run** — `lib/provenance-tables.ts` exports the recorded decision and grows a third refusal, `__tests__/provenance-tables.test.ts` reads it instead of re-typing it, +5 cases.

  **The only statement of the decision lived in a suite.** Which entries may stop the whole run is a choice with a shipped consequence — the first `unparseable` outcome halts the guard, so every table after it in `PROVENANCE_TABLES` loses its report — and it was one hand-written `Set` in `provenance-tables.test.ts`. A reader who opened the module the entry lives in, to decide whether theirs may, found `stopsTheRun` (derived, "what the code does") and nothing saying who is allowed to set it. `HARD_FAILURE_IDS` is now beside `PROVENANCE_TABLES`, and its doc comment states the blast radius at the line where somebody adds an id rather than two files away.

  **It became a refusal, not a second case.** The comparison used to be reachable only through `npm test`; it is the third check `provenanceRegistryRefusal` makes, after the empty registry and the duplicate id, so a registry whose hard failers are not the ones recorded stops the run with its cause named instead of suppressing a neighbour's report the first time a module is unreadable. Both directions are reported in one sentence, because the realistic mistake is a swap and naming only the half the check reached first sends the reader back for the second refusal.

  **`agreedHardFailers` is a parameter with the shipped list as its default.** A fabricated registry — the ones this suite builds out of `fakeTable()` — has its own decision, and judging it against the shipped one would have made the new refusal fire on cases that are about something else. The default is what the script passes, so the enforcement the guard actually runs is the no-argument form.

  Mutation-checked two ways: emptying `HARD_FAILURE_IDS` turns 4 cases red (the shipped-registry pair plus the two agreement cases), and dropping the agreement block from the refusal turns 3.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6686 tests (+5), build. No DB migration, no creds.

## Two cases nobody wrote, beside the things they were about (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Optional-sentence follow-ups": the first entry ("`stopsTheRun` and the suite's `HARD_FAILERS` set now say the same thing, and only one of them is derived ... If they are kept, the case comparing them to each other is the one nobody wrote") and the fourth ("`PRESENT_AND_UNREADABLE` is one string standing in for three parsers' failure ... that is a property of the parsers, not of the fixture ... A per-entry fixture, or a case asserting each parser returns null for this exact text, would keep it honest"). -->

- [DONE: 2026-08-27] [est: 9000] **The recorded decision is compared to the derived flag, and the unreadable fixture gets the control that makes it a claim** — `__tests__/provenance-tables.test.ts`, `HARD_FAILERS` hoisted to module scope, +2 cases.

  **Two statements of one fact, and nothing comparing them.** `stopsTheRun` is derived — the entry wrote an `unparseable` sentence — and `HARD_FAILERS` is the decision that it may, hand-written because one entry's hard failure suppresses every other table's report and which entries have that power is a thing somebody chose. Keeping both was the right call and the earlier note said so; what was missing is the case holding them against each other. A fourth spec growing a sentence nobody argued for is now a red case naming the id, rather than a quiet fourth entry with the power to stop the run.

  **A fixture that worked for a reason nothing checked.** `PRESENT_AND_UNREADABLE` stands in for three parsers' failure across four cases, and it works because none of the three can read a table out of a bare comment — a property of the PARSERS. A parser that one day accepted an empty table would read it as a legitimate empty result and those four cases would keep passing while testing nothing they claim to. The control is each entry's own module: `run` over `readRepoFile(table.module)` must come back evaluated with the drift half NOT skipped, which is the parser saying it found a table, and the fixture must not produce that outcome for any entry. "The parser read one" and "the parser did not" are now distinguishable from outside, which is what the fixture had been assuming.

  Reached through `run` rather than through the parsers, because `spec.parse` is erased by `defineProvenanceTable` and that erasure is the registry's point — `table.module` being readable is what made the control possible without opening the surface further.

  Mutation-checked two ways: giving a second spec an `unparseable` sentence turns 2 cases red (the new parity case and the reachability pair it complements), and pointing the control at the unreadable fixture instead of at each module turns 1.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6681 tests (+2), build. No DB migration, no creds.

## An age attributed to nothing, and the case that called it safe (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Shared-walk follow-ups": the second entry ("`oldestRecord` silently drops a key its `order` argument does not know, and the safety is entirely in the callers ... a page recorded in 2019 under a typo'd code would leave the line reading the second-oldest date as the oldest. Cheap fix if it is ever wrong: return the unknown keys as a third field and let the callers refuse") and the fourth ("The scrub-promise pass line prints `Recorded <date>` unconditionally while the other two render nothing when they have no age ... a line at `ScrubPromiseProvenanceResult.recordedOn` saying why this one is not optional would save it"). -->

- [DONE: 2026-08-27] [est: 13000] **`oldestRecord`'s two halves read the same entries, and the key neither of them can speak for is reported** — `lib/oldest-record.ts`, both adapters carry `unknownLanguages`, `lib/scrub-promise-provenance.ts` says why its date is not optional, +5 cases across three suites.

  **The walk disagreed with itself, and the suite recorded the disagreement as correct.** `recordedOn` was the minimum over `Object.values(table)` — every entry — while `keys` was filtered through the caller's `order`. So a table whose OLDEST entry sat under a key the order did not know produced a date with an empty key list: an age attributed to nothing, rendered onto a pass line as a fact about no page at all. The existing case asserted exactly that (`{ recordedOn: "2020-01-01", keys: [] }`) and called it safe because the callers validate first. It was not the failure the suggestion predicted (a second-oldest date read as the oldest) and it was worse; both halves read the known entries now.

  **The safety was a property of the CALLERS and is asserted about them.** `unknownKeys` on the walk, `unknownLanguages` on both adapters — not so a pass line can print it, but so the fact is expressible at all. The claim that made the old behaviour tolerable ("every caller runs a shape half that fails an unknown key before any pass line is built") lived in a doc comment two modules from the guards it was about; each guard's suite now hands its evaluator a table with a typo'd code and requires an `unknown_language` failure and `ok: false`. If a caller ever loses that check, the walk hands it a list to refuse on instead of an age quietly measured over a subset.

  **Null now means "this order knows no entry", not "the table is empty".** A table of nothing but unknown keys used to return a record built from entries it could not name; it returns null, which is the same answer as the empty table and is a failure for every caller either way. The mirror case is deliberately NOT a failure and has a case saying so: an order entry the table does not carry is a missing page, which is the shape half's business rather than this walk's.

  **The third table's date is unconditional, and now says why.** `oldest` is nullable on both map-shaped results because a table can be empty; a single record cannot be absent from a result that reached the formatter, so the scrub-promise line renders its date unconditionally while the other two render nothing. That is why the three formatters no longer branch alike, and it was a thing a fourth table's author would have had to re-derive from three formatters that disagree.

  Mutation-checked three ways: restoring the date to a minimum over every entry turns 3 cases red, emptying `unknownKeys` turns 3 (two of them in the adapters, which is the propagation working), and narrowing the null to the empty table alone turns 3.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6679 tests (+5), build. No DB migration, no creds.

## The four lines every sweep in this tree had written for itself (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Declared-width follow-ups": the first entry ("The width sweep now runs twice over two file sets and the two cases differ only in which walk they call ... the honest version is one helper taking a walk plus a reader") and the fourth ("`WHOLE_STRING_HEX_RUN` is `.test`ed in a filter over hundreds of files and its own doc comment has to say why it carries no flags ... the sweep's own regex is the second object with the same liability"). -->

- [DONE: 2026-08-27] [est: 13000] **`assertNoOffenders` — one walk, one reader, one rule — with the stateful-pattern and empty-walk hazards refused by construction** — new `__tests__/helpers/offence-sweep.ts`, new `__tests__/offence-sweep.test.ts` (10 cases), `assertExemptionsHonest` takes the walk it is policing, the hex-width sweep adopts and widens to the whole source tree.

  **Two cases that differed only in which walk they called.** The hex-width rule is one pattern swept twice — over `lib`/`scripts` with a path exclusion, over the suites with a named exemption list — and the four lines around it (walk, read, `.test`, report) had been written once per scan root. That is why the source half was narrowed to two directories in the first place: a third root meant a third copy, not that `app/` and `components/` were places the offence could not land. It walks `sourceFiles()` entire now, and the widening cost one argument.

  **The prose became a refusal, which is the actual upgrade.** `WHOLE_STRING_HEX_RUN` carries no `g` flag, and the reason was a paragraph in its doc comment: `.test` advances `lastIndex` between calls, so a pattern reused across a filter skips every other file and the sweep goes green while reading half the tree. The sibling pattern one file over has a CASE for the same property; this one had a sentence. The helper refuses a `g` or `y` rule outright, so no sweep built on it can carry the liability — and a negative control keeps the refusal about state rather than about flags, since `i` and `m` hold nothing and banning them would push callers into building a RegExp per file.

  **An empty walk satisfies every absence.** The second property no caller could be trusted to remember: a sweep asserts that nothing matched, and a walk that stopped matching — a renamed directory, a filter that got too clever — reads exactly like a clean tree. Refused, with the two shipped walks asserted non-empty in the new suite as well, so a `sourceFiles()` returning nothing fails once with a name instead of once per sweep.

  **`assertExemptionsHonest` had `suiteFiles()` welded into it.** Seven of its eight callers sweep the suites and the eighth does not; a hole in a `lib`/`scripts` sweep held against the suite walk would be reported stale on every run, which is a guard failing about files it was never about. The walk is a parameter now, defaulted, and the hex-width call names the one it means. What the helper still does NOT do is judge its own exemptions — that stays where it was, because an exemption is a recorded human decision and this is a loop.

  Checked the class rather than the instance: four module-scope `g`-flagged patterns exist in this tree (`ENV_VAR`, `OPT_OUT`, `BACKGROUND_RE`, `KEY_POSITION_LITERAL`) and all four are fed to `match`/`matchAll`, which do not carry `lastIndex`. No second live case of the bug the refusal is for.

  Mutation-checked four ways: dropping the flags refusal turns 2 cases red, dropping the empty-walk refusal turns 1, ignoring `exempt` turns 2 (one of them the shipped Cloudinary hole, which is the adoption working), and ignoring the new `walk` parameter turns 1.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6674 tests (+11), build. No DB migration, no creds.

## The rule that was five exemptions out of seven (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Console-capture follow-ups": the first entry ("Five suites still swap a console method by hand, and now there is a helper they cannot use ... A `beginCapture()`/`endCapture()` pair would serve the first two and would give up the `finally` that makes the callback form safe — which is the trade nobody has stated") and the fourth ("`captureConsole` is now the single place a suite may take the global console, and nothing says so anywhere a reader would look first"). -->

- [DONE: 2026-08-27] [est: 14000] **`beginCapture` for the lifetimes a callback cannot hold, five suites adopt it, and the hand-rolled swap is banned with no exemptions** — `__tests__/helpers/capture-console.ts`, `sentry-init` / `sentry-config` / `analytics-init` / `analytics-status` / `clarity` adopt, `__tests__/default-console-seams.test.ts` +6 cases.

  **A helper that five of seven suites could not use.** `captureConsole` owns its `finally`, which is what makes it safe and what made it unusable for a swap held across `beforeEach`/`afterEach`, for an `await`ed body (which its thenable refusal exists to reject), and for a case that only wants a stream silenced. Those were the five, and a sweep banning the hand-rolled swap written that day would have carried five exemptions — a paragraph pretending to be a rule. `beginCapture()` returns the streams plus a `restore()` and serves all three shapes; the ban below now has none.

  **The trade is stated where a reader meets it, and made loud rather than pretended away.** `beginCapture` gives up the `finally`: a caller who forgets `restore()` leaves the runner writing into an array nobody reads, and no helper can fix that from the inside. So captures do not nest — the next `beginCapture` or `captureConsole` refuses, names the likely causes (a `restore()` the case never reached, an `afterEach` that did not run), and closes the leaked capture before throwing so the rest of the file is not swallowed too. `restore` is idempotent for the same reason: a `finally` and an `afterEach` both calling it is the normal shape, and a second restore running after the NEXT case opened its capture would put the pre-first console back over the new swap.

  **Three of the five did not need the new form at all.** `sentry-config`'s three sites were synchronous all along and are now plain `captureConsole` calls reading `result` and `error` — the hand-rolled swap was habit, not necessity. The other two suites and `clarity` take `beginCapture`, and `analytics-init`/`analytics-status` swapped `console.warn = () => {}` for a capture, so the stream goes somewhere readable instead of nowhere.

  **The ban knows both spellings, because the helper uses the other one.** `console.warn =` and `console[method] =` — a rule that matched only the dotted form would be dodged by the exact line it exists to be the single instance of, and the positive control (the helper's own file must still match) would have passed while the ban read nothing. What the ban buys is not tidiness: a hand-rolled swap saves and restores ONE method, so a case that starts writing to a second stream loses it into the runner's output.

  Mutation-checked six ways: dropping the nesting refusal in `beginCapture` turns 1 case red, dropping it in `captureConsole` turns 1, making `restore` non-idempotent turns 1, throwing without giving the console back turns 1, re-introducing one hand-rolled swap in `analytics-status` turns 1, and dropping the bracket form from the ban turns 1.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6663 tests (+6), build. No DB migration, no creds.

## The helper that answered confidently about the wrong declaration (2026-08-27)

<!-- Promoted from .tasks/.suggestions.md, "Declared-shape follow-ups": the second entry ("reads the FIRST `function <name>(` in a module — an overload set, or a local function shadowing an exported one, gives it the wrong signature silently"), the fourth ("matches a member against its own line anywhere in the module, without knowing which TYPE it belongs to") and the fifth ("`parameterList` balances parentheses and `splitParameters` balances four bracket kinds, and neither knows about strings"). -->

- [DONE: 2026-08-27] [est: 11000] **`declared-shape` skips string literals, names the type a member belongs to, and refuses every declaration it cannot read** — `__tests__/helpers/declared-shape.ts` (new `declarationBody`, `assertRequiredMember` grows a required `declaration`), `__tests__/declared-shape.test.ts` +9 cases, `danger-icon-button.test.ts` names its props type.

  **The whole point of the helper was to stop three suites getting a signature question half-right, and it could still get one entirely wrong.** Not by failing — by answering. `parameterList` took the first `function <name>(` in a module, so an overload set handed back the first overload's parameters rather than the implementation's; `assertRequiredMember` matched a member against its own line anywhere in the file, so a module declaring two types with a same-named member answered from whichever came first. Both produce a green case that asserts something about a declaration nobody asked about, which is worse than a red one.

  **`declaration` is required, and that is the change.** Naming the type costs a caller four words and is the difference between a claim about a shape and a claim about a file. The new `declarationBody` takes one type's or interface's members between balanced braces; the two member regexes run inside that text and nowhere else. The fixture pair is the argument: `FixtureAlpha.shared` is required and `FixtureBeta.shared` is optional, and a module-wide read gets one of the two questions wrong whichever way it answers.

  **Where it cannot answer, it refuses.** A name declared more than once — an overload set, two merged `interface` blocks, both legal TypeScript — throws naming the count, rather than reading the first block and reporting on half a type. So does a parameter list whose brackets do not balance: `(value: string) => void` closes a `>` nothing opened, the old counter went to -1 and carried on splitting, and out-of-scope-and-loud beats out-of-scope-and-confident. The module note said it was not a parser; it now says what happens at each edge it meets.

  **Strings, because both depth counters were counting characters inside them.** A `")"` default ended the parameter list one parameter in and an `"a,b"` type split into two, and the reason neither had bitten is that no signature in this tree carries one — the suggestion's own point. Scanning is local (from a declaration's opening bracket to its balanced close) rather than over a whole module, deliberately: an apostrophe in a JSX text node is not a literal, and a whole-file mask would have masked the rest of every `.tsx`.

  Checked the layer under it rather than assuming: `stripComments` already tracks string literals AND regex literals, so the text these scanners receive has real quotes and no comment bodies. The one gap left is written down instead of guessed at — `endOfString` does not descend into a template literal's `${…}`, and no declaration in this tree is that shape.

  Mutation-checked six ways: dropping the quote skip in `balanced` turns 2 cases red, dropping it in `splitParameters` turns 1, dropping the duplicate-function refusal turns 1, dropping the negative-depth refusal turns 1, widening the member search back to the whole module turns 1, and dropping the merged-interface refusal turns 1.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6657 tests (+9), build. No DB migration, no creds.

## Three suites reading source text to pin a signature (2026-08-24)

<!-- Promoted from .tasks/.suggestions.md, "Floor-kind follow-ups", the third entry: "The helper's own suite reads its source text to assert a REQUIRED parameter — a compile-time fact checked by a regex, because a case cannot call a function with an argument missing. That is the third suite in a week to reach for source text to pin a signature ... A shared 'this function's signature is' helper is now a thing three suites would use." -->

- [DONE: 2026-08-24] [est: 11000] **`assertRequiredParameter` / `assertRequiredMember`, and the negative half two of the three call sites did not have** — new `__tests__/helpers/declared-shape.ts`, new `__tests__/declared-shape.test.ts` (11 cases), three suites adopt it.

  **A fact no case can call.** `arguedFloor`'s reason is required, `localesDeclaring`'s first parameter is an index rather than a source string, `<DangerIconButton>`'s `accessibilityLabel` is required rather than optional-with-fallback — a missing argument is a compile error, so the case that would prove any of them is a case that does not build. All three reached for the module's own text instead, each with its own read, its own regex and its own idea of what "required" means.

  **Two of the three regexes were half the rule.** `/because: string,?\s*\)/` matches the required form and says nothing about the optional one. It happens to be safe — `because?: string` does not contain `because: string` — and it is safe by accident rather than by statement; the one site that asserted both directions is the one that had been bitten by an optional label. Both directions are asserted for every caller now, along with the type and, where the position is the rule, the position.

  **Not a parser, and it says so.** The parameter list is taken between a function's opening parenthesis and its BALANCED closing one — a default with a call in it (`= String(1)`) is the shape a first-`)` search ends the list inside — and split on top-level commas, so `Pick<Console, "log" | "error">` stays one parameter rather than three. A parameter whose TYPE contains a parenthesis is out of scope and should be pinned by hand with a comment, rather than quietly matched by a splitter that would get it wrong.

  **The helper is tested on what it should REJECT.** Its three callers all point it at a real declaration, so every one of them passes and none exercises a failure — a checker whose negative half nothing runs is a checker that could be `return true`. The suite hands it declarations in its own file (read back off disk, so it is the real function being tested): optional, renamed, retyped, moved, and a function that is not there at all, which gets its own message rather than being reported as a rename.

  One gap found by mutating rather than by reading: the first draft's balanced-paren scan survived being replaced with a first-`)` search, because no fixture had a parenthesis inside its parameter list. The fixture has one now, and the mutation is red.

  Mutation-checked three ways: dropping the optional check turns 1 case red, splitting on every comma turns 1, and abandoning the balanced scan turns 1.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6648 tests (+11), build. No DB migration, no creds.

## Two suites that wrote the same twelve lines, and a comment that was not true (2026-08-24)

<!-- Promoted from .tasks/.suggestions.md, "Default-seam follow-ups": the second entry (`captureConsole` swaps globals, the runner interleaves, and `() => void` accepts an async arrow), the third (both seam modules point at the suite and it points back at neither) and the fourth (`warn`/`info`/`debug` are not captured). Checking the fourth found it was not speculative: two suites had written `withCapturedWarns` byte for byte because the helper knew only two streams. -->

- [DONE: 2026-08-24] [est: 10000] **`captureConsole` watches all five streams, hands back the callback's result, and refuses a thenable** — `__tests__/helpers/capture-console.ts`, two suites adopt it and delete their copies, `__tests__/default-console-seams.test.ts` +5 cases.

  **The doc comment claimed something the type does not do.** "Callbacks passed in must be synchronous, which the signature enforces" — `() => void` accepts an `async` arrow silently, and so does `() => T`. An async callback is captured up to its first `await` and then logs through a restored console, or, since node's runner interleaves cases, through the NEXT case's capture, which is a failure that surfaces somewhere else entirely. The result is checked for a `then` method instead, AFTER the console is restored so the refusal itself is readable, and on the SHAPE rather than `instanceof Promise` — a hand-rolled thenable has the identical problem. A negative control keeps the check about thenables: `{ then: "not a function" }` comes back through `result` untouched.

  **Three more streams, because the two-stream helper was producing copies.** `withCapturedWarns` existed twice, byte for byte, in `analytics-validate.test.ts` and `analytics-prop-values.test.ts` — a helper that knew only `log` and `error` sends anybody testing a `console.warn` back to swapping the method by hand. Both copies are gone and both suites call `captureConsole` at ten sites between them. `result` is the other half of what made adoption possible: the retired copies returned `{ result, warns }`, and without it those ten sites would each have had to assign into a local from inside the callback.

  **The restore puts the same function object back.** The first draft saved `console[method].bind(console)`, which restores behaviour and not identity — and the case asserting the console was given back compares against what it saved, so a bound wrapper reads as a leak. Caught by that case on its first run; the originals are stored directly now, with a line saying why.

  Also: the seams suite names the two modules it is about (`printProvenanceOutput`, `createDevLogger`), which both name it — the cross-reference ran one way. Named as a courtesy rather than as the rule, since `SEAMS` is derived and a third seam should arrive covered by the sweep and missing from the paragraph.

  Not done, and honestly: five suites still swap a console method by hand (`sentry-init`, `sentry-config`, `analytics-init`, `analytics-status`, `clarity`). Each has a reason a callback-scoped helper cannot serve — a swap held across `beforeEach`/`afterEach`, an `await`ed body, silencing rather than capturing — so a sweep banning the swap would be five exemptions out of seven. Filed as a suggestion instead of forced.

  Mutation-checked three ways: dropping the thenable refusal turns 2 cases red, dropping the `warn` channel turns 4 (two of them in the adopting suites, which is the adoption working), and restoring the console after the throw rather than before turns 2.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6637 tests (+5), build. No DB migration, no creds.

## The constant learned by hashing the empty string (2026-08-24)

<!-- Promoted from .tasks/.suggestions.md, "Derived-width follow-ups": the first entry (a SHA-256 of "" at module load, on the app's Sentry path, to learn a number the function's own body states), plus its second (the sweep says nothing about `__tests__/`), third (the module note still spelling "Sixteen" above a function that no longer does) and fourth (a shared `RegExp` whose lack of a `g` flag was luck rather than a stated property). -->

- [DONE: 2026-08-24] [est: 10000] **`POLICY_CHECKSUM_LENGTH` is declared and the SLICE reads it, the width sweep reaches the suites, and the shared pattern's statelessness is a rule** — `lib/privacy-translated-section.ts`, `__tests__/privacy-translated-section.test.ts`, 4 new cases.

  **A hash to learn a literal, at import, on the app's path.** The width was measured as `policyChecksum("").length` to stop four validators restating it — the right fix for the right problem, with one oddity left over: a SHA-256 of the empty string computed at module load to learn a number the function's own body spelled two lines up, in a module the app imports for `scrubPII`'s promise fingerprint rather than for any guard. It is declared now and the `.slice` reads it, which keeps every property the measurement bought — one statement, no validator able to disagree with another about what it is validating, a widening that is one edit and fails every recorded entry until the tables are re-recorded — and costs no hash to start the app.

  **What a declaration cannot do, said in a case.** A measured constant made "the slice stopped using it" inexpressible; a declared one agrees with any slice. So the function's OUTPUT is asserted against the number, over five inputs including a 4096-character one, rather than assumed to follow from it. Hard-coding the slice to 12 turns 13 cases red.

  **The sweep reaches `__tests__` now, which is where the fifth copy would land.** Two of the three original `/^[0-9a-f]{16}$/` copies were in suites and the sweep walked `lib` and `scripts` only — a suite's regex needs no import, so it is the cheapest place to write one. Read through `suiteCode`, so the prose above may go on quoting the shape it retired, and over `suiteFiles()` rather than the runnable set, because the rule is about what is WRITTEN and a shared helper is where a retired shape hides. One exemption, held honest by `assertExemptionsHonest`: `cloudinary-signed-upload.test.ts` asserts a forty-character SHA-1 from an external API, which moves for none of the reasons this width does.

  **The shared `RegExp` may not carry `lastIndex`.** One object, imported by both provenance modules and `.test`ed in a loop over a table; `g` or `y` would make the second entry judged from where the first left off. Adding `g` turns 6 cases red — including five per-language checksum cases, which is the interference itself rather than the rule about it.

  The offence pattern is now a named `WHOLE_STRING_HEX_RUN` read by both sweeps, since a rule spelled twice is what this whole describe exists against, and the module note stopped saying "Sixteen" above a function that no longer does.

  Mutation-checked four ways: hardcoding the slice (13 red), a `g` flag on the pattern (6), a suite restating the width (1), and an exemption that stopped needing the hole (1).

  `npm run verify` green: typecheck, `lint:all` 16/16, 6632 tests (+4), build. No DB migration, no creds.

## The age two of three pass lines could not give (2026-08-24)

<!-- Promoted from .tasks/.suggestions.md, "Published-age follow-ups", the first entry: "`PRIVACY_BODY_BASELINES` carries a `recordedOn` per page and `SCRUB_PROMISE_BASELINE` one for the whole record, and both pass lines still print only a count ... a reader scanning three lines will read the one that is different as the only one that has an age." Its fifth entry (a builder for the hand-written result literals) was closed on the way. -->

- [DONE: 2026-08-24] [est: 11000] **All three provenance pass lines publish how old their record is, over one shared walk** — new `lib/oldest-record.ts`, `lib/privacy-baseline-provenance.ts`, `lib/scrub-promise-provenance.ts`, `lib/privacy-translation-provenance.ts` repointed, new `__tests__/oldest-record.test.ts` (10 cases), 9 more across two suites.

  **One of three lines had an age, which is worse than none having one.** The translation guard started printing its oldest confirmation yesterday; the baselines and scrub-promise lines still printed a count and a base ref. Three green lines where one is different reads as "that table has an age and the other two do not", when in fact both carry the same dates and neither published them. Baselines now say `Oldest record 2026-08-11 (en, ru, be, pl, de, es).` and the promise record says `Recorded 2026-08-23.`

  **The walk is shared; the words are not.** `oldestRecord(table, dateOf, order)` is a leaf with no imports — the earliest date and every key carrying it, ordered by the caller's list rather than the object literal so the line does not change when somebody retypes a table. Both keyed tables delegate to it and keep their own nouns: the translation table's date is `checkedOn` because somebody CONFIRMED a page on it, the baselines' is `recordedOn` because somebody MEASURED one. That the two disagreed about nothing but the field name is exactly why the tie-break should not have been written twice.

  **A single record has an age, not an oldest.** `SCRUB_PROMISE_BASELINE` is one record, so there is no key to name beside the date and no absent case to render; its line is one sentence rather than a degenerate use of the walk, and it is carried through the result rather than read from the constant by the formatter, so the line describes the record that was actually evaluated. Six baselines rather than five languages, deliberately: the English original has a baseline and the five translations are measured against its count, so a stale reading of it is the staleness that matters most — and it is the one a translated-codes list would have silently dropped.

  Also closed: the formatter cases in the baselines suite were hand-building the whole result literal, so the new field broke two cases that are about WORDING — the second time this month a provenance suite paid that. They take a `result({...})` builder now.

  Mutation-checked three ways: dropping the age from the baselines line turns 3 cases red, dropping it from the promise line turns 4, and walking the object literal instead of the caller's order turns 5 — including two that predate this change, which is the evidence the delegation kept `oldestConfirmation`'s behaviour.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6628 tests (+19), build. No DB migration, no creds.

## Two sentences no run can print, deleted rather than proofread (2026-08-24)

<!-- Promoted from .tasks/.suggestions.md, "Erased-callback follow-ups", the first entry: "`defineProvenanceTable` erases `spec.unparseable`, and the entries that declare those two collapse `unparseable` into `absent`, so neither is reachable from either direction." Its third, fourth and fifth entries were closed on the way (the blast radius written at the field, the twice-written fixture named, the two collapsing reasons made per-parser). -->

- [DONE: 2026-08-24] [est: 10000] **`ProvenanceTableSpec.unparseable` is optional, the two unreachable sentences are gone, and `stopsTheRun` carries the decision out of the closure** — `lib/provenance-tables.ts`, `__tests__/provenance-tables.test.ts`, 3 new cases.

  **The prose nobody could proofread.** Every registry entry declared an `unparseable` sentence and only `body-baselines` could reach one: `translation-sources` and `scrub-promise` collapse that classification into `absent`, so their two sentences were reachable from neither direction — no run printed them and no case could read them, because `defineProvenanceTable` erases the spec with the generic. They could have named the wrong module or the wrong table, or been a copy of a neighbour's line, and every build stayed green. Deleted, and the field is now optional so an entry that cannot reach one does not write one.

  **`stopsTheRun` is derived, not declared.** It is `spec.unparseable !== undefined` — the presence of the sentence, which is the single statement of the fact, surfaced where a case can read it. The registry case previously written as a limit ("prose no run can print is prose no case can proofread") is now a rule: each entry is run against a present-but-unreadable source and the OUTCOME must agree with the flag, in both directions. A sentence written for a `classify` that can never produce one is red; so is a `classify` that reaches for a sentence its entry never wrote.

  **The unreachable branch refuses in words.** Typing cannot rule out the second direction (a two-way `classify` is assignable to the three-way signature), so the loop answers it rather than falling through to an evaluation that would print a pass over a table nothing compared: a `checkError` line naming the entry, its module and the revision, and telling the reader which of the two edits to make.

  Three things closed alongside, all from the same suggestion block: the blast radius of declaring a sentence (the first `unparseable` suppresses every later table's report) is now written AT the field where the decision is made rather than one module over; the two collapsing entries stopped giving one copy of the same reason and each names its own parser, since the trigger is per-parser and they stop being ready at different moments; and the present-but-unreadable fixture, written out by hand in four cases with one already drifted to different wording, is `PRESENT_AND_UNREADABLE`.

  Mutation-checked both ways: giving the translation entry back an `unparseable` sentence it cannot reach turns the new registry case red on its own, and giving it the three-way classification without one turns 4 cases red.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6609 tests (+3), build. No DB migration, no creds.

## The classification that lived in a comment, and two sentences no run can print (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Provenance-registry follow-ups", the third entry: "Every table's `driftSkipped` and `unparseable` sentence is a template literal in the registry ... The existing case requires the skip lines to DIFFER and to name their own module; nothing says the same about the two `unparseable` sentences." Checking it found the entry's premise ("only one table can produce one") was right and the reason was undocumented in code — so the shipped rule is about the classification, not about the wording. -->

- [DONE: 2026-08-23] [est: 8000] **Pin which registry entries stop the run on an unreadable module, and hold the one reachable refusal to what the skip lines are held to** — `__tests__/provenance-tables.test.ts`, 2 cases.

  **One entry stops the whole run and two do not, and that was a comment.** `classify` is where a table decides what "the module was there and yielded nothing" means. `body-baselines` calls it `unparseable` — a hard failure that prints nothing else, so a mangled baselines module suppresses a translation report that would have been fine. `translation-sources` and `scrub-promise` collapse it into `absent`, a skip, each with a paragraph saying why: their parsers are new and this suite round-trips them every run. Nothing held either entry to its half, so flipping one was a two-line edit that changed which failures a reader ever sees, in a file whose other properties are all guarded.

  **Asserted per id, not counted.** "One of the three" is the fact; WHICH one is the decision. A collapsed entry must also still produce a `driftSkipped` line — collapsing into `absent` and then saying nothing is a pass over a table nothing compared, which is the vacuous green the whole guard exists against.

  **The reachable refusal now answers the two questions a reader has.** It is the message that stops a run, so there is no other line telling them which module and which base revision the guard was looking at; both are asserted, along with its being a `checkError` line at all.

  **What this deliberately does NOT do, and the honest reason.** The suggestion asked for the `unparseable` sentences to be pairwise distinct, the way the skip lines are. They cannot be read: `defineProvenanceTable` erases `spec.unparseable`, so the two collapsed entries' sentences are reachable only through a `run` that their own `classify` can never produce. Prose no run can print is prose no case can proofread — written into the suite as the limit rather than worked around by widening the registry's public surface for a test, which is the seam this session closed twice elsewhere. Filed as a suggestion instead.

  Mutation-checked both ways: giving the translation entry the three-way classification turns 3 cases red (including two that existed before), and dropping the module name from the baselines refusal turns 2 red.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6606 tests (+2), build. No DB migration, no creds.

## The age that only ever reached a reader as a failure (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Privacy-translation follow-ups", the first entry: "Nothing renders `checkedOn`, so the age of a confirmation is invisible until something breaks — a translation confirmed against an English section that has not changed in a year is green and indistinguishable from one confirmed yesterday ... A published line — oldest confirmation, per page — costs nothing and is the kind of number a person acts on before a suite makes them." -->

- [DONE: 2026-08-23] [est: 9000] **`oldestConfirmation`, and the pass line that now says how long a page has gone unlooked-at** — `lib/privacy-translation-provenance.ts`, 6 new cases, 1 fixture repaired.

  **The table's whole subject was legible only on a red build.** `PRIVACY_TRANSLATION_SOURCES.checkedOn` records the day somebody confirmed a translated disclosure still says what the English says, and nothing printed it. A page confirmed a year ago against an English section that has not moved since is green, and indistinguishable in every line the guard emitted from one confirmed this morning — correct in that there is nothing to fix, and it hides the case a reader would want: a disclosure that is old because nobody is looking rather than because nothing changed. The pass line now ends `Oldest confirmation 2026-08-22 (ru, be, pl, de, es).`

  **On the PASS line and nowhere else**, which is a decision rather than an omission: a failing run prints findings, and an age line under them is noise beside the thing that has to be fixed. A case asserts both halves.

  **Every language at the oldest date, ordered by the constant.** A table recorded in one sitting has all five on the same day, and a line naming one of five reads as a fact about that page. The order comes from `PRIVACY_TRANSLATED_LANGUAGES` rather than from `Object.keys`, so a reader's line does not change when somebody re-sorts the table literal — the fixture deliberately writes its keys in a different order, so the claim has a case that can fail.

  **String comparison is the ordering**, correct for `YYYY-MM-DD` and only ever asked of a table whose dates already passed their shape check, since the line it feeds is printed on a pass and a malformed date is a failure.

  One thing fixed on the way: `__tests__/provenance-key-set.test.ts` hand-builds a `TranslationProvenanceResult` to render one refusal, and the new field made that a compile error — which is the field doing its job. It passes `null`, with a note saying why a fixture date there would be a claim nobody reads.

  Mutation-checked both ways: taking the LATEST date turns 2 cases red, and ordering the languages by `Object.keys` turns 2 red including the one whose fixture exists for it.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6604 tests (+6), build. No DB migration, no creds.

## The width the function decides and three validators remembered (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Promise-fingerprint follow-ups", the second entry: "`policyChecksum` is now exported and three call sites take it, so 'sixteen hex of SHA-256' is one function and its OUTPUT LENGTH is still a number in three tables ... Deriving both from the function would make a change to the fingerprint width one edit instead of three, and would catch the version of this bug where the function is widened and the tables silently keep accepting the old width." -->

- [DONE: 2026-08-23] [est: 10000] **`POLICY_CHECKSUM_LENGTH` / `POLICY_CHECKSUM_PATTERN`, measured from `policyChecksum` and imported by everything that validates a recorded one** — `lib/privacy-translated-section.ts`, both provenance modules repointed, 2 suites repointed, 4 new cases and a sweep.

  **Four coordinated edits, with a nasty way to get one wrong.** `/^[0-9a-f]{16}$/` was written out in `lib/privacy-translation-provenance.ts`, `lib/scrub-promise-provenance.ts` and twice in `__tests__/privacy-translated-section.test.ts`, and each is what decides whether a table's recorded fingerprint is well-formed. Widening `policyChecksum` therefore meant moving the number in four places: re-record the tables correctly, miss one validator, and that validator rejects the CORRECT values as `malformed_checksum` with the entry quoted — a red build blaming the data for a width nobody moved.

  **The length is measured, not restated.** `POLICY_CHECKSUM_LENGTH = policyChecksum("").length`, and the pattern is built from it. Lowercase hex stays a character class because it is `digest("hex")`'s own alphabet rather than a separate decision, and the pattern is anchored — a `{16}` matching anywhere accepts a full 64-character digest that was never sliced, which has its own case.

  **What a derivation cannot notice is that the width CHANGED**, since it agrees with any value. So sixteen is asserted once, in the suite, with the reason next to it: short enough to read in a diff and retype into a table, long enough not to collide. Moving it means re-recording every fingerprint in the repository, and now that is one edit plus one line in a diff rather than four edits and a guess.

  **The sweep is over a whole-string run of hex**, not over `[0-9a-f]{n}` anywhere: `lib/uuid.ts` and `lib/analytics-mirror-payload.ts` both hold `[0-9a-f]{8}-[0-9a-f]{4}-…`, and five runs separated by hyphens is a different shape validating a different thing. Reporting them would have been the first entry in an exclusion list — the failure the first draft of this sweep actually produced, and the reason the rule is `^…$` rather than a name list.

  Mutation-checked both ways: widening `policyChecksum` to 32 turns 19 cases red across the two tables (before, the shape halves would have gone on passing the old values while the drift halves failed), and putting a literal `{16}` back into either provenance module turns the sweep red by module name.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6598 tests (+4), build. No DB migration, no creds.

## The parameter that exists for the suite, and the overload that ships (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Stops-and-prints follow-ups", the fourth entry: "`printProvenanceOutput` defaults its console and the script relies on the default — so the parameter exists only for the suite, which is honest but means the production path is the untested overload." Scanning for the shape found a second instance, `createDevLogger`, whose default is what the app-wide `devLog` is built from. -->

- [DONE: 2026-08-23] [est: 11000] **Exercise the defaulted console in both seams, and put a rule under the third** — new `__tests__/helpers/capture-console.ts`, new `__tests__/default-console-seams.test.ts` (4 cases), 2 cases added and 1 tightened across `__tests__/provenance-report.test.ts` and `__tests__/safe-log.test.ts`.

  **A seam covered everywhere except where it runs.** `printProvenanceOutput(output, out = console)` and `createDevLogger(isDev, sink = console)` both take a console so a case can be one, and every case was. Which left the DEFAULT — the spelling `npm run lint:baseline-provenance` prints through, and the one `devLog` is built from — with no case at all. It could have named a method the console does not have, routed both streams to `error`, or been dropped for a required parameter, and every case beside it stays green while the guard prints nothing and a dev build crashes on its first debug line.

  **`captureConsole` swaps the globals, because nothing else can see a default.** A default parameter is not readable through the function object, and a source-text regex for `= console` pins the spelling rather than the behaviour; calling with the argument omitted and watching `console.log`/`console.error` is what actually closes it. The restore is in a `finally` and has its own case with a throwing callback — a helper that could leave the runner without a console is worse than the gap it closes.

  **One claim was prose and is now an assertion.** `devLog` under the node runner was pinned by `assert.doesNotThrow` with a comment saying "should produce no console output" — a claim the no-op logger and a logger printing the payload in the clear both satisfy. It asserts the empty capture now, on both streams.

  **The sweep is the point, not the two cases.** Two cases fix today's two seams; the rule is that a third cannot arrive without one. The population is derived (`= console` on a parameter, anywhere under `lib/` or `scripts/`), and since "the default is exercised" is not statically observable, the rule asks for the closest proxy: the module's suite reaches for `captureConsole`, which is the only way in this tree to watch the global console. It carries the positive control a sweep over an absence needs — at least one seam must still exist, or the suite is passing over an empty set. The regex is bounded by `[^\n=]` rather than `[^,()]`: both seams are typed `Pick<Console, "log" | "error">`, and a class excluding commas matched neither.

  Mutation-checked three ways: dropping `captureConsole` from `safe-log`'s suite turns the sweep red by module name, defaulting the sink to a swallowing stub turns the new `createDevLogger` case red, and pointing `printProvenanceOutput`'s default at `console.error` for both streams turns the new provenance case red.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6594 tests (+6), build. No DB migration, no creds.

## The token that was a line number, and the fifth refusal that printed for itself (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Sweep-predicate follow-ups", the second and fourth entries filed by the run directly below: "The predicate's lookahead matches a name-shaped token before the separator, so `1: ERROR — ` would report"; and "`stop`'s comment now describes four callers and the count is in prose ... A case asserting the number of `stop(` call sites would pin it." Scanning for the fifth caller the second entry anticipated found it already written, by hand, in `resolveRootOrExit`. -->

- [DONE: 2026-08-23] [est: 9000] **`buildsARefusalByHand` now wants a NAME rather than a token, `stop`'s caller count is pinned, and the last hand-written refusal block is routed through it** — `__tests__/check-error.test.ts`, `__tests__/privacy-baseline-provenance-script.test.ts`, `scripts/check-privacy-baseline-provenance.ts`.

  **`[\w-]+` is also the shape of a line number.** The sweep's rule was "the separator with a name-shaped token in front of it", and `  1: ERROR — Type X is not assignable` satisfies it — a quoted compiler diagnostic being the single most likely thing for a fixture in this tree to hold. Reporting it would have made a guard an offender for quoting the output it refuses over, which is how a rule starts collecting exclusions instead of stating a shape. A check name is `check-…` or another identifier, so it starts with a LETTER and is two or more characters; `[A-Za-z][\w-]+` says that, and a name that merely contains digits (`check-i18n`) stays an offender, which is its own case.

  **Prose said four, and the file said five.** `stop`'s doc comment is the only place the guard's endings are counted, and the count had already gone stale once. Looking for the fifth caller it would eventually gain found it: `resolveRootOrExit` was still spelling out `console.error(error.message); process.exit(1);` — the exact four-line block `stop` was extracted from, at the EARLIEST refusal the guard can raise, during module load before `main` is entered. It goes through `stop` now (a hoisted function declaration, so a caller that runs before it is declared is fine), and the refusal is byte-identical: checked by hand against both a `GuardRootError` (a relative `PRIVACY_BASELINE_REPO_ROOT`) and the `git()` path beside it.

  **Two cases, and only one of them is a count of exits.** `stop(` call sites are pinned at five, with the declaration excluded by the `function` keyword in front of it rather than by counting one fewer — a `const stop = (…)` rewrite cannot silently drop the number while the callers are unchanged. The second case pins `console.error` at ONE and deliberately does not pin `process.exit(1)`: two of that call's four sites are the silent endings after `printProvenanceOutput` has already said everything, and an exit that prints nothing is not a second copy of `stop`. A refusal that prints for itself is.

  Mutation-checked both ways: widening the predicate back to `[\w-]+` turns the line-number case red, and restoring the hand-written block in `resolveRootOrExit` turns both new cases red — the caller count at four and the second `console.error`.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6588 tests (+4), build. `npm run lint:baseline-provenance` reports the same three well-formed lines. No DB migration, no creds.

## A sweep that could not tell building from mentioning (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Refusal-line follow-ups", the second and fifth entries filed by the run directly below: "The `CHECK_ERROR_PREFIX` sweep matches the separator anywhere in a file, including inside a message a module quotes"; and "The script's `stop` is now reached from inside `git()` ... its doc comment still describes it [as one of the three endings of `main`]". -->

- [DONE: 2026-08-23] [est: 6000] **`buildsARefusalByHand`, tested against strings a healthy tree cannot produce; and `stop`'s comment told about its fourth caller** — `__tests__/check-error.test.ts`, `scripts/check-privacy-baseline-provenance.ts`, 3 cases added.

  **The offence was "contains the separator", which is the offence plus two things that are not.** A fixture holding a guard's output for a case to assert against, and a module documenting the convention itself, both contain it. `lib/provenance-key-set.ts` misses being reported only because its mention sits in a comment `stripComments` removes — the same sentence in a real string would have read as a second copy of the formatter, and the fix a reader would reach for is an exclusion list, which is how a sweep starts collecting exceptions instead of stating a rule.

  **What a hand-built line actually is: the prefix with a NAME in front of it.** Two ways to write that — interpolate one (all nineteen sites this replaced) or spell one — and both are matched. A separator with no name before it is a mention.

  **The rule is a function, so it can be tested on inputs the tree cannot supply.** A sweep's predicate is normally observable only through whatever the repository happens to contain, which means the tolerant half is never exercised: nothing here currently quotes the prefix in a string, so "it does not report a mention" was a claim with no case behind it. Three cases now cover both directions from fixtures.

  **`stop` has four callers and its comment said three.** `git()` refuses a `git` that could not be run at all, during argument resolution, before `main` has a comparison to stop on — so `stop` is the guard's ending rather than `main`'s. A reader with it filed as "one of the three stops in `main`" is a reader who eventually moves it there.

  Mutation-checked in both directions: restoring one module's template literal turns the sweep red by module name, and a module added purely to quote the prefix in an exported string is NOT reported — which the previous version would have failed on.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6584 tests (+3), build. No DB migration, no creds.

## Nineteen copies of the line a guard refuses in (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Map-and-narrowing follow-ups", the fourth entry: "`resolveComparison`'s `fail` variant carries a `reason` that only one caller turns into a message, prefixing the check name — the other two stops receive a fully formatted refusal from a pure function. Giving that variant a `refusal` field would ... remove the last `ERROR — ` template literal from `main`, which is where the guard's wording last liv[es]." Scanning for that last literal found eighteen more, so the entry's shape was right and its scope was one script too small. -->

- [DONE: 2026-08-23] [est: 20000] **`lib/check-error.ts`, and the nineteen sites that spelled it out** — new leaf module, 11 modules and 1 script repointed, new `__tests__/check-error.test.ts` (6 cases), `Comparison`'s `fail` variant carries a refusal instead of a reason.

  **A convention nobody could restate.** `` `${checkName}: ERROR — ${message}` `` was written at nineteen sites in eleven modules and a script. It is load-bearing in two ways that no single copy shows: the check NAME comes first, so a log holding four guards' output can be read by guard, and the em dash separates a machine-readable prefix from prose so the halves can be told apart without parsing. Nineteen copies is nineteen chances to write `ERROR: `, or a hyphen for the dash, or to drop the name — and the failure is not a red build, it is a line that scrolls past looking almost right.

  **A LEAF, no imports**, for the same reason the language list is one: guard modules are read by node build scripts, and this is the module every one of them wants. Not a logger and not a thrower — it returns the line, because guards here separately decide whether a refusal is printed, collected into a report, thrown (`GuardRootError` builds its message through it) or handed back to a caller that will choose.

  **The suggestion's own site, done last.** `Comparison`'s `fail` variant carried a bare `reason` and `main` spliced the name and dash back on — the one caller that could disagree with the two beside it, which receive finished lines from pure functions. It carries a `refusal` now, built where the sentence is decided, and `main`'s three stops are three of the same statement. The script's other literal, the one for a `git` that cannot be run, went through `stop` at the same time: it was a `console.error` plus a `process.exit(1)` written out beside a helper that is exactly those two lines.

  **The refactor is behaviour-preserving and the existing suites are the proof.** Every message is byte-identical, and several hundred cases assert on those exact strings — including six that match `ERROR — ` by regex. All 6581 pass, and `npm run lint:privacy-baseline-provenance` reports the same three well-formed lines.

  **The sweep looks for the string, not for a list of modules.** Any file under `lib/` or `scripts/` that spells the separator is an offender, so a twelfth guard is covered without being added to anything. Its positive control is the one a sweep over an ABSENCE needs: a tree where nothing refuses anything would satisfy it perfectly, so at least eight modules must still build a line through the helper — an argued floor, four below the twelve that do.

  Mutation-checked: restoring one module's template literal turns the sweep red by module name.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6581 tests (+6), build. No DB migration, no creds.

## Two floors that look identical and mean different things (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Exclusion-and-control follow-ups", the first and fifth entries filed by the run directly below: "`declaresAConstant` builds a `RegExp` per constant per file, inside a filter over every module in two directories"; and "Two floors in this family now carry paragraphs explaining which kind they are ... a named helper would put the distinction in the code instead, and there is a third floor in `__tests__/privacy-translated-section.test.ts` that carries neither label". -->

- [DONE: 2026-08-23] [est: 9000] **`measuredFloor` and `arguedFloor`, applied at the family's three floors** — new `__tests__/helpers/coverage-floor.ts`, new `__tests__/coverage-floor.test.ts` (7 cases), 3 call sites, plus a per-file `RegExp` hoisted out of a filter.

  **Ninety-odd assertions that look like one thing and are two.** A sweep that walks the tree and matches nothing reports CLEAN, which is the failure a structural case cannot survive, and `assert.ok(found.length >= N)` is the guard against it. The number means one of two things. A MEASURED floor is the count today: it catches a pattern that stopped matching and nothing else. An ARGUED floor sits below the count on purpose, so ordinary growth needs no test edit, and holds the sweep to a coverage claim somebody decided — "no fewer than the six the hand-written list this replaced covered" is the one that produced the helper. Written out they are indistinguishable, so a reader meeting a failing one cannot tell whether to raise the number or investigate — and on a measured floor, raising it is the fix that hides the break forever.

  **Message builders, not assertions.** An `assertMeasuredFloor(...)` calling `assert.ok` itself would report every failure at the helper's line rather than the case's, which is the first thing a reader needs. The assertion stays at the call site and takes its message from here: the kind is in the code, greppable, impossible to leave off, and the failure still points where it happened.

  **The reason is required on an argued floor and absent on a measured one**, which is the difference stated as a signature — a number chosen rather than observed is exactly the one that reads as arbitrary six months on, and a measured floor's reason is that it is the count. A case reads the signature, because a missing argument is a compile error rather than something a case can call.

  **The third floor got classified.** `__tests__/privacy-translated-section.test.ts` floored the translated-page count at five and said neither which kind nor why; it is a measurement, and now says so.

  **A sweep case keeps the family honest.** The three suites must each state a kind, so one going back to a hand-written message is caught rather than starting the drift back to prose. And the helper is refused to production: a `lib/` module importing it would mean a floor had reached the app, which is a different thing wearing the same word.

  **A regex per constant per file, hoisted.** `declaresAConstant` compiled two patterns for every module in two directories, because the pattern interpolates the constant's name — the shape that gets rebuilt inside a loop without anybody noticing. Two compiled once outside the filter.

  Mutation-checked: replacing one sweep's `measuredFloor(...)` with the template literal it used to have turns the family case red by suite name.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6575 tests (+7), build. No DB migration, no creds.

## Three gaps in the sweep written one run earlier (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Derived-sweep follow-ups", the second, fourth and fifth entries filed by the run directly below: the two floors "whose number is an argument rather than a measurement"; "Two modules are excluded from the population by name ... The leaf's exclusion could be derived (it is the module that EXPORTS them)"; and "The re-export backstop reads `lib/privacy-page.ts` for a pattern that the module has never matched ... it would pass identically against an empty file". -->

- [DONE: 2026-08-23] [est: 7000] **The leaf's exclusion is derived, the re-export refusal has a positive control, and the two floors say which kind they are** — `__tests__/provenance-key-set.test.ts`, `__tests__/provenance-report.test.ts`, 2 assertions added and 2 comments that carry a decision.

  **A list replaced by a derivation, with two paths left in the filter.** One of them had a property to be derived from: the leaf is the module that EXPORTS the constants, so it is excluded for the thing that makes it the leaf. Moving the declaration to a differently-named module now excludes that one instead, with nothing to update — where a path in a filter would have quietly swept a module it was written before.

  **The other exclusion stays by name and now says why it cannot be derived.** `lib/privacy-page.ts` is not the declarer and IS a genuine asker: it reads the list to build the picker, exactly like the other seven. What takes it out of the sweep is history — it is the module the constants were moved out of, so "does it take them from the renderer" is a question about itself.

  **A derivation can over-match, which the floor does not catch.** A regex matching everything leaves an empty list (the floor fires); one matching nothing leaves the LEAF in the sweep, and nothing else would notice. So both exclusions are asserted by name, and a third assertion goes red when the leaf stops exporting under those names — the moment the exclusion starts matching nothing and the leaf is out of the sweep by luck.

  **A refusal with no positive control passes against an empty file.** The re-export backstop asks whether `privacy-page` re-exports a language constant, which it has never done. That assertion is equally green against a file somebody is mid-way through emptying, so it now also requires the module to still export `renderPrivacyPage` — that the refusal is being read against the page renderer at all.

  **Two floors, two different kinds, and both now say so.** Six here is an ARGUMENT (what the hand-written list covered, one below the current seven, so growth does not have to be ratified); five in `provenance-report` is a MEASUREMENT (the count the pattern matches today, catching a pattern that stopped matching). They look like the same construct and are not, and the note at each says editing one to match the other loses the reason it was written.

  Mutation-checked: dropping `export` from `renderPrivacyPage` turns the backstop's case red, and rewriting the leaf's declarations as a trailing `export { ... }` — which keeps the module working and stops the exclusion matching — turns the asker case red by name.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6568 tests, build. No DB migration, no creds.

## A parse per finding, made inexpressible rather than merely avoided (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Orphan-scanner follow-ups", the second entry filed by the run directly below: "`localesDeclaring` re-parses the whole translations source once per orphan key — `findLocaleBlock` is called `orphans × languages` times over the same string ... One `Map` from code to block, built once, is the fix". -->

- [DONE: 2026-08-23] [est: 8000] **`indexLocaleDeclarations`, and a lookup that no longer holds a source it could re-parse** — `lib/check-orphan-i18n-keys.ts`, 2 cases added in `__tests__/check-orphan-i18n-keys.test.ts`.

  **Two costs, both invisible at three orphans.** `localesDeclaring` took the translations source and called `findLocaleBlock` itself, once per locale per finding: a 500-key file re-parsed six times a finding, and then `block.keys.includes(key)` scanning an array of ~498 keys linearly for each. Neither has a reason to exist and both grow with the thing the guard is for — a run that finds a dead family finds a run of adjacent keys, which is exactly when the multiplier is largest.

  **The fix is a signature, not a discipline.** A hoisted call is a thing a reader has to keep hoisted, and the obvious simplification a year from now is threading the source back in. `localesDeclaring(declarations: LocaleDeclarations, key)` does not HAVE a source, so a per-key re-parse is not a shape it can express; the parse happens once in `findOrphanI18nKeys` and the compiler holds the rest.

  **A missing locale is missing, not empty.** The index omits a language the source does not declare rather than mapping it to an empty `Set` — "this checkout has no `de` map" and "`de` translated nothing" are different facts that an empty set would print identically.

  **Order stays the constant's, not the map's.** They agree, because the index is filled by walking `TRANSLATION_LANGUAGES`. The lookup walks the constant anyway: the report prints this list verbatim, so it should depend on the thing that documents itself as picker order rather than on how the index happened to be filled.

  **Two cases, and the second one covers what a type cannot.** The signature is asserted in source text, because a guarantee living in a parameter list is one nobody reads until they are already changing it. Then: the index must be built BEFORE the per-key walk, which is a statement order and the one remaining way to pay the cost twice.

  Mutation-checked: restoring the old `localesDeclaring(i18nSource, key)` and its call turns the new case red by name while every behavioural case stays green — which is the point of writing it structurally.

  `npm run lint:orphan-i18n` still reports 277 files scanned and every base key read.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6568 tests (+2), build. No DB migration, no creds.

## The sweep that found a seventh asker the list never had (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Language-leaf follow-ups", the first entry: "The asker list in the new sweep is six module paths written by hand, which is the fourth hand-written module list in this family ... This one could be derived the same way (every `lib/privacy-*` and `lib/*-provenance` module) and would then cover a seventh". The suggested pattern was rejected on inspection — see below — and the derivation used instead is sharper. -->

- [DONE: 2026-08-23] [est: 9000] **The language-list sweep derives its own population, and the rule it enforces got sharper because of what the derivation found** — `__tests__/provenance-key-set.test.ts`, 1 case added and 1 rewritten.

  **A list that goes stale in the direction a sweep cannot survive.** Six module paths were typed into the case. A seventh module reaching for the page renderer is a module the sweep does not look at, so it reports clean about a tree it did not read — the one failure mode a structural guard has. It was also the fourth hand-written module list in this family.

  **The suggested pattern was rejected.** `lib/privacy-*` plus `lib/*-provenance` misses `lib/bundle-smoke.ts` (which is neither) and catches the leaf and the renderer (which are not askers). The population is "modules that want the language list", and the honest way to ask that is what a module NAMES: deriving it from the IMPORT would have made the positive control vacuous, since every member would import the leaf by construction. Naming a constant is independent of where it came from, which is the property the sweep is about.

  **The walk found a seventh asker on its first run** — `scripts/build-spa-fallback.ts`, which reads both constants to write the six pages and was never in the list. It is named in a case of its own, so the derivation has to keep buying something rather than be taken on trust, alongside a floor of six that stops a walk matching nothing from reading as a clean sweep.

  **And it found the rule was wrong.** The seventh asker imports `renderPrivacyPage` from the renderer, because writing the pages is its whole job — so "does not import `privacy-page`" reported it as an offender. That shape was only ever survivable because the hand-written list happened to contain no module that renders a page. What is actually banned is taking the LANGUAGE LIST from the renderer, which is a different clause of the same import, so the check now reads the named-import clause and asks whether either constant is in it.

  **The backstop says why it cannot fire.** The constants were MOVED, so `privacy-page` exports neither and the banned import does not compile. A companion assertion refuses a re-export from `privacy-page`, which is the day the compiler stops caring and the sweep becomes the only thing that does.

  Mutation-checked twice, both ways the old list could not be: adding a re-export to `privacy-page` and rerouting `bundle-smoke` through it turns the case red by module name, and a NEW `lib/privacy-seventh-asker.ts` importing the list from the renderer is caught without anybody adding it to anything.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6566 tests (+1), build. No DB migration, no creds.

## The default that was one of six by convention, and a fourth copy of the locale list (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Language-leaf follow-ups", the second and third entries: "`PRIVACY_DEFAULT_LANGUAGE` is a bare `"en"` and `PRIVACY_PAGE_LANGUAGES` is a `readonly {code: string}[]`, so nothing ties them together in the type system" and "The leaf has no suite of its own and its three cases live in `__tests__/provenance-key-set.test.ts`". Also closes the "Closed-set follow-ups" entry asking for a `PrivacyPageLanguageCode` union derived from the literal. The bug scan that follows every task found the same shape one module over, in i18n. -->

- [DONE: 2026-08-23] [est: 14000] **`PrivacyPageLanguageCode`, the leaf's own suite, and the orphan scanner's private locale list** — `lib/privacy-languages.ts`, `lib/check-orphan-i18n-keys.ts`, new `__tests__/privacy-languages.test.ts` (8 cases), 2 cases moved out of `__tests__/provenance-key-set.test.ts`, 3 cases added in `__tests__/check-orphan-i18n-keys.test.ts`.

  **Two constants side by side with nothing between them.** `PRIVACY_PAGE_LANGUAGES` was annotated `: readonly PrivacyPageLanguage[]` and `PRIVACY_DEFAULT_LANGUAGE` was a bare `"en"`, so a default of `"eng"` — or an `en` entry deleted from the list — was a contradiction nothing in the repository would have found. What it produced was not an error: a `/privacy` root page with no picker entry, a `PRIVACY_TRANSLATED_LANGUAGES` silently six long instead of five, and a provenance guard measuring `PRIVACY.md.eng` against a diff and reporting the file as UNTOUCHED. One one-word mistake arriving in three places far from itself.

  **`as const satisfies`, which is the whole mechanism.** The annotation was the thing widening every code to `string`; `satisfies` checks the same shape and keeps the literals, so `PrivacyPageLanguageCode` is the six-literal union and `PRIVACY_DEFAULT_LANGUAGE: PrivacyPageLanguageCode` is a compile-time tie rather than a comment. The runtime membership checks in the three provenance evaluators stay — their keys come out of object literals and git output, which no compiler sees. What the type buys is the other half: a value written in SOURCE as one of the six, checked when it is typed.

  **The half that could be restored by a helpful reader** is `: readonly PrivacyPageLanguage[]`, which collapses the union to `string` and leaves the default's annotation LOOKING like a constraint while accepting anything. Pinned twice: a source-text case naming that exact mutation, and `type CodeUnionHasWidened = string extends PrivacyPageLanguageCode ? true : false` assigned `false`, which fails `npm run typecheck` — the leg `npm test` runs first — rather than any case.

  **The leaf got its suite.** Its three module-shape cases shipped inside `provenance-key-set.test.ts`, which was right for the sweep over the six askers and wrong for the other two: that the leaf has no imports, and that the page renderer reads the same list. Neither is about the provenance family, and a reader deleting the module would not think to look in a key-set suite for what breaks. The sweep stayed where the askers are.

  **The bug scan found the same shape in i18n.** `localesDeclaring` in `lib/check-orphan-i18n-keys.ts` walked `["ru", "en", "be", "pl", "de", "es"]` written out in the `for` — a fourth copy of a constant every other i18n suite derives from `TRANSLATION_LANGUAGES`. It fails quietly and in the direction that matters: the report exists to say what deleting an orphan would touch, so a seventh language translating the key means "declared in ru, en" where the truth is three locales, and the number a reader acts on is wrong. Now derived; `i18n-coverage` imports nothing back this way, so no cycle.

  **The two behavioural cases for it cannot catch the regression and the third says so.** The hand-written list held the same six codes in the same order, so attribution and ordering cases pass either way today and diverge only on the run after a seventh language lands — the run the guard is meant to already be right on. The mutation is textual, so the case is: the module must read `TRANSLATION_LANGUAGES` and must not spell a locale array again.

  Mutation-checked, four ways: a default of `"eng"` turns two cases red by name; re-annotating the list fails typecheck at the widening guard AND turns the literal-codes case red; restoring the hand-written locale list turns the derivation case red while the two behavioural cases stay green, which is the point of the third.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6565 tests (+11), build. No DB migration, no creds.

## Six modules that wanted six codes and got a page renderer (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Closed-set follow-ups", the third entry: "Four of the six privacy modules now import `privacy-page` only for its language list ... `privacy-page` also carries the HTML renderer, the CSP string and the language picker, so four provenance modules pull a page renderer into their import graph to ask which six codes exist. Splitting the list into its own module is the kind of change that is cheap now and awkward once something bundles one of them." Closes the "Shape-rule follow-ups" entry about the same constant being reached through two paths, which is the same split from the other side. -->

- [DONE: 2026-08-23] [est: 12000] **`lib/privacy-languages.ts` — the list, on its own** — new leaf module, 10 importers repointed, 3 cases in `__tests__/provenance-key-set.test.ts`.

  **What the six modules were actually asking for.** `PRIVACY_PAGE_LANGUAGES` and `PRIVACY_DEFAULT_LANGUAGE` lived beside an HTML renderer, a markdown-to-HTML pass, a CSP string and a language picker. Six modules import them and none wants any of that: four are provenance evaluators comparing a recorded value against a table key, one is the baselines table, one is the build's bundle smoke check. The list is now a leaf with two constants and a type in it.

  **The two-path import, closed.** `lib/privacy-body-baselines.ts` imported the list and `lib/privacy-baseline-provenance.ts` imported both that module and the list directly — the same constant reached two ways, harmless until somebody moves the path helper and closes a cycle. There is one path now.

  **MOVED, not re-exported**, which is the difference between a split and a redirect. `lib/privacy-page.ts` imports the two constants like everybody else and exports neither, so a module that reaches for them through the renderer fails to compile rather than passing a sweep. The type system is the guarantee; the sweep is the backstop for the day somebody adds a convenience re-export.

  **Three cases, each with its positive control.** No asker imports `./privacy-page` — AND all six do import `./privacy-languages`, so a module that stopped reading the list cannot pass as a clean sweep. The leaf has no imports at all, because an import here is one every provenance module inherits. And the page renderer reads the same list rather than declaring a second one.

  Mutation-checked: adding any import to the leaf turns its case red by name. The first sweep could not be mutation-checked the usual way and that is the point — reverting an importer to `privacy-page` fails at the import, because the constants are not there to be imported.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6554 tests (+3), build. No DB migration, no creds.

## The script, told the truth about itself (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Stops-and-prints follow-ups", the second, third and fifth entries filed by the run directly below: `stopOn` "returns `void`, so nothing tells the type system that the code after a refusal is unreachable"; the two zero-table refusals "say almost the same sentence in two modules ... Each message should name the other"; and the script's module note "still opens with a module note describing two tables and a hand-written pass, which stopped being true three runs ago". -->

- [DONE: 2026-08-23] [est: 10000] **`stop` returns `never`, the two zero-table refusals name each other, and the script's map is checked** — `scripts/check-privacy-baseline-provenance.ts`, `lib/provenance-tables.ts`, `lib/provenance-report.ts`, 1 case added in `__tests__/provenance-report.test.ts`.

  **A helper that ended the run and could not say so.** `stopOn(refusal: string | null): void` saved each caller an `if` and cost the type system the fact that the run was over: `comparison` kept its `fail` variant through every branch below, and every read of it was safe by luck rather than by design. `stop(refusal: string): never` gives the condition back to the caller — two words — and buys narrowing, so a later read of a field that only exists on a refused shape is a compile error instead of a branch nobody re-derives.

  **Two refusals for one claim, each now naming the other.** "A pass over zero tables is not a pass" is said in `provenanceRegistryRefusal` about the REGISTRY and in `provenanceOutput` about the OUTCOMES. They are not duplicates and the difference is worth the sentence: the first is earlier and names the cause, the second cannot be bypassed by a caller that forgot to ask. Two guards for one claim with nothing pointing at each other is how one of them gets deleted as redundant.

  **A module note that had stopped being true.** The script opened by describing two tables and a hand-written pass; there are three tables, the pass is a loop, and the pure half is now five modules and larger than the script. The note is a MAP — what each module decides, one line each — followed by what is left here, which is git and only git.

  **The map is derived-checked**, because an unchecked map is a comment that decays exactly as the old one did: a case lists `lib/provenance-*.ts` and `lib/*-provenance.ts` through the shared source-file helper and requires the leading comment to name every one, with a floor of five so a pattern that stopped matching cannot pass as a clean tree. It reads the comment ONLY, so a module named in the import list and missing from the map is what it is looking for.

  Mutation-checked: deleting the `lib/provenance-key-set.ts` line from the map turns the new case red by module name.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6551 tests (+1), build. No DB migration, no creds.

## Three endings written three times, and a pass over zero tables (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Output-decision follow-ups", the first, second and fourth entries filed by the run directly below: the script's three stops "are still three hand-written `if`s"; `provenanceOutput` "returns `ok: true` for an empty outcome list and a case documents that rather than refusing it"; and `ProvenanceLine.stream` "is a string union and the script maps it to `console.log`/`console.error` with a ternary". The list-of-checks half of the first entry was rejected on inspection and the script now says why. -->

- [DONE: 2026-08-23] [est: 11000] **`stopOn`, `printProvenanceOutput`, and a refusal that does not need its caller** — `scripts/check-privacy-baseline-provenance.ts`, `lib/provenance-report.ts`, 2 cases changed and 1 added in `__tests__/provenance-report.test.ts`.

  **Three copies of an ending.** Three checks stop this guard before it reports anything — the work tree, the registry, the diff's shape — and each was its own four-line block with its own `console.error` and its own `process.exit(1)`. Three copies of an ending is how one of them comes to exit 0, or to print on stdout, in a diff nobody reads twice. `stopOn(refusal)` is the ending, once, and all three now hand it a `string | null` — including the work-tree check, whose `kind: "fail"` becomes a refusal string at the call site so the three read as three of the same thing.

  **The suggestion asked for a LIST of checks and the script says why it is not one.** The diff cannot be asked for before a base revision resolves, so a list would be a list of thunks and the order — the only interesting thing about it — would stop being visible. What the three genuinely share is the ending, and that is what was extracted.

  **A pass over zero tables, refused where the claim lives.** `provenanceOutput` returned `ok: true` for an empty outcome list, because `every` over nothing is true; the thing that stopped that reaching it was `provenanceRegistryRefusal`, one level up, in a caller neither this module nor its suite controls. It now refuses locally and `halted`s, with the registry refusal still in front of it — a second opinion rather than a replacement, and a case says which is which.

  **The last half-sentence of the stream rule moved with it.** The script walked `output.lines` and mapped `"stdout"` to `console.log` itself, which is a caller that can disagree with the rule that assigned the name. `printProvenanceOutput(output, out = console)` takes the console as a parameter so a test can be the console, and the sweep now also refuses `output.lines` in the script.

  Mutation-checked both ways: short-circuiting the empty-run refusal turns its case red, and swapping the two streams in the printer turns the printing case red.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6550 tests (+1), build. No DB migration, no creds.

## The cheapest property with the most expensive cover (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Order-and-path follow-ups", the top entry: "The script's report loop is pinned by a case that spawns a scratch repository, which is the slowest cover in the suite for the cheapest property ... extracting the 'which reports, in what order, to which stream' decision into a pure function over outcomes would let one call assert it, and would leave the scratch repository for the git questions only it can answer." The second entry of the same group — the `unparseable` case tied to three formatters' wording — is closed in the same pass, because the extraction is what made a derived assertion available. -->

- [DONE: 2026-08-23] [est: 16000] **`provenanceOutput` — the guard's output decision, made testable without a repository** — new `lib/provenance-report.ts`, `scripts/check-privacy-baseline-provenance.ts`, 11 cases in `__tests__/provenance-report.test.ts`, one rewritten case in `__tests__/privacy-baseline-provenance-script.test.ts`.

  **Four rules that were statements in a script.** An `unparseable` table stops the run and nothing else prints; reports print in registry order, all of them, before any exit code; each report goes to the stream its own verdict earns; skip lines come last and there is either one for the run or one per table. None of them need git, and all of them were covered only by a case that builds a throwaway repository, commits to it twice and spawns the script. That cover is right for the questions only a repository can answer and it is the wrong altitude for "does the second report print after the first".

  **Text in, lines out.** The function returns `{lines: [{stream, text}], ok, halted}` and the script's print half is now four lines: iterate, pick the stream from the line, exit on `halted`, honour `ok`. A case asserts the script no longer picks a stream from a verdict itself, so rule 3 cannot come to exist in two places.

  **`halted` is a separate bit from `ok`, and the trailing summary is why.** A run stopped by an unparseable table has not read the other tables, so the "that run read history from …" line would be describing something that did not happen. The script exits before it; the flag is what says so out loud.

  **The wording coupling the extraction exposed, closed.** The scratch-repository case asserted that the word "well-formed" appeared nowhere on stdout — true today because three formatters happen to share the word, and a pass over a stdout full of pass lines the day one of them says "valid". It now asserts that a halted run prints nothing on stdout but the redirect announcement, which is derived from the run rather than from a formatter's vocabulary, and which the mutation check confirms is load-bearing in both suites.

  Mutation-checked: short-circuiting the halt rule turns the new suite red AND the scratch-repository case red — the second is the one that would have stayed green under the old wording assertion.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6549 tests (+11), build. No DB migration, no creds.

## The one input every evaluator trusted (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Closed-set follow-ups", the fourth entry: "Every provenance evaluator validates its inputs and none validates `changedFiles` ... A caller that passed absolute paths, or output from a `git diff` run in a subdirectory, would find nothing in the set and every value would read as unbacked; the guard would report six failures naming files the reader can see in their own diff." Closed together with the "Provenance-registry follow-ups" entry asking for a sentence in `ProvenanceBaseRevision` saying the list is deliberately unfiltered — the same field, and the sentence is half of why the check belongs to the loop rather than to a table. -->

- [DONE: 2026-08-23] [est: 12000] **`changedFilesRefusal` — the diff is shape-checked before any table reads it** — `lib/provenance-tables.ts`, `scripts/check-privacy-baseline-provenance.ts`, 6 cases in `__tests__/provenance-tables.test.ts`.

  **Six right-shaped failures with the wrong cause.** Every evaluator validates its own table down to the note's word count, and then compares a recorded path against a list that arrives from `git diff --name-only` and is trusted on sight. A diff run somewhere other than the repository root, or a caller that resolved the paths first, makes every lookup miss: each table reports its value as describing an untouched file, and the reader spends the morning looking for a bug in the recorded values while the answer is in the caller. A stopped run with a reason is worth more than six failures naming files the reader can see in their own diff.

  **A guard-level check, because the list is a guard-level fact.** It is read once and shared by all three tables, so a list that is not repo-relative is wrong for all of them in the same way — one refusal beside `provenanceRegistryRefusal`, not a rule each table has to remember. `ProvenanceBaseRevision.changedFiles` now says out loud that the list is deliberately unfiltered and shared, which is the other half of the same argument.

  **Two shapes refused, one deliberately not.** Absolute paths (posix `/…`, or a `C:` drive prefix) and paths re-anchored with `./` or `../` cannot come out of a repo-root `--name-only` and cannot match a recorded path. Backslashes are left alone and the module says why: git emits forward slashes on Windows too, so a backslash here is either a legal posix filename or one of git's quoted `"…\303\251…"` paths, and a guard that refused to run because somebody added a file with an accent in its name is a guard that gets deleted within the week. An empty list stays legal — it means the diff touched nothing, which each table already reads correctly.

  **The message is the diagnosis.** It counts offenders against the whole list (“1 of 2” and “2 of 2” are different problems), quotes three and says how many it did not, states what the guard would have concluded, and names the two causes that actually produce it. A case pins the truncation, because the failure is systematic and a refusal that printed a 400-path diff would bury its own first sentence.

  **Ordering is the whole value, so it is asserted rather than assumed** — a source case requires the script's call to come before the loop, since a refusal printed under the six failures it exists to prevent is a footnote.

  Mutation-checked both ways: dropping the `../` pattern turns the refusal case red, and moving the script's call after the loop turns the ordering case red.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6538 tests (+6), build. No DB migration, no creds.

## The rule written twice in one morning (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Shape-rule follow-ups", the top entry filed by the run directly below: "Two shape halves now open with the same eight lines against two different closed sets ... This is the third pair of near-identical rules in the provenance family, and it is the cheapest of the three to merge, and merging it is also how the fourth table gets the rule without anybody remembering to write it." The second entry of the same group — the keyless table having nothing to say about its absence — is closed in the same pass, because it is the sentence this merge makes possible. -->

- [DONE: 2026-08-23] [est: 13000] **One closed-set rule for the tables that have keys** — new `lib/provenance-key-set.ts`, `policySourcePathFor` in `lib/privacy-body-baselines.ts`, both provenance evaluators, 14 cases in `__tests__/provenance-key-set.test.ts`.

  **A rule written twice, three hours apart.** The run below added an `unknown_language` shape rule to `PRIVACY_BODY_BASELINES`; the one before it had added the same rule to `PRIVACY_TRANSLATION_SOURCES`. Same membership test, same failure code, same detail (the set, joined), and two sentences that differ only in which table and which set they name. Two copies is a shape; the third is the copy nobody reviews, and the fourth table's author has no reason to know this rule exists. The rule is now one module and each table brings the three phrases that genuinely differ.

  **The set is DATA, not a constant the rule reaches for**, and the two sets are the reason. Baselines cover every page the picker offers, English included — English is a page here, and the largest baseline in the table. `PRIVACY_TRANSLATION_SOURCES` covers the five translations only, because an `en` entry there is a table that has fingerprinted the English source as a translation of itself. A merge that picked one list would have broken one of the two tables silently.

  **`unknownProvenanceKeyDetail` returns the detail rather than a boolean**, which is what keeps the two `failures.push` blocks identical: neither caller reaches into the set to build the message's list, so neither can join a different list from the one membership was decided by.

  **The refusal messages had to name a path the checked helper refuses to answer for.** They spelled `PRIVACY.md.<code>` by hand — a third and fourth copy of the template `privacyPolicySourcePath` owns, and the copies that survive a rename. `policySourcePathFor` is that template with the refusal removed, documented as not-for-reaching-a-file-with, and `privacyPolicySourcePath` is now the validation plus a call to it. One knock-on worth having: an `en` entry in the translation table is now reported as describing `PRIVACY.md` rather than `PRIVACY.md.en`, which is the interesting thing about that entry.

  **The merge is asserted, not described.** One case renders both refusals, substitutes the three per-table phrases, and requires the strings to be equal — so a hand-written third wording goes red rather than passing as prose nobody diffed. A source sweep requires any evaluator that raises `unknown_language` to import the shared rule, with a positive control that both still raise it.

  **The keyless table now says why it is out of scope** rather than leaving a reader to reconstruct it from three modules: `SCRUB_PROMISE_BASELINE` is one record measured from one constant file, so there is no key to be unknown and the rule would be a check on nothing.

  Mutation-checked: restoring either table's hand-written message turns the sentence-shape case red, and a prefix- or case-insensitive membership test turns the exactness case red.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6532 tests (+14), build. No DB migration, no creds.

## The backstop that reported instead of throwing (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Closed-set follow-ups", the top entry filed by the run directly below: "The helper now throws and its callers are two provenance evaluators, so a bogus table key crashes the guard rather than reporting ... A shape-half rule would turn the backstop into a report, and would fire before any drift call rather than only when that key's value happened to move." -->

- [DONE: 2026-08-23] [est: 11000] **`unknown_language` is a shape failure in both provenance tables** — `lib/privacy-baseline-provenance.ts`, `lib/privacy-translation-provenance.ts`, four cases across their suites.

  **A right refusal at the wrong altitude.** The run below made `privacyPolicySourcePath` throw on a language nothing publishes, which closed a silent wrong answer. It arrived as an exception in a script whose every other failure is a `check-...: ERROR — ` line, and it arrived only on the run where that entry's value happened to move — the drift half is the only caller, and it asks for a path only after a number has changed. Both tables now ask the question in their SHAPE half, which runs every time and reports.

  **Asked first in each loop**, so the entry that cannot name a file is the first thing said about it, and the rest of the table's findings still print: a shape half that threw would take five other entries' findings with it, which is the failure this move exists to avoid. A case pins that it does not throw.

  **The two tables validate against different sets, and that is the point of doing it twice.** Baselines cover every page the picker offers, English included, because English is a page. `PRIVACY_TRANSLATION_SOURCES` covers the five translations only — an `en` entry there is a table that has recorded a checksum of `PRIVACY.md` against itself, which every other case in that suite would pass on.

  **Each message says what the entry would have named** (`PRIVACY.md.<code>`), why the silence would have looked like a pass (the name is compared against the files a diff touched, so a file that never existed reads as untouched), and the list of codes that are valid, so the reader does not go looking for it.

  Mutation-checked both ways: short-circuiting either rule turns two cases red in its own suite.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6518 tests (+4), build. No DB migration, no creds.

## The path for a language nothing publishes (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Third-table follow-ups", the fourth entry filed two runs below: "`privacyPolicySourcePath` now answers for six files and takes a bare `string` — so `privacyPolicySourcePath(\"de-DE\")` returns `PRIVACY.md.de-DE`, a path that has never existed, and every caller compares that against a diff and concludes the file was untouched." -->

- [DONE: 2026-08-23] [est: 9000] **`privacyPolicySourcePath` refuses a code no page is published for** — `lib/privacy-body-baselines.ts`, four cases in `__tests__/privacy-baseline-provenance.test.ts`.

  **A wrong answer that reads like a right one.** The helper templated any string into `PRIVACY.md.<code>`, and every caller hands the result to a set of the files a diff touched. A path that never existed is reported absent, the caller concludes "untouched", and a guard whose entire job is to notice that a file did not move gives its right answer for the wrong reason. That is worse than a stopped run, which is why this is a throw rather than a null.

  **The languages are a closed set**, so this is a question with a known list of valid inputs; the refusal names the code, says what path it would have invented, says why the silence would have looked like a pass, and lists the six known codes so the reader does not go looking for them.

  **A backstop, not the primary rule.** Both provenance tables are already held key-for-key against `PRIVACY_PAGE_LANGUAGES` by their own parity cases, so a bogus key fails there first; this closes the path where a caller reaches the helper with a code that came from somewhere else.

  Verified non-vacuous by construction: the four refused codes (`de-DE`, `EN`, `fr`, the empty string) each returned a plausible-looking path before this change, and each is a shape a real caller could produce — a locale tag, a case slip, an unadded language, and a missing key.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6514 tests (+1), build. No DB migration, no creds.

## The order that became load-bearing, and the path spelled twice (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Third-table follow-ups", the third and fourth entries filed by the run directly below: the registry's ordering "is now load-bearing over three entries and stated only in a comment", and `SCRUB_PROMISE_SOURCE_FILE` "is a string in the evaluator and `PRIVACY.md` is a string in four other modules". The top entry — merging the three evaluators into one drift pass — is left at the top of the file: it is a design decision about what shape all three are instances of, not a refactor, and it wants its own run. -->

- [DONE: 2026-08-23] [est: 14000] **Two properties the third table made load-bearing, pinned** — `__tests__/privacy-baseline-provenance-script.test.ts`, `__tests__/provenance-tables.test.ts`, `lib/privacy-translation-provenance.ts`, `lib/scrub-promise-provenance.ts`.

  **The registry's order is the reporting order, and it was an array literal with a comment.** Two hand-written passes could only be reordered by moving statements; a list can be reordered by a merge. Two cases now: the script prints one report per registered table in declaration order (derived from `PROVENANCE_TABLES`, so a fourth table is covered on the commit that adds it), and the registry's contents and order are pinned to an ordered list, so a reorder is a diff somebody wrote rather than one nobody noticed.

  **The first `unparseable` stops the run, and nothing said so from the outside.** The existing case checked that the failure reached stderr and did not read as a skip; it did not check that no OTHER table printed a pass beside it. That is the whole reason the loop asks every table for its classification before printing anything — a pass line next to a table whose drift half is off is a report from a guard running on one leg.

  **The English policy path was spelled two ways inside one two-element array.** `evaluateTranslationProvenanceDrift` built `[[sourceMoved, "PRIVACY.md"], [pageMoved, privacyPolicySourcePath(language)]]` — the same question answered by a literal on one line and by the helper on the next, which is exactly how one of two adjacent lines stops being true. Both go through the helper now, and `SCRUB_PROMISE_SOURCE_FILE` with them, so the path compared against `git diff --name-only` has one name.

  **The sweep that keeps it that way carries its own positive control.** No provenance evaluator may spell `"PRIVACY.md"` (comments stripped, since two of the three discuss the path in prose), and `lib/privacy-body-baselines.ts` — the helper that owns the literal — must still contain it, so a pattern that stopped matching anything cannot pass as a clean tree.

  Mutation-checked three ways: reversing the script's report loop turns the ordering case red, reordering the registry turns the contents case red, and restoring the `"PRIVACY.md"` literal turns the sweep red by module name.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6513 tests (+3), build. No DB migration, no creds.

## The third table, added as data (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Promise-fingerprint follow-ups", the top entry filed by the run directly below: "The promise clause now has a checksum and the guard that makes a checksum reviewable does not cover it ... It is now a registry entry plus a parser rather than a script change, which is what the loop shipped this morning was for." -->

- [DONE: 2026-08-23] [est: 22000] **`SCRUB_PROMISE_BASELINE` joins the provenance registry** — `lib/scrub-promise-provenance.ts`, `parseScrubPromiseBaseline` in `lib/sentry-scrub-promises.ts`, `__tests__/scrub-promise-provenance.test.ts`.

  **The claim the loop made this morning, tested.** `scripts/check-privacy-baseline-provenance.ts` is untouched by this change: the third table is an entry in `PROVENANCE_TABLES` with a parser and an evaluator of its own, and the script picked it up without a line moving. That was the argument for building the registry before a third table existed rather than after, and this is the run that could have disproved it.

  **What it closes.** The promise fingerprint shipped with a static case — edit the clause, the parity suite goes red. That is what it is for, and it is also the shape that decays: the documented repair is "record the new value here", the failure message says so in as many words, and a value whose repair is a paste is one commit from being a rubber stamp. The drift half now asks whether the note MOVED with the number, and whether `PRIVACY.md` — the file the number is measured from — was touched in the same diff.

  **One deliberate divergence from the rule it copies.** The translation drift lets a note stay put when only the date moved, because a translation can be re-confirmed against unchanged English. There is no equivalent here: the checksum moving means the sentence changed, so the note is required to move with it, and the re-confirmation of an unchanged clause is what the static case does on every run.

  **Not merged with `lib/privacy-translation-provenance.ts`, and the module says why rather than leaving it to be rediscovered.** The two do the same job over different shapes — a record per language with two checksums each, versus a single record with one — so merging them means inventing the shape both are instances of, which is a design decision and not a refactor. What is shared is `policyChecksum`, so the two values stay comparable, and the loop, so neither owns a copy of the git half. The merge is filed as a suggestion instead of smuggled in under this change.

  **The parser needed one shape change to be an honest parser**, and it is worth naming: the baseline was annotated with an inline object type, so the first `{` after the declaration was its SHAPE rather than its value. A named `ScrubPromiseBaseline` puts the literal where the same anchor the other two parsers use will find it, and a case round-trips the file on disk through the parser so a reformat that defeats it fails on the commit that reformats.

  **Related gap closed in the same pass:** the registry's own "reports over the tables this checkout actually ships" case addressed two tables by name, so a third could have arrived with no evidence line and nothing saying so. It is now held exhaustive against `PROVENANCE_TABLES` in both directions.

  Mutation-checked end to end: a working tree whose recorded checksum moved with the note and `PRIVACY.md` untouched exits 1 naming both causes, with the other two tables' passes still on stdout.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6510 tests (+18), build. No DB migration, no creds.

## The promise a narrowed word could survive (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Promise-parity follow-ups", the third entry: "The probe table matches promised phrases by regex and three of the four patterns are anchored on one word ... 'email address' narrowed to 'email domain' still matches /^email/i and would keep the same probe. The exhaustiveness case catches an ADDED field and cannot catch a narrowed one. A checksum of the promise sentence, next to the existing section checksum, is the cheap complement." The second entry of the same group was checked and closed as already shipped — `resolveSentryConfig` cannot enable Sentry in `development`, and three suites already pin it. -->

- [DONE: 2026-08-23] [est: 18000] **Fingerprint the sentence the probes were matched to** — `SCRUB_PROMISE_BASELINE` in `lib/sentry-scrub-promises.ts`, six cases in `__tests__/sentry-scrub-policy-parity.test.ts`.

  **The parity suite reads the promise as a LIST and the gap is in the words.** It asks which fields the policy names and whether each is claimed by a probe, which catches a field ADDED to the disclosure with nothing checking it. It cannot catch one NARROWED, because the probe patterns are anchored on a single word each — `/^email/i`, `/^ip\b/i`, `/^authorization/i` — so "email address" reworded to "email domain" still matches, still claims the same probe, and leaves every assertion green while the app and the disclosure have come apart.

  **The checksum cannot read the new wording either, and that is the whole claim.** It refuses to let the rewording pass unlooked-at: any edit to the clause turns the suite red and the documented repair is to re-read every probe against the new sentence, then record what was found. The same reviewability guarantee — and the same honest limit — as the translation checksums next door.

  **Fingerprinted over the CLAUSE, not the paragraph.** From the intro through the period that ends its sentence, normalised first through the same `normalisePolicyText` the translation checksums use, because the policy is hard-wrapped inside a blockquote and a guard that goes red for a rewrap is a guard somebody deletes within a month. A fingerprint over the rest of the policy would move on every unrelated edit, so a clause with no terminator is a throw rather than a slice to end of file.

  **One checksum implementation, not two.** `checksum` in `lib/privacy-translated-section.ts` became the exported `policyChecksum`: a second spelling of "sixteen hex of SHA-256" is how two recorded values stop being comparable, and every provenance value in this repository now comes through one function.

  **The case for the rule is an experiment rather than a sentence in a comment** — one case performs the narrowing, asserts the exhaustiveness check is still satisfied by it, and then asserts the fingerprint moved. If that ever stops being true, the checksum is buying nothing and the case says so instead of the comment.

  **The value carries provenance**, not just a number: `recordedOn` and a note of at least eight words, for the same reason the translation notes ask for eight — this value moves for more than one reason (a rewrap that changed nothing, a narrowed field, a field added) and the note is the only place the diff says which.

  Mutation-checked: rewording "stripped client-side" to "redacted client-side" in `PRIVACY.md` turns the checksum case red by name; a rewrap of the same clause leaves it green.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6492 tests (+6), build. No DB migration, no creds.

## The third copy, written before it existed (2026-08-23)

<!-- Promoted from .tasks/.suggestions.md, "Two-table-guard follow-ups", the top entry: "One guard now covers two tables with two parsers, two evaluators and two reports, and the only thing they share is the base-ref resolution ... the shape that actually generalises is a list of `{module, parse, evaluate, format}` records the script loops over, and writing it before the third table is how it stays a loop rather than a third copy." -->

- [DONE: 2026-08-23] [est: 28000] **The provenance guard loops over a registry instead of carrying one pass per table** — `lib/provenance-tables.ts` and `__tests__/provenance-tables.test.ts`, with `scripts/check-privacy-baseline-provenance.ts` reduced to the git half.

  **Two copies is a shape; the third is the copy nobody reviews.** The script asked one question of two tables — a recorded measurement moved, did the diff say why — and asked it twice, in two hand-written passes of the same six steps. The tables were already drifting in ways nobody chose: one three-way classification and one two-way, one `git diff --name-only` each for the same diff, two `previous === null` tests spelled differently. The suggestion's own timing argument is the reason this shipped at two rather than at three: written after the third table lands, the loop is a refactor of three copies; written before, it is the thing the third table is added to.

  **What each table now brings, and nothing else:** its module path, its current value, how to parse a previous revision, how to judge one, and the five sentences it says about itself. Adding a table is an entry in `PROVENANCE_TABLES`; the script does not change.

  **The generic parameter is erased at the boundary on purpose.** `ProvenanceTable` is a closure over a table's own type, so the registry holds two entries whose tables share no field name without widening either to `unknown` — and so the script cannot reach a parsed table at all, which is the point: it has nothing left to get wrong about one.

  **What deliberately stayed in the script is everything that needs git** — the base-ref resolution (`--base`, the env var, the PR target, `HEAD~1`, all through `merge-base`), the work-tree probe, the shallow-clone refusal. That is the CI-specific reasoning, it is shared by every table by construction, and it is the half that cannot be unit-tested without a repository. Everything in the new module is pure: text in, verdict and sentences out, which is why the loop's ordering rules are now pinned against a fabricated table rather than against a legal document.

  **The one place the two entries disagree is now declared rather than derived.** The baselines table treats "the module was there and yielded no table" as a hard failure; the translation table collapses it into the same skip an absent module gets, because that parser is days old and the suite round-trips it every run. Before, that difference lived in two functions that happened to be written differently; it is now two lines in one file with the argument beside them, and a case pins each side so closing the gap later is a decision somebody makes out loud.

  **Related bug found by the same pass and fixed in it:** a loop can skip itself in a way two hand-written passes could not — `every` over nothing is true, so a registry that lost its entries would exit 0 with every report line gone and nothing saying why. `provenanceRegistryRefusal` refuses an empty registry and a duplicated id (both entries would run, and every case addressing a table by id reaches only the first), which is the claim each table's own formatter already makes about ITS records — "a pass over zero baselines is not a pass" — made one level up, where the records now live.

  **Behaviour is byte-identical where it should be:** all ten existing script cases pass unchanged, including the two that match on the exact wording of the per-table skip lines. The one behavioural change is that `git diff --name-only` runs once rather than twice.

  Mutation-checked twice: classifying a base revision that does not exist turns one loop case red; flipping the translation table onto the baselines' three-way classification turns two red, one of them the case that exists to say the divergence is deliberate.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6486 tests (+18), build. No DB migration, no creds.

## The table whose repair is a paste (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Two-checksum follow-ups", the top entry: "The table now holds ten checksums and one script compares any provenance table against its previous revision — this is not the table it compares ... the suite requires a note of at least eight words and cannot see whether the note MOVED with the checksum. That is the whole difference between a record and a ritual." -->

- [DONE: 2026-08-22] [est: 35000] **`PRIVACY_TRANSLATION_SOURCES` gets the provenance guard its neighbour has** — `lib/privacy-translation-provenance.ts`, wired into `scripts/check-privacy-baseline-provenance.ts`.

  **The checksums' strength is also their weakness.** They turn every edit to a shipped legal page into a red suite, and the documented repair is "record the new value here" — the failure message says so in as many words. A table whose repair is a paste is one commit from being a rubber stamp. `PRIVACY_BODY_BASELINES` has the same problem and the same answer next door; this is that answer applied to the second table, not a new idea.

  **What it can and cannot do, stated rather than implied.** It cannot tell a re-translation from a paste — nothing can, both change the file and both change the checksum. It forces the diff to SAY which, to a reviewer who may not read the language. A reviewability guarantee, which is the only kind available for prose.

  **One deliberate divergence from the rule it copies.** The baselines drift requires the note to NAME the number it replaced, because "1171 → 980" is a fact a reader can weigh. Sixteen hex characters are not: a note quoting the old checksum proves only that somebody could copy. What is required here is that the note MOVED, which is what a paste does not do — and the minimum length is eight words rather than five, because a checksum moves for two reasons (the English changed, or this page did) and the note is the only place the diff says which.

  **Which file has to be in the diff depends on which checksum moved**, and that is the useful half of the message: `sourceChecksum` is measured from `PRIVACY.md`, `translatedChecksum` from that language's own page. A value that moved with its file untouched cannot have been measured from anything.

  **One guard, two tables, two independent verdicts.** Sharing the script rather than adding a second one, because the base-ref resolution — `--base`, the env var, the PR target, `HEAD~1`, all through `merge-base` — is where the CI-specific reasoning lives and is the part a second script would copy. Both reports print before either exit code is honoured, since the commit that amends the English disclosure moves the word count and all five checksums; each report goes to the stream its own verdict earns.

  **Two existing script cases matched on output this change reworded, and both were fixed by making the output more specific rather than the assertions looser** — the translation skip line no longer says "drift half skipped" (two skips with different modules behind them is how somebody concludes the whole guard sat out), and the redirected-history line says "pass" only when it was one.

  Mutation-checked end to end: a base revision whose checksums differ while the note and date match the shipped ones exits 1, naming `de` and "same note and date", with the other table's pass still on stdout.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6468 tests (+17), build.

## The promise and the code that keeps it (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Two-checksum follow-ups" — the last of the five, taken ahead of the four above it because it is the only one where the failure is the app doing something different from what it told a user: "A rule tying the scrub list in lib/sentry-config.ts to the field names in PRIVACY.md would close the one gap where the app could stop doing what the policy promises." (The scrub list is in `lib/sentry.ts`, not `lib/sentry-config.ts` — the suggestion named the wrong file.) The first entry, generalising the provenance script over the new checksum table, is left at the top of the file for the next run. -->

- [DONE: 2026-08-22] [est: 20000] **`scrubPII` against the sentence that promises it** — `lib/sentry-scrub-promises.ts` and `__tests__/sentry-scrub-policy-parity.test.ts`.

  **Two well-covered files and nothing between them.** `PRIVACY.md` promises "personally identifying fields (email address, IP address, cookies, and `Authorization` headers) are stripped client-side before transmission"; `sentry-scrubber.test.ts` checks each field `scrubPII` removes. Neither asks whether those are the same fields, and each file stays internally consistent while the pair comes apart.

  **One-directional, and that is the whole framing.** Stripping more than the policy mentions is the app doing better than it said. Promising more than the scrubber removes is the app sending a third party data it told a user it would not — and it arrives by ordinary editing, not by malice.

  **The promise is PARSED out of the policy, not restated.** A hand-kept list of the four fields would be a third copy of the sentence and would drift exactly the way this rule exists to prevent. What is hand-kept is the probe per field — a value that must not survive, and where in a Sentry event it sits — which is the part no parser can infer, and it is held exhaustive against the sentence in both directions: a promised field no probe claims, and a probe matching nothing the policy still promises.

  **The other half of the promise gets a case too.** The disclosure says the payload DOES carry the Supabase user identifier so reports correlate per account; a scrubber that stripped everything would satisfy every case above and break what the policy says the product does.

  Mutation-checked twice: deleting `delete headers.Authorization` from `lib/sentry.ts` fails the Bearer probe with "the app sending a third party something it told a user it would not"; adding "device identifiers" to the policy's list fails the exhaustiveness case by name, because nothing would ever have checked it.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6451 tests (+9), build.

## The half the checksum was not guarding (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Privacy-translation follow-ups", filed an hour earlier by the run that added the source-side checksum: "The checksum guards the English half and nothing records what the TRANSLATIONS say ... the symmetric unit is a checksum of each translation too, and is the cheaper half, since it needs no new extraction." Its neighbour ("all five source checksums are identical, which is a state and not a design") was folded in — one note and one case, in the file it is about. -->

- [DONE: 2026-08-22] [est: 15000] **`translatedChecksum` — the shipped page, fingerprinted too** — the other direction a legal disclosure drifts.

  **`sourceChecksum` catches the English moving out from under five translations; nothing caught one of the five moving on its own.** A merge that drops the retention sentence from `PRIVACY.md.de`, a "fix" that turns 90 into 30 in one language: the size gate sees a big enough deletion as a word count and sees nothing at all in a changed number. That is the edit that matters most on a page nobody reviewing the diff necessarily reads.

  **The whole file, not its blockquote.** The sentence above the blockquote tells the reader the full policy exists and is in English, which is the other half of what art. 12 asks for; an edit to it is an edit to the disclosure.

  **One normaliser, shared.** `normalisePolicyText` is now what both checksums hash — quote markers off, whitespace collapsed — so a prettier pass over five legal pages cannot demand five explanations, and everything a reader could act on still moves the value.

  **Two premise cases where there was bookkeeping.** The five page checksums must all DIFFER (five equal values mean five identical files, which is one language pasted over the rest, and the similarity gate only refuses that in the English direction). The five source checksums are all EQUAL, asserted so the equality is a recorded fact somebody changes deliberately rather than a coincidence inviting a collapse of the column into one constant — the first amendment splits them, because whoever refreshes the German records only German's.

  Mutation-checked: changing 90 to 30 in `PRIVACY.md.de` alone fails exactly one case, naming that file; the English-side rule stays green, which is correct.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6442 tests (+7), build.

## The disclosure that could go wrong in five languages at once (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Completeness-rule follow-ups": "`PRIVACY.md` has six translations as separate files and no coverage rule touches them at all ... the same defect class the whole i18n programme just closed, one directory over, and the honest unit there is a per-file checksum of the English source recorded next to each translation." Taken ahead of the four entries above it in the file because it is the only one on the board where the failure reaches a user rather than a suite. -->

- [DONE: 2026-08-22] [est: 30000] **A checksum of the English disclosure, recorded next to each translation** — `lib/privacy-translated-section.ts` and `__tests__/privacy-translated-section.test.ts`.

  **The five translated pages are not translations of `PRIVACY.md`, which is why nothing had noticed.** Each carries ONE section — the "Crash reporting and diagnostics" blockquote naming Sentry — published under GDPR art. 12 for a reader who cannot read the full policy. Every rule the repo had about them is a property of the TRANSLATED file: a word count floored against its own baseline, a similarity gate refusing the English text with edits. Both stay green while the English section changes underneath them.

  **The failure is a wrong disclosure, not a stale one.** Move the retention window from 90 days to 30, add a field to the stripped list, move the opt-out to another screen — every suite passes and `/privacy/de/` keeps promising the old terms to the reader who has no other copy. That is the one place in this repository where a silent gap reaches a person rather than a number.

  **A checksum of the source, not a comparison between the files.** Nothing here can read German and decide whether it matches. What can be mechanised is "the text these five were written against has moved and nobody has looked at them since", which is a question a reviewer can answer. Per-language entries, so a partial catch-up shows as four remaining names instead of being cleared by one edit.

  **Normalised before hashing, and that is what makes the rule keepable.** The section is hard-wrapped prose: a rewrap changes every line and no meaning, and a guard red for that gets switched off long before the amendment it was written for arrives. Quote markers stripped, whitespace collapsed — so a rewrap is invisible and a number inside an otherwise identical sentence is not.

  **The premise case names the section without stating its terms.** The first draft asserted "90 days" was present, which would have gone red on exactly the amendment the file exists for — a premise that fails on the event it is a premise for is the rule again, wearing the wrong hat. It matches the lead-in, "Sentry" and "sub-processor" instead.

  **Extraction throws rather than returning empty.** A missing or duplicated marker means the English policy was restructured; an empty string would hash to a stable value and report all five current forever.

  Mutation-checked: changing 90 days to 30 in `PRIVACY.md` fails exactly the five per-language cases, each naming its own file, the recorded date and the repair.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6435 tests (+20), build.

## The edge that was guarded by a comment (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Two-list follow-ups" — filed by the run that created the edge an hour earlier: "The reverse import is the one that would cost something and only a comment says so ... bundle-size would catch it late, as a number rather than as a name, and the repo already runs check-analytics-imports for exactly this shape of rule." -->

- [DONE: 2026-08-22] [est: 18000] **The parser stays out of the bundle** — the import direction `lib/i18n-coverage.ts` → `lib/translation-status.ts` relies on, stated as a rule instead of as a comment.

  **The failure it guards is quiet, which is the whole argument for a named case.** The reverse import compiles, every existing suite stays green, and what changes is the size of a file nobody was looking at: `app/settings.tsx` calls `isPartiallyTranslated` per picker row, so `lib/translation-status.ts` ships to the browser, and an import from it to `lib/i18n-coverage.ts` would drag `lib/i18n-source.ts` and `lib/js-tokens.ts` in behind it. `bundle-size` would notice eventually, as a number, on whichever unrelated PR pushed it over.

  **"Imports nothing at all" rather than a list of banned specifiers.** A module with no imports cannot reach the parser by any path, so the rule needs no import graph and cannot be defeated by an intermediate module. It is stricter than the danger on purpose: the question a future failure should provoke is not which specifier is banned but what the settings screen now pays to render a badge, and a deny-list would answer neither.

  **The other half is an allow-list of the modules that DO import the parser, with the reason each never loads.** Two today — `lib/i18n-coverage.ts` and `lib/check-orphan-i18n-keys.ts` — swept out of the whole source tree rather than out of `app/` and `components/`, because the way this breaks is a `lib/` module the app already imports growing the edge. A second case refuses a stale entry: a permission naming a module that no longer imports the parser permits nothing and looks exactly like one that does.

  **The regex gets its own case, because a sweep's green run is "no file matched" either way.** Static `from`, dynamic `import()` and `require()` are all read; a string literal that merely looks like a specifier is not.

  Mutation-checked: adding one import to `lib/translation-status.ts` fails both cases, naming the file and the cost.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6415 tests (+4), build.

## Two lists over the same six names (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Completeness-rule follow-ups" — the top entry, filed by the run that added the completeness ceiling: "`COMPLETE_LANGUAGES` and `PARTIALLY_TRANSLATED_LANGUAGES` are two lists over the same six names, and neither knows about the other ... One case comparing the two against the measurement would say which of the three states each locale is in and refuse the impossible one." -->

- [DONE: 2026-08-22] [est: 26000] **`localeListingState` — the two locale lists, made aware of each other** — one rule over the pair, refusing the state no locale can be in.

  **The contradiction was already caught, and by two suites that each blamed the wrong thing.** A name in both lists fails the completeness ceiling (which demands it inherit nothing) and the picker's parity case (which demands it measure under 90%), so it was never going to ship. What neither failure said is that the two LISTS disagree: both read "this list has drifted from the measurement", in two files, and neither knows the other list exists. The new case is structural — it compares the lists to each other and holds with the parser deleted — and it names both modules in the message.

  **Three legal states, and `unlisted` is the one worth naming.** In neither list reads as an oversight and usually is; it is also exactly where a seventh language sits after the work that carries it over `TRANSLATION_COMPLETE_PERCENT` and before the work that finishes it. A rule that could not express it would have to choose between refusing a legal position and saying nothing about an illegal one. Each state is now held to the band of the measurement it implies: `complete` ⇒ zero inherited, `partial` ⇒ under the threshold, `unlisted` ⇒ over the threshold with keys still to go — and the failure message names the list that needs the edit rather than the number that moved.

  **The classifier takes its lists as an argument, for the same reason `translationCoverage` takes its source.** All six locales are `complete` today, so three of the four branches are unreachable from the shipped pair and would have been under description rather than under test. A fabricated pair exercises `partial`, `unlisted` and `contradictory`; a separate case pins that the DEFAULT is still the two real lists, which is how a testable seam quietly stops describing anything.

  **Import direction recorded at the import.** `lib/translation-status.ts` stays a leaf — the app bundles it, `app/settings.tsx` calls it per picker row — so `lib/i18n-coverage.ts` reaches down for the picker's list and never the reverse, which would pull the parser into the bundle.

  Mutation-checked in both directions: putting `ru` in `PARTIALLY_TRANSLATED_LANGUAGES` fails the structural case naming both files; dropping `be` from `COMPLETE_LANGUAGES` fails its band case with "add it to COMPLETE_LANGUAGES, which is what stops it drifting back".

  `npm run verify` green: typecheck, `lint:all` 16/16, 6411 tests (+11), build.

## The rule fifteen runs of translation work did not leave behind (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Sweep-finished follow-ups" — the top entry, filed an hour earlier by the run that finished the sweep: "Every i18n rule in the tree is now green by arithmetic, and none of them would notice the next English key ... the trade is worth re-opening on its own terms rather than inheriting the old answer." Taken immediately because the cheap moment is while every locale is at zero. -->

- [DONE: 2026-08-22] [est: 30000] **`COMPLETE_LANGUAGES` — a finished locale stays finished** — the ceiling `TRANSLATION_FLOORS` argues against, applied only where the argument stopped holding.

  **Every measurement here reports a STATE, and nothing reported the invariant.** `be` declares 498 of 498; fifteen runs moved that from ~40%, and not one left behind anything that would notice it moving back. The floors come closest and they floor the DECLARED count, which is a different claim: a locale can keep every declaration it has and still fall behind, because the base map grows and `...en` serves the new key in English. That is exactly how the gap reached 300 keys the first time.

  **The old argument was right and was about a different situation.** `TRANSLATION_FLOORS` says a ceiling on `inherited` "would go red on every feature PR and be deleted within a week" — written when four locales were two hundred keys behind, where the ceiling would have demanded that whoever added one English string first close somebody else's backlog. At zero behind it costs six values on the PR that adds the key, written by whoever just decided what the English one means. Same rule, and the difference is the whole argument.

  **A LIST rather than "every language in the picker", and that is what keeps `lib/translation-status.ts` alive.** The previous commit left `PARTIALLY_TRANSLATED_LANGUAGES` empty and had to argue that the badge still has a future; a completeness rule phrased over all languages would have made that argument false, because a seventh locale could not then arrive at 0% at all. Phrased over a list, it can: added to the picker, badged while somebody works on it, promoted here on the run that finishes it. A rule over all languages makes adding a language a 498-value PR, which is how a language does not get added.

  **Derived-checked in both directions.** A listed locale that slips fails by name; an unlisted locale measured at 100% fails too, because a finished locale that nothing protects is precisely the state this rule exists to end — and is what the last fifteen runs would otherwise have left behind, six times over.

  **The floors are not made redundant, and both files now say why.** This ceiling is RELATIVE — each locale against the base map — so a merge that deleted two hundred keys from every map including `en` satisfies it exactly. The absolute numbers are what refuse that, and a case pins the premise that makes the pairing worth keeping.

  Mutation-checked: one new English key in the base map fails five locales by name and the derived-list case with them.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6400 tests (+10), build.

## The last 62 keys: a per-locale sweep, not a seventh clever unit (decomposed 2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Home-screen follow-ups" / "Collection-detail-screen follow-ups" / "Screen-family follow-ups" — three consecutive runs have now filed the same endgame: "62 keys remain English in all four ... past the point where a family entry pays for itself. The endgame is the per-locale sweep the last two runs predicted." Decomposed rather than taken whole because finishing a locale is what CROSSES it over `TRANSLATION_COMPLETE_PERCENT`, and that crossing is a product decision the suggestions have filed unanswered three times. -->

Context, so the next run does not re-derive it:

- **The measurement, taken today.** `be` 87.6% (62 inherited), `pl`/`de`/`es` 86.5% (67 each); `ru` and `en` 100%. 62 base keys are English in ALL four; five more (`myCollections`, `createCollection`, and the three the home screen left) are English in some. 67 keys × 4 locales is the whole remainder.
- **Why not another screen family.** The 62 are spread over ~26 files and the largest holder is `app/friends.tsx` at 9. A family entry costs a table row, a `because` paragraph and a size number to catch nine keys, and the four biggest holders after it hold 8, 8, 7, 7. The unit stopped paying.
- **The crossing is the decision, and it resolves itself if the sweep FINISHES.** Every run since 2026-08-22 morning has filed "when `be` crosses 90, `PARTIALLY_TRANSLATED_LANGUAGES` loses a name while ~60 keys behind it are still English — either 90 is the promise or the threshold wants a second tier". At 100% in all six that argument is empty: nothing is badged because nothing is partial. Finishing is the answer to the question, which is why the sweep is one task rather than a decision followed by a task.
- **What the sweep must not do.** `slavicPlural` for `be`/`pl` and `plural` for `de`/`es` wherever a word follows the count (`__tests__/i18n-agreement.test.ts` enforces the shape); `de` addresses the reader as *du*; each locale's own quotation marks. All three are guarded, so the sweep's mistakes are red rather than shipped.

- [DONE: 2026-08-22] [est: 55000] **1/2 — finish `be` and `pl`** — 62 keys in each, plus whatever either declares partially, taking both to 100%. Both cross the threshold, so both leave `PARTIALLY_TRANSLATED_LANGUAGES` in the same commit that earns it, and the module's note stops predicting the crossing and records it.

  **`pl` was 67 keys behind, not 62, and the five extra are the finding.** `addFriend`, `removeFriend`, `follow`, `unfollow` and `saveCollection` — the social verbs on the profile screen and the button that submits the collection form. `be` translated all five days ago; `pl` never did, and no report named them, because every count this repo publishes is either per-LOCALE (a percentage that averages five keys away) or per-KEY across the four partial locales (which the "English in all four" filter drops the moment one locale translates it). A key three locales have and the fourth does not is the shape that falls between both.

  **The threshold argument three runs filed was answered by not needing an answer.** Every run since the morning of 2026-08-22 wrote down the same dilemma: when `be` crosses 90%, `PARTIALLY_TRANSLATED_LANGUAGES` loses a name while ~60 keys behind it are still English, so either 90 is the promise or the badge wants a second tier. Finishing the locale dissolves it — at 100% there is nothing to qualify and nothing to tier — and that is worth stating as a habit rather than as luck: a threshold argument is only worth having about a locale that stops short of the threshold.

  **The blind spot the sweep itself walked through, closed in the same commit.** Coverage counts DECLARATIONS, so declaring a key and pasting the English text satisfies every percentage in this repository while shipping exactly the string the count was invented to find. `be` at 100% and `be` at 100% with fourteen English strings in it are indistinguishable from every number here. `i18n-complete-families.test.ts` has made that comparison since it was written and only over the keys a LISTED family owns — which covered the work while it was being done family by family, and stopped covering it the moment 129 values were written by hand outside every family. `__tests__/i18n-english-verbatim.test.ts` makes it over the whole map.

  **Its first run is ten exemptions and zero findings, and both halves of that are the point.** Every verbatim value in the tree turned out legitimate: a brand (`templateHotWheelsName`), four loanwords a language genuinely took (`Comics`, `Chat`, `Chats`, `Tags`), `Premium`, an example handle, and two strings with no words in them. So the guard catches nothing today and is written for the next sweep — which is the honest description of a rule written after the work rather than before it.

  **The wordless values are out by a property, not by name.** ``≈ ${amount} ${currency}`` and ``${current} / ${total}`` are identical in six locales because there is one way to write them, and "this locale kept the English text" is not a claim either can be measured against. Gating on letters OUTSIDE the interpolations keeps them out without a line in the exemption list, so the next wordless value added is out for free. Only the gate strips interpolations; the comparison keeps them, because `${slavicPlural(params?.count, "предмет", …)}` is where a paste hides — translated words inside what looks like code.

  **`be` and `pl` floors re-measured from 279/271 to 463.** A floor of 279 under a 498-key map catches nothing a bad merge could do; it was measured when the locale declared 304.

  Mutation-checked: pasting `removeAccess: "Remove"` into `be` fails naming the locale and the key.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6388 tests (+10), build.

- [DONE: 2026-08-22] [est: 55000] **2/2 — finish `de` and `es`, and say what an empty partial list means** — the same 62 keys in two two-form languages, `plural` rather than `slavicPlural`, and `du` throughout the German. The list empties; `TRANSLATION_COMPLETE_PERCENT` and `isPartiallyTranslated` stay, because the next language added to the picker arrives at 0% and the badge is what the picker owes that reader — the parity case is what makes the arrival red rather than silent.

  **All six locales declare all 498 translatable keys.** `ru` 100%, `en` 100%, and `be`/`pl`/`de`/`es` at 498 each — the same 498, since the three `UNTRANSLATABLE_KEYS` are the only difference between the four partial rows and the base map. Seven months of "this screen is half-translated" ends as a diff to one file.

  **German needed a shape the guard could not have asked for.** `selectedCount` is "1 selected" in English and `ausgewählt` does not inflect, so the honest German is either `1 ausgewählt` with an INVARIANT exemption saying the participle agrees with nothing — the third such entry — or the count last. `Ausgewählt: 5` is also what a selection bar reads like in German. Recorded at the call site, because the next reader's question is why this one locale is shaped differently.

  **An empty verdict list is a module a later reader deletes, and two cases now stand where the four names did.** The parity case compares `[]` to `[]`, which is true either because nothing measures below 90% or because the measurement stopped working — and until today the second reading was ruled out by there being four names in it. So the DERIVATION gets a case of its own, on a source the suite writes: a locale declaring one key of four must come back named at 25%. And the module's claim that the badge survives for a seventh language is worth exactly the caller it has, so a structural case pins that `app/settings.tsx` still asks for the verdict and still renders the string — every other case in that file passes with the picker's branch deleted. Mutation-checked: replacing `isPartiallyTranslated(option.code)` with `false` fails naming the screen.

  **Floors re-measured for all four: 267–279 → 463.** Numbers taken when the locales declared ~300 keys, sitting under maps of 498, catching nothing a bad merge could do.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6390 tests (+12 across both halves), build.

## The base map said "1 items" (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Plural-agreement follow-ups" — "The same class of defect is wide open in `de` and `es`, and nothing looks for it ... A two-form counterpart to `slavicPlural` plus the same syntactic scan would close it". Filed by the run that fixed the Slavic half, an hour earlier, which named two examples and did not go looking for the rest. -->

- [DONE: 2026-08-22] [est: 28000] **Agree the two-form languages too, and put both rules in one module** — 26 values across `en`, `de` and `es`, and the finding is which locale was worst.

  **English. `itemsCount` rendered "1 items", `photosCount` "1 photos", `sharedWithPeople` "Shared with 1 people", `itemsMoved` "Moved 1 items", `galleryOpenPhotos` "Open photo gallery, 1 photos".** Seven keys in the base map — not a translation lagging behind English, but English, in the language every reviewer of this repo reads. The previous run named `de.exportPdfItemCount` and `es.accessOpenFor` as the follow-up and did not scan `en`, because the mental model was "the translations need fixing". The scan was two lines and it answered a question nobody had asked.

  **`de` had six and `es` eight, and two of them need a VERB to agree, not a noun.** `Zugriff haben 1 Personen` and `1 personas tienen acceso` — so the German and Spanish values pass "Person hat"/"Personen haben" and "persona tiene"/"personas tienen" through the same helper. That is why neither function knows any vocabulary: a rule that took a noun and pluralised it could not have expressed either.

  **`lib/plural-slavic.ts` became `lib/plural.ts`, one hour old, and the rename is the point.** Two modules named by language family is how one half gets applied and the other forgotten — which is a description of what had just happened, twice. The question a reader arrives with is "how do plurals work in this map", and it now has one answer with both rules in it.

  **Zero is the interesting number in the two-form rule, and it is why `plural` is written `=== 1` rather than `< 2`.** "0 items", "0 Objekte", "0 objetos" all take the plural, and 0 is what an empty collection shows — a rule phrased as "fewer than two takes the singular" is green at 1 and wrong on the emptiest screen in the app. Pinned in three languages.

  **The guard now covers all six locales and carries five exemptions, each a word that genuinely does not inflect.** *фото* and *фота*, indeclinable loanwords in Russian and Belarusian; `selected` and `remaining` twice, English participles that agree with nothing. Which of the two helpers a locale uses is deliberately NOT checked — that is a question about vocabulary rather than shape, and a syntactic scan claiming to answer it would be claiming more than it can see.

  Sixteen count-bearing keys, six locales: before today four values agreed anything at all; now every one that needs to.

  `npm run verify` green: typecheck, `lint:all`, 6378 tests (+4), build.

## "1 предметов" — the defect no coverage number could report (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Collection-detail-screen follow-ups" — "`be` and `pl` use the invariant genitive plural everywhere and only `ru` has real plural functions ... It is a bug rather than a gap: the keys are DECLARED, so no coverage number reports them". Filed by the previous run, which followed the convention rather than fixing it and said so. -->

- [DONE: 2026-08-22] [est: 32000] **Agree the Slavic nouns with their counts, and guard the shape** — one pure module, 19 values across `ru`, `be` and `pl`, and a defect on the most ordinary screen in the app.

  **A collection with one item read "1 предметов", "1 прадметаў", "1 przedmiotów".** The plural under the number one, in three languages, on the first thing a collector sees. `accessOpenFor` read "для 1 человек", `sharedWithPeople` read "Udostępniono 1 osobom", and `pl.photosCount` read "1 zdjęć".

  **Six runs of coverage work could not have found it, and that is the point worth keeping.** Every one of those keys is DECLARED in all six locales, so the coverage percentages count them as done and every family suite passes — they are translated, and translated wrongly. A count of decisions made cannot see a decision made badly. What found it was going looking: sixteen count-bearing keys, printed side by side across four locales, and reading them.

  **The rule was in the tree already, applied to two keys out of sixteen.** `ru.selectedCount` and `ru.deleteItemsTitle` spelled the one/few/many branch out inline, twice, which is how it came to be applied where somebody happened to think of it. `lib/plural-slavic.ts` states it once (renamed to `lib/plural.ts` an hour later, when the two-form languages needed the same treatment); the words stay at the call site, which is what lets `pl` use the same branch for `osoba`/`osoby`/`osób` and different words from `ru`.

  **The scan found a seventeenth key nobody had listed.** `ru.syncingPillA11y` read "1 изменений синхронизируется с облаком" — noun and verb both wrong under one. It is a screen-reader label, so it had no visual reviewer at all.

  **`ru`'s inline rule had a real bug in it, and moving it out fixed that too.** The old `deleteItemsTitle` excluded the teens with `mod100 < 10 || mod100 >= 20`, which also excludes 10 and 15–19 from the "few" branch — harmless, because those end in 0 and 5–9 and never reach it, but it is not the rule. `slavicPlural` uses `mod100 < 12 || mod100 > 14` and `__tests__/plural.test.ts` (then `plural-slavic.test.ts`) pins both sides of the boundary: 11–14 and 111–114 take "many", 21 and 121 come back out.

  **The guard is syntactic and one exemption wide.** In `ru`, `be` and `pl`, a value that interpolates the count and then puts a WORD next to it must call `slavicPlural`. Colon forms, parenthesised counts and values ending on the number agree with nothing and pass untouched. The exemption is *фото* / *фота*, indeclinable in both languages, with a reason per entry and a case that fails if an exemption stops applying. Mutation-checked by restoring `be.itemsCount` to its old flat form, which fails naming the file, the locale and the value.

  **The block-bodied reader shipped this morning lost its last real occupant, so it got a direct case rather than a quiet death.** Both `ru` block bodies became one-line arrows here. `readableText` is now extracted from `valueOf` and exercised on a branching body as a literal — a capability that is correct and unused is the one a future "nothing uses this" removes truthfully and wrongly.

  `npm run verify` green: typecheck, `lint:all`, 6374 tests (+16), build.

## The home screen, and a guard that fired on the safe direction (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Collection-detail-screen follow-ups" — "`app/index.tsx` is still worth one more entry — it is the home screen — and after it the entries are worth less than the sweep". Filed by the previous run in the same breath as the measurement that named it. -->

- [DONE: 2026-08-22] [est: 30000] **Translate the home screen, and correct yesterday's margin guard to the one direction churn can move** — 19 keys across be, pl, de and es, and a case fixed by the failure it produced an hour after being written.

  **The most MIXED screen listed so far, which is the case the screen family exists for.** `createCollection` was declared in five locales and missing only in `de`; `myCollections` was missing in three; `friends`, `following`, `statsTitle` and `recentlyAdded` in all four. So a German reader met "Meine Sammlungen" above two English tiles and an English "Statistics" heading — a screen half-translated key by key rather than section by section, which no per-key gap count surfaces and which the family closes in one demand. 25 keys after 12 prefix-owned and `appName` are subtracted; 19 of them needed work.

  **The margin case shipped this morning fired on `be` at 87.6%, and the guard was wrong, not the number.** It compared `Math.abs(coverage − 90) < 4`, which treats both sides of the threshold as risky. They are not: denominator drift is one-directional. Adding English keys lowers every row that has not translated them and raises nobody, so a locale BELOW the threshold cannot cross it by churn — `be` can only reach 90 by somebody translating twelve more keys, and the parity case goes red on the run that does it. The reclassification churn CAN cause is the other one, a complete locale falling under and getting badged by a PR nobody thought was about translations, and `ru`/`en` at 100% are ten points clear of that.

  Widening the margin was the available wrong repair, and it is the one a symmetric guard teaches: the case now checks only locales at or above the threshold, and says in its own comment why the version it replaces was wrong.

  **`be` is now twelve keys from 90%, so the badge decision lands on the next screen family.** When `be` crosses, `PARTIALLY_TRANSLATED_LANGUAGES` loses a name and the picker stops qualifying Belarusian while roughly 60 keys behind it are still English. That arrives as a red parity case rather than as a silent reclassification, which is the right place to have the argument — recorded in `lib/translation-status.ts` so whoever meets the red suite meets the reasoning with it.

  Mutation-checked: raising the margin to 12 names `ru` and `en` and no partial locale, which is the asymmetry working.

  Coverage: be 84.9% → 87.6%, pl 83.5% → 86.5%, de 82.7% → 86.5%, es 82.9% → 86.5%. Keys English in all four partial locales: 75 → 62.

  `npm run verify` green: typecheck, `lint:all`, 6358 tests (+5), build.

## The largest screen left, and the guard that went red because the work is succeeding (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Item-detail-screen follow-ups" — "`app/collection/[id].tsx` is the largest remaining holder and it is now 26, not 31 ... It is the obvious next screen entry". Filed an hour earlier by the run that translated the item screen, with the instruction to re-measure first. -->

- [DONE: 2026-08-22] [est: 34000] **Translate the collection-detail screen, and re-decide what the badge threshold's guard measures** — 27 keys into be, pl, de and es, one reader rewrite that had to come first, and one guard that failed for the right reason.

  **Re-measuring changed the number again, upward this time: 27, not the 26 the suggestion predicted.** `deleteCollection` is declared in `be` but not in `pl`, `de` or `es`, so it is a gap key for three locales and was invisible to a count that asks "English in all four". The suggestion's own advice — re-run the measurement, do not trust the entry — was right in the direction it did not expect.

  **The verbatim check could not read `ru`'s plural keys, and the family could not be added until it could.** `valueOf` knew two shapes, a string literal and a one-expression arrow. `ru` declares `selectedCount` and `deleteItemsTitle` as arrows with a BLOCK body, because Russian needs three plural forms and a ternary cannot spell them — so the reader returned `null`, and `null` is what the verbatim case reads as "nothing to compare". The two keys most likely to be copied wholesale, where the English sits inside what looks like code, were the two it silently skipped. Rewritten over `LocaleBlock.values`, the brace-aware value span the parser already isolates, which handles all three shapes and deletes two regexes.

  Mutation-checked in both directions, because one alone proves half of it: pasting the English arrow into `ru.deleteItemsTitle` fails the verbatim case by name, and restoring the old regex reader fails the vacuity case with "could not read 'ru'.deleteItemsTitle". The second is the one that matters — it is the failure the old reader could not produce about itself.

  **be/pl/de/es keep the invariant plural form, which is the standing convention and also a defect.** `itemsCount` reads `${count} прадметаў` in `be` and `${count} przedmiotów` in `pl` — genitive plural regardless of the number, so "1 прадметаў". Only `ru` has real plural functions. Following the convention rather than fixing it here was deliberate: four locales' worth of plural rules is its own task, and doing it inside a translation run would have buried it. Filed.

  **`translation-status.test.ts` went red, and the honest fix was to change what it measures.** It demanded a 20-point gap between the highest partial locale and the lowest complete one; `be` reached 84.9% and the gap closed to 15.1. The case was written when the four partials sat near 40% and nothing came near the boundary — so a between-groups gap measures how little has been translated, and could only be restored by stopping. Replaced with the thing the gap was a proxy for, per row: no locale may sit within 4 points of the 90% threshold, which is two feature PRs' worth of denominator drift at ~5 keys to the point. It is a tripwire and is meant to fire — `be` is 5.1 points clear, about one more screen family — and the answer then is to decide what the Belarusian badge says at 90%, not to widen the margin.

  Coverage: be 79.7% → 84.9%, pl 78.1% → 83.5%, es 77.5% → 82.9%, de 77.3% → 82.7%. Keys English in all four partial locales: 101 → 75.

  `npm run verify` green: typecheck, `lint:all`, 6353 tests (+5), build.

## The screen the collection hands you to, and the 22 keys it had already been given (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Add-item-screen follow-ups" — "`app/item/[id].tsx` is now the largest single holder at 33 of 59 ... The next screen entry is `app/item/[id].tsx` on size alone". Filed this morning by the run that translated the add-item screen. -->

- [DONE: 2026-08-22] [est: 24000] **Translate the item-detail screen, and pin the screen-screen overlap the header only claimed** — 12 keys into be, pl, de and es, and the interesting number is the one that was wrong.

  **The suggestion said 33 of 59 and the answer was 12.** The count it was filed with was a FILE-level one taken before the add-item screen shipped, and `app/item/[id].tsx` shares 22 keys with `app/create.tsx` — the detail screen's edit mode IS the add-item form, down to `conditionLabel`, `tagsAdd` and `requiredFieldsMissing`. Twenty-one more are owned by prefix families. So the family is 38 keys, of which 12 were English in all four partial locales: `delete`, `itemNotFound`, `addedBy`, `acquiredHow`, `acquiredDate`, `variants`, `editItem`, `saveChanges`, `cancelEdit`, `editPhotos`, `share`, `shareItemHint`.

  **Re-measuring changed which screen is next, too.** `app/collection/[id].tsx` is the largest remaining holder at 31 of 56, not the item screen — and five of the twelve translated here (`delete`, `acquiredHow`, `acquiredDate`, `variants`, `share`) are on that screen, so it starts from 26 rather than 31. The lesson is in the suggestions: a per-screen count filed one run ago is stale the moment an overlapping screen lands, and the honest form is to re-run the measurement rather than to trust the number in the entry.

  **The screen-screen overlap stopped being hypothetical, so it got a case.** The disjointness case has always skipped screen families, with a header saying "two screens rendering `cancel` is the ordinary case" — a claim about a situation that had not happened: when the sign-in screen joined, the three listed screens shared nothing at all (create ∩ login = 0, login ∩ item = 0). The item screen made it real at 22, and the new case pins the overlap at non-empty rather than at a number, because the day no two screens overlap the honest edit is to fold screens into the disjointness check rather than keep a skip nothing exercises. Its second half asserts the shared keys are declared in all six locales — the overlap is benign precisely because both entries make the SAME demand, and a shared key missing from a locale should read as one bug, not two.

  **German stayed on *du* for the eighth consecutive family** — `Schick diesen Link an wen du willst`, which the register guard would have failed as `Schicken Sie`.

  Size case mutation-checked: a `t("settingsTitle")` added to the screen fails with "now has 39 base keys, measured at 38".

  Coverage: be 77.3% → 79.7%, pl 75.7% → 78.1%, es 75.1% → 77.5%, de 74.9% → 77.3%. Keys English in all four partial locales: 113 → 101.

  `npm run verify` green: typecheck, `lint:all`, 6348 tests (+7), build.

## The exclusion that was defending against nothing it claimed to (2026-08-22)

<!-- Not from a suggestion: found while re-reading the module extracted three tasks earlier, checking a rationale the extraction had carried over verbatim. -->

- [DONE: 2026-08-22] [est: 6000] **Measure what `check-orphan-i18n-keys`' one exclusion is actually worth** — the guard skips `lib/i18n-context.tsx`, and the reason both files gave was "it declares every key, so including it would make every key read and the guard vacuous". Measured: including it makes ZERO keys read. The map writes its keys as identifiers (`greeting: "Hello"`), and an identifier is not a string literal, so no key in that file sits in a key POSITION — the rule the guard was tightened to a year of commits ago.

  **The exclusion is still right, and now says what it defends against.** Quoting the keys (`"greeting": "Hello"`) puts all but the first after a comma, which IS a key position, and the orphan guard would go vacuous in a commit whose diff reads as formatting. That is a real edit somebody could make; "declares every key" was not.

  **Pinned at zero rather than argued.** A case reads the real translations file and asserts it names none of its own keys in a key position, then asserts the quoted shape IS visible to the rule — because the second half is what makes the first half worth writing down. The "all but the first" precision came from the case failing on its first fixture, where the only key followed `{` rather than a comma.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6342 tests (+1), build.

## The screen the sign-in screen hands you to (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Screen-family follow-ups" — "`app/create.tsx` is the strongest of the three by the same argument the sign-in screen won on: it is the first thing a new account is asked to do, and 38 of its 46 keys are English". Filed an hour earlier by the run that added the screen kind. -->

- [DONE: 2026-08-22] [est: 30000] **Translate the add-item screen, and settle who owns an overlapping key** — 37 keys into be, pl, de and es, plus the ownership rule the entry could not be added without.

  **The overlap arrived exactly where the previous run predicted it would.** `app/create.tsx` renders `collectionFieldLabel` and `collectionPickerSearchA11y` (the `collection*` family) and `searchPlaceholder`/`searchNoResults` (the `search*` family), so the disjointness case had to be decided rather than dodged. The rule: a PREFIX family owns its keys and a screen family claims what is left of its file. A key under the prefix rule is already all-or-nothing in six locales, so the screen's claim would add nothing but a second name in a failure message, while the other answer would quietly remove a key from the family a reader goes looking for it in. Two SCREENS overlapping stays fine and unchecked — `cancel` and `saving` are on half the screens in the app, and both entries wanting them translated is the same demand made twice.

  **The rule found a gap in its first run that the measurement had missed.** `saveItem` was declared in `ru` and `be` and inherited by `pl`, `de` and `es` — invisible to the "English in all four" scan the screen was chosen by, because `be` had it. That is the seam the screen unit exists to close, and it closed one on day one.

  **`acquiredDatePlaceholder` is the third exemption, and it is a format rather than a phrase.** `2025-11-03` is what the field's parser accepts, so a locale writing `03.11.2025` would be documenting an input that gets rejected. `ru` declares it identically and is therefore not "using" the exemption, which the row reports correctly — a case pins both halves.

  **The placeholder-length guard caught four strings, which is the guard working rather than a mistake.** `sourcePlaceholder` is a single-line field capped at 40 characters and all four translations ran 43–49; they are now examples rather than sentences (`Аўкцыён, падарунак, барахолка...`, `Auktion, Geschenk, Flohmarkt...`). English is 38, so the cap was always going to bind on the languages that spell things out.

  Coverage: be 69.7% → 77.3%, pl 67.9% → 75.7%, es 67.3% → 75.1%, de 67.1% → 74.9%. Keys English in all four partial locales: 153 → 116.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6341 tests (+6), build.

## The first screen of the app, in none of the four languages (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Key-usage index follow-ups" — "pick the screen with the most keys still English in the four partial locales (`byFile` answers this in one pass now), translate that screen's set, and let the rule be 'this screen's keys are complete in every locale'". Filed this morning as the consumer the index was built for; taken with the measurement it asked for. -->

- [DONE: 2026-08-22] [est: 26000] **Translate the sign-in screen, and make SCREEN a kind of family** — the index answered the question three runs have been asking, and the answer was not the screen anybody would have guessed. `app/collection/[id].tsx` has the most untranslated keys (38 of 56) and `app/create.tsx` is close behind (38 of 46), but `components/login-screen.tsx` is **23 of 23**: the only screen in the tree where every string a user reads is English in be, pl, de and es. It is also the FIRST screen, reached before anything else in the app, by a person who has just chosen their language in the picker.

  **22 keys translated, not 23, and the 23rd is why yesterday's exemption mattered today.** `emailPlaceholder` is `you@example.com` — an address, in `UNTRANSLATABLE_KEYS` since this morning — so the screen family subtracts the exemptions and asks for 22. Without that, the first screen family would have demanded the English placeholder be pasted into five locale maps, which is the shape that teaches people to exempt a rule rather than meet it.

  **`Family` gained a second kind rather than a second table.** A screen family names a file and READS its keys through `lib/i18n-key-usage.ts`; the alternative is a hand-written list of the keys somebody believed the screen renders, which is a second statement of the file's contents that drifts the moment a string is added. Because the file is the list, a new string on a listed screen arrives already inside the rule: mutation-checked by adding one `t()` call to the sign-in screen, which fails the size case naming 23-against-22.

  **That same mutation failed a SECOND case, and that is the interesting one.** The key added was `wishlist`, which the wishlist prefix family already claims — so "names no family twice" went red too. A screen legitimately renders keys from prefix families, so the day a listed screen grows one, somebody has to decide which entry owns it rather than discovering the key is checked twice under two names. The disjointness case is what makes that a decision instead of a surprise.

  **German followed *du* for the seventh consecutive family, and this time the rule enforces it.** `Melde dich an`, `Gib die E-Mail-Adresse ein`, `versuche es später erneut` — the register guard shipped yesterday would have failed a `Geben Sie` here, which is the point of having written it down.

  Coverage: be 65.3% → 69.7%, pl 63.5% → 67.9%, es 62.9% → 67.3%, de 62.7% → 67.1%. Keys English in all four partial locales: 175 → 153.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6335 tests (+6), build.

## Six suites asking the per-screen question with the rule the guard abandoned (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Key-usage index follow-ups" — "Seven suites ask the per-screen question with a rule this repo has already measured as wrong ... Seven call sites, one helper, and the rule stops being stated eight ways". Filed by this morning's run, and taken next because the entry above it says the new index shipping with no consumer is the risk. -->

- [DONE: 2026-08-22] [est: 12000] **Route "does this screen render these strings" through the key-usage index** — `share-sheet`, `bulk-bar-extraction`, `edit-collection-modal`, `move-collection-modal`, `collection-share-sheet` and `photo-lightbox-item-card` each built `new RegExp(\`t\\("${key}"\`)` against one file's text, once per key. Nothing was wrong with the answers; the RULE is the one `check-orphan-i18n-keys` measured as wrong for 43 of 502 keys, because a key can be stored in a table, handed to a helper, or named in an `as` clause. The first screen to move a key into a table goes red while still rendering it.

  **The index got its consumer, which was the open risk on it.** An enabling extraction with no caller is one run away from reading as speculative generality — filed as such this morning, in the same entry that named these six. `assertReadsKeys(file, keys)` is fifteen lines over `indexI18nKeyUsage`, and the rule is now stated once for the guard and the suites together.

  **The conversion added a check the regex form could not make.** A key asserted here must exist in the base map — the old shape passed as long as the SCREEN named the key too, and a screen calling `t("undeclaredKey")` renders the key name itself, in every language. Nothing in the tree was violating it; it is a hole that was open, not a bug that was live. Mutation-checked with both halves at once: a key no map declares and a key declared but unread fail separately, each naming the file and the whole missing set rather than the first name.

  **`a11y-icon-labels.test.ts` was in the suggestion's list of seven and is deliberately not converted.** Its pattern is `key: "…", label: tabLabel(t("\\w+"), badge)` — a claim about the SHAPE the label is composed with, not about which keys the file reads. Converting it would have replaced a structural assertion with a weaker presence one, which is how a consolidation quietly deletes a check.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6329 tests, build.

## A percentage of something nobody can finish (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Per-screen i18n follow-ups" — "`appName` and `emailPlaceholder` still subtract from every percentage ... an `UNTRANSLATABLE_KEYS` set excluded from the denominator would make `ru` read 100% and make the four partial rows a percentage of what is actually translatable. Cheaper now than later — the set is two keys." Filed three times across three runs, which is the argument. -->

- [DONE: 2026-08-22] [est: 14000] **Take the brand name and the email address out of the denominator** — `appName` ("Collectables") and `emailPlaceholder` (`you@example.com`) are correct in English in all six locales, and every coverage row counted them as gaps. `ru` read 99.6% and could not be moved: the missing 0.4% was a brand and an address, under a threshold somebody would eventually be tempted to lower rather than a number anybody could raise.

  **The list is the one exemption this module has, so it is built to be hard to grow.** `UNTRANSLATABLE_KEYS` is a Record of key → REASON, not a set: an exemption is a key nobody has to justify again, and the cheapest defence is making the justification something a reader meets. The membership test is written down — would a translator handed this key correctly hand it back unchanged — and a case caps the list at five while another refuses a reason shorter than twenty characters.

  **`translatable` is a new field rather than a redefinition of `baseKeys`.** One number would silently mean the second while being read as the first; `501 keys in the map, 499 of them translatable` is the honest pair, and `formatCoverageRow` now names the exemptions on any row that uses one so a reader who sees a denominator of 499 does not go looking for the difference.

  **The staleness check could not live in the module, and finding that out was the useful part.** Shipped first as a throw inside `translationCoverage` — an exemption naming a key the base map does not declare removes nothing and looks exactly like one that works — and it turned three cases red immediately: that function is handed ANY source, and every hand-checkable fixture in the suite declares four keys, none of them a brand name. So the arithmetic stays general and inert, and the claim about THIS repository's map is asserted in the suite against the real map, where it can be made honestly. Mutation-checked by planting a `goneKey` exemption: two cases fail, and the second one's message was rewritten because it said "every locale declares it" for a key the base map does not have — right finding, wrong sentence.

  **The old case that pinned `ru`'s two inherited keys by name survives as two.** `inherited` is a WORKLIST now and `ru`'s is empty; the exemptions it is using are reported by `untranslatable`, so a third untranslated key arriving in `ru` still fails one case and a third exemption fails another. A locale that declares an exempt key anyway is credited with it rather than reported as having a gap.

  Floors untouched — they are on DECLARED keys, which is a fact about the file and did not change. The `ru` note now says why it reads 100% without declaring one more key.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6329 tests (+7), build.

## The reverse index the orphan guard was throwing away (2026-08-22)

<!-- Promoted from .tasks/.suggestions.md, "Per-screen i18n follow-ups" — "A per-screen family needs a way to know what a screen renders, and the repo already has most of it ... extracting that as a reusable reader is the enabling step, and it is smaller than the family rule that would sit on top of it". Taken as filed: the entry above it says the prefix unit is spent at 175 keys, and the per-screen unit cannot be built until something can say what a screen reads. -->

- [DONE: 2026-08-22] [est: 18000] **Extract `lib/i18n-key-usage.ts` — file → keys, key → files — and make the orphan guard a projection of it** — `check-orphan-i18n-keys` walked every source file, collected the key-position literals of each, and poured them into one flat `mentioned` set. The file each literal came from was gone by the time the answer was formed, so the walk computed the per-screen fact and discarded it to answer the smaller question.

  **The extraction is the enabling half and stops there, deliberately.** It indexes; it does not judge. No coverage rule, no screen table, no threshold — those are the thing that sits on top, and filing them into the same module is how an index becomes a rule nobody can reuse. What moved is `collectStringLiterals`, its regex and the whole "what counts as a READ" argument (four forms, five key positions, the `?? "you"` that made position matter); what stayed in `check-orphan-i18n-keys.ts` is the orphan QUESTION — which keys came back with no readers, which locales declare them, and how that reads as a report. That file is 143 lines shorter and now says what it is about.

  **Both maps are complete over what was asked, and that is the design decision worth the empty arrays.** Every scanned file has an entry in `byFile` and every key handed in has one in `byKey`, empty where there is nothing to report. A sparse map cannot tell "this screen renders no translated text" from "this screen was not scanned" — and a per-screen rule that could not tell them apart would read an unwalked directory as a screen with nothing left to translate, which is the failure mode that makes a green guard worse than none. Mutation-checked by making `byFile` sparse: two cases fail, naming both halves.

  **`findOrphanI18nKeys` throws where the index would shrug.** No base map means no keys asked about, which the index answers honestly with "nothing unread" — a green run and not a finding. So the base-map lookup keeps its throw, now in a `baseKeys()` of its own that says why in three lines rather than by sitting inline.

  **Per-file lists come back in BASE-MAP order**, not in the order literals appear in the file: keys belonging to one feature are adjacent in the map, so a family reads as a run. Per-key lists keep scan order, which is the caller's own — the walk sorts, so the list reads alphabetically without this module claiming to have sorted anything.

  **The new suite runs over the real tree, which is the case the extraction is FOR.** An index that credited `app/` with nothing would pass every unit case and be useless to build a rule on; the suite asserts more than half the screens read a key, that every key it names is a base key, and that the index and the guard agree on there being no orphans. Floors rather than counts, since the exact numbers are a chore and the claim is about the index being populated.

  **The seven suites that ask this question by hand are the follow-up, not this task.** `share-sheet`, `bulk-bar-extraction`, `edit-collection-modal`, `move-collection-modal`, `collection-share-sheet`, `photo-lightbox-item-card` and `a11y-icon-labels` each match `t\("key"` against one file's text — the per-screen question, answered with the rule this repo already measured as wrong for 43 of 502 keys. Nothing is red today because those screens all use the direct call; the first one to store a key in a table would go red while still rendering it. Filed with the counts.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6322 tests (+14), build.

## The third spelling of one rule, and the two buttons the funnel could not tell apart (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Dead-branch follow-ups" — three entries from yesterday's run: "the same `visibility === "private"` shape guards the save-time clamp and nothing says whether THAT one is reachable", "`premium_upsell_shown` now fires from two controls per screen and the event cannot tell them apart", and "the locked hint is a `<Pressable>` with no pressed state and no hit-slop". One task because all three are the same row, and the first two are both about a decision that gets read off it. A fourth entry in the group — "three screens now decide is private locked and the third is the marketplace listing sheet" — was checked and is wrong: `app/item/[id].tsx` gates the LISTING CAP on premium, not visibility, and shares no rule with these two. -->

- [DONE: 2026-08-21] [est: 15000] **Route the save-time clamp through `isPrivateVisibilityLocked`, tag the upsell with which control fired it, and make the sentence feel like a button** — yesterday's task made one rule out of the two visibility ROWS and left the third copy where nobody was looking: `handleSaveEdit` in `app/collection/[id].tsx` wrote `!isPremium && editVisibility === "private" && (collection.visibility ?? "private") !== "private"` for itself. Same three clauses, a fourth screen away from the chip they have to agree with, and free to drift in exactly the way the two rows already had.

  **First, the question the suggestion said nobody could answer: the clamp is unreachable, deliberately.** `openEditModal` seeds `editVisibility` from the collection, and the sheet's locked chip calls the upsell and returns before changing it, so a free user cannot arrive at the save having selected "private" on a public collection. That makes it defence in depth rather than dead code, and it is kept — the day the chip is bypassed it is the only thing between that bypass and a private collection on a free account. What it is not is a place to restate the rule, so it is now `clampVisibilityForSave(isPremium, editVisibility, collection.visibility ?? "private")` in `lib/premium-helpers.ts`, one branch over `isPrivateVisibilityLocked`. Being unreachable at runtime is precisely why it needed pinning: nothing exercises it, so a regression to a fourth hand-written spelling would look like working code forever. An exhaustive case walks all four `(isPremium, saved)` combinations and requires the clamp and the chip's lock to agree, because the two disagreeing is the actual bug — a chip that refuses a change the save would have allowed reads as broken, and a chip that allows one the save silently reverts is worse. The argument order is asserted in both directions: `selected` and `savedVisibility` are both `CollectionVisibility`, so swapping them type-checks and clamps backwards in silence.

  **Second, the event that could not tell its two senders apart.** The chip and the sentence beside it are the same button, so they send the same `feature` and the same `source` — which meant `premium_upsell_shown` had no way to say which one a person pressed. That matters because the standing `[needs-data]` decision further down this file is specifically about the CHIP: with the two merged, taps on the SENTENCE would keep it alive forever, and hiding it would look like it cost traffic it never had. `control: "chip" | "hint"` is now a required argument of each screen's `showPrivateUpsell`, so a new call site cannot forget it, and it is registered in `ANALYTICS_EVENTS` — `assertValidProps` strips an unlisted key in production and throws on it in dev, so a payload the registry does not name is lost rather than wrong. A case asserts both literals appear in both files: forwarding the argument is worth nothing if both call sites pass `"chip"`, which is what a copy-paste lands on, and the funnel would then look exactly as it did before with a name on it.

  **Third, the sentence now behaves like the control it became yesterday.** It was a 20px-tall line of text with no hit-slop and no pressed state — the shape most likely to feel broken on a phone. `hitSlop={8}` is this tree's existing answer (eight other controls use it) and the pressed style is opacity rather than a colour swap, so the amber that marks it as pressable survives the press. Both halves are asserted, because either alone still leaves it feeling dead.

  `docs/powerbi-connection.md` is regenerated rather than edited — `lint:powerbi-doc` caught the drift the moment the registry gained a prop, which is the guard working. `npm run verify` green: typecheck, `lint:all` 16/16, 6145 tests (+9), build. No DB migration, no creds, no new i18n keys.

## Fifteen strings for screens that were rewritten, and six for a UI nobody built (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Dead-i18n-key follow-ups" — the top entry, and the family the suggestion's second bullet asked to trace before deleting. No unblocked task remained in this file: the three survivors are all `[needs-data]` / `[needs-dep-decision]` / `[needs-dev-dep]`. Taken family by family as the suggestion advised, and this family is the one that does NOT cross the `ru` floor, so it needs no floor re-measurement and cannot collide with the two that do. -->

- [DONE: 2026-08-21] [est: 18000] **Delete the fifteen dead social/people i18n keys, and give the third absence guard the helper the second one earned** — an orphan scan over the base map (rerun here, 35 orphans, matching the suggestion's count exactly) confirmed no reader in `app/`, `components/`, `lib/`, `scripts/`, `data/`, `__tests__` or `docs/`, and no dynamic `t(\`prefix${x}\`)` build spells them — the only dynamic constructor in the tree is the `condition*` one, which touches none of these. The suggestion's second bullet asked for `git log -S` per key before deleting, and it needed the clone un-shallowed (50 commits of 892) to answer; the answer splits the family in two and the split is the interesting part. NINE had real call sites, added at `init` and deleted in `8b35e1b` on 2026-04-12 when `app/people.tsx` was rewritten and `app/friends.tsx` was added — `peopleTitle`, `peopleSubtitle`, `noPeopleFound`, `loadingPeople`, `noFriendsYet`, `noFriendsYetTab`, `noRequestsYet`, `noFollowingYet`, `subscribeToSeeCollections` — ordinary leftovers. SIX never had a call site in any commit: `friendBadge`, `followingBadge`, `invitationSent`, `searchFilterByOwner`, `profileIdHint`, `saveProfileId` — copy for a friend/following badge on a profile row, a "By owner" search filter and a "Save profile ID" field with its hint, none of which was ever built. `invitationSent` is the one of the fifteen translated into all six locales, so four languages carried a translation for a string nobody ever rendered; the suggestion's "dead keys inflate every denominator" point, with a name on it. Filed as a suggestion below rather than left implicit, which is what the promoting entry asked for.

  Counts: base 537 → 522, `ru` 535 → 520 (both clear of their floor of 500 — `en` and `ru` declared all fifteen), and be/pl/de/es drop one each with `invitationSent` (237/228/224/224 → 236/227/223/223, floors 205/197/193/193). No `TRANSLATION_FLOORS` re-measurement, which is exactly why this family went first: the two still outstanding (collections-list 10, feed/share 10) land `ru` at 500 against a strictly-greater floor, so whichever is removed last owns that re-measure. `i18n-translation-coverage.test.ts`'s "publishes the honest number" moved to `en: 522/522` with the reason prepended to its note; `ru`'s inherited list stays at two (a brand and an email address) because `ru` DECLARED all fifteen, so removing them adds nothing to the inherited set.

  The guard is the third of its shape, which the suggestion named as the point to stop copying the loop: `assertNoLocaleDeclares(source, predicate, why)` now sits in `__tests__/helpers/i18n-locales.ts` beside `assertDeclaredInEveryLocale` as its mirror, and both hand-rolled loops moved onto it — `visibility-chip-a11y.test.ts`'s `visibilityShared` case and `runtime-config-i18n-forbidden.test.ts`'s `runtimeConfig*` case. A predicate rather than a key because the three claims are three shapes (an exact name, a prefix, a set of fifteen), and the part worth writing once is the ANSWER: every offending `locale: key` pair listed. The realistic regression is a removal that reaches `en` and misses `ru` — the inheriting locales serve English either way, so nothing at runtime shows it — and that is what the message names. Deliberately reads DECLARED keys rather than resolved ones: a key only `en` declares resolves for `ru` through its `...en` spread, and reporting `ru` for it would make the failure unfixable, because there is nothing in the `ru` map to delete. Pinned by a case.

  New `__tests__/dead-social-i18n-keys.test.ts` (3 cases) pins both halves of the removal, because re-adding the key is only half of a reintroduction: the absence in every locale map, AND that nothing in the source tree names one of the fifteen — through `sourceCode`, so a comment recording why a key went is not itself an offence, and over the source tree only, since this suite writes all fifteen names out. Mutation-checked both ways: reviving `peopleTitle` in the `ru` map turns both cases red naming `ru: peopleTitle`, and planting a `"peopleTitle"` reference in `app/people.tsx` with no declaration turns the second red naming `app/people.tsx:peopleTitle`. Five cases added to `i18n-locales-helper.test.ts` for the new helper, including the half-removed fixture and the mirror-of-the-declaration-check pair.

  `npm run verify` green: typecheck, `lint:all` 14/14, 5960 tests (5961 before — twelve per-locale cases collapsed into three helper calls whose messages name the locale, eleven added), build. No DB migration, no creds.

## "Mark as received" buyer confirmation (decomposed from "Persist a Mark as received button on each purchase card")

Context: lets a buyer confirm a marketplace purchase arrived via an `arrivedAt` timestamp, unlocking dispute handling and a "delivery confirmed" signal on the seller's sales card. Decomposed bottom-up — pure data layer first, then local persistence, then buyer UI, seller UI, then cloud sync + migration. Each sub-task ships independently.

<!-- Decomposed 2026-07-04 from "Hide the visibilityChip for private entirely on free accounts once we have analytics confirming nobody clicks the locked chip": the precondition was unverifiable because the locked-chip tap was never instrumented — no event, so "nobody clicks" can never be confirmed. Split into (a) instrumentation, actionable now, and (b) the hide itself, blocked on collected data. -->

- [needs-data] Hide the `visibilityChip` for "private" entirely on free accounts (instead of locking it) once PostHog shows ~zero `premium_upsell_shown` events with `feature = "private_collection"` over a meaningful window (say 30 days after the instrumentation ships) — a cleaner flow at the cost of a less discoverable upgrade prompt. Blocked until that data exists; do not implement before checking the dashboard.

<!-- Decomposed 2026-07-11 from "Add a __tests__/analytics-provider-strict-mode.test.ts that mounts <AnalyticsProvider> under <React.StrictMode>": the repo has no React mounting harness (no jsdom, no react-test-renderer — only the fake-dom stub), so a real StrictMode mount is blocked on a dev-dep decision. Decomposed bottom-up: pure scheduler first (node-testable now, mock timers), provider wiring second, the actual mount test last (blocked). Note the 500ms identify debounce already shipped — the double-mount invariant is now "two rapid schedules collapse into one identify". -->

### ItemCard design-tokens migration (2026-05-25)

- [needs-dep-decision] Migrate `LazyPhoto` onto `expo-image` for real offscreen deferral (`priority="low"`, `recyclingKey`, built-in transition) — blocked on adding the `expo-image` dependency (new package + lockfile churn; decide alongside the jsdom/react-test-renderer dev-dep decision the `[needs-dev-dep]` tasks are waiting on). The `LazyPhoto` component shipped 2026-07-16 is the single adoption point, so the migration is a one-file swap when the dep lands.

### EmptyState design-tokens migration (2026-05-25)

- [needs-dev-dep] **Migrate the `borderStyle: "dashed"` rendering test** [blocked 2026-07-17: a headless-browser screenshot test needs a browser-automation dev-dep (`@playwright/test` or similar) that `npm test`'s plain `tsx --test` harness doesn't have — decide it alongside the jsdom/react-test-renderer dev-dep the `[needs-dev-dep]` analytics tasks wait on] — React Native Web renders dashed borders inconsistently across browsers (Safari + Firefox honor it; Chrome on Android sometimes drops it). A visual regression test using a headless browser screenshot would catch silent drops, especially since `EmptyState` is the only component in the design-tokens-migrated set that uses `borderStyle: "dashed"`. Without it, a future React Native Web upgrade could mute the visual without anyone noticing.

### Item-filters design-tokens migration (2026-05-23)
