## The last lines both rows still spelled twice (2026-09-10)

<!-- Second and fifth bullets of "Reorder-extraction follow-ups" (2026-09-10): "The pick-up announcement is the last thing both screens still duplicate" and "The two rows carrying custom actions still carry no accessibilityRole". -->

- [DONE: 2026-09-10] [est: 18000] **The long press and the row's role joined `reorderActionProps`** — after the extraction the two screens still spelled one thing identically: `announceReorder(t, "reorderPickedUp", index, rows.length)` immediately before `drag()`. It is also the line whose ORDER matters, and the order is not obvious — announcing after `drag()` names wherever the finger has reached rather than where the row was, which is a sentence that is wrong in a way only a listener can notice. It comes back as `onLongPress` in the same spread now, and the ordering is asserted by running it rather than by matching two files.

  **`drag` is optional, and that is the viewer's row.** Omitting it leaves `onLongPress` undefined — no long press at all, rather than one that announces a pick-up the user cannot perform. `app/collection/[id].tsx` passes `isOwner ? drag : undefined` and keeps the whole ownership decision in one expression; the home screen passes `drag` unconditionally, because every owned collection is the user's own to reorder.

  **`announce` is one pass-through callback instead of two.** `(key, at, total) => announceReorder(t, key, at, total)`: which moment just happened and which position it is about are both the module's answers, and a screen that named the key itself would be choosing when to speak in two places again. `at` is `number | undefined` because the pick-up is the one moment an unknown index must not block — the gesture has already begun, so the drag runs and `announcedPosition` refuses the number downstream.

  **`accessibilityRole: "button"`** on both rows, raised twice before this and never done because it was two edits: `accessibilityActions` on a node with no role is announced inconsistently — TalkBack reads them off anything focusable, VoiceOver frequently does not offer the rotor until the element claims one. One return value now covers both call sites.

  6 new cases in `__tests__/reorder-actions.test.ts` (the announce-then-drag order, the pick-up naming the row's current place, the undefined index that must not block the gesture, the absent long press, the two routes being independent, the role). Both screens' i18n families fell one more (27 → 26, 48 → 47): `reorderPickedUp` is no longer named in either file, while `reorderMoved` still is, because the DRAG's own landing announcement stayed in the screen — both numbers moved with the reason beside them. Seven pins across three suites were re-anchored on what each screen still owns. `npm run verify` green: 7984 tests, 20/20 guards, bundle 10.6 KiB under budget. No DB migration, no creds.

