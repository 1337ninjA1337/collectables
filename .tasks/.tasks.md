- [DONE] [est: ~180k tokens] add chats (users can chat to each other but only friends)
- [DONE] [est: ~30k tokens] Set `EXPO_PUBLIC_APP_URL` as a build-time environment variable in CI/GitHub Actions so `getAppBaseUrl()` always returns the exact deployed URL instead of inferring it from `window.location.pathname` at runtime. This makes deep links reliable regardless of which page the user is on when they trigger the share flow.
- [DONE] [est: ~100k tokens] Wire the new test suite into a GitHub Actions workflow that runs `npm test` on every push and PR. The tests are fast (≈1s locally) and would catch regressions in the pure logic (placeholder color hashing, AI JSON parsing, Cloudinary URL parsing, seed integrity, i18n key coverage) before they reach main. Also fix the pre-existing `tsc --noEmit` errors in `app/profile/[id].tsx` (`activeProfile possibly null`) and add `npx tsc --noEmit` to the same workflow so type regressions are caught too.
- **[~150k tokens]** When a private collection is opened via a shared deep link we now auto-register the visitor as a viewer, but there's no "revoke access" UI for the owner yet. Add a list of everyone who currently has access on the share sheet (not just the friends toggle list) so an owner can see link-granted viewers and remove them if needed. This turns the implicit deep-link grant into an auditable permission list and reduces the risk of a leaked link giving indefinite access.
- **[~200k tokens]** Chats are currently stored only in each user's local AsyncStorage (key `collectables-chats-v1-{userId}`), so a message I send never actually reaches the recipient's device — the recipient sees nothing. Move chat storage to a Supabase `chat_messages` table (columns: `id`, `chat_id`, `from_user_id`, `to_user_id`, `text`, `created_at`) with RLS policies that restrict read/write to the two participants, mirror incoming messages into local state via a realtime channel subscription, and gate writes on a server-side friendship check so the "friends only" rule can't be bypassed by a crafted client. Add a "typing..." indicator and a small notification badge on the bottom-nav friends tab so users know when a friend has sent them something while they were elsewhere in the app.

- implement marketplace {
    - add page for marketplace (also it should be added into navbar)
    - add possibillity to add items into marketpalce (item on marketplace should show info about it and owner)
    - items added to market can be traded or sold (sold outside of app)
    - add possibillity to send messages to owner of item that found on marketplace (chat will open to both persons who owns item and who wants to buy/trade it)
    - items that added into market should include price history of same items (for now check just items name if it same more than 90% that show its price history)
    - users can add only 1 item on market untill they don't have a premium account that will be added later
}
- add possibility to upgrade account to a premium status
