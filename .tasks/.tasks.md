## The rule stopped at lib/ and five of the nine copies were outside it (2026-09-25)

<!-- First bullet of "After the pinned fix and the quoted prose" (2026-09-25): "Three suite headers quote the same 115 KiB and the prose rule was handed `lib/` only — `proseQuoteProblems` reports an unregistered copy in any module it is HANDED. The argument is whether a suite's header is prose a reader trusts or scaffolding beside the case that proves it." -->

- [DONE: 2026-09-25] [est: 12000] **The argument for `lib/` only did not survive counting the copies** — the rule was handed the modules a reader reaches for, on the reasoning that a suite header is scaffolding beside the case that proves it. Five of the nine statements of `ascii_only`'s cost are outside `lib/`: three suite headers, one comment in `scripts/check-bundle-smoke.ts`, and — the one that settled it — `assert.match(line, /115 KiB/)` in `bundle-smoke.test.ts`, a literal copy of a measured number sitting one screen above a case that derives the same number out of the audit table.

  **The literal assertion is gone rather than registered.** That case is about the SHAPE of the failure message (it has to name the option, the config file and a cost); which cost is the next case's question, and that case already asks the table. `/~\d+ KiB/` is what the first one actually meant, and it leaves one copy of the number where there were two.

  **The call site hands over the whole tree now** — `sourceFiles()` plus `suiteFiles()`, so `app/`, `components/`, `data/` and `scripts/` are in reach of the unregistered-copy scan as well. Four new registry entries, eight files covered.

  0 new cases (34 in the minifier suite), 9630 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The fix npm offered and the tree could not take (2026-09-25)

<!-- Not from a suggestion list: `npm run verify` went red on its audit leg between one run and the next, with no commit in between. That is the failure shape `check-audit-baseline`'s own closing sentence describes, and the gate was right that something had changed and wrong about what to do next. -->

- [DONE: 2026-09-25] [est: 30000] **`npm audit` started calling two accepted advisories FIXABLE, and every fix it named did nothing** — `image-size@2.0.3` clears GHSA-5p2g-fcmc-qvqq and GHSA-w3rx-r6r6-pgpr, npm reported `fixAvailable: true`, and the gate printed "Run `npm update image-size` and commit the lockfile" on every run. `npm update image-size` left 1.2.1 in place. So did `npm audit fix`. `fixAvailable` is a judgement about DIRECT dependencies — a patched version npm can install without a semver-major on anything the manifest declares — and `metro` declares `^1.0.2`, which is no part of that judgement and is the thing actually holding the tree.

  **The exemption is a measurement, not a sentence.** `AcceptedAdvisory.inRangeFixPinned` takes four fields: which dependent pins the range, what range it declares, which version first carries the fix, and what forcing it past the pin actually did. The last one is a build that was run: an `overrides` entry for `^2.0.4` installs cleanly and `npm run build` then dies on the first image asset, because image-size@2 dropped its default export and `metro/src/Assets.js` calls `_interopRequireDefault(require("image-size")).default(content)`. The first two are facts about `package-lock.json` and are asserted against it, so a dependency bump that relaxes the pin turns the exemption red rather than leaving it describing last month's tree.

  **Loud rather than forgiven, and pruned like every other exemption here.** `pinnedFix` is carved out of `fixableInRange` instead of being excused inside it, and it prints on the GREEN path — an exemption nobody sees is the silence this gate was built to end. `pinnedFixUnused` is the other end: a baseline entry claiming a pinned fix that npm no longer reports as in-range fixable fails the gate, the same way `stale` does and for the same reason, and `worthAsking` re-asks it because it too is built out of what npm did not say.

  6 new cases (191 in the audit suite), 9630 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Three copies of one quotient, one of them checked (2026-09-25)

<!-- First bullet of "After the pinned percentage" (2026-09-23): "The module header quotes '3% of the bundle' and no case reads it — the new rule holds percentages inside `note` fields, which is where the table's data is; `lib/minifier-audit.ts`'s own header makes the same claim about `ascii_only` in prose, and so does `lib/bundle-smoke.ts`'s." -->

- [DONE: 2026-09-25] [est: 20000] **The percentage rule stopped at the table's edge, and the numbers that started the audit live outside it** — `auditedDeltaShare` is held against every percentage inside a `note`, which is the half of the duplication that sits in the data. The half a reader actually meets is in module HEADERS: `ascii_only` cost "3% of the bundle" and "115 KiB", and those sentences are in five `lib/` modules, none of them a note, none of them read by anything. Re-measure the row and all of them keep making the old argument.

  **A verbatim fragment rather than a number and a filename.** `AUDITED_PROSE_QUOTES` holds the sentence as the file spells it; the number is parsed back out of the fragment, so the registry never carries a second copy of the quantity it is checking. A fragment that has left the file is a rewritten sentence and says so; a fragment whose number has left its row names the file, the row, both numbers and the edit.

  **And the direction a registry usually lacks.** Any module that spells an audited row's KiB figure and has no entry for it is reported as `unregistered`, so the next header to say "115 KiB" joins the check by existing rather than by somebody remembering the list. KiB only for that half, and the asymmetry is argued: an integer KiB figure is a string a scan can look for, while a percentage is rounded to whatever reads well and cannot be searched for without guessing how the writer rounded.

  8 new cases (34 in the minifier suite), 9630 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The percentage a note spells, held to the one the row derives (2026-09-23)

<!-- Second bullet of "After the one-number denominator" (2026-09-23): "The table's notes spell percentages that `auditedDeltaShare` now derives — '0.0009%' is in `compress.reduce_funcs`'s note as text, and the function computes 0.00089% from the row it describes. One of the two moves when the row is re-measured." -->

- [DONE: 2026-09-23] [est: 6000] **A quotient written twice, and the prose copy is the one that stays behind** — `compress.reduce_funcs` argues from "0.0009%" in its own note, beside the delta and the total that produce it; re-measure the row and the note is still making the old argument in a sentence nobody edits. The case reads every percentage any note quotes and holds it against `auditedDeltaShare` for that row.

  **A relative tolerance, because a note rounds on purpose.** 0.0009% against 0.00089% is a rounding a reader wants; what the assertion refuses is a percentage about a different measurement, so the bound is 20% relative rather than an exact match — and the case asserts it found at least one percentage to check, since a scan over prose that matched nothing reports clean.

  1 new case (26 in the audit suite), 9616 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The two denominators were one number in two units (2026-09-23)

<!-- Third bullet of "After the two-ended evidence link" (2026-09-23), carried from the two lists before it: "`MINIFIER_AUDIT_TOTAL_BYTES` is 3,718,641 and `check-bundle-size` reports 3,631.8 KiB — two denominators, both right, and a reader turning -33 bytes into a percentage cannot tell which they hold." -->

- [DONE: 2026-09-23] [est: 13000] **The suggestion asked three times and the arithmetic answers it in one line: 3,718,641 bytes IS 3,631.5 KiB** — the same seven chunks the size gate adds up, in bytes because the deltas are in bytes. There was never a denominator to pick wrong; there were two units and nothing saying so, which is how a reader spends three runs being warned about a choice that does not exist.

  **What can actually go wrong is staleness, and that is checkable.** `BUDGET_SNAPSHOT.bundleBytes` is the same quantity taken on the day the budget last moved — 1,350 bytes away, 0.04% — so two dated readings of one artifact agree until one of them stops being re-taken. The suite holds them within 5% of each other with a message naming the edit; the budget's own headroom (23.8 KiB, 0.6%) bounds how far one build can move them, so a gap that wide means several raises have happened since the audit and the table's percentages are against a bundle that no longer exists.

  **`auditedDeltaShare` so no call site divides by hand.** A delta with no denominator is a number rather than a finding, and the note that says "0.0009%" can now be derived from the row it describes — a percentage rather than a fraction, because the unit is the half of a number a copy drops. `null` for an option with no size answer and for a path the table does not hold, matching `auditedDeltaKiB` beside it.

  3 new cases (25 in the audit suite), 9615 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Six floors, six decisions, and a count that read as a remainder (2026-09-23)

<!-- Fourth bullet of "After the two-ended evidence link" (2026-09-23), and the same question in the two lists before it: "Three floors have drifted past 40% and nobody has decided about any of them — `check-console-swap` at 41%, `check-problem-phrasing-imports` at 46%, `check-comment-terminators` at 40%. `driftAccepted` now exists, so each is one of two edits: re-measure, or record the dated reason it is loose on purpose." -->

- [DONE: 2026-09-23] [est: 24000] **All three notes had already made the argument, and none of them had a date** — every one of the three says some version of "`roots` holds the no-single-root property, so what is left is a plausibility check on the total", which is precisely the sentence `RUNTIME_CODE_WALK_FLOOR` was given `driftAccepted` for two days earlier. An argument with no date is not a decision: nobody can re-open it, and the tool cannot read it, so it asked all three again on every run. Each is now argued about its OWN walk — `check-console-swap`'s 41% is lib/ at 242 files growing weekly, `check-problem-phrasing-imports`'s 46% and `check-comment-terminators`' 40% are both __tests__/ at 631 of the walk — because three walks and three numbers have no constant to hang one sentence on, and the specifics are the part a re-opener needs.

  **Re-measuring was the other option and it is the chore declaring the roots removed.** 744 and 746 would be right for about a week; the failure these numbers still catch is a walk that came back at a tenth of the count with every root present, and 40% slack does not stop them catching it.

  **The precondition is now checked, not assumed.** `acceptedWithoutRoots()` — an acceptance argues from `roots` carrying the property, and on an entry with no declared roots the number is the only thing holding it, so accepting the looseness would be accepting the hole rather than the chore. Same direction as `unusedUncountedExcuses`: a decision whose premise has gone is a paragraph nobody reread.

  **And with nothing left to drift, the report said "6 more floor(s)" with nothing above it.** The word belonged to a sentence the script decided on separately, so clearing the drifted list left a count reading as a remainder of nothing — the reader told where to look, and looking somewhere that is not there. `describeLooseFloors` composes both halves and the shared-edit line between them, so the wording is decided where a case can read it: "6 floor(s) sit this loose on purpose" today, "3 more…" the day something drifts again.

  6 new cases (54 in the floor-walks suite), 9612 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## Evidence that names its subject, at both ends (2026-09-23)

<!-- First bullet of "After the shipped-evidence link" (2026-09-21): "`shippedEvidence` is required of overrides and nothing asks the other direction — a guard could stop being the evidence for anything (an option un-overridden, a row deleted) and keep running as a check with no stated subject. The symmetry `UNCOUNTED_MODULE_REASONS` has — an excuse nothing matches is a finding — has no counterpart here." -->

- [DONE: 2026-09-23] [est: 21000] **An evaluator cannot read the row that cites it** — `unverifiedOverrides` asks every overridden option to name what would go red, and two days of that rule being green hid the direction it does not cover. Delete the `output.ascii_only` row, or flip its `overridden` to false, and `evaluateBundleCharset` keeps running inside `check-bundle-smoke` — a gate leg — as a check with no stated subject: green forever, about a config line nobody asserts any more. That is the shape `unusedUncountedExcuses` was written for one directory over, where an excuse nothing matches is a finding.

  **The link is written at both ends and neither end is a list.** The table names the evaluator; the evaluator's own doc carries `@shippedEvidence output.ascii_only`. A second table of "evaluators that are evidence" would have to be remembered, which is the failure being closed — so membership is parsed out of the source the way `sharedFloors()` reads `SCANNED_FLOORS`, and the evaluator name is the first `export function` after the marker, so a rename moves the claim with it.

  **Three findings, because the three ways a link breaks are different edits.** `evidence_without_subject` is data-only (a row that kept its proof after the `metro.config.js` line went); `unclaimed_evaluator` is the state every override was in before this existed; `orphan_claim` is the suggestion's case, and a deleted row, an un-overridden row, a renamed evaluator and a typo at either end all arrive as it. A broken link reports once — a row that stopped overriding must not also be counted as an evaluator that failed to claim it, or the summary's count is a lie.

  **A doc TAG, not a mention.** The marker is matched only at the start of a comment line, which is what let `lib/minifier-audit.ts` spell it inside its own header while explaining it; a looser match would have read that sentence as a claim about whichever function came next, and the suite asserts exactly that difference. A marker above nothing exported is a claim rather than a skip: an option is named and there is no callable evidence, which is the same quiet green the rule exists to remove.

  8 new cases (22 in the audit suite), 9606 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke (30038 characters as UTF-8, 50 as escapes), ships-to-client. No DB migration, no creds.

## Three settled questions asked on every run (2026-09-21)

<!-- Fourth bullet of "After the shipped-evidence link" (2026-09-21): "Six floors have drifted past the 40% line and the tool now says whose edit each one is… and the three on `RUNTIME_CODE_WALK_FLOOR` at 45% whose looseness is a decision recorded two days ago. The first three are not, and nobody has decided about them." -->

- [DONE: 2026-09-21] [est: 24000] **`remeasure-floors` reported six drifted floors and three of them had been argued two days earlier** — `RUNTIME_CODE_WALK_FLOOR`'s own header says why it is loose: the no-lost-root property moved to `roots`, so the number no longer rides above the largest root, and raising it would reintroduce the re-measure chore that declaring the roots removed. The drift line knew none of that and asked anyway, three times a run. A settled question asked every run is how a reader learns to skim the section the UNSETTLED ones appear in — which today is `check-console-swap` at 41%, `check-problem-phrasing-imports` at 46% and `check-comment-terminators` at 40%, none of which anybody has decided about.

  **`driftAccepted` is the decision recorded where the floor is.** A dated sentence on the entry, or on the shared constant when one number covers several entries — one argument for one walk, the same reason the number itself is not written three times. Not an exemption: the row still prints its percentage and still says the number is doing less than it reads; what it drops is the suggestion, and with it the "edit the constant" advice, since nobody is being sent to the constant.

  **The report splits rather than hides.** Six loose floors are still six lines; the summary now reads "3 floor(s) have drifted loose as the tree grew — check-console-swap, check-problem-phrasing-imports, check-comment-terminators" and then "3 more sit that loose on purpose", each stating its reason in the row above. A tool that quietly dropped the argued ones would disagree with its own table.

  **A date is required and checked.** The acceptance is a decision, and a decision with no date is one nobody can re-open; the case asserts every accepted reason carries a `YYYY-MM-DD` and is longer than a label. One existing case had to move from the drift reading to the `arithmetic` one — it was pinned on a row that now has an answer, which is the case doing its job.

  5 new cases (48 in the floor-walks suite), 9598 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## What proves an override reached the bundle (2026-09-21)

<!-- Fifth bullet of "After the latest-ref guard" (2026-09-19): "`auditMinifierPreset` checks the preset and nothing checks the result — the table says `ascii_only` is overridden, and what proves the override reached the bundle is `evaluateBundleCharset` counting escapes in `dist/`, one module over with no link between them. A second overridden row would arrive with no pattern for how it is verified end to end." -->

- [DONE: 2026-09-21] [est: 31000] **`overridden: true` is a claim about a config file, and a config file is not a bundle** — `auditMinifierPreset` compares Metro's preset against what was measured, which catches a changed DEFAULT. What it cannot catch is an override that stops being honoured: a spread replaced by an assignment, a reordered merge, a key the preset starts ignoring. The thing that would actually notice for `ascii_only` is `evaluateBundleCharset` counting escapes in `dist/` — and it sat one module over, connected to the table by nothing but the fact that the same person wrote both.

  **`shippedEvidence`, required of every overridden row and checked rather than described.** Three named parts: the guard that goes red, the exported evaluator that decides it, and what the bundle LOOKS like when the override has stopped applying. `unverifiedOverrides()` is the rule — a function over the table, because "every row that claims to have changed something" is a property of the data — and the suite reads `scripts/check-bundle-smoke.ts` for the call site and `lib/` for the export, so a rename is a red run rather than a paragraph that quietly became false.

  **And 115 was written twice.** The charset failure message spelled "~115 KiB" beside a table where the same number is a measurement taken on a dated build; re-measuring one would have left the other lying with nobody editing it. `auditedDeltaKiB("output.ascii_only")` is the one copy now, and `null` — for an option with no size answer, or a row that no longer exists — reads as a message with no number rather than a thrown error inside a failure formatter.

  4 new cases (14 in the audit suite, 135 in bundle-smoke), 9594 in the suite. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size, bundle smoke (30038 characters as UTF-8, 50 as escapes), ships-to-client. No DB migration, no creds.

## The table that could be incomplete, and the paste it would hide (2026-09-21)

<!-- First bullet of "After the shared-floor naming" (2026-09-21): "`SHARED_FLOOR_CONSTANTS` is a table nothing forces to be complete — a fourth entry could take `RUNTIME_CODE_WALK_FLOOR` and be found (membership is derived), but a NEW constant extracted the same way joins by somebody remembering to add it here." -->

- [DONE: 2026-09-21] [est: 9000] **Membership is derived by VALUE, so three entries with 174 pasted into them look exactly like three entries taking the constant** — which is the whole failure the naming was added to prevent, and the one thing the derivation cannot see. `sharedFloors()` reads `SCANNED_FLOORS` for entries whose `minimum` equals a declared constant over the same walk; a literal equals it just as well. The tool would keep saying "edit the constant" about rows that had already left it.

  **The spelling is where it is visible, so the case reads the spelling.** It slices the `SCANNED_FLOORS` literal out of `lib/scanned-floor.ts`, collects every `minimum:` whose right-hand side is an identifier, and asserts two directions: no entry takes a constant `SHARED_FLOOR_CONSTANTS` does not name, and every declared constant is spelled by exactly as many entries as the derivation counts as its members. A pasted literal breaks the second — checked by making the paste and watching the case go red, rather than by assuming it would.

  **The anchor needed a colon.** `SCANNED_FLOORS_ENTRY_SUBJECT` is declared earlier in the same file and `indexOf("export const SCANNED_FLOORS")` finds it first, which made the first run fail on its own non-vacuity assertion — the one that exists because a scan over the wrong slice reports clean. That is the assertion doing its job before the case had ever seen a real entry.

  1 new case (44 in the floor-walks suite), 9590 in the suite. 0 new keys, 0 bundle cost. `npm run lint:ci` green: typecheck, 23/23 guards, 9590 tests. No DB migration, no creds.

## The green check that meant "queued" and said "build" (2026-09-21)

<!-- Fifth bullet of "After the shared walk floor" (2026-09-19), and the same bullet in the two lists before it: "The EAS job reports success for queueing, and the job should say so — `--no-wait` is a well-argued choice, but the green check means 'a build was queued' and reads as 'the native app builds'. One line in the job summary naming the Expo dashboard removes a false signal that has been up for months." -->

- [DONE: 2026-09-21] [est: 28000] **A check named "EAS iOS preview build" has been going green for having queued one** — `--no-wait` is the right trade and nothing here argues with it: waiting would hold a runner for a twenty-minute native build whose result nothing in this repository reads. What it leaves behind is a green check making a claim about building that nobody measured, on the one target `npm run verify` does not cover — and `npm run bundle:native` currently fails, so the optimistic reading is not merely unsupported, it is wrong.

  **The summary says which of the two it is, in the place a reader lands.** "A build was QUEUED. It has not been built" — then why the check is green (EAS accepted the job), and where the outcome actually is. The build page EAS prints is captured out of the CLI output and linked; when the output format changes the dashboard is named instead, so a changed format costs a convenience rather than the whole signal.

  **Three paths, not one.** `if: always()`, because the runs whose colour is hardest to read are the ones with no build behind them: a green from a skipped `EXPO_TOKEN` probe now says nothing was queued and no minutes were spent, and a red from a refused queue says the step log is the whole story because nothing reached the dashboard. A summary written only after a successful queue would be missing from exactly those two.

  **The doc makes the same point for the other audience.** The summary is read by whoever opens the run; README-DEPLOY.md is read by whoever is deciding whether the native target is covered by CI. Both had been given the same green to misread.

  4 new cases (11 in the workflow suite), 9589 in the suite. 0 new keys, 0 bundle cost — the workflow ships to nobody. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds — `EXPO_TOKEN` is read from the repository secret, as before.

## The suggestion that would have undone the constant (2026-09-21)

<!-- First bullet of "After the shared walk floor" (2026-09-19): "`npm run remeasure-floors` now prints a number three entries no longer spell — the tool suggests a `minimum` per walk, and for the three on `RUNTIME_CODE_DIRS` the edit it implies is to one constant rather than to three entries. Nothing in its output says so, and the next person to run it will paste 174 back into a row." -->

- [DONE: 2026-09-21] [est: 42000] **The tool's advice pointed at the shape the constant had just removed** — `RUNTIME_CODE_WALK_FLOOR` took 174 out of three entries two days earlier. `remeasure-floors` walks a floor at a time, so today's tree (315 files against 174, 45% deletable) produced the drift note three times, each ending "Re-measure to 236 when convenient" with no hint that the three numbers are one number. A reader following it lands on three `minimum:` fields, and the obvious edit — paste 236 into each — un-shares the constant one row at a time.

  **`SHARED_FLOOR_CONSTANTS` names the value and the walk; membership is derived.** Roots are half the key deliberately: an unrelated floor measured at the same number over a different walk is a coincidence, and telling its reader to edit a constant their entry does not mention is worse than saying nothing. `check-console-swap` — `RUNTIME_CODE_DIRS` plus `scripts/` — is the live example of a near-miss that stays out. Members are read back out of `SCANNED_FLOORS` by `sharedFloorFor()`, the way `floorWalks()` already derives the walk list, so an entry joins by taking the constant and there is no second list to drift.

  **Two places say it, and only where a number is actually suggested.** Each row that prints a suggestion now names the constant, the rows that move with it, and the instruction that keeps the consolidation: edit the constant, because pasting the number here moves one row and un-shares all 3. The run summary counts EDITS beside its count of rows — "check-inline-hex, check-latest-ref and check-reduced-motion take their number from RUNTIME_CODE_WALK_FLOOR in lib/scanned-floor.ts — that is 3 row(s) and one edit." A healthy row says nothing: a constant named beside a row nobody is being asked to edit is the line that gets skipped on the run where it matters.

  **The join helper was written without a length test, which the plural sweep found first.** `joinNames` began as `if (names.length <= 1) return names[0] ?? ""` and `__tests__/plural.test.ts` went red — it sweeps `lib/` for modules deciding something by comparing a count against 1, and the fix an exemption list would have bought is a name in a list. Slicing does the branching instead: an empty head means there was nothing before the last name, and the sweep has nothing to see.

  12 new cases (43 in the floor-walks suite), 9585 in the suite. 0 new keys, 0 bundle cost — the tool ships to nobody. `npm run verify` green: typecheck, 23/23 guards, tests, audit baseline, build, bundle secrets, bundle size (23.8 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## One number instead of three, and the two fixtures that noticed (2026-09-19)

<!-- First bullet of "After the runtime-roots constant" (2026-09-19): "Three guards now share a walk and three `SCANNED_FLOORS` notes each explain the same alignment to the reader… The roots are one statement now; the NUMBER is still three." -->

- [DONE: 2026-09-19] [est: 13000] **The roots were consolidated an hour earlier and the number was not** — `check-inline-hex`, `check-reduced-motion` and `check-latest-ref` each carried `minimum: 174`, and each carried a sentence in its `note` explaining to the reader why it matched the other two. Three copies of a number, plus three defences of the copying. `RUNTIME_CODE_WALK_FLOOR` is that argument stated once.

  **What it is NOT, said in the constant's own header.** Not a bound on how much code this tree may hold and not a number anybody should feel pressure from. The property that actually matters — a walk that came back healthy-looking with `app/` missing — is carried by each entry's `roots` and asserted directly, so this number's only remaining job is the walk that came back implausibly small with every root present: a narrowed glob, a broken extension filter, a widened skip list. At 174 against 311 files it is loose, deliberately: tightening it would mean re-taking it every time the tree grows, for a property that does not need a tight number.

  **Two fixtures caught the change, both correctly, and one of them had been caught by this before.** `lint-guard-invalid-floor` and `guard-fixture-refusals` both rewrite `minimum: <number>` to prove a guard refuses an unusable premise — and the right-hand side stopped being a number. The first had already broken once when a floor was re-measured, and had been taught to read the current value out of the table; that is why it broke a second time, because the new spelling is not a value at all. Both match a TOKEN now, which survives either. The failure was loud rather than silent only because one of them asserts that the rewrite changed something: a patch that matches nothing produces a perfectly valid entry and a case that fails claiming the guard would not refuse, which is a puzzling failure rather than a named one.

  **And a suggestion from the same list, checked and dropped.** "`check-console-swap` is `RUNTIME_CODE_DIRS` plus `scripts/` and could say so as a set" cannot be built: `__tests__/helpers/guard-scan-list.ts` parses each guard's `SCANNED_DIRS` as a LITERAL array, deliberately, and its header already records why — a `[...SOURCE_DIRS]` spread would satisfy the one-copy rule while defeating the pin that keeps every guard's scan list readable from its own file. The suggestion was a one-line refactor wearing the clothes of a design this repository has already decided against.

## The third rule to write out the same narrowing (2026-09-19)

<!-- First bullet of "After the latest-ref guard" (2026-09-19): "Two guards arrived today and both narrowed to `app` + `components` + `lib` with the same floor of 174 — `check-inline-hex` makes three… the roots that hold code that runs are a fact about the tree, and three rules have now stated it separately." -->

- [DONE: 2026-09-19] [est: 12000] **Three rules, three copies of one fact** — `check-inline-hex` (a colour literal is a token decision only where something is drawn), `check-reduced-motion` (an animation has to consult the setting only where it can run) and `check-latest-ref` (a per-render ref sync needs a render) all narrow to `app` + `components` + `lib`, and all three wrote the narrowing out. The second and third arrived within hours of each other this morning, which is the only reason it was visible: one copy is a choice, two is a coincidence, three is a fact about the tree that nowhere states.

  **`RUNTIME_CODE_DIRS`, named for what it MEANS.** The same move `MARKUP_DIRS` made one level narrower, and the naming argument is the one that file already records: "the roots the three code rules scan" is a list to re-justify every time one of them changes, and "the roots that hold code that runs" is a fact a rule wanting something else has to argue against.

  **`NON_RUNTIME_REASONS` ships in the same commit, which `MARKUP_DIRS` did not manage.** That constant landed as a narrowing with no statement of what it excluded and needed a follow-up commit to say so; this one is the pair from the start. And `scripts/` is the entry that has to be said carefully — it RUNS, on a developer's machine and in CI, so a rule about "code that runs" catches it on a careless reading. What these three rules are about is code on a USER's device, which is Metro's decision, and Metro never resolves `scripts/`.

  **The guards keep their own literal `SCANNED_DIRS`.** `guard-scan-dirs.test.ts` parses it out of each wrapper precisely so a guard's scope is readable from the guard's own file, and the suite asserts the three lists EQUAL the constant. One statement plus a checked agreement, rather than three lists that happen to match — which is what they were doing until this morning.

  **The claims about the constant live beside the claims about its sibling, not in a second home.** The partition (every source root is runtime or has a sentence), the minimum-length rule on each sentence, the non-empty check on each root, and the nesting — `MARKUP_DIRS` ⊆ `RUNTIME_CODE_DIRS` ⊆ `SOURCE_DIRS` — all went into `source-files-helper.test.ts` next to the markup versions of the same four. The first draft put them in the guard suite, where they would have been a second place to look for one kind of question.

## The rule that was a test case (2026-09-19)

<!-- First bullet of "After the latest-ref consolidation" (2026-09-19): "The no-render-body-assignment rule is a test case and could be a lint guard — it lives in `use-latest-ref.test.ts` and walks `app/` + `components/` + `lib/` exactly as `check-inline-hex` does." -->

- [DONE: 2026-09-19] [est: 14000] **A rule about a shipped-code idiom, enforced only by a suite** — the sweep that found the eleven hand-written ref syncs stayed where it was written, inside the hook's own test file. That is enforcement (it runs in `npm test`, a gate leg) and it is the wrong shape: the twelfth copy would be reported by a suite about `useLatestRef`, under a case name nobody grepping for a lint failure would find, with no annotation on the diff that introduced it. `lint:latest-ref` is the 23rd guard, beside the 22nd, which arrived this morning.

  **The narrowness IS the design, and it took six negative cases to say so.** Ordinary ref usage is almost the same text: `timerRef.current = null` is a reset, `timerRef.current = setTimeout(...)` stores a handle, `mountedRef.current = false` is a lifecycle flag — `app/chat/[id].tsx` alone has six, every one of them correct. So the name must be the SAME identifier either side, with the `Ref` suffix carried by a backreference rather than by convention. And the statement must sit at exactly two spaces: four or more means inside an effect, a handler or a branch, which is where `use-transition-event` writes its own `prevRef` correctly and where a rule that argued would simply be wrong.

  **Indentation is a weak signal and it is the honest one available.** Deciding "is this statement in the component body" properly means parsing the function, and this repository has `check-jsx-walk` precisely because of what it thinks of hand-rolled parsers that are almost right. The two-space rule under-matches — a component nested inside another function is missed — and under-matching is the direction a style rule should fail in.

  **The half it refuses to attempt is the more interesting one.** The other side of `useLatestRef`'s contract is that the ref must not be READ during render, which is a real React violation rather than a preference, and the hook makes that mistake one line shorter to write. Telling a render-body `.current` read from a callback's is a question about scope, not about text, and the shapes that separate them — an arrow in a prop, a function declared and called later, a `useMemo` body — are indistinguishable to a scan. A rule catching nine of ten would be worse than none, because the tenth would read as cleared. A case asserts the module does not grow one quietly.

  **No subject floor, and the absence is reasoned rather than forgotten.** `check-reduced-motion` refuses a run that matched no animation, because its rule is about things that exist. This one's healthy state is zero findings and there is nothing in the tree for it to count, so the equivalent hazard — the pattern silently ceasing to match after a formatter change or a rename — is demonstrated against fixtures in its suite instead. That is the only place a negative rule can show it still recognises its subject, and its floor entry says so.

## Eleven copies of one assignment (2026-09-19)

<!-- "Three refs in one component now exist for one hazard", carried on two suggestion lists: "`activeRef`, `tabsRef`, `onChangeRef`, `translateRef` and `useReducedMotionRef` are all in `components/swipe-tabs.tsx` for the same reason… A `useLatestRef(value)` hook is four lines; the alternative worth pricing first is rebuilding the responder." -->

- [DONE: 2026-09-19] [est: 17000] **`const ref = useRef(x); ref.current = x;` in eight files, for three different reasons** — a `PanResponder` built once inside `useRef(...).current` and never rebuilt (`swipe-tabs`, four of them); a callback whose identity changes every render and must not restart a timer (`toast-host`, `use-transition-event`, `use-visibility-refresh`, `use-entry-currency`, `use-dwell-time`); and render state read by a handler that outlives the render (`app/index.tsx`, `app/collection/[id].tsx`). Three sentences, one mechanism, and each file arguing for it in its own words.

  **The second line is the whole hook, and it is the line that gets forgotten.** Not because it is hard but because it reads as bookkeeping and sits below the declaration it belongs to. The failure when it is missing is the reason this is worth a hook rather than a comment: nothing crashes, the ref simply keeps the FIRST value, and the component is correct until the value changes — which in four of these sites is a language switch, a second toast or a reorder, none of which the first render can see. That is a bug that ships.

  **The one site that was already right is what suggested the other ten.** `useReducedMotionRef` was written two days earlier with the assignment INSIDE it, and its header said why: "rather than by a line each caller has to remember". It is now `useLatestRef(useReducedMotion())` — the signal plus the hook, with nothing of its own left.

  **The sweep found an eleventh site the suggestion's grep had missed.** `lib/use-dwell-time.ts` was below a `head -30`, which is how a hand-run survey misses one. The case that found it stays as the floor: no `xRef.current = x` at the top level of a component body anywhere under `app/`, `components/` or `lib/`, with the name on both sides required to match so an assignment inside an effect or a handler — a different thing — is not caught.

  **Mutable on purpose, which is one deliberate concession.** `use-transition-event` keeps a second ref it writes from inside its own effect; a `readonly` return type here would push that one caller back to a hand-rolled `useRef`, and one holdout is exactly how an idiom stays two idioms.

  **The behaviour of a one-line hook is RUN rather than read.** The load-bearing property is that the ref object is the same one across renders — a hook returning a fresh object every render would satisfy every naive case and none of the actual call sites, because the `PanResponder` holds the object it was handed on render one. So: identity pinned across three renders, a first-render capture asserted to see the third render's value, `undefined` and `null` carried as values rather than treated as absent, and the mutability exercised. Five source-text assertions in other suites moved to the new spelling; one of them had been asserting the assignment line itself, which no longer exists anywhere.

## The native bundles nothing had ever built (2026-09-19)

<!-- "Put a size line on the native bundles", carried on four suggestion lists in a row, most recently: "`metro.config.js` is shared, so iOS and Android got the same 115 KiB back and nothing measured it. The EAS preview build already runs in CI; a line reporting its bundle size would make the native output as visible as the web one." -->

- [DONE: 2026-09-19] [est: 19000] **The job every list pointed at cannot supply the number** — `eas build --platform ios --no-wait` QUEUES a build on Expo's infrastructure and exits, which is what `--no-wait` is for; the workflow then reports success for having queued something. Four lists in a row proposed reading a size off it. So `npm run bundle:native` exports the platform locally and measures what comes out, which is the only way this repository can answer the question at all.

  **And the first run of it failed, for a reason nothing here could have seen.** A SECOND COPY of react-native — 0.86.0 — sits at `node_modules/react-native/node_modules/react-native`, put there by npm to satisfy `@react-native/virtualized-lists`' `react-native: *` peer, which it resolved to the newest published version instead of deduping to the 0.81.5 already at the root. Metro resolves a private component (`VirtualViewExperimentalNativeComponent`) out of the nested copy and the root copy's codegen cannot parse it. Confirmed both directions: move the nested directory aside and the export succeeds at 7643.0 KiB of Hermes bytecode; put it back and it fails again.

  **The web build never resolves that file, which is why every signal in this repository says fine.** `npm run verify` is green, all nine CI legs are green, and the EAS job is green for having queued a remote build whose outcome nothing here reads. Three green lights over a platform that does not build. That is not a gap a guard could have caught, because no check in this tree had ever asked Metro for a native bundle — the failure was not hiding from the checks, it was outside the set of things any of them look at.

  **The diagnosis is the report's real output.** Metro's own message names a component nobody working on this app has heard of and points at a path six directories deep; that is precisely why this went unexamined. `diagnoseNativeExportFailure` requires BOTH halves of the signature — a nested react-native path AND a codegen parse error — and returns nothing for anything else, because a diagnosis offered for every failure is one that will eventually be attached confidently to a failure it is wrong about.

  **A report, not a tenth gate leg, and the distinction is the point.** `npm run verify` chains its nine steps fail-fast, so a leg that cannot pass today would turn every commit red for a dependency-tree problem no commit caused. `bundle:composition` is the standing precedent: gates nothing, run when a question comes up. This one is run when the question is "how big is the native bundle", and today it answers "there isn't one, and here is exactly why".

  **The fix is left to a human on purpose.** `overrides` — both the plain form and the nested `@react-native/virtualized-lists` form — did not dislodge the copy; npm kept the resolution from the existing lock. Forcing it would mean hand-editing `package-lock.json` and shipping a dependency tree whose only real verification is an EAS build this session cannot run. Writing down a reproduction that takes one command is worth more than a lockfile edit nobody can check.

## Four settings nobody had opened, opened (2026-09-19)

<!-- "Audit the rest of Metro's transformer config the same way", carried on the suggestion lists of 2026-09-17 (twice) and 2026-09-19: "`reduce_funcs: false`, `mangle.toplevel: false` and `toplevel: false` are three more that were chosen by somebody else for some other app. Each is worth one build to measure, and the answer may well be that they are all correct." -->

- [DONE: 2026-09-19] [est: 16000] **The answer is that Metro is right about all of them, which is worth knowing once rather than never** — `ascii_only` was one option in a preset with no reader and it was costing 3% of the bundle, which is what made the rest of that preset a fair question. One build each, against a 3,718,641-byte bundle: `compress.reduce_funcs: true` saves 33 bytes, `mangle.toplevel: true` saves nothing, `toplevel: true` saves nothing. Nothing in `metro.config.js` changes.

  **The two zeroes are structural, and that is the whole finding.** Not "nearly nothing" — ZERO, byte for byte across all seven chunks, which is not how a conservative minifier behaves and is not luck. Metro wraps every module in a `__d(function (global, require, …) { … })` factory, so a Metro bundle has no meaningful top level: every name is already function-scoped and already mangled. Whatever those two options are for, this bundle SHAPE cannot reach it, and that is a better answer than a number because it will still be true after the next dependency bump.

  **33 bytes is 0.0009%, and React Native turns that option off on purpose.** `reduce_funcs` inlines single-use functions, which moves work off the wire and onto startup — a cost that lands where nothing in this repository measures. The interesting part is not the risk assessment: it is that the number is small enough that nobody has to do one.

  **A result like this is re-derived every few months unless it is written down.** So it is, twice over: `lib/minifier-audit.ts` holds the table (path, the value Metro sends, the measured delta, why the current value is right) and `metro.config.js` holds the summary, because the next person to ask this question will open the config and not a lib module. The suggestion list asked it three days running, which is what a finding with nowhere to live looks like.

  **And the premise gets a check, which is the part that will still be earning its keep in a year.** A measurement is only as good as the config it was taken against, and a dependency bump can move a default silently: the bundle stays correct, every guard stays green, and those paragraphs become fiction with nothing to say so. That is the same failure mode that put `evaluateBundleCharset` inside `check-bundle-smoke` rather than leaving it as a comment. The suite loads the preset through `createRequire` anchored at `metro.config.js` — the build's resolution, not the suite's — and compares it in BOTH directions: a changed value is named with the date its number is now stale against, and a setting the toolchain ADDS is reported as unpriced. The second direction is the one a table of known options cannot see by itself, and it is exactly how `ascii_only` survived unexamined for as long as it did.

## The tab change nothing said out loud (2026-09-19)

<!-- First bullet of "After the reduce-motion scanner" (2026-09-19), and of the three lists before it: "Say the tab change out loud, now that the pager may not show it" — carried four times, which is itself the finding. -->

- [DONE: 2026-09-19] [est: 18000] **On native there are no tab buttons at all** — `components/swipe-tabs.tsx` renders a `<Text>` and a row of dots for its header, and the swipe is the only way to change tab. So a commit produced no spoken feedback of any kind: no role, no selected state, no sentence. On WEB the same component renders `<Pressable>`s carrying `accessibilityState={{ selected }}`, which the platform announces on press — which is why the gap was invisible to anyone testing in a browser, and why the fix could not go where it first looked like it belonged.

  **The sentence belongs to the GESTURE, not to `commitTo`.** Both routes into a tab change pass through that function, and a version wired into it would say the same thing twice on web and nothing on the native route that has no buttons. `spoken` defaults to FALSE and only the two pan-release call sites opt in, rather than the other way round: a third caller added later is quiet until somebody decides it should not be. And it fires AFTER `onChangeRef.current(targetKey)` inside the completion callback, because the `!finished` branch immediately above returns without changing anything — a sentence spoken for a gesture the pager then abandoned is a lie the listener has no way to check.

  **Not conditional on reduced motion, though that is what raised it.** The suggestion framed this as a reduced-motion gap: under the setting the commit is instantaneous, so the only sign a swipe did anything is the content changing. True, and the narrower half. A screen-reader user perceives the 220ms slide exactly as well as they perceive its absence, which is not at all, so the sentence is owed on every swipe commit — a version firing only under the setting would be a feature for the smaller group.

  **`announcedPosition` gets a second caller rather than a second derivation.** "Position N of M, or nothing" is one rule, already written for the reorder announcements with six cases behind it, and re-deriving it here would have been a second set of edge cases to get right. Both halves of the sentence are required: a blank label says ", tab 2 of 3" — a position attached to no subject — and a position out of range says a number that is simply wrong. A `findIndex` miss resolves to `-1`, which is exactly that case, and it is refused rather than read aloud.

  **The translator is read through a ref, like everything else the PanResponder touches.** The responder is built inside `useRef(PanResponder.create(...)).current`, which runs once, so a `translate` captured in that closure is the one from the first render for the life of the component — the announcement would keep speaking the language the app was STARTED in after the user changed it. That is the same hazard `useReducedMotionRef` was written for two days earlier, in the same file, and the third ref in a row this component has needed for it.

  **Two modules, because `@/lib/announce` is a platform pair.** It pulls react-native (the announcement API is an empty function on web, so the web half builds an ARIA live region), and anything importing it is unreachable from a node suite. So `lib/tab-sentence.ts` holds the DECISION and `lib/tab-announcement.ts` holds the saying of it — the same split `lib/drag-reorder.ts` and `lib/reorder-announcement.ts` already have, for the same reason: the interesting cases here are the ones that produce nothing, and a function whose only effect is a side effect can be tested only by watching for the absence of one. The case that pins the split asserts on the IMPORTS rather than the text, because the module's own header explains the split by naming the module it must not pull.

## The four filenames that were holding the line (2026-09-19)

<!-- Last bullet of "After banking the saving" (2026-09-17): "Promote the animated-surface list into a real scanner" — the reduce-motion sweep's last case names four files by hand, so a fifth animated surface joins unchecked. -->

- [DONE: 2026-09-19] [est: 20000] **A sweep's floor was a list of four filenames somebody typed** — the toast, the skeleton, the pager and the wishlist sheet were taught to consult `AccessibilityInfo.isReduceMotionEnabled` two days earlier, and what stopped a fifth surface from joining without asking was an array in a test: green on the day it was written and silent about every file added after it. The setting is not a preference about taste — vestibular disorders are why it exists — so "nobody remembered" is the failure mode worth spending the 22nd guard on. `check-reduced-motion` walks `app/` + `components/` + `lib/` and refuses any `Animated.timing/spring/decay/loop` without `@/lib/reduced-motion` in scope.

  **The rule is the IMPORT, and that is a decision rather than a shortcut.** There are three right answers and the module's header is about which is which: a `motionDuration` of zero for a transition whose completion callback commits state, a `setValue` for a spring with nothing waiting on it, an early return for a loop. A text scan cannot tell which one a surface needed, and a guard that guessed would be wrong on two thirds of this tree. What it CAN say is that the author was handed the question — which is the claim that catches the surface written by somebody who has never opened the header.

  **Three narrowings, each with a case behind it.** Comments are blanked first, and this rule is the reason that matters: its own header names three of the four drivers, `lib/reduced-motion.ts` names two, and `components/skeleton.tsx` quotes `Animated.loop` to explain its early return — a rule that read prose would report every file that documents itself. The combinators are OUT (`sequence`, `parallel`, `stagger` only arrange drivers, so a file holding one holds a driver too) and `loop` is IN despite being one, because it has its own answer. And `lib/` is in the walk rather than `MARKUP_DIRS`, because twelve context providers live there and render JSX — "animation lives where the markup does" is a sentence this tree has already falsified once.

  **A second premise, beside the count floor.** `assertScannedWalk` proves files were read; nothing proved the rule still RECOGNISES an animation. Every pattern here is a spelling of one library's API, and the day this app moves to Reanimated's `withTiming` all of them stop matching at once: a clean report over a tree full of unguarded motion, which is exactly the failure every floor in this repository exists to catch. One animated file is the floor — the number of animated surfaces is a product decision and the guard is not the file that should have an opinion about it.

  **The repository's own meta-guards named the four registries the guard had to join, in one run.** `guard-registration.test.ts` reported all three gaps at once — a partial-root fixture, a scan-dirs exclusion table, a planted offender — rather than one per run, which is the shape it was built for and the first time it has been exercised by a guard arriving after it. The planted offender is the one entry in that table written LITERALLY rather than assembled: `__tests__/` is the source root this rule deliberately does not walk, and a fixture has to be able to spell a driver out.

  **And the bug that fell out of deriving the list.** `components/skeleton.tsx` returns early under the setting and its cleanup is `loop.stop()`, which freezes the animated value wherever it had got to. Turning reduce-motion on MID-SWEEP is not the exotic case — it is the ordinary one, since it is turned on BECAUSE something animated — and it parked the highlight band across the middle of the box, permanently, on the surface that was supposed to go quiet. The comment above it promised "the box keeps its base colour and sits still"; `anim.setValue(0)` is what makes that sentence true, and it is the `setValue` shape rather than a timing back to zero because nothing is waiting on it.
