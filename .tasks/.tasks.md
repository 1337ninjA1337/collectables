- [DONE] [est: 22000] there is still same error that says Supabase needs to be connected Add EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY, then enable Email, Google, and Apple in Auth settings.
- (decomposed from the original [~200k tokens] cross-device chat task)
- [DONE] [est: 18000] chat-cloud-1: add Supabase migration for `chat_messages` table (id uuid pk, chat_id text, from_user_id uuid, to_user_id uuid, text text, created_at timestamptz default now()) with indexes on (chat_id, created_at) and (to_user_id, created_at); enable RLS; add SELECT policy "either participant can read" and INSERT policy "sender must equal auth.uid() AND must be a friend of recipient (lookup against existing friends table)". Document policy SQL inside `supabase/migrations/`.
- chat-cloud-2: add `lib/supabase-chat.ts` with REST helpers (`fetchMessagesForChat`, `sendMessage`, `markRead`) that talk to the new table via the existing `supabaseRest` pattern in `supabase-profiles.ts`. Falls back to no-op when `!isSupabaseConfigured` so local-only mode keeps working.
- chat-cloud-3: refactor `lib/chat-context.tsx` to load+send through `supabase-chat.ts` first, mirror writes into AsyncStorage as a local cache, and merge cloud results on top of cached results so offline reads still work. Keep the `collectables-chats-v1-{userId}` cache key.
- chat-cloud-4: subscribe to a Supabase realtime channel filtered to `to_user_id = current user` inside `chat-context.tsx`, push incoming messages into state immediately, and unsubscribe on sign-out / unmount.
- chat-cloud-5: add a "typing..." indicator using a Supabase Presence channel keyed by `chat_id`; broadcast on `onChangeText` (debounced 1s) and clear on send; render below the message list in `app/chat/[id].tsx`.
- chat-cloud-6: add an unread-message badge on the bottom-nav friends tab driven by chat-context state (count of cloud messages where `to_user_id = me` AND `created_at > last_seen_at`); update `last_seen_at` whenever the user opens that chat screen.
- chat-cloud-7: add tests for `supabase-chat.ts` request shaping (URL, headers, body), the friend-check helper, and the unread-counter selector.

- implement marketplace {
    - add page for marketplace (also it should be added into navbar)
    - add possibillity to add items into marketpalce (item on marketplace should show info about it and owner)
    - items added to market can be traded or sold (sold outside of app)
    - add possibillity to send messages to owner of item that found on marketplace (chat will open to both persons who owns item and who wants to buy/trade it)
    - items that added into market should include price history of same items (for now check just items name if it same more than 90% that show its price history)
    - users can add only 1 item on market untill they don't have a premium account that will be added later
}
- add possibility to upgrade account to a premium status
