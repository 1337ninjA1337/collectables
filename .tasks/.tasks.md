
## Crash tracking — Sentry (free tier: 5k errors/mo, single-user org)

[DONE: 2026-05-07] [est: 1200] - Crash #1 (deps + env) — Installed `@sentry/react-native@~7.5.0` into `dependencies`. Added `EXPO_PUBLIC_SENTRY_DSN` + `EXPO_PUBLIC_SENTRY_ENV` rows to the GitHub Actions secrets table in `README-DEPLOY.md` and to the EAS `eas secret:create` block in `APPSTORE-SUBMISSION.md`. Updated the section-12 "Crash reporting" line to reflect the SDK now ships in deps. Skipped `.env.example` because it is intentionally gitignored (line 35) per the CLAUDE.md GitHub-Secrets-first policy. Added structural test `__tests__/sentry-env-docs.test.ts` asserting (a) `package.json` lists the SDK, (b) both env vars appear in README-DEPLOY.md, (c) both env vars appear in APPSTORE-SUBMISSION.md, (d) the SDK package name is referenced as the crash reporter.

- Crash #2 (config helper) — Create `lib/sentry-config.ts` (pure, no react-native import) exporting `resolveSentryConfig(env: Record<string, string | undefined>)` returning `{ dsn, environment, release, enabled }`. `enabled` is `false` when DSN is empty or env equals `development` so dev builds never report against prod quotas. Mirror the shape-helper pattern of `lib/cloudinary-config.ts`. Add unit tests covering: missing DSN → `enabled: false`, dev environment override, release derived from `EXPO_PUBLIC_APP_VERSION` fallback to `app.json` version.

- Crash #3 (init module) — Create `lib/sentry.ts` exporting `initSentry()` that lazy-imports `@sentry/react-native`, calls `Sentry.init({ dsn, environment, tracesSampleRate: 0.1, enableNative: true })` only when `resolveSentryConfig(process.env).enabled === true`, and exports `captureException(err, ctx?)` + `addBreadcrumb(msg, data?)` thin wrappers. Both wrappers must noop when SDK isn't initialised so call sites stay safe in tests. Structural test asserts both wrappers are exported and check the `enabled` flag before delegating.

- Crash #4 (provider wiring) — Call `initSentry()` once at the top of `app/_layout.tsx`, before the provider tree mounts. Wrap the root `<Stack>` in `Sentry.ErrorBoundary` (with a `<EmptyState>`-styled fallback that mirrors the existing 🪧 not-found screen). Structural test on `app/_layout.tsx` asserts the `initSentry` import + `ErrorBoundary` wrapping pattern.

- Crash #5 (user context) — Inside `lib/auth-context.tsx`, after a successful sign-in / OTP verify, call `Sentry.setUser({ id: session.user.id, email: session.user.email })`. On `signOut()`, call `Sentry.setUser(null)` so logged-out crashes don't carry stale identity. Skip when `enabled === false`. Add a unit test that mocks `Sentry.setUser` and asserts it's called with the expected shape on the auth state transition.

- Crash #6 (navigation breadcrumbs) — Hook `expo-router`'s `usePathname()` in a `<NavigationBreadcrumbs />` component mounted under the providers. On every pathname change, call `addBreadcrumb({ category: "navigation", message: pathname, level: "info" })`. Closes the gap where Sentry currently only sees the entry-point route on a crash.

- Crash #7 (manual capture in catch blocks) — Replace the silent `catch { /* ignore */ }` blocks in the cloud wrappers (`lib/supabase-chat.ts`, `lib/supabase-marketplace.ts`, `lib/supabase-profiles.ts`, `lib/cloudinary.ts`) with `captureException(err, { context: "supabase-chat.send" })`. Keep the silent fallback behaviour for the user; only forward the error to Sentry. List of touch points should be enumerated in the task body before implementation to avoid scope creep.

- Crash #8 (web sourcemaps in CI) — Update `.github/workflows/deploy.yml` to upload web sourcemaps to Sentry after `expo export`. Use the official `getsentry/action-release@v1` step gated on `secrets.SENTRY_AUTH_TOKEN`. Without sourcemaps the bundled JS stack traces are unreadable. Add `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT` to the GitHub secrets list in `README-DEPLOY.md`. Add to `MANUAL-TASKS.md`: "Generate a Sentry auth token at Settings → Auth Tokens (project:releases scope), copy org/project slug from the URL."

- Crash #9 (native sourcemaps for iOS/EAS) — Add `expo.extra.sentry.organization` / `expo.extra.sentry.project` to `app.json` and the Sentry config plugin entry to `expo.plugins`. EAS Build will then upload native + Hermes sourcemaps automatically. Document the matching `eas secret:create` calls (mirror Crash #8) in `APPSTORE-SUBMISSION.md`. No code changes inside `lib/` — pure config.

- Crash #10 (release tagging) — Have CI export `SENTRY_RELEASE=collectables@${{ github.sha }}` before `expo export` so every web bundle is associated with its commit. Pass the same value into `Sentry.init({ release })` via `EXPO_PUBLIC_SENTRY_RELEASE`. Allows clicking from a Sentry issue back to the deploy commit.

- Crash #11 (rate-limit guard) — Wrap `captureException` in `lib/sentry.ts` with a per-process rate limiter (max 50 events/min) so a runaway `useEffect` loop cannot exhaust the 5k/month free quota in seconds. Add a `__resetSentryRateLimitForTests` escape hatch and unit-test the throttle behaviour against a mocked `Sentry.captureException`.

- Crash #12 (PII scrubber) — Add a `beforeSend` hook in `Sentry.init` that strips email addresses from `event.user.email` outside dev environments and removes `event.request.cookies`. Mirrors the App Privacy declaration in `APPSTORE-SUBMISSION.md` (we don't share PII with third parties beyond what the user logs into). Unit test the scrubber with a fixture event.

- Crash #13 (web error boundary fallback UI) — Render a localised "Something went wrong" screen (using the existing `t()` pipeline) when Sentry's `ErrorBoundary` catches. Add 6 translations (`crashFallbackTitle`, `crashFallbackBody`, `crashFallbackRetry`) across all language maps. The fallback's "Retry" button calls `resetError()` provided by Sentry. Includes a parity test with the existing i18n key audit.

- Crash #14 (privacy policy update) — Update the App Privacy section of `APPSTORE-SUBMISSION.md` to mention Sentry under "Crash data / diagnostics" with `Yes / Linked to user / Not used for tracking`. Generate a one-paragraph addition for the public privacy policy noting Sentry as a sub-processor with a link to https://sentry.io/privacy/.

- Crash #15 (uninstall path) — Add a `disableSentry()` helper that calls `Sentry.close()` and a corresponding settings toggle in `app/settings.tsx` ("Diagnostics & crash reports" with default ON). Persist the user's choice in AsyncStorage under `collectables-diagnostics-v1` and respect it in `initSentry()`. Required for GDPR jurisdictions and a common App Store reviewer ask.

- Crash #16 (test coverage) — Add `__tests__/sentry-init.test.ts`, `__tests__/sentry-rate-limit.test.ts`, `__tests__/sentry-scrubber.test.ts`, and a structural test asserting the `_layout.tsx` ErrorBoundary wiring. Each one mocks `@sentry/react-native` so the test suite stays peer-dep free.

## Analytics — PostHog (free 1M events/mo) + Power BI Desktop for reporting

- Analytics #1 (platform decision doc) — Add a short `docs/analytics-platform.md` documenting why PostHog is chosen (free 1M events/mo, EU host available, product-analytics primitives like funnels + retention) and how Microsoft Clarity (free, unlimited) is layered on top for session-replay only on web. Power BI Desktop (free) is the BI surface that reads from a Supabase Postgres view of the event table (no PostHog→PowerBI connector exists; we mirror events into Supabase via PostHog's webhook).

- Analytics #2 (deps + env) — Add `posthog-react-native` (and `posthog-js` for web) via `npx expo install`. Add `EXPO_PUBLIC_POSTHOG_KEY`, `EXPO_PUBLIC_POSTHOG_HOST` (default `https://eu.posthog.com`), `EXPO_PUBLIC_CLARITY_PROJECT_ID` to `.env.example`. Document each in `README-DEPLOY.md` and `APPSTORE-SUBMISSION.md` (EAS secrets section).

- Analytics #3 (config helper) — Create `lib/analytics-config.ts` with `resolveAnalyticsConfig(env)` returning `{ posthogKey, posthogHost, clarityId, enabled }`. `enabled === false` in dev or when keys are missing. Mirrors `lib/sentry-config.ts`. Unit-tested in `__tests__/analytics-config.test.ts`.

- Analytics #4 (init wrapper) — Create `lib/analytics.ts` exporting `initAnalytics()`, `trackEvent(name, props?)`, `identifyUser(userId, traits?)`, `resetUser()`. Lazy-imports the platform SDK; noops when `enabled === false`. The `trackEvent` signature accepts a typed union (`AnalyticsEventName`) defined in the same module so misspelled events fail at compile time. Structural test verifies the typed union covers every call site.

- Analytics #5 (event taxonomy) — Define the initial event list as TypeScript const literals in `lib/analytics-events.ts`: `signup_completed`, `collection_created`, `item_added`, `item_photo_attached`, `listing_created`, `listing_claimed`, `chat_opened`, `friend_requested`, `premium_activated`, `language_switched`. Each entry carries `description` + allowed `props`. Used by both the SDK wrapper and the Power BI schema doc.

- Analytics #6 (provider wiring) — Call `initAnalytics()` once at the top of `app/_layout.tsx` after `initSentry()`. Add a `<AnalyticsProvider>` in the provider tree that calls `identifyUser(session.user.id, { language, isPremium })` on auth changes and `resetUser()` on signout. Structural test for the provider order.

- Analytics #7 (auth flow) — Fire `signup_completed` from `lib/auth-context.tsx` when `verifyOtp` returns a freshly-created user (detected by `created_at` within last 5 min). Fire `language_switched` from `lib/i18n-context.tsx` on `setLanguage()`. Add unit-tests with a mocked `trackEvent`.

- Analytics #8 (collections / items) — Fire `collection_created` from `app/create-collection.tsx` after a successful save with `{ visibility, isPremium }`. Fire `item_added` from `app/create.tsx` with `{ collectionId, hasPhoto }`. Fire `item_photo_attached` when an existing item gets its first photo. Tests assert the event names + prop keys.

- Analytics #9 (marketplace) — Fire `listing_created` from `app/item/[id].tsx`'s post-listing path with `{ mode, hasPrice }`. Fire `listing_claimed` from `app/listing/[id].tsx` after the buy/trade flow with `{ mode, sellerWasFriend }`. Mirrors the new marketplace transfer feature.

- Analytics #10 (chat + social) — Fire `chat_opened` from `app/chat/[id].tsx` `useEffect` (with debounce so navigating in/out doesn't double-count). Fire `friend_requested` from the request-send action in `lib/social-context.tsx`. Fire `premium_activated` from the existing transition hook in `components/bottom-nav.tsx` (already detects the false→true transition for the toast).

- Analytics #11 (Microsoft Clarity for web) — Add a `<script>` tag injection (web-only) in `app/_layout.tsx` gated on `Platform.OS === "web"` and `clarityId` present. Clarity provides session replay + heatmaps for free with no event quota. Document the privacy implications (no PII in DOM, mask all `<input>`s) in `docs/analytics-platform.md`.

- Analytics #12 (Supabase event mirror) — Add `supabase/migrations/20260508_analytics_events.sql` with an `analytics_events(id, occurred_at, user_id, name, properties jsonb)` table + RLS policy allowing inserts from a Supabase Edge Function only. Add the migration command to `MANUAL-TASKS.md` per CLAUDE.md rules.

- Analytics #13 (PostHog → Supabase webhook) — Create `supabase/functions/analytics-mirror/index.ts` Edge Function that accepts PostHog's outgoing webhook payload, validates a shared secret header, and INSERTs into `analytics_events`. Configure the webhook in PostHog (manual step → `MANUAL-TASKS.md`). Lets us own the long-tail event store even if we churn off PostHog later.

- Analytics #14 (Power BI schema doc) — Add `docs/powerbi-connection.md` with: how to install the free Power BI Desktop, the Supabase Postgres connection string + service-role notes, the column types of `analytics_events`, and three sample DAX measures (DAU, listing-creation funnel, premium-conversion rate). Includes screenshots once available.

- Analytics #15 (Power BI starter report) — Commit a `docs/powerbi/Collectables-Starter.pbit` template (Power BI report template) parameterised by Supabase host + port. Users open it, paste their Supabase connection, and get DAU + funnel out of the box. Stored under `docs/powerbi/` so it ships in the repo.

- Analytics #16 (RLS test) — Add a SQL test (or a plain checklist in `MANUAL-TASKS.md`) verifying that anonymous + authenticated callers cannot SELECT `analytics_events` (only the service-role used by Power BI can read it). Prevents accidental leakage of full event history.

- Analytics #17 (rate limiter for trackEvent) — Mirror the Sentry rate limiter (Crash #11) for `trackEvent` to bound the free-tier consumption. 200 events/min/user feels right; expose `__resetAnalyticsRateLimitForTests`.

- Analytics #18 (Do-Not-Track + GDPR opt-out) — Add a `<DiagnosticsToggle>` in `app/settings.tsx` that controls both Sentry (Crash #15) and Analytics. Persist under `collectables-diagnostics-v1`. Honour the browser's `navigator.doNotTrack === "1"` on web. Required for App Store compliance for EU users.

- Analytics #19 (privacy policy update) — Extend `APPSTORE-SUBMISSION.md` App Privacy table with PostHog (Analytics → Linked / Not for tracking), Microsoft Clarity (Diagnostics → Not linked / Not for tracking), Power BI (no SDK in app, only consumes from Supabase). Update the public privacy policy paragraph to list each sub-processor.

- Analytics #20 (test coverage) — Add `__tests__/analytics-config.test.ts`, `__tests__/analytics-events.test.ts` (asserts every event in the typed union has a description + at least one allowed prop), `__tests__/analytics-rate-limit.test.ts`, and a structural test on `app/_layout.tsx` confirming `initAnalytics` is called after `initSentry`.

[DONE: 2026-05-03] [est: 1000] - Cache `node_modules` and the Expo Metro cache (`~/.expo`, `node_modules/.cache`) between CI runs in both `ci.yml` and `release.yml`. The new web-build step adds ~1min per PR push that's mostly Metro re-bundling unchanged dependencies; an `actions/cache` step keyed on `package-lock.json` would cut that close to zero on warm runs without changing semantics.

[DONE: 2026-05-03] [est: 1500] - Add a small `lib/cloudinary-config.ts` (pure, no react-native import) that reads the three `EXPO_PUBLIC_CLOUDINARY_*` env vars once and exports a single `cloudinaryConfig` object (apiBase, cloudName, uploadPreset). Today `lib/cloudinary.ts` reads `process.env` at module top-level — fine for production but invisible in tests. A pure config module with a `resolveCloudinaryConfig(env)` helper that takes a record makes it trivial to add coverage for the priority rules (override URL wins over cloud name, etc.) and lets future modules share the same parsed config without re-reading env in three places.

[DONE: 2026-05-03] [est: 2000] - Replace the dual fixed-interval polls landed in `app/chat/[id].tsx` (8s) and `app/chats.tsx` (15s) with a single visibility-aware scheduler that pauses when the tab is hidden / app is backgrounded and resumes (with one immediate refresh) on return. Today both intervals run continuously regardless of focus, which wastes Supabase REST quota on idle tabs and adds avoidable battery drain on mobile. A small `useVisibilityRefresh(refreshFn, intervalMs)` hook in `lib/chat-context.tsx` (listening to `document.visibilitychange` on web and `AppState` change on native) would centralise the cadence and let future cloud-backed screens (premium, marketplace) reuse the same primitive.
[DONE: 2026-05-03] [est: 1500] - Surface a "connection / sync" indicator next to the new web header chats button so users know whether realtime is online (cheap green dot) or whether their messages will land via the on-mount REST refetch (yellow). The plumbing exists: `subscribeToInbox` already receives `SUBSCRIBED` / `CHANNEL_ERROR` status updates but ignores them. Threading that boolean through `ChatContext` (a `realtimeOnline` flag) and rendering an 8px dot on the header icon would close the visibility gap without adding a new screen, and pairs naturally with the new `refreshFromCloud` fallback.

[DONE: 2026-05-03] [est: 300] - The CI test job failed: 1 test out of 367 failed — "web header chats button" in __tests__/web-header-chats-button.test.ts. Because of this failed test the deployment workflow stopped and Web build / Bundle smoke check / Deploy were skipped. Open the file __tests__/web-header-chats-button.test.ts and fix the failing test. To see the exact error message, run the test locally: npm test -- web-header-chats-button or npx jest web-header-chats-button. The error log shows "1 subtest failed" but the specific assertion message is hidden — find it by scrolling up in the GitHub Actions log above line 3108, or run locally to see which assertion failed. Once you find the failing assertion, decide: 1) If the test is correct and the code is broken — fix the component so the test passes. 2) If the test is outdated (component was changed intentionally) — update the test to match new component behavior. If you want to deploy right now without fixing the test, you can either: a) temporarily skip the test by adding .skip to the failing test case b) modify the deploy workflow so the build job does not depend on the test job — open .github/workflows/deploy.yml and remove "needs: test" from the build job. But better fix the test properly before deploying.

[DONE: 2026-05-03] [est: 500] - Extract font constants (`FONT_DISPLAY`, `FONT_BODY`, `FONT_BODY_BOLD`, `FONT_BODY_SEMIBOLD`) into a shared `lib/fonts.ts` module so the string literals `"Syne-ExtraBold"`, `"DMSans-Bold"` etc. don't need to be copy-pasted across 16 files. The current approach works but a typo in a fontFamily string silently falls back to the system font on native; a constants file + a TypeScript union type would catch mismatches at compile time.

[DONE: 2026-05-03] [est: 400] - Extract a `<NavTab>` component from the repeated `<Pressable>` + `<Ionicons>` + `renderBadge` block in `components/bottom-nav.tsx`. The three-cell repetition (left map, plus, right map) now renders the same 6-line icon/badge pattern twice; a 20-line sub-component with `icon`, `iconActive`, `active`, `badge`, and `onPress` props would shrink the render output and make it easier to add future tabs (e.g. notifications) without copy-pasting.

[DONE: 2026-05-03] [est: 300] - Add a `postinstall` npm script that runs `npx expo install --check` to validate peer dependencies after `npm install`, and commit a `package-lock.json` to CI so `npm ci` can be used instead of `npm install`. Today the pipeline fails silently when `node_modules` is absent because `tsx` falls back to the globally-cached version which lacks tsconfig path-alias support; a lock-file + `npm ci` step would make installs deterministic and surface peer-dep drift before tests run.
[DONE: 2026-05-03] [est: 200] - Add a `lint:ci` script in `package.json` that runs `npx tsc --noEmit && npm test` together as a single pre-push gate. Today CI catches TypeScript errors and test failures separately, but a developer can accidentally push code that passes tests yet has TS errors (as happened with the duplicate `mergeCloudMessages`). A combined gate at the bottom of the CI job list would surface both classes of failures in the same place and make the pipeline failure reason obvious at a glance.
[DONE: 2026-05-04] [est: 800] - Expose `clearRuntimeSupabaseConfig` from `lib/supabase.ts` as a dev-only escape hatch via the Expo DevMenu (registered with `addDevMenuItems`) instead of a persistent UI button. Today the function is dead code since the runtime config form was removed; surfacing it only in dev builds via the DevMenu would let engineers flush a bad local override without polluting the production login screen with credentials-reset UI.
[DONE: 2026-05-03] [est: 600] - Add a realtime subscription to `marketplace_listings` inserts in `lib/supabase-marketplace.ts` so the marketplace screen auto-refreshes when another user posts a new listing — mirroring the `subscribeToInbox` pattern from `lib/supabase-chat.ts`. Today the context only fetches on mount, so a listing posted by another user is invisible until the viewer hard-reloads. Wiring a Supabase realtime channel on `marketplace_listings` (filtered to `sold_at=is.null`) closes the gap with ~30 lines using the existing `RealtimeClient` setup.
[DONE: 2026-05-03] [est: 400] - Extract `NavTab` from `components/bottom-nav.tsx` into a standalone `components/nav-tab.tsx` file so it can be reused in a future desktop/tablet sidebar nav without pulling in the full `BottomNav` component. Today `NavTab` is a local helper inside `bottom-nav.tsx`; the 20-line extraction costs nothing and makes the component graph explicit.
[DONE: 2026-05-03] [est: 300] - Extend `CurrencyInput` with a locale-aware default currency by reading `useI18n().language` in the component and mapping it to a suggested currency (e.g. `ru`→RUB, `de`→EUR, `pl`→PLN). Today the component always defaults to USD on first open, so a Russian user sees USD pre-selected even though RUB is more natural. A 10-line lookup map inside the component closes that gap without adding a prop.
[DONE: 2026-05-03] [est: 200] - Show a `<SkeletonItemDetail>` placeholder in the listing detail screen while `fetchingRemote === true` instead of the empty-state "🪧" icon with "..." title. Today deep-link visitors see a brief flash of the not-found empty state while the remote fetch resolves; a skeleton placeholder would communicate loading intent and match the pattern used in `app/item/[id].tsx`.
[DONE: 2026-05-03] [est: 300] - Make `COLLECTIBLE_STOPWORDS` configurable per-collection via a `Collection.stopwords?: string[]` field so users who collect stamps (where "rare" IS a meaningful distinction) can opt out of the default word list. Today the filter applies globally, which may over-strip for some categories. A per-collection override stored in AsyncStorage requires no migration and keeps the default fast.
[DONE: 2026-05-03] [est: 100] - Expand the bundle smoke check to also grep for the six i18n language codes (`"ru"`, `"en"`, `"be"`, `"pl"`, `"de"`, `"es"`) to catch cases where a tree-shaking regression strips the non-default translation maps. Today the check only verifies key names in the English map and provider names; a full-locale grep is an extra 6-line addition to the shell script.
[DONE: 2026-05-03] [est: 200] - Add a CI step that checks all `EXPO_PUBLIC_*` secrets are non-empty before the build starts and fails early with a clear message (e.g. "EXPO_PUBLIC_SUPABASE_URL is required"). Today an unset secret produces an empty string that silently disables login buttons in the deployed app; a pre-build validation step converts a silent UX bug into an obvious CI failure.
[DONE: 2026-05-03] [est: 200] - Add a `migrateStorageKey(oldKey, newKey)` utility in `lib/storage-keys.ts` that reads the old key, writes the value under the new key, then deletes the old key. Today renaming a versioned key (e.g. `collectables-chats-v1` → `v2`) silently loses all cached data for existing users because the new key starts empty. A 10-line migrate helper used once at context boot would let any future versioned rename be lossless.
[DONE: 2026-05-03] [est: 300] - Extend the premium dot in `components/bottom-nav.tsx` with a `useEffect` that fires a one-time `ToastProvider` "Premium active" toast on the session when `isPremium` transitions from false to true. Today activating premium from the settings screen navigates back without any ambient confirmation visible on the home tab; a single toast keyed on the transition closes the feedback gap without adding a new screen.
[DONE: 2026-05-07] [est: 800] Marketplace transfer #1 — Add `buyerUserId: string | null` field to `MarketplaceListing` type. Propagate through `lib/supabase-marketplace-shapes.ts` (`MarketplaceRow.buyer_user_id`, `rowToListing`, `listingToInsertPayload`). Change `markListingSold(id, buyerUserId)` to take buyer ID and update `cloudMarkSold` to PATCH both `sold_at` and `buyer_user_id`. Add Supabase column migration to MANUAL-TASKS.md.
[DONE: 2026-05-07] [est: 1500] Marketplace transfer #2 — Add a "Buy / Trade" button on `app/listing/[id].tsx` for non-self viewers when `listing.soldAt === null`. Tapping shows a confirmation, then calls `markListingSold(listing.id, user.id)` and routes back. On already-sold listings render a "Sold to @buyer" banner.
[DONE: 2026-05-07] [est: 1500] Marketplace transfer #3 — On listing sold, transfer the item to the buyer's "Acquired" collection (auto-created on first transfer). Implemented `transferItemToBuyer(snapshot, options)` in `lib/collections-context.tsx`; the listing-detail claim flow runs the transfer before `markListingSold` so the buyer's collection is the source of truth even if the seller later removes the original.
[DONE: 2026-05-07] [est: 600] Marketplace transfer #4 — Added a "My purchases" section to `app/marketplace.tsx` driven by the new `purchasesForUser` helper + `myPurchases` field on `MarketplaceContext`. Section hides when empty.
[DONE: 2026-05-07] [est: 700] For regular users lock possibility to create a private collections. they can only create public. Wired `usePremium()` into `app/create-collection.tsx`: free users default to public visibility, the Private chip renders with a 🔒 lock indicator, tapping it shows a `visibilityPrivatePremiumOnly` toast, and the save handler clamps `finalVisibility = isPremium ? visibility : "public"` so the cap can't be bypassed.
[DONE: 2026-05-07] [est: 600] Reuse `formatRelativeDate` for the chat preview "last seen" timestamp and the marketplace listing "added X ago" hint. Replaced the bespoke `formatWhen` in `app/chats.tsx` with `formatRelativeDate(preview.lastMessageAt)`. Added a "Listed {when}" hint on the listing detail header (`marketplaceListedAt` translation key, 6 languages) and switched the price-history rows from `recordedAt.slice(0,10)` to `formatRelativeDate(entry.recordedAt)`.
[DONE: 2026-05-07] [est: 100] Use `<PremiumGate>` in `app/marketplace.tsx` … N/A — verified `app/marketplace.tsx` has no `isPremium` branching to replace. The listing-cap UI lives in `app/item/[id].tsx` where it relies on a compound `overFreeCap = premiumReady && !isPremium && !existingListing && myActiveListingCount >= 1` boolean used as both a `disabled` prop and a render guard. `<PremiumGate>` is a binary ready/premium/non-premium switch and cannot replace that compound predicate without expanding the component's API. Leaving `<PremiumGate>` available for future premium-only marketplace sections.
[DONE: 2026-05-07] [est: 700] Add a `lib/design-tokens.ts` module that exports the color palette as typed constants. Created `lib/design-tokens.ts` with 21 named tokens (HERO_DARK, AMBER_ACCENT/LIGHT/SOFT, CARD_BG/2/3, BORDER/2, TEXT_DARK/ON_DARK, MUTED, DANGER, SUCCESS_GREEN, etc.) plus a frozen `designTokens` object and `DesignToken` union type. Migrated `app/marketplace.tsx` as the representative consumer (tokens import + style-sheet swap). Added `__tests__/design-tokens.test.ts` (palette hex format, frozen-object guard, marketplace adoption check).

[DONE: 2026-05-07] [est: 100] Add a CI lint step that validates all structural test regexes against their target files … N/A — already satisfied. Every `__tests__/*.test.ts` structural test calls `read("…path…")` then `assert.match(src, /.../)`, so the existing `npm test` job (gated by `lint:ci` in CI) already fails the build whenever a regex drifts away from its target file. A separate audit step would be duplicate work.
[DONE: 2026-05-07] [est: 600] Share a single `RealtimeClient` instance across `supabase-chat.ts` and `supabase-marketplace.ts`. Extracted `getSharedRealtimeClient` into `lib/supabase-realtime.ts` (memoised module-scoped client, `__resetSharedRealtimeClientForTests` escape hatch). Both chat and marketplace now alias `getSharedRealtimeClient`, halving WebSocket connection overhead. Removed the duplicate `realtimeEndpoint`/`new RealtimeClient(...)` blocks; updated structural wiring tests.
[DONE: 2026-05-07] [est: 300] Export `getDefaultCurrencyForLanguage` into `lib/locale-helpers.ts`. Created `lib/locale-helpers.ts` (pure, no RN imports) with `getDefaultCurrencyForLanguage` + `languageCurrencyMap`. `components/currency-input.tsx` now imports from the new module and re-exports the function so existing callers (`app/item/[id].tsx`) keep working without churn. Added unit tests covering each supported language + the USD fallback.
[DONE: 2026-05-07] [est: 200] Make `VIEWER_PROFILE_TTL_MS` configurable via `EXPO_PUBLIC_PROFILE_CACHE_TTL_MS`. Added `resolveViewerProfileTtlMs()` in `lib/social-context.tsx`: reads the env var, validates it's a positive finite number, and falls back to the historical 10-minute `DEFAULT_VIEWER_PROFILE_TTL_MS`. Tests assert default, env-read, validation guard, and the derived export.
[DONE: 2026-05-07] [est: 3500] - prepare file with everything that needed to upload application into app store(apple). Created `APPSTORE-SUBMISSION.md` covering Apple Developer prerequisites, required `app.json`/Info.plist additions (NSPhotoLibrary/Camera usage strings, ITSAppUsesNonExemptEncryption, CFBundleLocalizations for all six supported languages, associatedDomains for Universal Links), visual assets (1024×1024 icon, screenshots per device class), App Store Connect listing copy, App Privacy declarations, EAS build/submit commands (`eas build`, `eas submit`, `eas credentials`), TestFlight + review flow, pre-submission checklist, and EAS secret parity with the GitHub Actions deploy. Added link from `README-DEPLOY.md` and a structural test (`__tests__/appstore-submission-guide.test.ts`) that asserts every required section / Info.plist key / EAS command / locale / EXPO_PUBLIC_* secret stays present.
- Promote the `resolveViewerProfileTtlMs` pattern (env-with-validation-and-fallback) into a `resolveNumericEnv(varName, defaultValue)` helper in `lib/env.ts` so other tunables (chat poll interval, marketplace cache, etc.) can use the same one-liner instead of each re-implementing the parse-and-guard dance.

- Add a runtime warning toast that fires once if `EXPO_PUBLIC_PROFILE_CACHE_TTL_MS` is set to a value smaller than 30 seconds, since aggressive overrides can hammer Supabase. Today the validator silently accepts any positive number — a soft floor with a visible warning protects free-tier rate limits.

- Document the new `EXPO_PUBLIC_PROFILE_CACHE_TTL_MS` env var in `.env.example` and `README-DEPLOY.md`. Today the variable is undiscoverable unless an engineer reads `lib/social-context.tsx`; adding it to the example file mirrors how `EXPO_PUBLIC_SUPABASE_URL` is documented.

- Add an `EXPO_PUBLIC_REALTIME_DISABLED` kill-switch read inside `lib/supabase-realtime.ts` that returns `null` from `getSharedRealtimeClient` regardless of `isSupabaseConfigured`. Useful for incident response (drop all WebSocket traffic without redeploying) and for offline-only QA scenarios.

- Migrate the `CURRENCIES` whitelist out of `components/currency-input.tsx` into `lib/locale-helpers.ts` so other forms can render their own picker without re-declaring the same array. Today only the `<CurrencyInput>` component knows the supported list.

- Pair `getDefaultCurrencyForLanguage` with a `getDefaultLocaleForLanguage` helper that returns a BCP-47 tag (e.g. `ru` → `ru-RU`, `pl` → `pl-PL`) so date/number formatting is consistent across screens. Today the same `LOCALE_MAP` exists inside `lib/i18n-context.tsx`; centralising it next to the currency map ends the duplication.

- Make the `LANGUAGE_CURRENCY` map driven by an env override (`EXPO_PUBLIC_LANGUAGE_CURRENCY=ru:RUB,en:EUR`) so QA can flip per-region defaults without a code change. Useful for testing localized launches.

- Expose `setUserPreferredCurrency(currency)` that persists to AsyncStorage under a new `collectables-currency-v1` key so a power user who picks JPY once doesn't have to re-pick it on the next listing form. The helper module is the natural home for the read/write pair.

- Add a `__tests__/i18n-locale-map-parity.test.ts` test asserting that every key in `languageOptions` (i18n) also exists in `languageCurrencyMap` (locale-helpers). A new language added to one map but not the other would silently fall back to USD — a parity test catches that drift.

- Add a `__resetSharedRealtimeClientForTests` invocation to a global `beforeEach` so any future direct (non-structural) test of the realtime path starts with a clean cached client. Today the helper is exported but unused — wiring it into the test bootstrap closes a potential cross-test contamination footgun.

- Forward subscription events from one shared `RealtimeClient` channel into multiple consumers (chat inbox + marketplace listings) instead of re-subscribing per-screen. Today every screen creates its own channel; if 3 tabs are open the same UPDATE row arrives 3×. A small fan-out subscriber registry on top of `getSharedRealtimeClient` would deduplicate.

- Track the `RealtimeClient` connection state (CONNECTING/OPEN/CLOSED) on a shared context value so any screen can show a "reconnecting" pill without each module wiring its own status listener (chat already does this via `realtimeOnline`; marketplace doesn't yet).

- Add a `closeSharedRealtimeClient()` cleanup in `lib/supabase-realtime.ts` invoked on sign-out to release the WebSocket promptly. Today the socket lingers for the rest of the JS-realm's lifetime; closing it on `signOut` mirrors how `clearRuntimeSupabaseConfig` resets local state.

- Migrate `lib/social-context.tsx`'s future realtime subscriptions (presence/profile updates) to also use `getSharedRealtimeClient` once they ship, so we keep the "one socket per app" invariant from regressing.

- Migrate the remaining 20+ files (`app/index.tsx`, `app/chat/[id].tsx`, `app/listing/[id].tsx`, `components/bottom-nav.tsx` etc.) from inline hex literals to `lib/design-tokens.ts` imports. `app/marketplace.tsx` is the demo migration; replicating it codebase-wide would cap the brand-color rotation cost at one constant change instead of `grep -rn '#261b14'`.

- Add an ESLint rule (or a small CI grep step) that fails on raw `#[0-9a-fA-F]{6}` hex literals inside `app/**/*.tsx` and `components/**/*.tsx`, with an allowlist for `lib/design-tokens.ts` itself. Today nothing prevents a future contributor from re-introducing inline hexes; a lint guardrail makes the design-tokens convention enforceable.

- Extend `lib/design-tokens.ts` with semantic spacing/radius tokens (e.g. `RADIUS_CARD = 22`, `RADIUS_PILL = 999`, `SPACING_TIGHT = 6`) so card geometry is also one-line tunable. Today the same `borderRadius: 22` recurs across 14+ stylesheets — same problem, same solution.

- Generate a Storybook-style "tokens preview" route at `app/_dev/tokens.tsx` (gated behind `__DEV__`) that renders every named color/spacing token as a swatch with its key. Designers reviewing PRs could verify visual changes without spelunking through stylesheets.

- Type the `BackgroundColorValue`/`ColorValue` props on shared components (`PremiumGate`, `EmptyState`, etc.) as `DesignToken | (string & {})` so consumers can pass either a token name or a one-off literal but get autocomplete + typo-detection on the common path. The `(string & {})` trick keeps the prop assignable to anything but surfaces the named tokens in IntelliSense.

- Memoise the `Intl.RelativeTimeFormat` instance per language inside `formatRelativeDate` so a screen with N timestamps (chats list, price-history table) doesn't construct N formatters per render. The current implementation news up the formatter on every call — fine for one-off renders, wasteful for lists. A `Map<AppLanguage, Intl.RelativeTimeFormat>` cache keyed by locale closes the gap with ~5 LOC and zero behavioural change.

- Auto-tick the "Listed X ago" / chat preview timestamps every 60 seconds via a single shared `useNow()` hook so a chat preview that says "1 minute ago" rolls over to "2 minutes ago" without requiring a screen re-mount. Today the relative timestamp is captured on render and stays frozen until the parent context re-renders. A 30-line hook in `lib/use-now.ts` would close the gap.

- Extract the new `marketplaceListedAt(when)` template into a reusable `relativeDateLabel(prefix, when)` i18n helper so other screens (item detail's "Acquired X ago", profile's "Member since") can use the same prefix-translation pattern without each adding a new key. Today every "X ago" hint requires a new translation entry per language.

- Render the chat preview timestamp in absolute form (HH:mm) for messages received in the last hour and switch to `formatRelativeDate` only for older entries — mirrors how mainstream chat apps surface fresh activity. The `formatWhen` helper that just got removed already had the same-day branch; we could keep that logic and only use `formatRelativeDate` for older messages.

- Add a tooltip / accessible label on the new "Listed X ago" `<Text>` that reveals the absolute ISO date on hover (web) / long-press (native), so power users who care about precise timestamps aren't punished by the relative format. React Native's `accessibilityLabel` + a `Pressable`/`onLongPress` toast covers both platforms.

- Add an `expo-dev-menu` peer dep wired through `loadDevMenuModule` in `lib/dev-menu.ts` so the new dev-only DevMenu hook actually surfaces a "Clear runtime Supabase config" entry on native dev builds. Today `loadDevMenuModule` returns null because the package isn't installed, so engineers only get the `globalThis.__clearRuntimeSupabaseConfig` web-console fallback. A 1-line `npx expo install expo-dev-menu` plus a custom config plugin entry would unlock the native DevMenu path without changing any application code.

- Move the `__DEV__` global type declaration into a project-wide `types/globals.d.ts` so both `lib/dev-menu.ts` and any future dev-gated module can reference `__DEV__` without the `(globalThis as { __DEV__?: boolean })` cast. The cast pattern works but already appears in two places; a single ambient declaration removes the repetition and lets TypeScript flag misspelled `__dev__` accesses.

- Extend `registerDevMenu` in `lib/dev-menu.ts` to accept a per-action label (instead of using the function key as the DevMenu label) so engineers see "Clear runtime Supabase config" in the menu instead of the camelCase identifier. Today the action map's keys double as labels, which is fine for `globalThis` helpers but reads awkwardly in the user-facing DevMenu list.

- Auto-register a `__resetCollectablesStorage` dev helper alongside `__clearRuntimeSupabaseConfig` that wipes all `collectables-*` AsyncStorage keys via the existing `lib/storage-keys.ts` exports. Today engineers manually clear AsyncStorage to reset onboarding state; a one-line dev escape hatch keyed off the same `registerDevMenu` call closes that gap.

- Add an integration test in `__tests__/dev-menu-wiring.test.ts` that asserts `app/_layout.tsx` calls `registerDevMenu` with `clearRuntimeSupabaseConfig` in its `actions` map. Today the unit test only covers `registerDevMenu` itself; a structural regex test against `_layout.tsx` would catch a future regression where someone removes the `useEffect` block while refactoring providers.

- Promote the new `normalizeListing` helper out of `lib/marketplace-context.tsx` into `lib/marketplace-helpers.ts` so any future code path that hydrates `MarketplaceListing` objects from JSON (push notifications, deep-link previews, server-rendered routes) can reuse the same defensive-default logic without re-implementing the `?? null` coalesce. Keep the original call site untouched.

- Add a `cloud-marketplace` test file (`__tests__/supabase-marketplace.test.ts`) that mocks `fetcher` and `tokenProvider` to assert `cloudMarkSold` actually serialises `buyer_user_id` into the PATCH body. Today only the pure shapes module is unit-tested; threading the new buyer field through the cloud wrapper layer means a future regression (e.g. someone re-introducing the old `{ sold_at }` literal) would go unnoticed until UI tests catch it.

- Surface a small "transferred" badge on `app/listing/[id].tsx` and the marketplace card grid for sold listings whose `buyerUserId !== null`. Today sold listings are simply hidden from the active feed; once the buy/accept flow lands, an explicit "Sold to {buyerName}" pill on `My listings` makes the cycle visible without opening detail. The plumbing now exists — only the rendering is missing.

- Add a `buyerUserId` filter helper `listingsAcquiredByUser(listings, userId)` in `lib/marketplace-helpers.ts` (mirroring the shape of `listingsForUser`) so future profile screens can render a "Trades & Transfers" section without re-implementing the `l.buyerUserId === userId && l.soldAt != null` predicate. A 5-line addition with a paired unit test.

- Replace the loose `JSON.parse(raw) as MarketplaceListing[]` cast in `lib/marketplace-context.tsx` with a runtime validator that drops malformed entries (e.g. missing `id` or `mode`) instead of trusting the AsyncStorage payload. Today a corrupt local cache crashes downstream rendering because `mode` ends up `undefined`; a `coerceListing(raw)` helper that returns `null` on validation failure would harden first-paint without changing the happy path.

- Surface a "Sold to @buyer" pill on the marketplace `ListingCard` for sold listings (currently filtered out via `activeListings`), keyed off the new `MarketplaceListing.buyerUserId` field. A "recently sold" carousel under the active grid would let users see what's moving without changing the active-only marketplace contract — useful pricing context for sellers drafting their own listings.

- Add a `MarketplaceListingHistory` view on the user profile screen that lists prior `buyerUserId === user.id` listings (purchases) and `ownerUserId === user.id && soldAt !== null` listings (sales). The data is already in `useMarketplace().listings`; it's a single `useMemo` filter and a list render, no new context required.

- Persist the `buyerUserId` change through the `subscribeToListings` realtime channel by switching the filter from `event: INSERT` to also include `UPDATE` events, so when a buyer claims a listing on device A it disappears from device B's marketplace immediately rather than after a manual refresh. The PostgresChanges payload already exposes the updated `buyer_user_id`; threading it through the same `rowToListing` path would close the gap with ~10 LOC.

- Add a `marketplace_transfers` table (or extend `marketplace_listings` with `transferred_at`) so the future item-ownership transfer step (sub-task #3) records *who got what and when* even after the seller deletes their listing, giving us audit history. Today even with `buyer_user_id` we lose the price/mode context if the listing row is dropped.

- Reuse the new `markSoldPayload(soldAt, buyerUserId)` helper from `lib/supabase-marketplace-shapes.ts` to add a strict integration test asserting that `cloudMarkSold` PATCH bodies always include `buyer_user_id` (even when null) — guards against a future refactor accidentally dropping the column from the PATCH and silently leaving the field unset on existing rows.

- Lock the Buy / Trade button in `app/listing/[id].tsx` behind the same `friendsOnly` rule that already gates the Message-owner CTA, so a non-friend viewer cannot claim a listing whose owner has restricted who can interact with them. Today the claim button renders for any non-self viewer; the friends-only branch already returns a placeholder for messaging — we should reuse it for claiming too.

- Send an automatic chat message to the seller via `ensureChatWith` + `appendMessage` immediately after a successful claim, using a templated body like "Bought your listing for {price}". This closes the loop for the seller without forcing the buyer to open a separate chat thread; the existing chat infrastructure already supports it.

- Add a system-toast on successful claim using the existing `ToastProvider` to mirror the `marketplaceListingCreated` / `premiumActivated` toast pattern. Today the only feedback after claiming is the listing flipping to "Sold" — a transient toast keyed on the `markListingSold` callback's success would feel more responsive on slow networks.

- Persist a `claimingListingId` flag in `MarketplaceContext` instead of the local `claiming` state in `app/listing/[id].tsx`, so navigating away and back during a slow PATCH doesn't reset the spinner. The context already centralises listings — it's the natural home for in-flight claims.

- Migrate the `Platform.OS === "web"` branch in `handleClaimPress` to a shared `lib/confirm-dialog.ts` helper so other screens (delete-collection, cancel-premium, leave-friend) can use the same web-vs-native confirmation primitive instead of each duplicating the `window.confirm` / `Alert.alert` pair.

- Have the seller's device react to a "their listing was claimed" realtime event by offering an "Archive item" or "Delete from my collection" prompt. Today the buyer pulls a snapshot into their collection but the seller still keeps the original — useful for collectors who want a sale audit trail, confusing for those who expect the item to "leave" their collection.

- Migrate the `transferItemToBuyer` Acquired-collection ID generator from `${userId}-acquired-marketplace` into a `userScopedCollectionId(userId, "acquired-marketplace")` helper in `lib/collections-helpers.ts` so future system-managed collections (e.g. "Wishlist", "Trash", "Premium-only") share the same naming pattern.

- Persist the `transferItemToBuyer` invocation in a local "transfer history" log keyed by `${listingId}-${createdAt}` so a buyer can see every marketplace acquisition with its origin listing, even after the original listing is deleted upstream — opens the door to a proper "Purchases" tab (sub-task #4).

- Stop hard-coding `acquiredFrom: t("marketplaceTitle")` in `performClaim` and instead store the seller's `@username` (or fallback `displayName`) so the buyer's Acquired collection records *who* the item came from. Provenance is the most useful field for collectors auditing their own catalogue.

- Add a unit test in `__tests__/transfer-item-to-buyer.test.ts` that drives the `transferItemToBuyer` reducer (extract the inner update logic from the closure into a pure helper if needed) so we can assert: (a) idempotent re-claims don't duplicate items, (b) the Acquired collection's `coverPhoto` updates on first transfer, (c) the new item carries `condition` / `tags` from the snapshot.

- Add a "Sales" subsection on the marketplace screen mirroring "My purchases" — it would render `myListings` filtered by `soldAt !== null`. Together they form a complete personal trading audit ("what I bought" / "what I sold") without leaving the marketplace tab.

- Render the buyer's avatar/displayName chip on each My-purchases card by passing `buyer = getProfileById(listing.buyerUserId)` instead of `owner = getProfileById(listing.ownerUserId)`. Today the purchases list reuses `ListingCard` which shows the seller's name — useful, but for purchases the *seller* is the relevant counterparty so reverting to a "From @seller" treatment would be consistent.

- Make `purchasesForUser` accept an optional `since: Date` argument so the future profile screen can show "purchases this month" without re-filtering on the caller side. The helper already sorts newest-first, so the cap-by-date is a single `.filter` extension.

- Replace the inline `{ ...styles.cardWrap, flexBasis: \`${100/columns}%\` }` spread in the new purchases section with a memoised wrapper component (or extract `<ListingGrid>` from the existing active-listings render). Today the markup is duplicated character-for-character which makes future grid tweaks (gap, alignment) error-prone.

- Persist a "Mark as received" button on each purchase card so the buyer can confirm they actually got the item shipped. That bit (an `arrivedAt` timestamp) would unlock dispute handling and let sellers see "delivery confirmed" in their own listings view.

- Extend the new private-collection premium gate to also block *editing* an existing collection's visibility from public→private when the user isn't premium. Today the gate only fires on creation; a non-premium user could conceivably keep an old private collection alive after letting their subscription lapse without the flow re-prompting upgrade.

- Surface a "Locked premium feature" upsell sheet (instead of the toast) when a non-premium user taps the Private chip in `app/create-collection.tsx`. The toast pattern works but is fire-and-forget; a sheet that explains the benefit + offers a one-tap "Activate premium" CTA would be more conversion-friendly.

- Migrate the `isPremium ? "private" : "public"` default-state expression to a `defaultCollectionVisibilityForUser(isPremium)` helper in `lib/premium-helpers.ts` so the rule lives next to other premium-derived defaults (listing cap, premium badge, etc.) and can be unit-tested independently of the React component.

- Add a structural test to `__tests__/i18n-translations.test.ts` that asserts every language map either declares `visibilityPrivatePremiumOnly` directly or inherits it via the `...en` spread — a regression here would silently render the English string for non-English users on the private-locked toast.

- Hide the `visibilityChip` for "private" entirely on free accounts (instead of locking it) once we have analytics confirming nobody clicks the locked chip — a cleaner flow at the cost of a less discoverable upgrade prompt.

- Fetch this design file, read its readme, and implement the relevant aspects of the design. https://api.anthropic.com/v1/design/h/ns7d1RAssYmHmrqG82s2yw
Implement: the designs in this project