## Three refusals that must stay three, and the errno nobody had (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (second and third entries of "Errno-reporting follow-ups", filed by the run directly below): `UNKNOWN` was printed in the slot where every other case prints a real errno, and nothing said the three loader refusals were DISTINCT. The first entry of that section was left standing: it proposes reversing a decision shipped an hour earlier, which wants a run of its own rather than same-day churn. -->

- [DONE: 2026-08-15] [est: 7000] **Say "no errno" in words, and pin the three loader refusals as three** — the classification behind the unreadable-link report printed `UNKNOWN` for an error carrying no `code`, in the slot where every other row prints `EACCES`/`ELOOP`/`ENAMETOOLONG`. That reads like a real filesystem code, and the reader spends the next minute looking one up that does not exist; the error's own message is the only thing actually known there. It is now `no errno: <first line>`, and the classification moved into an exported `describeResolveFailure(error)` — a pure function of one argument, which is what makes its interesting rows a TABLE instead of three arranged kernel states: the two absence codes stay silent, every other code is passed through, an error with no `code` is described in words, and a thrown non-Error survives (`catch` binds whatever was thrown, and this value is rendered straight into the diagnosis that exists to explain the failure — one `String(...)` away from a TypeError inside it). `UnreadableLink.code` became `.detail` for the same reason: the field no longer always holds an errno and a name that says it does would be a lie the next reader has to discover. The second half is the drift the individual `assert.match`es cannot see: the three refusals — does-not-resolve, GLOBAL_FOLDERS, unreadable-link — are the reader's whole map of what went wrong, and each was pinned only by its own regex in its own case, so nothing said they were DIFFERENT. The first draft of the third message repeated `npm ci` from the second, which is exactly that drift caught by hand. One case now renders all three with the mkdtemp roots normalised out and asserts three distinct strings. Non-vacuity checked by short-circuiting the unreadable branch so its case falls through to the global-folder sentence: the two collapse into one after normalisation and the case goes red, alongside a red for the `UNKNOWN` regression. 5 new cases. `npm run verify` green: typecheck, `lint:all` 13/13, 5375 tests (+5), build. No DB migration, no creds.

## The link that could not be read, told apart from the one that is not there (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Containment-check follow-ups", filed by the run directly below): realpathOrNull collapsed every failure reason into null, so three unrelated errnos arrived as the GLOBAL_FOLDERS refusal. -->

- [DONE: 2026-08-15] [est: 9000] **Keep the errno instead of collapsing every unresolvable chain link into `null`** — `realpathOrNull` answered `null` for `ENOENT` (the ordinary state of nearly every ancestor's `node_modules`) and for `EACCES` on a directory the runner cannot traverse, `ELOOP` on a symlink cycle and `ENAMETOOLONG` alike, and the caller reads `null` as "this chain link does not exist". So a checkout whose own install this process merely cannot LOOK at got the GLOBAL_FOLDERS sentence — a diagnosis naming a resolution rule that had nothing to do with it, ending in `npm ci`, which fixes none of the three. Worse than unhelpful: the unreadable link may be exactly the install the resolver loaded, so "not from the checkout" was an answer the check was not entitled to. `probeChainLink` now returns the realpath or the errno, treating only `ENOENT`/`ENOTDIR` as absence, and `resolvedFromCheckout` returns `{fromCheckout, unreadable}` — collected along the whole walk, and returned even on success so the CALLER decides, because a chain that contains the resolution has nothing left to explain. The third refusal names each unreadable path with its code, says in as many words that this is not "your checkout has no install", and deliberately does not print `npm ci`. The entry inside an unreadable directory is no longer probed: it is reached through the same path, so it can only repeat the errno under a longer name. Two new cases, both mutation-checked: a scratch checkout whose `node_modules` is a self-referential symlink plus a loader on `$NODE_PATH` (run in a CHILD, since `NODE_PATH` is read once at startup) gets a refusal that names `ELOOP` and the link and mentions neither GLOBAL_FOLDERS nor `npm ci`; and the same cycle at the NEAREST link with the real hoisted install one directory up is ACCEPTED, since an unreadable link is a reason to withhold the answer, not to refuse an install the spawn goes on to load. A symlink cycle rather than a `chmod`, because these suites run as root in CI containers and root traverses an `EACCES` directory anyway — the permissions case would have passed without ever producing an errno. `npm run verify` green: typecheck, `lint:all` 13/13, 5370 tests (+2), build. No DB migration, no creds.

## The chain entry that is there and does not answer (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Provenance-check follow-ups", filed by the run directly below), and the fixture it asks for found a hole rather than confirming a comment: the second arm of the provenance check accepted an install that had NOT answered. -->

- [DONE: 2026-08-15] [est: 8000] **Prove the chain entry ANSWERED, instead of trusting that it exists** — `resolvedFromCheckout` accepted on either of two grounds, and the second was `existsSync(<chain>/node_modules/tsx)`, defended by an argument in a comment: the chain is searched before the global folders, so an entry that is there must have won. The argument has a gap the comment did not, and the fixture the suggestion asked for walked straight into it — the chain is searched first, but an entry the resolver cannot LOAD is skipped rather than fatal, so an empty `node_modules/tsx` (an interrupted `npm ci`, a pruned store), a broken symlink or a manifest whose `main` was never written all leave the resolver falling through to `$NODE_PATH` and succeeding there. Existence then reads that as "the checkout answered", clears the check, and hands five suites the spawn that exits 1 with both pipes empty — a half-written install plus a good global one being precisely the case this check was added for a commit earlier. Reproduced before it was fixed: an empty `node_modules/tsx` under the root and a loadable `tsx` on `NODE_PATH` resolves to `/tmp/…/g/tsx/index.js` while `existsSync` on the chain entry returns true. The existence arm is gone; both arms are now containment, REALPATHED on both sides — the chain's `node_modules` and the package entry inside it — which keeps every case the existence arm was protecting (a symlinked or store-backed install lives at the entry's realpath; a root reached through a symlinked ancestor realpaths to the same place) while refusing every unloadable one, since a path that does not resolve has no realpath to compare. One new case, red under the shipped `existsSync` and green now. `npm run verify` green: typecheck, `lint:all` 13/13, 5368 tests (+1), build. No DB migration, no creds.

## The install proved to be the checkout's own (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Loader-resolution follow-ups", filed by the run directly below): `require.resolve` keeps GLOBAL_FOLDERS even when `paths` is passed, so a globally installed loader answered for a checkout that has none. -->

- [DONE: 2026-08-15] [est: 11000] **Refuse a loader that only a global folder can see** — the resolution that replaced `existsSync` yesterday asks node's own question, and node's answer is wider than the question: `paths` replaces the resolution roots "with the exception of GLOBAL_FOLDERS", so `$NODE_PATH`, `~/.node_modules` and `$PREFIX/lib/node` still answer. A developer with a global `tsx` therefore got a GREEN loader check for a checkout with no install — and then the spawn the check clears is `node --import tsx`, an ESM resolution, which consults no global folder at all. That is the empty-pipes failure the whole check exists to prevent (exit 1, both pipes empty, byte-for-byte a guard REFUSING), arriving through the check itself, with the one message that would have explained it suppressed. `tsxLoaderIn` now asks a second question after the resolution succeeds: did the answer come out of the checkout's own dependency chain? `nodeModulesChain(root)` is `root/node_modules` plus every ancestor's — precisely what CJS walks and what the ESM resolver walks, and precisely NOT the global folders — and `resolvedFromCheckout` accepts on either of two grounds, because a resolved path is REALPATHED: the file lies under a chain entry, or a `node_modules/tsx` entry exists on the chain. The second arm is what keeps a symlinked or store-backed install (pnpm, `npm link`, a workspace) accepted — the chain is searched BEFORE the global folders, so if such an entry exists the successful resolution necessarily came through it — and it is also what keeps a root reached through a symlinked ancestor (`/var` → `/private/var` on macOS) from being read as a global answer. The refusal names the resolved file, the `node_modules` that should have answered, the GLOBAL_FOLDERS rule that let it through, and `npm ci`. Three new cases: the global-folder refusal, run in a CHILD process carrying `NODE_PATH` (it is read once when node builds its global paths at startup, so assigning it in-process changes nothing — a call could not have tested this); a symlinked install accepted; a hoisted install one directory up accepted, in a temp directory made inside this repository so the ancestor chain is real. The first is non-vacuous by construction: the same fixture under the previous code resolves `/tmp/…/tsx/index.js` and answers `"tsx"`. `npm run verify` green: typecheck, `lint:all` 13/13, 5367 tests (+3), build. No DB migration, no creds.

## The install proved by resolving it, and the last spawn through the bin (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Fixture-refusal follow-ups", filed by the run directly below): the loader check proved a DIRECTORY named `node_modules/tsx` exists, which is not the thing the spawn depends on. -->

- [DONE: 2026-08-15] [est: 9000] **Resolve `tsx` the way node will, instead of asking whether a directory shares its name** — `tsxLoaderIn` is the one check standing between five suites and their worst failure mode: an absent loader exits non-zero with BOTH pipes empty, byte-for-byte what a guard REFUSING looks like, so every "this run failed" assertion passes for the wrong reason and every "this run passed" fails with a bare `1 !== 0` and nothing to explain it. It proved that with `existsSync` on `node_modules/tsx`, which is a claim about a directory NAME rather than about the thing the spawn needs: an interrupted `npm ci`, a pruned store or a half-restored cache all leave the directory in place with the package's entry points missing, so the check passes and the spawn produces exactly the empty-pipes signature the check exists to prevent — the one failure it must not let through. `require.resolve("tsx", {paths: [root]})` asks node's own question, through a `createRequire` bound to the helper only so `resolve` exists (every call passes `paths`, which replaces the resolution roots entirely, so the answer is about the checkout in the argument). The refusal now names the checkout, the `node_modules` it looked under, AND the resolver's own first line, because a half-written install and an absent one want different fixes and the sentence is the only place a reader can tell them apart — pinned by a case that renders both with the root normalised out and requires them to DIFFER, or "it says why" is satisfied by two identical sentences. It still answers the bare specifier rather than the file the resolver named: `--import /abs/node_modules/tsx` is an `ERR_UNSUPPORTED_DIR_IMPORT`, and the CJS resolution may name a different entry than the `import` condition the spawn gets. Three new cases (the empty `node_modules/tsx` directory, the manifest whose declared entry point is missing, the two-sentence comparison), all three red under the `existsSync` this replaces. **Related bug found by the same sweep and fixed in the same pass:** `__tests__/lint-guard-premise.test.ts` — the file that runs all thirteen guards for real — still carried its own `execFileSync(node_modules/.bin/tsx)`, the last one in the repository. That is the spawn shape that sat in CI's "Run tests" step past 25 minutes twice: the bin is a WRAPPER around its own node child, so killing it leaves the grandchild holding the write end and the parent's read never sees EOF, which makes a `timeout` decoration — and this copy had no `timeout`, no `maxBuffer` and no loader check at all, so on a checkout with no install it produced thirteen bare `1 !== 0` failures with empty output. Migrated onto `runGuardWith`, deleting ~25 lines of private spawn and a second `checkNameOf`. The migration got a drift guard rather than a comment: a sweep over every `__tests__/*.test.ts` (comments stripped, since two files explain this in prose) refusing the bin spawn, with the pattern assembled from two halves on separate lines because written as one literal it matches ITSELF and would report only its own file forever. The sweep carries its own positive control — both spellings the repository has actually used must match, and a `.bin/tsc` must not — so a pattern that stopped matching anything could not pass as a clean sweep. Mutation-checked both ways: restoring the `existsSync` turns 3 loader cases red, and planting a bin spawn in a sibling suite makes the sweep name the file. `npm run verify` green: typecheck, `lint:all` 13/13, 5364 tests (+5), build. No DB migration, no creds.

## The diagnosis a fresh checkout gets (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (first entry of "Fixture-refusal follow-ups", filed by the run directly below): tsxLoader's refusal was the one throw in the helper still without a run, and it fired for real during that run. -->

- [DONE: 2026-08-14] [est: 5000] **Make `tsxLoader`'s refusal reachable from a test, and give it two** — the last untested throw in `__tests__/helpers/guard-fixture.ts`, and the one with the strongest claim on being tested: an absent loader exits non-zero with BOTH pipes empty, which is byte-for-byte what a guard REFUSING looks like to these suites, so without it every "this run failed" assertion in five files would pass for the wrong reason. It was unreachable only because `TSX_MODULE` was a `const` resolved against `REPO_ROOT` at import, which is the one thing a test cannot vary. Now `tsxLoaderIn(root)` takes the checkout to look in and `tsxLoader()` is it against `REPO_ROOT` — the same shape `makePartialRoot` already uses for the tree it copies out of. Two cases: an empty scratch directory gets a refusal that names `npm ci` AND the path it looked at (a diagnosis that omits either is one the reader cannot act on), and the real checkout answers with the BARE specifier rather than the directory it checked, which is the `ERR_UNSUPPORTED_DIR_IMPORT` the doc comment warns about stated as an assertion. Not hypothetical: the check fired for real on the checkout this was written on. `npm run verify` green: typecheck, `lint:all` 13/13, 5359 tests (+2), build. No DB migration, no creds.

## The fixture's own refusals, seen firing (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fifth entry of "Shared-fixture follow-ups"): makeSharedPatchedRepo's marker injection is asserted per case and the injection itself is untested. Widened to every refusal in the helper, since the same argument covered all nine. -->

- [DONE: 2026-08-14] [est: 16000] **Give every refusal in `__tests__/helpers/guard-fixture.ts` a red run** — the helper is the floor under five suites and a few hundred assertions, and nearly every claim those suites make is really a claim about it: that the scratch root holds what the spec asked for, that the patched copy is the thing that ran, that the patch landed. It defends each with a throw, and until now only one of the nine had ever been seen to fire (`makePartialRoot([], {})`, next door in the empty-input suite). A guard that cannot fire is indistinguishable from one that fires correctly — `makeSharedPatchedRepo`'s marker check is the ONE thing standing between thirteen provenance assertions and being vacuous, and `entryPatch`'s brace counter is the one thing standing between a patched table and a rewrite that landed in the wrong half. New `__tests__/guard-fixture-refusals.test.ts`, 22 cases: the absolute entry, the entry this repository does not have, the marker module missing from the copied entries, the patch aimed at a file the copy does not hold, the patch that changed nothing, `entryPatch`'s missing key / unclosed entry / no-op rewrite, plus the positive halves those refusals only make sense against (a nested entry sliced WHOLE rather than stopped at its inner brace, the neighbouring entry left alone, and the same parse run against the committed `SCANNED_FLOORS` so the fixture's idea of an entry is checked against a real one). `assertNoStackTrace` — cited by four suites and never tested — got four: prose containing the word "at" passes, a frame on line ONE is caught (a bare `/\n\s+at/` would not be), the nameless `at /path:1:2` shape is caught, and the context prefix names the run. **Two real bugs found and fixed in the helper, both mutation-checked.** (1) `makePartialRoot` refuses AFTER `mkdtempSync`, so every bad-spec assertion leaked a scratch directory into `os.tmpdir()` — invisible precisely because the throw it rides on is the expected outcome. The population is now in a `try` that `rmSync`s and rethrows, and the three cases compare the set of `lint-guard-partial-*` directories before and after rather than trusting the sentence. (2) `runPatched` called `applyPatches` OUTSIDE its `try`, and `applyPatches` writes file by file — so a multi-file patch whose second anchor had drifted threw with the first file already rewritten and the `finally` never reached, handing the next case a copy broken in a way its own patch never mentions. Moved inside the `try`; `pristine` is captured per file as it is touched, so a partially applied patch restores exactly as far as it got. Mutation-checked by putting both back: 5 cases go red, including the cascade where the leftover patch makes the NEXT case's restore assertion fail — which is the failure shape the bug would have produced in the two patched-table suites. `npm run verify` green: typecheck, `lint:all` 13/13, 5357 tests (+22), build. No DB migration, no creds.

## One parser for the table, in the file both harnesses read it from (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fourth entry of "Lookup-code follow-ups", filed by the run directly below): that run wrote a second copy of the entry-slicing brace counter because the first lived in a *.test.ts file rather than in helpers/. -->

- [DONE: 2026-08-14] [est: 4000] **Move `entryPatch` into `helpers/guard-fixture.ts` so one brace counter serves both patched-table harnesses** — `lint-guard-invalid-floor.test.ts` (ten malformed-declaration cases) and `lint-guard-missing-floor.test.ts` (three unanswerable-lookup cases) patch the SAME table the same way: find `  "<key>": {`, count braces from the opening one, rewrite inside the slice, refuse a rewrite that matched nothing. The second copy existed only because the first was declared in a file the `__tests__/*.test.ts` runner glob owns and therefore could not be imported from. It now sits beside `makeSharedPatchedRepo`, which is the function it exists to feed, and both harnesses import it — so the assumption the parse rests on (an entry that ever NESTS must be sliced whole, or the rewrite lands in the wrong half and `runPatched`'s no-op check waves it through because the patch did change something) is written down once instead of being right twice by hand. Pure move: no behaviour change, both harnesses green unchanged at 108 cases. `npm run verify` green: typecheck, `lint:all` 13/13, 5335 tests, build. No DB migration, no creds.

## The refusal that arrived as a stack trace (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fifth entry of "Blank-key follow-ups"): scannedFloorFor threw a bare Error for an unknown check name beside a ScannedFloorError for an unusable one. -->

- [DONE: 2026-08-14] [est: 14000] **Give the three floor LOOKUPS a failure code instead of a bare `Error`** — `scannedFloorFor` (no entry under this name), `countFloorFor` (entry is not count-shaped) and `assertParsedInputs` (entry declares no inputs) each threw a plain `Error`, and every wrapper's handler catches `ScannedFloorError || GuardRootError` and re-throws anything else. So the three most likely consequences of a RENAME printed as node stacks naming `scannedFloorFor`, while the ten ways a malformed entry can be unusable printed as one line headed by the guard's own name — the refusal a reader is likeliest to meet was the one that looked like a crash. `no_floor_declared`, `no_count_floor` and `no_inputs_floor` are the seventh, eighth and ninth `ScannedFloorFailureCode`, so all nine now render out of `FAILURE_MESSAGE` and the module owns every word it prints. A private `lookupFailure(code)` builds them with zeroed `count`/`minimum`, and their sentences deliberately read NEITHER: these are findings about a lookup, so quoting a number would invent a measurement nobody took, and the remeasure hint would send the reader to change a number that is not the problem (both pinned by a test). `SHAPE_MISMATCH_REASON` is exported for the same reason `FLOOR_BELOW_ONE_REASON` is: the two shape sentences are one disagreement stated from either side, so the shared half lives once and the halves that differ are the assertion the wrapper should have called instead (`assertParsedInputs` vs `assertScannedFloor`). New `__tests__/lint-guard-missing-floor.test.ts` gives each of the three a red run through a real wrapper on the shared patched copy — the key renamed out from under `check-inline-hex`, its `count` swapped for `inputs`, and `check-appstore-config`'s `inputs` swapped for a `count` (with a date added to the note in the same patch, because an undated count floor would refuse FIRST on `undated_note` and the case would silently stop testing the code it names). Each case asserts exit 1, the patched-copy marker, the WHOLE sentence rendered by `formatScannedFloorFailure` rather than a re-typed fragment, no stack trace, no `scanned-floor: ` prefix (the bare errors' shape), and no clean-scan line. The excuse list is mechanical rather than prose-only: `EVALUATOR_PRODUCED` names, per excused code, the harness that produces it, and a test requires each named `__tests__/…` path to EXIST — so `LookupFailureCode = Exclude<…>` makes a new failure code fail to compile until it has a case here or a harness elsewhere. Mutation-checked by putting the bare `Error` back on `countFloorFor`: three cases go red, including the shared-reason one. `npm run verify` green: typecheck, `lint:all` 13/13, 5335 tests (+34), build. No DB migration, no creds.

## The rule stated where it applies, not where it was written (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (third entry of "Subject-id follow-ups", which is the standing "repo-wide guard for PROBLEM_DETAIL readers" suggestion restated for the second table): one regex over one file was the whole defence. -->

- [DONE: 2026-08-14] [est: 26000] **Add `check-problem-phrasing-imports`: a phrasing PART may not be read without a sentence JOINER in scope** — `lib/scanned-floor.ts` renders one problem as one sentence, a subject joined to a clause in exactly one function. Both halves are exported on purpose (a test asserting what a refusal says should read the words rather than re-type a fragment), and that is also the hole: any module can import `scannedFloorProblemDetail` and `scannedFloorProblemSubject` and build its own `"<subject> <clause>"`. The only thing standing against it was the `one problem, one phrasing` drift guard — a regex over `lib/scanned-floor.ts` that sees a second reader IN THAT FILE and nothing anywhere else — which is the same gap the suggestion named twice, once per private table. The rule now lives where it applies: `lib/check-problem-phrasing-imports.ts` (pure matcher) + `scripts/check-problem-phrasing-imports.ts` (walker), registered as the thirteenth entry of `LINT_GUARDS` and `npm run lint:problem-phrasing`, in the shape `check-analytics-imports` already uses. It gathers the named bindings of every `import … from "…/scanned-floor"` in a file and refuses when a part is present and no joiner is — `describeScannedFloorProblem`, `describeScannedFloorProblemRef`, `formatScannedFloorProblem` or `formatScannedFloorFailure`, the last because a failure carries a problem ref and the message table renders it through the same path. Namespace imports are followed rather than left as a one-line walk-around: `import * as floor` plus `floor.scannedFloorProblemDetail(…)` reads a part and is reported, while a file whose namespace reads reach a joiner passes. Comments are stripped first, so the guard does not refuse on its own doc block naming the accessors. `lib/scanned-floor.ts` needs no exemption and gets none: it declares these functions and imports none of them, which a test pins by asserting the file contains no self-import — an exemption list that cannot fall out of date because there is none. Scan roots are `app/`, `components/`, `lib/`, `scripts/` and `__tests__/`; tests are deliberately IN scope, since a test asserting what a refusal says is precisely the caller that wants both halves. Its `SCANNED_FLOORS` entry commits 450 against 638 files measured today, above the 396 that `__tests__/` alone contributes, so no single scan root clears the floor by itself — and the partial-root harness got a fixture for it, since a count floor with no red run is a number nobody has ever seen fail (the suite refuses a new count-shaped guard without one, which is how the omission surfaced). 20 new unit cases cover both parts, all four joiners, two-statement imports, type-only and aliased bindings, a longer neighbouring module name, prose in comments, and all three namespace shapes; five repository files are asserted clean under the rule they are subject to. Mutation-checked by planting a two-part hand-joined sentence in `lib/`: the guard exits 1 and names both reads, the file, the line and the way out. `npm run verify` green: typecheck, `lint:all` 13/13, 5301 tests (+36), build. No DB migration, no creds.

## The key nobody validated, and the sentence that opened with a space (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fifth entry of "Subject-id follow-ups"): the validator checked everything under a SCANNED_FLOORS key and never the key itself. -->

- [DONE: 2026-08-14] [est: 12000] **Report a blank `SCANNED_FLOORS` key as its own problem code instead of rendering a headless sentence** — `validateScannedFloorEntry` checked ten properties of an entry's DECLARATION and never the entry's own name, so a blank key was invisible to it: every shape check passes, `formatScannedFloorProblem` renders the key as the sentence's subject, and the reader gets `" declares no premise…"` — a line that opens with a space where the guard's name belongs. The subject-id refactor a day earlier closed the ref path (both ids resolve to non-empty exported constants) and left this one open, because it is not a rendering bug at all: it is an unvalidated input reaching a renderer that cannot defend itself. `empty_check_name` is the eleventh `ScannedFloorProblemCode` and the only one about the NAME rather than the declaration under it. It is added FIRST in the validator, deliberately: `scannedFloorFor` raises the first problem, so a blank key reported second would print as whatever else the entry got wrong, under a subject that is a space. Its clause in `PROBLEM_DETAIL` says what the defect is rather than describing a different entry — "is keyed by a blank name, so no wrapper can look its floor up and every sentence about it opens with a space where the guard's name belongs" — so the one sentence in the table that genuinely renders with a missing first word is the sentence explaining that the missing first word is the finding. `describeScannedFloorProblem`'s doc now says where a blank subject is defended (at the source, not in the renderer) rather than leaving the next reader to conclude it is not defended anywhere. The pure suite's `BROKEN` fixture map grew a `BROKEN_UNDER` companion — the check name a fixture is validated under, needed by exactly one code — so the three loops that read `BROKEN` (produces-its-code, has-a-fixture, has-a-sentence) cover the new code without the map itself changing shape; new cases pin that a sound floor under a blank key reports `empty_check_name` and NOTHING else, that it is reported first, that `validateScannedFloors({"": floor})` catches it (the route it actually travels, since no wrapper asks for a floor under a blank literal), and that every real `SCANNED_FLOORS` key is non-blank. The wrapper harness in `lint-guard-invalid-floor.test.ts` could not gain an eleventh case honestly: a guard asks for its floor under a string literal in its own source, so a blank KEY is not a row any wrapper can request. Rather than fake one, `ProblemCodeKey` became `Exclude<ScannedFloorProblemCode, WrapperUnreachableCode>` beside a `WRAPPER_UNREACHABLE` record that costs a written reason per excused code, with `Extract<…>` on the excuse type so a code cannot be excused under a name the module does not have — a new code still fails to COMPILE until it has a wrapper run or an argument about reachability. A runtime case asserts the excuse is non-empty and that no code is listed on both sides. `npm run verify` green: typecheck, `lint:all` 12/12, 5265 tests (+6), build. No DB migration, no creds.

## The other type's words field, and the branch nobody could reach (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Two-field-problem follow-ups", plus the second entry of that section and the third of "Single-phrasing follow-ups", which are the same finding stated three ways): the PROBLEM type lost its rendered-sentence field yesterday and the FAILURE type kept one. -->

- [DONE: 2026-08-14] [est: 11000] **Carry a problem code on an `invalid_floor` failure, not a sentence someone else rendered** — `ScannedFloorProblem` dropped its `detail` field yesterday because every renderer derived the words from `code`; `ScannedFloorFailure` still carried `detail?: string`, and `FAILURE_MESSAGE.invalid_floor` printed it verbatim, which made it the ONE of six codes whose message came from outside the message table. So the sentence a guard showed for a bogus declaration was chosen by whichever of three call sites built the failure — exactly the state the problem type was in a day earlier, on the type that actually crosses the boundary from the pure function to the wrapper. `detail` is gone and `problem?: ScannedFloorProblemRef` replaces it: `{ subject, code }`, two enums and no prose. `ScannedFloorProblemSubject` is `"entry" | "caller"` — the only part that genuinely varies between producers — resolved to words by `scannedFloorProblemSubject`, the single reader of a `PROBLEM_SUBJECT` record that is exhaustive over the union and points at the two already-exported subject constants, so the ids and the constants cannot drift into two vocabularies. `describeScannedFloorProblemRef` joins the pair through the existing `describeScannedFloorProblem`, so a failure printed by a wrapper and a problem listed by a test say the same thing about the same code by construction rather than by convention. The `?? NUMERIC_FLOOR_DETAIL(f.minimum)` fallback stopped being a fallback: `evaluateScannedFloor`'s bogus-minimum branch no longer fills a detail, so that arm is now the REACHED renderer for the one `invalid_floor` that is not about a table entry at all (a number handed straight to the pure function — no entry to send the reader to, and no problem code that honestly fits), and the optionality of `problem` is documented as that either/or rather than left looking like a default. Both producers stopped building strings: `evaluateParsedInputs` reports `{subject: "caller", code: "empty_inputs"}` and `scannedFloorFor` reports `{subject: "entry", code: problem.code}`. New assertions run over the failure type's WHOLE surface instead of one fixture, which is the second half of the promoted suggestion: an exhaustive `Record<ScannedFloorFailureCode, () => ScannedFloorFailure>` gives every code a producer, and each produced failure must (a) carry no string field containing any of the ten sentences from the table, (b) declare no field outside a `Record<keyof ScannedFloorFailure, true>` — a `keyof` claim, so a re-added words field fails to COMPILE where a runtime key check cannot see a declared-but-never-populated one — and (c) carry a `problem` only when the code is `invalid_floor`. The same `keyof` treatment went onto `ScannedFloorProblem`, replacing the single-fixture `Object.keys` check. All ten problem codes are now rendered through the failure path and asserted to contain the table's own clause in the entry's voice, and the numeric branch is pinned to name the number AND to not sound like a finding about the table. Mutation-checked by flipping `scannedFloorFor`'s subject to `"caller"`: `scanned-floor.test.ts` stays green (it builds its own refs) and all ten wrapper runs in `lint-guard-invalid-floor.test.ts` go red, which is the honest division of labour — the pure suite pins the rendering, the wrapper suite pins which subject a guard actually reports. `EVERY_PROBLEM_CODE` moved to module scope in the test file so the phrasing block and the new failure block read one exhaustive list rather than two. `npm run verify` green: typecheck, `lint:all` 12/12, 5259 tests (+9), build. No DB migration, no creds.

## The field that could lie, and the clause read off a dash (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md (second and third entries of "Single-phrasing follow-ups", filed by the run directly below and taken together): both are the same finding one step on — after the subject-joining refactor, two SECOND representations of a clause were left standing, one in the type and one in the test that guards it. -->

- [DONE: 2026-08-13] [est: 8000] **Drop `ScannedFloorProblem.detail` and export the clause its guard was slicing out of a sentence** — the refactor below made `formatScannedFloorProblem` derive its words from `problem.code`, which left `detail` as a field every renderer IGNORES: `validateScannedFloorEntry` filled it from `scannedFloorProblemDetail(code)` and nothing read it, so a problem constructed by hand could carry any sentence at all and print a different one. That is the one state where the object can lie — not a drift risk, a guaranteed-silent disagreement — and it is gone: a problem now carries WHICH entry and HOW, and the only route from a problem to its words is the accessor. The type is two fields, pinned by an assertion over `Object.keys` for every problem a multi-problem entry reports, so re-adding a words field fails rather than being noticed in review. Second half, in the test rather than the source: the new "both messages say why a floor below 1 is a bug" guard recovered the shared clause with `declared.slice(declared.indexOf("— ") + 2)`, which is an assertion about PUNCTUATION — true of both sentences today, and quietly satisfied by whatever follows the first dash the day either sentence grows a second one. `FLOOR_BELOW_ONE_REASON` is exported now and both messages are required to contain it, plus neither may BE it, or "both say it" is satisfied by two messages that say nothing else. Mutation-checked by rewording `invalid_minimum`'s tail to a paraphrase: the guard names the side that dropped the clause, where the em-dash slice would have compared the paraphrase against itself. `npm run verify` green: typecheck, `lint:all` 12/12, 5250 tests, build. No DB migration, no creds.

## One subject, one sentence (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Detail-accessor follow-ups", filed by the run directly below): that run exported the clause and left three call sites each building their own sentence out of it. -->

- [DONE: 2026-08-13] [est: 9000] **Join the subject to the problem's clause in exactly one function** — `scannedFloorFor` built `its SCANNED_FLOORS entry ${problem.detail}`, `formatScannedFloorProblem` built `${problem.checkName} ${problem.detail}`, `evaluateParsedInputs` built `the caller ${PROBLEM_DETAIL.empty_inputs}`, and `scanned-floor.test.ts` re-typed the first of those verbatim to build a fixture: four ways to phrase one problem, three of them in source. New `describeScannedFloorProblem(subject, code)` is the only place a subject and the table's clause are joined; `formatScannedFloorProblem(problem)` delegates to it and stays at ARITY ONE on purpose, because it is the callback `validateScannedFloors(...).map(...)` passes and an optional second parameter there would silently receive the array index. The two non-obvious subjects are exported constants rather than literals — `SCANNED_FLOORS_ENTRY_SUBJECT` and `PARSED_INPUTS_CALLER_SUBJECT` — and they are deliberately kept distinct, asserted per code, because the reader's next move differs: one sends them to the table, the other to whoever called the pure function with an empty list, and collapsing them would point half the readers at a row that is fine. `PROBLEM_DETAIL` now has exactly one reader (`scannedFloorProblemDetail`), which is what the new source-text assertion pins — the drift guard, and the only assertion that can see a FOURTH call site, since every other test in the block exercises the function it already goes through. Mutation-checked by putting the interpolation back in `scannedFloorFor`: the guard names the call site and the sentence assertions stay green, which is exactly why the source check has to exist. `lint-guard-invalid-floor.test.ts` gets the stronger claim the refactor makes available — each of the ten wrapper runs now asserts the guard printed the WHOLE sentence, subject included, where it previously matched `/SCANNED_FLOORS entry/` and the clause as two independent substrings that could stop being one sentence without failing. **Related bug found in the same file and fixed in the same pass:** `NUMERIC_FLOOR_DETAIL` (a runtime failure about a number a caller passed) and `PROBLEM_DETAIL.invalid_minimum` (a table problem about a number an entry declares) carried the same 17-word trailing clause verbatim — the second copy of one reason, in the module whose whole subject this task is. It is now `FLOOR_BELOW_ONE_REASON`, and a test reads the clause out of one message and requires the other to contain it, so the two can never explain the same rule in different words. `npm run verify` green: typecheck, `lint:all` 12/12, 5249 tests (+6), build. No DB migration, no creds.

## The sentence, read rather than re-typed (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md (third and fifth entries of "Ten-code harness follow-ups", filed by the run directly below): both are about the harness holding a copy of something the lib owns — the wording of a problem, and the shape of an entry. -->

- [DONE: 2026-08-13] [est: 6000] **Assert the detail a guard prints, not a fragment of it, and parse the entry by braces** — the ten cases in `lint-guard-invalid-floor.test.ts` each carried a hand-typed `says: /not a positive integer/`-style clause copied out of `PROBLEM_DETAIL`, which is private. Two copies of one sentence drift the first time either is reworded, and the drift surfaces as a failing assertion in a file that has nothing to do with the rewording. New `scannedFloorProblemDetail(code)` exports the sentence (the record itself stays private — it is a thing a caller could iterate and reformat), every case now asserts the guard's output CONTAINS the whole sentence, and the `says` field is gone. That is a strictly stronger claim than the fragment match it replaces, and it passed unchanged on the first run, which is the evidence that the guard really does print the table's own words. Pinned from the other side in `scanned-floor.test.ts`: for every code, the accessor's sentence must equal the `detail` the validator puts in the problem it reports — an accessor that answered from a second copy would be exactly the drift this removes. Second half: `entryPatch` found an entry's end with `indexOf("\n  },")`, which is a fact about how the table is formatted today rather than about where the entry ends. Braces are counted from the opening one now, so an entry that ever nests is sliced correctly instead of ending early and having the rewrite land in the wrong half — where `runPatched`'s no-op check would wave it through, because the patch did change something. `npm run verify` green: typecheck, `lint:all` 12/12, 5243 tests (+1), build. No DB migration, no creds.

## All ten problem codes, one copy, one table (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Shared-copy harness follow-ups", filed by the run directly below): that run made a case cost two writes instead of a 200-file copy, which is what made covering the remaining eight worth doing at all. -->

- [DONE: 2026-08-13] [est: 14000] **Give every `ScannedFloorProblemCode` a red run through a wrapper** — `validateScannedFloorEntry` reports ten ways an entry can be unusable and two of them (`invalid_minimum`, `empty_inputs`) had a wrapper run; the other eight were proven about the pure function and never about a guard, which is the exact state `invalid_floor` itself was in two days ago. `lint-guard-invalid-floor.test.ts` is now keyed by `Record<ScannedFloorProblemCode, ProblemCase>`, so a new code fails to COMPILE until it has a case — the same exhaustiveness trick `PROBLEM_DETAIL` uses one file over — and each case gets the same seven assertions: exit 1, the marker proving the patched copy is what spoke, a refusal naming the guard and quoting the code's own sentence, the file the declaration lives in, no `Check the scan roots` (that line sends the reader hunting for a broken walk that does not exist), no stack trace, and no report of the clean scan the guard never established. Patches moved from whole pretty-printed declarations to `entryPatch(checkName, rewrite)`, which slices out ONE entry by its key and rewrites inside it: the anchor is now `/minimum: 160/` scoped to `check-inline-hex` rather than a re-typed copy of the declaration that one prettier run breaks, and the key is a thing `lib/scanned-floor.ts` declares rather than a thing the test remembers. Every rewrite introduces exactly one problem, because `scannedFloorFor` raises the FIRST and a rewrite tripping two would silently stop testing one of them the moment the validator's order changed. The interesting one is `empty_delegation`, and it is not run against the guard that delegates: `check-privacy-baseline-provenance` is the only entry with a real `delegatedTo`, and its wrapper never reads the table — delegation MEANS the premise is asserted elsewhere — so no run of it can produce the code. It is reachable only through a guard that does read its own entry, which is also the finding: a broken declaration on a delegated entry is inert at runtime and caught only by the `validateScannedFloors(SCANNED_FLOORS)` assertion in `scanned-floor.test.ts`. The negative control is now derived from the cases rather than hand-listed (every distinct guard a case patches must pass unpatched, and must print the marker while doing it), and the memo-separation tests reuse the case runs instead of rebuilding their own patches. 82 cases in the file, up from 18, on the same single copy. `npm run verify` green: typecheck, `lint:all` 12/12, 5242 tests (+64), build. No DB migration, no creds.

## One copy for the whole file, and the channel a green run threw away (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Restored-harness follow-ups" said to do the shared-copy refactor BEFORE the eight remaining problem codes each take a full `lib/` + `scripts/` copy, and the two entries beside it (the memo key covering `scriptRoot`, the control asserting provenance rather than mere success) turned out to be the same refactor's preconditions. -->

- [DONE: 2026-08-13] [est: 21000] **Copy the checkout once, patch it per case, and prove which copy answered** — `makePatchedRepo` built a fresh ~200-file copy of `lib/` + `scripts/` per fixture, so the three cases in `lint-guard-invalid-floor.test.ts` cost three copies and the negative control a fourth. `SCANNED_FLOORS` reports ten problem codes and two have a wrapper run, so the next eight were eight more copies before a single new assertion. `makeSharedPatchedRepo` copies once and `runPatched(label, patches, guard, scanRoot)` writes the patch, spawns, and puts the pristine bytes back in a `finally` — a case is now two `writeFileSync` calls, and a case that throws mid-run cannot hand the next one a copy broken in a way its own patch never mentions. The restore is what makes the memo dangerous, so the memo key moved with it: `runGuardWith(guard, {scriptRoot, scanRoot, env, label})` keys on all four, sorted, where the old key was a template string over three. Both new fields are load-bearing and both are asserted rather than remembered — `label` because every case here shares one `scriptRoot` and differs only in the bytes at it (without it the second case is served the first's result, and the wrong answer is a CACHE HIT rather than an error: a green control certifies a patch that never ran), `scriptRoot` because the copy and this checkout must not answer for each other in either direction. Provenance stopped being an article of faith: `makeSharedPatchedRepo` injects a `console.error` marker into the copy's `lib/scanned-floor.ts` — a module every wrapper imports — so each case asserts the COPY spoke. That is the assertion the old negative control could not make: it proved a wrapper ran and exited 0, which is exactly what a bug resolving `scriptPath` against the real repository would also produce, leaving the file's whole integrity resting on its patches rather than on its control. **Related bug the marker found, and the larger one of the two:** every green run was discarding stderr. `execFileSync` RETURNS stdout, and the harnesses read only that, so a passing guard's stderr went nowhere — and `scripts/guard-io.ts` announces a redirected scan root with `console.error`. `lint-guard-empty-root.test.ts`'s "an ordinary run announces nothing about the override" was therefore vacuous: it passed because the channel the announcement would have arrived on was thrown away, not because nothing was announced. `spawnSync` replaces it, both pipes are captured on success and failure alike, and a spawn error that is not a timeout (ENOBUFS and friends) now appends its own line instead of leaving a truncated report looking like a finding. **Second related bug:** `lint-guard-empty-root.test.ts` had never been migrated onto the shared helper and still carried its own spawn — `node_modules/.bin/tsx`, the wrapper process, with no `timeout` and no `maxBuffer`: the exact shape that hung CI's "Run tests" step for 25 minutes, twelve guards' worth of it, in a file whose siblings were fixed a day earlier. It now calls `runGuardIn`/`runGuardWith` and its ~45 lines of private spawn are gone. `assertNoStackTrace` in `guard-fixture.ts` replaces the four hand-typed copies of the "prints a refusal, not a crash" regex, which had drifted into two shapes — `/\n\s+at\s/`, blind to a frame on line one, and `/^\s+at .+:\d+:\d+\)?$/m` — now one pattern anchored on the `file:line:column` tail that makes a line a V8 frame rather than an English sentence. `npm run verify` green: typecheck, `lint:all` 12/12, 5178 tests (+8), build. No DB migration, no creds.

## The spawn that could not be timed out (2026-08-13)

<!-- Not promoted from a suggestion: found on CI. The invalid-floor harness below went green locally in 3s and hung the "Run tests" step on two consecutive runs, past 25 minutes each, with install and typecheck completing normally beside it. -->

- [DONE: 2026-08-13] [est: 11000] **Spawn guards as `node --import tsx`, not through the `tsx` bin, and bound every run** — `runGuardIn` had spawned `node_modules/.bin/tsx <script>` since the first harness. That bin is a WRAPPER: it spawns its own node child and hands it the same stdout pipe `execFileSync` is reading. So a `timeout` on the parent is decoration — killing the wrapper leaves the grandchild holding the write end, the read never sees EOF, and the call hangs exactly as long as it would have without one. Two CI runs sat in "Run tests" past 25 minutes on a suite that takes 60s locally; nothing timed out because nothing could. Spawning `process.execPath` with `--import tsx` removes the intermediate process entirely (bare specifier, not the module directory — an absolute directory path is `ERR_UNSUPPORTED_DIR_IMPORT`), so the 30s `timeout` + `killSignal: "SIGKILL"` added alongside it can actually fire, and a killed run now appends a line naming the script, the root and the budget so the reader is not left with the empty output + exit 1 that looks identical to a guard refusing. `maxBuffer` went to 16MB for the same reason: node's 1MB default turns an overlong report into an ENOBUFS throw with the output discarded, which reads as a crash rather than as the finding it is. Free speedup: one process instead of two takes the four guard-harness suites from ~40s to 7.9s, and the whole `lint:ci` leg to 75s. `npm run verify` green: typecheck, `lint:all` 12/12, 5170 tests, build. No DB migration, no creds.

## The declared input with no run of its own (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md (second entry of "Empty-input harness follow-ups"): the empty-input harness gave every guard that declares inputs a red run, which is not the same as giving every declared INPUT one. -->

- [DONE: 2026-08-13] [est: 9000] **Give every declared input its own empty-input run, not every guard** — `evaluateParsedInputs` returns on the FIRST failure, so a fixture that empties every input at once only ever exercises the first name in the list. `check-sentry-version` declares `["package.json", "package-lock.json"]` and the old fixture blanked both, so the assertion pinned `package.json` and the lockfile half of the declaration had no run at all: a wrapper that stopped handing `package-lock.json` over would have failed this suite on the other key and looked covered. `EMPTY_INPUT_FIXTURES` is now `checkName → EmptyInputFixture[]`, one entry per declared input, each blanking ITS input while copying the real file in beside it (`entries: ["package-lock.json"], files: { "package.json": "{}" }` and the mirror image). Two new registry assertions replace the old per-guard one so this cannot come back: coverage is over every name in every entry's `inputs` rather than over guards, and each fixture must blank EXACTLY ONE declared input — a fixture that blanks two tests whichever the wrapper checks first and silently stops testing the other when that order changes, which is the failure this split exists to remove. 35 cases in the file (+6). `npm run verify` green: typecheck, `lint:all` 12/12, 5170 tests, build. No DB migration, no creds.

## A wrapper-level red run for `invalid_floor`, without a copied checkout (2026-08-13)

<!-- Filed by the run below after its own attempt was reverted. Everything here is what that attempt LEARNED; the code is on the branch history at 8152c61 if it helps, but the technique is the part that needs replacing, not the assertions. -->

- [DONE: 2026-08-13] [est: 14000] **Produce `invalid_floor` from a real wrapper: the removed harness, restored** — the sixth and last `ScannedFloorFailureCode` now has a red run through a wrapper, closing the set. The previous run pulled this harness out of the merge on the belief that it hung CI's "Run tests" step; that belief came from check-run snapshots that were badly stale in that session, and the entry it left behind said so and said restoring it was the first thing to try. It was: `__tests__/lint-guard-invalid-floor.test.ts` (10 cases) and the `makePatchedRepo` / `runGuardFrom` pair in `__tests__/helpers/guard-fixture.ts` are back verbatim from `8152c61`, on top of the merged `node --import tsx` spawn fix, with no change to the technique. `makePatchedRepo` copies `lib/` + `scripts/` into a scratch root and rewrites the table in the COPY; `runGuardFrom` splits the guard's SOURCE root from the tree it SCANS, so a patched wrapper walks the real repository — every walk full, every input readable — leaving the bogus declaration as the only thing that can fail. Two patches, one per shape of the table: `check-inline-hex`'s `minimum: 160` → `0` (count-shaped) and `check-appstore-config`'s `inputs: ["app.json"]` → `[]` (inputs-shaped), plus a negative control that patches only a comment into the copy and asserts BOTH guards still exit 0 — without it, a fixture that failed to run the guard at all would look exactly like a guard refusing for the right reason. 2.2s for the suite; no hang, on the merged spawn fix or otherwise. Related bug found while restoring it and fixed in the same pass: `tsxLoader()` — the `existsSync` check that exists precisely to distinguish "no install" from "guard refused" — was called from inside the `execFileSync` ARGUMENT LIST, so its thrown diagnostic was caught by the same `catch` that captures guard failures and rewritten into `{ output: "", status: 1 }`: the exact empty-output refusal it was written to tell itself apart from. It was dead code in the only situation it exists for, and this checkout proved it — with `node_modules` absent the whole suite failed with four bare regex mismatches against `''` and no mention of the missing install. The call is now resolved into a `const` before the `try`. `npm run verify` green: typecheck, `lint:all` 12/12, 5170 tests (+10), build. No DB migration, no creds.

## The last failure code, and the contract it was a second copy of (2026-08-13)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Empty-input harness follow-ups", filed by the run directly below): five of the six floor failure codes had a red run through a wrapper and `invalid_floor` did not, because it is the only one that means the GUARD is broken rather than the tree. The entry framed it as "a code path to delete or a check to move"; it turned out to be neither on its own — the check moved, and moving it is what made the code path reachable. -->

- [DONE: 2026-08-13] [est: 22000] **Give `invalid_floor` a red run through a wrapper, by moving the table's contract into the table** — the sixth and last `ScannedFloorFailureCode`. The other five are findings about the tree, so a scratch scan root produces them; `invalid_floor` is a finding about the guard's own entry in `SCANNED_FLOORS`, so no scan root can. It was asserted in two places that were each half a contract: a runtime branch in `evaluateScannedFloor` that no guard could reach (every guard reads its floor through `scannedFloorFor`, and the committed table is sound), and four hand-rolled loops in `__tests__/scanned-floor.test.ts` checking properties of the table from outside it. Both are now one function beside the table: `validateScannedFloorEntry` / `validateScannedFloors` return a `ScannedFloorProblem[]` over ten codes (`no_shape`, `invalid_minimum`, `empty_label`, `empty_inputs`, `blank_input`, `duplicate_input`, `delegation_with_shape`, `empty_delegation`, `empty_note`, `undated_note` — the four loops covered four of them). `scannedFloorFor` now validates the entry it returns and raises the first problem as a `ScannedFloorError` carrying `invalid_floor`, which is the type every wrapper already catches, so a bogus entry is a one-line refusal inside the wrapper's own `try` rather than a floor of 0 waving an empty walk through. `ScannedFloorFailure` grew an optional `detail` (a whole sentence that REPLACES the numeric clause) so one code can say what is wrong when the bare `minimum` cannot. **The wrapper-level red run did NOT ship — see the `[WIP]` entry below.** It was written and green locally (`makePatchedRepo` copies `lib/` + `scripts/` into a scratch root, patches the table in the COPY, `runGuardFrom` runs the copied wrapper against the REAL repository, 10 cases including a negative control) and APPEARED to hang CI's "Run tests" step, so it was pulled before merge. That judgement was wrong and is corrected in the `[WIP]` entry above: the CI snapshots driving it were stale, and `8152c61` carries this harness through a fully green run ("Run tests" in 101s). It was removed out of caution, not because it was shown to be broken — restoring it is the top of the follow-up entry. `scanned-floor.test.ts` swapped its four loops for one `validateScannedFloors(SCANNED_FLOORS)` assertion plus a fixture per problem code and an exhaustiveness check over them. Related bug found while building it and fixed in the same pass: `runGuardFrom` spawns `node_modules/.bin/tsx`, and on a checkout with no install `execFileSync` throws ENOENT with empty stdout AND empty stderr — so all four guard-fixture suites captured `""` and exit 1, which is byte-for-byte what a guard REFUSING looks like to them. Every "this run failed" assertion passed for the wrong reason and every "this run passed" assertion failed with a bare `1 !== 0` and nothing to explain it. `tsxBinary()` now checks for the binary once and names `npm ci`. `npm run verify` green: typecheck, `lint:all` 12/12, 5164 tests (+16), build. No DB migration, no creds.

## The input that reads fine and says nothing (2026-08-12)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Partial-root harness follow-ups", filed by the run directly below): that run gave `below_floor` a red run and left `empty_input` — the code the whole `inputs` half of the table exists for — reachable only from a unit test of the pure function. -->

- [DONE: 2026-08-12] [est: 18000] **Produce `empty_input` through a wrapper, for every guard that declares one** — `lib/scanned-floor.ts` declares six failure codes; the empty root produces `no_files` and `unreadable_input`, the partial root produces `below_floor`, and `empty_input` had never been produced by anything but a direct call to `evaluateParsedInputs`. It is the interesting one because NOTHING CRASHES: an `app.json` that reads fine, parses fine and is `{}` sends `check-appstore-config` on to report nine missing keys — a loud failure about the wrong thing, nine consequences of one cause with the cause never named. No path in this repository is that file, so `makePartialRoot` grew a second parameter that writes literal content into the scratch root beside the copies (`{}` for the JSON inputs, whitespace for the text ones — both are what a truncated or half-written file actually looks like, and `isNonEmptyInput` already treats them alike). New `__tests__/lint-guard-empty-input.test.ts` covers all five inputs-declaring guards (29 assertions): exit 1, the message NAMES the input, it is `empty_input` and not the `missing_input`/`unreadable_input` codes beside it (all three are about a declared input and only one means "the file is there and useless"), the named input is one the registry actually declares, and no stack trace. Registry-driven both ways — a guard that grows a declared input with no run to exercise it fails, and a fixture for a guard that declares none fails too. The migration guards need their migrations copied in because their count floor asserts before the input check, which the fixture comment says out loud. Mutation-checked by commenting out `assertParsedInputs` in `check-appstore-config`: the guard still exits 1 (nine missing keys), and the "names the input" assertion is what catches it — which is the entire point of the code. `runGuardIn`/`checkNameOf` moved into `__tests__/helpers/guard-fixture.ts` so this file and the partial-root file share one spawn-and-memoise implementation rather than growing a third copy.

## `below_floor` gets a red run too (2026-08-12)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Empty-root harness follow-ups", filed by the run directly below): that run gave every guard a red run against an EMPTY directory, which exercises `no_files` — the loud code, and the one the floor was never really for. -->

- [DONE: 2026-08-12] [est: 26000] **Point the count-shaped guards at a PARTIAL root and assert `below_floor`** — the empty-root harness makes all twelve guards fail on `no_files`, the code `evaluateScannedFloor` returned before any floor existed; the NUMBER in `SCANNED_FLOORS` buys the other refusal, and `below_floor` had only ever been produced by hand-editing a script. New `__tests__/helpers/guard-fixture.ts` builds the empty root plus part of this repository copied into it, and `__tests__/lint-guard-partial-root.test.ts` drives all eight count-shaped guards through it (56 assertions). COPIES, not symlinks, and the comment says why: a symlinked root works for the guards that walk `path.join(root, "app")` directly and silently yields ZERO files for `check-secrets`, which enumerates the top level with `withFileTypes` where a symlink answers `isDirectory() === false` — a fixture that reproduces `no_files` while wearing a `below_floor` test's name. That confusion is itself asserted against: every guard must report `count > 0` alongside `count < minimum`, so a fixture that degenerates into the empty-root case fails instead of passing for the wrong reason. Registry-driven — a guard with no fixture must appear in `NO_REACHABLE_COUNT` with the reason (three are inputs-shaped or delegated; `generate-powerbi-schema-doc` counts `ANALYTICS_EVENTS`, imported TypeScript no tree on disk can shrink), and the two lists may not overlap or name a guard that is not registered. Related bug found and fixed: `check-inline-hex`'s floor was 150, EXACTLY the count of `.ts`/`.tsx` in `lib/`, so a walk that lost both `app/` and `components/` passed at the boundary while the note in `SCANNED_FLOORS` claimed the opposite — the number was true when measured and the tree grew out from under it, which is the same silent decay the floors exist to catch one level down. Raised to 160 (213 files today, ~25% slack) and the note now states what is actually enforced. The claim is no longer prose: `SINGLE_ROOT_LOCKS` runs each multi-root guard against each of its scan roots ALONE and requires a refusal, so `components/` growing past the 45 that three guards share turns a quiet loss of meaning into a red CI run naming the floor to re-measure. Both halves mutation-checked by reverting the floor to 150 and by lowering the radius floor to 44 — each produced exactly the failure its message promises.

## Every floor gets a red run, not a hand-check (2026-08-12)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Guard-premise follow-ups", filed by the run directly below): that run made all twelve guards runnable in a test and could only ever read a green result from them. -->

- [DONE: 2026-08-12] [est: 28000] **Point every guard at an empty directory and assert it refuses** — new node-pure `lib/guard-root.ts` owns the one shared override (`LINT_GUARD_REPO_ROOT`) all twelve wrappers resolve their scan root through, in the shape `PRIVACY_BASELINE_REPO_ROOT` already had for the history guard (which keeps precedence, so an existing invocation still means what it meant). Two refusals are built into the resolver rather than left to callers: a RELATIVE override is a hard failure, because the same string names three different trees depending on whether `npm run`, `lint:all` or a test spawned the guard; and a redirected guard prints a NOTE naming the variable and the root on every run, because a green line from a redirected root is otherwise indistinguishable from a green line about this repository. Blank (`LINT_GUARD_REPO_ROOT= npm run lint:all`) reads as unset, not as cwd. New `__tests__/lint-guard-empty-root.test.ts` runs all twelve against one `os.tmpdir()` directory and asserts exit 1, a one-line refusal prefixed with the guard's own name, NO stack trace (an uncaught ENOENT also exits 1, so the exit code alone cannot tell "refused" from "crashed"), and that the empty root is still empty afterwards. Getting there needed the wrappers to stop crashing on the way to their own floor: `scripts/guard-io.ts` holds the three reads they share (`listDirEntries`/`listDirNames` return nothing for an unreadable directory, so the floor is what speaks; `readTextInput`/`readJsonInput` return an `unreadableInput` marker), and `lib/scanned-floor.ts` grew an `unreadable_input` code to keep "the file is not there" apart from `missing_input`, which reads as a wiring bug. Both migration guards now declare `MANUAL-TASKS.md` as a fixed input beside their count floor — they read it and never said so. 88 new assertions across `guard-root.test.ts` and `lint-guard-empty-root.test.ts`, plus two source-text checks per wrapper (root comes from the shared resolver; exactly one `path.join(__dirname, "..")`, the resolver's default, so a second walk cannot dodge the override). Related bug found while this PR was the first ever to run the release gate: `release.yml`'s `validate` job checks out at the default depth-1 and runs `npm test`, which now executes the baseline-provenance guard — a guard that FAILS in a shallow clone rather than skipping. ci.yml's test job had `fetch-depth: 0` and a comment saying why; the release gate did not. Fixed, and pinned by a new `__tests__/ci-history-depth.test.ts` derived per JOB (ci.yml's single-suite `sentry-sdk-absent` job stays exempt because it runs one file, not the suite), in the same shape as the `ci-node-version.test.ts` guard that caught the same class of drift between those two files before.

## Zero-file passes across the guard fleet (decomposed 2026-08-12 from the top suggestion "The other eleven guards never ask what they are pointed at either")

<!-- Not started. Promoted from .tasks/.suggestions.md and split here because one sweep over twelve guards is too large for a single run: the premise work below made `lint:baseline-provenance` refuse to pass over a directory it could not read, and the same hole is open in the eleven beside it. Confirmed by inspection, not inferred — `check-inline-hex.ts:59` and `check-secrets.ts:107` both print `scanned 0 file(s), no ...` and exit 0 when their walk finds nothing, which is what a rename, a moved directory or a tightened `.gitignore` produces. Take these in order; each is independently shippable. -->

- [DONE: 2026-08-12] [est: 16000] **Give every `LINT_GUARDS` entry a scanned-file floor, starting with the two proven cases** — new node-pure `lib/scanned-floor.ts` holds `assertScanned(checkName, count, minimum, label)` plus the pure `evaluateScannedFloor` / `formatScannedFloorFailure` pair it is built from, in the shape `lib/bundle-premise.ts` already uses (verdict object, exhaustive `FAILURE_MESSAGE` record keyed by failure code, so a new code cannot ship without a message). Three codes, and the middle one is the reason this is a FLOOR rather than the `count > 0` both siblings settle for: `no_files` is the loud failure everyone pictures, `below_floor` is the quiet one — a walk that lost `app/` but kept `lib/` still returns a comfortable-looking number and still reports "no inline hex literals" — and `invalid_floor` fails a minimum below 1, because a floor of 0 passes over an empty walk while making the hole look covered. `no_files` deliberately does NOT carry the re-measure hint the other two do: nothing-found is a broken scan root, and pointing that reader at the number is how a floor gets lowered to whatever the broken walk happens to return. Floors live in one `SCANNED_FLOORS` table keyed by the guard's OWN check name — the string it prints — so the name in the failure line and the key are the same thing; `scannedFloorFor` throws on an unknown name rather than defaulting, so a guard cannot opt out by typo. Both floors are measurements with ~30% slack, dated in their notes (`check-inline-hex` 150 against 212 real files, `check-secrets` 500 against 713), sized to survive ordinary churn and not to survive a lost source root. The two wrappers now assert BEFORE their early "no findings" return (a floor checked after it never runs on the run that needed it) and catch `ScannedFloorError` themselves to print one line instead of a stack trace. Mutation-checked all three ways: an empty `SCAN_ROOTS` gives `no_files` exit 1, walking only `components/` gives `scanned 44 … below the committed floor of 150` exit 1, pointing the secret walk at `assets/` gives `scanned 2 … below 500` exit 1, and all three go back to green on revert. New `__tests__/scanned-floor.test.ts` (29 tests) covers the evaluator, every message, the error class, the table's own invariants (positive integer floor, dated note) and the wiring in both wrappers including the assert-before-report ORDER. `npm run verify` exits 0 — typecheck + 12/12 guards + 4884 tests (+29) + web build. No DB migration, no creds.

- [DONE: 2026-08-12] [est: 14000] **Wire the floor through the remaining nine guards** — `SCANNED_FLOORS` grew from a flat `{label, minimum}` to three declarable shapes, because the twelve guards are not all walks: `count` (a measured minimum over whatever the guard enumerates), `inputs` (a fixed set of files that must have parsed to something), and `delegatedTo` (the premise is enforced elsewhere, named here so the table can be walked exhaustively). A guard may declare `count` and `inputs` together; the registry test now asserts SCANNED_FLOORS ↔ LINT_GUARDS is a bijection keyed by check name, derived from `scriptPath` rather than a second hand-kept list, so neither side can grow an entry the other lacks. Shape per guard: **count** — `check-inline-radius` 45/63, `check-analytics-imports` 45/63, `check-clarity-input-mask` 45/62, `check-env-inlining` 3/5, `check-migration-docs` 18/25, `check-supabase-migration-naming` 18/25. The radius/analytics floor of 45 is picked deliberately above the 44 that `components/` alone contributes, so losing `app/` — the root holding most of the literals — fails instead of passing; the migration floors are append-only, so a count below them means the directory moved rather than that migrations were deleted; `check-env-inlining` at 3 is the smallest and the most valuable, since `-config.ts` is a filename SUFFIX and one rename drops a resolver out of the scan with no other symptom. **inputs** — `check-appstore-config` (`app.json`) and `check-sentry-version` (`package.json`, `package-lock.json`): `readFileSync` already throws on a missing file, so the hole for a fixed reader is the file that reads fine and carries `{}`, after which every downstream check reports a consequence instead of the cause. `check-sentry-version` deliberately does NOT declare the resolved version strings as inputs — `findSentryVersionIssues` already reports a missing declaration in its own words, and duplicating it would turn a legitimate SDK removal into a premise failure. **both** — `generate-powerbi-schema-doc`, the only guard needing two: an empty `ANALYTICS_EVENTS` renders a header-only table that matches a header-only doc and prints "up to date", which is a drift check passing precisely because there is no schema left to drift, so the taxonomy gets a count floor (12/17) and the doc file gets the input assertion. **delegated** — `check-privacy-baseline-provenance`, whose `checked === 0` refusal stays where it is because the thing it counts is the same object it validates. New failure codes `missing_input` (declared but never handed over, so a wrapper that stops reading a file fails instead of silently checking one fewer) and `empty_input`, plus `isNonEmptyInput`, which accepts `0` and `false` — presence is the question, and treating `0` as empty would reject a legitimately-zero reading. Mutation-checked both new shapes: `app.json` forced to `{}` exits 1 naming the empty input, a zeroed taxonomy count exits 1 naming the floor, both green on revert. `__tests__/scanned-floor.test.ts` 29 → 66 tests, now driving its wrapper assertions off `LINT_GUARDS` so a thirteenth guard cannot ship without a floor. `npm run verify` exits 0 — typecheck + 12/12 guards + 4921 tests (+37) + web build. No DB migration, no creds.

- [DONE: 2026-08-12] [est: 12000] **Assert the premise in the registry test rather than per-guard** — new `__tests__/lint-guard-premise.test.ts` runs all twelve `LINT_GUARDS` entries the way `lint:all` does (`node_modules/.bin/tsx` directly, memoised so twelve spawns cover thirty-eight assertions) and reads what came back, so the chain the floors protect — guard runs → walks a real tree → reports a real count — is asserted end to end instead of inferred from source text. Four assertions per guard, chosen by the shape its floor declares: it exits 0 and says something at all; its report is prefixed with the name its floor is keyed by (so a failure never points at a key that does not exist); a **count** guard names some number at or above its committed minimum; an **inputs** guard names every file it declares; the **delegated** guard still names a non-zero count, because delegation is about where the refusal is implemented and not about whether the guard has to say what it looked at. Kept as its own file rather than folded into `lint-guards.test.ts`, which is pure parity checking over text and should not start spawning processes. Two guards could not satisfy the premise as written and were changed rather than exempted, which is the finding: `generate-powerbi-schema-doc --check` printed "docs/powerbi-connection.md is up to date" and named NO count, so an empty taxonomy compared against a header-only doc reported success in exactly those words — it now names the events it checked; and `check-sentry-version` named neither file it reads, so it now says "read from package.json + package-lock.json", which is also what makes its declared inputs assertable. Mutation-checked: stripping the count out of `check-inline-radius`'s pass line turns `names a count at or above its committed floor of 45` red, and restoring it turns it green. The test also asserts its own premise — a registry that failed to load would leave a file full of passing nothing, so `LINT_GUARDS.length >= 12` and a floor lookup per guard run outside the loop. `npm run verify` exits 0 — typecheck + 12/12 guards + 4959 tests (+38) + web build. No DB migration, no creds.

## A guard pointed at a non-repository is not a pass (2026-08-12)

<!-- Promoted from .tasks/.suggestions.md (second entry of the section the run directly below filed, taken ahead of the first because it closes a hole that run opened rather than extending it). -->

- [DONE: 2026-08-12] [est: 14000] **Make a root with no history a hard failure, and forbid a redirected history from guessing its base** — the run below added `PRIVACY_BASELINE_REPO_ROOT` so the three `classifyBaselineRevision` branches could be exercised, and an env var that moves which repository a history guard reads deserves more than an announcement. Following the thread found the older and larger bug behind it: every branch of `resolveComparison` reads a `null` from `git()` as an ANSWER about history, and a directory that is not a work tree answers null to all of them — `HEAD~1` fails, `HEAD` fails, and the guard reported "no commits in this repository yet", a SKIP, printed as a PASS, over a directory that never had commits at all. `PRIVACY_BASELINE_REPO_ROOT` is one way in; an exported copy of the sources with no `.git` is the ordinary one, and it needed no env var. `git rev-parse --is-inside-work-tree` is now the FIRST question asked, and anything but `true` exits 1 with a message that names the root and branches on whether an override is in play. The skip it displaced kept its place but not its wording — "no commits in this WORK TREE yet" now genuinely means an unborn HEAD, because the only path to it is through a confirmed work tree. Second constraint, aimed squarely at the env var: an override may not fall through to `HEAD~1`. `HEAD~1` resolves in any repository whatsoever, so a workflow that exported the variable once would silently compare this table against a stranger's parent commit and report the skip that comparison earns; a redirected history must now be given `--base` (or `PRIVACY_BASELINE_BASE_REF`) explicitly. Third, the override is named on the PASS line as well as before the run — a green summary that does not say where it read from is how a redirected guard quietly becomes the guard. Three new cases in `__tests__/privacy-baseline-provenance-script.test.ts` (7 → 9): an empty directory with a README exits 1 saying it is not a work tree and never prints the unborn-HEAD skip, an override with no base exits 1, and the pass line carries the root. `npm run verify` exits 0 — typecheck + 12/12 guards + 4855 tests (+2) + web build. No DB migration, no creds.

## The guard's git half runs in a test, not by hand (2026-08-12)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): that run added the three-way classification and could only reason about two of its branches. -->

- [DONE: 2026-08-12] [est: 18000] **Exercise all three `classifyBaselineRevision` branches end to end through a throwaway repository, and stop a missing git reading as an empty one** — the run below split the drift half's skip into `table` / `absent` / `unparseable`, unit-tested the classifier in all three states, and left the SCRIPT path that feeds it (`git cat-file -e <ref>:<path>`, then `git show`) covered by a presence probe run once by hand. `merge-base` is exactly what makes the obvious local test impossible: a scratch commit here that deletes or mangles `lib/privacy-body-baselines.ts` resolves back to a fork point that still holds a good table, so `absent` and `unparseable` are unreachable from inside this repository — reasoned about, never executed. `scripts/check-privacy-baseline-provenance.ts`'s `REPO_ROOT` is now overridable by `PRIVACY_BASELINE_REPO_ROOT`, and new `__tests__/privacy-baseline-provenance-script.test.ts` builds a two-commit repository per case under `os.tmpdir()` and points the guard at it: a base holding the real table reports `compared against <sha>` and never says "skipped", a base without the module gives the predates-it skip and exits 0, and a base where the module exists but yields no table exits 1 with the exact `formatBaselineRevisionUnparseable` text. The override moves the HISTORY half only — the table under test is still this checkout's `PRIVACY_BODY_BASELINES`, which is what lets the fourth case rewrite `en` to 900 in the fixture and assert the two real failure strings verbatim — and it announces itself on stdout whenever it is set, because an env var that silently redirects a guard at an empty repository is the vacuous green this file was written against. Related bug found and fixed while covering it: `git()` collapsed "git exited non-zero" and "git could not be RUN" into the same `null`, so on a machine with no `git` on PATH every probe answered null in turn, the first one to be asked reported `HEAD` has no parent, and `resolveComparison` printed "no commits in this repository yet" — a SKIP, reported as a PASS, by a guard that had not run at all. A spawn error or signal death is now a hard exit 1 naming the failed command and the root it ran in, and the seventh test runs the guard with `PATH=""` to prove it. Both fixes were mutation-checked: reverting either turns its test red. `npm run verify` exits 0 — typecheck + 12/12 guards + 4853 tests (+7) + web build. No DB migration, no creds.

## A removed baseline table is a failure, not a skip (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): that run's own skip path was the hole this closes. -->

- [DONE: 2026-08-11] [est: 12000] **Separate "the guard predates this revision" from "the table was removed" in the drift half's skip** — the run below reads the previous revision of `lib/privacy-body-baselines.ts` as text and skips the drift half when `parsePrivacyBodyBaselines` returns null. That skip is right for exactly one case: a base revision from before the module existed, which is every commit before the guard shipped and, unavoidably, the commit that introduced it. It is wrong for the case that looks identical from the parser's side — the module was there and is not any more, or was renamed, or was reformatted past the regex — and that is precisely how a guard gets turned off for good: one skip line, in a log nobody reads twice, forever. The parser cannot tell them apart because the difference is not in the text; only git knows. `git cat-file -e <ref>:<path>` is now asked FIRST and separately, and its answer is an input to a new pure `classifyBaselineRevision(present, source)` returning `table` / `absent` / `unparseable`. `absent` skips and says so ("did not exist at <ref>, so the guard predates it" — the old message claimed "no parseable table", which was true of both cases and useful about neither); `unparseable` exits 1 with a message that names the three ways it happens and says why it is not the skip its sibling gets. Also tightened `note_omits_previous_words` from a substring match to a digit-bounded one, so a note mentioning `1690` no longer counts as having named the `169` it replaced. Verified against real history: the presence probe is `absent` at every commit before `c008754` and `present` from it on, so the drift half now genuinely runs on every push rather than skipping — and the first CI run with the guard live went green on `Code-style guards (lint:all)` with `fetch-depth: 0`. `__tests__/privacy-baseline-provenance.test.ts` 39 → 44 tests. `npm run verify` exits 0 — typecheck + 12/12 guards + 4846 tests + web build. No DB migration, no creds.

## A baseline may not change without saying why (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): that run made the six sizes checkable and left the checker itself editable without comment, which is the hole this closes. -->

- [DONE: 2026-08-11] [est: 30000] **Make a `PRIVACY_BODY_BASELINE_WORDS` change carry a dated reason and a matching policy edit, so the drift gate cannot be silenced in one commit** — the run below records what each of the six policy files renders to and fails anything outside a ±25% band, and a test re-measures the table from the tracked markdown so a *wrong* number fails immediately. What nothing caught was a number changed for the WRONG REASON: the documented fix for a red drift gate is "update the constant to the measured count", the failure message says exactly that, and a merge that truncated a policy and was resolved that way turns the guard into a rubber stamp — silently, permanently, in one commit that reads like the fix it was told to make. Nothing automatic can separate that from a real amendment, because both edit the file and both move the count. What can be enforced is that the person moving it writes down why, next to the number, in the same commit. Five decisions worth keeping. (1) The table moved to its own `lib/privacy-body-baselines.ts` and grew `recordedOn` + `note` per entry; `PRIVACY_BODY_BASELINE_WORDS` is now DERIVED from it and re-exported from `lib/bundle-smoke.ts`, so every existing import and message is untouched and there is still exactly one hand-maintained copy of the six numbers. The move is load-bearing, not tidiness: the guard reads the PREVIOUS revision as text via `git show <base>:lib/privacy-body-baselines.ts`, and a 40-line file whose whole job is one literal is a far more stable parse target than an 800-line module. (2) `parsePrivacyBodyBaselines` is a regex over the literal, anchored on `export const` (the name also appears in the file's prose) and stopped at the `};` in column 0 — a revision from a commit that predates the field set must be READ, never executed. A round-trip test parses the file on disk and asserts it equals the imported object, so a reformat that defeats the parser fails on the commit that does it instead of quietly disabling the drift half three commits later. (3) Two halves, one report: SHAPE (a real `YYYY-MM-DD`, a note over a 5-word floor — `note: "updated"` is precisely the shrug the convention exists to prevent, and words rather than characters so one long token cannot buy past it) runs from the working tree always; DRIFT runs only against a readable previous revision. Four drift codes — `policy_unchanged` (the number moved, `PRIVACY.md[.code]` did not), `provenance_unchanged` (the paste-and-move-on resolution), `provenance_date_regressed`, `note_omits_previous_words` (the note must name the count it replaced, so `1171 → 980 (dropped the analytics section)` is legible in the diff without checking out the parent). `provenance_unchanged` reports ALONE and short-circuits the two after it, which both read provenance that was never written — one cause is not three problems. (4) A SHALLOW clone is a hard failure, not a skip, and `ci.yml`'s test job (the only job running the `lint:all` fan-out) now checks out with `fetch-depth: 0`; a guard that passes because it could not see the previous revision is the vacuous green the post-build premise work exists to stamp out. A root commit, and a base predating the table, are genuine skips — both are printed, never silent. (5) Base resolution is `--base` → `PRIVACY_BASELINE_BASE_REF` → `origin/$GITHUB_BASE_REF` → `HEAD~1`, always through `merge-base`, and a base someone NAMED that does not resolve stops the run while a GUESSED one falls through — comparing against something other than what the caller asked for is worse than stopping. Proven end-to-end against real git: the number moved alone exits 1 with both `policy_unchanged` and `provenance_unchanged`; note+date+`PRIVACY.md` all updated exits 0; a note omitting `1171` exits 1 naming it. New `__tests__/privacy-baseline-provenance.test.ts` (39 tests). `npm run verify` exits 0 — typecheck + 12/12 guards + 4841 tests (+39) + web build. No DB migration, no creds.

## Committed baseline size for every policy file (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): that run made the counts readable and left them uncheckable, which is the hole this closes. -->

- [DONE: 2026-08-11] [est: 14000] **Fail a policy file that drifts from its recorded size, so a shrinking disclosure is a red build rather than a number in an old log** — the run below started printing `(169-1171 words)`, which makes a shrinking translation *readable* and still not *checkable*: reading it requires comparing two CI logs from different days, which nobody does. Meanwhile the only floor under any policy file was the same 40 words as everything else, so the canonical English disclosure cut from 1171 to 400 by a bad merge cleared it by an order of magnitude, differed from every translation, had a heading and a body, and passed every gate — and then every content check downstream ran against a document that had lost two thirds of itself, quietly, because the translations still looked nothing like it. `PRIVACY_BODY_BASELINE_WORDS` records what each of the six renders to and `body_size_drift` fails anything outside a ±`PRIVACY_BODY_BASELINE_TOLERANCE` (25%) band. Four decisions worth keeping. (1) The check runs LAST, after the prose comparisons, which is why it lives in `evaluatePrivacyPages` and not in `evaluatePrivacyPage` where the measurement is: `PRIVACY.md.de` holding the English disclosure is *both* a copy and 593% of its recorded size, and only one of those two sentences tells anyone what to do — same reason `body_too_short` still wins on a twelve-word stub, since it is also what explains the near-copy ratio going quiet. Extracting the prose gates into their own `evaluatePrivacyPageProse` is what made that ordering expressible; the loop is now `prose ?? size`, and the English page — skipped by the prose half because it IS the baseline — is size-checked like everything else, being the document whose shrinking matters most. (2) A language with NO baseline is a failure (`missing_baseline`), not a skip, or adding a seventh language silently exempts its policy from the one check that would notice it shrinking; a test also pins the table's key set against `PRIVACY_PAGE_LANGUAGES` in both directions. (3) The band is symmetric even though shrinking is the case it was written for, because the cheapest way for a translation to grow 40% is someone pasting the English disclosure into it, and because a bound that only ever constrains one direction stops describing the file. (4) The failure quotes the measured count, the baseline, the signed percentage AND the name of the constant to update — this is the one gate that goes red on a legitimate amendment, so the fix has to be in the message; updating the table is the mechanism, not a side effect of it. The six numbers are re-measured from the tracked markdown by a test, so a typo in the table fails immediately instead of quietly widening someone's band, and a second test pins every baseline's lower bound above the 40-word floor so the drift message can never be unreachable for a language. `evaluatePrivacyPages` took an optional baselines table (the shape `evaluateBundleSmoke` already uses for its token list) so the prose cases — whose 80-word fixtures are deliberately the wrong size for their language — can neutralise the size gate and keep asserting the one thing each was written for; the general fixture is now sized from the real baselines instead of a flat 45 words. Proven end-to-end against a real build in both directions: truncating `dist/privacy/de/index.html` exits 1 with `renders 99 word(s) against a baseline of 169 (-41%)`, truncating the ENGLISH page to a third exits 1 naming `PRIVACY_BODY_BASELINE_WORDS["en"]`, and the untouched build prints `6 privacy page(s) present and rendered (169-1171 words)`. `__tests__/bundle-smoke.test.ts` 111 → 123 tests, including both band edges accepted and both just-outside points rejected for two languages, the two precedence cases, the unknown-language case, and a run of the REAL renderer over all six tracked files. `npm run verify` exits 0 — typecheck + 11/11 guards + 4802 tests (+12) + web build. No DB migration, no creds.

## Body word counts in the privacy page report (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): every task left in .tasks.md is blocked — one [needs-data] on a 30-day PostHog window, two on dependency decisions — and this is the number the run below started measuring and then threw away. -->

- [DONE: 2026-08-11] [est: 8000] **Report the six body word counts on success instead of discarding them** — `evaluatePrivacyPage` counts every page's words to apply the 40-word floor and dropped the number the moment it passed, so the only visible state was binary: over the floor or red. A translation SHRINKING across several commits — a section dropped in a merge, a paragraph lost in a re-translation — was invisible until the commit that pushed it under 40, at which point the build goes red with no history to read and nobody can say when it started. The counts now ride out on `PrivacyPagesResult.wordCounts` and the success line reads `6 privacy page(s) present and rendered (169-1171 words)`, which is a number that moves in the log commit by commit and costs one `Math.min`/`Math.max`. Four decisions worth keeping. (1) The count is returned by the ok verdict (`{ ok: true, wordCount }`) rather than recomputed by the aggregate — the floor already did the work, and a second call to `privacyBodyWords` is a second place for the two to drift. (2) FAILED pages contribute nothing: a page whose body never rendered has no size worth quoting, and `body_too_short` already carries its own count into its own message, so this list is non-empty exactly when the run passed — pinned as an invariant test rather than left as a coincidence, because an empty list on a passing run is what would make the parenthetical silently vanish. (3) A page that clears the per-page gates and then fails a CROSS-page one (`untranslated_heading`, `near_english_body`) still contributes its count: size is a property of the page's own body, and being a copy of the English page does not make it unknown. (4) `formatPrivacyPageWordCounts` collapses an all-equal set to one number — `169-169 words` reads like a bug in the formatter rather than a fact about the pages — and returns null rather than an empty parenthetical for the empty set, which is unreachable on a passing run and handled anyway because a format helper is not where a build guard should crash. Per-page counts are kept as `{ code, words }` in input order rather than a bare min/max pair, so the standing `--json` suggestion has the six numbers to emit without another pass. Proven end-to-end against a real build: `npx tsx scripts/check-bundle-smoke.ts` prints `6 privacy page(s) present and rendered (169-1171 words)` — the same 169 (de) and 1171 (en) the shipped files measure, which is also the spread that makes the range worth printing, German compounding what English spells out. `__tests__/bundle-smoke.test.ts` 100 → 111 tests, including the verdict returning exactly what `privacyBodyWords` measures, the count list tracking passing pages only, both formatter branches, and a case running the REAL renderer over all six tracked `PRIVACY.md.<code>` files that asserts the report quotes a range and that no counted page sits under the floor it just passed. `npm run verify` exits 0 — typecheck + 11/11 guards + 4790 tests (+11) + web build. No DB migration, no creds.

## Word floor under the privacy page bodies (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): it names a hole that run opened, and the fix turns out to also close `empty_body`'s stated intent, which had been checking the wrong thing since it shipped. -->

- [DONE: 2026-08-11] [est: 13000] **Fail a page whose rendered body is under the ratio's own floor, so the near-copy comparison can never silently not run** — `privacyBodySimilarity` returns null below 40 words and the aggregate read null as "nothing to compare", which meant a `PRIVACY.md` truncated under the floor took `near_english_body` offline for all five translations at once and the run still printed `6 privacy page(s) present and rendered` — the vacuous-green shape every other guard in this repo was fixed for, re-introduced hours earlier by the ratio itself. Fixed by making the floor a FAILURE (`body_too_short`) rather than a skip, and by sharing ONE constant between the gate and the ratio: `PRIVACY_BODY_SIMILARITY_MIN_WORDS` is both the number below which the ratio refuses to measure and the number below which a page is rejected, which makes "nothing that passes is too short to compare" an invariant instead of a hope. A test asserts exactly that — it drives every fixture through the gates and then through the ratio and fails if the two floors ever drift apart. The fix turned out to close a second, older hole: `empty_body` was named for "the policy body rendered empty" and has always checked `<h1` presence, which proves the HEADING rendered — the half that did work. Six pages with a heading and no body extracted to six null bodies, skipped every prose comparison, and passed; that case is now six failures and is pinned as a regression. Gate order puts the floor last (a page with no heading has no body worth counting, and `empty_body` is the truer thing to say), and the failure carries its word count into the message, so the log says `renders a heading over 3 word(s) of policy text` rather than making the reader go and count. `PrivacyPageVerdict` grew the same optional `detail` the aggregate's failure type already had, spread rather than always-set so the five codes that measure nothing keep the two-key shape their `deepEqual` pins expect. Proven end-to-end against a real build, in both directions: truncating `dist/privacy/be/index.html` to a heading and three words exits 1 naming the file and the count, and truncating the ENGLISH baseline — the case that used to silence all five translations — now reports itself and nothing else, one cause and one failure. The suite's `renderedPage` fixture grew a 45-word body of language-distinct words for the same reason it grew a heading and then a body in the two runs before this one; distinct rather than shared filler, because 45 identical words plus one language marker score 0.978 against each other and would have made every fixture in the file trip `near_english_body`. `__tests__/bundle-smoke.test.ts` 94 → 100 tests. `npm run verify` exits 0 — typecheck + guards + 4779 tests (+6) + web build. No DB migration, no creds.

## Near-copy threshold on the privacy body gate (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): every task left in .tasks.md is blocked — one [needs-data] on a 30-day PostHog window, two on dependency decisions — and this is the hole the body gate shipped with. -->

- [DONE: 2026-08-11] [est: 15000] **Replace the body equality test with a similarity ratio, so "not literally identical" stops passing** — `untranslated_body` fires on a normalised byte match, which means the gate shipped hours earlier is exactly one edit from useless: the English disclosure with a German sentence appended, or one word changed, or a stray character, is unequal to the English page and walks through. `privacyBodySimilarity` measures shared words as a fraction of the SHORTER body and fails at or above `PRIVACY_BODY_SIMILARITY_THRESHOLD` (0.9). Four decisions worth keeping. (1) Over the SHORTER body rather than the union, because the cheapest way to dress an English document up as a translation is to ADD to it — an appended paragraph moves every symmetric measure and moves this one not at all, since every English word is still accounted for; it also scores a TRUNCATED English body 1.0, which is right, half the English policy still being the English policy. (2) Multiset, not set: counting `min(count)` per word stops a body that repeats `data` forty times from borrowing forty matches from a document that says it once, which is precisely where set intersection inflates short pages. (3) `\p{L}`/`\p{N}` rather than `\w`, because `ru` and `be` are not Latin and a `\w` split reduces those bodies to their digits and scores every pair of them 1.0 on the numbers alone. (4) A 40-word floor below which the ratio does NOT run — two unrelated short notices sharing `Collectables`, a URL and the effective date score 1.0 without being related — and the floor skips only the ratio, never the equality check, so the gate is strictly stronger than the one it replaces at every length. 0.9 is a measured margin, not a hedge: the six real rendered bodies sit at 0.157–0.280 against each other while a copy is 1.0, and nothing lives in the 0.3–0.9 band, which a test recomputing the real ratios now pins at `< 0.5` so a translation drifting toward the line fails while there is still room to think. Reported as its OWN code `near_english_body` rather than folded into `untranslated_body` — "this file IS the English policy" and "this file is the English policy with edits" send the reader to different places — and the failure carries the measured percentage into the message, because 97% and 91% are the difference between a copy and a page worth a second look. `PrivacyPageFailure.detail` is optional and left OFF (not set to undefined) by the five codes that measure nothing, so their `deepEqual` pins still hold. Proven end-to-end: splicing the English body under the German heading in `dist/` WITH one German sentence appended makes `lint:bundle-smoke` exit 1 naming the file and `100% of its policy text`, the state that passed before this change. `__tests__/bundle-smoke.test.ts` 71 → 94 tests, including the real English policy retitled in Spanish and padded (expects exactly `es:near_english_body`), the exact-copy case still reporting `untranslated_body`, the boundary at exactly 0.9 failing, and a floor-vs-shortest-shipped-body check so the ratio can never go quiet on a real page. `npm run verify` exits 0 — typecheck + guards + 4773 tests (+23) + web build. No DB migration, no creds.

## Untranslated-body gate on the privacy pages (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry, filed by the run directly below and taken immediately): the heading gate that shipped an hour earlier reads a translated TITLE as a translated page, which is the state a half-finished translation is actually left in. -->

- [DONE: 2026-08-11] [est: 16000] **Extend the prose comparison from the `<h1>` to the policy body** — the heading gate closes "this file was never touched" and passes "this file was touched once": render `# Datenschutzerklärung` over the English disclosure and `PRIVACY.md.de` satisfies every gate including the new one, while the half a reader is owed under GDPR Art. 12 is still in English. `extractPrivacyPageBodyText` cuts at the first `</h1>` and normalises the remainder — deliberately a cut rather than a `<p>`/`<li>`/`<h2>` selector list, because `renderPrivacyPage` emits head → style → language picker → body, so the heading's close tag is the last piece of page chrome and the check does not need updating every time the markdown converter learns a new block type. Everything ABOVE the cut is byte-identical across all six pages (same style block, same picker), so including it would have made every comparison a comparison of the chrome — the reason the naive "compare the whole file" version of this is worthless. Three decisions worth keeping: (1) heading and body are compared SEPARATELY so the report names which half was not translated, but at most one failure is reported per page — a wholly-English page fails both, and "the heading is English" is the smaller, truer thing to say, so the body message is reserved for what the heading check let through; (2) tags normalise to a SPACE, not to nothing, or `<p>a</p><p>b</p>` and `<p>ab</p>` compare equal, which is a collision two real documents can hit; (3) a null baseline half disables only its own comparison, so a heading-only English page does not brand five heading-only translations copies of it. Proven end-to-end the same way the heading gate was: splicing the English body under the German heading in `dist/` makes `lint:bundle-smoke` exit 1 naming the file and the half. The suite's `renderedPage` fixture grew a body for the same reason it grew a heading last time — it had none, so the body comparison would have been dead against every fixture in the file. `__tests__/bundle-smoke.test.ts` 63 → 71 tests, including one asserting the six REAL rendered pages have six distinct bodies of >200 chars each. Also corrected the test count in the entry below: it recorded `63 → 76`, the true figures are 50 → 63.

## Untranslated-heading gate on the privacy pages (2026-08-11)

<!-- Promoted from .tasks/.suggestions.md (top entry). Every remaining .tasks.md task is still blocked — one [needs-data] on a 30-day PostHog window, two on dependency decisions — so the run took the top suggestion, which is also the last hole in the guard shipped the day before. -->

- [DONE: 2026-08-11] [est: 14000] **Assert each translated `/privacy` page's `<h1>` is actually translated, not the English heading pasted through** — the guard added yesterday proves each of the six pages exists, is a privacy page, carries `<html lang="<code>">` and rendered a body. It cannot see `PRIVACY.md.de` containing the English text, and that is the whole point: the `lang` attribute comes from `renderPrivacyPage`'s ARGUMENT, not from the content, so a translation that was never done satisfies all four gates and ships as a GDPR Art. 12 disclosure in the wrong language. Proven end-to-end, not just in the suite: copying `dist/privacy/index.html` over `dist/privacy/de/index.html` with the `lang` swapped passed `lint:bundle-smoke` before this change and now exits 1 naming the file. The comparison is cross-page, so it lives in `evaluatePrivacyPages`, not `evaluatePrivacyPage` — a page cannot know its own heading is someone else's. Three details worth keeping: (1) `extractPrivacyPageHeading` compares heading TEXT with tags stripped and whitespace collapsed, because `renderInline` runs over the heading and a `**bold**` word on one side would make two identical sentences compare unequal; (2) the English heading is taken ONLY from an English page that passed its own gates, and the check is skipped entirely when it did not — otherwise a missing English page reports one real failure plus five inventions against a baseline that does not exist; (3) an unclosed or empty `<h1>` extracts as `null` rather than as `""`, so it never collides with another `null` and manufactures a match. The suite's `renderedPage` fixture had to grow the language code into its heading — it gave all six pages `<h1>Policy</h1>`, which is exactly the shape now rejected, and a fixture that cannot pass the check it feeds is worse than no fixture. Two tests run the REAL renderer over the REAL tracked `PRIVACY.md.<code>` files: one asserts all six pass, the other re-renders the English markdown into the Spanish path and asserts exactly `es:untranslated_heading` comes back, which is the mutation the first test structurally cannot make on itself. `lib/bundle-smoke.ts` +1 failure code (the message table is exhaustive over the union, so it could not ship without a message), `__tests__/bundle-smoke.test.ts` 50 → 63 tests, `npm run verify` green on all four legs.

## Translated privacy pages in the smoke check (2026-08-10)

<!-- Promoted from .tasks/.suggestions.md — filed by the run directly below and taken immediately, out of order, because it is the only entry in the file whose failure mode has a legal edge: the five GDPR Art. 12 disclosure pages were the surface nothing gated. -->

- [DONE: 2026-08-10] [est: 9000] **Check all six emitted `/privacy` pages, and check them for more than a title** — `build-spa-fallback.ts` writes the canonical English policy at `/privacy/` plus five translated Sentry-disclosure pages at `/privacy/<code>/`, and every check that has ever existed looked at exactly one of them. `PRIVACY_PAGE_TARGETS` is now DERIVED from `PRIVACY_PAGE_LANGUAGES` (importable here: `lib/privacy-page.ts` is node-pure, being what the build scripts already use), so a seventh language is covered the moment that list learns about it rather than when someone remembers. The three-line loop was the easy half; the interesting half is that the existing marker check could not have caught anything subtle, because `renderPrivacyPage` puts `Collectables — Privacy Policy` in the `<title>` of EVERY language — so `renderPrivacyPage("")` produces a page that passes a title grep and contains no policy, and the English page copied into `/privacy/de/` passes it too. Two more codes close both: `empty_body` (no `<h1…>`, which only the rendered markdown emits — the picker and the style block carry no heading) and `wrong_language`. The first draft of `wrong_language` matched a bare `lang="de"` and PASSED a deliberately mis-copied page — the language picker renders `<a lang="de" hreflang="de">` on all six pages, so the loose form is always present; it is now `<html lang="de">`, and the near-miss is pinned as its own case so the loose form cannot come back. `evaluatePrivacyPages` reports every broken page instead of failing fast (a fail-fast loop costs one CI run per language) and refuses an empty input list, the same vacuity rule `evaluateBundleSmoke` follows. 26 more cases in `__tests__/bundle-smoke.test.ts` (64 total): target derivation and the path shapes against what `build-spa-fallback.ts` actually writes, the four-code truth table, the picker near-miss, per-page message targeting, the aggregate's report-everything / one-bad-translation / empty-list behaviour, and — because every gate is a substring check against a template this suite does not own — a case running the REAL `renderPrivacyPage` for all six languages, so a renderer reword that breaks all six at once fails here rather than in CI. Verified functionally: against a real build the guard reports `6 privacy page(s) present and rendered`; copying the English page over `dist/privacy/de/index.html` exits 1 naming the path and the missing `<html lang="de">`; writing a chrome-only page to `dist/privacy/es/index.html` exits 1 on `empty_body`. `npm run verify` exits 0 — typecheck + 11/11 guards + 4729 tests (+16) + web build. No DB migration, no creds.

## Post-build smoke checks off the inline shell (2026-08-10)

<!-- Promoted from .tasks/.suggestions.md, taken OUT of order for the third run running: every entry above it in the file rewrites `verify` or the `test:*` scripts, both of which shipped hours earlier in this same session, and re-litigating a just-shipped file reads as thrash. This is the top of the next block, is independent of that plumbing, and finishes the job the entry directly below started — the three npm guards got the shared premise; the three YAML copies of the smoke check did not. -->

- [DONE: 2026-08-10] [est: 13000] **Route the inline `Bundle smoke check` / `Privacy page smoke check` shell steps through the shared premise and one token list** — the post-build premise work covered the three guards that are npm scripts and stopped there, leaving the checks that live as `run: |` blocks in YAML hand-rolling their own input discovery. There were THREE copies, not the two the suggestion counted (ci.yml, release.yml, deploy.yml), and they had already drifted: release.yml's never grew ci.yml's six-language loop, so a tree-shaking regression that dropped a translation map would fail CI and pass the release gate. New `lib/bundle-smoke.ts` (pure: `BUNDLE_SMOKE_TOKENS` as one flat kind-tagged list built from the three declared arrays, `evaluateBundleSmoke`, `evaluatePrivacyPage` with an exhaustive `Record<PrivacyPageFailureCode, string>` message table) plus `scripts/check-bundle-smoke.ts` (the fs half), wired as `lint:bundle-smoke` and declared in `LINT_ALL_EXEMPT` — it needs `dist/`, so it cannot join the working-tree aggregator. All three workflows now run the one script. Three things the shell could not do. (1) FRESHNESS: `assertBundlePremise` is asserted first, so a `dist/` older than the source tree fails by name instead of being grepped over as if it were current — verified by `touch lib/supabase.ts`, which turns the guard red naming the file and the drift in seconds. (2) EVERY CHUNK: `ls … | head -1` searched chunk one only, so a Metro chunking change moving `marketplaceTitle` into chunk two would fail a check whose premise ("the bundle contains this key") was still true. Every watched token is in chunk one today, so this was latent rather than live — the fix is that the check now asks the question it means. (3) TESTABILITY: the token lists were YAML, so nothing could compare them to the app. The parity block parses the `AppLanguage` union out of `lib/i18n-context.tsx` (which imports react-native and so cannot be imported under `tsx --test`) and fails if a seventh language ships unwatched, checks every watched provider is actually mounted in `app/_layout.tsx`, and checks every watched i18n key still exists — so a renamed key fails the suite instead of failing the build for the wrong reason. Both halves are reported before exiting, for the reason `lint:all` runs every guard: a fail-fast chain would hide a missing privacy page behind a missing key and cost a second CI run to find it. deploy.yml was converted too even though the suggestion only named ci.yml — it publishes the artifact that actually ships, its inline copy was a second definition of the `"Privacy Policy"` marker, and it now gets the bundle half of the check it never ran; the step stays ahead of the sourcemap strip so it inspects the artifact it claims to. Pinned by new `__tests__/bundle-smoke.test.ts` (38 cases: the token evaluator incl. tokens SPREAD across chunks, zero chunks failing rather than passing vacuously, literal-not-regex matching, every-missing-token-not-just-the-first, input purity; the quoted-language rule — a bare `de` matches half the identifiers in any minified bundle — plus no-duplicates and union-of-the-three-lists; the report's per-kind wording and ERROR-only-on-failure; the privacy verdict truth table with unreadable-reported-as-missing and per-code distinct messages; script pins that it takes its chunk list from the premise rather than its own `readdir`, reads every chunk, and reports before exiting; the npm/exempt/three-workflow wiring; and the three parity checks) plus a rewritten `privacy-deploy-smoke.test.ts` (13 cases — wiring and ordering, with the old two-grep shape pinned as a regression in BOTH workflows) and a rewritten `bundle-premise.test.ts` assertion that now fails if ANY workflow re-introduces the `dist/_expo/static/js/web/*.js` glob rather than requiring ci.yml to contain it. Verified functionally: against a real build the guard passes `15 token(s) found across 2 chunk(s)`, a moved-aside privacy page exits 1 on the named message with the bundle half still reported, and a touched source file exits 1 on staleness. `npm run verify` exits 0 — typecheck + 11/11 guards + 4713 tests (+39) + web build. No DB migration, no creds.

## Post-build premise guard (2026-08-10)

<!-- Promoted from .tasks/.suggestions.md, taken OUT of order: the four entries above it in the file all rewrite the `verify` script, which shipped hours earlier in this same session and whose "compose lint:ci, do not re-list its legs" decision is pinned by a test written to say why. Re-litigating that is thrash. This entry is the top of the next block, is independent of that plumbing, and closes the same vacuous-green class as the task directly below — one instance of which was still live. -->

- [DONE: 2026-08-10] [est: 14000] **Give every post-build guard a shared "the input exists and is fresh" premise assertion** — the `sentry-sdk-absent` fix below closed one instance of the vacuous-green class; the three checks that run over `dist/` were the rest of it, and one was actively broken. `check-bundle-secrets` verified `dist/` existed as a DIRECTORY and then scanned whatever the walk returned, so an empty or partially-written export printed `scanned 0 bundle file(s), no secrets leaked` — a green report over zero bytes, from the guard whose entire job is proving a negative about shipped credentials. `check-bundle-size` guarded emptiness but nothing else (0 bytes is comfortably under a 4.5 MiB budget), and `verify-bundle-inlining` is the most exposed of the three because "the secret is not in the bundle" is its FAILURE signal — scanning an empty bundle reports the exact symptom of the bug it exists to catch, from the wrong cause. The second, quieter route to the same green was never checked by any of them: a STALE `dist/`, exported before the change being checked existed, which locally is one forgotten `npm run build` away and looks identical in the log. New `lib/bundle-premise.ts` (pure: `evaluateBundlePremise`, `evaluateScannedFiles`, an exhaustive `Record<BundlePremiseFailureCode, …>` message table so a new code cannot ship without a message) plus `scripts/bundle-premise.ts` (the fs half — the repo's `lib/` is Metro-bundled and no module there imports `node:fs`, which is now pinned rather than left as a convention). All three guards call `assertBundlePremise(CHECK_NAME)` at the top of `main()` and take their chunk list from its return value, so the `dist/_expo/static/js/web/*.js` glob has one definition instead of three hand-written `readdirSync().filter(".js")` copies that could drift into matching different sets. Gate order is most-specific-first — a tree that was never built is also older than its sources, and reporting "stale" there sends the reader looking for the wrong fix — and the staleness comparison is SKIPPED when either mtime is unknown, so a collector that could not stat the tree cannot fail a build on a comparison it never made. The freshness half only works if it never cries wolf, so `BUNDLE_SOURCE_PATHS` covers `app`/`components`/`lib`/`assets` + the config files and deliberately excludes `__tests__`, `scripts` and `.github` (none reach Metro; a test edit invalidating the bundle is how a freshness check gets turned off), with nested `node_modules` skipped so a hoisted dep cannot dominate the newest mtime after every `npm ci`. Pinned by `__tests__/bundle-premise.test.ts` (39 cases): the evaluator truth table incl. the equal-mtime boundary and the null-mtime skip, the formatter's distinctness/rebuild-hint/exhaustiveness, the source-path in/out lists, then a fixture block that drives the REAL collector against temp trees with explicit `utimesSync` mtimes — never-built, dist-without-chunks, sourcemaps-only, fresh, stale, a newer test/script/workflow NOT counting as stale, nested `node_modules` skipped, a missing optional config path tolerated, and a vacuity guard asserting the collector finds this repo's own sources — plus per-guard adoption pins and a regression pin on the original `existsSync(DIST_DIR)` shape. Verified functionally, not only structurally: after a real build all three pass (12 files scanned / 4503.8 KiB / 2 chunks), `touch lib/supabase.ts` makes all three exit 1 naming the file and the drift in seconds, and moving the chunks aside makes all three exit 1 on the empty-glob premise. Deploy ordering audited on the way — `deploy.yml` runs checkout → `npm ci` → `expo export` → verify, and `ci.yml` checks out before it builds, so the bundle is always newer than the tree on CI. `npm run verify` exits 0 — typecheck + 11/11 guards + 4674 tests (+39) + web build. No DB migration, no creds.

## sentry-sdk-absent vacuity guard (2026-08-10)

<!-- Promoted from .tasks/.suggestions.md out of order again: this one is a latent CI bug (a job that can pass having tested nothing), which outranks the two polish entries above it. -->

- [DONE: 2026-08-10] [est: 6000] **Fail the `sentry-sdk-absent` job when the deletion silently no-ops** — `rm -rf node_modules/@sentry` exits 0 whether or not the path was ever there, so the job's entire premise was unverified: an install-layout change (a hoist, a rename, a different package manager, a `npm ci` that partially failed) would leave the SDK in place and the job would report success having exercised nothing. That is the vacuous-green shape the Node 20 outage and the `tsx` type-stripping bug both belonged to, and it was sitting in the one job whose whole purpose is proving a negative. The step now asserts the premise before acting on it and asserts the deletion took effect before running the suites, failing with a named message either way. Both checks go through `node -e "require.resolve('@sentry/react-native')"` rather than `test -d node_modules/@sentry`, because a nested copy under another package (`node_modules/<pkg>/node_modules/@sentry`) resolves perfectly well with the top-level scope gone — precisely the layout a path check would call absent and the module loader would not. There are no nested copies today; the point is that the check stays correct when there are. Pinned by 3 more cases in `__tests__/sentry-sdk-absent-ci.test.ts` (13 total): a pre-delete resolve check exists, a post-delete one exists between the deletion and the run and carries an `exit 1` rather than only printing, and no `test -d`/`test -e` path check has crept back in. Verified by extracting the step body out of the YAML and running it verbatim: with the scope present it deletes and runs 259/259 green; run again with the scope already gone it exits 1 on the premise check with the intended message. `npm run verify` exits 0 — typecheck + 11/11 guards + 4635 tests (+3) + web build. No DB migration, no creds.

## sentry-sdk-absent job off the hand-written runner line (2026-08-10)

<!-- Promoted from .tasks/.suggestions.md, taken OUT of order: the top two entries in the newest block both rewrite `verify` itself, which shipped an hour earlier in this same session, and re-litigating a just-shipped file reads as thrash rather than progress. This one is independent of that plumbing and closes a real asymmetry. -->

- [DONE: 2026-08-10] [est: 8000] **Point the `sentry-sdk-absent` CI job at a script instead of a hand-written `npx tsx --test` line, and write down why it does not typecheck** — it was the last CI path executing suites outside `npm test`, so its runner flags were a copy of `npm test`'s that nothing kept in sync, and the `--import ./__tests__/test-globals.ts` setup file it carries is load-bearing (without the global reset, module state leaks between suites and produces false failures) — i.e. the one flag most expensive to lose was held in place by copy-paste. New `test:sentry` script, and the job's step is now `rm -rf node_modules/@sentry` + `npm run test:sentry`. `npx` was also the wrong launcher here for exactly the reason it is wrong for `tsc`: it resolves the pinned binary only when `node_modules` is populated, and this job's entire premise is deleting part of `node_modules`. The second half of the suggestion asked for a typecheck step *or* a statement of why none is needed, and the honest answer is the latter, in both directions: after the `rm -rf`, `tsc` fails on `lib/sentry.ts`'s `import("@sentry/react-native")` — the types resolve through `node_modules` even though the import is lazy, so the SDK being absent is a type error regardless of the runtime property this job tests — and running the typecheck BEFORE the deletion only repeats the `test` job's named Typecheck step over the same files. That reasoning is now the job's header comment, naming `test` as what covers it, so the next hand-rolled suite-running job copies a decision instead of an accident. Pinned by an extended `__tests__/sentry-sdk-absent-ci.test.ts` (10 cases, +6): the job runs through the script, no executable line in ci.yml runs `npx tsx … --test` at all, the deletion still precedes the run in the same step, `test:sentry` is byte-identical to `test` once the glob is masked out (so a flag can only diverge by failing the suite), the glob is `sentry-*` and matches at least one file with `npm test`'s one-level glob still covering it — a sentry suite reachable ONLY from this job would run with the SDK absent but never with it present, and would never be typechecked — plus the header comment mentioning the typecheck and the `test` job keeping the named step that discharges it. Verified functionally rather than structurally: with `node_modules/@sentry` actually deleted, `npm run test:sentry` runs 256/256 green, then the scope was restored. `npm run verify` exits 0 — typecheck + 11/11 guards + 4632 tests (+6) + web build. No DB migration, no creds.

## One-command verify gate (2026-08-10)

<!-- Promoted from .tasks/.suggestions.md (top entry) — every remaining .tasks.md task is still blocked ([needs-data] on a 30-day PostHog window, two on dependency decisions), so this run took the top actionable suggestion again. -->

- [DONE: 2026-08-10] [est: 10000] **Add a `verify` script and make it the one command the loop runs** — the gate was four entry points (`typecheck`, `lint:all`, `test`, `build`) chained only by `lint:ci`, which covers three of them and omits the build. Every task entry in this file ends by reciting the same four results, which is the shape of a checklist rather than a gate: the sequence was re-assembled by hand on every run and any run could quietly drop a leg. `"verify": "npm run lint:ci && npm run build"` makes it one invocation. It COMPOSES rather than re-lists — spelling the four legs out again would create a second definition of the gate that can drift from `lint:ci`, and `lint:ci` is the one ci.yml's fail-fast ordering was reasoned about against. CLAUDE.md's Commands block now names `npm run verify` as the command to run before committing (with the reasoning: a hand-assembled sequence is exactly how a leg gets skipped), and the PR checklist's two lines (`lint:ci` is green / `npm run build` succeeds — which named no leg wrongly but invited running only one of them) collapse into one `npm run verify` box. ci.yml is deliberately UNCHANGED: its per-leg named steps are what make a red run readable at a glance, and it runs four more things after the build (bundle secrets, bundle size, bundle smoke, privacy page) that have no business in a local pre-commit gate. Pinned by `__tests__/verify-gate-script.test.ts` (14 cases) whose core is a small resolver that expands `npm run <script>` references transitively — splicing in the `pretest` hook, which npm invokes but no command names — so the assertions ask "does verify REACH tsc/lint-all/--test/expo export?" instead of pattern-matching one spelling of the chain. On top of the four-leg reachability check: composition (delegates to `lint:ci`, does not inline its legs), ordering (cheap legs before the slow export), a fail-fast pin rejecting `;`/`||` in the chain, no `preverify`/`postverify` (a hook would make the gate do something its definition does not show), `verify:bundle-inlining` surviving as a separate script and NOT being absorbed (it scans `dist/`, so it only makes sense post-deploy), both doc surfaces, and a drift pair asserting every leg ci.yml runs is reachable from `verify` and vice versa — green locally now means green on CI by construction, which is the class the Node 20 outage belonged to. Verified negatively: rewriting `verify` to drop the build turns 14 green into 5 named failures. `npm run verify` itself exits 0 — typecheck + 11/11 guards + 4626 tests (+14) + web build. No DB migration, no creds.

- [DONE: 2026-08-10] [est: 17000] **Make `npm test` typecheck before it runs, so a type-broken test file can never be green locally and red on CI** — the failure had already shipped twice: `npm test` executes the suites through `tsx`, which STRIPS types without checking them, so `dfdd0a1` reported 4566/4566 green locally and then went red on CI with a TS2345 in a test file. The local order was source → `tsc --noEmit` → write tests → run tests → commit, which leaves exactly one file that can still be wrong — the one written after the typecheck — and nothing in the run says so. Fixed by turning the ordering discipline into a script hook rather than a checklist line: new `typecheck` script (`tsc --noEmit`), new `pretest` hook (`npm run typecheck`) that npm invokes automatically before `test`, and `test:only` as the deliberate escape hatch for iterating on one suite without paying ~5s per run. `lint:ci` now reads `npm run typecheck && npm run lint:all && npm test` — the typecheck leg kept FIRST so a type error costs 5s instead of a full guard fan-out, and ci.yml keeps its own named Typecheck step for the same fail-fast reason even though `npm test` would now catch it anyway. Verified functionally, not just structurally: dropping a `const n: number = "x"` file into `__tests__/` makes `npm test` exit 2 from the pretest hook before a single suite loads. Second bug found on the way, from this environment rather than from the tree: `npx tsc` resolves the pinned devDependency only when `node_modules` is populated and otherwise DOWNLOADS the latest TypeScript major from the registry — on a cold checkout here that was 6.0.2, which reported two errors (`expo/tsconfig.base` not found, `baseUrl` deprecated) that the pinned ~5.9 compiler does not, i.e. a red typecheck with nothing wrong in the repo. Both workflows (`ci.yml`, `release.yml`) now run `npm run typecheck`, which can only ever execute `node_modules/.bin/tsc`. Also corrected CLAUDE.md, which still claimed "There is no test suite" over 4610 tests and recommended `npx tsc --noEmit` — it now documents the five-command gate — and the App Store pre-submission checklist, which spelled out `lint:ci`'s old inline form. Pinned by `__tests__/test-typecheck-prelude.test.ts` (12 cases: the `typecheck`/`pretest`/`test`/`test:only` script shapes, `test` staying free of a duplicate tsc, byte-parity between the two test scripts, `lint:ci` leg ORDER asserted by index rather than by exact string, no `npx tsc` on any executable workflow line, TypeScript still a devDependency, `tsconfig.include` still covering `__tests__`, and a walk asserting no `.test.ts` is parked in a subdirectory where the runner's one-level glob would never execute it). `lint-guards.test.ts`'s byte-for-byte `lint:ci` assertion was the one existing test this broke; it now asserts composition + order, which is the property that actually matters. `npx tsc --noEmit` + 4612 tests (+14) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds.

- [DONE: 2026-08-10] [est: 11000] **Fix the Node 20 harness outage that was still live on `release.yml`, and make the guard derive its own scope** — surfaced by CI on the PR above, not by the tree: the "Release Gate" workflow went red with `# fail 7` over `# tests 4531 / # suites 1077`, against 4612/1095 locally. Same signature as the `ci.yml` outage fixed on 2026-08-09 and the same root cause — `validate` pinned `node-version: 20`, so every suite importing `__tests__/helpers/render.ts` died at LOAD with `registerHooks is not a function` (Node 22.15+). 7 reported failures standing in for 127 suites that never ran; the count is the dangerous half, because a suite that fails to load reports ONE failure instead of its N cases. The previous fix believed "the deploy/release/eas workflows run `npm run build`, never the test harness" — true of `deploy.yml` and `eas-preview.yml`, false of `release.yml`, which runs `npm test` on every pull request. So the outage was live on a second workflow for the entire life of the guard written to prevent it, on every PR, and nobody looked because the failing job was not the one named CI. Pinned `release.yml` to 22 and rewrote `ci-node-version.test.ts` to stop naming files: it now scans `.github/workflows/*.yml`, classifies each by whether its non-comment lines invoke the runner (`npm test`, `npm run lint:ci`, `tsx … --test`), and requires every pin in a harness-running workflow to clear `MINIMUM_NODE_VERSION` — so a future workflow that runs suites is covered the moment it exists rather than when someone remembers to add it. Two assertions guard the derivation itself: one fails if NO workflow is classified as harness-running (which would make every other assertion vacuously true), one re-checks that each file exempted as build-only genuinely invokes no runner. Verified by reverting the pin to 20 and watching the guard name the file and the version. 5 cases (+2). `npx tsc --noEmit` + 4612 tests + `npm run lint:all` (11/11) all green.

## Price-range validation (2026-08-09)

- [DONE: 2026-08-09] [est: 21000] **Add `validatePriceRange(from, to)` and gray out Apply on invalid input** — the filter sheet's price fields accepted anything and failed silently in two directions: a bound that does not parse is SKIPPED by `applyItemFilters` while still counted by `countActiveFilters` (the bar reads "Filters (1)" over a list nothing was filtered out of), and an inverted range parses fine and matches no item at all (an empty collection with nothing explaining why). Both got worse when the draft went sticky — the bad range now survives a dismiss instead of being discarded on reopen. `lib/item-filters.ts` grew `parsePriceBound` (`{value, error}` mirroring `parseCurrencyValueDetailed`'s gate order, with `"negative"` in place of `"non_positive"` because a filter bound of 0 is a legitimate "from free"), `validatePriceRange` returning a flat `PriceRangeError` union, and `PRICE_RANGE_ERROR_I18N_KEY` as an exhaustive record so a new code cannot ship without a message. The sheet disables Apply on a non-null error (plus an `accessibilityState.disabled`, a dimmed CTA and an `role="alert"` inline line), `apply()` re-checks for the paths `disabled` does not cover, and Reset stays enabled — a sticky invalid draft behind a dead Apply with no escape hatch would be worse than the bug. Two bugs fixed on the way, both from `applyItemFilters` using `parseFloat` where the validator used something stricter: `parseFloat("12,50")` is 12, so the comma five of six locales write silently filtered at the wrong price, and `parseFloat("12abc")` is 12, so trailing junk filtered by a number the user never typed. Both bounds now route through `parsePriceBound`, so a range the sheet accepted can never be matched on a different number than the one it validated; an unparseable bound is still skipped rather than treated as no-matches, so filters persisted before this shipped do not empty a collection on upgrade. 4 new i18n keys (en + ru, matching the surrounding filter keys). Pinned by `__tests__/item-filters-price-range.test.ts` (28 cases: the parser truth table, the validator's precedence order, the i18n record in both directions, and an invariant case asserting the validator and the matcher agree on every accepted bound) and `__tests__/item-filters-apply-guard.test.ts` (15 cases through the render harness: the gate per failure mode, the direct-`onPress` bypass, the sheet staying open on a blocked apply, re-enabling on correction, Reset remaining reachable, distinct messages per mode with no raw i18n key on screen, and the error surviving a backdrop dismiss alongside the sticky draft). `npx tsc --noEmit` + 4566 tests (+43) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds.

- [DONE: 2026-08-09] [est: 19000] **Give the date range the same treatment `validatePriceRange` just gave the price range** — `dateFrom`/`dateTo` were raw text fields matched against `item.acquiredAt` with a raw string `<`/`>`, which is correct and timezone-free for zero-padded `YYYY-MM-DD` on BOTH sides and silently wrong otherwise: `"2024-01-15" < "2024-1-1"` is TRUE, so a user who typed the perfectly reasonable `2024-1-1` filtered out most of the year the range was meant to include, with nothing on screen looking wrong. `parseDateBound` accepts `YYYY-M-D` and NORMALISES to the padded shape the comparison is valid for, so the matcher only ever sees dates it can compare; calendar validity is checked arithmetically (full Gregorian leap rule) rather than by round-tripping through `Date`, because `new Date("2024-02-30")` does not throw — it rolls over to March 1st, so a `Date`-based check would accept the typo and filter by a different day than the one on screen. `validateDateRange` returns a `DateRangeError` union separating a wrong SHAPE (`"tomorrow"`, `"01/02/2024"`) from a day that does not exist (`"2024-02-30"`, `"1900-02-29"`) — different codes because the fix differs — with the same precedence as the price range (`from` before `to`, a parse failure before the inversion it would imply) and a matching exhaustive `DATE_RANGE_ERROR_I18N_KEY`. `applyItemFilters` routes both bounds through the same parser, still skipping an unparseable one rather than emptying a collection on upgrade. The sheet gained a SECOND inline slot under the date row (one per range, so both can be wrong at once and fixing one does not swap the other's message out), and Apply is now gated on `applyBlocked = priceError || dateError`. 4 new i18n keys (en + ru). Pinned by `__tests__/item-filters-date-range.test.ts` (26 cases: the parser truth table including every month's last day and the ÷100/÷400 leap edge, the shape-vs-calendar split, normalisation-driven inversion in both directions, the i18n record, and the validator/matcher agreement invariant) plus 6 more in `item-filters-apply-guard.test.ts` (unpadded input accepted rather than blocked, both slots at once, the gate staying closed while the OTHER range is still invalid). `npx tsc --noEmit` + 4598 tests (+32) + `npm run lint:all` (11/11) + `npm run build` all green. Also carries the typecheck fix for the previous task: CI went red on `dfdd0a1` with TS2345 in `item-filters-apply-guard.test.ts` — `Object.values` on an `as const` record narrows to a literal union, so `.includes(string)` does not typecheck. Local `npm test` passes it because `tsx` strips types without checking them; the lesson is that `npx tsc --noEmit` has to run AFTER the last test file is written, not just after the source change.

## CI Node pin (2026-08-09)

- [DONE: 2026-08-09] [est: 9000] **Fix CI red on every harness suite — `ci.yml` ran Node 20** — caught from the Actions run, not from the local tree: `npm test`, `npx tsc --noEmit`, `npm run lint:all` and `npm run build` were all green locally on Node 22.22, while CI reported `# fail 4` and a test COUNT that had silently dropped by 55. The four failures were the four suites importing `__tests__/helpers/render.ts`, each dying at load with `registerHooks is not a function`: `module.registerHooks` shipped in Node 22.15 and both CI jobs pinned `node-version: 20`. The shrinking count is the dangerous half — a suite that fails to load reports one failure instead of its N cases, so coverage can evaporate while the report still looks small. Both jobs now pin 22 (the deploy/release/eas workflows are untouched: they run `npm run build`, never the test harness), the harness throws a named error carrying the running Node version instead of letting the raw `TypeError` surface from a helper nobody is looking at, and new `__tests__/ci-node-version.test.ts` (3 cases) parses `ci.yml` and fails if any pin drops below the harness's exported `MINIMUM_NODE_VERSION`, if a `setup-node` step omits `node-version` entirely (inheriting the runner default), or if the running process lacks `registerHooks`. `npx tsc --noEmit` + 4523 tests (+3) + `npm run lint:all` (11/11) all green.

## BE backend roadmap (decomposed 2026-06-15 from "make all upgrades from .be-suggestions.md")

Source: `.tasks/.be-suggestions.md` (BE-1…BE-37). Implement top-to-bottom, grouped by the doc's phasing. Items needing the live Supabase dashboard or a second project (BE-4 bootstrap, live-schema confirmation) are flagged `[needs-creds]` — the SQL/code/doc/test halves are still implementable offline from the `*-shapes.ts` builders. Each item is independently shippable; keep DB changes mirrored into `MANUAL-TASKS.md` per CLAUDE.md.

## Automatic Configuration (Recommended)

Add Sentry automatically to your app with the [Sentry wizard](https://docs.sentry.io/platforms/react-native/#install) (call this inside your project directory).

```bash
npx @sentry/wizard@latest -i reactNative --saas --org anton-m3 --project collectables
```

The Sentry wizard will automatically patch your project with the following:

 Configure the SDK with your DSN
 Add source maps upload to your build process
 Add debug symbols upload to your build process

## Collection page chunked loading (decomposed from "items should load via chunks to prevent iOS memory leaks")

Context: `app/collection/[id].tsx` is wrapped in a `NestableScrollContainer` (so its outer scroll is a `ScrollView`, not a virtualized list) and has 3 mutually-exclusive render branches for items — (a) owner + drag mode → `NestableDraggableFlatList data={items}` (nested in a ScrollView, so no real virtualization), (b) owner + selection mode → plain `items.map(<SelectableItemRow>)` (unwindowed), (c) viewer/read-only → masonry `items.filter().map(<ItemCard compact>)` across two columns (unwindowed). All three mount every item-card (and its remote image) up-front, which is the iOS memory hot-spot the user flagged. Plan: ship a pure pagination helper first, wire it into all 3 branches behind one "Load more" CTA (minimum viable per the user — pagination is the explicit fallback), then optionally graduate to true `FlatList` windowing once the pagination is proven.

## Virtualization migration (decomposed from "Stretch: replace NestableScrollContainer with FlatList")

Context: chunked pagination already shipped (caps mount count), so the original "stretch" goal of true row-recycling is now incremental. Original task spread three concerns: (1) hoist Hero+filters into a ListHeaderComponent, (2) swap masonry for `FlatList numColumns={2}`, (3) decide drag-mode fate. Decomposed bottom-up so each sub-task ships independently — pure helper first, then refactor on top of it, then UI swap.

## In-collection search + sort (decomposed from the two parent tasks above)

Both extend the existing `ItemFilters` / `applyItemFilters` shape in `components/item-filters.tsx` and surface in the filter sheet, so the decomposition splits each parent into "pure logic" (testable without React) and "UI + i18n" (the visible glue). The drag-corruption defense for sort is intentionally bundled with the UI sub-task because the gate lives in `app/collection/[id].tsx`, not in the pure helper.

## Low-density design-tokens migration (decomposed from "remaining low-density files sweep")

Context: 15 files remain with 1–13 inline hex literals each. `app/wishlist.tsx` was already migrated (line 79). Each per-file sub-task is independently reviewable and follows the established per-file migration pattern (add tokens where needed, import, bulk-replace, add palette + adoption tests). Estimates scale with hex count.

## "Mark as received" buyer confirmation (decomposed from "Persist a Mark as received button on each purchase card")

Context: lets a buyer confirm a marketplace purchase arrived via an `arrivedAt` timestamp, unlocking dispute handling and a "delivery confirmed" signal on the seller's sales card. Decomposed bottom-up — pure data layer first, then local persistence, then buyer UI, seller UI, then cloud sync + migration. Each sub-task ships independently.

<!-- Decomposed 2026-07-04 from "Hide the visibilityChip for private entirely on free accounts once we have analytics confirming nobody clicks the locked chip": the precondition was unverifiable because the locked-chip tap was never instrumented — no event, so "nobody clicks" can never be confirmed. Split into (a) instrumentation, actionable now, and (b) the hide itself, blocked on collected data. -->

- [needs-data] Hide the `visibilityChip` for "private" entirely on free accounts (instead of locking it) once PostHog shows ~zero `premium_upsell_shown` events with `feature = "private_collection"` over a meaningful window (say 30 days after the instrumentation ships) — a cleaner flow at the cost of a less discoverable upgrade prompt. Blocked until that data exists; do not implement before checking the dashboard.

<!-- Decomposed 2026-07-11 from "Add a __tests__/analytics-provider-strict-mode.test.ts that mounts <AnalyticsProvider> under <React.StrictMode>": the repo has no React mounting harness (no jsdom, no react-test-renderer — only the fake-dom stub), so a real StrictMode mount is blocked on a dev-dep decision. Decomposed bottom-up: pure scheduler first (node-testable now, mock timers), provider wiring second, the actual mount test last (blocked). Note the 500ms identify debounce already shipped — the double-mount invariant is now "two rapid schedules collapse into one identify". -->

- [DONE: 2026-08-09] [est: 24000] **Add the real `__tests__/analytics-provider-strict-mode.test.ts`** — shipped, still with no new dev-dep. `__tests__/helpers/render.ts` grew the two pieces this needed: `<StrictMode>` reproduction (each body invoked twice per pass with the HOOK CURSOR REWOUND between the calls, and every effect in the strict subtree mounted → cleaned up → mounted again) and `mockModule(specifier, exports)`, which aliases a module onto a plain object for the rest of the process. The provider consumes four sibling contexts that reach supabase and the network, so `@/lib/auth-context`, `@/lib/i18n-context`, `@/lib/premium-context`, `@/lib/diagnostics-context` and `@/lib/analytics` are mocked while the provider and the REAL `createIdentifyScheduler` stay untouched — the point is the wiring between them. 9 cases: one identify for a signed-in user under StrictMode and the identical count without it, never synchronous (the debounce), one more per REAL user change but none for a re-render at the same identity, a premium+language change landing mid-window collapsing into a single identify carrying the SETTLED traits, synchronous reset on sign-out, no reset when the pending identify was cancelled before it fired, no timer surviving the remount, plus `optedOut` as the inverse of the diagnostics toggle and the provider-order error from `useAnalytics`. Writing it found a real harness bug the app does not have: the strict double-invoke was not rewinding `hookIndex`, so the second call allocated a fresh cell set and the provider got a brand-new `useRef` — two schedulers, two timers, two identifies. That is precisely the state-identity class StrictMode exists to surface, so it is now pinned in `render-harness.test.ts` (+6 cases: cursor rewind, effect remount, non-strict effects mounted once, cleanup-before-re-run on a dep change, and `mockModule`'s named/default/non-function exports plus its invalid-identifier guard). Mocked modules are written as real `.mjs` files in a temp dir rather than served from a `data:` URL or a synchronous `load` hook — both of those fight the async tsx loader, and the reasoning is doc-commented so it is not re-litigated. `analytics-identify-debounce.test.ts`'s header comment said importing the provider at runtime was impossible; it now says why it stays structural anyway (a provider re-rolling an inline `setTimeout` would pass every runtime assertion). `npx tsc --noEmit` + 4509 tests (+16) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds.

- [DONE: 2026-08-09] [est: 14000] **Add a `__tests__/analytics-language-roundtrip.test.ts`** — shipped, no new dev-dep. The real `<I18nProvider>` mounts through the render harness (react-native and AsyncStorage are aliased to stubs; only `@/lib/analytics` is mocked, so the provider, its `useMemo` and the whole 2000-line translation table are the real thing) with a probe component inside it holding the live context value. 6 cases: en → ru → ru fires `language_switched` exactly twice, each event carries `previousLanguage` so the direction is recoverable, nothing fires on mount, the no-op repeat still APPLIES the language (suppressing the event must not suppress the state — the failure mode a naive `if` around both would create), `t()` actually returns different copy after the switch, and a walk across all six offered locales with each hop immediately re-selected fires once per hop and never for a repeat. `setLanguage` resolves outside a render pass, so the helper re-renders after each call — that is where the provider's `useMemo` rebuilds — and `beforeEach` clears the AsyncStorage stub's module-scope store so one case's persisted language cannot reach the next case's mount. `npx tsc --noEmit` + 4515 tests (+6) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds.

### ItemCard design-tokens migration (2026-05-25)

- [needs-dep-decision] Migrate `LazyPhoto` onto `expo-image` for real offscreen deferral (`priority="low"`, `recyclingKey`, built-in transition) — blocked on adding the `expo-image` dependency (new package + lockfile churn; decide alongside the jsdom/react-test-renderer dev-dep decision the `[needs-dev-dep]` tasks are waiting on). The `LazyPhoto` component shipped 2026-07-16 is the single adoption point, so the migration is a one-file swap when the dep lands.

### EmptyState design-tokens migration (2026-05-25)

- [needs-dev-dep] **Migrate the `borderStyle: "dashed"` rendering test** [blocked 2026-07-17: a headless-browser screenshot test needs a browser-automation dev-dep (`@playwright/test` or similar) that `npm test`'s plain `tsx --test` harness doesn't have — decide it alongside the jsdom/react-test-renderer dev-dep the `[needs-dev-dep]` analytics tasks wait on] — React Native Web renders dashed borders inconsistently across browsers (Safari + Firefox honor it; Chrome on Android sometimes drops it). A visual regression test using a headless browser screenshot would catch silent drops, especially since `EmptyState` is the only component in the design-tokens-migrated set that uses `borderStyle: "dashed"`. Without it, a future React Native Web upgrade could mute the visual without anyone noticing.

### In-collection search UI (2026-05-23)

- [DONE: 2026-08-08] [est: 22000] **Debounce the `draft.query` keystrokes via a shared `useDebouncedValue(value, 150)` hook before threading them into `onChange(draft)`** — shipped the shared hook (`lib/use-debounced-value.ts`) with its pure half in `lib/debounce-helpers.ts` (`DEFAULT_DEBOUNCE_MS = 150`, `PROFILE_SEARCH_DEBOUNCE_MS = 300`, `resolveDebounceDelay`, `isImmediateDelay`, `commitDebouncedValue`) so it stays node-importable under `tsx --test`, exactly like `share-link-helpers.ts`. One deviation from the description, deliberate and pinned by a test: the hook is NOT wired into `ItemFilterBar`. The sheet's Apply reads `draft` directly, so debouncing the value handed to `onChange` would apply a stale query to anyone who taps Apply within the window — the debounce belongs on the live-filtering path, which does not exist yet (filed as the first follow-up in `.suggestions.md`). Instead the hook was adopted where per-keystroke work happens TODAY: `app/people.tsx` and `components/search-overlay.tsx` each hand-rolled their own `setTimeout(..., 300)` inside a `useEffect` around a `searchProfiles()` round trip, and both now share one implementation and one constant. Three things the shared version fixes for every future consumer: `resolveDebounceDelay` clamps `NaN`/`Infinity`/negatives (an `Infinity` delay would have stalled the settle forever), `commitDebouncedValue` returns the CURRENT reference on an unchanged value so a settle can't invalidate a downstream `useMemo`, and a `0` delay takes a no-timer path rather than `setTimeout(fn, 0)` (which would still cost one stale render). The in-flight `cancelled` guard is kept at both sites so a late response can't overwrite a newer one. Pinned by `__tests__/use-debounced-value.test.ts` (43 cases: pure-helper units incl. `-0`, idempotence and the `Object.is`-vs-`===` NaN case; structural pins on the hook's dep array — listing `settled` would turn the debounce into an interval — plus per-consumer adoption pins, a no-hand-rolled-timer pin, and the ItemFilterBar non-adoption decision recorded as an assertion). The two `profiles-search.test.ts` pins that named the old inline variables were updated to require the hook. `npx tsc --noEmit` + 4139 tests + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — today the search field only takes effect after the user taps Apply, so per-keystroke filtering hasn't been added yet. When the natural next step lands (live-as-you-type filtering directly from the bar without opening the sheet), debouncing protects collections with thousands of items from re-running `applyItemFilters` on every keystroke. A 150ms debounce shim is the standard tradeoff between feeling instant and not thrashing the matcher.

- [DONE: 2026-08-08] [est: 8000] **Mirror the `searchInCollectionPlaceholder` shape with a `searchInCollectionA11y` accessibility label + `accessibilityRole="search"` on the new TextInput** — shipped in `components/item-filters.tsx`, with two additions the audit turned up while wiring it. (1) The clear button next to the field was an icon-only `Pressable` with no `accessibilityRole` and no label at all — strictly worse than the input it sits beside, since a screen-reader user got an anonymous tappable element with no way to know it wipes the query. It now carries `accessibilityRole="button"` and a new `filterClearSearch` key. (2) Both `Ionicons` in the row (the magnifier and the clear glyph) were focusable but unnamed, so the fixed input was still reached by swiping past anonymous elements; both are now hidden from the accessibility tree with `accessibilityElementsHidden` AND `importantForAccessibility="no"` — shipping only one prop leaves the other platform announcing the icon, which a test now pins by requiring the two counts to match. The label is a separate i18n key from the placeholder on purpose: a placeholder is length-capped by the field width, a spoken label is not, so `searchInCollectionA11y` is the fuller phrase ("Search items in this collection by title" vs "Search items by title") and a test asserts the two differ — and that the label is the longer of the pair — in all 6 languages. Both new keys are localized in en/ru/be/pl/de/es rather than falling back to en. Pinned by 12 new cases appended to `__tests__/item-filters-search-ui.test.ts`. `npx tsc --noEmit` + 4151 tests (+12) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — VoiceOver currently reads "Search by title, edit text" because the input is unlabeled beyond its placeholder. A 1-key i18n addition + 2 props would close the gap and let blind users discover the search affordance from the sheet structure rather than waiting for the placeholder to be announced after focus.

- [DONE: 2026-08-08] [est: 11000] **Surface the active `query` as a removable chip in the `ItemFilterBar` quickChips row** — shipped as a sibling of the existing sort chip, sharing its amber "applied value" fill so the two read as one family of active-narrowing chips. Four decisions worth recording. (1) The chip renders `filters.query.trim()`, not the raw value — that is what `applyItemFilters` actually matches on, so showing the raw string would claim a leading space is part of the needle, and a whitespace-only query (which narrows nothing) correctly renders no chip at all. (2) It reads from `filters`, never from the sheet's `draft`: a chip driven by uncommitted draft state would name a narrowing the user typed but never applied. Pinned by a test, since `draft` is in scope at that point and the mistake would look right. (3) `clearQuery()` mirrors `clearSort()` in touching only its own field — the `reset()` chip beside it wipes everything, and a ✕ that silently dropped the user's price range would be the same bug the sort chip's comment already warns about. (4) The chip is width-capped (`maxWidth: 180` + `flexShrink: 1` + `numberOfLines={1}`) because the needle is unbounded user input: without the cap, one long query becomes a chip wider than the screen and pushes the sort and reset chips past the end of the horizontal scroll, where nothing hints they exist. New `queryChipClear` i18n key interpolates the needle into the spoken label in all 6 languages (a bare "tap to clear" would tell a non-visual user that something is narrowing the list but not what), and both decorative icons are hidden from the accessibility tree with the paired iOS/Android props established in the previous task. Pinned by 10 new cases in `__tests__/item-filters-search-ui.test.ts`. `npx tsc --noEmit` + 4161 tests (+10) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — today the search query is invisible until the sheet is reopened, which makes it easy to forget you've narrowed a collection by a stale needle. A "🔎 needle ✕" chip in the horizontal scroll bar (analogous to the existing `resetChip`) would give one-tap clear without the modal round trip and let users see the active narrowing at a glance.

- [DONE: 2026-08-08] [est: 14000] **Extract the new search-row markup into a reusable `<SheetSearchRow value placeholder onChange />` component** — shipped as `components/sheet-search-row.tsx` (memoized, controlled, ~120 LOC incl. the decision comments). The audit found THREE hand-rolled copies, not the two the task named: `app/create.tsx` (collection picker), `components/currency-sheet.tsx` (currency picker) and `components/item-filters.tsx` (filter sheet) — and the three had already drifted apart in four ways, which is the whole argument for the extraction. Resolved toward the majority shape and pinned each decision: input `fontSize: 15` + `FONT_BODY_SEMIBOLD` (item-filters was 14 with no family at all), magnifier tinted `MUTED_13` (item-filters used `MUTED_15`, the placeholder-weight tint — too light for a persistent affordance), and `borderRadius: RADIUS_INPUT` instead of the bare `16` all three repeated (the token exists and its doc comment literally says "text inputs, search rows"). The fourth and largest drift was accessibility: only the filter sheet had ever been made screen-reader-complete, so the two picker sheets shipped an unlabeled input plus an anonymous icon-only clear button. Baking the a11y into the component rather than leaving it to props is what makes that unrepeatable — both `Ionicons` are hidden with the PAIRED iOS/Android props and the clear chip is a named `button`, for every consumer, including future ones. Two decisions worth recording: the clear chip's label is read from i18n inside the component (`filterClearSearch` is identical in every sheet, so a per-consumer prop would just be four copies waiting to drift), while `placeholder` and `accessibilityLabel` stay caller-owned (they are per-sheet, and defaulting the label to the placeholder would re-couple a length-capped string to a spoken one). Two new i18n keys — `collectionPickerSearchA11y`, `currencyPickerSearchA11y` — localized in all 6 languages rather than falling back to en; a test pins them as distinct from each other, since "Search" twice tells a non-visual user nothing about which sheet they are in. Step 7 scan turned up the same gap in the two search fields that legitimately can NOT use the shared row and were therefore never going to be fixed by the extraction: `components/search-overlay.tsx` (carries a trailing close-overlay button the component has no slot for, and sizes its chrome a step up) had an unlabeled input and TWO anonymous icon-only Pressables, and `app/people.tsx`'s profile search relied on a visible sibling `<Text>` that RN never binds to the input — there is no `htmlFor`, so it announced as "edit text". Both fixed in place with the same contract, plus `searchOverlayA11y` / `searchClose` keys in all 6 languages. Pinned by the new `__tests__/sheet-search-row.test.ts` (35 cases: markup order, controlled-not-stateful, clear-only-when-clearable, the Clarity-mask wrapper, each resolved drift, per-consumer adoption + a no-private-copy-of-the-styles pin on all three, the two out-of-band fields, and the i18n coverage). `item-filters-search-ui.test.ts`'s markup-level pins were rewritten as delegation pins, and `design-tokens.test.ts` gained a `sheet-search-row.tsx` adoption block while `app/create.tsx`'s dropped the two tokens that left with the row. `npx tsc --noEmit` + 4196 tests (+35) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — `app/create.tsx` (collection picker) and `components/item-filters.tsx` (this task) both declare the same `<View><Ionicons name="search"/><TextInput/><Pressable><Ionicons name="close-circle"/></Pressable></View>` shape with near-identical styles. A ~30 LOC component plus a 5-case structural test would let any future sheet (people picker, template picker, marketplace filter) drop in the same affordance without re-implementing it.

- [DONE: 2026-08-08] [est: 9000] **Add a `__tests__/i18n-translations-no-stale-placeholder.test.ts` audit that asserts every placeholder-typed string ends in a `?`-or-`.`-free phrase shorter than 40 characters** — shipped, but with the rule corrected against the actual corpus, because the description's version fails on 10 strings that are all correct as written. Two deviations, each pinned. (1) ONE global cap is wrong: four keys (`descriptionPlaceholder` at 87 chars in German, `marketplaceNotesPlaceholder`, `collectionDescriptionPlaceholder`, `chatInputPlaceholder`) render into `multiline` textareas several lines tall, where a full sentence of guidance fits and genuinely does not on a single-line input. The cap is now per field shape — 40 for single-line, 90 for multiline — and a test asserts both shapes exist so neither cap can quietly become dead code. Every single-line placeholder in the repo already passes at 40, so the number is a real ceiling and not a rubber stamp. (2) "`.`-free" bans idiom, not sentences: a trailing `...`/`…` is the standard "keep going" hint (`Write a message…`, `Search items, collections, people...`) and `e.g.` / `np.` / `z. B.` / `p. ej.` open hints in five of the six languages. The banned shape is a placeholder written as a SENTENCE — ends in a single `.`, or contains a `?` — which is what actually runs long. The property that makes this audit hold up is that the key list is DERIVED from the JSX rather than hardcoded: a new `placeholder={t("…")}` anywhere under `app/` or `components/` is covered the moment it is written, which is the opposite of the per-key tests elsewhere in this repo. The element scan walks OUT from the placeholder prop to the nearest enclosing tag instead of matching a list of component names — `app/create.tsx` renders 5 of its placeholders through a local `<Field>` wrapper and `currency-input.tsx` / `sheet-search-row.tsx` through their own props, so a name-based scan silently skips them, which is the blind spot that would make the audit worthless. Three structural guards ride along: the scan must find ≥20 usages in both source dirs (a refactor behind one wrapper would otherwise leave the suite passing vacuously), every key it finds must resolve to a real declaration (catches a typo'd `t("itemTitlePlaceholdr")`, which type-checks and renders an empty hint at runtime), and no key may be rendered as both shapes (it could only satisfy the stricter cap, making the looser one a lie). Verified the audit bites by temporarily lengthening `searchInCollectionPlaceholder` into a 48-char question — both the cap and the question-mark rule failed, then passed again on revert. 53 cases, all derived. `npx tsc --noEmit` + 4249 tests (+53) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — the new `searchInCollectionPlaceholder` is the 12th placeholder-typed i18n key; without a structural cap, future contributors can drift into long sentences that get truncated on narrow phones (placeholders aren't ellipsised in React Native's `TextInput`). A simple regex/length audit caught at CI time prevents the regression class.

### Home-screen design-tokens migration (2026-05-23)

- [DONE: 2026-08-08] [est: 8000] **Promote the `[HERO_DARK_4, HERO_DARK, HERO_DARK_5]` triplet to a shared `lib/gradients.ts` constant** — shipped, with two changes to the plan the codebase forced. (1) It is THREE copies now, not the two the task counted: `components/login-screen.tsx` grew a third while this sat in the backlog, which is the extraction argument making itself. (2) The export is a spreadable PROP BUNDLE, not a bare colour array. All three sites also shared `start={{ x: 0.2, y: 0.6 }}` / `end={{ x: 1, y: 0 }}`, and the angle is as much the recipe as the stops are — a fourth site that imported `HERO_DARK_GRADIENT` for the colours and re-typed (or forgot) the angle would render a visibly different banner while reading as correct in review. `<LinearGradient {...HERO_DARK_GRADIENT} style={styles.hero}>` makes that drift impossible, and collapses a 6-line element to 1 at each site. The scan for the first recipe turned up a SECOND duplicated one the task never mentioned: the cover-photo scrim `["rgba(34, 24, 17, 0.08)", "rgba(34, 24, 17, 0.55)"]`, byte-identical in `components/collection-card.tsx` and `app/collection/[id].tsx`, invisible to `lint:hex` because that guard only scans `#rrggbb` and these are `rgba()`. Hoisted as `PHOTO_SCRIM_GRADIENT` — deliberately with NO `start`/`end`, since the default top-to-bottom axis IS its recipe and adding an angle would silently re-aim every cover photo (pinned as an assertion). Typed `as const satisfies Pick<LinearGradientProps, …>` because expo-linear-gradient 15 types `colors` as `readonly [ColorValue, ColorValue, ...ColorValue[]]` — a plain `string[]` does not satisfy it. The scrim stays raw `rgba()` rather than becoming tokens: it is `HERO_DARK_2` at two alpha tiers and the repo still has no `withAlpha()` helper (the `OVERLAY_*` family filed repeatedly in `.suggestions.md`), but centralising the literal means that swap is now a one-file edit. Pinned by `__tests__/gradients.test.ts` (11 cases). The load-bearing ones are the adoption sweeps, not the value checks: two repo-wide scans fail if any file re-declares either recipe inline, and a third asserts that EVERY `<LinearGradient>` under `app/`+`components/` either spreads a shared bundle or is on a one-entry allow-list (`components/skeleton.tsx`, whose shimmer is animation chrome with its own highlight colour) — so a new inline gradient anywhere fails CI rather than quietly becoming copy number four. `design-tokens.test.ts`'s three hero blocks dropped the tokens that left with the recipe; `login-screen.tsx` used all three ONLY in that gradient, so its `HERO_DARK` pin went too. `npx tsc --noEmit` + 4260 tests (+11) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — the home screen now declares the same dark gradient as the settings hero. Two duplicated 3-stop arrays is the trigger for extraction: a single `HERO_DARK_GRADIENT` export would let both files import one constant, and the upcoming login-screen-redux migration would close the loop entirely. Pairs naturally with the `withAlpha()` helper for the eventual `OVERLAY_*` family.

- [DONE: 2026-08-08] [est: 26000] **Extract the `<HeroBanner />` shape into a shared component** — shipped as `components/hero-banner.tsx`, covering EIGHT screens rather than the three the task counted. The scan the extraction forced is the whole story: `app/index.tsx`, `app/settings.tsx` and `components/login-screen.tsx` draw the banner over `HERO_DARK_GRADIENT`, but `app/people.tsx`, `app/friends.tsx`, `app/chats.tsx`, `app/marketplace.tsx` and `app/collections-feed.tsx` draw the identical `eyebrow + title + subtitle` block over a flat `HERO_DARK` fill — same content shape, same tokens, eight private stylesheets. Tone is therefore the only prop that varies (`tone?: "gradient" | "solid"`, defaulting to the gradient); everything else is one declaration. Resolved by simple majority across all eight and pinned each decision by value, because "which of the eight shapes won" is the one thing a future contributor cannot recover from the diff: `gap: SPACING_LIST` (6 screens, over home + login's `SPACING_CARD`), `borderRadius: RADIUS_HERO_LG` (5, over the bare `32` that login, friends and the feed each inlined — same number, but invisible to a retheme AND to `lint:radius`, which only bans a fixed set of magic values), `padding: SPACING_GUTTER` (all eight were `24`; only home routed it through the token), title `28`/`36`/`800`. Four of the drifts were outright bugs, fixed by construction rather than by hand: the collections-feed headline declared NO `fontFamily` at all and had been silently rendering in the platform default face beside seven serif siblings; friends used `FONT_DISPLAY` (Syne) where five screens used `FONT_DISPLAY_EDITORIAL`; chats, marketplace and the feed had all omitted `FONT_BODY_EXTRABOLD` from the eyebrow, so their kickers rendered in the system face beside DM Sans ones; and marketplace coloured its title `PAGE_BG` instead of `TEXT_ON_DARK_3` — two near-identical creams, one of them semantically a page background that a themed page would have dragged the headline along with. Subtitle `fontSize` was declared on only two of seven copies, so the same supporting line rendered at RN's default 14 on five screens and 15 on two. Three structural decisions worth recording. (1) There is deliberately NO `style` prop: an override slot would let the next screen re-open every one of those drifts while still reading as an adoption in review, and a test asserts the prop's absence. (2) The home screen's account row is a `headerSlot`, not `children` — the ordering IS the recipe, and a row appended after the copy would push the sign-out control below the fold on a short viewport while looking like a faithful port. (3) Both tones render one shared `body` fragment rather than two branches that each re-list the slots, which would be the extraction's own drift reintroduced one level down; a test counts `styles.title` exactly once. `accessibilityRole="header"` on the title is new — none of the eight copies had it, so screen-reader users had no landmark and had to swipe the banner linearly. Pinned by the new `__tests__/hero-banner.test.ts` (27 cases: markup order, tone branching, the no-style-prop decision, every resolved drift asserted against the imported token so a token value change cannot silently un-resolve it, per-consumer adoption on all eight, and two repo-wide sweeps that fail if any file re-declares the eyebrow recipe or fills a container with `HERO_DARK` behind one). Both sweeps were verified to bite by temporarily re-adding a fake hero to `app/chats.tsx`. The eyebrow sweep is anchored on the FULL recipe (amber + 12pt + uppercase + 1.2 tracking) rather than any two of its lines, because `app/listing/[id].tsx` renders a legitimately different 11pt amber kicker in its sold-banner and a looser regex would flag it forever. `gradients.test.ts`'s "the three hero screens spread the bundle" pin became "`HERO_DARK_GRADIENT` has exactly one consumer"; five `design-tokens.test.ts` adoption blocks dropped the tokens that left with the banner; `spacing-gap-adoption.test.ts` flipped marketplace + collections-feed to `list: false` (the hero gap was their only `SPACING_LIST` use) and added the new component. Net −194 LOC. `npx tsc --noEmit` + 4289 tests (+29) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — the home screen's hero card (`LinearGradient` + eyebrow + title + profile row + actions row) is the third near-identical hero shape across the codebase (settings, profile detail). A component with optional slots would let those screens use one widget and centralise the `HERO_DARK_GRADIENT` gradient + `MUTED_18` border + `TEXT_ON_DARK_3` title recipe in one file.

- [DONE: 2026-08-08] [est: 14000] **Extract `<DashboardBanner icon title hint onPress />` from the wishlist + stats banner blocks** — shipped as `components/dashboard-banner.tsx`. The API takes `href` rather than the `onPress` the task sketched, and that is the one decision worth arguing: both call sites are `<Link asChild>`, and `Link asChild` renders its child through Radix's `Slot`, which CLONES the child with `href`/`onPress` props. A plain function component in that position has to forward them by hand, and forgetting to is a silently dead row — no type error, no crash, just a banner that does nothing. Owning the `<Link>` inside the component keeps the two adjacent in one file and makes the failure mode impossible. The related hazard is pinned rather than commented: the Pressable's style is an object spread, never an array, because `Slot.mergeProps` merges styles with a plain object spread and an array arrives at the DOM as `{0: …, 1: …}` — the `CSSStyleDeclaration` crash that took the deployed web build down on 2026-08-02. Both originals were hand-flattened; that property is easy to lose when markup moves between files, so `dashboard-banner.test.ts` parses the `<Pressable>` element and asserts it. Tone (`"amber"` | `"dark"`, defaulting to dark) drives three COUPLED values — surface, border and icon disc — which is why it is one prop and not three: three independent props would let a caller mix a dark disc into an amber row and still type-check. Three drifts resolved on the way through: `borderRadius: 20` → `RADIUS_CARD_SM` and `gap: 14` → `SPACING_SECTION` (both tokens already existed; both copies inlined the literals, and `gap: 14` slipped past `lint:radius`, which only bans 10 / 12 / 8), and the icon glyph's `fontSize` unified at 22 — the wishlist star rendered at 22 and the stats glyph at 20 with nothing behind the split. The two rows also stop threading `theme` through the call site: the component reads `useAppTheme()` itself. Pinned by `__tests__/dashboard-banner.test.ts` (15 cases: slot order, the Link-ownership and object-style rules, theme sourcing, each resolved drift asserted against the imported token, per-row adoption incl. the stats row deliberately passing no tone so the default stays exercised, and a repo-wide sweep for a third hand-rolled chevron row). The sweep is anchored on the trailing `›` glyph rather than on the 44×44 disc: the disc geometry is generic enough that `app/chat/[id].tsx`'s send button and `search-overlay.tsx`'s result thumb share it without being this row, and `app/settings.tsx`'s currency chevron is allow-listed because it trails a settings list row with no disc, no hint and no route. `design-tokens.test.ts` gained an adoption block and `app/index.tsx`'s dropped `HERO_DARK` + `theme.bannerBg`, which left with the rows. `npx tsc --noEmit` + 4305 tests (+16) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — the home screen now declares two near-identical "icon circle + title + hint + arrow" banner blocks (`wishlistBanner` and `statsBanner`) totaling ~80 LOC of style declarations. Extracting them would close the duplication and let the marketplace screen's upcoming "Sales" banner reuse the same widget without re-implementing the recipe.

- [DONE: 2026-08-08] [est: 16000] **Add a `useDebouncedValue(refreshing, 100)` shim on the home screen's `handleRefresh`** — shipped the OUTCOME the task wanted (no spinner flash when `refresh()` resolves from cache) with the opposite primitive, because the one named would have made it worse. `<RefreshControl>` is already on screen while the user drags it: if `refreshing` never goes true, the control retracts the moment they let go. A 100ms leading debounce therefore does not suppress the flash — it adds a second one, letting the control snap shut on release and re-open 100ms later. The symptom calls for a TRAILING hold: show immediately, stay up for at least `minMs`. That distinction is the first thing `lib/minimum-visible-helpers.ts` states, and the rejected approach is pinned by an adoption test (`nobody reached for the debounce hook to solve this`) so it cannot quietly come back on the next reading of this backlog entry. Shipped as `lib/use-minimum-visible.ts` with its pure half in `lib/minimum-visible-helpers.ts` (`MIN_SPINNER_VISIBLE_MS = 400` — roughly one indicator rotation, below which a spinner reads as a glitch rather than as feedback — plus `resolveHoldDelay` and `remainingHold`), split exactly like `use-debounced-value.ts` / `debounce-helpers.ts` so the logic stays node-importable under `tsx --test`. Three edge cases are decisions rather than accidents, each pinned: a second refresh started INSIDE the first one's hold does not restamp (it would otherwise extend the hold indefinitely); a flag that never went up never lingers (first render, or a handler that bails before `setRefreshing(true)` — lingering there would show a spinner the user never triggered); and a backwards clock jump yields the FULL hold rather than zero, since 400ms of extra spinner is a strictly better failure than the flash this exists to prevent. `Infinity` and `NaN` holds both fold to 0 — an `Infinity` would have pinned the spinner up forever, and `NaN` degrades inside `setTimeout` silently. Step 7 scan: the flash is not a home-screen bug, it is the same six lines copy-pasted onto SEVEN screens (`index`, `stats`, `people`, `profile/[id]`, `item/[id]`, `collection/[id]`, `wishlist`), all running `setRefreshing(true); try { await refresh(); } finally { setRefreshing(false); }`. All seven now hold, including the two that drive a `<FlatList>`'s own `<RefreshControl>` rather than `<Screen>`. One real bug caught during the sweep, by an existing guard rather than by review: the first placement in `app/collection/[id].tsx` put the hook call after that file's early returns, which is the exact 'Rendered more hooks than during the previous render' crash `collection-detail-hook-order.test.ts` was written for. Moved above the first early return. Two older tests that named the raw flag in their regexes (`collection-detail-vm-d-viewer-scroll`, `wishlist-flatlist-migration`) were updated to `showRefreshing`. Pinned by `__tests__/use-minimum-visible.test.ts` (20 cases: pure-helper units incl. `-0`, the clock-skew path and the degenerate holds; structural pins on the hook's leading-edge contract, its ref-not-state bookkeeping, its cleanup and its dep array — listing `lingering` would restart the timer forever, the same trap `useDebouncedValue` documents; then per-screen adoption on all seven plus a no-raw-flag sweep). `npx tsc --noEmit` + 4325 tests (+20) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — today the `setRefreshing(true)` toggle flashes the pull-to-refresh indicator even when the underlying `refresh()` promise resolves synchronously (e.g. when offline and the local cache is fresh). A 100ms debounce shim would suppress the brief flash on the happy path while still showing the spinner on real network round trips. Pairs naturally with the `useDebouncedValue` hook suggested for the people-screen search debounce.

- [DONE: 2026-08-09] [est: 14000] **Pin a `__tests__/index-recent-items.test.ts` that asserts `recentItems` filters out wishlist items AND non-owned collections** — extracted the memo body into `selectRecentItems` (`lib/home-helpers.ts`) so the contract is node-testable, then pinned all three exclusions (wishlist, archived, non-owned), the newest-first ordering, the `RECENT_ITEMS_LIMIT` cap, the negative-limit clamp and non-mutation, plus a structural check that `app/index.tsx` uses the selector and does not re-inline the filter. The shared "owned + not wishlist + not archived" predicate moved to `selectOwnedActiveItems` / `ownedCollectionIds` in `lib/collections-helpers.ts` (pinned by `__tests__/owned-active-items.test.ts`), which surfaced and fixed a real bug: `app/stats.tsx` filtered on `isWishlist` alone, so archived (sold) items still counted toward `totalValue`, the growth chart and the condition breakdown; its `ownedCollections` was also rebuilt every render, making the `ownedItems` `useMemo` a no-op.

### Settings design-tokens migration (2026-05-23)

- [DONE: 2026-08-09] [est: 16000] **Batch-migrate the new `AMBER_SOFT_3` (`#f0d6a1`) across the 3 remaining inline consumers** — `components/empty-state.tsx` had already been migrated, so `lib/toast-context.tsx` was the last inline copy. Migrating one value out of its 3×4 `PALETTES` table would have left a half-tokenised literal, so the whole file went through instead: 9 new tokens (`CARD_BG_15`, `DANGER_SOFT_6/_7`, `DANGER_DEEP_7/_8`, `SUCCESS_GREEN_3`, `SUCCESS_SOFT`, `SUCCESS_SOFT_2`, `SUCCESS_DEEP`) plus the 4 that already existed (`AMBER_SOFT_3`, `AMBER_ACCENT`, `HERO_DARK_2`, `HERO_DARK_9` for the shadow). That let `lib/toast-context.tsx` come off the `HEX_ALLOWLIST` in `lib/check-inline-hex.ts` — the file is now guarded by `npm run lint:hex` like any other module, which is a stronger guarantee than the adoption test. Pinned by `__tests__/toast-design-tokens.test.ts`: token values + aggregate membership, per-slot palette mapping (so a copy-paste giving the error toast the success accent fails), a soft-before-deep luminance ordering check on both new families, no-value-aliases-an-existing-token, and a zero-inline-hex assertion run through the lint's own matcher.

- [DONE: 2026-08-09] [est: 11000] **Introduce a cool-palette family (`COOL_GRAY`, `COOL_SLATE`, `COOL_OFFLINE`) for system/diagnostic UI** — shipped the 3-token axis the body specified: `COOL_GRAY_LIGHT` (`#cbd5e1`) / `COOL_GRAY` (`#94a3b8`, unchanged so the settings diagnostics toggle is untouched) / `COOL_GRAY_DARK` (`#64748b`), i.e. Tailwind slate-300/400/500 so the steps are perceptually even, plus a `COOL_*` line in the palette header's naming convention. Skipped the two names from the title in favour of `_LIGHT`/`_DARK`: "slate" names the hue twice, and `COOL_OFFLINE` would duplicate `STATUS_OFFLINE`, which is amber on purpose because offline is a warning rather than a neutral (pinned by test). Pinned by `__tests__/design-tokens-cool-family.test.ts`: values + aggregate membership, light→dark luminance monotonicity, a minimum step size so a future insert can't be invisible-but-passing, a blue>red/green assertion that keeps the family actually cool-toned (the entire reason it exists — a warm grey here would be indistinguishable from `MUTED_*`), no aliasing of an existing token or of `STATUS_OFFLINE`, source-order light→dark, and the `app/_dev/tokens.tsx` prefix that pulls both new shades into the preview route without editing it.

- [DONE: 2026-08-09] [est: 19000] **Extract a `<DangerSection>` component** — shipped `components/danger-section.tsx` with the suggested `title hint actionLabel onAction tone={"soft"|"hard"}` API plus a `disabled` prop (every call site ORs its own conditions, so one flag beats a `pending`/`busy` pair). `tone` drives the four coupled values — surface, border, label colour, and whether the button stretches or hugs — and defaults to `"soft"` so a call site that forgets the prop gets the reversible treatment rather than a filled red delete button. Supplying `title`/`hint` is what makes the zone card render at all, so settings' bare sign-out pill returns the button unwrapped instead of drawing a red box around a routine action. `app/settings.tsx` lost 9 stylesheet entries and 8 token imports; the `design-tokens.test.ts` adoption assertions for those tokens moved out with them, following the existing "left with `<HeroBanner>`" comment pattern. Pinned by `__tests__/danger-section.test.ts` (13 assertions): per-slot token mapping for both tones, the stretch-vs-hug asymmetry, the shared `opacity: 0.6` disabled treatment with `accessibilityState`, the delete button's in-flight label and its `pending || deleting` double-disable, that settings dropped all 9 dead styles, and — the one that matters for drift — a repo-wide sweep asserting no `app/**` or `components/**` file inlines any of the 9 hex values, plus that the `CARD_BG_11` + `DANGER_SOFT_3` pair (which *is* the danger-zone card) appears in exactly one file. The hex lint also caught the component's own doc comment quoting a near-miss hex literal, which is a decent sign the guard earns its keep.

- [DONE: 2026-08-09] [est: 17000] **Pin a `__tests__/design-tokens-danger-family.test.ts` luminance-monotonicity test** — shipped the helper and the test file, but NOT the assertion as written, because it is false against the palette and would have failed on values that are all correct. The family has grown from 9 shades to 17, and the suffix digits are arrival order, not rank: `DANGER_DEEP_3` (`a5402d`, orange-leaning) is ~19 luma LIGHTER than `_2`, and `DANGER_DEEP_7` (the toast accent) is lighter than the unsuffixed base. Renaming 17 tokens to make the digits sort is a large mechanical diff buying a convention nobody reads a colour from. What the names do promise, and what the test pins instead: (1) the `_SOFT` and `_DEEP` BANDS do not overlap, with a 50-luma floor against today's 60-luma gap — this is what actually catches the regression the entry named, since a `DANGER_DEEP_9 = "#ff8888"` lands 64 units above the deep ceiling and fails with the token named (proven by a test that inserts exactly that, plus its mirror for a too-dark `_SOFT`); (2) the unsuffixed `DANGER` > `_MEDIUM` > `_DEEP` trio, the one place the words claim a ladder, darkens in that order; (3) every member stays red-dominant, including `DANGER_DEEP_8`, which sits in the `HERO_DARK` luma range and is only distinguishable from a hero brown by its red channel; (4) the full 17-name light→dark ranking as a snapshot, so an insert cannot land unplaced. The helper is `lib/color-luminance.ts` (`luminance`, `rgbChannels`, `isLightHex`, `isRedDominant`, `rankByLuminance`, `luminanceBand`, `findLuminanceBreak`, `findBandOverlaps`), which existed as three copy-pasted six-line YIQ approximations that agreed by luck — `app/_dev/tokens.tsx`, `toast-design-tokens.test.ts` and `design-tokens-cool-family.test.ts` all now import it, so the preview's idea of "light" and the tests' idea of "lighter than" cannot diverge. Documented in the module header that this is Rec. 601 luma, not WCAG relative luminance: no gamma expansion, so it must not be used for contrast checks — it is only valid for ORDERING shades that share a hue, which is every question the palette asks. Non-colours return `NaN` rather than coercing to 0, since coercing would rank a typo as the darkest shade in a family and read as a legitimate result. The entry's closing line ("same shape would extend to `MUTED_*` / `HERO_DARK_*` / `CARD_BG_*`") shipped as a cross-family block: those are no more internally monotonic than `DANGER_*`, but they form a real three-band ladder (`HERO_DARK` ≤47.6 < `MUTED` 70.0–186.5 < `CARD_BG` ≥227.1) and both gaps are pinned at 20 luma — a muted label drifting past a card surface is invisible text, and a hero surface drifting up into the muted range fails its cream-text contrast without any single token looking wrong. The palette header now states that numeric suffixes are IDs and word suffixes rank, asserted by the test so the note cannot be deleted. Step 7 scan: the hex lint caught two doc comments in the new helper quoting hex literals (same catch as the `<DangerSection>` task), which is the second time that guard has earned its keep on a comment. `npx tsc --noEmit` + 4431 tests (+35: 20 pure-helper units incl. shorthand expansion, tie-as-break, first-break-not-last, and NaN propagation on both sides of a band comparison; 15 palette assertions) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — with `DANGER` / `DANGER_DEEP` / `_2` / `_3` / `_4` / `_5` and `DANGER_SOFT` / `_2` / `_3` now spanning 9 distinct red shades, the suffix numbering should be enforced structurally (deeper = lower luminance, softer = higher luminance). A simple helper that converts each token to YIQ luminance and asserts strict monotonic ordering would catch a future contributor accidentally inserting `DANGER_DEEP_6 = "#ff8888"` (lighter, breaking the family convention). Same shape would extend to `MUTED_*`, `HERO_DARK_*`, `CARD_BG_*` once we ship the helper.

- [DONE: 2026-08-09] [est: 0] **Replace the LinearGradient `colors={[HERO_DARK_4, HERO_DARK, HERO_DARK_5]}` triplet with a named `lib/gradients.ts` constant** — already shipped, unmarked, as part of the `<HeroBanner>` extraction: `lib/gradients.ts` exports `HERO_DARK_GRADIENT` (and `PHOTO_SCRIM_GRADIENT`), pinned by `__tests__/gradients.test.ts`, and `components/hero-banner.tsx` is the single consumer. It went one step further than this entry asked, exporting a spreadable `{colors, start, end}` prop bundle rather than the bare colour array the entry named — the angle is part of the recipe, and a copy that kept the three colours but dropped `start`/`end` renders a visibly different banner while looking correct in a diff. Verified no file inlines the triplet. — the settings hero now declares a 3-stop dark gradient that the login screen and onboarding flow are likely to reuse. A single `HERO_DARK_GRADIENT = [HERO_DARK_4, HERO_DARK, HERO_DARK_5] as const` exported from `lib/gradients.ts` would let those screens import one constant instead of re-declaring the triplet. Pairs with a `withAlpha()` helper for the eventual `OVERLAY_*` family.

### Item-filters design-tokens migration (2026-05-23)

- [DONE: 2026-08-09] [est: 0] **Batch-migrate the new `DANGER_DEEP_4` / `DANGER_SOFT_2` / `CARD_BG_10` "soft destructive chip" trio across the 5 remaining inline consumers** — already complete, unmarked: all three values are now imported by name at every consumer (`app/collection/[id].tsx`, `components/item-filters.tsx`, `components/danger-section.tsx`, `components/bulk-bar.tsx`) and the only place any of the three hexes appears as a literal is its own `export const` in `lib/design-tokens.ts`. The batch got absorbed into the per-file migrations that followed, and `npm run lint:hex` now enforces the outcome repo-wide (204 files scanned, zero inline literals), which is a stronger guarantee than this entry asked for. — the trio now lives inline in `app/settings.tsx` (3 occurrences) and `app/collection/[id].tsx` (4 occurrences). Each uses the same `paddingHorizontal/Vertical + borderRadius:999 + CARD_BG_10 bg + DANGER_SOFT_2 border + DANGER_DEEP_4 fg` recipe to render a "soft destructive action" chip. Rolling them in a single PR (rather than waiting for the per-file sub-tasks 2+ tasks away) would lock in the canonical chip styling and prevent a future contributor from inlining `#d9a0a1` (off by one in the green channel).

- [DONE: 2026-08-09] [est: 15000] **Extract a shared `<SoftDestructiveChip />` component from the resetChip recipe** — shipped `components/soft-destructive-chip.tsx` with the `icon label onPress` API the entry named (plus an optional `accessibilityLabel`), lifted verbatim from `resetChip` so this is a refactor and not a redesign — pill radius, 12/8 padding, gap 4, 12pt/700 label, all pinned. `icon` defaults to `close-circle`, which is what every current caller passes. The entry's premise needed one correction: it says the settings and collection screens "replicate inline" this chip, and they do not — they replicate the *colour trio* in BLOCK-BUTTON form, which `<DangerSection tone="soft">` already owns. The shared thing across the three is the palette, not the geometry (a 12pt inline pill and a 15pt full-width button), so the trio moved to `lib/danger-surface.ts` as `SOFT_DESTRUCTIVE_SURFACE` + `SOFT_DESTRUCTIVE_FOREGROUND` and BOTH widgets spread it, while each keeps its own padding and radius. That module sits in `lib/` rather than beside either component for the same reason `lib/gradients.ts` does: anything under `components/` pulls `@expo/vector-icons` through the import graph and cannot be transformed under `tsx --test`, so the values would have had to be pinned by regex over source text instead of compared — the first draft put it in the component and the test failed on exactly that. The chip also closes a small a11y gap for every future caller at once: the reset chip shipped with no `accessibilityLabel` while the sort and query chips beside it had one. `app/collection/[id].tsx` is left as the one screen still importing the trio directly — its delete button is the block form at `RADIUS_CARD`, and adopting `<DangerSection>` there would round its corners, which is a design decision rather than a refactor; the adoption test names it as the single known exception so a second one cannot appear quietly. Adoption assertions in `design-tokens.test.ts` and `danger-section.test.ts` moved out with the tokens, following the same "left with `<HeroBanner>`" pattern. Pinned by `__tests__/soft-destructive-chip.test.ts` (11 assertions: the surface equals the trio by value and carries NO geometry key, the two consumers spread it rather than repeating it, the lifted pill measurements, the icon tint, the a11y fallback, memoization, and the single-holdout sweep). `npx tsc --noEmit` + 4456 tests (+11) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — item-filters now declares the exact "icon + label, pale red bg/border, deep red fg" shape that the settings screen and collection screen replicate inline. A single `<SoftDestructiveChip icon label onPress />` component would let those screens import one widget instead of re-implementing the 4-line stylesheet, and gives the danger family its first composite "soft destructive action" UI primitive (sibling to the `<DangerIconButton />` extraction logged in the wishlist suggestions). Pairs naturally with the `__tests__/design-tokens-danger-family.test.ts` luminance assertion.

- [DONE: 2026-08-09] [est: 46000] **Add a `<FilterBar />` integration test that mounts the component with an active filter and asserts the rendered `resetChip` color matches `DANGER_DEEP_4`** — shipped, and it did NOT need the jsdom / react-test-renderer dev-dep three other tasks are still parked on. Two things blocked a real mount and both were fixable in-repo. (1) Module loading: `react-native` ships Flow source and `@expo/vector-icons/build/createIconSet.js` ships raw JSX in a `.js` file, so esbuild threw before a line ran — `__tests__/helpers/render.ts` now aliases those two specifiers (plus AsyncStorage, pulled in transitively by every provider) onto plain `.mjs` stand-ins in `__tests__/helpers/stubs/` via Node's synchronous `module.registerHooks`. Host components are bare strings so a rendered node's `type` reads back as `"View"`/`"Pressable"`/`"Ionicons"`, and `StyleSheet.create` is the identity function so a rendered style can be compared to an imported token BY VALUE — the whole point. (2) Hooks: calling a component directly makes `useState` throw because React's dispatcher is null outside a renderer, so `render()` installs its own dispatcher into React's shared internals for the duration of the pass. Nothing is stubbed away — the component imports hooks from `"react"` exactly as it does in the app — and the dispatcher implements useState/useReducer/useMemo/useCallback/useRef/useContext/useEffect/useLayoutEffect/useId/useSyncExternalStore/useDeferredValue/useTransition with real dep-array comparison. The walker unwraps `memo`/`forwardRef`/Fragments/arrays, maintains a provider stack for `useContext` (so `<I18nProvider>` mounts and `t()` returns real strings), flushes effects after the pass and re-renders if one set state, and treats `<Modal visible={false}>` as mounting nothing — without that last rule the shut filter sheet's own Reset button lands in the tree and "find the reset chip" matches the wrong one. Two ordering constraints are load-bearing and documented: `installNativeModuleStubs()` must run at module scope BEFORE the component is pulled in with a dynamic `await import()` (ESM resolves a file's whole static graph before evaluating any of it), and `expo/tsconfig.base`'s `"jsx": "react-native"` makes esbuild fall back to the CLASSIC transform, emitting bare `React.createElement` into files that correctly never import React — so the harness publishes `React` as a global rather than forking tsconfig for tests. New `__tests__/design-tokens-rendering.test.ts` (12 cases) is the centralised runtime-token suite the entry asked for: the reset chip's surface/label/icon each compared to `CARD_BG_10`/`DANGER_SOFT_2`/`DANGER_DEEP_4`, the shared `lib/danger-surface` constants pinned as the single route, pill geometry pinned as the chip's and NOT the shared surface's, the chip absent when no filter is active and absent for a whitespace-only query, the press actually clearing every field, plus the filter button's `HERO_DARK`/`TEXT_ON_DARK_5` active swap, the amber query chip rendering the TRIMMED needle, the sort chip gated on a non-default sort, and the shut Modal. New `__tests__/render-harness.test.ts` (26 cases) pins the harness itself against locally-declared components so a failure there reads as "the harness broke", not "a screen changed". `npx tsc --noEmit` + 4493 tests (+37) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — pinning rendered styles against imported token constants would catch any future regression where a contributor accidentally re-introduces an inline hex literal that compiles fine but skips visual review. Same shape as the chat detail / search-overlay integration test suggestions; centralising them in a `__tests__/design-tokens-rendering.test.ts` suite would let each file's adoption test cover both source-level + runtime assertions.

- [DONE: 2026-08-09] [est: 18000] **Persist the `ItemFilters` draft across modal open/close so partial input isn't lost on accidental backdrop tap** — shipped exactly as the entry specified: `openModal()` no longer resets `draft`, and a `useEffect(() => setDraft(filters), [filters])` took its place. The two failure modes stay apart, which is why the sync is on `filters` rather than nothing at all: an edit the user ABANDONED is preserved (backdrop tap, then reopen — the half-typed price is still there), while an edit invalidated by the state moving underneath it is discarded (the reset chip, the has-photos quick chip and the sort/query chip clears all write through `onChange`, so the sheet must reopen showing what those left behind rather than an edit of a filter set that no longer exists). `filters` is the caller's `useState` value in `app/collection/[id].tsx` — the only consumer — so the effect fires on a real change and not on every render; a caller that rebuilt the object inline would wipe the draft on every keystroke, which is doc-commented at the effect. New `__tests__/item-filters-sticky-draft.test.ts` (5 cases) drives the real component through the harness: type → backdrop → reopen preserves, reopen → Apply commits the preserved value, the reset chip and the quick chip each discard the stale draft (with the chip's own change visible in the resynced sheet), and a first open with no edit in flight shows the applied filters. The Apply button is matched by its `HERO_DARK` full-width style rather than its label so the assertions do not depend on the active locale. `npx tsc --noEmit` + 4520 tests (+5) + `npm run lint:all` (11/11) + `npm run build` all green. No DB migration, no creds. — today the filter modal resets `draft` to the current `filters` on every `openModal()` call, but the *current* `filters` snapshot is itself stale if the user opened the modal, typed a partial price, then tapped the backdrop instead of "Apply". A `useEffect(() => setDraft(filters), [filters])` (rather than the open-side reset) would keep the draft sticky until "Reset" is pressed. The change is 2 lines and removes a footgun the design team has called out as confusing.

- **Add `keyboardType="numeric"` enforcement + min/max validation to the `priceFrom`/`priceTo` `TextInput`s** — today the inputs accept any string and the apply step calls `parseFloat` which can return NaN or negative numbers. A pure `validatePriceRange(from, to)` helper in `lib/filter-helpers.ts` would let the apply button gray out on invalid input (e.g. `from > to` or non-numeric) and surface a `t("filterInvalidPrice")` hint below the row, mirroring the existing `inputInvalid` style from `create-collection.tsx`.

### Wishlist design-tokens migration (2026-05-23)

- **Batch-migrate the new `CARD_BG_9` (`#fff4e5`) across the 6 other inline consumers** — the value lives inline in `app/index.tsx` (2 occurrences), `app/collection/[id].tsx` (2 occurrences), and `app/item/[id].tsx` (2 occurrences). All seven sites use it as the warm-cream "pick / image-picker / amber-cta" button background; rolling them in a single PR (rather than waiting for the per-file sub-tasks 3+ tasks away) would lock in its canonical role as "warm cream secondary-button surface" and prevent the value from drifting while someone edits one of those screens in isolation. ~3 minutes per file, fully mechanical.

- **Promote `CARD_BG_7` (`#f4ecdf`) into the page-background gradient declaration in `components/screen.tsx`** — that component still declares `colors={["#f4ecdf", "#fffaf4", "#f4f1ea"]}` inline. The first stop now has a named token; replacing it would let a future "warm vs. cool theme" toggle flip the gradient through token re-export rather than a hardcoded edit, and would close the gradient's first inline literal. The `"#fffaf4"` mid-stop is already `PAGE_BG_2`, and `"#f4f1ea"` would join `CARD_BG_*` as a sibling once we touch it.

- **Replace the 3 remaining `backgroundColor: "#fff"` 3-digit shorthands with a new `WHITE_INPUT_BG` token** — wishlist's input / cancelButton / collectionRow each render pure-white surfaces on the cream page. Today the 3-digit shorthand sidesteps the 6-digit regex, but a named token (which could simply re-export `PURE_WHITE`) would make the intent visible and let a future "subtle off-white inputs" rebrand be a one-token swap. Same pattern would apply to the `chatsBadgeText` color call-out from prior migrations.

- **Add a `<DangerIconButton />` extraction so the trash/delete row gets its own token-aware widget** — wishlist's `deleteButton` style (borderRadius 999 + DANGER_SOFT border + CARD_BG_8 bg + DANGER_DEEP_3 icon) is a recipe that will recur on every list view that supports per-row delete (item card, collection card, listing card). Extracting it would let those screens use one component instead of re-implementing the 4-line stylesheet, and gives the danger family its first composite "destructive action" UI primitive.

- [DONE: 2026-08-09] [est: 0] **Pin a `__tests__/design-tokens-danger-family.test.ts` test that asserts `DANGER` < `DANGER_DEEP` < `DANGER_DEEP_2` < `DANGER_DEEP_3` darken-monotonically by luminance** — duplicate of the entry above (same file, same claim, filed twice from two different migration sections); closed by that task's ship. Its specific ask is the one part that could not be honoured: `DANGER_DEEP_2` < `DANGER_DEEP_3` is false by ~19 luma, so the strict chain would fail on correct values. See the entry above for what shipped instead and why. — today the suffix numbering is documentation-only; a structural assertion that converts each token to YIQ luminance and asserts strict monotonic ordering would catch a future contributor accidentally inserting `DANGER_DEEP_5 = "#ff8888"` (lighter, breaking the family convention). Same shape would extend to `MUTED_*`, `HERO_DARK_*`, `CARD_BG_*` once we ship a similar density helper.

### Profile-detail design-tokens migration (2026-05-23)

- **Add an `OVERLAY_*` token family with a `withAlpha(token, alpha)` helper** — the profile hero now declares three alpha-channel surfaces (`rgba(255, 244, 229, 0.12)`, `rgba(255,255,255,0.12)`, `rgba(255,255,255,0.08)`) that can't fit the existing 6-digit-hex contract. A `lib/with-alpha.ts` module exporting `withAlpha(PURE_WHITE, 0.12)` would let those surfaces share a named anchor (`OVERLAY_HERO_LIGHT_12` etc.), keep the immutable palette intact, and make alpha-tweaks one-line edits. Pairs with the prior bottom-nav / search-overlay backdrop notes.

- **Batch-migrate the new `MUTED_18` (`#6e5541`) across the remaining 3 inline consumers in `app/index.tsx`** — the token now exists but `app/index.tsx` lines 420, 433, 491 still inline the value as `borderColor`. Rolling those into a single PR (rather than waiting for the index migration sub-task 4+ tasks away) would lock in its canonical role as "border on dark hero card" and prevent the value from drifting to `#6e5542` while someone edits the home screen.

- **Batch-migrate the new `DANGER_DEEP_2` (`#922a2a`) across `app/settings.tsx`** — the value already appears as a destructive-action button background in settings, and centralising it before that screen's migration lands would let the settings sub-task focus on the *new* hex values it introduces. Pairs with a future "destructive action style guide" pass.

- **Extract a `<HeroProfileCard />` component reusable on `app/people.tsx`, `app/profile/[id].tsx`, and the future "user picker" sheet** — all three render the dark `HERO_DARK` background + avatar + name + username + bio block with subtle variations. The hero block here is ~50 LOC of layout that has zero variance from the people-screen card aside from the settings-icon corner button. A single component with optional slots would close the divergence loop and let the new `MUTED_18` token have exactly one consumer site (the hero border).

- **Extract a shared `<ProfileActionButtons />` widget that renders the right `primaryAction`/`secondaryAction`/`statusBadge` combination based on `relationship: ProfileRelationship`** — the profile detail screen now declares 6 separate cases (`friend`, `request_sent`, `request_received`, `following`, `none`, plus the admin row) totaling ~60 LOC. The same logic will be needed on the future "user picker" sheet and the chat header. Extracting it would deduplicate the friend/follow state machine logic that's currently spread across 3 files and let the new token-mapped pill styles live in one place.

### Listing-detail design-tokens migration (2026-05-23)

- **Extract the `mode === "sell" ? AMBER_ACCENT : SUCCESS_GREEN` ternary into a shared `lib/marketplace-helpers.ts` helper `modeAccentColor(mode)`** — the listing detail screen now applies that exact ternary in three different JSX call sites (modeBadge, claimButton, priceHistoryModeBadge). The next sub-task targeting `app/marketplace.tsx` will likely add a fourth call site (the active-listings grid uses the same color contract). Centralising it would also let the eventual "trade mode = green accent" rebrand be a one-line edit instead of a sweep.

- **Batch-migrate the new `AMBER_MUTED_3` (`#d8c7b1`) across the remaining 3 inline consumers** — the value now lives inline in `components/item-card.tsx` (2 occurrences) and `app/marketplace.tsx`. Each treats `#d8c7b1` as the photo-fallback fill; rolling those into a single PR (rather than waiting for the per-file sub-tasks 4+ tasks away) would prevent the value from drifting to `#d8c7b0` while someone edits one of those screens in isolation. ~5 minutes per file, fully mechanical.

- **Batch-migrate the new `HERO_DARK_7` (`#2a1e17`) and `TEXT_ON_DARK_6` (`#fff7ed`) pair across `app/item/[id].tsx` and `app/settings.tsx`** — both tokens appear together as the "dark hero card + cream-on-dark text" recipe in the item detail and settings header. Rolling them in now (one ~5-line PR per file) would close the visual loop before each screen's larger migration sub-task lands, and let those tasks focus on the *new* hex values they introduce instead.

- **Promote the `<OwnerChip />` (avatar + label + name + handle) shape into a shared component** — `app/listing/[id].tsx`, `app/profile/[id].tsx`, and the marketplace card grid render conceptually-identical "user attribution chip" blocks with 6 styles each (`ownerChip`, `ownerAvatar`, `ownerAvatarFallback`, `ownerAvatarText`, `ownerLabel`, `ownerName`, `ownerHandle`). Extracting it would eliminate ~40 LOC across each consumer and let the new `AMBER_MUTED` + `HERO_DARK_3` + `MUTED` + `TEXT_DARK` tokens have exactly one consumer site.

- **Add a `<ModeBadge mode />` extraction so the `paddingHorizontal/Vertical + borderRadius: 999 + AMBER_ACCENT|SUCCESS_GREEN ternary` recipe lives in one component** — listing detail declares both a `modeBadge` (larger) and `priceHistoryModeBadge` (smaller) with the same color contract. A `<ModeBadge mode size?: "sm" | "md" />` component would let `app/marketplace.tsx`, `app/item/[id].tsx`, and the future "Sales" subsection share the styling without duplicating the conditional. Pairs naturally with the `modeAccentColor()` helper suggested above.

### Create-collection design-tokens migration (2026-05-23)

- **Batch-migrate the new `AMBER_MUTED_2` (`#dbc7ae`) across the remaining 4 inline consumers** — the value now lives inline in `components/photo-preview.tsx`, `app/wishlist.tsx`, `app/index.tsx`, `app/collection/[id].tsx`. Each of these uses `#dbc7ae` as an image-placeholder background; rolling those into a single PR (rather than waiting for the per-file sub-tasks several tasks away) would lock in its canonical role as "warm tan image-placeholder fill" and prevent the value from drifting to `#dbc7af` while someone edits one of those screens in isolation. ~5 minutes per file, fully mechanical.

- **Batch-migrate the new `TEXT_DARK_4` (`#2b2017`) across the remaining 2 inline consumers** — the value now lives inline in `app/create.tsx` (3 occurrences) and `app/item/[id].tsx`. The semantic role ("display-heading dark brown") is already shared by all four call sites; centralising the value before those screens are migrated would let their per-file sweeps focus on the *new* hex values they introduce instead of reasoning about which `_4` suffix to pick.

- **Batch-migrate the new `MUTED_17` (`#7a6453`) across the remaining 2 inline consumers** — the value lives inline in `components/photo-preview.tsx` and `app/collection/[id].tsx` (2 occurrences). All four sites use it as a muted-brown hint/secondary-text color; landing the rename now prevents a future contributor from inlining `#7a6452` (off by one in the green channel — invisible to the eye, invisible to git blame, but breaks the single-source-of-truth contract).

- **Extract a `<HeroBanner>` component shared by `create-collection.tsx` and `create.tsx`** — both screens render a near-identical hero card (`backgroundColor: CARD_BG_5`, `borderRadius: 28`, `padding: 20`, title + subtitle). The create-item screen's hero will land its own `CARD_BG_5` consumer in the next migration sub-task; deduplicating the shape into `components/hero-banner.tsx` would let the rest of the codebase use one cited component rather than re-implementing the visual recipe each time.

- **Add a `templates` empty-state hint when `collectionTemplates` is filtered to zero results** — the current screen renders an empty horizontal `ScrollView` when no templates match (e.g. a future user-locale filter), with no visible indication of *why* nothing shows. A 4-line conditional rendering `t("noTemplatesAvailable")` would close the UX gap; the i18n key would map naturally onto the existing `coverFallbackHint` pattern. Pairs with a feature flag for "personalised templates" if/when that lands.

### People design-tokens migration (2026-05-22)

- **Extract `<Pagination>` into a shared component** — `app/people.tsx` declares 6 pagination-related styles (`pagination`, `pageButton`, `pageButtonDisabled`, `pageButtonText`, `pageButtonTextDisabled`, `pageInfo`) for a widget the marketplace, wishlist, and acquired-items screens all want. Extracting a `<Pagination page total onPageChange />` component would let the new `BORDER_4` + `MUTED_16` tokens have exactly one consumer site (semantically: "disabled-button styling") instead of every screen re-wiring them.

- **Promote the `<ProfileActionCard />` extraction (already suggested in the friends migration suggestions) before migrating `app/profile/[id].tsx` (40 hex literals, the next big-ticket file in the queue)** — both screens render the same "card + avatar + meta + 1-2 pill buttons" shape with friend/follow/request states. Extracting it now means the profile migration becomes a one-line component update for the action-row instead of yet another full-file sweep. The people screen's `renderProfileCard` is the cleaner reference implementation.

- **Add an a11y label to the disabled pagination button stating "previous page disabled" / "next page disabled"** — today the buttons gray out with `disabled={page <= 1}` but expose no screen-reader hint about why they're disabled, which fails WCAG 4.1.2. The fix is a 2-line conditional inside the existing `<Pressable>`. Pairs naturally with the `<Pagination>` extraction above.

- **Switch the people-screen search debounce from synchronous `setQuery` to a 200ms `useDebouncedValue(query, 200)` hook** — the screen currently re-filters the entire `others` array on every keystroke. With ~100 profiles loaded (the current page size) the cost is negligible, but as the directory grows past a few hundred users the per-keystroke filter will become user-visible jank on older Android devices. A debounce hook in `lib/use-debounced-value.ts` would also be reusable by search-overlay and future filter screens.

- **Add an integration test that asserts the `pageButtonDisabled` background is `BORDER_4` and the `pageButtonTextDisabled` color is `MUTED_16`** — pinning the disabled-state styling against the imported token constants would catch any future regression where someone re-introduces an inline hex while editing the pagination markup. Same shape as the chat detail / search-overlay integration test suggestions; centralising them in a `__tests__/design-tokens-rendering.test.ts` suite would let each file's adoption test cover both the source-level assertion (no inline hex) and the runtime assertion (rendered token matches).

### Search-overlay design-tokens migration (2026-05-22)

- **Batch-migrate `MUTED_15` (`#b8a08a`) across the other 2 inline consumers (`app/create.tsx` + `components/item-filters.tsx`) in one sweep** — the token now exists but its other call sites still inline the value; rolling those into a single PR (rather than waiting for the per-file sub-tasks 5+ tasks away) would lock in its canonical role as "light-muted-brown clear/close icon" and prevent the value from drifting to `#b8a18a` while someone edits one of those screens in isolation.

- **Extract the search-overlay backdrop (`rgba(38, 27, 20, 0.55)`) into a shared `OVERLAY_BACKDROP` token** — the same semi-transparent dark cream appears in the bottom-nav modal and would appear in every future overlay sheet. The existing 6-digit token convention can't represent alpha; a separate `OVERLAY_BACKDROP` (or a `withAlpha(HERO_DARK, 0.55)` helper that derives it at module-evaluation time) would centralise the value without breaking the hex-shape contract on the rest of the palette.

- **Promote the filter-chip / owner-chip pair into a shared `<FilterChip />` component** — search-overlay declares two near-identical chip variants (filter vs. owner) with subtly different padding and text color. Extracting a single component with `variant: "primary" | "ghost"` would let `app/create.tsx`, `app/marketplace.tsx`, and `components/item-filters.tsx` (which already render conceptually-similar chips) deduplicate ~40 LOC apiece while the token mapping lives in one file.

- **Add a `search-overlay.tsx` integration test that mounts the component with a mocked CollectionsProvider and asserts the active-filter chip's `backgroundColor` equals `HERO_DARK`** — pinning rendered styles against imported token constants would catch any future regression where a contributor accidentally re-introduces an inline hex literal that compiles fine but skips visual review. Same shape as the chat detail integration test suggestion.

- **Wire a `__tests__/design-tokens-density.test.ts` that ranks every migrated file by `(unique-hex-literals-before / unique-tokens-used-after)` ratio** — search-overlay landed at 19 inline values mapping to 19 unique tokens (1:1 — no consolidation opportunity), but later migrations (e.g. `app/collection/[id].tsx` with 86 hex literals) likely have far more duplicated values per file. Surfacing the ratio in CI output would help the design team identify "good consolidation candidates" (high ratio = many duplicated inline values that could collapse onto one token) before each migration sub-task lands.

### Chat detail design-tokens migration (2026-05-22)

- **Batch-migrate the new `TEXT_ON_DARK_5` (`#fff7ea`) across the remaining 6 inline consumers** — the value now lives inline in `app/create.tsx`, `app/settings.tsx`, `app/wishlist.tsx`, `app/index.tsx`, `components/selectable-item-row.tsx`, `components/empty-state.tsx`, `components/item-filters.tsx`. The chat migration introduced the token but only swept its first call site; landing the rename across the other six in a single PR (rather than waiting for each per-file sub-task) would lock in its canonical role as "primary cream-on-dark button/icon foreground" and prevent the value from drifting to `#fff7eb` while someone edits one of those screens in isolation.

- **Promote the offline-pill widget (`offlinePill` + `offlinePillText` styles) into a shared `<RealtimeOfflinePill />` component** — the same "warm cream pill with dark-amber text" pattern is the natural rendering target for the `RealtimeStatusProvider` consumers added in the marketplace status work. Today the pill is duplicated in chat detail + the registered `<RealtimeStatusPill />` component; deduplicating into a single token-driven component means any future palette tweak to the warning state is a one-line edit.

- **Extract the message-bubble pair (`bubble` / `bubbleMine` / `bubbleTheirs` + the four text variants) into a `<ChatBubble mine={…} text={…} time={…} />` component** — once the chat screen grows reactions, edited indicators, or read receipts, the inline bubble markup will need surgery in N places; a single shared component keeps the token mapping in one file and makes a future Tailwind-style design pass tractable.

- **Add a `chat/[id].tsx` integration test that mounts the screen with a mocked ChatProvider and asserts the empty state, typing-row, offline pill, and disabled-send-button each render with their expected token-driven background colors** — the chat detail screen is the highest-traffic synchronous-interaction surface but has no behavioural tests today. Pinning the rendered backgroundColor via `findAllByType(View)` against the imported token constants would catch any future regression where a contributor accidentally re-introduces an inline hex literal that compiles but skips the visual review.

- **Rename `MUTED_12 = "#7a4f1a"` to a semantic `WARN_TEXT` token** — the chat migration named it ordinally for consistency with the rest of the palette, but its role (warning text on warm cream pill) is categorical, not positional. The next palette refactor will likely keep the value but want the name to reflect its semantic role (mirroring the existing `STATUS_ONLINE` / `STATUS_OFFLINE` naming break from the ordinal convention). A rename + `RealtimeOfflinePill` extraction would land naturally as one PR.

### Currency conversion for total cost (2026-05-22)

- **Surface a currency picker in `app/item/[id].tsx` (item edit) so users can correct a wrong-currency entry without deleting and re-adding the item.** Today only `app/create.tsx` exposes the picker — an item created in USD that should have been EUR is locked unless the user deletes it. The picker UI already exists; rewiring it under the existing cost field in the edit modal is ~30 LOC.

- **Add a tiny "rates unavailable" pill on cards / detail pages when `currencyRates === null` AND items carry mixed currencies.** Today the fallback summation treats all items as if they were already in `displayCurrency`, which is silently wrong for mixed-currency collections during the first paint or when the API is down. The context already exposes the data (`getCollectionTotalCost(id).skipped > 0` or the rates state), but the UI doesn't distinguish the two states.

- **Switch `getCollectionTotalCost`'s fallback from "assume all items in displayCurrency" to "return null amount + a `pending: true` flag"** — the caller could then render `--` instead of a wrong number. Today the fallback (`reduce(... + e.amount, 0)`) over-reports for mixed-currency collections during the rates-loading window; an explicit pending state would also unlock a skeleton placeholder.

- **Add a manual "Refresh rates" entry in `app/settings.tsx`** that calls the new `refreshCurrencyRates()` from `CollectionsContext` so power users can force a refresh after a known FX move without waiting 24h. The wiring is 5 lines (Pressable + toast) since the context already exposes the action.

- **Move the rate fetch into `lib/auth-context.tsx`'s sign-in success path** so rates are warmed once when the session resolves rather than on every CollectionsProvider mount. Today the rates effect re-runs on app cold-start; gating it on the auth session would avoid an unnecessary fetch on the offline sign-out path and would let a future "premium tier" gate the higher-precision rate source behind an entitlement check.

### Friends design-tokens migration (2026-05-22)

- **Batch-migrate `MUTED_10` (`#624a35`) across the other 7 inline consumers in one sweep** — the value now lives inline in `components/search-overlay.tsx`, `app/people.tsx`, `components/item-filters.tsx`, `app/create-collection.tsx`, `app/create.tsx`, `app/collection/[id].tsx`, `app/item/[id].tsx`. Doing the token-rename in a single PR (rather than waiting for each file's per-task sub-task) would lock in its canonical role as "uppercase section-label brown" and prevent someone drifting the value to e.g. `#604a36` while editing one of those screens in isolation.

- **Extract the chats-link card (lines 126-141) into a shared `<ChatsLinkCard unread={n} />` component** — the same widget (icon + title/subtitle + unread badge / chevron-forward) is already very close to what would live in profile screens, account headers, and the marketplace dashboard once those screens grow chat hooks. Extracting it now keeps the design-token mapping in one place and centralises the unread-badge color contract before the friends/marketplace/profile triples it.

- **Replace the `chatsBadge` `color: "#fff"` 3-digit shorthand with `TEXT_ON_DARK` (or a new `WHITE_PURE_SHORT` token)** — keeping shorthand as an escape hatch from the 6-digit regex is a slippery slope; eventually one of the design-tokens-adoption tests should also fail on 3-digit literals, and the friends/chats badges should be the first to migrate. The trade-off is that `TEXT_ON_DARK = #fff7ef` is slightly cream — a tiny perceptual difference on a red badge but worth a design-led call before flipping it.

- **Promote the friend/follow/request "card with two pill buttons" row into a `<ProfileActionCard profile={p} kind={…} />` component** — `app/friends.tsx`'s `renderProfileCard` already encodes three kinds (`friend` / `following` / `request`) with two-button or one-button variants. The same pattern shows up in `app/people.tsx` and `app/profile/[id].tsx` (with subtly different button copy / actions). Extracting it would let the token migration of those two files become a one-line component update instead of another full-file sweep.

- **Add a `friends.tsx` integration test that mounts the screen with a mocked SocialProvider and asserts the empty state, friend list, and incoming-request list each render with their expected accessibility labels** — the friends screen is the highest-traffic social surface but has no behavioural tests today. A test that pins the tab labels, section headers, and primary-action button copy per-locale would catch any future regression in the friends-vs-following swap (esp. when the `dotHighlight` logic ships per-locale variations).

### Layout design-tokens migration (2026-05-22)

- **Reconcile native splash bg (`app.json:25 — #f3ecdf`) with `PAGE_BG_2` (`#fffaf4`)** — the two cream values differ by 12 RGB units per channel, which produces a visible flash when the splash fades into the shell on iOS/Android. Either (a) align them to a single token `SPLASH_BG` referenced by both files via a build-time codegen step, or (b) document the intentional gradient between splash and shell. A `__tests__/splash-shell-bg-parity.test.ts` that reads app.json and asserts the value matches a documented constant would prevent silent drift the next time someone edits one without the other.

- **Batch-migrate `ACCENT_DEEP` (`#8a5a2b`) across 4 other consumers** — `app/auth/callback.tsx:92`, `app/item/[id].tsx:1062`, `components/screen.tsx:27` all inline the same value today. A token-anchored sweep would lock in its canonical role as "refresh/loading spinner color" before someone drifts the value during a later per-file migration.

- **Promote `STATUS_ONLINE` / `STATUS_OFFLINE` into a semantic `realtimeStatusColor(state)` helper in `lib/realtime-status-context.tsx`** — the layout currently encodes the mapping inline (`realtimeOnline ? styles.realtimeDotOnline : styles.realtimeDotOffline`); the helper would let `components/realtime-status-pill.tsx` (shipped two tasks ago) share the same color contract instead of duplicating it. Today the pill renders text only — adding a colored dot in the pill that matches the header dot is a one-line follow-up once the helper exists.

- **Add `loadingText` to a shared `<LoadingScreen t={t}/>` component** — the same 3-line "ActivityIndicator + text" widget now appears in `app/_layout.tsx:118-121` and `app/auth/callback.tsx`. Extracting it would let the migration of those colors live in one place and would centralise the "checking session" UX language for future polish (skeleton instead of spinner, themed-spinner palette, etc.).

- **Replace the duplicated chats-icon `<Pressable>` block (lines 175-189 and 197-211)** — both branches render the same icon button with the same realtime dot and unread badge; the duplication exists only to handle the `/chats` route guard. Extracting a `<HeaderChatsButton />` component would halve the JSX and would let the future "show pill instead of dot at small widths" iteration touch one site.

### Swipe-tabs design-tokens migration (2026-05-22)

- **Audit `MUTED_2` (`#6b5647`) and `MUTED_8` (`#6b5543`) for unification** — the two tokens differ only in the last byte (47 vs 43), almost imperceptible visually. A perceptual-distance test (CIE-Lab ΔE under 1.5) would let a future refactor consolidate them safely, or document that the difference is intentional (e.g. one is "muted on cream", the other is "muted on amber"). Today neither file calls out the distinction.

- **Replace the `{...styles.tab, ...(isActive ? styles.tabActive : {})}` style spreads with array form** — `components/swipe-tabs.tsx` rebuilds 4 style objects (`tab`, `tabText`, `subTab`, `subTabText`) on every render of every tab. RN's `style={[styles.tab, isActive && styles.tabActive]}` short-circuits cleanly on `false` and keeps StyleSheet identity-equality intact. Tiny perf win on a hot interactive widget.

- **Add a `<DotRow>` sub-component for the indicator dots** — the inline `tabs.map((t, i) => <View …/>)` block at lines 205-218 mixes three style-merge concerns (dot, dotActive/subDotActive, dotHighlight). Extracting it to a typed sub-component `<DotRow tabs={tabs} activeIndex={i} variant={…} highlight={…} />` would clean up the JSX, make each style-merge concern testable in isolation, and let designers iterate on the dot row independently.

- **Move `ANIM_DURATION = 220` into a `lib/animation-tokens.ts` family** — alongside any future timing constants. Today the swipe-tabs animation duration is a magic number embedded in the file; pulling it into a sibling module to design-tokens would let an accessibility-first reduced-motion pass flip every transition duration from one file.

- **Render the swipe-tabs widget inside a future `app/_dev/tokens.tsx` preview** — the widget exercises six tokens (`HERO_DARK`, `AMBER_ACCENT`, `AMBER_SOFT`, `CARD_BG_3`, `TEXT_ON_DARK_4`, `MUTED_3`) plus 4 interactive states (idle/active/highlight, sub vs main variant). Including it would let designers verify the full tab-row contrast story on one row of the swatch grid.

### Login-screen design-tokens migration (2026-05-22)

- **Batch-migrate the 3 other files using the `[HERO_DARK_4, HERO_DARK, HERO_DARK_5]` gradient** — the exact same `colors={["#3d2810", "#261b14", "#1e140e"]}` 3-stop now lives inline in `app/settings.tsx:95` and `app/index.tsx:98` after this task. A single token-anchored sweep replacing those 3 inline copies with the new tokens (vs. waiting for their per-file sub-tasks in `.tasks.md`) would lock in the canonical hero-gradient signature before someone duplicates a near-but-not-identical 3-stop (`#3d2911`, `#1e150e`) and the brand visually splits across screens.

- **Batch-migrate `#fff4e8` (`TEXT_ON_DARK_4`) across the 9 files using it inline** — it's the most-shared token shipped in this batch (search-overlay, swipe-tabs, item-filters, people, _layout, settings, friends, collection, profile). Doing them all in one sweep keeps the "cream-on-dark button text" semantics consistent and prevents the same drift risk above.

- **Add a `DANGER_DEEP` semantic alias `FORM_ERROR_TEXT`** — `#a13434` is named today as a darker red but its only consumer is form-error text under inputs. A semantic alias `export const FORM_ERROR_TEXT = DANGER_DEEP;` would make call-site intent explicit and would let designers tune error-message contrast separately from destructive-button reds (which use `DANGER`).

- **Promote the `…{...styles.primaryButton, ...(pending ? styles.disabledButton : {})}` spread to `[styles.primaryButton, pending && styles.disabledButton]`** — the four button Pressables in `components/login-screen.tsx` build a brand-new style object on every render. RN's style-array API short-circuits cleanly on `false` and gives StyleSheet identity-equality the chance to skip a re-flatten. Free perf win on the auth screen specifically (it re-renders during the OTP flow).

- **Add an `__tests__/login-screen-gradient.test.ts` that pins the 3-stop colors to the token names, not the values** — today the no-inline-hex assertion catches that no literal remains, but a future renaming of `HERO_DARK_4` (e.g. to `HERO_DARK_TOP`) without updating the import in `login-screen.tsx` would slip through. Asserting the `<LinearGradient colors={[HERO_DARK_4, HERO_DARK, HERO_DARK_5]}>` shape (rather than just the presence of the imports) would close the rename-drift gap.

### Stats design-tokens migration (2026-05-22)

- **Batch-migrate the 9 other files now also using `TEXT_DARK_3` (`#2d2117`)** — the token currently appears inline in `app/create.tsx`, `app/index.tsx`, `app/profile/[id].tsx`, `app/collection/[id].tsx`, `app/item/[id].tsx`, `components/item-filters.tsx`. Each is already in the decomposed `.tasks.md` migration backlog, but doing a single token-anchored sweep (vs. file-by-file) would lock in `#2d2117`'s canonical role as "summary numeric/highlight text" before a near-but-different value (`#2d2118`, `#2e2117`) creeps in and quietly splits the palette.

- **Add an `Intl.NumberFormat` currency-aware total in stats** — `totalValue.toLocaleString()` formats the bare number but doesn't render a currency symbol; the items each carry `costCurrency` (`USD` / `EUR` / …) so the screen could group sums per currency and render via `Intl.NumberFormat(getDefaultLocaleForLanguage(language), { style: "currency", currency }).format(sum)`. Today the user sees "10000" instead of "$10,000.00", which is a real readability regression on the stats screen specifically (where the value is the headline).

- **Wire a chart accent palette and a "chart pill colors should stay raw" guard** — the original task description anticipated a multi-color chart palette that should NOT migrate to brand tokens, but the current stats screen is a single-bar chart so the caveat went unexercised. The next chart (e.g. a category breakdown pie/donut) will need 6–8 non-brand colors; adding a `lib/chart-palette.ts` now with documented "intentionally not in design-tokens" framing + a regex assert that the file lives outside `lib/design-tokens.ts` would document the boundary before the first multi-color chart is implemented.

- **Refactor the bar-chart math into a pure `bucketItemsByMonth(items, monthsBack)` helper in `lib/stats.ts`** — the `useMemo` block in `app/stats.tsx:32-44` is 13 lines of pure date-bucketing logic that would test cleanly in isolation (today there's no `__tests__/stats-growth.test.ts`). Extracting it would let a future analytics dashboard reuse the same bucketing without re-implementing the `padStart(2, "0")` / `slice(-12)` / `localeCompare` dance, and would unblock writing the empty-state / single-item / DST-boundary test cases the screen's growth chart relies on.

- **Replace the `barFill` style spread (`{ ...styles.barFill, height: `${…}%` }`) with `[styles.barFill, { height }]`** at `app/stats.tsx:83-86` — RN's array-style API is the idiomatic way to merge a static style with a dynamic prop; the spread form allocates a new object on every render of every bar and bypasses StyleSheet's identity-equality optimisation. Tiny but free perf win on a screen that renders up to 12 bars × 1 hot effect each refresh.

### Chats design-tokens migration (2026-05-22)

- **Migrate the four other files using `AMBER_MUTED` in one batch** — `#d9c2a8` is now also live in `components/search-overlay.tsx:501`, `app/people.tsx:263`, `app/friends.tsx:316`, and `app/listing/[id].tsx:382`. Migrating them together (vs. doing them per-file in the existing decomposed sub-tasks) would lock in the token's canonical role (avatar/empty-state fallback bg) before a future inline-hex regression introduces a near-but-not-identical value (`#d9c2a9`, `#d8c2a8` etc.) and quietly splits the palette.

- **Migrate `color: "#fff"` (3-digit shorthand) too** — the no-inline-hex adoption test only catches the 6-digit form, but `#fff` / `#000` shorthand still bypasses the token system. A second assertion in the adoption helper `assert.deepEqual(src.match(/#[0-9a-fA-F]{3}\b/g) ?? [], [])` (boundary-anchored to avoid matching the start of 6-digit literals) would close the gap; today only `app/chats.tsx`'s badge text uses `#fff`, so a single migration to a new `PURE_WHITE` token (or a justification to leave it) clears the codebase.

- **Promote `HERO_DARK_3` to `HERO_AVATAR_TEXT` (semantic alias)** — `#3a2716` originally entered the palette as a hero variant but its only consumer in `app/chats.tsx` is the avatar fallback letter color. A semantic alias `export const HERO_AVATAR_TEXT = HERO_DARK_3;` would make consumer intent explicit at the call site, and would let designers darken/lighten avatar text without thinking about whether the value is also wired into a hero background.

- **Add a `chats-hero` story to the future `app/_dev/tokens.tsx` preview** — the chats hero exercises four cream-on-dark tokens (`AMBER_LIGHT` eyebrow, `TEXT_ON_DARK_3` title, `TEXT_ON_DARK_SOFT` subtitle, `HERO_DARK` bg) on one compact widget. Including it would let designers verify the full hero contrast story on one row of the swatch grid; same logic that landed bottom-nav's preview suggestion last task.

- **Audit the three existing `TEXT_ON_DARK*` variants** — the palette now ships TEXT_ON_DARK (#fff7ef), TEXT_ON_DARK_2 (#fff5ea), and TEXT_ON_DARK_3 (#fff8ef). The three colors are visually nearly indistinguishable; only the design-system contract that "each surface owns one variant" justifies keeping them separate. A `__tests__/design-tokens-perceptual.test.ts` that asserts the pairwise CIE-Lab ΔE between TEXT_ON_DARK variants stays under some threshold would document the visual relationship and let a future palette refactor consolidate them safely.

### Nav-tab design-tokens migration (2026-05-22)

- **Promote `MUTED_4` (the new `#bbb0a6` inactive-nav-icon token) to a semantic alias `ICON_INACTIVE`** — `MUTED_4` is named by its position in the muted-brown ramp, not by its role. A second named export `export const ICON_INACTIVE = MUTED_4;` (or a renamed token with `MUTED_4` kept as a back-compat alias) would make consumer intent self-documenting at the call site (`color={item.active ? ICON_ACTIVE : ICON_INACTIVE}`), and would let designers raise/lower contrast on inactive icons without thinking about whether the value is also wired into a non-icon muted-text surface.

- **Move `Ionicons size={22}` into a `lib/design-tokens.ts` numeric scale** — every nav icon hard-codes `22` for the icon size and `4/8/10/18` for badge dimensions; the design-tokens module today only covers colors. Extending it with `ICON_SIZE_NAV`, `BADGE_DOT_SIZE`, `BADGE_COUNT_HEIGHT`, etc. would mirror the existing color migration pattern and let an accessibility pass (larger tap targets) flip everything from one file. Listed adjacent to the existing `.tasks.md` line 88 "semantic spacing/radius tokens" task — same shape, sized one notch smaller.

- **Add a `tokens-coverage` audit script** — `npm run audit:design-tokens` could grep every `.tsx` under `app/`+`components/` for `#[0-9a-fA-F]{6}` and fail the build (or emit a count) if the total exceeds a baseline. Today the decomposed migration list in `.tasks.md` hard-codes the per-file count from a manual sweep done weeks ago; an automated counter would update those numbers in CI and surface drift the moment a new contributor re-introduces a literal.

- **Render `<NavTab>` inside the future `app/_dev/tokens.tsx` preview** (already a `.tasks.md` backlog item) — the badge dot, premium dot, and active dot together exercise four tokens (`HERO_DARK`, `DANGER`, `AMBER_ACCENT`, `TEXT_ON_DARK`) on one compact widget. Including the tab in the preview would let designers verify the full bottom-nav contrast story (active + inactive + badge + premium) on one row of the swatch grid.

- **Migrate the inline `Ionicons` `color={…}` prop in every screen header (`app/chats.tsx`, `app/friends.tsx`, `app/people.tsx`, etc.) using the same `HERO_DARK`/`MUTED_4` active-vs-inactive pair** — most header icons today hard-code `#261b14` for the active icon and a separate hex for the inactive state; standardising on the nav-tab's token pair would mean a single design-tokens edit re-skins the entire icon system, not just the bottom nav.

### Bottom-nav design-tokens migration (2026-05-21)

- **Add a `palette-coverage` test that asserts every hex value used by `bottom-nav.tsx` matches an exported token, not just that the *names* are imported** — today the adoption test pins token *names* (`HERO_DARK`, `CARD_BG_2`, etc.) and the no-inline-hex assertion catches the absence of literals, but a future refactor that renames `TEXT_ON_DARK_2` while leaving the value `#fff5ea` inline would slip through. A snapshot check `assert.equal(parseStyle(bottomNavSrc).modalPrimaryText.color, TEXT_ON_DARK_2)` would close the rename-drift gap.

- **Extract the `rgba(20, 12, 6, 0.55)` modal backdrop into a `BACKDROP_DARK` token** with a parallel `withAlpha(token, 0.55)` helper. The backdrop is the only color in `bottom-nav.tsx` that *isn't* in the palette today; standardising it as a token (plus an alpha helper) would let any future "darken on overlay" surface (image preview overlays, photo-edit modes) share one source of truth instead of each modal hard-coding its own rgba quartet.

- **Migrate `components/nav-tab.tsx` next** — it's the sibling of `bottom-nav.tsx` (rendered as `<NavTab>` inside the `NavRow`) and carries 9 inline hex literals. Doing the two together would mean the entire bottom-nav surface re-skins from a single design-tokens edit; today only the wrapper migrated.

- **Add a `bottom-nav` story to the future `app/_dev/tokens.tsx` preview route** (already on `.tasks.md` line 70) — rendering the actual `BottomNav` next to a swatch grid of the 9 tokens it consumes would make the rotation argument concrete for designers reviewing palette changes. The preview route is on the backlog; pre-staging the bottom-nav fixture means it can be the first concrete example when the route lands.

- **Surface a TypeScript-friendly `bottomNavStyles` factory** — `bottom-nav.tsx` now references 9 tokens scattered across its `StyleSheet.create({...})`. A factory `createBottomNavStyles(tokens)` that takes the palette as a parameter would let the same component render in two themes (e.g. a future high-contrast a11y mode) without forking the file. The migration just unlocked this — every token is now a named import instead of a literal, so threading a `tokens` arg through is one refactor away.

### Social-context realtime invariant guardrail (2026-05-21)

- **Extend the realtime-invariant guard to every consumer file** — the test currently only protects `lib/social-context.tsx`. The same pattern (`no new RealtimeClient`, `no inline channel.subscribe`, `must import from @/lib/supabase-realtime`) applies to every future cloud-backed module: `lib/premium-context.tsx`, `lib/collections-context.tsx`, `lib/marketplace-context.tsx`, `lib/chat-context.tsx`. A parameterized helper `assertSharedRealtimeInvariant(filePath)` exported from a new `__tests__/_helpers/realtime-invariant.ts` would let one wiring test cover all five files with a single `forEach`. Today each file would need its own bespoke test that copy-pastes the four regexes.

- **Promote the invariant into an ESLint rule** — structural tests catch the regression in CI but won't surface it in the editor before commit. A custom rule `collectables/no-direct-realtime-client` (or a `no-restricted-imports` config for `@supabase/realtime-js` outside the allowlisted `lib/supabase-realtime.ts` + `lib/realtime-channel-registry.ts` paths) would give the same protection with a red squiggle in VS Code. Mirrors the design-tokens-lint-rule suggestion already on `.tasks.md` line 66 — same problem shape, same solution.

- **Add a `social-context.tsx` presence prototype guarded behind a flag** — the task framing was "once realtime ships in social-context, route through the shared client." That moment can be brought forward by wiring up a behind-the-flag presence channel (e.g. a `friends:online` topic that emits `friendId -> online` events keyed off `EXPO_PUBLIC_REALTIME_SOCIAL_PRESENCE=1`). Even an empty stub that calls `subscribeShared` then `unsubscribe`s would exercise the integration path and let `RealtimeStatusProvider` surface "social channel reconnecting" pills without changing the user-facing UI.

- **Render the `topics` Map snapshot in a `__DEV__`-gated overlay** — once social-context starts publishing channels, the `useRealtimeStatus().topics` map becomes the single source of truth for "which sockets does this device have open right now?" An overlay component `<RealtimeTopicsDebugOverlay />` (mounted in `app/_layout.tsx` behind `__DEV__`) would render the live topic list with each entry's `connected: boolean`, giving engineers an in-app analog of the Chrome Network panel's WS view without leaving the app.

- **Add a one-shot reconnect test that exercises social + chat + marketplace simultaneously** — the registry's WeakMap-per-client design already isolates topics, but no test today walks the full "three contexts subscribing to the same shared client, then status emits propagate to all three" flow. A small integration test in `__tests__/registry-multi-consumer-status.test.ts` that registers three fake handlers on three topics and asserts the snapshot reflects all three would catch a regression where the WeakMap accidentally collapses entries from different contexts.

### Shared realtime status context (2026-05-21)

- **Reuse `<RealtimeStatusPill />` in `app/chat/[id].tsx`** — the chat screen still renders its own inline `{allowed && !realtimeOnline ? <View style={styles.offlinePill}> … </View> : null}` block driven by `useChat().realtimeOnline`. Now that the shared pill is wired and the inbox channel publishes status into the registry, the chat-context-local `realtimeOnline` state is redundant. Swap the inline pill for `<RealtimeStatusPill topic={inboxChannelTopic(user.id)} />` (after adding the optional `topic` prop that defaults to the aggregate), then remove `setRealtimeOnline` from `lib/chat-context.tsx` to delete ~10 LOC of duplicated state.

- **Add a `topic` prop to `RealtimeStatusPill`** so callers can scope the offline indicator to a single channel (chat inbox, marketplace listings) instead of the aggregate `connectionState`. The pill currently shows "offline" when *any* tracked topic is `connecting`, which can flash a misleading state on the marketplace screen while the chat channel reconnects. `isTopicOnline(topic)` is already exposed on the context — `<RealtimeStatusPill topic="marketplace-listings-inserts" />` would be a one-line addition that derives a per-topic pill state.

- **Render a global "reconnecting" banner in `app/_layout.tsx`** — the registry now publishes connection state app-wide, but only `app/marketplace.tsx` shows the pill. A one-line `<RealtimeStatusPill />` in `_layout.tsx`'s `AppShell` (between `<NavigationBreadcrumbs />` and `<Stack>`) would surface socket loss on every screen, matching how mainstream chat apps render a persistent "Connecting…" header instead of per-screen indicators.

- **Add a `useTopicOnline(topic)` convenience hook** to `lib/realtime-status-context.tsx`. Today consumers must call `useRealtimeStatus().isTopicOnline(topic)`, which re-renders on every snapshot change even if the specific topic's state didn't move. A `useTopicOnline(topic)` hook backed by `useSyncExternalStore` (or a memoized selector) would only re-render on the *topic-specific* transitions — important for `app/chat/[id].tsx` which renders on every keystroke and shouldn't re-render when an unrelated marketplace channel reconnects.

- **Surface the registry snapshot in the diagnostics screen** — `app/diagnostics.tsx` (if it exists, or a future debug route) could render `Array.from(getRegistryStatusSnapshot().entries())` so engineers can see exactly which topics are subscribed/online during a session. Today the only way to inspect realtime state is via the Sentry breadcrumb timeline, which is post-hoc and limited; an in-app view would help triage "why isn't my marketplace updating?" complaints without a remote-debug round-trip.

### Realtime channel fan-out registry (2026-05-21)

- **Migrate `subscribeToTyping` onto the fan-out registry too** — today it's the one remaining inline `client.channel(topic)` consumer in `lib/supabase-chat.ts`. The presence semantics (`.track({ typing })` + per-user state) don't fit the current `configure(channel, emit)` API, but extending `subscribeShared` with an optional `presence?: { selfPayload, onStateChange }` block would let the typing channel share the same ref-counting + status fan-out as inbox/listings, keeping the "one socket per app" invariant from regressing when the same chat is open in two tabs of the same realm.

- **Move the realtime connection-state pill into a `RealtimeStatusProvider` consuming `subscribeShared`** — the next `.tasks.md` suggestion (line 58, "Track the RealtimeClient connection state … on a shared context value") is now a one-line plumbing job. Have the provider call `subscribeShared` with a sentinel topic + a no-op configurator just to register a status handler; the registry's late-joiner replay means every screen reads the live status without each module wiring its own listener.

- **Auto-fan-out `closeSharedRealtimeClient` into the registry** — today `closeSharedRealtimeClient` clears the module-scope cache in `lib/supabase-realtime.ts` but the registry's `WeakMap<RealtimeClient, …>` entries for the closed client still hold references to dead channels until GC. A `__resetRegistryFor(client)` helper called from `closeSharedRealtimeClient` would drop the per-client map deterministically + tear down any subscribed channels' fan-out handlers so a sign-out + re-sign-in cycle starts clean.

- **Add a structural test asserting no `app/**/*.tsx` calls `client.channel(...)` directly** — the registry is now the canonical entry point for new realtime consumers, but nothing prevents a future screen from re-introducing the inline-channel pattern. A regex grep across `app/**` + `components/**` paired with an allowlist for the registry itself would catch the regression in CI before it ships.

- **Promote the per-handler `try/catch` isolation contract into a typed `FanOut<T>` helper** — the registry hand-rolls the "for (handler of set) { try { handler() } catch {} }" loop in both the payload and status paths. Lifting it into a tiny `createFanOut<T>(): { add, remove, emit }` helper in `lib/fan-out.ts` would let the next telemetry stream (PostHog session events? Sentry breadcrumbs?) reuse the same isolation guarantee with one import.

### useNow hook (2026-05-21)

- **Wire `useNow()` into `app/chats.tsx`** — the chat list is the highest-value consumer. Today every preview's "X minutes ago" is frozen at last context re-render; subscribing the list (or each row) to `useNow()` makes the label live with negligible cost now that `formatRelativeDate` is memoised.

- **Wire `useNow()` into `app/listing/[id].tsx`** — both the "Listed X ago" header and each price-history entry's relative date benefit. The screen already imports `formatRelativeDate`; threading `useNow()` through `t("marketplaceListedAt", { when: formatRelativeDate(listing.createdAt) })` is a one-line change once the hook lands.

- **Singleton ticker via `useSyncExternalStore`** — the per-mount `setInterval` pattern is fine for ≤10 screens, but a future dashboard rendering dozens of relative-time labels would carry dozens of independent intervals. A single module-scope ticker exposing a `subscribe(listener)` API consumed via `useSyncExternalStore` would collapse them to one timer with deterministic React tear-off semantics.

- **Pause the ticker on `document.visibilityState === "hidden"`** — a backgrounded web tab doesn't need to re-render relative labels. Pairing the singleton ticker (above) with a `visibilitychange` listener that suspends ticks while the tab is hidden saves real CPU on long-lived sessions.

- **Add a `useTimestampWithTick(iso)` convenience that combines `useNow` + `formatRelativeDate`** — every caller writes the same pair of lines (`const now = useNow(); const label = useMemo(() => formatRelativeDate(iso), [iso, now])`). Promoting that to a single helper would cut each migration call site from 3 lines to 1 and gives a natural place to add per-locale memoization later.

### formatRelativeDate memoization (2026-05-21)

- **Extend the cache to `Intl.NumberFormat` / `Intl.DateTimeFormat`** — `lib/locale-helpers.ts` already exports `getDefaultLocaleForLanguage`; pairing it with a `getNumberFormat(bcp47, opts)` + `getDateTimeFormat(bcp47, opts)` helper following the same Map-cache pattern would close the same allocation hot-path for currency/price formatting in `<CurrencyInput>` and `app/listing/[id].tsx`.

- **Add a benchmark in `__tests__/format-relative-date-memoization.test.ts`** — assert that 1000 calls with the same locale return in under, say, 5ms (where unmemoized was ~50ms). A timing-based assertion would catch a future refactor accidentally reintroducing `new Intl.RelativeTimeFormat` inside the function body, even if the cache map declaration is preserved.

- **Auto-tick relative timestamps via a shared `useNow()` hook** — the existing suggestion in `.tasks.md` (line 76) becomes much cheaper now that the formatter is cached: re-rendering "1 minute ago" → "2 minutes ago" every 60s no longer re-walks the ICU tables. A 30-line hook in `lib/use-now.ts` paired with the new cache makes the auto-tick negligible-cost.

- **Promote `getRelativeTimeFormat` out of `i18n-context.tsx`** — the formatter cache is locale-derived, not React-context-derived; it belongs next to `getDefaultLocaleForLanguage` in `lib/locale-helpers.ts` so non-React call sites (server-side rendering, build-time string generation) can use the same primitive without bringing in the i18n provider.

- **Add an LRU bound to the cache** — today the Map is unbounded. Six locales today, fine; a future "all 24 EU languages" launch would let it grow to 24 entries (still cheap), but a strict LRU eviction (e.g. cap=32) is a one-line `if (cache.size > 32) cache.delete(cache.keys().next().value)` guard that future-proofs the pattern.

### closeSharedRealtimeClient on sign-out (2026-05-21)

- **Call `closeSharedRealtimeClient` from the `SIGNED_OUT` branch of `onAuthStateChange` too** — today the cleanup runs only on the user-initiated `signOut`/`deleteAccount` paths. If a session is invalidated server-side (admin revoke, password reset, expired refresh) the socket would stay open until the next reload. Mirroring the cleanup inside the auth-state listener catches every termination path.

- **Drain pending channel state from each context before disconnect** — `lib/supabase-chat.ts` and `lib/supabase-marketplace.ts` hold per-screen channels off the shared client. `closeSharedRealtimeClient` just calls `disconnect()`, which leaves the per-context `channelRef`s pointing at dead channels until those screens re-subscribe. Exposing a `closeAllSharedChannels()` that walks `client.getChannels()` would close the loop deterministically.

- **Reset the realtime cache in `clearRuntimeSupabaseConfig` too** — the URL/key reset path constructs new credentials but leaves the cached `sharedRealtimeClient` pointing at the OLD endpoint. A user who switches projects via the runtime config form would silently keep the socket alive against the old URL until something else nukes it. The new helper makes this a one-line fix.

- **Expose `getSharedRealtimeClientState()` for diagnostics** — diagnose stale sockets post-disconnect by exposing the connection state (CONNECTING/OPEN/CLOSED) of the cached client. Pairs naturally with the future "reconnecting" pill suggestion in `.tasks.md` and gives QA a one-tap visibility point.

- **Add a `disconnect-on-window-blur` heuristic** — release the socket when the web tab is hidden for >5 minutes (`document.visibilityState`). Useful for users who pin the app in a background tab — keeps the connection count under Supabase's free-tier WebSocket limit without forcing sign-out.

### Test bootstrap (2026-05-21)

- **Migrate `__resetSentryForTests` / `__resetAnalyticsForTests` into the same global bootstrap** — both helpers are currently re-called inside every Sentry/Analytics test file's `beforeEach`. Moving them into `__tests__/test-globals.ts` (with the same `require.cache` lazy-peek pattern that supabase-realtime uses) would shrink each suite by ~3 lines and close the same cross-test contamination footgun globally instead of per-file.

- **Auto-register module-resets via a `registerTestReset(fn)` side-channel** — instead of the bootstrap hand-maintaining a list of `require.cache` paths to peek at, expose a `registerTestReset` in a tiny `lib/test-reset-registry.ts` that any module can call from its top level. The bootstrap then iterates registered fns; no path strings, no growing list of `path.join(...)` calls. Mirrors how `expo-router`'s `Stack.Screen` self-registers without the parent maintaining a child list.

- **Add a `--watch` mode to the test script** that re-runs only affected suites on change (`tsx --import ./__tests__/test-globals.ts --test --watch __tests__/*.test.ts`). With the preload now in place, watch mode would re-apply the global beforeEach automatically — fast inner loop for the multi-second full-suite run.

- **Wire the test-globals preload into the lint:ci alias** — today `lint:ci` already runs `tsc --noEmit && npm test`, so it inherits the preload via `npm test`. But future contributors may add a separate "fast" test script that forgets `--import`. Add a structural test that asserts every package.json `*test*` script that runs `tsx --test` also carries `--import ./__tests__/test-globals.ts`.

- **Capture the realtime cache state in a debug snapshot** — the bootstrap silently resets; if a test fails after a contamination round-trip, there's nothing in the failure output linking the cause to the cache. Logging `[test-globals] reset shared realtime client` when `DEBUG_TEST_GLOBALS=1` would make it trivial to see whether the cache *was* populated when the failing test ran — useful when chasing flaky integration tests.

### i18n ↔ locale parity (2026-05-21)

- **Export `languageOptions` from `lib/i18n-context.tsx`** — the parity test currently parses the array from the source string via regex because `languageOptions` is module-local. Promoting it to a named export (it's already part of the public `I18nContextValue` shape returned by `useI18n`) would let the test import the actual array and remove the regex parse, closing the small risk that a future refactor changes the literal shape and slips past the regex.

- **Extend parity to translations record keys** — the test catches drift between `languageOptions` and the locale maps but not between the picker and the `translations` record (`{ en, ru, be, pl, de, es }`). A future language added to the picker without a translation map would render bare keys; mirror the same parity assertion against the `translations` Record<AppLanguage, TranslationMap> declaration.

- **Pull the EXPECTED_LANGUAGES list into a shared `__tests__/helpers/supported-languages.ts`** — both `i18n-translations.test.ts` and the new parity test hand-maintain the list of language codes. A single tuple consumed by both keeps them in lockstep and is the natural place to add the next language.

- **Make `languageOptions` order-stable via an `as const` tuple typed against `AppLanguage`** — today the array is loosely typed `{ code: AppLanguage; label: string }[]`. Switching to `as const satisfies readonly { code: AppLanguage; label: string }[]` lifts the codes into a literal union, lets the i18n type system flag a code that isn't a known `AppLanguage`, and makes the picker ordering visible to consumers.

- **Add a runtime guard `assertLanguageMapsAligned()` in `lib/locale-helpers.ts`** — invoked once at module init in dev (`__DEV__`), `console.warn`s when `languageCurrencyMap` and `languageLocaleMap` cover different key sets. The new test guards drift at PR time; a runtime guard closes the gap if someone bypasses the test via an env override (the maps are env-merged for `LANGUAGE_CURRENCY`).

### User preferred currency (2026-05-20)

- **Surface the preferred currency on the settings screen** — add a "Default currency" row next to the language picker so a power user can change it from one place instead of editing a draft listing. The `getUserPreferredCurrency` / `setUserPreferredCurrency` pair is already in place; the UI is the only missing piece.

- **Reactive subscription via a `useUserPreferredCurrency()` hook** — every screen currently hydrates the value with a one-shot `useEffect`. A tiny in-module subscriber registry inside `locale-helpers.ts` (set-on-write fans out to listeners) would keep multiple open forms in sync without each one polling AsyncStorage.

- **Server-side sync for cross-device parity** — persist the preferred currency into `profiles.preferred_currency text` and read it back on auth so a user who picks JPY on the web sees JPY on iOS without re-picking. Today the slot is local-only; a `chat_id_integrity`-style migration plus a small `lib/supabase-profiles.ts` field would close the gap.

- **Show a "currency changed" toast on first persist** — non-obvious side effect (the next listing form will remember the pick) currently happens silently. A one-shot toast keyed on the first `setUserPreferredCurrency` call this session would teach users the feature exists.

- **Migrate to the same env-override pattern as `EXPO_PUBLIC_LANGUAGE_CURRENCY`** — add `EXPO_PUBLIC_PREFERRED_CURRENCY_DEFAULT` that ships a build-time override (e.g. JPY for the JP App Store track) honoured only when no per-user pick has been stored yet. Mirrors how `parseLanguageCurrencyOverride` already lets QA flip per-region defaults without a code change.

### Chat DB (2026-05-16)

- **Cursor pagination for `fetchMessagesForChat`** — it currently `select=*&order=created_at.asc` with no `limit`, so a long-lived conversation pulls the entire history into memory on every hydrate. Add `&limit=50` + a `created_at` cursor (or PostgREST `Range` header) and load older pages on scroll-up. Bounds memory and free-tier egress.

- **Realtime reconnect backfill** — while the WebSocket is down, `subscribeToInbox` misses INSERTs and only `refreshFromCloud` (mount-time) closes the gap. On `SUBSCRIBED` after a drop, fetch messages `created_at=gt.<last-known>` per active chat so no message is silently lost during a flaky connection.

- **First-class conversations table** — replace the free-text `chat_id` with a `conversations(id uuid, user_a uuid, user_b uuid, UNIQUE(user_a,user_b))` table and a `chat_messages.conversation_id uuid REFERENCES conversations(id)`. A real FK + unique participant pair is stronger than the derived-string CHECK and unlocks per-conversation metadata (muted, archived, last_message_at) without re-deriving from messages.

- **Functional idempotency test with a mocked fetcher** — the new ignore-duplicates path is only covered structurally. Add a `__tests__/supabase-chat-idempotency.test.ts` that injects a fake `fetcher` returning `200 []` and asserts `sendMessage` resolves to the synthesized message (not `null`), plus a fake returning a populated row for the happy path.

- **`chat_messages.client_id` + server uuid split** — keep the server `id uuid DEFAULT gen_random_uuid()` authoritative and add a separate indexed `client_id uuid` (the composition-time key) with a UNIQUE constraint for idempotency. Decouples "who owns the primary key" from "idempotency key", which is the canonical chat-storage pattern and avoids ever trusting a client-supplied PK.

- **SPA fallback regression guard** — Add a CI step that runs `npm run build` then asserts (a) `dist/404.html` contains `location.replace`, (b) `dist/index.html` contains `data-spa-restore`. This catches a future contributor accidentally reverting the post-build script (e.g. swapping back to `cp dist/index.html dist/404.html`) before the change reaches production iOS users.

- **Service worker for instant deep-link load** — Even with the sessionStorage redirect, the user sees a flash of the redirect page on iPhone. Registering a service worker (Workbox `NetworkFirst` + `NavigationFallback` to `/collectables/index.html`) would let cached deep links resolve to 200 directly and remove the redirect entirely on repeat visits.

- **Sentry breadcrumb for SPA URL restore** — Drop an `addBreadcrumb("spa-redirect", { from, to })` inside the restore script (gated on `__sentry_loaded__`) so Sentry sessions show exactly which deep-link URLs hit the 404 fallback. Useful for ranking which routes deserve a real static export.

- **Static export of common collection IDs** — Expo Router could pre-render the top-N most-shared collections at build time (e.g. by reading from Supabase) so their URLs return real 200s without the redirect dance. Reduces flicker and helps SEO for public collections.

- **Migrate baseUrl to a single source** — `app.json` declares `expo.experiments.baseUrl`, `lib/url-helpers.ts` infers it at runtime, and `scripts/build-spa-fallback.ts` re-reads it from app.json. A single `lib/base-url.ts` exporting a const + a helper that all three import would eliminate the drift risk if the deployment path ever changes (e.g. moving off the `1337ninja1337.github.io/collectables` prefix).

- **CI guard: regenerate the .pbit and fail on drift** — Add a `build:powerbi` step to the GitHub Actions workflow that runs `npm run build:powerbi` then `git diff --exit-code docs/powerbi/Collectables-Starter.pbit`. The `powerbi-template.test.ts` parity test already catches drift, but a workflow step makes the regeneration command discoverable and stops a stale binary ever reaching `main` if someone edits `measures.dax`/`queries.m` without rebuilding.

- **Reference report layout in the .pbit** — The template currently opens to a blank canvas. Build a real `Report/Layout` with a line chart (`occurred_at` × `DAU`), two KPI cards (`ListingFunnelRate`, `PremiumConversionRate7d`), and a default theme resource package so users get a dashboard on first open. Gate it behind a `--with-report` flag so the minimal (lower open-risk) layout stays the default until it's validated in Power BI Desktop.

- **Generalise the OPC/ZIP writer** — `zipStore`/`readZipEntries`/`crc32` in `lib/powerbi-template.ts` are a generic, dependency-free deterministic ZIP. Promote them to `lib/opc-zip.ts` so future binary artefacts (e.g. an `.xlsx` data export, a bundled theme pack) reuse the same byte-stable packer instead of pulling in `jszip`/`archiver`.

- **Power BI parameter validation in the M** — The four `Supabase*` parameters are free-text. Add a guard in the partition M (`if SupabasePort = "" then error "SupabasePort is required" else ...`) and a regex check on the host so a user who clicks through the prompt with blanks gets a clear Power Query error instead of an opaque PostgreSQL connection failure.

- **`.pbit` schema-version matrix doc** — `compatibilityLevel: 1550` targets a broad Power BI range, but the report/model JSON evolves across Desktop releases. Add a short table in `docs/powerbi/README.md` mapping the Power BI Desktop versions the template was sanity-checked against (and known-incompatible ones) so users hitting an open failure immediately know to fall back to the copy-paste assets.

- **pgTAP automated RLS test** — The Analytics #16 leak check is a manual paste-into-SQL-editor checklist. Add a `supabase/tests/analytics_events_rls.sql` pgTAP test (`SET ROLE anon; SELECT throws_ok('SELECT … analytics_events', '42501')`) so `supabase test db` enforces the default-deny posture in CI instead of relying on an operator remembering to run it.

- **Reusable RLS-deny audit query** — Generalise the leak check into a single query over `pg_policies` + `information_schema.role_table_grants` that lists every `public.*` table reachable by `anon`/`authenticated`, and document it in MANUAL-TASKS.md. One run flags *any* table that regresses to readable, not just `analytics_events` — cheap defence-in-depth as more tables land.

- **CI grep guard against analytics_events policies** — Add a lint/CI step that greps `supabase/migrations/*.sql` for `CREATE POLICY .* analytics_events` (executable SQL, comment-stripped) and fails the build. The structural test already asserts this for the one migration; a directory-wide grep catches a *future* migration file that adds a permissive policy from outside `20260508_*`.

- **Edge Function service-role assertion** — `analytics-mirror` inserts with the service-role key; add a startup self-check (and a unit assertion) that the resolved key is the service-role JWT (`role: "service_role"` claim) and not the anon key, so a misconfigured secret fails loudly at deploy time instead of silently 401/permission-denying every insert against the RLS'd table.

- **Retention / PII sweep for analytics_events** — The store is long-tail by design; add a documented `DELETE FROM analytics_events WHERE occurred_at < now() - interval '13 months'` cron (pg_cron) plus a note in the privacy doc, so the unbounded event history honours a stated retention window for GDPR — pairs naturally with the existing default-deny posture.

### Analytics rate limiter (2026-05-19)

- **Surface a dropped-event counter** — `rateLimitAllow()` silently returns false when the window is full, so an over-firing screen looks identical to a quiet one. Track a module-level `droppedEventCount` and a `getAnalyticsRateLimitStatus()` snapshot (mirrors `getSentryStatus()`); a future admin/devtools probe can then tell "no events" apart from "rate-limited", which is the difference between a dead instrumentation path and a hot loop.

- **Per-event-name sub-limits** — A single global 200/min bucket lets one chatty event (e.g. a mis-placed `item_photo_attached` in a render) starve rare high-value events like `premium_activated`. A small `Map<AnalyticsEventName, number[]>` with a low per-name cap (e.g. 30/min) plus the existing global ceiling would protect conversion-critical events from a noisy neighbour.

- **Coalesce dropped events into one summary capture** — Instead of fully discarding everything past the cap, emit a single `analytics_rate_limited` event once per window carrying `{ dropped, topEventName }`. Keeps the free-tier bounded while preserving the signal that *something* is looping — currently the loop is invisible in PostHog itself.

- **Shared rate-limiter primitive** — `lib/sentry.ts` and `lib/analytics.ts` now carry byte-identical `RATE_LIMIT_WINDOW_MS` / `recentEvents` / `rateLimitAllow` blocks. Extract a `createSlidingWindowLimiter(max, windowMs)` factory into `lib/rate-limit.ts` (returning `{ allow, reset }`) so both wrappers, plus future ones (chat send, marketplace claim), share one tested implementation instead of three copies drifting apart.

- **Make the cap env-tunable** — Add `EXPO_PUBLIC_ANALYTICS_MAX_EVENTS_PER_MIN` resolved via the proposed `resolveNumericEnv` helper so incident response can throttle analytics to near-zero (or raise it for a load test) without a redeploy — same operational lever the `EXPO_PUBLIC_REALTIME_DISABLED` kill-switch suggestion proposes for WebSockets.

### Diagnostics Do-Not-Track (2026-05-19)

- **Surface a "respecting your browser's Do-Not-Track" line in settings** — When `readDoNotTrack()` is true and the user has made no explicit choice, the toggle silently sits OFF with no explanation. Add an i18n hint under the toggle (`t("diagnosticsDntHint")`) shown only in that state so an EU user understands *why* it defaulted off and that flipping it on is an explicit consent override — clearer GDPR UX and removes a "why is this off?" support question.

- **Global Privacy Control (GPC) signal** — GPC (`navigator.globalPrivacyControl === true`) is the modern successor to DNT and is legally recognised under CCPA/CPRA. Extend `readDoNotTrack()` (or add a sibling `readPrivacySignal()`) to also honour GPC so the opt-out default tracks the standard regulators now point to, not just the deprecated DNT header.

- **Re-evaluate DNT default on web focus** — DNT is only read once on provider mount. If a user changes their browser DNT setting mid-session (or in another tab) the app keeps the stale default until reload. A `visibilitychange`/`focus` listener (web-only) that re-runs `resolveDiagnosticsEnabled` when there's still no explicit stored choice would keep the signal honoured live.

- **Persist a `source` alongside the stored choice** — `{ enabled }` doesn't record whether the value came from an explicit user toggle, a DNT default, or a migration. Storing `{ enabled, source: "user" | "dnt-default" }` lets a future migration safely re-derive defaults (e.g. if the DNT policy changes) without clobbering real user consent, and feeds an analytics property on how many installs are DNT-defaulted.

- **`__tests__/diagnostics-provider-wiring.test.ts`** — The pure helpers are well covered but the `DiagnosticsProvider` effect (hydrate → `setSentryOptOut`/`setAnalyticsOptOut`/`setClarityOptOut` + conditional `init*`) is only exercised indirectly. Add a structural test asserting the provider calls `resolveDiagnosticsEnabled(raw, readDoNotTrack())` on hydrate and that `setDiagnosticsEnabled(false)` calls all three `shutdown*` paths — guards the GDPR contract against a refactor that drops a sub-processor from the fan-out.

### Privacy policy & sub-processors (2026-05-19)

- **Generate PRIVACY.md from the App Privacy table** — `APPSTORE-SUBMISSION.md` §6 now holds the canonical table + sub-processor paragraphs, but the actual public `PRIVACY.md` (the URL App Review checks) is described, not generated. Add a `scripts/build-privacy-md.ts` that extracts the table + paragraphs into a standalone `PRIVACY.md` deployed to GitHub Pages, plus a parity test, so the published policy can never drift from the App Store Connect declaration.

- **Localise the public privacy paragraphs** — The app ships 6 languages (`ru/en/be/pl/de/es`) but the privacy paragraphs are English-only. EU (GDPR) and Apple guidelines expect the policy in the app's supported languages; wire the paragraphs through the existing `t()` i18n catalogue (or a `docs/privacy/<lang>.md` set) so a German/Polish user gets a policy they can read.

- **CI guard: SDK ↔ privacy-table parity** — Add a test that greps `package.json` + `lib/clarity.ts` for telemetry SDKs (`@sentry/*`, `posthog-react-native`, `clarity.ms`) and asserts each has a matching row in the `APPSTORE-SUBMISSION.md` App Privacy table. Today adding a new analytics SDK wouldn't fail any check until App Review rejects the build — a parity test shifts that left.

- **Document the PostHog/Clarity data-retention windows** — The Sentry paragraph states a concrete 90-day retention; the new PostHog and Clarity paragraphs don't. GDPR Art. 13(2)(a) requires the storage period. Add the configured/contractual retention for each (PostHog project setting, Clarity's 30-day default) so the disclosure is complete.

- **Surface the active sub-processor list in-app** — Add a read-only "Data & privacy" sub-screen under Settings that renders the same sub-processor list (Sentry/PostHog/Clarity) and a link to the policy. Several App Review rejections stem from the policy being hard to reach; an in-app surface that mirrors `APPSTORE-SUBMISSION.md` closes that and gives users one place to see who processes their data.

### Analytics test-coverage audit (2026-05-19)

- **Functional rate-limiter window-expiry test** — `analytics-rate-limit.test.ts` covers the cap and the reset helper but not the *sliding* behaviour. Inject a `now` clock (or temporarily monkeypatch `Date.now`) and assert that events allowed >60s ago no longer count, so a regression that turns the sliding window into a fixed one (or drops the `recentEvents.filter` cutoff) is caught.

- **CI guard: every test file is actually picked up by the glob** — `npm test` runs `tsx --test __tests__/*.test.ts`; a file mis-named `*.tests.ts` or in a sub-dir is silently skipped (it would still pass CI). Add a meta-test that globs `__tests__/**` and asserts every `*.test.ts` it finds is also matched by the exact `__tests__/*.test.ts` pattern the script uses.

- **Assert AnalyticsProvider mounts under DiagnosticsProvider** — wiring tests check AnalyticsProvider sits under Auth/I18n/Premium, but not that it mounts *inside* `<DiagnosticsProvider>`. If it ever rendered outside, `identifyUser` could fire before the opt-out flag hydrates. A one-line index-order assertion closes that ordering gap.

- **Snapshot the `AnalyticsEventName` ↔ `ANALYTICS_EVENTS` parity at the type level** — `analytics-events.test.ts` checks the runtime keys equal the expected list, but a `satisfies Record<AnalyticsEventName, AnalyticsEventDefinition>` annotation on `ANALYTICS_EVENTS` would make a missing/extra event a *compile* error too, catching drift before the test even runs.

- **Coverage threshold in CI** — There's no coverage gate, so a new uncovered branch can land green. Wire `node --test --experimental-test-coverage` (or `c8`) with a modest line/branch floor on `lib/analytics*.ts` + `lib/diagnostics-context.tsx` so the telemetry/consent core can't silently lose coverage.

- **Make `resolveNumericEnv` behaviourally unit-testable** — it now lives in `lib/env.ts` which imports `react-native` (`Platform`), so it can only be structurally tested. Move the pure parser into the existing react-native-free `lib/url-helpers.ts` (or a new `lib/env-helpers.ts`) and re-export it from `lib/env.ts`, mirroring the `getAppBaseUrl` → `resolveAppBaseUrl` split. Then a real `__tests__/env-helpers.test.ts` can assert the empty/NaN/zero/negative/valid matrix instead of regex-matching source.

- **Add an optional `{ min, max }` clamp to `resolveNumericEnv`** — the `EXPO_PUBLIC_PROFILE_CACHE_TTL_MS < 30s` foot-gun already has its own pending task (a warning toast). A `resolveNumericEnv(raw, def, { min: 30_000 })` overload that floors out-of-range overrides at the source would protect Supabase free-tier limits for *every* future tunable (chat poll, marketplace cache) without each caller re-deriving the guard.

- **Promote `normaliseEnvironment` into `lib/env.ts` next to `resolveNumericEnv`** — `lib/sentry-config.ts` / `lib/analytics-config.ts` both re-implement an env-name normaliser; co-locating it with the new numeric resolver finishes the "one env-parse surface" consolidation the `.tasks` entry started and lets a single `runtime-config-parity` test cover both.

- **Lint guard against computed `process.env[...]` access** — the Metro-inlining foot-gun is currently policed by hand-written structural tests (`sentry-env-inlining`, `env-resolve-numeric`). A 15-line ESLint `no-restricted-syntax` rule banning `MemberExpression[computed=true][object.object.name="process"][object.property.name="env"]` would enforce it codebase-wide and fail the PR instead of one specific file's test.

- **Reuse `resolveNumericEnv` for the next tunable now, not later** — the `.tasks` entry cites "chat poll interval, marketplace cache" as future beneficiaries but ships zero second callers, so the helper's generality is unverified. Migrate one real numeric knob (e.g. a `EXPO_PUBLIC_CHAT_POLL_MS` for `lib/chat-context.tsx`) in the same PR family so the abstraction is proven by a second consumer rather than asserted.

### Low-TTL warning toast (2026-05-20)

- **Surface the configured TTL value inside the warning toast** — today the message reads "below 30 seconds" without echoing what the operator actually set. Threading the parsed milliseconds via `t("profileCacheTtlLowMessage", { configured, minimum })` would make the warning self-diagnosing ("you set 5000ms, recommended ≥30000ms") and remove the operator's "what did I set, again?" round-trip to `.env`.

- **Promote `lowProfileCacheTtlWarningShown` into a typed `lib/once-toast.ts` registry** — the module-scope `let warned = false` pattern is now used by Sentry (`__resetSentryForTests`), Analytics (`__resetAnalyticsRateLimitForTests`) and this new TTL warning. A `registerOnceToast(key, () => …)` helper that owns the flag + reset would shrink each call site to a single `useOnceToast("low-profile-cache-ttl", …)` and centralise the testing escape hatch.

- **Mirror the same soft-floor warning for future TTL overrides** — `EXPO_PUBLIC_REALTIME_DISABLED` (pending), a hypothetical `EXPO_PUBLIC_CHAT_POLL_MS`, and the marketplace cache TTL all want the same "value below recommended floor → warn once" treatment. A generic `useNumericEnvFloorWarning(rawValue, { minimum, title, message })` hook would let new tunables ship the warning with one line instead of replicating the `useEffect` + module flag in every provider.

- **Document the soft floor in `README-DEPLOY.md` and the upcoming `.env.example`** — the `MINIMUM_RECOMMENDED_PROFILE_CACHE_TTL_MS = 30_000` constant is now the canonical floor but only this file mentions it. Surfacing it in the operator docs (alongside the existing pending "document the env var" suggestion) closes the loop between the warning toast and the place an engineer reads to learn *why* 30s was chosen.

- **Add an `__tests__/i18n-translations-profile-cache.test.ts` parity test** — five language maps now carry `profileCacheTtlLow{Title,Message}`. A structural test that walks every `AppLanguage` and asserts both keys resolve to a non-empty string would catch a future language addition that forgets the override (today `...en` masks the regression at runtime by silently shipping English to a non-English user).

### Tracked `.env.example` + env-doc parity (2026-05-20)

- **Promote the README/.env.example parity scan into a runnable script** — the new `profile-cache-ttl-env-docs.test.ts` walks the README table and asserts every `EXPO_PUBLIC_*` row exists in `.env.example`. Lift the walker into a small `scripts/check-env-docs.ts` so a contributor can run `npm run check:env-docs` locally before pushing, instead of waiting for CI. Pairs naturally with the pending `runtime-config-parity.test.ts` suggestion.

- **Extend the parity test to scan `eas.json` and `.github/workflows/deploy.yml`** — today the scan covers README ⇄ `.env.example` only. The same `EXPO_PUBLIC_*` names also live in EAS secrets and the GitHub Actions secrets block; a four-way scan would catch the "added a new env var to one workflow but missed another" foot-gun that the existing `eas-secrets-parity` suggestion in `.tasks.md` already flagged but never landed.

- **Document `EXPO_PUBLIC_PROFILE_CACHE_TTL_MS` in `APPSTORE-SUBMISSION.md` §13 EAS secrets** — the iOS submission checklist already lists every other `EXPO_PUBLIC_*` as an `eas secret:create …` line; threading the new variable in keeps the iOS build path aligned with the web build path. Today only the web `README-DEPLOY.md` mentions it.

- **Add an `# How to use` header block at the top of `.env.example`** — the file now boots the convention but reads as a flat list. A short stanza pointing at `README-DEPLOY.md` for full descriptions + flagging which vars are optional vs required would shorten an engineer's first-touch from "read three files" to "copy and edit one".

- **Wire `.env.example` placeholders through a structural test that rejects committed `.env`** — the no-secret-in-example guard already exists in the new docs test. Add a counterpart `__tests__/dotenv-not-committed.test.ts` that asserts `.env` is absent from `git ls-files`, so a future contributor running `git add -A` after editing their local `.env` can't accidentally land credentials in a PR.

### Realtime kill-switch (2026-05-20)

- **Surface the kill-switch state on `app/settings.tsx` diagnostics screen** — `EXPO_PUBLIC_REALTIME_DISABLED=1` silently flips every realtime feature off; a "Realtime: disabled by environment" row (read from `isRealtimeDisabledByEnv(process.env.EXPO_PUBLIC_REALTIME_DISABLED)`) tells a tester why their chat inbox is offline without spelunking the bundle. Mirrors the pending `getAnalyticsStatus()` diagnostics row suggestion.

- **Add a `closeSharedRealtimeClient()` cleanup that the kill-switch can call** — today flipping the env var on a running session still leaks the active WebSocket because `sharedRealtimeClient` was already constructed. Pairing the kill-switch with a `closeSharedRealtimeClient()` helper (callable from a DevMenu entry or on auth sign-out) would drop the socket promptly. Already pending as its own suggestion — promote both together for incident-response symmetry.

- **Mirror the truthy-literal parser in a shared `lib/env.ts:isTruthyEnv(rawValue)` helper** — the new `isRealtimeDisabledByEnv` accepts `1`/`true`/`yes`. Future boolean toggles (analytics mirror disable, premium-gate bypass, etc.) want the same shape. A 6-line helper next to `resolveNumericEnv` would centralise the lowercase/trim/whitelist dance and let `isRealtimeDisabledByEnv` shrink to a one-liner wrapper.

- **Wire a Sentry breadcrumb when the kill-switch is active** — an `addBreadcrumb("realtime-disabled-by-env", { source: "EXPO_PUBLIC_REALTIME_DISABLED" })` on first call to `getSharedRealtimeClient()` while disabled would make "user says chat is offline" tickets one grep away in the Sentry timeline. Cheap to add: gated on `__sentry_loaded__` so it no-ops when Sentry isn't configured.

- **Add a runtime integration test for the kill-switch path** — today only structural assertions exist. A test that monkey-patches the env, re-imports the module (via `vm.SourceTextModule` or by exporting a `__buildSharedClient(env)` factory) and asserts `getSharedRealtimeClient()` returns null even when `isSupabaseConfigured` is true would catch a future refactor that re-orders the gates wrong.

### CURRENCY_CHIPS picker whitelist (2026-05-20)

- **Tighten `<CurrencyInput>`'s currency prop type from `string` to `CurrencyChipCode`** — the new literal-union type is exported but not yet threaded through the component props. Switching `currency: string` → `currency: CurrencyChipCode` makes a typo on any consumer page (`<CurrencyInput currency="USDT" />` → arbitrary unknown chip) fail at compile time instead of silently rendering nothing-selected. Same for `onChangeCurrency: (c: string) => void`.

- **Reuse `CURRENCY_CHIPS` in `app/create.tsx`'s autocomplete to short-list the most common codes** — today the full 150-entry `lib/currencies.ts` dictionary feeds the autocomplete; surfacing `CURRENCY_CHIPS` as a "Common" header pinned to the top of the suggestions would speed up the 99% case (USD/EUR/GBP/etc.) without removing the long tail. The two lists are intentionally separate; a UI that uses both gets the best of each.

- **Add an `EXPO_PUBLIC_EXTRA_CURRENCY_CHIPS=NZD,SGD` operator override** — `CURRENCY_CHIPS` is now centralised but still hard-coded. A comma-separated env var read once at module load (using the same literal-`process.env.X` pattern as `EXPO_PUBLIC_REALTIME_DISABLED`) would let a regional launch add chips without a code change. Mirror the validation: filter inputs against `lib/currencies.ts:isCurrencyCode` so a typo doesn't render a broken chip.

- **Promote a `localeDisplayName(code, language)` helper next to `CURRENCY_CHIPS`** — today the picker renders raw codes ("USD", "EUR"). `Intl.DisplayNames` makes `"USD"` → `"US Dollar"` (English) / `"Доллар США"` (Russian) trivial; co-locating the helper in `lib/locale-helpers.ts` keeps the picker presentation logic near its data and lines up with the pending `getDefaultLocaleForLanguage` suggestion.

- **Add a `__tests__/currency-chips-iso-4217-parity.test.ts` that asserts every entry in `CURRENCY_CHIPS` exists in `lib/currencies.ts:CURRENCIES`** — a future contributor adding a deprecated currency code (e.g. ISO 4217 retired some after 2024) to the chips list would render a chip that the autocomplete can't validate. A 10-line parity test catches the drift between the curated shortlist and the canonical dictionary.

### BCP-47 locale helper (2026-05-20)

- **Use `getDefaultLocaleForLanguage` to drive a centralised `formatCurrency(amount, currency, language)` helper** — today `<CurrencyInput>` shows the raw code (e.g. `USD 12.34`); pairing the new BCP-47 lookup with `new Intl.NumberFormat(locale, { style: "currency", currency })` would render `$12.34` / `12,34 €` / `12,34 zł` automatically. A 10-line helper in `lib/locale-helpers.ts` would replace every hand-rolled price label across `listing/[id]`, `chats`, marketplace.

- **Extend the parity test to scan `languageOptions` (i18n) against `languageLocaleMap` and `languageCurrencyMap`** — today the new test asserts the two locale-helpers maps share a key set. Adding a third arm — every key in `languageOptions` must also exist in both maps — would close the "new language added to the picker but not to the formatter maps" foot-gun (pending suggestion at line ~52 of `.tasks.md`).

- **Add region-fallback constants** (`DEFAULT_LOCALE = "en-US"`, `DEFAULT_REGION = "US"`) **next to the new map** so a future caller that needs *only* the region (e.g. tax/VAT lookup) doesn't have to `.split("-").pop()` the BCP-47 tag. Cheap encapsulation that grows with the i18n surface.

- **Mirror the new helper inside the chat realtime client** — `lib/chat-context.tsx` formats message timestamps with `formatRelativeDate(iso, language)`. The BCP-47 upgrade is now active there for free, but a future `formatExactTime(iso, language)` would benefit from the same `Intl.DateTimeFormat(getDefaultLocaleForLanguage(language), { hour, minute })` pattern — co-locate the second helper next to the first.

- **Document the new map in `.env.example` as a comment block** so an engineer adding a new language for QA understands they need to touch *three* lib files: `LANGUAGE_LOCALE`, `LANGUAGE_CURRENCY`, and the i18n translations map. Today the trio is a tribal-knowledge requirement.

### Language→currency env override (2026-05-20)

- **Expose a `__resetLanguageCurrencyForTests()` helper** in `lib/locale-helpers.ts` that re-evaluates `parseLanguageCurrencyOverride(process.env.EXPO_PUBLIC_LANGUAGE_CURRENCY)` and rebuilds `LANGUAGE_CURRENCY`. Today the map is captured once at module-load time, so a test that wants to assert real override behaviour (vs. structural-only) has no clean way to set the env var mid-suite and observe the change. Mirrors the `__resetSharedRealtimeClientForTests` / `__resetLowProfileCacheTtlWarningForTests` pattern.

- **Warn once at runtime when `EXPO_PUBLIC_LANGUAGE_CURRENCY` carries dropped tokens** (e.g. `ru:EURO` is silently ignored today). A single `toast.info` listing the rejected entries — gated on `__DEV__` so it doesn't ship to production — would surface typo'd overrides during QA instead of letting the operator wonder why their override "didn't work". Mirrors the low-TTL warning toast pattern shipped on 2026-05-20.

- **Extend the parser to accept fallback-currency syntax** like `ru:RUB|EUR` so QA can stage a primary + fallback currency for A/B testing locale launches. Today the parser is strict 1:1; a `|`-separated tail would unlock a `getDefaultCurrencyCandidatesForLanguage(language): string[]` helper for future fallback-aware pickers without re-implementing the parse-and-validate dance.

- **Add an `EXPO_PUBLIC_LANGUAGE_LOCALE` companion env override** (format `ru:ru-RU,en:en-GB`) in `lib/locale-helpers.ts` mirroring this task's shape, so QA can flip BCP-47 region tags (date/number formatting) per language alongside the currency flip. The two maps already share a key set per the existing parity test — a parallel parser + structural test would close the symmetry gap.

- **Migrate `EXPO_PUBLIC_PROFILE_CACHE_TTL_MS` to use the same defaults-then-override merge pattern this task introduced** — promote a `DEFAULT_PROFILE_CACHE_TTL_MS = 600_000` next to the resolver in `lib/env.ts` (already exists locally in `lib/social-context.tsx` as `DEFAULT_VIEWER_PROFILE_TTL_MS`) and export it so a future test or operator UI can show "configured: X (default: Y)" without re-declaring the constant. Cheap consolidation that mirrors the new `DEFAULT_LANGUAGE_CURRENCY` export shape.

### TAG_* categorical palette (2026-05-23)

- **Export the `TAG_COLORS` rotation array from `lib/design-tokens.ts` itself** so the categorical palette ships as a first-class token-module export (`export const TAG_COLORS = [AMBER_ACCENT, TAG_RUST, ...]`) and the create-item screen consumes the same array a future settings/profile screen would. Today `TAG_COLORS` is re-declared at the top of `app/create.tsx` — DRY-ing it into the design-tokens module would also let the existing palette-frozen test in `__tests__/design-tokens.test.ts` enforce the rotation order (so a tag re-shuffling fails CI). Sub-payoff: any future `TagColorPicker` component can `import { TAG_COLORS } from "@/lib/design-tokens"` without re-implementing the array.

- **Add a `TAG_*` adoption test that asserts no `app/**/*.tsx` file re-declares a literal `["#d89c5b", "#c47a5a", ...]` array** — the 10-color tag rotation is now a brand-anchored palette; a future contributor copy-pasting the array into `app/item/[id].tsx` (next migration target) would silently bypass the centralized palette. A grep-style structural test (`assert.equal(src.match(/"#c47a5a"/g)?.length ?? 0, 0)` across the `app/` tree) closes the regression vector for free.

- **Wire the existing `TAG_COLORS[tags.length % TAG_COLORS.length]` modulo-rotation into a `pickNextTagColor(existing: ItemTag[]): string` pure helper** in `lib/tag-colors.ts` — today the modulo lives inline in two onSubmit handlers (`tagInput`-keyboard-submit + `tagAddButton`-press) in `app/create.tsx` (lines 314 + 324). Extracting it as a pure function would (a) DRY the two call sites onto one helper, (b) let a future "skip a color already used elsewhere in the collection" smart-pick swap in without touching the JSX, (c) earn a unit test that pins the rotation behaviour (today untested). Mirrors the `lib/uuid.ts` carve-out pattern.

- **Add an `AMBER_MUTED` family palette test** that asserts the 6-tier (`AMBER_MUTED` → `AMBER_MUTED_6`) lineage is monotonically lighter on the L axis of HSL space — today the lineage is named by index but a future "AMBER_MUTED_7 = #fff000" addition would silently break the visual ladder. A 15-line test using a `hexToHslLightness(hex): number` helper would enforce `L_n < L_{n+1}` across all 6 tiers. Same shape applies to `DANGER_DEEP_*` (5 tiers) and `MUTED_*` (21 tiers) — semantic ordering checks are cheap.

- **Generate a runtime-introspectable "tag swatch preview" gated behind `__DEV__`** at `app/_dev/tag-colors.tsx` rendering each of the 10 `TAG_COLORS` as a labelled chip alongside a sample "Rare" / "Limited" / "Vintage" tag label. Pairs naturally with the existing pending suggestion ("Generate a Storybook-style 'tokens preview' route at `app/_dev/tokens.tsx`") and gives designers a way to vet the categorical palette without spinning up the full create-item screen with 10 sample tags.

### app/collection/[id].tsx design-tokens migration (2026-05-25)

- **Consolidate `MUTED_22 = "#6b4d35"` and `MUTED_23 = "#6a4d35"` into one token** — they're 1 byte apart on the red channel (same visual color to the human eye). The collection-detail file uses them as `bulkBarButtonGhost.borderColor` and `modalCancelText.color` respectively; merging onto a single `MUTED_22` would close a likely typo-origin lineage without any visual regression. Cheap palette-consolidation pass; pair with an HSL-distance test that flags any two tokens within `< 1% ΔE`.

- **Extract the `loadMore` + `loadMoreText` style pair into a reusable `<LoadMoreButton>` component** under `components/load-more-button.tsx` — the same styles + onPress + accessibility shape will likely repeat once the in-collection search/sort gets its own paginated child surface (and any future "Load more" on chats / marketplace / wishlist). The migration already centralized the colors; lifting the JSX closes the duplication before it spreads.

- **Add a structural test asserting `app/collection/[id].tsx` has no top-level `Object.freeze(...)` palette re-declaration** — now that the design-tokens migration is "fully proven", a future contributor refactoring the file might be tempted to re-declare a local palette object. The codebase's convention is `lib/design-tokens.ts` is the single source of truth; a grep-style guard (`assert.equal(src.match(/Object\.freeze/g)?.length ?? 0, 0)`) closes the regression vector.

- **Generate a token-usage heatmap script `scripts/token-usage-report.ts`** that scans `app/**/*.tsx` + `components/**/*.tsx` for each `lib/design-tokens.ts` export and prints a histogram of usage counts. Today nothing tells you that `BORDER` is used 47x while `MUTED_23` is used 1x — a usage report would surface candidates for the next palette-consolidation pass (e.g. promote single-use tokens into their files, or flag tokens with `0` uses as dead weight).

- **Add an HSL-axis monotonicity test for the `MUTED_*` family** — `MUTED_1` through `MUTED_23` is now a 23-tier lineage but the index order doesn't guarantee any visual ordering. A 20-line test using `hexToHslLightness(hex): number` would assert one of the axes (lightness, saturation, or hue) is monotonic across the index — surfacing accidental "MUTED_24 = #ff0000" additions that break the naming convention's implicit semantics. Same shape works for `TEXT_ON_DARK_*` (9 tiers), `DANGER_DEEP_*` (6 tiers), `CARD_BG_*` (13 tiers).

### components/qr-code.tsx design-tokens migration (2026-05-25)

- **Accept design-token names (not raw strings) in `QrCodeProps.color/background`** — today the prop is typed as `string`. Typing it as `DesignToken | (string & {})` (the established `(string & {})` trick already pending in `.tasks.md`) would give consumers autocomplete + typo-detection on the common path while still accepting any raw color string for one-off uses. Pairs with the existing pending `BackgroundColorValue/ColorValue` type-narrowing task.

- **Cache the `QRCode.create(value, ...)` matrix** across renders for the same `value` — today every `useMemo` walks the full library encode on each new `value`. A `WeakMap<string, boolean[][]>` keyed by value would close the gap for re-render heavy screens (e.g. the share sheet that re-renders the QR on every keystroke in the link field). Cheap optimisation matching the same memoization-by-key pattern already used elsewhere.

- **Add a structural test asserting `components/qr-code.tsx` exports the `QrCode` function with default `color`/`background` that resolve to design tokens** (not raw hex). Today the new adoption test only checks for the absence of inline hex; a stronger contract test would `import { QrCode } from "@/components/qr-code"` and assert the function signature carries `color = HERO_DARK, background = PURE_WHITE` via source-scan. Closes the regression vector where a future refactor could re-introduce a raw default while keeping the tokens imported elsewhere.

- **Promote the `QRCode.create` `errorCorrectionLevel: "M"` literal into a named constant** (`QR_ERROR_CORRECTION_LEVEL = "M"`) at module scope so a future "Q" or "H" upgrade for high-density QR data is a one-line edit. Today the literal lives inside the `useMemo` callback — fine for one consumer, but the share-sheet QR has data lengths that vary widely (deep-link → ~80 chars vs. UUID payloads → 36 chars), and the right error-correction level depends on the cell density.

- **Add a "QR contrast guard" test** that asserts the default `color` and `background` produce sufficient WCAG contrast (≥ 4.5:1) so a future palette swap that pairs two similar-luminance tokens (e.g. `MUTED_22` on `MUTED_23`) doesn't silently produce an un-scannable QR. A 20-line test using a `relativeLuminance(hex): number` helper + the WCAG ratio formula would close the regression vector for free.

### components/crash-fallback.tsx design-tokens migration (2026-05-25)

- **Consolidate `MUTED_24 = "#8a705a"` with `MUTED_7 = "#856d5a"`** — they're 4 channel bytes apart, visually indistinguishable to most users. The crash-fallback uses MUTED_24 once; if there's no design intent behind the slight difference, merging onto MUTED_7 would shrink the palette. Pair with an HSL-distance audit that flags any two MUTED_* tokens within ΔE < 2.

- **Add a per-file Sentry breadcrumb in `components/crash-fallback.tsx`** — today the fallback renders the error message but doesn't add an "encountered crash UI" breadcrumb. Wiring `addBreadcrumb("CrashFallback rendered", { extra: { detail } })` from `lib/sentry.ts` would let Sentry traces show that the user actually saw the fallback (vs. silently recovered). Cheap one-liner.

- **Type the `t` prop in `CrashFallbackProps`** more strictly using the project's `TranslationKey` union from `lib/i18n-context.tsx` — today `t?: (key: string) => string` accepts any string. Narrowing to `t?: (key: keyof typeof FALLBACK_STRINGS | string) => string` would surface typos in the `pick(t, "crashFallbackXxx")` calls at compile time.

- **Add a structural test asserting `components/crash-fallback.tsx` never imports the full i18n context** — the entire point of the fallback is to render *even when* the provider tree has collapsed. A grep-style guard (`assert.equal(src.match(/useI18n/g)?.length ?? 0, 0)`) closes the regression vector where a future refactor accidentally couples the crash boundary to the broken provider.

- **Promote the `FALLBACK_STRINGS` literal into a `lib/crash-fallback-strings.ts` module** so the same English defaults can be reused by other crash boundaries (e.g. a future `<RootErrorBoundary>`). Today the constants are inlined in the component file — extracting them centralises the canonical English fallback copy and lets a structural test enforce that no other crash UI re-declares the strings.

### components/screen.tsx design-tokens migration (2026-05-25)

- **Promote the 3-stop `<LinearGradient colors={[CARD_BG_7, PAGE_BG_2, PAGE_BG_3]}>` array into a `PAGE_BG_GRADIENT` exported tuple from `lib/design-tokens.ts`** — today the gradient stops are reconstructed inline. A `const PAGE_BG_GRADIENT = [CARD_BG_7, PAGE_BG_2, PAGE_BG_3] as const` export would (a) DRY the array if a second screen ever wants the same page-bg, (b) let a structural test enforce that no other file constructs an ad-hoc gradient with different cream stops, and (c) make the gradient direction/intent visible to a designer reviewing the palette doc.

- **Wire `useResponsive()` into a project-wide `lib/responsive.ts` module** — today the hook lives in `components/screen.tsx` but a header pill, a modal sheet, or a chat composer all want the same `isMobile`/`isTablet`/`isDesktop` breakpoints. Hoisting it would let those consumers import without pulling in the `<Screen>` JSX boilerplate.

- **Add a memoized `WeakMap<...>` cache for `useResponsive()`'s `contentMaxWidth` math** — today every consumer renders the hook fresh and re-derives the value from `width`. A `useMemo` keyed by width would close the gap; the per-pixel allocation cost is invisible but a 60fps scroll on a low-end Android hits the hook 60×/s.

- **Add a structural test asserting the `Screen` component's `staticInnerStyle` always includes `styles.fillContent`** — today the `scroll=false` branch depends on this for nested FlatList virtualization to size its viewport. A regression that drops the `flex: 1` would silently produce zero-height lists on every viewer-FlatList migration so far (VM-D). A 5-line source-scan guard would close the regression vector.

- **Type the `Screen.children` prop more strictly** — today `PropsWithChildren` accepts anything. Narrowing to `ReactNode` and adding a runtime warning when more than one root child is passed without a parent View would surface common Screen-misuse patterns (e.g. accidentally passing 2 sibling FlatLists, which the parent ScrollView can't size correctly).

### components/realtime-status-pill.tsx design-tokens migration (2026-05-25)

- **Consolidate `MUTED_25 = "#7a4d18"` with `MUTED_12 = "#7a4f1a"`** — these are 1-byte siblings (same shade to the human eye). The MUTED_22/_23 1-byte pair was also flagged for consolidation; if there's no design intent behind the small diff, merging onto MUTED_12 would shrink the palette. Pair with an HSL-distance audit that flags any two MUTED_* tokens within ΔE < 2.

- **Add an "online" + "offline" pill variant to `RealtimeStatusPill`** — today the pill only renders for `connecting` (mid-transition). A green "online" variant (using `STATUS_ONLINE`) shown briefly after reconnect would close the loop visually; an explicit "offline" variant (using `STATUS_OFFLINE`) shown after a connection timeout would distinguish "trying" from "gave up". Both fit on the existing pill chrome.

- **Wire the existing `useOptionalRealtimeStatus()` into a structural test that asserts the pill returns `null` outside the provider** — today the early-return is intentional but untested. A short unit test using a render-time check would close the regression vector against a future refactor that switches to `useRealtimeStatus()` (which throws outside the provider, crashing every screen that mounts the pill).

- **Add an `accessibilityRole="alert" + accessibilityLiveRegion="polite"` to the pill** so screen readers announce the connection state change. Today the pill is mute to assistive tech; a single prop change would close the a11y gap with zero design impact.

- **Make the pill text translatable via a `connectingPill` key instead of reusing `chatOfflinePill`** — today the pill reads "Offline · reconnecting" (the chat-specific copy) even when shown on marketplace/profile screens. A neutral "Reconnecting…" string would fit better across contexts; the chat module can keep the old key for its own banner.

### components/skeleton.tsx design-tokens migration (2026-05-25)

- **Promote `BASE_COLOR` and `HIGHLIGHT` into a named `SKELETON_BASE` + `SKELETON_HIGHLIGHT` pair in `lib/design-tokens.ts`** — today `HIGHLIGHT` is a raw `rgba(...)` string and `BASE_COLOR` is just a local alias for `TEXT_ON_DARK_SOFT`. If a future `<Skeleton variant="dark">` is added, both surfaces would benefit from canonical semantic names instead of re-deriving alpha from the cream palette. Pair with the planned `OVERLAY_*` family + `withAlpha()` helper so the `rgba(255, 250, 244, 0.9)` literal becomes `withAlpha(PAGE_BG_2, 0.9)`.

- **Memoize the `Animated.Value(0)` ref + the `loop.start()` effect with a stable dep** — today every Skeleton instance allocates a fresh `Animated.Value` even if it remounts with the same key. A `useRef(() => new Animated.Value(0))` lazy-init + a `useMemo` around the interpolation tuple would skip allocations for the 60-skeleton SkeletonProfileList burst. The win is invisible on desktop but ~2-3ms per remount on a low-end Android.

- **Add a `<SkeletonItemCard>` variant matching the `ItemCard compact` layout** — today the SkeletonCollectionDetail wraps a generic row-skeleton, but the viewer-FlatList branch in `app/collection/[id].tsx` mounts `<ItemCard compact />` cards. A purpose-built skeleton that mirrors the masonry cell shape would prevent a layout shift when the real cards swap in (the row layout currently jumps from ~80px row → ~220px card).

- **Hoist the local `styles.card` block into a shared `lib/skeleton-styles.ts` module** — the same `backgroundColor: PAGE_BG_2 + borderColor: BORDER_2 + radius: 20 + padding: 18` chrome appears in multiple skeleton variants and in real `<ItemCard>` / `<CollectionCard>` files. Centralizing would let a future card-radius/padding tweak propagate without grepping every skeleton variant.

- **Add a `reducedMotion` gate that disables the `Animated.loop`** when `AccessibilityInfo.isReduceMotionEnabled()` is true. Today the shimmer animation runs unconditionally, which is a documented a11y nausea trigger for vestibular-disorder users. A 5-line `useEffect` + state hook would stop the loop when the OS-level reduced-motion flag is on without changing the static placeholder appearance.

### app/collections-feed.tsx design-tokens migration (2026-05-25)

- **Hoist the `hero` chrome (`backgroundColor: HERO_DARK + borderRadius: 32 + padding: 24 + gap: 10`) into a `<HeroCard>` component** — the same 4-property block recurs across `app/index.tsx`, `app/wishlist.tsx`, `app/people.tsx`, and now `app/collections-feed.tsx`. A shared component would also let the eyebrow+title+subtitle chrome migrate together (today every screen rolls its own 3-text-block hero); centralizing would shrink each consumer to a `<HeroCard eyebrow={...} title={...} subtitle={...}>` 3-prop call.

- **Promote the `eyebrow`/`title`/`subtitle` typography triplet into a shared `lib/text-styles.ts` module** — today the same `textTransform: "uppercase" + letterSpacing: 1.2 + fontWeight: "800"` eyebrow shape repeats across 6+ screens, and the title's `fontSize: 28 + fontWeight: "800" + lineHeight: 36` also recurs. A `HERO_EYEBROW` / `HERO_TITLE` / `HERO_SUBTITLE` exported StyleSheet entry would let each screen reuse the typography without re-deriving.

- **Pin a structural test asserting the dark-hero text colors stay on the `TEXT_ON_DARK_*` family** — today nothing prevents a future contributor from picking `TEXT_DARK` (dark brown text on dark brown hero = invisible) for the title/subtitle. A grep-style guard like "if a file declares `backgroundColor: HERO_DARK`, every nested `color:` must come from the `TEXT_ON_DARK*` namespace" would catch the contrast regression at PR time.

- **Add an `accessibilityRole="header"` to the `hero` View** so screen readers announce it as a section heading rather than a generic group. Today the `<Text style={styles.title}>` is rendered without semantic structure; the eyebrow+title pair could also share a `accessibilityLabel` that combines both into one announcement instead of two.

- **Wire `feedCollectionsItemCounts` into a memoized `useItemCountsBatch(collections)` hook** — today the `useEffect` re-fires whenever `friendCollections` or `subscribedCollections` change identity (which happens on every social-context re-render). A hook keyed by stable collection IDs (rather than the full collection objects) would skip the redundant `Promise.all(fetchItemsByCollectionId(...))` round-trip when only the title/visibility of a collection changes — useful for the Supabase free-tier rate limits.

### app/auth/callback.tsx design-tokens migration (2026-05-25)

- **Hoist the `<ActivityIndicator size="large" color={ACCENT_DEEP}>` shape into a `<BrandSpinner>` component** — today the same accent-brown spinner color appears across 6+ screens (callback, marketplace loading, profile fetch, friend pull-to-refresh). A 5-line wrapper would mean a future "loading spinner" rebrand is a one-line edit, and the test surface shrinks from N adoption tests to one.

- **Replace the inline `try/catch` exchange-code/token-hash branch chain with a `lib/auth-callback.ts:resolveAuthFromUrl(url)` pure helper** — today the 50-line useEffect mixes URL parsing, three branch arms, and Supabase calls. Extracting the URL parsing into a `parseCallbackUrl(url): { kind: "tokens" | "code" | "tokenHash" | "error", ... }` discriminated union would let the screen be a thin "show working / show error / replace route" wrapper and let the helper be unit-tested without mocking the Supabase client.

- **Add an analytics event `auth_callback_error` keyed by the discriminated `errorCode` from the URL hash** — today errors are surfaced to the user but not captured. Pairing it with the existing `authClient` event taxonomy would surface "Google OAuth flow gives `access_denied` 30% of the time" patterns that today are invisible because they fail silently outside the Supabase dashboard.

- **Wire the `cancelled` flag through a more idiomatic `AbortController`** — today the `cancelled` boolean is a one-off pattern; the rest of the codebase uses `AbortController`/`signal` for cancellation. Standardising would make the cleanup pattern legible to any reader and skip the (rare) race where `setError` fires after the useEffect returns its cleanup function.

- **Surface the error message in the OS error reporter (Sentry)** — today the user sees the error text but `lib/sentry.ts:captureException` isn't called. A 2-line `captureException(nextError instanceof Error ? nextError : new Error(...), { tags: { surface: "auth_callback" } })` would log every callback failure with full stack to the diagnostics backend, gated by the existing `DiagnosticsProvider` opt-out so it respects user consent.

### components/collection-card.tsx design-tokens migration (2026-05-25)

- **Delete the unused `role` StyleSheet entry** — declared on line 82 but never referenced in JSX (the `<View style={styles.topRow}>` block uses `<VisibilityBadge>` instead, which has its own internal "role"/visibility chrome). Dead style entries add noise and produce false positives in the adoption test's "expected token names" list. A one-line removal would also slim the file by ~7 lines.

- **Audit the 5 new `TEXT_ON_DARK_10..13` + `MUTED_26` tokens for cross-file reuse** — these were added because no existing palette entry came within ΔE<3 of the inline literals, but the same warm-cream shades may appear in `components/item-card.tsx` (still pending migration) or in marketing-style heroes in `app/index.tsx`. A bulk grep-then-consolidate pass after the design-tokens sweep completes would shrink the `TEXT_ON_DARK_*` family from 13 entries back to ~6 by retiring near-duplicates onto a single canonical value.

- **Promote the `rgba(34, 24, 17, 0.36)` overlay into a `HERO_OVERLAY` token + `withAlpha(HERO_DARK, 0.36)` helper** — today this is the second `rgba(...)` literal that the 6-digit regex can't catch, after the rgba in `components/skeleton.tsx`. Centralizing both alpha overlays under the planned `OVERLAY_*` family (with a typed `withAlpha(token: DesignToken, alpha: number)` helper) would close the alpha-shade migration gap that 6+ pending files all carry.

- **Add a structural test asserting collection-card text contrast against the `MUTED_26` fallback bg** — today `MUTED_26 = #c8b8a4` is the warm tan-grey bg shown when there's no cover. The text colors are tuned for the dark-image-with-overlay case; on the bare fallback bg, the white title + cream text may fail WCAG AA contrast. A computed-contrast test or a documented "use shaded variant when no cover" comment would close the a11y regression vector.

- **Wire `<CollectionCard>` through the existing `Skeleton` placeholder** while the cover image is loading — today the `<Image>` mounts directly with no loading state, so a cold network produces a brief flash of `MUTED_26` warm-grey before the photo paints. A 5-line `useState(false) + onLoad → setLoaded(true)` gate + `<Skeleton style={StyleSheet.absoluteFillObject}>` overlay during the loading window would smooth the cold-load UX without changing the warm-state appearance.

### components/selectable-item-row.tsx design-tokens migration (2026-05-25)

- **Hoist the `<Pressable>` + checkbox overlay shape into a generic `<SelectableRow>` component** in `components/selectable-row.tsx` — today selectable-item-row is item-specific (`<ItemCard>` inside) but the same Pressable+checkbox+borderRadius+accent-border pattern is needed for `<SelectableCollectionRow>` and a future `<SelectableContactRow>` (bulk friend-actions). A generic component taking a `children` slot would let three+ call sites share the chrome.

- **Replace the inline `selected ? <Text>✓</Text> : null` with an `Ionicons` checkmark** to match the rest of the app's icon system (the codebase uses `@expo/vector-icons` Ionicons for every other checkmark — see `app/create.tsx`, `app/item/[id].tsx`). A `name="checkmark"` Ionicons would also scale crisp at any pixel density (the current Text-based glyph relies on the system font's "✓" character which varies across iOS/Android/web).

- **Add an `accessibilityRole="checkbox" + accessibilityState={{ checked: selected }}`** to the outer Pressable so screen readers announce "checkbox, checked"/"checkbox, not checked" instead of the generic "button". The checkbox semantic is the correct WAI-ARIA role for a multi-select toggle. Single-prop addition with zero design impact.

- **Tune the React.memo's compare function** with an explicit `(prev, next) => prev.selected === next.selected && prev.item === next.item && prev.onToggle === next.onToggle` instead of the default shallow compare. Today the default works because all 3 props are scalar/reference-identity-stable, but pinning the comparator makes the perf invariant explicit and surfaces any future prop additions that need their own equality strategy.

- **Add a structural test asserting the `SelectableItemRow` Pressable doesn't nest inside another Pressable** — today the row is rendered standalone in selection mode, but a future "tap-to-expand row + select sub-checkbox" layout would create nested Pressables (React Native warns about this). A grep-style guard that the row's outer is always a direct FlatList child would close the regression vector.

### components/reaction-bar.tsx design-tokens migration (2026-05-25)

- **Inline the unused `REACTION_EMOJIS` import** — `components/reaction-bar.tsx` line 4 pulls `REACTION_EMOJIS` from `@/lib/use-reactions` but never references it; only `useReactions` is consumed. A 1-token grep+removal keeps the import surface honest and pairs with the eslint `no-unused-vars` rule already in `eslint.config.js`. The lint guardrail would catch the regression that a future contributor reintroduces a dead import.

- **Promote chipActive's identical bg+border to a single `chipActiveSurface` style entry** — today `chipActive.backgroundColor = HERO_DARK` and `chipActive.borderColor = HERO_DARK` repeat the same token, which means a future "change the active chip accent to AMBER_ACCENT" tweak requires editing two lines instead of one. A `backgroundColor: HERO_DARK, borderColor: HERO_DARK` pattern recurs in 5+ other StyleSheets (`components/selectable-item-row.tsx`, `app/chats.tsx`, etc.); a shared `surface(token)` style helper that returns `{ backgroundColor: token, borderColor: token }` would dedupe the pattern across the codebase.

- **Add an `accessibilityRole="button" + accessibilityState={{ selected: item.mine }}`** to each reaction chip Pressable so screen readers announce "heart reaction, selected"/"thumbs-up reaction, not selected" instead of the generic Pressable announcement. Today the chip just reads as a tap target with no semantic context. Mirrors the same pattern listed for `<SelectableItemRow>` — apply both in one a11y pass.

- **Wire the reaction toggle through an optimistic-update pattern** — today `void toggle(item.key)` fires-and-forgets, and the UI doesn't reflect the new count until `useReactions` re-fetches. A `useOptimistic` (or manual `setLocalCounts`) layer would let the chip count tick up/down instantly on tap, then reconcile when the cloud confirms. The latency hit is invisible on a fast network but stark on slow connections — RX-friendly UX for free.

- **Add a "long-press chip to see reactor names" affordance** — the existing `useReactions` hook already knows which user IDs reacted (it computes `mine` from `userId`), so wiring a long-press handler that opens a `<Sheet>` with the user list would surface "who liked this" without a new endpoint. Mirrors Instagram/Telegram reaction UX, makes the social signal much richer than just the count.

### components/photo-preview.tsx design-tokens migration (2026-05-25)

- **Migrate the three `rgba(38, 27, 20, 0.x)` + `rgba(141, 43, 43, 0.8)` overlay literals into a shared `OVERLAY_*` family** — `photo-preview.tsx` carries 3 distinct alpha overlays (`0.6`, `0.7`, `0.8`) all keyed off either `HERO_DARK = #261b14` (= `rgb(38, 27, 20)`) or `DANGER_DEEP = #8d2b2b` (= `rgb(141, 43, 43)`). Today the 6-digit regex doesn't catch them so the adoption test passes, but a future `withAlpha(HERO_DARK, 0.7)` helper would dedupe these across 6+ files (`photo-preview`, `skeleton`, `collection-card`, ...) and unblock the design-tokens "alpha shade" gap that's been mentioned in 4+ prior suggestions.

- **Replace the inline `<Ionicons name="chevron-back" />` / `<Ionicons name="chevron-forward" />` button content with `<ChevronButton direction="back|forward">`** — today both move buttons hand-roll the same `Pressable` + `Ionicons` + `hitSlop={4}` + `styles.moveButton` chrome, with the only difference being the icon name and the move target. A 20-line `<ChevronButton>` extraction would dedupe the pair and give the codebase a reusable left/right paginator for other grid-reorder UIs (`app/profile/edit-photos.tsx`, the avatar reorder sheet, etc.).

- **Promote `removePhoto`+`movePhoto` into a `usePhotoOrdering(photos, onChange)` hook** — the two handlers are pure transformations over the `photos` array with no React state, but they recreate the array via `filter` / `splice` on every render. A custom hook returning `{ removePhoto, movePhoto }` wrapped in `useCallback` would (a) stop the per-render allocation and (b) let `<PhotoPreview>` consumers (item-detail edit, profile edit, listing create) share the same logic for free.

- **Add a drag-to-reorder gesture wrapper** — today the only reordering affordance is the two chevron buttons, which means moving photo #5 to position #1 requires 4 taps. Wrapping `<View style={styles.item}>` in `<DraggableFlatList>` (already a dependency for `app/collection/[id].tsx` drag mode) would deliver a single-gesture reorder UX without changing the data contract.

- **Render a long-press preview / fullscreen viewer when a thumb is held** — the photo-preview is the only path users have to verify they uploaded the right images, but at 104×104 px a thumbnail is too small to spot a mis-cropped shot. A 30-line `<PhotoViewer>` modal that opens on long-press (mirroring `app/item/[id].tsx`'s lightbox) would let users sanity-check uploads before committing. The data is already in hand — only the modal scaffolding is missing.

### components/visibility-badge.tsx design-tokens migration (2026-05-25)

- **Inline the unused `FONT_BODY` import** — `components/visibility-badge.tsx` line 6 pulls `FONT_BODY` from `@/lib/fonts` but never references it; only `FONT_BODY_BOLD` is consumed across the 4 text variants. A 1-token removal would silence the `unused-vars` warning that the project's eslint config should be flagging.

- **Promote the 7 `rgba(...)` overlay literals into an `OVERLAY_*` family with `withAlpha(token, opacity)` helper** — `visibility-badge.tsx` has the densest concentration of `rgba(...)` literals in the codebase (7 in one file across 3 alpha tiers per variant × hero/card pair), each one tied to a specific named base color (`#4a7c59` viewer green, `#d89c5b` shared amber, `#6b5647` private muted, `#ffffff` hero white). A `withAlpha(SUCCESS_GREEN_2, 0.12)` / `withAlpha(AMBER_ACCENT, 0.15)` helper would dedupe all 7 onto 4 named base tokens × declared alphas, closing the rgba migration gap that's blocked 6+ files from achieving 100% adoption.

- **Extract the `isViewer ? ... : isShared ? ... : ...` 4-way ternary into a `getBadgeColors(state, variant)` helper** — today the same 4-way branch appears twice in the component: once in the `Ionicons color` prop and once spread across 4 StyleSheet entries (`badgeText` / `badgeTextHero` / `badgeTextViewer` / `badgeTextShared`). A single `getBadgeColors({ isViewer, isShared, isHero })` returning `{ icon, text }` would make the state→color mapping a one-line table instead of duplicate ternary chains.

- **Add an `accessibilityRole="text" + accessibilityLabel={`Visibility: ${label}`}`** to the outer `<View>` so screen readers announce the visibility state ("Visibility: private") instead of just the label text. Today the badge is a decorative icon+text pair with no a11y semantics; one prop closes the gap.

- **Wire the badge through `<Pressable onPress={openVisibilitySheet}>`** to make the chip itself a tappable affordance that opens the visibility-edit sheet (today the badge is purely display and the edit path is buried in collection-detail menus). Mirrors how Notion / Linear surface visibility-toggle as a directly-clickable chip — discoverability win for ~10 LOC.

