## One number instead of three, and the two fixtures that noticed (2026-09-19)

<!-- First bullet of "After the runtime-roots constant" (2026-09-19): "Three guards now share a walk and three `SCANNED_FLOORS` notes each explain the same alignment to the reader… The roots are one statement now; the NUMBER is still three." -->

- [DONE: 2026-09-19] [est: 13000] **The roots were consolidated an hour earlier and the number was not** — `check-inline-hex`, `check-reduced-motion` and `check-latest-ref` each carried `minimum: 174`, and each carried a sentence in its `note` explaining to the reader why it matched the other two. Three copies of a number, plus three defences of the copying. `RUNTIME_CODE_WALK_FLOOR` is that argument stated once.

  **What it is NOT, said in the constant's own header.** Not a bound on how much code this tree may hold and not a number anybody should feel pressure from. The property that actually matters — a walk that came back healthy-looking with `app/` missing — is carried by each entry's `roots` and asserted directly, so this number's only remaining job is the walk that came back implausibly small with every root present: a narrowed glob, a broken extension filter, a widened skip list. At 174 against 311 files it is loose, deliberately: tightening it would mean re-taking it every time the tree grows, for a property that does not need a tight number.

  **Two fixtures caught the change, both correctly, and one of them had been caught by this before.** `lint-guard-invalid-floor` and `guard-fixture-refusals` both rewrite `minimum: <number>` to prove a guard refuses an unusable premise — and the right-hand side stopped being a number. The first had already broken once when a floor was re-measured, and had been taught to read the current value out of the table; that is why it broke a second time, because the new spelling is not a value at all. Both match a TOKEN now, which survives either. The failure was loud rather than silent only because one of them asserts that the rewrite changed something: a patch that matches nothing produces a perfectly valid entry and a case that fails claiming the guard would not refuse, which is a puzzling failure rather than a named one.

  **And a suggestion from the same list, checked and dropped.** "`check-console-swap` is `RUNTIME_CODE_DIRS` plus `scripts/` and could say so as a set" cannot be built: `__tests__/helpers/guard-scan-list.ts` parses each guard's `SCANNED_DIRS` as a LITERAL array, deliberately, and its header already records why — a `[...SOURCE_DIRS]` spread would satisfy the one-copy rule while defeating the pin that keeps every guard's scan list readable from its own file. The suggestion was a one-line refactor wearing the clothes of a design this repository has already decided against.

## The third rule to write out the same narrowing (2026-09-19)

<!-- First bullet of "After the latest-ref guard" (2026-09-19): "Two guards arrived today and both narrowed to `app` + `components` + `lib` with the same floor of 174 — `check-inline-hex` makes three… the roots that hold code that runs are a fact about the tree, and three rules have now stated it separately." -->

- [DONE: 2026-09-19] [est: 12000] **Three rules, three copies of one fact** — `check-inline-hex` (a colour literal is a token decision only where something is drawn), `check-reduced-motion` (an animation has to consult the setting only where it can run) and `check-latest-ref` (a per-render ref sync needs a render) all narrow to `app` + `components` + `lib`, and all three wrote the narrowing out. The second and third arrived within hours of each other this morning, which is the only reason it was visible: one copy is a choice, two is a coincidence, three is a fact about the tree that nowhere states.

  **`RUNTIME_CODE_DIRS`, named for what it MEANS.** The same move `MARKUP_DIRS` made one level narrower, and the naming argument is the one that file already records: "the roots the three code rules scan" is a list to re-justify every time one of them changes, and "the roots that hold code that runs" is a fact a rule wanting something else has to argue against.

  **`NON_RUNTIME_REASONS` ships in the same commit, which `MARKUP_DIRS` did not manage.** That constant landed as a narrowing with no statement of what it excluded and needed a follow-up commit to say so; this one is the pair from the start. And `scripts/` is the entry that has to be said carefully — it RUNS, on a developer's machine and in CI, so a rule about "code that runs" catches it on a careless reading. What these three rules are about is code on a USER's device, which is Metro's decision, and Metro never resolves `scripts/`.

  **The guards keep their own literal `SCANNED_DIRS`.** `guard-scan-dirs.test.ts` parses it out of each wrapper precisely so a guard's scope is readable from the guard's own file, and the suite asserts the three lists EQUAL the constant. One statement plus a checked agreement, rather than three lists that happen to match — which is what they were doing until this morning.

  **The claims about the constant live beside the claims about its sibling, not in a second home.** The partition (every source root is runtime or has a sentence), the minimum-length rule on each sentence, the non-empty check on each root, and the nesting — `MARKUP_DIRS` ⊆ `RUNTIME_CODE_DIRS` ⊆ `SOURCE_DIRS` — all went into `source-files-helper.test.ts` next to the markup versions of the same four. The first draft put them in the guard suite, where they would have been a second place to look for one kind of question.

## The rule that was a test case (2026-09-19)

<!-- First bullet of "After the latest-ref consolidation" (2026-09-19): "The no-render-body-assignment rule is a test case and could be a lint guard — it lives in `use-latest-ref.test.ts` and walks `app/` + `components/` + `lib/` exactly as `check-inline-hex` does." -->

- [DONE: 2026-09-19] [est: 14000] **A rule about a shipped-code idiom, enforced only by a suite** — the sweep that found the eleven hand-written ref syncs stayed where it was written, inside the hook's own test file. That is enforcement (it runs in `npm test`, a gate leg) and it is the wrong shape: the twelfth copy would be reported by a suite about `useLatestRef`, under a case name nobody grepping for a lint failure would find, with no annotation on the diff that introduced it. `lint:latest-ref` is the 23rd guard, beside the 22nd, which arrived this morning.

  **The narrowness IS the design, and it took six negative cases to say so.** Ordinary ref usage is almost the same text: `timerRef.current = null` is a reset, `timerRef.current = setTimeout(...)` stores a handle, `mountedRef.current = false` is a lifecycle flag — `app/chat/[id].tsx` alone has six, every one of them correct. So the name must be the SAME identifier either side, with the `Ref` suffix carried by a backreference rather than by convention. And the statement must sit at exactly two spaces: four or more means inside an effect, a handler or a branch, which is where `use-transition-event` writes its own `prevRef` correctly and where a rule that argued would simply be wrong.

  **Indentation is a weak signal and it is the honest one available.** Deciding "is this statement in the component body" properly means parsing the function, and this repository has `check-jsx-walk` precisely because of what it thinks of hand-rolled parsers that are almost right. The two-space rule under-matches — a component nested inside another function is missed — and under-matching is the direction a style rule should fail in.

  **The half it refuses to attempt is the more interesting one.** The other side of `useLatestRef`'s contract is that the ref must not be READ during render, which is a real React violation rather than a preference, and the hook makes that mistake one line shorter to write. Telling a render-body `.current` read from a callback's is a question about scope, not about text, and the shapes that separate them — an arrow in a prop, a function declared and called later, a `useMemo` body — are indistinguishable to a scan. A rule catching nine of ten would be worse than none, because the tenth would read as cleared. A case asserts the module does not grow one quietly.

  **No subject floor, and the absence is reasoned rather than forgotten.** `check-reduced-motion` refuses a run that matched no animation, because its rule is about things that exist. This one's healthy state is zero findings and there is nothing in the tree for it to count, so the equivalent hazard — the pattern silently ceasing to match after a formatter change or a rename — is demonstrated against fixtures in its suite instead. That is the only place a negative rule can show it still recognises its subject, and its floor entry says so.

## Eleven copies of one assignment (2026-09-19)

<!-- "Three refs in one component now exist for one hazard", carried on two suggestion lists: "`activeRef`, `tabsRef`, `onChangeRef`, `translateRef` and `useReducedMotionRef` are all in `components/swipe-tabs.tsx` for the same reason… A `useLatestRef(value)` hook is four lines; the alternative worth pricing first is rebuilding the responder." -->

- [DONE: 2026-09-19] [est: 17000] **`const ref = useRef(x); ref.current = x;` in eight files, for three different reasons** — a `PanResponder` built once inside `useRef(...).current` and never rebuilt (`swipe-tabs`, four of them); a callback whose identity changes every render and must not restart a timer (`toast-host`, `use-transition-event`, `use-visibility-refresh`, `use-entry-currency`, `use-dwell-time`); and render state read by a handler that outlives the render (`app/index.tsx`, `app/collection/[id].tsx`). Three sentences, one mechanism, and each file arguing for it in its own words.

  **The second line is the whole hook, and it is the line that gets forgotten.** Not because it is hard but because it reads as bookkeeping and sits below the declaration it belongs to. The failure when it is missing is the reason this is worth a hook rather than a comment: nothing crashes, the ref simply keeps the FIRST value, and the component is correct until the value changes — which in four of these sites is a language switch, a second toast or a reorder, none of which the first render can see. That is a bug that ships.

  **The one site that was already right is what suggested the other ten.** `useReducedMotionRef` was written two days earlier with the assignment INSIDE it, and its header said why: "rather than by a line each caller has to remember". It is now `useLatestRef(useReducedMotion())` — the signal plus the hook, with nothing of its own left.

  **The sweep found an eleventh site the suggestion's grep had missed.** `lib/use-dwell-time.ts` was below a `head -30`, which is how a hand-run survey misses one. The case that found it stays as the floor: no `xRef.current = x` at the top level of a component body anywhere under `app/`, `components/` or `lib/`, with the name on both sides required to match so an assignment inside an effect or a handler — a different thing — is not caught.

  **Mutable on purpose, which is one deliberate concession.** `use-transition-event` keeps a second ref it writes from inside its own effect; a `readonly` return type here would push that one caller back to a hand-rolled `useRef`, and one holdout is exactly how an idiom stays two idioms.

  **The behaviour of a one-line hook is RUN rather than read.** The load-bearing property is that the ref object is the same one across renders — a hook returning a fresh object every render would satisfy every naive case and none of the actual call sites, because the `PanResponder` holds the object it was handed on render one. So: identity pinned across three renders, a first-render capture asserted to see the third render's value, `undefined` and `null` carried as values rather than treated as absent, and the mutability exercised. Five source-text assertions in other suites moved to the new spelling; one of them had been asserting the assignment line itself, which no longer exists anywhere.

## The native bundles nothing had ever built (2026-09-19)

<!-- "Put a size line on the native bundles", carried on four suggestion lists in a row, most recently: "`metro.config.js` is shared, so iOS and Android got the same 115 KiB back and nothing measured it. The EAS preview build already runs in CI; a line reporting its bundle size would make the native output as visible as the web one." -->

- [DONE: 2026-09-19] [est: 19000] **The job every list pointed at cannot supply the number** — `eas build --platform ios --no-wait` QUEUES a build on Expo's infrastructure and exits, which is what `--no-wait` is for; the workflow then reports success for having queued something. Four lists in a row proposed reading a size off it. So `npm run bundle:native` exports the platform locally and measures what comes out, which is the only way this repository can answer the question at all.

  **And the first run of it failed, for a reason nothing here could have seen.** A SECOND COPY of react-native — 0.86.0 — sits at `node_modules/react-native/node_modules/react-native`, put there by npm to satisfy `@react-native/virtualized-lists`' `react-native: *` peer, which it resolved to the newest published version instead of deduping to the 0.81.5 already at the root. Metro resolves a private component (`VirtualViewExperimentalNativeComponent`) out of the nested copy and the root copy's codegen cannot parse it. Confirmed both directions: move the nested directory aside and the export succeeds at 7643.0 KiB of Hermes bytecode; put it back and it fails again.

  **The web build never resolves that file, which is why every signal in this repository says fine.** `npm run verify` is green, all nine CI legs are green, and the EAS job is green for having queued a remote build whose outcome nothing here reads. Three green lights over a platform that does not build. That is not a gap a guard could have caught, because no check in this tree had ever asked Metro for a native bundle — the failure was not hiding from the checks, it was outside the set of things any of them look at.

  **The diagnosis is the report's real output.** Metro's own message names a component nobody working on this app has heard of and points at a path six directories deep; that is precisely why this went unexamined. `diagnoseNativeExportFailure` requires BOTH halves of the signature — a nested react-native path AND a codegen parse error — and returns nothing for anything else, because a diagnosis offered for every failure is one that will eventually be attached confidently to a failure it is wrong about.

  **A report, not a tenth gate leg, and the distinction is the point.** `npm run verify` chains its nine steps fail-fast, so a leg that cannot pass today would turn every commit red for a dependency-tree problem no commit caused. `bundle:composition` is the standing precedent: gates nothing, run when a question comes up. This one is run when the question is "how big is the native bundle", and today it answers "there isn't one, and here is exactly why".

  **The fix is left to a human on purpose.** `overrides` — both the plain form and the nested `@react-native/virtualized-lists` form — did not dislodge the copy; npm kept the resolution from the existing lock. Forcing it would mean hand-editing `package-lock.json` and shipping a dependency tree whose only real verification is an EAS build this session cannot run. Writing down a reproduction that takes one command is worth more than a lockfile edit nobody can check.

## Four settings nobody had opened, opened (2026-09-19)

<!-- "Audit the rest of Metro's transformer config the same way", carried on the suggestion lists of 2026-09-17 (twice) and 2026-09-19: "`reduce_funcs: false`, `mangle.toplevel: false` and `toplevel: false` are three more that were chosen by somebody else for some other app. Each is worth one build to measure, and the answer may well be that they are all correct." -->

- [DONE: 2026-09-19] [est: 16000] **The answer is that Metro is right about all of them, which is worth knowing once rather than never** — `ascii_only` was one option in a preset with no reader and it was costing 3% of the bundle, which is what made the rest of that preset a fair question. One build each, against a 3,718,641-byte bundle: `compress.reduce_funcs: true` saves 33 bytes, `mangle.toplevel: true` saves nothing, `toplevel: true` saves nothing. Nothing in `metro.config.js` changes.

  **The two zeroes are structural, and that is the whole finding.** Not "nearly nothing" — ZERO, byte for byte across all seven chunks, which is not how a conservative minifier behaves and is not luck. Metro wraps every module in a `__d(function (global, require, …) { … })` factory, so a Metro bundle has no meaningful top level: every name is already function-scoped and already mangled. Whatever those two options are for, this bundle SHAPE cannot reach it, and that is a better answer than a number because it will still be true after the next dependency bump.

  **33 bytes is 0.0009%, and React Native turns that option off on purpose.** `reduce_funcs` inlines single-use functions, which moves work off the wire and onto startup — a cost that lands where nothing in this repository measures. The interesting part is not the risk assessment: it is that the number is small enough that nobody has to do one.

  **A result like this is re-derived every few months unless it is written down.** So it is, twice over: `lib/minifier-audit.ts` holds the table (path, the value Metro sends, the measured delta, why the current value is right) and `metro.config.js` holds the summary, because the next person to ask this question will open the config and not a lib module. The suggestion list asked it three days running, which is what a finding with nowhere to live looks like.

  **And the premise gets a check, which is the part that will still be earning its keep in a year.** A measurement is only as good as the config it was taken against, and a dependency bump can move a default silently: the bundle stays correct, every guard stays green, and those paragraphs become fiction with nothing to say so. That is the same failure mode that put `evaluateBundleCharset` inside `check-bundle-smoke` rather than leaving it as a comment. The suite loads the preset through `createRequire` anchored at `metro.config.js` — the build's resolution, not the suite's — and compares it in BOTH directions: a changed value is named with the date its number is now stale against, and a setting the toolchain ADDS is reported as unpriced. The second direction is the one a table of known options cannot see by itself, and it is exactly how `ascii_only` survived unexamined for as long as it did.

## The tab change nothing said out loud (2026-09-19)

<!-- First bullet of "After the reduce-motion scanner" (2026-09-19), and of the three lists before it: "Say the tab change out loud, now that the pager may not show it" — carried four times, which is itself the finding. -->

- [DONE: 2026-09-19] [est: 18000] **On native there are no tab buttons at all** — `components/swipe-tabs.tsx` renders a `<Text>` and a row of dots for its header, and the swipe is the only way to change tab. So a commit produced no spoken feedback of any kind: no role, no selected state, no sentence. On WEB the same component renders `<Pressable>`s carrying `accessibilityState={{ selected }}`, which the platform announces on press — which is why the gap was invisible to anyone testing in a browser, and why the fix could not go where it first looked like it belonged.

  **The sentence belongs to the GESTURE, not to `commitTo`.** Both routes into a tab change pass through that function, and a version wired into it would say the same thing twice on web and nothing on the native route that has no buttons. `spoken` defaults to FALSE and only the two pan-release call sites opt in, rather than the other way round: a third caller added later is quiet until somebody decides it should not be. And it fires AFTER `onChangeRef.current(targetKey)` inside the completion callback, because the `!finished` branch immediately above returns without changing anything — a sentence spoken for a gesture the pager then abandoned is a lie the listener has no way to check.

  **Not conditional on reduced motion, though that is what raised it.** The suggestion framed this as a reduced-motion gap: under the setting the commit is instantaneous, so the only sign a swipe did anything is the content changing. True, and the narrower half. A screen-reader user perceives the 220ms slide exactly as well as they perceive its absence, which is not at all, so the sentence is owed on every swipe commit — a version firing only under the setting would be a feature for the smaller group.

  **`announcedPosition` gets a second caller rather than a second derivation.** "Position N of M, or nothing" is one rule, already written for the reorder announcements with six cases behind it, and re-deriving it here would have been a second set of edge cases to get right. Both halves of the sentence are required: a blank label says ", tab 2 of 3" — a position attached to no subject — and a position out of range says a number that is simply wrong. A `findIndex` miss resolves to `-1`, which is exactly that case, and it is refused rather than read aloud.

  **The translator is read through a ref, like everything else the PanResponder touches.** The responder is built inside `useRef(PanResponder.create(...)).current`, which runs once, so a `translate` captured in that closure is the one from the first render for the life of the component — the announcement would keep speaking the language the app was STARTED in after the user changed it. That is the same hazard `useReducedMotionRef` was written for two days earlier, in the same file, and the third ref in a row this component has needed for it.

  **Two modules, because `@/lib/announce` is a platform pair.** It pulls react-native (the announcement API is an empty function on web, so the web half builds an ARIA live region), and anything importing it is unreachable from a node suite. So `lib/tab-sentence.ts` holds the DECISION and `lib/tab-announcement.ts` holds the saying of it — the same split `lib/drag-reorder.ts` and `lib/reorder-announcement.ts` already have, for the same reason: the interesting cases here are the ones that produce nothing, and a function whose only effect is a side effect can be tested only by watching for the absence of one. The case that pins the split asserts on the IMPORTS rather than the text, because the module's own header explains the split by naming the module it must not pull.

## The four filenames that were holding the line (2026-09-19)

<!-- Last bullet of "After banking the saving" (2026-09-17): "Promote the animated-surface list into a real scanner" — the reduce-motion sweep's last case names four files by hand, so a fifth animated surface joins unchecked. -->

- [DONE: 2026-09-19] [est: 20000] **A sweep's floor was a list of four filenames somebody typed** — the toast, the skeleton, the pager and the wishlist sheet were taught to consult `AccessibilityInfo.isReduceMotionEnabled` two days earlier, and what stopped a fifth surface from joining without asking was an array in a test: green on the day it was written and silent about every file added after it. The setting is not a preference about taste — vestibular disorders are why it exists — so "nobody remembered" is the failure mode worth spending the 22nd guard on. `check-reduced-motion` walks `app/` + `components/` + `lib/` and refuses any `Animated.timing/spring/decay/loop` without `@/lib/reduced-motion` in scope.

  **The rule is the IMPORT, and that is a decision rather than a shortcut.** There are three right answers and the module's header is about which is which: a `motionDuration` of zero for a transition whose completion callback commits state, a `setValue` for a spring with nothing waiting on it, an early return for a loop. A text scan cannot tell which one a surface needed, and a guard that guessed would be wrong on two thirds of this tree. What it CAN say is that the author was handed the question — which is the claim that catches the surface written by somebody who has never opened the header.

  **Three narrowings, each with a case behind it.** Comments are blanked first, and this rule is the reason that matters: its own header names three of the four drivers, `lib/reduced-motion.ts` names two, and `components/skeleton.tsx` quotes `Animated.loop` to explain its early return — a rule that read prose would report every file that documents itself. The combinators are OUT (`sequence`, `parallel`, `stagger` only arrange drivers, so a file holding one holds a driver too) and `loop` is IN despite being one, because it has its own answer. And `lib/` is in the walk rather than `MARKUP_DIRS`, because twelve context providers live there and render JSX — "animation lives where the markup does" is a sentence this tree has already falsified once.

  **A second premise, beside the count floor.** `assertScannedWalk` proves files were read; nothing proved the rule still RECOGNISES an animation. Every pattern here is a spelling of one library's API, and the day this app moves to Reanimated's `withTiming` all of them stop matching at once: a clean report over a tree full of unguarded motion, which is exactly the failure every floor in this repository exists to catch. One animated file is the floor — the number of animated surfaces is a product decision and the guard is not the file that should have an opinion about it.

  **The repository's own meta-guards named the four registries the guard had to join, in one run.** `guard-registration.test.ts` reported all three gaps at once — a partial-root fixture, a scan-dirs exclusion table, a planted offender — rather than one per run, which is the shape it was built for and the first time it has been exercised by a guard arriving after it. The planted offender is the one entry in that table written LITERALLY rather than assembled: `__tests__/` is the source root this rule deliberately does not walk, and a fixture has to be able to spell a driver out.

  **And the bug that fell out of deriving the list.** `components/skeleton.tsx` returns early under the setting and its cleanup is `loop.stop()`, which freezes the animated value wherever it had got to. Turning reduce-motion on MID-SWEEP is not the exotic case — it is the ordinary one, since it is turned on BECAUSE something animated — and it parked the highlight band across the middle of the box, permanently, on the surface that was supposed to go quiet. The comment above it promised "the box keeps its base colour and sits still"; `anim.setValue(0)` is what makes that sentence true, and it is the `setValue` shape rather than a timing back to zero because nothing is waiting on it.

## The saving, banked (2026-09-17)

<!-- First two bullets of "After the UTF-8 bundle" (2026-09-17): "Re-take the composition baseline" and "Spend some of the 127.9 KiB deliberately, or lower the budget to keep the pressure". -->

- [DONE: 2026-09-17] [est: 14000] **The baseline had written the diagnosis down and left it as a fact about the world** — "`lib/i18n/ru.ts` is the heaviest module a page load still fetches, at 92 KiB, because Metro escapes non-ASCII as `\uXXXX` and Cyrillic costs six bytes a letter" was in `lib/composition-snapshot.ts` since 2026-09-13, as a description of the bundle rather than of a setting somebody had chosen. Re-taking it after the fix: `lib/` 554,338 → 442,000 bytes and `data/` 8,725 → 5,603, the two buckets that hold Cyrillic, with no code removed.

  **The mirror image of the round before it.** That one changed which CHUNK every Sentry bucket was in and moved the totals by 1.1 KiB; this one changes no bucket's membership at all and takes 113 KiB out of two rows. A report watching only totals would have called the first a no-op; one watching only rows would have nothing to say about the second beyond "the copy got smaller". That is why it prints both, and why `(lazy)` is a marker rather than a bucket.

  **The budget comes down in the same commit, because the repository's own rule says so.** 3.67 → 3.57 MiB, 25.5 KiB of headroom — still under `SMALLEST_GUARDED_SDK_BYTES`, so a statically-imported SDK trips on its own commit. Left at 3.67 the gate would have had 128 KiB of slack, which is not a loose guard but no guard at all: four rounds of ordinary work would have spent it without anybody deciding anything. `BUDGET_HISTORY` gains the second row that goes DOWN, and both were found by the composition report — which is what the count now carries: the first was not a fluke, it is what that report is FOR.

  **The measurement records that had to move with it, and the one case that made me find them.** `LAST_MEASURED_LAZY_BYTES`, `SMALLEST_MEASURED_LAZY_CHUNK_BYTES`, the `TODAY` fixture and three report strings were all taken from the previous export. The pairing case in `bundle-composition.test.ts` — "is measured against the same bundle the budget is", within 20 KiB — is what refused the re-taken baseline until the budget snapshot followed it. A baseline that could be updated alone is a baseline that drifts from the number it is supposed to be comparable with.

## 115 KiB of backslash-u (2026-09-17)

<!-- "Watch the bundle budget", said on four suggestion lists in a row, most recently: "This is the fourth list to say so, which is itself the finding: the next feature of any size crosses it. Either move the budget with a sentence saying what grew, or take out the largest avoidable weight." -->

- [DONE: 2026-09-17] [est: 22000] **The headroom was 12.5 KiB and the answer was not in any feature** — the budget had been flagged four days running and the two honest options were always "raise it with a sentence" or "spend a diff getting under it". The composition report said where to look: `lib/i18n/be.ts` shipped as 94 KiB and `ru.ts` as 97, against 28–33 KiB for English, Polish, German and Spanish — the same 554 keys, three times the bytes. That is not a translation being longer. It is `output.ascii_only: true` in Metro's terser preset, which rewrites every character above U+007F as a six-byte escape: a Cyrillic letter costs two bytes in UTF-8 and six as an escape, and there were 13,386 of them in the Belarusian chunk alone.

  **One line of `metro.config.js`, and 115 KiB.** Headroom 12.5 KiB → 127.9 KiB; the EAGER entry chunk alone dropped 56 KiB, because the default Russian locale lives in it. Safe because every consumer declares UTF-8 — `dist/index.html` carries `<meta charset="utf-8">` and GitHub Pages serves `.js` with `charset=utf-8` — and a script tag with no charset on the response inherits the document's anyway. Metro's other three output options are SPREAD rather than replaced: `quote_style` and `wrap_iife` are its own and the rest of the toolchain expects them, and assigning a fresh `output` object is exactly how this edit goes wrong.

  **A setting nothing reads is a setting that comes back.** A dependency bump or a config rewrite restores it silently: the bundle would still be correct, still pass every other check, and be 115 KiB heavier — the budget would absorb it and the headroom would be spent without anybody deciding to spend it. So `evaluateBundleCharset` joins the token sweep and the privacy pages inside `check-bundle-smoke`, over chunks it has already read. No tenth gate step, which the gate's own suite would have had something to say about.

  **The rule is a comparison, not a count, and both halves are load-bearing.** "No escapes at all" is wrong — a source file may legitimately escape a zero-width or control character, and the bundle carries 50 such today. "Some literals" alone is too weak: a stray `©` would satisfy it in a bundle escaping every translation. So it is a floor AND `literals > escapes`, and the second is what carries the meaning: with `ascii_only` on, literals go to roughly zero while escapes go to tens of thousands, which is the reverse of what this asserts and cannot be reached by accident. The floor is argued rather than measured — 1000 against ~30,000 today — so deleting a language does not fail it.

## Four animated surfaces, none of which had ever asked (2026-09-17)

<!-- "Give the toast stack a reduced-motion path", carried on the suggestions list since 2026-09-10 and restated three times since: "Every other animated surface in the app has the same gap, which makes it a sweep rather than a toast fix." -->

- [DONE: 2026-09-17] [est: 20000] **Nothing in this tree had ever read `AccessibilityInfo.isReduceMotionEnabled`** — the toast entrance slides 24px over 220ms, the skeleton shimmer loops forever, the swipe pager translates a full screen width and the wishlist sheet springs. All four ran unconditionally, on every platform, for every user. The setting is not a preference about taste: vestibular disorders are why it exists, and for some people a sliding panel or a looping shimmer means nausea, dizziness or a migraine. iOS, Android and every desktop browser expose the answer and React Native forwards it.

  **`duration: reduced ? 0 : ms` is the naive fix, and it is wrong in two of this app's three shapes.** A transition whose completion callback commits a state change — the pager's tab change, the sheet's dismissal — must still RUN, so zero is right there and skipping it would strand the user mid-gesture. A spring back to rest has nothing waiting on it, so the instant equivalent is `value.setValue(rest)`: a zero-duration spring is still a spring. And a LOOP must not start at all, because `Animated.loop` of a 0ms timing is not a still image, it is a frame callback that never stops — a battery drain handed to the person who asked for less. So `lib/reduced-motion.ts` exports the SIGNAL and the arithmetic, and each surface picks; one "make it instant" helper would have had to know which of the three it was called on.

  **The subscription is the half that matters most.** A value read once at mount would leave every already-mounted surface animating until the screen was navigated away from and back — and the user turning this on mid-session is turning it on BECAUSE something in this app made them ill, which is the moment it most needs to be heard. It defaults to ALLOWING motion when the platform cannot answer: an app that read a missing API as "no motion" would silently drop every animation on a platform whose only fault is not implementing the query.

  **`useReducedMotionRef` exists because two of the four cannot read state.** Both pan responders are built inside `useRef(PanResponder.create(...)).current`, which runs once: a `reduced` captured in that closure is the first render's value for the life of the component. The ref is updated by the hook rather than by a line each caller has to remember.

  **Fifteen cases, and one of them is the floor under the other five.** The arithmetic is called; the hook is mounted and driven through a mocked `AccessibilityInfo` — first answer, a change in each direction, an unmount that must remove its listener, and a platform whose promise REJECTS, which would otherwise be a crash on mount of every animated surface. The last case names all four files and fails if any of them animates without importing the module, so a fifth surface cannot join quietly.

## The tag list nobody had ever checked (2026-09-17)

<!-- First bullet of "After the tag-colour rebalance" (2026-09-17): "Rebalance the tags arriving from storage and from a peer, not only the ones deleted here." -->

- [DONE: 2026-09-17] [est: 18000] **Two functions enforce the tag rules and neither is on the way IN** — `addTagToList` refuses an empty label and a case-folded duplicate, and `removeTagAt` rebalances the hues. Both sit on the path through the forms, which is not how most tag lists in this app arrive. A row pulled from Supabase was written by another device, possibly a build that predates the label hash entirely; a blob read back from AsyncStorage was written by whatever this app was months ago. Two "sealed" tags in different casings, a label that is one space, a set of hues derived by the old rotation and meaningless on every other item: all of it reached the screen unchecked.

  **`normalizeTagList` is the form's rules, not new ones.** No empty label; the FIRST of a case-folded duplicate wins, matching the add rule because the casing that arrived first is the one the user typed; colours re-derived so a label means the same hue here as on an item created today. It is idempotent and identity-preserving, which is the property that lets a product rule sit on a read path at all.

  **Where it runs, and where it deliberately does not.** `mergeItemsFromCloud` — the seam a peer's edit crosses — and `normalizeItemTags` over the hydrated list, beside the three passes already there (`normalizeOwnItemIds`, `dedupeItems`, `applyTombstones`), written the same way for the same reason. NOT in `coerceItemRow`: that is a defensive validator, it says only that the shape is a `{label, color}[]` — which is true of every list this rejects — and a product rule inside it would rewrite hues on a path with no comparison to decide whether anything actually changed. The first attempt put it there and the validator's own suite said so.

  **A colour is a function of the label now, so it stopped being data.** A delta row that differs only in a derived hue says nothing the local row did not, and the merge's no-op contract — return `local` by reference, or pay a re-render and two storage writes on every pull — now holds for it. One case says that directly; two existing fixtures carried a hue this app no longer writes and were asserting the contract against a row that legitimately changed.

  **A suggestion that turned out to rest on a false premise, recorded rather than built.** "Reuse the label hash for the tag FILTER chips" has been carried since 2026-09-10; there is no tag filter. `lib/item-filters.ts` has query, quick chips and sort, and no mention of tags. The chips it describes do not exist, so the suggestion is a feature request for a tag filter wearing a one-line refactor's clothes.

## The freed hue that stayed free (2026-09-17)

<!-- First bullet of "After the label-derived tag colour" (2026-09-10): "Rebalance an item's hues when a tag is deleted, not only when one is added." -->

- [DONE: 2026-09-17] [est: 15000] **A borrowed tag colour was borrowed for good** — `tagColorForLabel` gives a label its own hue unless that hue is already on the item, in which case the rotation picks a free slot. It reads the visible set at ADD time and never looks at it again, so the set decays the moment anything is removed. "sealed" and "holo" hash to the same slot: add both and "holo" takes a rotation colour, delete "sealed" and the slot is free while "holo" keeps the borrowed one — for good, on every card that tag appears on. That is precisely the label-consistency the hash was introduced to provide, lost silently on a screen where nothing looks wrong.

  **`rebalanceTagColors` re-applies the rule to the whole list, left to right.** The direction is the decision: the order is the one the user sees, and an earlier tag changing colour because a later one was deleted is the more surprising of the two — the tag they did not touch is the tag that should not move. The list comes back BY IDENTITY when no colour changed and each unchanged tag keeps its own object, because the callers write it straight into state and a fresh array on every delete would re-render the row for nothing.

  **`removeTagAt` carries it, so no screen has to remember.** Both forms wrote `tags.filter((_, j) => j !== i)` inline — the same duplication `addTagToList` was extracted to end, one operation down. Removal is exactly where the colours go stale, so the rebalance rides in the remove rather than being a second call; a delete that left the hues alone is the bug above, and it would be invisible on the screen that did it. By index and not by label, so a list carrying two casings from storage or a peer's edit loses one row rather than two.

  **The colliding pair in the suite was found by running the hash, and a case fails first if they ever stop colliding.** On labels that never collide, a rebalance and a plain filter produce identical output — every case about a freed hue would pass on the bug, vacuously. Fourteen cases; 9400 in the tree, all green.

## The departure the browser does send (2026-09-17)

<!-- First bullet of "After the bounded toast" (2026-09-17): "Release the hold when the app loses focus, not only when the pointer leaves." -->

- [DONE: 2026-09-17] [est: 15000] **A ceiling is a backstop, and this one was about to become the ordinary path** — the toast holds its dismissal timer on `onHoverIn`, and a browser does not promise the `onHoverOut` that releases it. Alt-tab away mid-hover, switch tabs, drop a drag over the overlay: no pointer event is ever sent to that element again. `toastHoldCeilingMs` bounds the consequence at four windows, which is the difference between a nuisance and a permanent banner — but a tab switched away from mid-hover is ordinary, and every one of them would have spent the full ceiling.

  **`useAppAway` is the signal the browser DOES send in that case.** `lib/use-app-away.ts` listens for two web events, because they are two different absences: `visibilitychange` for a hidden tab, and `blur` for a window that lost focus while its tab stayed visible — the alt-tab-to-another-app case, which `document.hidden` reports as `false` and which a hook listening only for the first would miss entirely. On native it is `AppState`, and every state that is not `"active"` counts: iOS's `"inactive"` is a notification shade or an incoming call, which is as much "the user is not looking" as a full background.

  **Away only, deliberately.** There is no "back" callback. Every caller wants to CANCEL something a departure invalidates, and a return that re-armed it would be guessing that the user is once again doing whatever they were doing before — which, for a hover, they are not. `visibilitychange` fires both ways and carries no direction, so the handler reads the flag; a case asserts the return trip reports nothing.

  **`fake-dom.ts` grew a second registry, and that is the part that could have been faked wrong.** `window` now has its own `addEventListener`/`removeEventListener` and a `fireWindow`, rather than aliasing the document's. A merged registry would let a hook listening on the wrong object pass every case — including the leak case, which is the one no assertion about callbacks can see: ten cases run, and two of them say only that neither listener is still registered after the tree is down.

## A toast that nothing can dismiss, bounded at both ends (2026-09-17)

<!-- Two suggestions carried since 2026-09-10, both listed twice: "Cap how long a held toast can live" and "Extract the hold decision so it can be tested by being called". -->

- [DONE: 2026-09-17] [est: 16000] **The hold had no ceiling and the queue had no depth** — `<ToastView>`'s effect opened `if (held) return;`, so a held toast scheduled NO timer at all. That is correct while a pointer really is resting on the toast and wrong the moment the `onHoverOut` never arrives: a drag that ends over the overlay, a window that loses focus mid-hover, a phone reporting a stuck hover. `held` stays true with nobody there and a `zIndex: 9999` banner sits over the top of every screen for the rest of the session. The queue had the matching hole — `show()` appended, and the only thing that ever removed an entry was that entry's own dismissal timer, which a held toast does not have.

  **The fix is two pure functions, which is the other half of the task.** `nextToastDeadline({ held, hasAction, elapsedMs })` in `lib/toast-timing.ts` and `capToastStack` in `lib/toast-stack.ts`. Both rules used to be lines inside a React module that `tsx --test` cannot import, so the four cases covering them matched the effect's SOURCE — a `return`, a dep array, the name of a ref — which is green on a cleanup that never runs and on a ceiling off by a factor of a thousand. Fourteen cases call them now, and the two render cases that asserted `timers.live()` was EMPTY on hover assert the ceiling instead, which is the behaviour that changed.

  **Held life is capped at four windows from when the toast appeared; a release still restarts the full window.** So the worst case is ceiling + window rather than the ceiling exactly, and that is the trade on purpose: the failure being fixed is UNBOUNDED life, and a release is a real signal from a real pointer — the one thing a stuck hover never sends. `elapsedMs` comes from a `shownAt` ref captured once, not recomputed per re-run, or every hover would reset the ceiling and the bug would survive its own fix.

  **The stack keeps three and drops the OLDEST.** The direction is the whole decision: the newest toast describes what just happened and is the only one whose action the user is still reaching for. A list that already fits is returned by identity rather than copied — a fresh array on every `show()` would re-render the stack, restart the entrance animations, and reset the very timers the cap exists to bound.

## captureTimers moves to helpers/, and the debounce gets run (2026-09-14)

<!-- First bullet of "Fired-handler follow-ups" (2026-09-14): "`captureTimers` is in one suite and every provider suite wants it — all of them either await real time or assert on source. It is twenty lines beside `helpers/render.ts`, and the `finally`-restore discipline is the part a copy would leave out." -->

- [DONE: 2026-09-14] [est: 14000] **The twenty lines that answer "what did this schedule?" were private to the suite that happened to want them first** — `captureTimers` was written inside `toast-host-render.test.ts` yesterday, and everything else in this tree that reasons about a timer does one of the two worse things: awaits real time (a margin that passes on a fast machine and flakes on a loaded one) or matches `setTimeout` in the source (an assertion about spelling, green on a callback that is wrong and on a cleanup that never runs).

  **`withCapturedTimers` is the form that ships, not `captureTimers`.** The stubs go on `globalThis`, so a case that threw halfway and left them installed would break whatever file `tsx --test` ran NEXT — with a message about that file's timers, not about this one. The `finally` is inside the helper now instead of in each of the six bodies that remembered to write one; `captureTimers` stays exported for a capture that has to outlive one body, and says so.

  **A fired timer stops being live.** The original kept a `cleared` flag that only `clearTimeout` set, so an entry that had already run still reported itself as armed — which makes "and nothing is left over" pass on a component that re-armed and fail on one that did not. `run()` retires its own entry; `scheduled` keeps everything, because "one timer per keystroke was armed" and "one is still standing" are different questions and the burst case asks both.

  **The second caller is the debounce, whose suite said the harness did not exist.** `use-debounced-value.test.ts` opens by explaining that the hook can only be pinned structurally because "the repo has no React mounting harness" — true when written, false since `helpers/render.ts` landed, and the half it was standing in for is the entire point of a debounce. Eight cases mount it: a burst of five keystrokes leaves ONE timer standing and not five, the previous one is CLEARED rather than superseded, unmount clears the pending settle (a claim the hook's own doc comment has made since it was written and nothing had ever checked), a zero delay schedules nothing at all rather than a `setTimeout(fn, 0)` that settles a tick late, a NaN or a negative reaches `setTimeout` already folded to the default, and a changed delay re-arms at the new one.

  **Proved by mutation.** Deleting `return () => clearTimeout(timer)` from the hook turns four of the eight red. The helper's own suite asserts the part no caller can: that the globals come back when the body returns, when it throws, and when an await inside it rejects.

  18 new cases (9360 in the suite). 0 new keys, 0 bundle cost — nothing here ships. `npm run verify` green: typecheck, 21/21 guards, 9360 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The hold window, run rather than read (2026-09-14)

<!-- First bullet of "Rendered-toast follow-ups" (2026-09-14): "The hold/release window is the toast's most interesting behaviour and it is still only read, not run — the harness fires `onPress`; nothing fires the other four handlers." -->

- [DONE: 2026-09-14] [est: 12000] **`onHoverIn`, `onHoverOut`, `onFocus` and `onBlur` hold a toast's dismissal open, and three source-text matches were standing in for all four** — the feature stops an undo expiring under the cursor reaching for it, which is the whole reason the timer lives in the view rather than in the queue. The harness could fire `onPress` and nothing else.

  **`fire(node, handler, ...args)` is the generic form**, added because the first component moved into this harness on purpose turned out to hang its most interesting behaviour on events `press` cannot reach.

  **A captured `setTimeout`, not a fake clock.** Nothing here needs time to pass — only to see WHAT was scheduled and whether it was cleared — so the cases read the delay and the cleared flag: the plain window for a reporting toast, the longer one for a toast with an action, none standing after a hover, and the FULL window again on leave rather than the remainder. That last one is the deliberate choice the component's comment argues for and nothing had ever checked.

  **Focus gets the same window as hover**, asserted on the action button, which is `focusable` and a tab stop on the web — a toast that expired while its button was focused is the same bug with a keyboard.

  **And the other end of the timer**: running the callback dismisses the right toast by id, which no source match can say.

  **Proved by mutation.** Deleting `if (held) return;` from the component turns exactly two of these red; restored, all seventeen pass. Every case restores the real globals in a `finally`, because a suite that left a stubbed timer installed would break every later file in the same process.

  6 new cases and a harness method (9342 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9342 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The toast overlay, rendered (2026-09-14)

<!-- First bullet of "Toast-move follow-ups" (2026-09-14): "The toast host is scanned by four rules and mounted by no test — every case about it reads its SOURCE, because `lib/toast-context.tsx` pulled react-native and could not be transformed under `tsx --test`." -->

- [DONE: 2026-09-14] [est: 13000] **The reason every case about this markup was a text scan stopped being true when the markup moved** — `lib/toast-context.tsx` pulled the provider, the queue and the announcement seam, so nothing could import it under `tsx --test`. `components/toast-host.tsx` is presentational and takes its list as a prop, which is exactly the shape `helpers/render.ts` mounts.

  **An empty queue draws NOTHING, which a source scan cannot say.** The host is `position: absolute` with a `zIndex` of 9999, so a stray wrapper over an empty list would sit on top of every screen swallowing presses at the top of it. `root.children` is empty; a regex looking for `if (toasts.length === 0) return null` would pass on a component that returned an empty `<View>` from somewhere else.

  **Dismissal is by ID, and a case proves it with two toasts.** A host that closed "the first" would close the wrong one every time the second was tapped, and every source scan in this repository would have been happy with it.

  **The action runs BEFORE the dismissal**, asserted by the ORDER two handlers pushed into a list rather than by the order two statements appear in the file — which is the difference between reading the code and running it.

  **The palettes are read off the rendered nodes**, with a floor under them: the success and error tokens have to be different values, because a lookup that had collapsed to one entry satisfies either colour case written on its own.

  **The timing cases stay where they are.** `toastDisplayMs`, the hold/release pair and the ref the timeout reads are about how the component is WRITTEN, and a harness with no real timers has nothing to say about them.

  11 new cases (9336 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9336 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The toast overlay moves under the rules (2026-09-14)

<!-- First bullet of "Toast-gap follow-ups" (2026-09-14): "The toast host is unscanned by four rules and it is the surface every error in this app lands on — moving it to `components/toast-host.tsx` would put it inside all four and is a one-file move with one import to follow." -->

- [DONE: 2026-09-14] [est: 18000] **`components/toast-host.tsx` — the markup every error in this app lands on, finally inside the rules that have opinions about it** — two `<View>`s, two `<Pressable>`s and three `<Text>`s lived in `lib/`, where the a11y, clarity, empty-state and heading sweeps do not look, because `lib/` is where modules return values. That was true of twelve context providers and false of this one. `check-a11y-jsx` scans 72 screen files now instead of 71, and reports the host clean — which is a fact rather than the hope it was yesterday.

  **The split is queue-here, drawing-there.** `ToastProvider`, the api and the announcement stayed: a toast is announced when it is QUEUED, whether or not the host ever draws it. The dismissal window went with the view, because a toast the user is reading has to hold its own timer and a countdown owned by the queue cannot be paused by the toast it is counting down.

  **`ToastItem` is a type-only export**, so the two files do not form a runtime cycle — the host imports the shape, the context imports the component.

  **The rule that found this got its subject right on the second try.** "Any capitalised element outside the markup roots" flags a file for rendering `<ToastHost />`, a component whose own file is scanned and which no rule has anything to say about as a tag. What the five rules are about is PRIMITIVES, so an element now counts only if the file imported it from a library — derived from the imports rather than from a list of names, so a library this repo has not used yet is covered the day it arrives.

  **`MARKUP_OUTSIDE_MARKUP_DIRS` is empty**, and the list is kept rather than deleted: "the list is empty" is a stronger statement than "there is no list", and the next such file will want somewhere to be named.

  8 cases move with the markup, 2 new (9325 in the suite). 0 new keys, +0.2 KiB for the module boundary — 15.4 KiB of headroom left. `npm run verify` green: typecheck, 21/21 guards, 9325 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The lib/ sentence was false, and one file proves it (2026-09-14)

<!-- First bullet of "Single-mechanism follow-ups" (2026-09-14): "`NON_MARKUP_REASONS` claims `lib/` renders nothing and names two exceptions inside its own sentence — a rule that no `lib/` module declares an element would make the sentence a fact; nothing does." -->

- [DONE: 2026-09-14] [est: 11000] **"Modules that return values" was written yesterday about a directory where TWELVE files render JSX** — the suggestion asked for the claim to be checked and the check failed it on the first run. Eleven are context providers whose whole markup is `<Ctx.Provider>{children}</Ctx.Provider>`, which is why nobody had noticed and which no accessibility or design rule has an opinion about. The twelfth is a real gap.

  **`lib/toast-context.tsx` draws the toast host** — two `<View>`s, two `<Pressable>`s and three `<Text>`s that a screen reader meets on every error this app reports — and the a11y, clarity, empty-state and heading rules all stop at `app/` and `components/`. It is a NAMED entry in `MARKUP_OUTSIDE_MARKUP_DIRS` now rather than a clause inside a sentence, so a twelfth file that renders a `<View>` joins the list with a reason or moves into `components/`.

  **The exception list is held honest in both directions.** An entry that has stopped rendering anything is dropped, because an exemption nobody needs is one that will excuse the next thing to land in it.

  **"Eleven providers" is a number, not a memory** — a case counts them, so the sentence has to be updated rather than quietly drifting. And a case pins that a tag in a string or a doc comment does not count, which is what every lint module in `lib/` is full of.

  **Deliberately a text rule rather than `walkJsx`.** The question is whether a file declares any element at all, and a walk that gave up on the first unreadable tag would answer "no" for the wrong reason — the exact failure mode this week's other rounds were about.

  5 new cases (9323 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9323 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## Two mechanisms for one fact, down to one (2026-09-14)

<!-- First bullet of "Partition follow-ups" (2026-09-14): "`unionOfScanDirs` reads two guards' sources to derive an answer a case now pins to a constant — two mechanisms for one fact, and the parse is the one that can fail for a formatting reason." -->

- [DONE: 2026-09-14] [est: 6000] **The reach floor derived its scope by parsing two guards' `SCANNED_DIRS`, for a fact `guard-scan-dirs` already pins** — that suite asserts both markup guards EQUAL `MARKUP_DIRS`, so the parse was a second route to the same answer and the only one of the two that can go red because somebody reformatted an array. It takes the constant.

  **Three spellings of one scope in three commits**, which is the shape worth recording: hand-typed, then derived from the guards, then the constant the guards are themselves held to. Each step was an improvement on the last and only the third has one statement.

  **`unionOfScanDirs` is deleted**, having had exactly one caller for one commit. The `declaredScanDirs` parse stays in the helper — `guard-scan-dirs.test.ts` is its only reader again, but the file's other export has readers on both sides of the split, so moving it back would just trade one import for another.

  0 new cases (9318 in the suite, unchanged — this removes a path to an answer, not an answer). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9318 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The narrowing says what it leaves out (2026-09-14)

<!-- First bullet of "Markup-dirs follow-ups" (2026-09-14): "`MARKUP_DIRS` is a SUBSET of `SOURCE_DIRS` and nothing says so — it slipped in beside a constant that has a partition check, with neither." -->

- [DONE: 2026-09-14] [est: 7000] **`MARKUP_DIRS` landed as "app and components" with no statement of what it excludes** — which is exactly the shape `NON_APP_TS_DIRS` exists to prevent one level up, written by the round that had just finished praising that shape. `NON_MARKUP_REASONS` is the other half: `data/` holds seed literals with no elements declared in them, `lib/` returns values (and the two modules that touch JSX read it as TEXT), `scripts/` never reaches Metro.

  **A sixth source root can no longer join the tree and be silently outside every JSX rule.** The partition is asserted — markup roots plus excused roots equals `SOURCE_DIRS`, with neither list claiming a directory twice — which is the same check `assertSourceDirsCoverTheTree` makes about the level above it.

  **These do NOT replace the per-guard exclusion sentences, and the doc says why.** "No unnamed control to find" and "no lib module renders a TextInput" are what a particular rule would have been looking for, which is the more useful sentence and the one a reader checks when a directory changes character. Collapsing five of them into three would lose it.

  **A stale entry is caught too**: every markup root has to actually hold `.tsx`, the same honesty check `SOURCE_DIRS` carries, because a list naming a directory that renders nothing scans nothing while looking maintained.

  3 new cases (9318 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9318 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## MARKUP_DIRS — the roots that hold markup, said once (2026-09-14)

<!-- First bullet of "One-constant follow-ups" (2026-09-14): "`JSX_SUITE_SCAN_DIRS` and the guards' `SCANNED_DIRS` are the same two strings in two places — the same split `lib/source-dirs.ts` was created to resolve for the walk itself. A third entry there, named for what it is, is the version with one statement instead of two that agree." -->

- [DONE: 2026-09-14] [est: 7000] **Five rules narrow to `app/` + `components/` and each one wrote the narrowing out** — `check-a11y-jsx` and `check-clarity-input-mask` from `scripts/`, `check-empty-state-wrappers` and the two heading sweeps from `__tests__/`, and then a sixth copy when the reach floor arrived yesterday afternoon to measure how much of that markup the shared walk reads. `lib/source-dirs.ts` is where this repository already resolved that split for the walk itself; `MARKUP_DIRS` is the same answer for the subset.

  **Named for what it MEANS, not for who uses it.** "The roots the JSX rules scan" is a list that has to be re-justified every time one of them changes; "the roots that hold markup" is a fact about the tree — `data/` holds seed literals that declare no elements, `lib/` returns values rather than JSX — and a rule that wants something else now has to say why.

  **The two script guards keep their own literal**, because `guard-scan-dirs.test.ts` parses `SCANNED_DIRS` out of each wrapper so that a guard's scan list stays readable from its own file. The agreement is asserted instead: both must EQUAL `MARKUP_DIRS`, with a message that names the two ways out (widen the constant, or state why this rule differs). Until this commit they matched by coincidence.

  **`JSX_SUITE_SCAN_DIRS` is an alias rather than a bare re-export**, and the doc says why: the day a suite-side rule legitimately reads a root with no markup in it, that is where it stops being an alias and the diff says so.

  1 new case (9315 in the suite), one two-element list deleted. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9315 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## An assertion about values, not about spelling (2026-09-14)

<!-- First bullet of "Derived-scope follow-ups" (2026-09-14): "The three suite-side roots are pinned by a STRING MATCH on the call site — `readSuite(...).includes('tsxFiles(\"app\", \"components\")')` is an assertion about spelling: a reformat, or the same list built from a constant, turns it red while nothing changed." -->

- [DONE: 2026-09-14] [est: 7000] **The pin added one commit earlier could fail for a reason nobody cares about, and pass for one they would** — matching the literal text `tsxFiles("app", "components")` in three suites goes red on a reformat and green the moment somebody builds the same list from a constant. It was the cheapest thing that caught a widening, and cheap in exactly the way this repository keeps writing down as a mistake.

  **`JSX_SUITE_SCAN_DIRS` is the list, and the three suites read it.** `empty-state-wrapper-audit` and the two heading sweeps had three copies of one answer at three call sites; widening a rule is now an edit to one constant, which is what lets the reach floor follow it instead of watching for it.

  **The floor compares VALUES**: the suite-side roots minus the roots it covers, asserted empty, with a message saying what an uncovered root means — a directory whose markup is watched by nothing.

  **And a floor under that**, because a constant compared against itself proves nothing if the suites have stopped reading it: each of the three has to still spread it. That one IS a source match, and it is the right place for one — it is about whether a file shares a list, which is a fact about its text.

  1 new case, three copies of a list removed (9314 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9314 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The reach floor takes its scope from the rules (2026-09-14)

<!-- First bullet of "Reach-floor follow-ups" (2026-09-14): "The reach is watched over `app/` + `components/` and the rules read more than that." The premise turned out to be wrong — all four read exactly those two — which is the finding, and the fix is to stop restating it by hand. -->

- [DONE: 2026-09-14] [est: 7000] **The floor hand-typed `("app", "components")`, which is a copy of an answer two guards already state** — and a copy is right only until one of them widens. `unionOfScanDirs("check-a11y-jsx", "check-clarity-input-mask")` reads their own `SCANNED_DIRS`, so a rule that grows a third root grows the floor with it rather than leaving a directory whose markup nothing watches.

  **The suggestion that asked for this was WRONG and the round is worth having anyway.** It said the rules read more than the floor does; they read exactly the same two roots, because `lint:jsx-walk`'s six-root walk is a TEXT scan and not a JSX one. What was true underneath is that the agreement was a coincidence nothing checked.

  **The parse moved out of `guard-scan-dirs.test.ts`** — it had been the only reader since three guards were caught declaring one list under three names — into `helpers/guard-scan-list.ts`, on the round it gained a second. The literal is still what gets parsed, deliberately: a `[...SOURCE_DIRS]` spread would satisfy the one-copy rule and defeat the pin that keeps every guard's scan list readable from its own file.

  **The two suite-side rules cannot be read the same way**, because they name their roots at the call site rather than in a script, so a case pins that `empty-state-wrapper-audit` and the two heading sweeps still walk this set — which is where a widening there would say so.

  1 new case, one parser de-duplicated (9313 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9313 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## How much of the markup the rules actually read (2026-09-14)

<!-- First bullet of "Unparseable-tag follow-ups" (2026-09-14): "The walk STOPS at an unparseable tag and every rule silently loses the rest of the file — the honest output is 'read 40 of 180 tags in this file'. The primitive knows where it stopped and tells nobody." -->

- [DONE: 2026-09-14] [est: 12000] **`jsxReach` — the walk says where it gave up, and nothing in this tree gives it the chance** — four scanners reported a clean tree this morning while one of them was reading NOTHING for `components/nav-tab.tsx`. `walkJsx` stops at a tag with no end because it has nowhere to resume from, and it stopped silently: "no findings" and "no reading" print the same line. That is the failure every floor in this repository exists to catch, and the one they could not see, because they count FILES WALKED rather than markup understood.

  **Two assertions over `app/` + `components/`, and each is useless without the other.** No file truncates the walk — which a walk yielding nothing satisfies perfectly — and the tree yields at least 1300 opening tags against the 1625 measured across 71 files today. The same 20% slack the walk floors keep.

  **The failure message names the four rules that would have gone quiet**, because the answer to a truncation is never "look at this suite", it is "go and look at that tag" — and `unreadAt` is an offset into the comment-blanked source, so the line number printed is the real one.

  **The apostrophe has a case of its own, on RAW source.** `stripComments` would hide it, which is the right thing for the rules and the wrong thing for a regression guard: the case asserts the primitive survives the shape, so undoing this morning's `skipStringLiteral` fix turns it red rather than staying green behind a blanking step.

  **`onUnreadable` is optional on `JsxWalk`.** The two rules that inherit something have no use for it; `jsxReach` is what a caller that wants the number takes, and it reports the FIRST offset, because a render-prop recursion can give up inside a tag the outer walk then gives up on too.

  8 new cases (9312 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9312 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## What each rule does with a tag it cannot read (2026-09-14)

<!-- First bullet of "Fail-closed follow-ups" (2026-09-14): "`check-clarity-input-mask` is the only guard here whose unparseable input is a FINDING, and the others were never asked — the distinction is worth one sentence per guard rather than one decision per author." -->

- [DONE: 2026-09-14] [est: 8000] **Four rules walk this JSX and three of them had inherited "skip it" from whoever wrote the walk** — the fourth was caught failing open earlier today, which is what made the question visible. Nobody had decided; three of the answers happened to be right and none of them was written down.

  **The split is clean and it is about what the rule is LOOKING FOR.** A rule about a SHAPE — is this icon button unlabeled, does this wrapper set a card background — is hunting an offence, and a tag it cannot read is not evidence of one; reporting it would make a half-typed file fail a run about design tokens. A rule about a REQUIRED ATTRIBUTE — does this input carry the privacy mask — is hunting an absence, and an unreadable tag is exactly where the attribute might be missing and nobody can tell, so a skip is an unearned pass.

  **Each of the four is now pinned to its side by a case**, with a floor under each skip: a scanner that returned nothing for every source would satisfy "reports nothing about an unparseable tag", so each one also has to still report the tag it CAN read, on the line before.

  **The walk stops rather than resuming.** `walkJsx` has nowhere to pick up from after a tag with no end, so everything below an unparseable tag is outside the walk too — the fact all four decisions are about, asserted on its own so a future reader does not have to infer it.

  8 new cases (9304 in the suite), and the decision in the module header where the next scanner will read it. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9304 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The privacy guard that failed open (2026-09-14)

<!-- First bullet of "Loop-rule follow-ups" (2026-09-14): "`check-clarity-input-mask` failed OPEN and still does — `endOfJsxTag` returns `source.length` for a tag that never closes, so a malformed file is treated as one enormous tag and a single `clarity-mask` anywhere in it satisfies every input below." -->

- [DONE: 2026-09-14] [est: 9000] **One unclosed tag made every `<TextInput>` below it read as masked** — `endOfJsxTag` answered `source.length` for a tag it could not parse, with a comment explaining the choice: "treat the rest of the file as the tag so the mask check still sees every attribute". Every attribute, and every other input, and any string that happens to contain `data-clarity-mask`. For a rule about whether user keystrokes reach a session recorder, that is the worst failure available: the guard reports a clean tree.

  **It reads the tag or it says it could not.** `openTagEnd` answers -1, which is information; the tag is now reported with a hint naming the real problem rather than the mask advice, because "add data-clarity-mask" is not what to do about a tag that never closes.

  **The quoted-`>` fix has a case now, which it did not when it landed this morning.** `<TextInput placeholder=">" data-clarity-mask />` was argued from the code — nothing in this tree carries a quoted bracket before its mask — so the improvement was a claim. It is a fixture.

  **The unclosed-tag case pins the old behaviour by construction**: an unclosed tag on line 1 and a masked input on line 2, where the old reader found the marker below and reported nothing at all.

  3 new cases (9296 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9296 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The walk written as a loop, in a shipping guard (2026-09-14)

<!-- First bullet of "Anchored-wildcard follow-ups" (2026-09-14): "The guard still cannot see the walk written as a LOOP — both rules match a regex literal or a string search, so a hand-rolled `for` with a brace counter passes, and that is what `check-empty-state-wrappers` would have become if somebody had 'fixed' its regex by hand." -->

- [DONE: 2026-09-14] [est: 13000] **`char === ">" && depth === 0` is the whole of `openTagEnd`, and two files had it written out** — the guard's two rules matched a regex literal and a string search, so the walk spelled as a character loop went straight past. It is the shape the previous round's suggestion named as the honest limit; it turned out to be occupied.

  **One of the two is a SHIPPING guard, and its copy has the documented bug.** `check-clarity-input-mask` counted braces and did not skip string literals — the exact half `lib/jsx-open-tag.ts`'s header records an earlier copy leaving out, written again independently months later. A `<TextInput placeholder=">" data-clarity-mask />` ends at the quoted bracket, so the mask attribute is invisible and a masked input reports as unmasked. The escape-hatch `return source.length` meant it failed OPEN rather than closed.

  **The other had reimplemented `openTagsNamed` around it** — brace depth and string skipping, twenty-five lines, missing the backslash escape (`placeholder="a \" >"` closes early) and blind to a `<Pressable>` rendered through a render prop.

  **The rule is the PAIR, not either half.** A `>` comparison alone is ordinary text handling — the privacy-page reader tests for a markdown blockquote — and a depth counter alone is how `declared-shape` and `check-platform-pairs` read TypeScript type text. Together, and adjacent, they mean "the opening tag ends here". Adjacency is what keeps `char === ">" && code[i - 1] !== "="` out: that one closes a generic, and a case pins it.

  5 new cases for the rule, 2 copies deleted (9293 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9293 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The wildcard that reaches the `>` a few characters later (2026-09-14)

<!-- First bullet of "Empty-span follow-ups" (2026-09-14): "Sixteen suites assert a negative over a slice they cut by hand — those blocks are cut by a named regex rather than by a walk, so there is no shared helper to hang a floor on." -->

- [DONE: 2026-09-14] [est: 42000] **`lint:jsx-walk` matched a wildcard IMMEDIATELY followed by `>`, and forty call sites reached it through a couple of anchoring characters** — `/<FlatList[\s\S]*?windowSize=\{5\}[\s\S]*?\/>/` and `/<Pressable[\s\S]*?\n {6}>/` are the two commonest shapes here; the second finds an element's end by its INDENTATION. Same walk, same bug, invisible to the rule as it shipped this morning. A short lazy tail between the wildcard and the `>` turned nine findings into forty, and all forty are migrated.

  **`<FlatList[\s\S]*?prop[\s\S]*?\/>` reads as "the FlatList sets this prop" and means "somewhere after SOME `<FlatList` there is this text before SOME `>`".** `app/collection/[id].tsx` renders three of them — the viewer list's `windowSize={5}` and the selection list's `{7}` are the distinction those cases exist to hold, and one wildcard spanned both. It also crosses into the nested `<RefreshControl … />`, so the props being tested were partly another element's. `__tests__/helpers/jsx-element-props.ts` asks the element instead, and `elementTagWith` refuses TWO matches rather than taking the first: a marker that has stopped identifying anything is not a marker.

  **A shipping guard had the same bug.** `check-empty-state-wrappers` tracked `<View>` nesting with `/<View\b([^>]*?)(\/?)>/`, so a wrapper carrying `onLayout={() => measure()}` pushed a truncated attribute string — a `backgroundColor` after that point was invisible and the guard reported a clean tree — and the unmatched `>` desynchronised the open/close pairing for the rest of the file. It is a `walkJsx` with an inherited View stack now, which also reaches a wrapper rendered through a render prop.

  **An APOSTROPHE in a comment inside an open tag returned NOTHING for the whole file.** `skipStringLiteral` let a `'` open a string that never closed, `openTagEnd` ran off the end and returned -1, and `walkJsx` stops at a tag that does not close — so `components/nav-tab.tsx` walked to an empty list, silently. A quoted string cannot contain a raw newline in JavaScript; it ends at the line break now, and a template literal still spans lines because it legally does.

  **Two exclusions the widening needed, both found by running it.** `flushPendingQueue<ChatMessage>(pending, …)` is a type argument, told from JSX by the identifier character before the `<`. And a tag whose own `>` the author wrote (`/<I18nProvider>[\s\S]*?<DiagnosticsProvider>/`) is a nesting assertion with no wildcard finding anything — tested in code rather than as a `(?!>)`, which let the NAME backtrack and duly reported `<I18nProvide>`.

  15 new cases for the helper, 9 for the guard and the primitive, 40 call sites migrated across 21 files (9288 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9288 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The sheet reader asks the body, not the tag shape (2026-09-14)

<!-- First bullet of "One-meaning follow-ups" (2026-09-14): "`modalSpans` still decides for itself what a self-closing `<Modal />` means — it skips them, `elementSpan` now answers with an empty span, and both are right. Two statements of one rule is the shape that drifts." -->

- [DONE: 2026-09-14] [est: 7000] **`<Modal></Modal>` was a real span over nothing, and the skip that should have caught it was about the wrong thing** — `modalSpans` skipped `tag.selfClosing`, which is a second statement of what a self-closing element means now that `elementSpan` says it with an empty body. It skips an empty BODY instead, which is the rule this function actually has: a Modal with nothing in it wraps no sheet, however it was written.

  **The paired-and-empty case is what the old test on the tag shape let through.** `<Modal></Modal>` produced a span with a start and an end and nothing between them, so a sheet written after it could land in the gap between two spans and be reported as drawn outside a Modal — a message whose reason nobody would guess from reading it.

  **"Empty" has to mean empty, not short.** A case pins that a Modal whose only child is a whitespace-wrapped `<View />` still covers it, because `trim()` is doing the work and a rule that discarded small spans would be the same bug wearing the fix's name.

  2 new cases (9265 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9265 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## One meaning for `end` (2026-09-14)

<!-- First bullet of "Falsifiable-window follow-ups" (2026-09-14): "`elementSpan` gives a self-closing tag `end === tagEnd`, which is the `>` and not the `/` — two different meanings for one field, defensible for each and stated nowhere; the first caller that slices a mixed list will find out by rendering half a tag in an error message." -->

- [DONE: 2026-09-14] [est: 8000] **`elementSpan.end` is exclusive, like every other end offset a `slice` takes** — it shipped this morning as "the offset of the close tag" for a paired element and "the offset of its `>`" for a self-closing one. `code.slice(start, end)` therefore returned the element minus its close tag in the first case and the element minus its final bracket in the second, and a caller holding a mixed list of both gets one of each with nothing at the call site to say which.

  **Neither meaning was wrong; having two was.** The fix is the one a reader already expects from an `end`: just past the element's last character, so the slice is the whole element in both shapes. A case asserts exactly that, over one of each, because it is the property the field exists for and the one that was silently false.

  **Every reader of it got more correct rather than less.** `<BulkBar>` "after the Profiler closes" now means after the whole element instead of after the `<` of its close tag, and the sheet-span containment is unchanged because an `accessibilityRole="none"` cannot sit inside a close tag.

  **One day between the bug and the fix, which is the argument for the suggestion file.** The field had three callers and all three were green: nothing was going to surface this except somebody reading the type, and the note that said so was written by the round that introduced it.

  1 new case (9263 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9263 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## A window that shrank to nothing is a finding, not a pass (2026-09-14)

<!-- First bullet of "Flat-walk follow-ups" (2026-09-14): "Three suites assert a NEGATIVE over a slice that could be empty — a reader that answered -1 everywhere would make the first pass vacuously; the migrations are green and nothing says they are still reading anything." -->

- [DONE: 2026-09-14] [est: 11000] **`elementSpan` — where an element closes AND what is inside it, because the second half is what makes the first falsifiable** — nine call sites moved onto the shared JSX walk this morning and three of them assert that something is NOT in a window. A negative is true of an empty string: `entry-currency-notice` would have passed with a close tag at offset 0, reading nothing, and its only guard was `!== -1`. The window now has to contain the line it is about before it is asked what it does not contain.

  **`closeTagIndex(code, tag.tagEnd + 1, tag.name)` had been written out three times in a day**, and the `+ 1` is the part a fourth copy gets wrong — `tagEnd` is the `>` itself, so a body counted from there opens with the tag's own last character. One function, and a case that pins the offset.

  **The two ordering assertions were vacuous in the other direction.** `<BulkBar>` after `</Profiler>` holds just as well when the close came back too EARLY, and `<StorageNotice />` before `</ToastProvider>` holds when it came back at the end of the file. Both are stated over the body now: the Profiler has to wrap a `<FlatList>`, and the provider's body has to contain the mount. A broken reader cannot satisfy either.

  **A self-closing tag is a complete element with nothing inside** — `""` and its own `>`, not null. A caller asking what is inside `<HeroBanner />` has an answer, and looking for its close tag would find the next element's, which is the span-swallowing bug the sheet reader was fixed for yesterday. A missing close tag is null rather than -1, so a caller cannot slice with it by accident.

  6 new cases (9262 in the suite), 4 call sites shortened and 3 of them made falsifiable. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9262 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The flat walk says so once (2026-09-14)

<!-- First bullet of "Refused-fourth-walk follow-ups" (2026-09-14): "`openTagsNamed` returns TEXT and two of the four migrations wanted offsets — both dropped to `[...walkJsx(code, { seed: null, inherit: () => null })]` and a `.find`, which is the same six words in two files." Second bullet, folded in because it is the same change: "`{ seed: null, inherit: () => null }` is now written in five places." -->

- [DONE: 2026-09-14] [est: 9000] **`jsxTags` and `tagsNamed` — the argument that reads the same either way, said at the call site instead** — `walkJsx(code, { seed: null, inherit: () => null })` was written in five places within a day of the walk being extracted: four suites and `openTagsNamed`. None of them has an ancestor question; they want the tags, and the phrase that says so is fourteen tokens of noise on every line.

  **The two callers that DO inherit something still say so**, which is the point of separating them: the a11y guard tracks "an ancestor has hidden this subtree" and the heading sweeps track "a `<Modal>` is above this node", and those are the lines where a reader should stop and read the callback. The other five were carrying the same weight for nothing.

  **`jsxTags` yields a plain `JsxTag`.** The `inherited` field of a walk that inherits nothing is a null every caller has to ignore, and one of the five was already destructuring around it.

  **`tagsNamed` is the offsets half and `openTagsNamed` is now one line over it** — two suites had written `[...walk].find((tag) => tag.name === "Profiler")` on the same afternoon, which is the same six words for the same question. They read `const [profiler] = tagsNamed(block, "Profiler")` now, and a case pins that a name nothing renders comes back as an empty array rather than a throw, because a destructure is how both of them read it.

  4 new cases (9256 in the suite), 4 call sites shortened. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 21/21 guards, 9256 tests, audit baseline, build, bundle secrets, bundle size (15.6 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The fourth JSX walk is refused rather than discouraged (2026-09-14)

<!-- First bullet of "Last-copy follow-ups" (2026-09-13): "Three walks were merged today and nothing stops a fourth — the next sweep that needs 'every tag in this file' will write the tag stack again, because the reason not to is a paragraph in a module header rather than a rule." -->

- [DONE: 2026-09-14] [est: 25000] **`lint:jsx-walk` — and it found NINE more copies on the day it was written** — three walks were merged into `lib/jsx-open-tag.ts` on 2026-09-13, two of them carrying the same render-prop bug and fixed an hour apart. What stopped a fourth after that was a paragraph in a module header, and a paragraph is not a rule: the guard ran green on nothing and red on nine, in seven suites, every one of them passing at the time.

  **Two shapes, because there are two ways to fake the walk.** `open-tag-regex` is `/<HeroBanner[\s\S]*?>/` — the exact failure that module's header opens with, where the wildcard ends at the first `>` in the tag, which in this codebase is the one inside `onPress={() => close()}`. `close-tag-search` is `code.indexOf("</Modal>")` — the first close tag belongs to the INNERMOST element of that name, not the one being asked about.

  **Two of the nine were live bugs, not style.** `link-aschild-style-flatten` matched `<Link\b[^>]*\basChild\b[^>]*>`, so a link with a handler prop was never found at all — a sweep reporting a clean tree while reading none of it — and then ended the CHILD's tag at its first `>`, so `<Pressable onPress={() => …} style={[…]}>` had the array style cut off the end of the window. That ordering is the exact web crash the file exists to prevent.

  **Uppercase only, which is the JSX convention rather than an exemption list.** This tree genuinely parses HTML in three places — the built page's `<h1>`, the `<head>` a CSP meta is injected into, the `</head>` the SPA marker must precede — and none of `jsx-open-tag`'s premises hold there. Nothing has to be maintained as those three files change.

  **A precise regex is still allowed.** `/<Profiler\s+id="selection-flatlist"\s+onRender=\{…\}\s*>/` spells out the props it expects instead of wildcarding past whatever is there, and a case pins that it passes: a rule that fired on every match against markup is a rule somebody turns off.

  **The suite assembles every fixture from pieces**, because it sits inside the tree the guard walks — the same predicament `check-comment-terminators` records about its own terminator — and the last case asserts the assembly worked, so a literal fixture is caught here rather than as a confusing finding in `lint:all`.

  21 new cases for the guard, 4 for the new `openTagsNamed` primitive, 9 call sites migrated (9252 in the suite). 0 new keys, 0 bundle cost — the guard ships to nobody. `npm run verify` green: typecheck, 21/21 guards, 9252 tests, audit baseline, build, bundle secrets, bundle size (15.6 KiB headroom), bundle smoke, ships-to-client. No DB migration, no creds.

## The third JSX walk joins the other two (2026-09-13)

<!-- First bullet of "Shared-walk follow-ups" (2026-09-13): "`sheet-modal-semantics` still finds its `<Modal>` spans with a `code.indexOf(\"</Modal>\")` — the third walk over the same JSX, and it is the one remaining copy." -->

- [DONE: 2026-09-13] [est: 7000] **A sweep that found each `<Modal>`'s extent by taking the next `</Modal>` after it** — correct on this tree and correct for a documented reason, which its own comment spelled out: "these do not nest anywhere in this tree, so the next close is this element's close", with the note that a nested one would make the span too small and so fail closed. Failing closed is the right choice for an assumption. Not needing one is better: `walkJsx` finds the tags and `closeTagIndex` counts depth, so a nested sheet gets two spans with the outer one covering both.

  **Three copies of one walk this morning, one of them left.** Two were found carrying the same render-prop bug and merged into `lib/jsx-open-tag.ts`; this one was a different shape — a span reader rather than a tag stack — and the same question about the same text. It is the last one.

  **A self-closing `<Modal />` covers nothing now**, where the old reader would have gone looking for its close tag and found the NEXT sheet's — a span swallowing everything between two unrelated modals, and every rule in the file ruling on it.

  **Four cases for the reader itself, which had none**: the nested pair (with the assertion that the old `indexOf` answer falls INSIDE the correct outer span, so the case says what was wrong rather than only what is right), the self-closing tag, a `<Modal>` rendered through a render prop, and a half-typed sheet with no close tag at all — these files are read mid-edit and that is not a finding.

  4 new cases (9203 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 20/20 guards, 9203 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## The boot check says how long the app took to come up (2026-09-13)

<!-- First bullet of "Settle-poll follow-ups" (2026-09-13), written by the round that made the number exist: "The settle time is measured and then thrown away." -->

- [DONE: 2026-09-13] [est: 7000] **The poll knew each page mounted in about 160 ms and told nobody** — the number was computed on every run of `npm run check:boot` and dropped on the floor, which is the one measurement in this repository that answers "how long does the app take to come up". Three suggestion groups have asked for it since the startup buffer landed. The report says it now, on its own line: `settled after 158 ms`.

  **On a FAILURE it says something the report could not say before.** "gave up after 10012 ms" is a rule that was never satisfied — the copy never rendered, the chunk never arrived — and "gave up after 210 ms" is one that broke at once. Same failure list, two different things to go and look at, and until now a reader had neither.

  **Nothing is printed when nobody measured.** The parameter is optional and a missing clock prints no line rather than a zero, which would read as an instant boot — the same rule the copy-drift line follows in the size gate.

  **A number in a report, not a budget.** It is not a threshold, nothing goes red on it, and that is deliberate: a settle time measured once on one machine is exactly the kind of number the flat 1.5-second wait was, and this round exists because that turned out to be a guess.

  4 new cases (9199 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 20/20 guards, 9199 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount, in 158, 161 and 160 ms. No DB migration, no creds.

## One tag-stack walk, after two copies had the same bug (2026-09-13)

<!-- First bullet of "Render-prop-reach follow-ups" (2026-09-13), written by the round an hour earlier that fixed the second copy: "the same twenty lines, the same tag stack and now the same fix, applied twice within an hour." -->

- [DONE: 2026-09-13] [est: 11000] **`lib/jsx-open-tag.ts` said the guard's tag-stack walk was "a rule about that guard's domain rather than a primitive", and it was right until there were two of them** — one in `lib/check-a11y-jsx.ts`, one in `__tests__/helpers/jsx-headings.ts`, written months apart, both carrying the same hole: a render prop's JSX lives inside the PARENT'S open tag, so both walks stepped over everything a component renders through a prop. Two copies, one bug, two fixes an hour apart is the argument that module was written to make, so `walkJsx` is now in it and the header says why the old sentence stopped being true.

  **What the callers keep is the only thing that differed: what a tag passes down.** The guard inherits "an ancestor has hidden this subtree on all three platforms"; the heading sweeps inherit "a `<Modal>` is above this node". That is a one-line `inherit` callback each, against twenty lines of stack, brace-counting and close-tag matching that were identical — and identical is not the same as duplicated until one of them is wrong, which is exactly what this morning found.

  **The walk carries the inherited value INTO the render prop.** A prop's children are inside the element that hides them, so a decorative list drawn through a prop is as unreachable as one drawn through children; the case that pins it is the false positive the `covered` stack exists to prevent.

  **Seven cases for the primitive itself**, where before it had none: the carrier is yielded before the tags inside it, a self-closing tag never becomes an ancestor, inheritance stops at a close tag, `count < max` is not a tag, an unmatched close is ignored rather than thrown on, and the offsets point into the string the caller passed — with a `">"` inside an arrow function, which is the thing this whole module exists for.

  7 new cases (9195 in the suite), and 40 lines of duplicated walk deleted. 0 new keys, 0 bundle cost. `npm run verify` green: typecheck, 20/20 guards, 9195 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## The a11y guard had the same hole the heading sweep did (2026-09-13)

<!-- First bullet of "Screen-heading follow-ups" (2026-09-13), written an hour earlier by the round that found the hole in the suite copy of the walk: "whether the GUARD's own walk has it is unasked, and it is the same `openTagEnd` call." -->

- [DONE: 2026-09-13] [est: 10000] **Six shipping accessibility rules, and none of them had ever looked inside a render prop** — `check-a11y-jsx` walks the two source roots for unlabeled icon buttons, untranslated labels, half-hidden nodes, undecided icons, silent disabled buttons and roleless tappables. A render prop's elements live inside the PARENT'S OPEN TAG: `<SwipeTabs renderTab={(key) => (<View>…</View>)} />` is one tag, `openTagEnd` correctly reports it as ending after the closing brace, and the cursor resumed there — sixty lines and a screenful of `<Pressable>`s later. Every rule was blind to every one of them.

  **The hole was found in a copy and the question was whether the original had it.** The suite half, rewritten this morning, read `app/friends.tsx` as having nothing to classify because both tab panels are drawn through a prop; the guard makes the identical `openTagEnd` call, so the suggestion that round wrote asked. It did.

  **Nothing on this tree was actually unlabeled, which is the finding.** The guard is green after the fix: the components rendered through props here are already named. That makes this a closed blind spot rather than a bug list — and the reason to write it down is that "the scan found nothing" meant two different things before this commit and means one now.

  **Proved by removing the fix**: two of the five new cases go red with the recursion deleted, which is the guard reporting nothing at all about an unlabeled icon button. Restored, green.

  **The hide is carried down.** A render prop's children are inside the element that hides them, so the recursion seeds `covered` from the parent — a decorative list drawn through a prop is as unreachable as one drawn through children, and flagging it would be exactly the false positive the `covered` stack exists to prevent. A case holds it, with `no-hide-descendants` rather than `"no"`: the first draft of that fixture used the wrong Android prop and the guard was right to flag it.

  5 new cases (9188 in the suite). 0 new keys, 0 bundle cost — the guard ships to nobody. `npm run verify` green: typecheck, 20/20 guards, 9188 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## The search overlay says how many results it found (2026-09-13)

<!-- First bullet of "Search-landmark follow-ups" (2026-09-13): "The result count is in the heading and nothing announces it changing." -->

- [DONE: 2026-09-13] [est: 12000] **The count a screen-reader user is listening for was written into a heading they had already passed** — three section headings landed this morning and they are what the rotor finds AFTER you go looking. Typing is the other half: results re-rendered under every keystroke with nothing said, so the one question a search answers — did that letter help — had no answer short of navigating back through the sections. The total is spoken now, through the same `lib/announce.ts` channel the reorder and the toasts use, so a toast landing in the same tick cannot cut it off mid-word.

  **THE RULE IS "THE NUMBER CHANGED", NOT "SOMETHING RENDERED", and that is the whole design.** `announceForAccessibility` interrupts, so a reader that speaks on every render spends the session cutting itself off; hearing "12 results" again after one more letter says nothing, while hearing it drop to 3 is the entire signal. `lib/search-announcement.ts` holds it because it has four edges worth a case each: a repeat of the spoken number is silence, an empty field is not "0 results" but no search at all, a cleared field FORGETS (so the same query typed again is announced again — a second search is a new question), and the remote profile search resolving after the local matches must not say the same total twice.

  **Zero is the number most worth saying**, which is why the empty state does not get a special case: "0 results" is the difference between "nothing matched" and "the app did not react".

  **It waits for typing to settle.** The effect runs when the debounced query catches up with the field, so a fast typist gets one announcement rather than one per keystroke — the failure this feature exists to prevent rather than cause. The memory is a `useRef`: state would re-render, and a re-render is what the effect reacts to.

  **The words are the locale maps', not the module's.** Six languages, three of which inflect on the last digit, so `searchResultsAnnouncement` agrees through `plural`/`slavicPlural` — a flat template would read "1 результатов" on the most ordinary count there is. Cases hold 1/3/12 in Russian, the three-form Polish and Belarusian shapes, that no locale inherits the English wording, and that every language leads with the number in at most three words.

  16 new cases (9183 in the suite). 1 new key in all six locales (551 now), +1.4 KiB. `npm run verify` green: typecheck, 20/20 guards, 9183 tests, audit baseline, build, bundle secrets, bundle size (15.6 KiB of headroom), bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## Every screen's section headings are headings now, and the sweep can see round a render prop (2026-09-13)

<!-- Second bullet of "Search-landmark follow-ups" (2026-09-13), written an hour earlier by the round that gave the search overlay its three: "Nothing outside a modal is held to the heading rule". -->

- [DONE: 2026-09-13] [est: 13000] **Twenty section labels on ordinary screens, none of them announced as a heading** — the sweep two rounds ago covered modals, for a reason specific to sheets: a sheet has no landmark of its own. That is why the rule started there and it was never a reason a SCREEN needs none. A profile with a description, a collections list, a sale history and a wishlist under four unmarked labels is one long swipe from top to bottom, and the rotor — which is how VoiceOver and TalkBack move — had nothing in it. Settings, stats, marketplace, friends, home, profile, login and the dev token screen all say which section is which now.

  **THE SWEEP HAD A HOLE AND THIS IS WHAT FELL THROUGH IT.** The walk stepped over every element rendered inside a render prop: `<SwipeTabs … renderTab={(key) => …}>` is one open tag sixty lines long, `openTagEnd` correctly reports where it ends, and the cursor jumped the lot. `app/friends.tsx` renders both tab panels that way, so the rule read the file as having nothing to classify — the quietest way a sweep can be wrong, and the same hole hid anything a SHEET renders through a render prop from the modal rule next door. The scan recurses into an open tag's own JSX now, carrying the modal context with it, and a case pins friends' two headings because they are reachable no other way.

  **`section…` and not every title, deliberately.** A style called `section…` is a heading above a list by construction. `…Title` on a screen is genuinely ambiguous — `styles.title` is the page's own heading while `cardTitle`, `rowTitle` and `itemTitle` name the things in a list — and marking those would put one heading per row into the rotor, which is the exact failure the modal rule exempts the search overlay's rows for. So this rule has NO exemptions: the day a `section…` style turns out not to be a heading, the fix is to rename the style.

  **One walk, two rules, in `__tests__/helpers/jsx-headings.ts`.** The rules differ — a sheet's is a classification with exemptions, a screen's is an assertion without them — but the hard part is the same, and it is the part that was wrong: finding where an open tag ends, and answering "is this inside a `<Modal>`" with a tag stack. A second copy of that walk is the copy that keeps the render-prop bug.

  **The browser says it landed**: the exported `dist/` booted in Chromium renders 3181 characters of markup where it rendered 3155 all week — the login screen's two section headings, now `role="heading"` on web.

  7 new cases (9167 in the suite). 0 new keys, +0.6 KiB. `npm run verify` green: typecheck, 20/20 guards, 9167 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## The search overlay gets the landmarks its exemption implied it had (2026-09-13)

<!-- Third bullet of "Modal-heading follow-ups" (2026-09-13): "`components/search-overlay.tsx` is a full-screen modal with no heading at all — it is the one file the new sweep exempts, and the exemption is honest about the rows and silent about what is missing." -->

- [DONE: 2026-09-13] [est: 9000] **The one file the heading sweep exempted had three headings' worth of copy and none of the role** — the overlay's results are items, collections and people, each list under a `sectionLabel`, all three in one scroll. A screen-reader user reaching the two people at the bottom swiped through the twenty items above them, because the rotor had nothing to jump to. All three labels carry `accessibilityRole="header"` now.

  **The exemption was describing the bug, not the design.** "Marking the rows would put one heading per search result into the rotor and leave the overlay itself with none" — true about the rows, and the second half was a fact about the file rather than a reason: the overlay HAD none. It is a rule about rows now, in a file with landmarks of its own.

  **The sweep could not have found it, which is the part worth fixing.** It matched `styles.title` and anything ending in `Title`, so this file's only match was `rowTitle` — correctly exempt — and the verdict on the whole file was "nothing to classify" while three section labels sat above three lists. A section label is the other shape a heading takes here, so `section`, `sectionLabel` and `sectionHeading` are matched the same way `sheetTitle` is: what a style is CALLED is the only signal a scan has, and this repository names the heading above a list `section…`.

  **`sectionText` stays out, for the reason `subtitle` does** — it is the body copy under a heading, and claiming it would ask for two headers where the design has one. Both edges are a case rather than a sentence in the matcher's doc block.

  **Proved by removing one**: the People section's role deleted turns the classification case red by file and line (`components/search-overlay.tsx:464`) and the named case red beside it. Restored, green.

  **Not a title for the overlay itself, deliberately.** The suggestion asked for one; the surface is already named where a user arrives at it — the field is `accessibilityRole="search"` with `searchOverlayA11y` as its label and it takes focus on open — and a title row above an autofocused search field is vertical space on a full-screen sheet for a second announcement of the same thing.

  5 new cases (9160 in the suite). 0 new keys, no bundle growth. `npm run verify` green: typecheck, 20/20 guards, 9160 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## The boot check waits for an answer instead of for a number (2026-09-13)

<!-- Fourth bullet of "Rendered-copy follow-ups" (2026-09-13), carried through three groups since the check booted one page: "`SETTLE_MS` is 1.5s and pays three times". The group that carried it last also said what made it answerable — "polling for 'the root has markup AND the expected copy is on screen' is now a well-defined condition, which it was not before this round". -->

- [DONE: 2026-09-13] [est: 14000] **A flat 1.5-second wait after every load event, three times a run, under a comment arguing that polling "would make the check pass by waiting for the thing it is asking about"** — the objection was the right one and it has an answer: the poll is not allowed to decide anything. `isBootPollFinished` stops the loop only when the verdict can no longer change, and every condition it waits for — an empty root, copy that has not rendered, a lazy chunk not yet requested — is one `evaluateBundleBoot` FAILS on. A boot that never satisfies them is failed at the deadline exactly as it was failed at 1.5 seconds; what moved is when the answer is read, never what it is.

  **160 ms, measured, where the flat wait charged 1500.** Each scenario settles in about a sixth of a second, so the run gives back 4.0 of the 4.5 seconds it was spending on a guess — and the guess was the interesting half: 1.5 s was a number somebody picked on one machine before this check grew a lazy locale chunk and a third page, one bad connection away from reporting "the chunk was slow" as "the app is broken". The deadline is 10 s now and is only ever paid by a boot that is already failing.

  **It also stops EARLY, and that is the half the kinds had to be named for.** An exception, a console error or a 404 is an event the browser has reported and cannot un-report, so a run that has seen one has its answer; sitting out the remaining nine seconds would only make the report slower to say so. Which means the six failure kinds are now SORTED — three "not yet", three "no" — and a kind spelled at its push site could drift across that line silently, so `BOOT_FAILURE_KINDS` names them once and a case drives all six through `evaluateBundleBoot` to assert the set is the same on both sides.

  **The property that makes polling honest is a case rather than a paragraph**: each waitable kind, fed through the verdict, must make `ok` false. If one of them could ever appear on a passing result the loop would be inventing an answer instead of reading one.

  **One related bug the poll made real.** The observation handed to the verdict held the socket listener's live arrays. Harmless at one observation per boot; at a round every 100 ms it means the result that ends the loop carries whatever arrived after it was judged — a report listing a failure the verdict printed above it never saw. `observe()` copies now.

  **Proved against the real browser both ways**: all three scenarios mount and settle in ~160 ms each, and with `pl-*.js` moved aside the Polish boot fails with both the 404 and "copy not on screen" without spending the deadline — the 404 is terminal, so the loop stops on it.

  13 new cases (9158 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9158 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The Polish boot proves the copy RENDERED, not just that a chunk arrived (2026-09-13)

<!-- Second bullet of "Three-scenario-boot follow-ups" (2026-09-13), written by the round that added the scenario an hour earlier. -->

- [DONE: 2026-09-13] [est: 9000] **"A `pl-` chunk was fetched" and "the app is in Polish" are different claims, and only the first was checked** — a registry that requested the chunk and kept the default map satisfies `expectChunk` completely, mounts, fills the root and logs nothing. Every scenario now names copy that has to be on screen: "Аккаунт" for the two default-language boots, "Konto" for the Polish one. The report already printed the sentence; this makes it a rule.

  **Case-insensitive, because the heading is uppercased by a style** and `innerText` reports what is on screen — the copy says "Аккаунт" and the page says "АККАУНТ". The expectation is written the way the locale map writes it, and the comparison is what bridges them.

  **Held against the locale maps by a case**, so a re-worded auth heading is a red test with a message rather than a red check somebody has to go investigate: each scenario's fragment must appear in that language's `authAccount`, and the Polish fragment must NOT appear in the Russian one — a fragment both languages contain ("Collectables") would pass whichever map rendered, which is exactly the failure the scenario exists to catch.

  **The deep-link scenario now proves the storage clear too.** It seeds nothing and expects the default copy, so the leak that the first version of the loop shipped with — a scenario inheriting the previous one's language — is a red check rather than a line somebody has to notice in the output.

  **Proved against the real browser**: the Polish expectation pointed at a word the page does not contain — "FAIL — copy not on screen: … first line was "KONTO COLLECTABLES"". Restored, all three scenarios pass.

  5 new cases (9145 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9145 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The currency picker leads with the currencies you use (2026-09-13)

<!-- Fourth bullet of "Sheet-title follow-ups" (2026-09-13): "The pinned-currency list and the picker disagree about ordering". -->

- [DONE: 2026-09-13] [est: 12000] **The chip strip above the cost input has led with recently-used codes since it was written, and the sheet behind its "…" chip listed 160 rows of ISO order and nothing else** — so a collector who types in three currencies scrolled past all three to reach the fourth. The picker now opens on a "Recently used" block above "All currencies", built from the same `getPinnedCurrencies` MRU list the chips come from.

  **A query collapses it back to one list.** Somebody typing "PL" already knows what they want, and a match hidden under a second heading is worse than a match in one list — which is also exactly what the sheet did before this existed, so the search path is unchanged.

  **No currency appears twice.** A pinned code is removed from the long list rather than repeated in it: two identical rows, one of them checked, is a picker that looks broken. The shortlist keeps MRU order and the rest keep ISO order, because the two blocks answer different questions.

  **The decision is `currencyPickerGroups` in `lib/currencies.ts`, not a `useMemo`** — it has three edges (a query, a code storage kept after the currency list dropped it, a duplicate inside the shortlist itself) and a decision inside a component is one nobody can argue with. Eleven cases cover them, including the two fallbacks to the flat list.

  **Both section labels are headers**, so a screen-reader user skips the shortlist by heading rather than by swiping through it — the rule the sweep two rounds ago established for every modal heading in the tree, which this new one satisfies rather than exempting itself from.

  Two new keys in all six locales (550 now). 15 new cases (9140 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9140 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three boot scenarios mount. No DB migration, no creds.

## The boot check loads three pages, one of them in Polish (2026-09-13)

<!-- Second bullet of "Boot-check follow-ups" (2026-09-13): "It boots ONE page, signed out, in one language". -->

- [DONE: 2026-09-13] [est: 14000] **`npm run check:boot` booted the one page whose copy is in the entry chunk and whose route the server has a file for** — which is to say, the one page that could not see either of the two things that broke this year. It now loads three: the home screen, the home screen with `pl` seeded into storage, and a deep link the export has no file for.

  **The Polish boot is the only thing in this repository that proves a lazy locale chunk is REACHABLE.** A `pl` that silently fell back to the Russian copy mounts, fills the root, logs nothing and passes every other rule here — so `expectChunk` is what makes it a failure, and the report prints "KONTO COLLECTABLES" as the first line on screen. Proved by hiding `pl-*.js` from a built `dist/`: FAIL, HTTP 404 on the locale chunk, while the tree still mounted in Russian — exactly the silent fallback the scenario exists for.

  **The seed goes in before the page's first script**, because the language is read once on mount: a write after the load would be testing a language CHANGE, which is a different thing. `LANGUAGE_KEY` is imported rather than spelled, so a renamed key is a type error instead of a default-language boot under a Polish label.

  **A new target is not a new origin, and the first run of the loop proved it.** The scenario after the Polish one seeds nothing, is supposed to boot the default language, and rendered "KONTO COLLECTABLES" — it had inherited the previous scenario's `localStorage`. Every scenario clears storage now, seeded or not, and each closes its target rather than leaving three pages open in one browser running three copies of the app.

  **Every scenario runs even after one fails.** A run that stopped at the first would cost a second build to find out whether the other two are broken too.

  12 new cases (9125 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9125 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. All three scenarios mount. No DB migration, no creds.

## Every sheet in the app has a heading a screen reader can find (2026-09-13)

<!-- First bullet of "Sheet-title follow-ups" (2026-09-13), written by the round two hours earlier that gave one sheet the role and noticed the other eleven did not have it. -->

- [DONE: 2026-09-13] [est: 11000] **Twelve modal titles were ordinary text, so a sheet gave VoiceOver and TalkBack nothing to navigate to** — both screen readers move by heading, and a sheet that slides up over a screen has no landmark of its own. Until its title carries `accessibilityRole="header"` it is one more line above a list, and the way out of the 160-row currency picker is to swipe through it. Every sheet in `app/` and `components/` now says which one it is: the two cost sheets, the wishlist's add and promote sheets, the collection edit and move modals, both share sheets, the filter sheet, the premium upsell and the sold-listing prompt.

  **The rule is a CLASSIFICATION, not a count, which is what keeps it true for the thirteenth sheet.** A `Title`-styled `<Text>` inside a `<Modal>` subtree is either a heading — and then it says so — or it is a row label that happens to be named `…Title`, and then it is listed with the reason. `components/search-overlay.tsx` is the listed one: it is a full-screen modal whose results are rows, and marking those would put one heading per search result into the rotor and leave the overlay itself with none.

  **A scan over the two roots rather than twelve assertions**, because twelve assertions pass forever and say nothing about the next sheet. It walks with `lib/jsx-open-tag` and a tag stack for the same reason `check-a11y-jsx` does — the obvious `/<Text([\s\S]*?)>/` ends the open tag at the first `>` in an arrow function, and "inside a modal" is a question about ancestors: `app/item/[id].tsx` renders both an item title and a share sheet's title, and only one of them is a modal heading.

  **`subtitle` is deliberately not matched** — it is the second line under a heading, and claiming it would ask for two headers where the design has one.

  **Proved by removing one**: the share sheet's role deleted turns the sweep red by file and line, and names the file in the second case too. Restored, green.

  4 new cases (9113 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9113 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. `npm run check:boot` mounts. No DB migration, no creds.

## The currency sheet says which currency it is asking about (2026-09-13)

<!-- Fourth bullet of "Entry-currency-picker follow-ups" (2026-09-13): "`<CurrencySheet>` has no title, so both preferences open the same unlabelled list". -->

- [DONE: 2026-09-13] [est: 9000] **Settings opens one picker for two preferences and now names each of them** — the currency card asks two different questions — what totals are SHOWN in, and what new costs are TYPED in — through one mounted `<CurrencySheet>` with a hard-coded "Select currency" heading. The only thing on screen separating them was which row carried a checkmark: the last thing a screen-reader user reaches, and the thing a sighted user reads as "this is what I picked" rather than as "this is what you are picking".

  **The prop is optional, which is the other half of the rule.** `components/currency-input.tsx` and the collection screen open the picker from a labelled field, so the generic heading is still right there — a required title would have made them restate their own labels. The collection screen opens it in two MODES (defer the save to the modal, persist on the spot) and both are the same question about the same collection, which is what makes one heading correct there and wrong in settings.

  **One target chooses both the heading and the selected row**, so the two cannot disagree about which preference is open.

  **The heading is a header now.** A sheet that slides up over a screen gives a screen reader no landmark of its own; until the title carried `accessibilityRole="header"` it was one more line of text in a list of 160 currencies.

  Two new keys in all six locales (548 now), translated rather than inherited, and a case that no locale gives the two preferences the same words — a locale that rendered both as "Currency" would pass a declaration check and ship the original bug.

  6 new cases (9109 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9109 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. `npm run check:boot` mounts. No DB migration, no creds.

## A chunk that never arrives now says so (2026-09-13)

<!-- Third bullet of "Boot-check follow-ups" (2026-09-13), written by the round that shipped the check four hours earlier: "`Network.loadingFailed` is ignored, so a chunk that never answers reads as a slow one". -->

- [DONE: 2026-09-13] [est: 12000] **The boot check reports a request that DIED, not only one that answered 4xx** — `Network.loadingFailed` was handled by an empty case with a comment saying the URL was not on the event. It is not: the event carries a request id, and the URL is on the `requestWillBeSent` that preceded it. Without that map the whole class of "the chunk did not arrive" — DNS, refused, reset, a server that hung up — was invisible, and a locale chunk that was never served failed as an empty screen with no cause named.

  **The rule about which dead request is the bundle's problem is a decision, so it is `toBootRequestFailure` in `lib/bundle-boot.ts`.** A CANCELLED request is the page's own doing (an effect that unmounted, a fetch the app aborted) and would redden the check on ordinary React behaviour; `net::ERR_ABORTED` counts as cancelled whether or not Chromium set the flag. An UNATTRIBUTABLE one — an id with no sending event, so no URL — cannot be tested against the origin, and the requests that die without a response in a sandbox are exactly the ones this check ignores: Supabase, a font host, an analytics endpoint. The reason is now on the failure line, because "no response" alone reads as a slow chunk and `net::ERR_CONNECTION_REFUSED` does not.

  **Two related bugs in the same server, found by asking what a missing file would do.** The SPA fallback answered EVERY unresolved path with the shell, so a chunk missing from `dist/` came back as HTML with a `.js` content type and surfaced as a syntax error one step removed from the truth; `isSpaRouteRequest` now splits a route from a file by its last segment, and a missing file gets the 404 the deploy gives it. That 404 then exposed the second: Chromium asks every origin for `/favicon.ico` whether the page mentions one or not, this export ships none, and same-origin was the wrong boundary for "part of the artifact". `isBundleRequest` uses the base path the app was served on, so what sits above the deploy belongs to whoever owns the domain.

  **Proved against a real browser twice**, the same way the check itself was: a server that destroys the socket for the entry chunk — "FAIL — request failed: no response (net::ERR_EMPTY_RESPONSE)"; the entry chunk moved aside — "FAIL — request failed: HTTP 404", where before the change it was a syntax error. Restored, it mounts and prints the first line on screen.

  17 new cases (9103 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9103 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The app is loaded in a browser by a command now (2026-09-13)

<!-- Fourth bullet of "Lazy-locale follow-ups" (2026-09-13), carried through six groups: "the web app has never been loaded by anything that runs in CI". Half of it is closed; the half that is a decision about the gate is deliberately left open. -->

- [DONE: 2026-09-13] [est: 18000] **`npm run check:boot` loads the exported `dist/` in Chromium and says whether the tree mounted** — 9086 cases run against modules a test runner imported, `check-bundle-smoke` greps the chunks for watched keys and `check-bundle-size` weighs them, and none of them had ever loaded the artifact the deploy publishes. Two rounds today — the crash shell losing `Sentry.wrap` at the root, and four locale maps moving behind `import()` — were exactly the change where a green suite says nothing, and both were checked by driving a browser by hand.

  **A TOOL, and not a tenth gate leg, which is the half that stays open.** A gate that downloads a browser is a gate that goes red when a download does, and that is a decision about CI rather than a chore to finish. What this closes is the chore: nobody has to re-derive a static server, a base path and a CDP session again.

  **No dependency.** Chromium speaks the DevTools protocol over a WebSocket and Node 22 ships a WebSocket client, so the script spawns a headless browser, attaches, and reads back four things. Playwright would be a nicer API and a devDependency to audit, update and lock for a script that navigates to one URL.

  **The base path is READ from `app.json`, because the first hand-run got it wrong.** The export's asset URLs are absolute from `experiments.baseUrl`, so a server that hands `dist/` out at `/` produces a page that 500s on its own entry chunk — a failure that looks exactly like a broken bundle and is not one.

  **What counts as a failure is a decision, so it lives in `lib/bundle-boot.ts` rather than in a browser callback.** A page error, a console error, a same-origin request that 404s and an empty root all fail. A request to ANOTHER origin does not: Supabase, a font host and an analytics endpoint are unreachable from a sandbox, and a check that reddened on that would be a check about the network. Seventeen cases hold that rule, including the port-prefix collision (`:41730` starts with `:4173`, and the OS picks the port).

  **Proved against a broken tree the same way the round it would have caught was**: a one-line `throw` written over the entry chunk in a built `dist/` — "FAIL — 2 problem(s): page error … empty root", exit 1. Restored, it mounts and prints the first line on screen.

  17 new cases (9086 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9086 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The copy measure was counting doc blocks (2026-09-13)

<!-- Third bullet of "Lazy-locale follow-ups" (2026-09-13), written by the round that caused it two hours earlier. -->

- [DONE: 2026-09-13] [est: 9000] **"+5.8 KiB of that is translated copy" was prose about a refactor, and the copy had not moved at all** — `translationsFootprint` measured the whole translations SOURCE, which was the right thing while the six maps WERE the module: 3696 of its 3929 lines. They became six files this morning, each with a doc header explaining the move, and the measure counted every word of it as translation on the one line somebody reads when deciding whether to raise the budget.

  **The proof is that the number did not change.** Measured on the new basis — the locale maps' bodies, not the files around them — the maps at the commit that last moved the budget come to 236225 bytes, and they come to 236225 bytes today. Every kibibyte the line had reported since the split was headers and provider code. The gate prints no copy line now, which is the correct answer for a day that translated nothing.

  **Re-taking the head row's copy figure is the one edit `budget-snapshot.ts` argues against, so the reason is written into the field.** It is not a re-measurement for growth — that would reset the trend the module exists to accumulate — it is the same commit's copy in the unit the field always claimed: `sourceBytes` became `copyBytes`, and the name now describes what is counted.

  **The case that would have caught it is two lines**: a fixture with a 2000-character doc block above it measures the same as the fixture without one. The UTF-8 case was rewritten around the new basis too — two fixtures of the same shape and character count, one Latin and one Cyrillic — because "bigger than the file's length" stopped being a property once the file is not what is measured.

  2 new cases (9069 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9069 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The failure the last round only described (2026-09-13)

<!-- Not from the suggestions file: a review of the round pushed twenty minutes earlier, whose "a failed fetch keeps the language it had" was asserted from the source text rather than from the behaviour. -->

- [DONE: 2026-09-13] [est: 6000] **The locale loader's failure path is proved rather than described** — `lib/i18n/registry.ts` promised three things about a chunk that will not load: it resolves null instead of rejecting, it caches nothing, and it can be retried. All three were held by cases that matched the module's own source for a `.catch(` and a `.finally(` — which is an assertion about the text, and passes just as well when the promise chain around it is wrong.

  **`loaders` is module-private for a reason, so the seam is explicit.** A caller that could reach the record could also bypass the cache; `__setLocaleLoaderForTests` swaps one entry and `__resetLoadedLocalesForTests` puts the real one back, alongside the reset that was already there.

  **The report is asserted through the real `@/lib/sentry`, not a double.** With no SDK initialised a capture lands in the pre-init buffer from two rounds earlier, so `getSentryStatus().bufferedEvents` is the proof the failure was reported — the two halves of this morning meeting in one case, and one less mock than a spy would have cost.

  **The retry case is the one that would have caught a real bug.** A rejected promise left in the `inflight` map makes the first failure permanent, which is exactly what an offline user gets: one failed fetch and a language they can never switch to again until reload. Counting the attempts is what says the entry was cleared.

  3 new cases (9068 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9068 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The 208 KiB of copy nobody in the session reads (2026-09-13)

<!-- First bullet of "Startup-buffer follow-ups" (2026-09-13), decomposed into three sub-tasks at the top of this file and all three closed in one run. -->

- [DONE: 2026-09-13] [est: 45000] **1. The six locale maps are modules of their own** — they were 3696 of `lib/i18n-context.tsx`'s 3929 lines. The move itself is mechanical; the cost is in the readers, and there are a hundred of them. `readI18nSource()` is a CONCATENATION now — the provider, the types and the six maps — so every suite that asks "does each locale declare this key" keeps asking one string; `lib/i18n-source.ts`'s declaration matcher had to learn `export const`, without which 265 cases reported every map as renamed or deleted; and the copy measure had to read the set rather than stat one file, or the drift line would have reported the copy as having shrunk 300 KiB in a commit that only moved it. `lib/i18n-source-files.ts` names the files once, for the three readers that care.

- [DONE: 2026-09-13] [est: 30000] **2. be, pl, de and es arrive through `import()`; en and ru stay** — `ru` is what the provider starts in and `en` is what every missing key falls back to, so fetching either would put a round trip in front of the first paint for the common case. The other four are 208 KiB of copy nobody in a Russian session ever reads. `lib/i18n/registry.ts` is the cache and the loader: six literal specifiers, because Metro resolves a dynamic import statically and a computed one either bundles everything back together or resolves nothing.

  **A failed fetch keeps the language it had.** Offline, a stale service worker, a deploy mid-flight — the map is not there, and switching anyway renders English under a Polish setting. The loader answers null, the provider does not switch, tapping again retries, and the failure is reported with its own scope. The mount path is the same: a stored language whose chunk will not load leaves the app in the default rather than in a language whose strings are missing.

  **Proved in a browser, not only in a suite**: the exported `dist/` served on the deploy's base path in Chromium, with `collectables-language-v1` seeded to `pl`, fetches exactly two chunks — the entry one and `pl-<hash>.js` — and renders "KONTO COLLECTABLES". With nothing stored it fetches one.

- [DONE: 2026-09-13] [est: 12000] **3. Banked, and the floor learned to count** — entry chunk 2586.7 → 2399.4 KiB. The byte floor alone could not hold it: with four new chunks of 94, 33, 31 and 31 KiB, catching the smallest through a TOTAL would mean a floor within 31 KiB of the measurement, which goes red the first time a dependency bump makes an SDK smaller. So `LAZY_CHUNK_COUNT_FLOOR` is the other half — six chunks, a MINIMUM so splitting a feature in two is not a regression — and it is the half that scales, because a chunk that stops being lazy stops existing whatever it weighed. Byte floor 1000 → 1200 KiB, composition baseline re-taken: 1339.3 KiB of the bundle is now in chunks a page load does not fetch.

  **The budget did not move and did not need to**: the total went UP 4.6 KiB (chunk boilerplate), and 20.3 KiB of headroom is still inside the band the five raises were argued under. This round is the clearest case yet that the total is the wrong instrument — 187 KiB left the page load and the number the gate has watched for five days went the other way.

  27 new cases (9065 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9065 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The startup window the last round made longer (2026-09-13)

<!-- Second bullet of "Crash-shell follow-ups" (2026-09-13): "Sentry initialises after the diagnostics flag hydrates, and now that is a network fetch too — nothing queues events across it." Written by the round that widened the window, closed by the next one. -->

- [DONE: 2026-09-13] [est: 14000] **A crash before the SDK resolves is buffered now, and replayed only down the path that would have sent it live** — `captureException` has always returned early while `sdk` is null. That used to cost a few frames: the SDK was in the entry chunk and `initSentry` waited on a stored flag. An hour earlier the crash shell became a platform pair and took 874.4 KiB off the page load, so the wait is a network fetch of a chunk — and the crash most worth having is the one during startup.

  **THE QUEUE IS A CONSENT QUESTION BEFORE IT IS A DIAGNOSTICS ONE.** Nothing is replayed except from the success path of `runInit`, which is reached only when the user has not opted out AND the config is enabled. Every other path clears it: an opt-out flipped mid-fetch drops what was captured a second earlier, a config with no DSN drops rather than holds for a retry that cannot come, a failed chunk load drops because there is no SDK to replay into, and `shutdownSentry` drops so one session's errors never reach the next session's init. Six of the nine cases are those gates.

  **First events, not newest, and a cap of twenty.** A runaway render loop throws the same error thousands of times and the first few are the ones that say what broke. The overflow is counted rather than silent — `getSentryStatus()` answers `bufferedEvents` and `bufferOverflowed`, which is also how the suite sees a queue that never leaves the module.

  **Replayed through the same rate limiter**, because the limit is about the monthly quota and twenty events in one millisecond spend it exactly as twenty over a minute would. What is NOT replayed is the original timestamp: the SDK's capture API has nowhere to put one, so a buffered event is stamped when it is sent. That is written down in the module rather than discovered later from a timeline where every startup crash happened at the same instant.

  **The held-open loader is what makes "before init" a real state** rather than a mocked one: a promise the case releases, so the queue is observed between the capture and the SDK arriving.

  9 new cases (9038 in the suite), +0.4 KiB entry chunk. `npm run verify` green: typecheck, 20/20 guards, 9038 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. No DB migration, no creds.

## The lazy SDK that every page load was fetching anyway (2026-09-13)

<!-- First bullet of "Lazy-floor follow-ups" (2026-09-13): "Sentry is 645 KiB in the ENTRY chunk and now there is a floor to hold it once it moves." The floor landed an hour earlier; this is the round it was raised for. -->

- [DONE: 2026-09-13] [est: 26000] **874.4 KiB came off every web page load, and the SDK it moved was already behind a lazy `import()`** — `lib/sentry.ts` loads `@sentry/react-native` through `() => import(...)`, gated on `config.enabled`, under a comment about dev/test bundles never paying for it at startup. `app/_layout.tsx` then imported the same package twice at module scope, for `Sentry.wrap()` and `<ErrorBoundary>`, and an import is a dependency whatever the component does with it. `@sentry/core` 293 KiB, `@sentry/react-native` 195, replay 124, `@sentry/browser` 90, feedback 49, browser-utils, `@sentry/react`, replay-canvas — in the entry chunk, on a bundle whose own guard exists to catch exactly this.

  **THE TOTAL MOVED 1.1 KiB.** Entry chunk 3458.4 → 2585.0 KiB, a new lazy chunk of 874.5, and `check-bundle-size`'s sum went from 3821688 to 3822836 bytes. Six paragraphs of the budget's doc block argue about ~30 KiB of headroom; this round would have been invisible to all of them in both directions. The floor landed an hour earlier is what makes it a fact the gate holds: 1148.2 KiB lazy, 148.2 above the floor.

  **`components/crash-boundary.{tsx,web.tsx}`, the same shape as the gesture root.** Native keeps Sentry's own `ErrorBoundary` and `wrap()` — touch breadcrumbs and the app-start profiler are about the platform, and that bundle is downloaded once from a store. Web has a boundary of its own: a class, because `getDerivedStateFromError` has no hook equivalent, reporting through `@/lib/sentry`'s `captureException`, which adds the rate limit, the opt-out check and `scrubPII` that the SDK's boundary bypasses. A `Platform.OS` branch would not have worked — the import is the cost, so the split is at the file level.

  **What the web build gives up is written down rather than glossed.** `wrap()`'s touch breadcrumbs and start-up profiler are gone there. A crash before the SDK finishes loading is still unreported — it was before too, because `captureException` returns early while `sdk` is null and the SDK's boundary would have called a client `initSentry` had not created; what changes is the length of that window.

  **The PII guard refused the component stack and was right.** `analytics-pii.test.ts` rules that a `captureException` `extra` carries the app's own vocabulary because `scrubPII` never reads it. React's component stack is assembled text, and in a minified bundle the names in it are the minifier's — so the report carries the scope tag and no `extra`, rather than the allow-list widening for it.

  **`crashed` is a field, not `error !== null`.** `throw null` is legal, and a boundary keyed on the value would render the broken subtree again, throw again, and loop. One case.

  **Loaded in a real browser before pushing**, because no suite can answer "does the tree still mount without the root wrapper": the exported `dist/` served on the deploy's base path in Chromium renders the auth screen (3155 chars of markup), and the only chunk fetched at page load is the entry one — Sentry's stays unfetched until diagnostics initialise.

  Floor raised 200 → 1000 KiB in the same commit, with the bound the second lazy chunk introduced: the floor must sit above what would SURVIVE the smallest lazy chunk going eager (1148.2 − 273.7), or PostHog could go static under a floor that only catches Sentry. Composition baseline re-taken — every Sentry bucket now reads `(lazy)`.

  12 new cases (9028 in the suite). `npm run verify` green: typecheck, 20/20 guards, 9028 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. Budget unmoved at 3.67 MiB (24.8 KiB of headroom), no DB migration, no creds.

## The regression the total was blind to (2026-09-13)

<!-- Third bullet of "Lazy-marker follow-ups" (2026-09-13): "Nothing fails when a lazy package stops being lazy — the marker makes it visible in a report a human runs, and the budget only sees a total. A bucket that appears in the entry chunk having previously been lazy is a one-line check the baseline could support, and it is exactly the regression `lib/bundle-size.ts` was written to catch." -->

- [DONE: 2026-09-13] [est: 11000] **`check-bundle-size` fails when the bytes outside the entry chunk collapse — the one regression the budget exists for and could never see** — the gate is a SUM over `dist/_expo/static/js/web/*.js`. Move PostHog's 273.7 KiB out of the chunk nothing fetches until analytics initialises and into the one every page load does, and the sum changes by nothing. Six paragraphs of the budget's doc block argue about ~30 KiB of headroom against an SDK *arriving*; an SDK already in the tree that merely stops being deferred arrives at the browser for free, under budget, silently.

  **Proved before it was fixed, and against the real artifact.** Concatenating the lazy chunk onto the entry chunk in a built `dist/` and deleting it is exactly what a static `import "posthog-react-native"` produces. The gate's own output on that tree: `OK — 26.0 KiB of headroom left` from the budget, and `FAIL — only 0.0 KiB sits outside the entry chunk` from the new half. The suite holds the same fact as a case — same total, `overBudget` false, split failed — so the premise is asserted rather than described.

  **The floor is slack ON PURPOSE and that is the whole design.** The regression is a cliff (the lazy chunk collapses to nothing, because the only thing in it is the SDK), not a drift, so any floor below today's 273.7 KiB catches it on the commit that causes it. A tight floor would add nothing but false reds on a dependency bump that makes PostHog smaller, each costing a round to re-measure. 200 KiB leaves 73.7 KiB of room to shrink. No second treadmill: ordinary feature work lands in the entry chunk and never moves this number, which is why this is a floor and not the entry chunk's own budget.

  **A premise this check cannot verify fails loudly.** `isEntryChunk` knows Metro's `entry-<hash>.js` convention; the day the export stops following it, every chunk counts as lazy, the floor passes over a bundle that is entirely eager, and the guard is gone without a word. `missingEntryChunk` is its own failure with its own message, and a case that asserts the floor PASSES on that tree — which is the trap.

  **Two stale sentences found on the way.** `ci.yml` described a "4.59 MiB budget" and `scripts/check-bundle-size.ts` a "default 4.59 MiB"; the budget has been 3.67 since the gesture split. The budget's doc block had the same fault structurally — its first line is a header from when the block was one raise, and it still read "4.60 MiB" after the number went DOWN — so it now says where the live number is.

  16 new cases (9013 in the suite), no new gate leg — the split is reported inside `lint:bundle-size`, after the budget verdict and before the exit, so a build that is both over budget and newly eager says both things rather than costing a second CI run. `npm run verify` green: typecheck, 20/20 guards, 9013 tests, audit baseline, build, bundle secrets, bundle size, bundle smoke, ships-to-client. 0 bundle cost, no DB migration, no creds.

## The report says which chunk a package is in (2026-09-13)

<!-- Third bullet of "Gesture-split follow-ups" (2026-09-13) — a suggestion this round disproved rather than implemented. -->

- [DONE: 2026-09-13] [est: 7000] **A suggestion written this morning accused the lazy analytics loader of not being lazy, and it was the REPORT that was wrong** — "`@posthog/core` and `posthog-react-native` are 238 KiB and the app's own analytics module is lazy" read the combined view as if it were the entry chunk. It is not: `lib/analytics.ts` loads the SDK through `() => import("posthog-react-native")`, Metro puts it in a chunk of its own, and nothing fetches that chunk until analytics initialises. The guard works; the tool could not show it.

  **THE ONE DISTINCTION THE BUNDLE BUDGET IS ABOUT, and the report was blind to it.** `lib/bundle-size.ts` exists to catch an SDK that stops being lazy — that is the whole premise, written out in its first paragraph — and the combined section summed both chunks into one list where a lazily-loaded package and a page-load one look identical. The composition report was two rounds old and had already produced one false finding.

  **`(lazy)` on every bucket and module that lives outside the entry chunk**, plus a line saying how much of the total that is (273.8 KiB today, all of it PostHog). Marked rather than left out: lazy bytes are still in the bundle, still count against the budget, and the day one of them stops being lazy is the day this list has to be readable enough to notice.

  **`isEntryChunk` names the convention rather than guessing at it.** Metro names a web chunk after the module that asked for it, so the app's is `entry-<hash>.js` and everything else is somebody's `import()`. A path that merely contains the word is not the entry chunk, which is a case.

  **The per-chunk sections deliberately pass no marks.** Inside one chunk every byte is as fetched as every other, so a `(lazy)` there would be a claim about a file the section is not about.

  4 new cases (8996 in the suite). 0 new keys, 0 bundle cost. `npm run verify` green: 8996 tests, 20/20 guards, 26.0 KiB of headroom. No DB migration, no creds.

## A fifth of the web bundle was a root view (2026-09-13)

<!-- Second bullet of "Composition-baseline follow-ups" (2026-09-13), found by the report three rounds earlier and confirmed by the baseline written one round earlier. -->

- [DONE: 2026-09-13] [est: 21000] **964.2 KiB of gesture stack, carried by a provider that renders a `<div>`** — `app/_layout.tsx` imported `react-native-gesture-handler` for its side effects and wrapped the app in `GestureHandlerRootView`. Nothing on web needs either: `components/DraggableList.web.tsx` exists precisely because `react-native-draggable-flatlist` wants gesture-handler and reanimated worklets "that the web build does not carry", and the app's two other gestures are React Native's own `PanResponder`. The import was the cost regardless of what the component did with it.

  **THE TOOLING FOUND IT AND THEN PROVED THE FIX.** The composition report (three rounds earlier today) put `react-native-reanimated` at the top of the bucket list — 632.9 KiB, 13.5%, in a build with no reanimated feature in it — and the baseline (one round earlier) is what let the drift section answer in one line: `-963.9 KiB`, with reanimated, gesture-handler, worklets, hammerjs and semver all marked `(gone)`. 4696.0 → 3732.1 KiB.

  **A FILE PAIR, not a `Platform.OS` branch**: a conditional still imports the module and the import is the cost. `components/gesture-root.tsx` keeps the real root and the side-effect import for native, where draggable-flatlist IS the reorder list; `gesture-root.web.tsx` is a `<View>`. `check-platform-pairs` keeps their exports in step.

  **THE SAVING IS BANKED IN THE SAME COMMIT.** The budget goes DOWN for the first time — 4.60 MiB to 3.67 — because a gate 978 KiB above its bundle is not a loose guard, it is no guard: four rounds of ordinary work would have spent a fifth of it with nobody deciding anything. The new number keeps the rule every raise was argued under, 26.0 KiB of headroom, under Clarity's ~30 KiB.

  **A sign bug surfaced on the one build anybody would celebrate.** `formatBudgetTrendLine` hard-coded a `+` in front of the net, because every move in the history had been upward — so the first lowering would have printed `+-880.6 KiB`. It says "budget moved 5 times … (-880.6 KiB net)" now, and a case drives a falling history through it.

  **The web build was actually loaded, not just built.** Chromium against the exported `dist/` on the deploy's own base path: the app boots, renders its login screen, no page errors, no missing-provider crash from `react-native-screens` — which was the one risk that a build and a test suite could not answer.

  **Follow-up the same evening: the import went back to the top of the file.** `react-native-gesture-handler` asks to be imported before anything else in the entry file, and it WAS the first line here — the split moved the side effect one module down and left the import sitting among the component imports. On web that is nothing; on native it is the ordering the library documents, so `app/_layout.tsx` imports the split root first now and a case pins the position.

  **A sweep so it cannot come back quietly.** Exactly two files may import the gesture stack, both native halves of a platform pair, and a case asserts each has a web sibling — so an exemption that does nothing is red too. A screen that reaches for `GestureDetector` puts 964 KiB back into every page load, and nothing about that diff would look expensive.

  8 new cases in `__tests__/gesture-root-split.test.ts` (8997 in the suite); the budget history, the trend line and four pins moved with the lowering. 0 new keys. `npm run verify` green: 8992 tests, 20/20 guards, 26.0 KiB of headroom over a bundle that is 964 KiB smaller. No DB migration, no creds.

## The composition report gets a baseline (2026-09-13)

<!-- Fourth bullet of "Composition-report follow-ups" (2026-09-13), written the same day by the report that could say what the bundle IS and never what it became. -->

- [DONE: 2026-09-13] [est: 13000] **"The bundle grew 6.3 KiB" is a fact with no owner** — this morning's report can name every package in the bundle and had nothing to compare them to, so it answered "what is in here" and left "what changed" exactly where five budget raises found it. `COMPOSITION_BASELINE` is the other half: the report now ends with the buckets that moved, biggest move first, and the next raise can say *which* 6 KiB.

  **NOT a field on `BUDGET_SNAPSHOT`, and that is a decision rather than an oversight.** That module exists because half of a pair gets forgotten — but this has a different cadence: it can only be measured from a sourcemapped build, which no gate produces and CI never runs. Bound to the budget it would be either a raise that needs a special build or a field that is silently stale. It carries its own date and its own total, and the drift line names the measurement it speaks from.

  **Re-taking it is a copy, not a transcription.** `npm run bundle:composition -- --snapshot` prints exactly the three fields; a baseline with one fresh number among stale ones reports drift that never happened, which is the only way this file can lie.

  **A FLOOR, because a baseline that churns is one nobody re-takes.** Buckets under 1 KiB are a long tail of one-file packages that move on every dependency bump. The tail stays inside `totalBytes`, so the drift's own total is still the whole bundle and the unlisted moves show up as the gap between it and the rows.

  **The floor is also what stopped the first version being useless**, which the report said out loud on its first run: with the tail omitted from the baseline, every one of those packages read as `(new)` — thirty rows about dependencies that had been there for months. `compositionDelta` ignores the same tail on both sides now, and a bucket that crosses the floor is an arrival again.

  **Arrivals and departures are marked**, because they are the rows that explain themselves: `(new)` is a dependency somebody added and `(gone)` is the only evidence a removal worked. Both directions are reported for the same reason the copy-drift line had to learn — a round that gives bytes back should be visible.

  **The baseline's total is 0.3 KiB above the gate's**, and the module says why: a sourcemapped export appends a `sourceMappingURL` comment per chunk and the deploy strips them. A case holds the two within 20 KiB of each other, which is what catches a baseline taken from a different tree.

  12 new cases (8983 in the suite). 0 new keys, 0 bundle cost — nothing here is imported by a screen. `npm run verify` green: 8983 tests, 20/20 guards, 14.4 KiB of headroom. No DB migration, no creds.

## Four screens read one preference, and each read it once (2026-09-13)

<!-- First bullet of "Entry-currency-picker follow-ups" (2026-09-13), written the round before by the picker that made the staleness ordinary. -->

- [DONE: 2026-09-13] [est: 17000] **A `useState` and a `getEntryCurrency()` effect with an empty dep array, four times over** — settings, the add-item form, the wishlist sheet and the listing sheet all ask which currency a cost is typed in. A stack keeps the screen underneath MOUNTED, so a collector who opens the wishlist, walks to settings, changes the currency new costs are entered in and comes back finds the add sheet still offering the old one. Nothing is wrong on disk; the screen read the slot once, and that was that.

  **THE ROUND BEFORE THIS ONE IS WHAT MADE IT ORDINARY.** Until settings grew a picker it could only CLEAR the preference, and the forms wrote it themselves — so two mounted readers rarely disagreed. Giving the card a way to create the preference turned "the other screen is stale" into a normal Tuesday, which is the cost of that feature paid one round late.

  **`lib/entry-currency-store.ts` publishes a NUDGE, not a value.** An empty entry slot means "follow the display currency" — `getEntryCurrency` reads through — so a change to the DISPLAY currency moves the effective entry currency for everybody who has never chosen one. A store that published a value would have to know which of the two keys it is speaking for; a nudge lets the one function that knows the rule answer the question. `setUserPreferredCurrency` therefore publishes too.

  **Not the provider, for the reason the settings card was written under.** `CollectionsProvider` owns the display currency and every total re-renders on it; the entry currency re-renders no total.

  **A failed write says nothing.** The notification moved outside the `try`, with an early return in the catch: a slot that did not move has nothing to tell anybody, and a notification there would make four screens re-read and land on the value they already had — noise indistinguishable from a real change.

  **A broken listener cannot silence the rest.** Every listener runs and the failures are raised together as an `AggregateError` on the caller, which is the write that just happened. The first draft deferred the re-throw to a timer; that turns a bug in one screen into an uncaught exception with nothing in the stack to say where it came from, and the suite caught it by going red on exactly that.

  **`enterEditMode` keeps its one-off read, deliberately.** It is an event handler seeding an edit of an item that carries no currency of its own; a subscription there would re-seed a half-finished edit from a preference the user changed elsewhere.

  16 new cases in `__tests__/entry-currency-live.test.ts`, run against a spy store and a real render rather than read off the source (8971 in the suite); `removeItem` added to `helpers/spy-async-storage.ts`, which is the first case to need it. Six existing pins restated across five suites — every one of them describing the mount-time read. 0 new keys, +0.9 KiB. `npm run verify` green: 8971 tests, 20/20 guards, 14.4 KiB of headroom. No DB migration, no creds.

## The listing store is indexed, not re-scanned per row (2026-09-13)

<!-- Carried in five suggestion groups since the wishlist chip shipped: "findListingByItemId is a linear scan called once per visible wishlist row". -->

- [DONE: 2026-09-13] [est: 11000] **Eight thousand comparisons per render pass, and the shape was right the whole time** — the wishlist card asks the marketplace context whether this want has an open listing, which is the correct question for the screen: the by-id accessor is what the item screen asks, so the two cannot disagree about what "listed" means. What it COST was a linear scan of the whole listing store, once per rendered card. A forty-row window over two hundred listings is eight thousand comparisons, and it grows with both the collection and the marketplace.

  **THE FIX IS IN THE PROVIDER, so no screen had to move.** `listingByItemId` is a `useMemo` over the listings array and `findListingByItemId` is a `get` on it. The accessor's contract is unchanged, which is why the wishlist diff is empty and `app/item/[id].tsx` got the same improvement for free.

  **`openListingsByItemId` is the index `openListingsForItems` was already building inline.** The bulk paths built exactly this map to warn about a selection's standing offers and threw it away; the provider needed the same one per render. Two copies of a first-wins rule is the pair that drifts, so there is one builder and the array form derives from it.

  **FIRST WINS, preserved deliberately.** Nothing enforces one listing per item, and the scan answered with the first match — so a duplicate pair has to resolve the same way through the map, or the chip on a wishlist row and the warning in a bulk confirm would name different listings for the same want. A case holds it.

  **`isOpenListing` rather than `!soldAt` written out again**: what "still open" means is one decision, read the same way by the browse feed, the archive and this.

  6 new cases (8955 in the suite); the five suites that reached for the deleted scan now read the index. 0 new keys, no bundle growth. `npm run verify` green: 8955 tests, 20/20 guards, 15.3 KiB of headroom. No DB migration, no creds.

## The settings card can create the preference it explains (2026-09-13)

<!-- Carried in four suggestion groups since the key split: "nothing tells a user the entry currency exists until it already differs". -->

- [DONE: 2026-09-13] [est: 14000] **A card that explains a preference and can only clear it** — the key split gave the entry currency a slot of its own and the round after it gave the settings screen a line saying which currency new costs are typed in. Both were right and together they left one direction closed: the only way to SET a different entry currency was to open an add form and pick one there. Discoverable by accident, and the one screen that explains the preference could not change it in the direction that brings it into existence.

  **THE NOTICE BECAME THE WAY IN.** Tapping "New costs are entered in JPY · Change" opens the picker for the entry slot; "Use display currency" moved onto its own line beneath it, so the two outcomes are two buttons rather than one sentence with two meanings. When the currencies agree the notice would be a sentence about nothing, so a quiet one-liner takes its place — "Enter new costs in another currency", the affordance without the claim.

  **STILL NOT A SECOND PICKER, which is the previous round's argument surviving intact.** The card shows ONE value above ONE row; the entry preference is a line of text underneath that happens to open the same sheet. Two currency codes side by side would read as two equal settings, and one of them is a convenience the other is not.

  **One `<CurrencySheet>` and a target, not two mounts.** The list, the search box and the modal are identical either way, and a second picker in this file would be the duplication `currency-input-consistency` exists to stop. The target decides `selectedCode` and what `onSelect` writes, and a case pins the mount count at one.

  **The entry sheet opens on the EFFECTIVE currency** — `entryCurrency ?? displayCurrency`. `getEntryCurrency` reads through, so a null slot means "follow the display one"; a sheet opened with nothing selected would tell a user who has never chosen one that they have no entry currency, which is not what their forms do.

  **Three pins moved and one of them was a rule reversed.** `entry-currency-notice.test.ts` asserted settings NEVER calls `setEntryCurrency` — right about the reset, wrong about the screen — so it is now pinned to the reset's own handler instead of banned from the file; `currency-input-consistency` holds the display write to the provider and the entry write out of it; `settings-display-currency` follows the handler out of the JSX.

  8 new cases across three suites (8952 in the suite). 2 new keys in all six locales, coverage denominator 544 → 546. `npm run verify` green: 8952 tests, 20/20 guards, 15.3 KiB of headroom, +1.5 KiB this round. No DB migration, no creds.

## The bundle says what it is made of (2026-09-13)

<!-- The oldest carried suggestion in the file: "nothing has ever asked what the bundle is made OF", six rounds running and first in three of the last four groups. -->

- [DONE: 2026-09-13] [est: 19000] **Five budget raises argued from a total and a date, because that was everything anybody could measure** — `check-bundle-size` has said how big the bundle is since the first raise and `BUDGET_HISTORY` has said how fast it grew since the fifth, and neither can say what grew. So each raise was argued against the one before it in one sentence of prose, and the first round told to make the bundle SMALLER would have started with no data at all. The only reason anybody knows the currency-input adoption gave 1.1 KiB back is that somebody measured it by hand, before and after.

  **`lib/bundle-composition.ts` — attribution from the export's own sourcemap, which is the only thing that knows where a minified byte came from.** Each mapping segment claims the generated text from its column to the next segment's, and those bytes are charged to its source file. Sixty lines of arithmetic written out rather than a dependency: `source-map-explorer` ships nothing to users and is still a package to audit.

  **THE PARTS ADD UP TO THE WHOLE, which is what makes the report checkable.** A minifier emits text no module claims — the runtime preamble, the wrappers, every newline — and distributing it pro-rata would make every number slightly wrong and none of them wrong enough to notice. It is one named line instead, at 13.5%, and a case asserts the sum equals the chunk's own byte length.

  **UTF-8 bytes of the GENERATED text, and that is not pedantry.** Sourcemap columns are UTF-16 offsets, so the slice is taken by column and then encoded — the two units meet in the right order. Counting code units would under-report exactly the module the budget argues about most.

  **The first answer it gave is about the copy proxy.** `lib/i18n-context.tsx` is 314.0 KiB of the entry chunk against 238.1 KiB of source — the heaviest single module in the bundle, ahead of `react-dom`, because Metro escapes every non-ASCII character as `\uXXXX` and a Cyrillic letter that is two bytes in the source is SIX in the chunk. `translations-footprint.ts` has asked since it was written whether its delta tracks the bundle's; it does, and it under-states, which is the safe direction for a number used to decide whether to raise a budget.

  **A REPORT, not a gate.** No budget, no failure, not a tenth leg of `verify` — and it needs `npm run build:sourcemaps`, because `npm run build` exports without maps and the deploy strips them from the published artifact. The near-floor advice in `check-bundle-size` names it now, since that is the moment somebody is told to re-argue the budget and a tool nobody is pointed at is a tool only its author runs.

  **The buckets are derived from `SOURCE_DIRS`, not restated**, so a new top-level source directory cannot silently be reported as a dependency — the one direction of this report nobody would think to check. A case holds the two lists against each other, and `assets/` is the named exception with a reason: a bundled font is this repository's weight, and charging it to `expo-font` would credit a package with bytes nobody installed.

  **One bug, found by its own case.** expo-router's route table arrives as `/app?ctx=<hash>` — the whole directory, no file after it — and the first draft read it as a repo-root FILE, making a second bucket called `app` beside `app/`. One directory's bytes split across two rows of a report, visible only to a reader who adds the rows up.

  34 new cases in `__tests__/bundle-composition.test.ts` (8944 in the suite), plus the near-floor pointer pinned in `bundle-drift.test.ts` and two entries added to the `if`-shaped hole in `plural.test.ts` — both bounds checks, neither an inflection. 0 new keys, 0 bundle cost: nothing here is imported by a screen. `npm run verify` green: 8944 tests, 20/20 guards, 16.8 KiB of headroom. No DB migration, no creds.

## The currency sweeps reach the components directory (2026-09-12)

<!-- Second bullet of "Adoption follow-ups" (2026-09-12), a hole in the two sweeps the rounds before it built. -->

- [DONE: 2026-09-12] [est: 8000] **Two sweeps, a near-copy of one derivation, and the same hole in both** — `currency-input-consistency` and `create-currency-input-adoption` each walked `app/` only, because every currency surface anybody had thought about was a route. `components/edit-collection-modal.tsx` is not one: it renders the collection-currency button and asks its parent to open the sheet. So the single currency control under `components/` sat outside both rules by the WALK rather than by either rule.

  **It belongs on the excluded side, which is precisely why nobody noticed.** It picks a display currency and takes no amount, so every rule it was missing would have passed anyway. A gap that is currently harmless is still a gap, and it is the kind these sweeps exist to close — the same blind spot `LISTING_RULE_BY_ITEM_PATH` documents one domain across: a sweep written to the shape of the cases already known finds those and misses the rest.

  **A THIRD SIGNAL, because a currency control has three shapes here.** `<CurrencyInput>` is the cost-input component; `<CurrencySheet>` is the full picker, mounted by the display screens and by the component; and `onOpenCurrencySheet` is a control that opens a sheet its PARENT owns. The modal contains neither of the first two, so no amount of widening the walk alone would have found it.

  **`helpers/currency-surfaces.ts`, and the two lists are complements over one walk.** A file cannot fall out of both, which is exactly the state the modal was in — so adding a typed amount to a display picker moves it across and turns a case red rather than leaving it silently unruled. A case asserts the union is the whole of the six.

  **The two implementation files are exempt and NAMED rather than pattern-matched.** "Anything under `components/` with `currency` in the name" would also exempt the next screen-level component somebody puts there. `components/currency-input.tsx` mounts the sheet because it CONTAINS the picker, and asking it to read or write a preference would put the storage layer inside a presentational component that all three of its callers already handle.

  **The display-picker list is written out AND derived**, and a case asserts they match — so a new display picker is a red case rather than a silent omission, which is the failure mode the modal was in for its whole life.

  4 new cases and 1 new helper (`__tests__/helpers/currency-surfaces.ts`); 8910 in the suite. Both sweeps rewired onto it, and the adoption sweep's display-side case relaxed from "still mounts the sheet" to "does not take the cost-input control" — the modal mounts no sheet at all, which is the fact that made it invisible. 0 new keys, 0 bundle cost: the whole change is under `__tests__`. `npm run verify` green: 8910 tests, 20/20 guards, 16.9 KiB of headroom. No DB migration, no creds.

## The create form adopts the shared currency input (2026-09-12)

<!-- Third bullet of "Entry-notice follow-ups" (2026-09-12), carried through three groups since the consistency sweep had to work around it. -->

- [DONE: 2026-09-12] [est: 11000] **The last cost input driving a raw `<CurrencySheet>`** — `app/create.tsx` predates `<CurrencyInput>`, so it re-implemented three things the component already had (the sheet's open/query state, the MRU pin write, the error pill) and got a fourth wrong by omission.

  **THE OMISSION IS THE USER-FACING HALF.** The other two forms show a strip of currency chips — the codes you used most recently, one tap each, with a "…" chip opening the full picker. This screen showed the active CODE behind a chevron, so switching currency was two taps and a search field every time, on the form a new account meets first and the one place a collector adds items in bulk. It also passed `keyboardType="numeric"` where the component passes `"decimal-pad"`: the screen most likely to take a fractional price was the one without a separator key on iOS.

  **IT SHRANK THE BUNDLE, which is the rare direction.** Four styles, two imports, two pieces of state, a sheet mount and a `pinCurrency` call left, and nothing arrived — `<CurrencyInput>` was already in the bundle for the other two forms. Headroom went from 15.8 back up to 16.9 KiB, which is the first round today to give any back.

  **The pin moved rather than vanished.** `selectCurrency` inside the component writes it on both chip presses and sheet picks, so this form stopped writing it; doing both would double-pin one choice, which is harmless and is exactly the duplication being removed. A case reads the component to say the pin still happens.

  **A sweep so the next cost form cannot start the same way.** A screen that parses a typed amount reaches `<CurrencyInput>`; mounting `<CurrencySheet>` directly is for the DISPLAY pickers, which take no amount and want a plain list rather than a strip of recently-typed codes. Both sides are asserted, plus a case that the walk actually found the three cost screens.

  **The rule failed on its own rationale, for the third time in this repository and the second time today.** The new cost-row comment explains the adoption by naming the thing the screen stopped mounting, so a `doesNotMatch(/<CurrencySheet/)` read off raw source went red on correct code. Comment-stripped now, with the reason written next to it.

  17 new cases in `__tests__/create-currency-input-adoption.test.ts` (8906 in the suite). 0 new keys. 8 existing pins restated across 6 suites — every one of them pinning a piece of the hand-rolled row, including the a11y icon count (5 → 4) and the add-item screen family (42 → 41, DOWN: the screen stopped reading `currencyLabel` because the component labels its own control, and no key left the vocabulary — `components/edit-collection-modal.tsx` still reads it). `npm run verify` green: 8906 tests, 20/20 guards, 16.9 KiB of headroom. No DB migration, no creds.

## Settings says which currency new costs are entered in (2026-09-12)

<!-- First and second bullets of "Key-split follow-ups" (2026-09-12), both written the round before by the split that created them. -->

- [DONE: 2026-09-12] [est: 10000] **Splitting the currency key left a second preference no screen mentioned** — before the split one slot meant both things, so the settings card showing one currency was the whole truth. After it, a collector who displays in USD and types in JPY sees "USD" under Display currency and a JPY chip on every add form, with nothing anywhere connecting the two — a preference they can only change by opening a form they did not want to fill in.

  **Only when the two DIFFER.** "New costs are entered in USD" under a display currency of USD is a sentence about nothing, and this card's subject is the display currency; the entry one earns its space exactly when it is a surprise. That is also why it is a line under the existing row rather than a second picker: two currency controls in one card read as two equal settings, and one of them is a convenience the other is not.

  **CLEARING IS THE ACTION, and that is the second bullet answered rather than a mechanism chosen.** The empty slot already meant "follow the display currency" — `getEntryCurrency` reads through to the display one, which shipped as the migration for installations predating the split and never expires. That was an accident of how the migration was written and it is a perfectly good rule, so the button makes it a CHOICE. Writing today's display currency into the entry slot instead would pin the forms to it and silently stop following a later change, which is the opposite of what the button says.

  **The label says the outcome and not the mechanism.** "Use display currency", not "clear entry currency" — the slot is a thing the user has never been shown. A case holds every locale's label to words its own settings card already uses, matched on a five-character STEM rather than the whole word: ru says "Валюта отображения" in the title and "валюту отображения" in the label, and pl "Waluta" / "waluty", so a whole-word match is a case that passes in English and German and fails on correct Russian. The first draft did exactly that, and then failed again on the quotes `localeValuesOf` hands back with the value.

  **The entry currency stays out of the provider.** The provider owns the DISPLAY currency and every total re-renders on it; an entry currency added there would re-render all of them on a change that cannot affect any of them. Local state, re-read after the clear rather than assumed — the read falls through to the display currency, so the value after a clear is not null but whatever the fallback answers, and assuming either would leave the notice one render stale.

  **`locale-helpers.removeItem` is the first removeItem in `STORAGE_FAILURE_SITES`.** It reports for the same reason the writes do: a silent failure leaves the user typing in a currency they just asked the app to forget.

  16 new cases in `__tests__/entry-currency-notice.test.ts` (8889 in the suite). 2 new keys in all six locales; 1 existing pin restated (the coverage denominator, 542 → 544). `npm run verify` green: 8889 tests, 20/20 guards, 15.8 KiB of headroom, +4.9 KiB drift of which 1.6 KiB is copy. No DB migration, no creds.

## One currency key stops doing two jobs (2026-09-12)

<!-- First bullet of "Preference-key follow-ups" (2026-09-12), which the consistency sweep the round before it wrote down rather than fixed. -->

- [DONE: 2026-09-12] [est: 12000] **`collectables-currency-v1` answered both "what unit do I show your totals in" and "what unit did you last type an amount in"** — the settings screen wrote it as a display choice, three cost forms wrote it as a memory of what you typed. So noting a single want priced in yen re-denominated every collection total on the home screen: a global moved as a side effect of filling in one optional field, with nothing on either screen connecting the two acts.

  **They agree most of the time, which is why it lasted.** A collector who buys and thinks in one currency never sees a difference. And the consistency sweep written the round before asserted all three forms wrote the shared key and was RIGHT to — the forms agreeing with each other was the thing being checked, and they did. What no case had ever asked is whether the thing they agreed on was one fact.

  **`ENTRY_CURRENCY_KEY` + `getEntryCurrency` / `setEntryCurrency`.** Three forms moved over; `setUserPreferredCurrency` now means the display currency only and is written by the provider alone. A case asserts no screen writes it directly any more, and another that the provider seeds its display state from the DISPLAY slot — the direction that would be easy to get backwards and invisible when wrong, since totals would quietly re-denominate to whatever was last typed into a form.

  **THE FALLBACK IS THE MIGRATION, and it is done on read.** Every installation has a display currency and no entry currency, so `getEntryCurrency` reads through to the old slot when the new one is empty: a returning user's forms open exactly where they did before and the two diverge only once one is deliberately changed. Copying the value across at startup would write a key nobody has chosen a value for, which makes "never picked one" indistinguishable from "picked the same one" forever after. A storage throw on the new slot falls THROUGH to the fallback rather than returning early, because the old slot is a different key and may well be readable.

  **Both keys are wiped on sign-out**, for the reason the display half already was: a currency is a fact about the person who picked it, and leaving one behind means the next account on this device opens its first cost form on a stranger's unit.

  **The `\b` in the "writes only the entry slot" case is load-bearing.** `ENTRY_CURRENCY_KEY` contains `CURRENCY_KEY` as a substring and `_` is a word character, so a plain `includes` check failed on correct code — which is what the first draft did.

  15 new cases in `__tests__/entry-currency-key.test.ts` (8873 in the suite). 6 existing pins restated across 4 suites, every one of them naming the old helper: two said "the preferred-currency slot" when what they were claiming is that a form opens with the unit you last typed in, and that stopped being the same helper. 0 new keys. `npm run verify` green: 8873 tests, 20/20 guards, 17.8 KiB of headroom. No SQL migration — the AsyncStorage read-through is the whole of it. No creds.

## Every cost input remembers the currency you picked (2026-09-12)

<!-- Second bullet of "Want-currency follow-ups" (2026-09-12) — written the round before, from a misreading, and corrected here along with the code it describes. -->

- [DONE: 2026-09-12] [est: 8000] **THE ROUND BEFORE THIS ONE GOT IT WRONG, and that is the finding** — the wishlist's new cost row was written to READ the stored currency preference and deliberately not write it, arguing that `app/item/[id].tsx`'s edit form did the same. It does not. Both of that screen's currency inputs write it, as does `app/create.tsx`. So the argument rested on a file nobody re-read, and it shipped as a code comment and a passing test case asserting something false about another screen.

  **The behaviour was the smaller half of the mistake.** `setUserPreferredCurrency` is documented as "so a power user who picked JPY once doesn't have to re-pick on the next form", and every cost input seeds itself from it — so an input that reads without writing takes the convenience and does not pay for it: the collector who switched to JPY on the wishlist was asked again on the next screen. One line, now the same three lines the other three use.

  **`setCurrency` writes and hydration does not.** The effect that replaces the language default with the stored preference reaches for the raw setter, because writing back what was just read is a round-trip through storage that can only ever re-persist the value it came from. `app/item/[id].tsx`'s hydration effect does the same, which is where the shape came from.

  **THE SWEEP IS THE POINT.** `__tests__/currency-input-consistency.test.ts` derives every cost input from the tree and holds both halves of the rule on each: it opens with what you last picked, and picking one remembers it. Four call sites with a rule stated in three of them and nothing comparing them is exactly how the fourth got to differ, and a careful reader is what already failed.

  **A COST input is a screen that takes an amount AND names its unit, and the first draft of the derivation had only the second half.** It matched any screen driving a `<CurrencySheet>`, which swept in `app/settings.tsx` and `app/collection/[id].tsx` — both pick the currency a figure is DISPLAYED in, take no amount, and have no preference to remember. Demanding a write there would make choosing a display currency change what the next cost form opens with. `parseCurrencyValueDetailed` is the other half, and the two excluded screens get their own cases asserting they still take no typed amount — so a field added to either turns red rather than quietly joining the rules.

  **What the sweep deliberately does NOT claim** is that the preference should be one value. It is one AsyncStorage key doing two jobs — what a form opens with, and what totals are displayed in, which `app/settings.tsx` writes through `setDisplayCurrency`. So picking a currency for one item really does move the app's display currency. That is a real design question, it is now written down, and it is not this round's.

  12 new cases in `__tests__/currency-input-consistency.test.ts` (8859 in the suite). 1 case in `wishlist-cost-currency.test.ts` replaced — it asserted the false claim — and 1 added for the hydration setter. 0 new keys. `npm run verify` green: 8859 tests, 20/20 guards, 18.2 KiB of headroom. No DB migration, no creds.

## The wishlist says which currency a want is priced in (2026-09-12)

<!-- Second bullet of "Cost-sweep follow-ups" (2026-09-12), written the round before by the adoption that made the gap visible. -->

- [DONE: 2026-09-12] [est: 11000] **The last cost input in the app that could not say what its number meant** — `app/create.tsx` opens a currency sheet beside its amount and writes `costCurrency`; `app/item/[id].tsx` renders `<CurrencyInput>` in both its edit form and its listing sheet. The wishlist add sheet had a bare numeric field and wrote `cost` alone, so EVERY want ever added is stored currency-less and only reads correctly because `convertItemCost` treats a null as already-in-the-display-currency. A want noted while shopping abroad read as that many of the wrong unit, permanently, once the collector forgot which it was.

  **It was invisible until the round before it**, which is why it shipped this way for as long as the sheet has existed: the card rendered `{item.cost}`, a bare number with no unit after it, so a missing currency looked exactly like a present one. Adopting `<CostBadge>` made the row print a currency and made the absence of a stored one a thing you can see. Two rounds, and the second is the first one's finding rather than a new idea.

  **THE SAVE HAD TWO MORE FAILURES THE OTHER FORMS HAD ALREADY CLOSED, and the first is the worst thing in this file.** `cost ? Number(cost) : null` reads `"12,50"` as `NaN` — and a comma decimal is what four of this app's six locales type — so a Russian, Polish, German or Spanish collector's price VANISHED on save, silently, because `hasFiniteCost` reads NaN as no price at all. `"abc"` did the same. Both are the sanitizer's and the shared parser's job, and both arrive with the component.

  **`<CurrencyInput>` and not a fourth hand-rolled row.** It brings `sanitizeCurrencyInput` (which folds the comma before the parser ever sees it), the MRU chip strip and the `<ErrorPill>` — the three things this sheet was missing, each already written and each with its own suite. `parseCurrencyValueDetailed` is the gate on save, so the inline message a user reads and the analytics reason recorded for the same input cannot disagree.

  **"Empty" is not an error on this form.** A want with no price is the common case and the field is optional; the other two reasons are a number the user MEANT and the app could not read, which is worth stopping for rather than storing as nothing. Its own case, on both halves.

  **The app-wide display currency stays where it is.** `app/create.tsx` writes the preference back, so picking a currency for one item moves the whole app's display currency; `app/item/[id].tsx`'s edit form does not. A want is the wrong act to reset a global on — it is a thing you do not own, often priced in a currency you are only passing through. The preference is READ for the initial value and the MRU pin is still recorded by the component, so the code leads the strip next time either way. `currency` also survives `resetForm`, because somebody adding wants is usually adding several.

  16 new cases in `__tests__/wishlist-cost-currency.test.ts` (8847 in the suite). 0 new keys — `costPlaceholder` and the three `currencyError*` strings were already in six locales, which is what adopting a component instead of writing a row costs. 1 provider type widened (`DraftWishlistInput.costCurrency`), documented where it is declared. `npm run verify` green: 8847 tests, 20/20 guards, 18.3 KiB of headroom, +2.4 KiB drift of which 0.5 KiB is copy. No DB migration — `items.cost_currency` already exists and every other writer fills it. No creds.

## A want's price says what it is in (2026-09-12)

<!-- Third bullet of "Listed-want follow-ups" (2026-09-12), written the round before after the chip landed next to it in the same meta row. -->

- [DONE: 2026-09-12] [est: 8000] **`{item.cost}` — the last surface in the app that interpolated the raw field** — a want priced in euros and one priced in dollars both read "1500", with nothing in the row able to tell them apart, on the one screen whose whole job is comparing prices the collector has NOT paid yet.

  **The same defect the search row carried, found the same way.** Two sites, two rounds, and each was spotted by a person reading the file after an unrelated change — which is the argument for the sweep this round adds rather than for a third careful reader.

  **A want has no collection, and that is what makes the item mode correct here rather than merely available.** `addWishlistItem` writes `collectionId: ""`, so `getCollectionById` finds nothing and no collection currency override applies. That is the right answer and not a lucky fallback: a thing you do not own is not in a collection yet, so the viewer's display currency is the only currency the row can be about. Pinned on the provider, because it is the premise.

  **A missing `costCurrency` gains a currency without inventing a rate.** The wishlist add sheet has no currency picker, so every want stored through it has `costCurrency` unset — and `convertItemCost` treats that as already-in-target, the same fallback the collection totals use. Four cases walk the matrix: no stored currency, a real conversion, a conversion with no rate table, and the free/non-finite pair that `hasFiniteCost` exists for.

  **The gate stays, and the case says why.** `<CostBadge>` already renders nothing without a finite cost, so the surviving `hasFiniteCost` branch is about the CHIP: a pill background around an empty string reads as a rendering bug. Same branch-sharing the search row settled between its separator and its badge.

  **THE SWEEP IS THE HALF WORTH MORE THAN THE FIX.** No file under `app/` or `components/` may put a cost on screen except `components/cost-badge.tsx` itself, with an offender list printed as `file:line`. Comments are stripped first, because `components/search-overlay.tsx` explains its own fix by quoting the shape it replaced — a rule reading raw source would fail on its own rationale, which this repository has now done twice in two days. A companion case asserts the walk found the tree and that the pattern matches both fixed shapes, so the rule cannot pass vacuously.

  12 new cases in `__tests__/wishlist-cost-badge.test.ts` (8830 in the suite). 0 new keys, 0 existing pins restated. `npm run verify` green: 8830 tests, 20/20 guards, 18.8 KiB of headroom — the change cost 0.1 KiB, which is what adopting a component already in the bundle looks like. No DB migration, no creds.

## The wishlist says which wants are listed (2026-09-12)

<!-- Fifth bullet of "Budget-ceiling follow-ups" (2026-09-12), carried from "Wishlist-lock follow-ups" the round before; the fourth bullet of both groups is answered by the same chip. -->

- [DONE: 2026-09-12] [est: 9000] **The previous round shut the door and left the room full of people who were already inside** — `app/item/[id].tsx` refuses the "List on marketplace" CTA for a want now, and deliberately keeps the take-it-down path open, because wants listed BEFORE the lock still exist and a one-time sweep would withdraw offers somebody made on purpose. So the state is reachable, permanent until a human resolves it, and `app/wishlist.tsx` showed no sign of it at all.

  **The item screen grew three marketplace states and the wishlist row had none.** A collector with a standing offer for a thing they are still looking for could find it by opening the marketplace, or by promoting the want — which resolves the wrong half of the problem. The one screen whose entire job is managing wants was the one that could not see this.

  **It is also the only route from the wishlist to the item screen.** The card's two buttons are Promote and Delete; the take-it-down button lives on the item screen's `existingListing` branch, which sits ABOVE the wishlist refusal and is therefore still reachable for a want. Without the chip there was no way to press it from here, which is why the chip is a `Pressable` to `/item/{id}` and not to `/listing/{id}` — the buyer-facing screen is not where the offer comes down.

  **Zero new translation for the visible text.** The chip says `marketplaceListedForSale` / `marketplaceListedForTrade`, the item screen's own pill, already in six locales. The one new key is `wishlistListedHint`, the accessibility hint that says a press opens the screen where the listing can end — because the visible text states a STATE and a button has to say what it does. It is a wishlist key and not a marketplace one by the prefix rule, the same rule that put `marketplaceWishlistHint` on the other side of the same door.

  **Amber, not the item screen's `SUCCESS_GREEN` pill.** Green says the listing is working as intended, which is true of a holding and is exactly the claim this row cannot make: the app now refuses to create this state. Its own case, read off the comment-stripped source — the first draft failed on its own rationale, which names the token it is arguing against.

  **`promoteWishlistItem` stays EXEMPT and its reason was wrong.** It argued that a want has no listing to retire, which was never true and is the premise this round disproves. The verdict is unchanged for a better reason: promoting is the one transition that makes such a listing MORE honest, since the seller now owns the thing they were offering. Retiring it there would withdraw an offer at the moment it became keepable. Two cases hold the verdict and the new argument apart.

  20 new cases in `__tests__/wishlist-listed-chip.test.ts` (8818 in the suite). 1 new key in all six locales; the wishlist prefix family is 15. 3 existing pins restated — the a11y icon count for this screen (4 → 5), the family size, and the coverage denominator (541 → 542). `npm run verify` green: 8818 tests, 20/20 guards, 18.8 KiB of headroom, +1.9 KiB drift of which 0.5 KiB is copy. No DB migration, no creds.

## The fifth raise, argued from the tool instead of the paragraph (2026-09-12)

<!-- First bullet of "Wishlist-lock follow-ups" (2026-09-12), and the request `lib/bundle-size.ts`'s own doc block made when it took the fourth. -->

- [DONE: 2026-09-12] [est: 8000] **4.59 → 4.60 MiB, and the first raise this repo could argue from its own output** — the fourth ended with "somebody should measure KiB per round and split code from copy before the fifth". `BUDGET_HISTORY`, the drift line and the copy line shipped in the two rounds after it, so the fifth has the numbers the fourth asked for.

  **The answer is that there is no per-round rate to extrapolate.** Seven rounds today spent 16.3 KiB — 2.3 KiB each — while the single round before them spent 17.9 KiB on its own. What separates them is copy: 4.9 KiB of today's 16.3 is translated strings, and one round with four counted keys in six languages costs what seven without them do. "Rounds" was never the unit, which is a better answer than a rate would have been and is only visible because the split exists.

  **A RAISE HAS A CEILING, and that is what this one adds to the record.** A raise buys `budget − bundle` at the commit that measures it, and `bundle-size.test.ts` fails when that reaches Clarity's ~30 KiB — a budget with more headroom than the smallest SDK it must catch has stopped guarding. So 4.61 MiB was not available: it would have left 30.9 KiB. This buys 20.7 KiB, a smaller raise than any of the four before it, for a reason about the guard rather than about restraint. The budget can climb with the bundle indefinitely; what it cannot do is climb FASTER, so a round needing more than ~30 KiB of room has to make the bundle smaller instead.

  **Applied backwards over the whole history, not just the head.** Two new cases walk every row of `BUDGET_HISTORY` and hold both bounds on each — bought more than the re-argue floor, less than the smallest SDK. The existing bound case asked them of the live snapshot only, so "raise it by the usual 0.02 MiB" was a step nobody could have been stopped from taking retroactively wrongly. What the ladder can afford depends on where the bundle already is, and that is now a property of the record rather than of the paragraph describing it.

  **The measurement is this commit's**, so the drift and copy lines print nothing on the next build — "none of it spent", which is the zero case the drift line was written with.

  2 new cases in `__tests__/budget-snapshot.test.ts` (8798 in the suite). 1 new `BUDGET_HISTORY` row carrying both halves of the pair (4,802,272 bundle bytes and 243,824 copy bytes, taken together). 1 existing pin restated — the default-budget assertion, which names the figure. `npm run verify` green: 8798 tests, 20/20 guards, 20.7 KiB of headroom. No DB migration, no creds.

## A thing you do not have cannot be put up for sale (2026-09-12)

<!-- First bullet of "Live-filter follow-ups" (2026-09-12), found while fixing the collection delete's listing count the round before. -->

- [DONE: 2026-09-12] [est: 9000] **The archived round closed this door for things a collector USED to have and left it open for things they have never had** — `app/item/[id].tsx` rendered "List on marketplace" for a wishlist item exactly as for a holding, and `addListing` took the id.

  **`MarketplaceMode` is `"trade" | "sell"` and there is no "looking for".** Both modes are an offer to hand the item over, so listing a want promises a stranger a thing the seller is still trying to find — the archived mistake told forwards in time, with the same harm: an expectation in somebody else, who claims the listing and then waits. Its own case, on the type, because that is where the argument stops holding if a third mode is ever added.

  **The previous round found this from the other end.** The collection delete counted its open listings from a live-item-filtered list, so a listed want was a listing nothing would ever retire. Fixing the count made such a listing reachable by the code; this round asks whether it should have existed.

  **ONE lock, and the case says why rather than copying the archived shape.** The archived door has two because that state arrives from a realtime prompt that can fire while the listing sheet is open. `isWishlist` is written once, at creation — `promoteWishlistItem` only clears it — so a guard inside `handleSubmitListing` would be unreachable, and an unreachable guard is a claim nobody can check. The case counts the writers of `isWishlist: true` in the provider, so a demote path arriving later turns it red instead of leaving the second lock quietly missing.

  **Under the `existingListing` branch**, for the same reason the archived one is: taking a listing DOWN still works on a want, which is the direction that reduces the obligation rather than creating one — and it is where an item listed before this shipped lands.

  **`activeItem.isWishlist`, not `isLiveItem`.** One predicate cannot answer two branches: `isLiveItem` is also false for an archived item, and that case has its own sentence two lines up.

  **The hint points at the wishlist screen**, because the promote lives there ("Got it" → "Move to collection") and there is none on this screen to point at. A case asserts it says more than the terse archived refusal in every locale — a sentence that exists because there is no button left to press has to name the action that unblocks it.

  11 new cases in `__tests__/wishlist-not-listable.test.ts` (8796 in the suite). 1 new key in all six locales; the `marketplace*` prefix family is 77. 2 existing counts restated. `npm run verify` green: 8796 tests, 20/20 guards, **10.4 KiB of headroom** — above the 8 KiB floor and the closest any round has left it today; the next one should expect to argue a raise. No DB migration, no creds.

## Deleting a collection counts what it actually took (2026-09-12)

<!-- Second bullet of "Delete-outcome follow-ups" (2026-09-12), written the round before. The bug underneath it was found while reading the code the suggestion named. -->

- [DONE: 2026-09-12] [est: 10000] **The last silent resolution, and a listing the count could not see** — two findings in one callback, and the second is worth more.

  **`collectionOpenListings` was built from `allItems`, which is live-item filtered.** `getItemsForCollection` drops archived and wishlist rows; `deleteCollection` drops every row carrying the id and asks nothing about liveness. Nothing stops a wishlist item being listed — the item screen's marketplace block gates on `isArchived` and never on `isWishlist` — so a collection holding a listed want warned about one listing too few AND left that standing offer up after the item behind it was gone. The departure-path record was built for exactly this class and this is it one filter further down: the path was on the record, doing the right thing to the wrong list.

  **The fix is the provider's unfiltered `items`, aliased.** This screen already binds `items` to its own sorted, filtered render list, so the context's comes in as `unfilteredItems` — and the alias says which is which, because an alias that did not is how the two get swapped back. Two cases on values, not source: `isLiveItem` is false for a want, and the same listings through the two id lists differ by exactly the listed one.

  **The outcome toast is the smaller half.** `collectionDeleted` composed with `bulkListingsRemoved` — the fourth caller of the composer, and the same ordering rules as the other three: composed before the removals, because `collectionOpenListings` derives from items that stop existing on the next line; shown after the delete resolves, because a delete that threw should not claim to have happened; shown before the `router.replace`, so the message is queued while the screen is still mounted.

  **No item count in the outcome, deliberately.** The honest number is "every row carrying this id", and the screen's own arrays are each filtered for something — which is the bug this round just fixed, one line up. A count nobody can derive correctly on this screen is worse than no count, and the confirm already says the items go.

  **`collectionDeleted` is the collection PREFIX family's fifteenth key**, not the collection-detail screen's fifty-sixth: the prefix rule owns it, which is the rule that also keeps a key out of two families' counts.

  15 new cases in `__tests__/collection-delete-outcome.test.ts` (8785 in the suite). 1 new key in all six locales. 3 existing pins restated — the departure-path memo (it spelled out the live-filtered list, which is what made the regression visible the moment it was fixed), the prefix-family size and the coverage total. `npm run verify` green: 8785 tests, 20/20 guards, 12.4 KiB of headroom. No DB migration, no creds.

## Deleting one item finally says what it did (2026-09-12)

<!-- Third bullet of "Search-row follow-ups" (2026-09-12), carried since "Composed-outcome follow-ups" the same morning. -->

- [DONE: 2026-09-12] [est: 8000] **The one resolution in the family with no outcome at all** — the bulk delete counts rows and the listings that went with them, the bulk archive does the same, the single-item archive composes both halves, and the archive screen's own delete says `archiveDeleted`. `app/item/[id].tsx`'s delete removed the listing, removed the item and navigated away in silence — so a seller who deleted a listed item withdrew a standing offer from every buyer's device and was told nothing, on the one action in this family nothing can undo.

  **The confirm before it is why nobody noticed.** `deleteItemListedText` warns that the listing goes too, which reads like the app has this covered — but a warning is "this will happen" and an outcome is "this happened", and only the first existed. Its own case, on both halves of the pair.

  **No new key, because the sentence already exists.** `archiveDeleted` is "Item deleted" in all six locales and `app/archive.tsx` says it for exactly this act — one item, permanently, from the other screen. A second key would have been that sentence translated twice; a case pins both call sites. The item-detail family went from 50 keys to 51 and cost no translation, which is the kind of growth a per-key diff reports as work.

  **The third caller of `composeOutcome`, and the one that exercises the nullable clause.** Most deletes are of unlisted items, so the common path is the leading clause alone with no trailing separator — the property the hand-written join could not have.

  **`retiring` is now the same local both paths use.** The delete asked `isOpenListing` inline because it needed the answer once; the outcome needs it too, and a second call between the compose and the removal would be a second read of a store recomputed on every render. `item-departure-paths.test.ts`'s pin counts two of each shape now rather than matching one spelling twice — which is what it was claiming.

  **Ordering is two claims, not one.** The outcome is composed BEFORE anything is removed, because `existingListing` and `activeItem` stop describing anything two lines below; the toast is shown AFTER the delete resolves, because a delete that threw should not claim to have happened, and before the `router.replace`, so the message is queued while this screen is still mounted rather than depending on where the toast provider sits.

  11 new cases in `__tests__/delete-outcome-toast.test.ts` (8770 in the suite). 0 new keys. 3 existing pins restated across 3 suites — the departure-path count, the delete path's rule-asking shape, and the item-detail family size. `npm run verify` green: 8770 tests, 20/20 guards, 13.0 KiB of headroom. No DB migration, no creds.

## A search result says what the price is in (2026-09-12)

<!-- Found by the codebase scan that followed "Outcome-separator follow-ups" (2026-09-12): the separator sweep walked into the one inline `· ` that was a bug rather than a style. -->

- [DONE: 2026-09-12] [est: 7000] **`{item.cost ? ` · ${item.cost}` : ""}` — three defects in one expression, each already fixed somewhere else in the app** — the search overlay's result row, which is the surface a collector reaches when they know what they own and not where it is.

  **A bare number with no currency.** "1500" for an item priced in euros beside "1500" for one priced in dollars, with nothing in the row able to tell them apart — the defect the export round fixed in the PDF, on a surface read far more often than any document, and the only in-app cost that was still rendered by interpolating the field.

  **A truthiness check where the app has a gate.** `hasFiniteCost` exists because six call sites were asking a weaker question, and its first documented case is that zero and a negative are prices. `item.cost ?` hid a free item's price and printed `Infinity` for a non-finite one — falsy and truthy landing on opposite sides of the same bug. Three cases assert the two answers differ, in both directions.

  **No conversion.** Every other cost surface converts into the viewer's display currency (or the collection's override) and marks a real conversion with `≈`. `<CostBadge>` is that rendering, memoised, with the stored original on long-press and in the accessibility label — and it renders nothing without a finite cost, so the gate on the screen decides only the separator.

  **The separator and the badge share one branch**, which is the one property of the old expression worth keeping: a `·` on a row with no cost after it reads as a truncated line. Its own case, asserting the ordering inside the conditional rather than the presence of both.

  **The `· ` stays an inline literal here, deliberately.** This morning's `OUTCOME_SEPARATOR` punctuates two clauses of one outcome; this is metadata in a one-line row. They spell the same glyph and are not obviously the same fact, and collapsing them on that resemblance is how a constant ends up meaning "any dot".

  8 new cases in `__tests__/search-result-cost.test.ts` (8760 in the suite). 0 new keys. `npm run verify` green: 8760 tests, 20/20 guards, 13.1 KiB of headroom. No DB migration, no creds.

## The punctuation between two outcomes stops being a translation (2026-09-12)

<!-- First bullet of "Composed-outcome follow-ups" (2026-09-12), written the same round the third caller of that join appeared. -->

- [DONE: 2026-09-12] [est: 9000] **`lib/outcome-message.ts` — one punctuation mark split across two files and six translations** — `bulkListingsRemoved` began with a literal `· ` in every locale, and the space in front of it was a leading space inside the string the screen conditionally returned: `` const outcome = n > 0 ? ` ${t(...)}` : ""; ``. Three toasts joined their two halves that way.

  **It fails in both directions and neither is visible to a coverage count.** A translator who drops the glyph runs two sentences together — it is not a word, and nothing in the value says it is load-bearing; a caller who forgets the leading space joins them with the glyph alone. The key is DECLARED either way, which is the same boundary `lib/plural.ts` documents at length: translated, and translated wrongly.

  **The clauses are sentences and the punctuation between them is layout**, so the punctuation is code. Six values say what happened; `OUTCOME_SEPARATOR` says how two of them sit beside each other.

  **`composeOutcome` takes strings and not keys.** Every clause is already counted, agreed and declined by the time it arrives — `bulkListingsRemoved` runs through `slavicPlural` in three locales — and a composer that took keys would have to take their params too and would be a second module that knows how translation works. This one knows about punctuation, imports nothing, and has a case asserting both.

  **An absent clause is dropped, which is what the hand-written join could not do.** `composeOutcome(a)`, `composeOutcome(a, null)` and `composeOutcome(a, "   ")` are all `a`, with no trailing separator and no trailing space — so `listedOutcome` returns `null` rather than encoding "nothing happened" as a string that begins with a space. A whitespace-only clause is dropped too: a separator in front of nothing reads as a rendering bug.

  **Variadic, because that is the shape the composing argument rests on.** A third thing an act also did is another clause here and a fourth hand-written sentence otherwise; a case joins three and another drops one out of the middle.

  18 new cases in `__tests__/compose-outcome.test.ts` (8752 in the suite). 0 new keys; 6 values edited. 2 existing pins restated — both spelled the old join out as a regex, which is exactly the shape that goes red when the join moves and is why it was written that way. `npm run verify` green: 8752 tests, 20/20 guards, 13.2 KiB of headroom. No DB migration, no creds.

## One sentence per thing the archive did (2026-09-12)

<!-- Third bullet of "Archive-row follow-ups" (2026-09-12), carried through four rounds since "Bulk-outcome follow-ups". -->

- [DONE: 2026-09-12] [est: 8000] **`archiveActionDoneListingRemoved` — a purpose-written sentence for an outcome the bulk path had learned to compose** — the item screen said "Item archived and listing removed" in six languages; the bulk bar said `itemsArchived` and then `bulkListingsRemoved`, one clause per thing the act did. Two shapes for one distinction, and the single-item key was simply the older one.

  **The composed shape is the one that scales.** A third thing an archive also did (a reminder cancelled, a share revoked) is another clause in the bulk path and a fourth hand-written sentence in the other. `${t("archiveActionDone")} ${t("bulkListingsRemoved", { count: 1 })}` reads "Item archived · 1 listing taken down" — a count of one where a purpose-written sentence would not have had a number, which is the cost, and the reason the suggestion carried four rounds as a decision rather than a chore.

  **No `count > 0` gate on this branch, unlike the bulk one.** `listedOutcome()` on `app/collection/[id].tsx` exists because a selection may contain no listings; this branch only runs when `isOpenListing(existingListing)` is true, so there is exactly one and the conditional would be dead code arguing that it might not be.

  **The removal is asserted in both directions.** `lint:orphan-i18n` catches a key nothing reads; `assertNoLocaleDeclares` catches the other half — a removal that reaches `en` and misses `ru` is invisible at runtime, because the inheriting locales serve English either way. The claim the retired key carried is restated as a property of the composition: every locale's suffix is a sentence rather than decoration, so the listed toast still says materially more than the plain one.

  **The item-detail family did not shrink.** `i18n-complete-families.test.ts` counts 50 before and after — a key left the base vocabulary and the screen still renders a sentence about the listing, now the one the bulk bar already paid for. That is what a screen family counts and a per-key diff would not show, and it is the entry that says why the total went DOWN for the first time in the coverage log.

  2 new cases and 2 restated in `__tests__/archive-retires-listing.test.ts` (8734 in the suite). 6 keys removed, 0 added; 542 keys × 6 locales. `npm run verify` green: 8734 tests, 20/20 guards, 13.6 KiB of headroom — 0.7 KiB back, which is what six sentences weigh. No DB migration, no creds.

## The bulk bar cannot be handed nothing to do (2026-09-12)

<!-- Second bullet of "Archive-row follow-ups" (2026-09-12), carried through four rounds since "Bulk-outcome follow-ups". -->

- [DONE: 2026-09-12] [est: 7000] **`<BulkBar count={n} onCancel={f} />` type-checked and rendered a selection count over an empty button row** — a selection mode a user can enter and cannot resolve. The four actions became optional the day the archive screen mounted the second copy of the bar, which is what lets that screen state its refusal of a "delete all" by omission; the same change made the one combination no screen could mean a legal call.

  **A mapped union, not four written-out variants and not a discriminated one.** `AtLeastOneAction` is `{ [K in keyof BulkBarActions]-?: Required<Pick<BulkBarActions, K>> & Omit<BulkBarActions, K> }[keyof BulkBarActions]` — one member per action, generated. A discriminated union would have to name a variant per screen, which is a second place to edit for a third screen offering Move and Archive and no Delete; this names none. The suggestion that carried this asked for "a case asserting at least one action" as the cheap half, and the union is the same size in lines and is checked on every build instead of on every run.

  **The enforcement is five `@ts-expect-error` declarations and they fail in BOTH directions.** `tsconfig.json` includes `**/*.ts`, so `__tests__` is type-checked: each suppression is an assertion tsc makes, and if the union ever stops rejecting an action-less bar the directive becomes unused and the typecheck leg goes red on the comment itself. That is the half a runtime case cannot do, because the bad call never renders in a suite.

  **`onRestore: undefined` is rejected too**, which is the shape a screen reaches by spreading a half-built props object rather than by choosing it — and the one an `onRestore?: () => void` alone would have accepted.

  **The source half is a sweep, because a type says what is expressible and not what the screens pass.** Both call sites are matched on `<BulkBar\s+count=` rather than a bare tag: both files also name `<BulkBar>` in prose, and the first draft's lazy match ran from a comment to the next self-closing tag and found four call sites where there are two.

  9 new cases in `__tests__/bulk-bar-actions.test.ts` (8734 in the suite). 0 new keys, 0 bundle cost — the whole change is erased at compile time. 1 existing doc block restated: "every action is optional and Cancel is not" was the whole truth for a day, and a reader who stopped there would write the call the union now rejects. `npm run verify` green: 8734 tests, 20/20 guards, 12.9 KiB of headroom. No DB migration, no creds.

## The archive row is a memoized component (2026-09-12)

<!-- First bullet of "Bulk-restore follow-ups" (2026-09-12), written the same day the selection mode that made it matter shipped. -->

- [DONE: 2026-09-12] [est: 7000] **The screen built a `selectedById` map and then rendered inline** — which is the map without the latch that makes it worth building. The collection screen pairs its map with `<SelectableItemRow>`, a `React.memo` row, so a toggle re-renders two rows; the archive screen re-rendered every visible one, on a window of forty rows that each mount a remote image.

  **`<ArchiveRow>` takes a `collectionName` string, not the collection**, and reads the theme and `t` itself rather than being handed them — so the props that decide a re-render are the item and its two booleans. A theme object passed down would be a fresh reference on every parent commit and the memo would skip nothing, which is the failure mode that makes a memo look like it works.

  **`ARCHIVE_ROW_HEIGHT` moved with the markup and the screen imports it.** The number is a fact about the row's own styles, and "declared once" survives the move only if the list reads the component's copy — a constant left behind in the screen is exactly the drift the original constant was extracted to prevent.

  **6 existing pins restated in `archive-screen.test.ts` and 2 in `bulk-restore.test.ts`**, each split along the same line: what a row RENDERS is read from the component, what the screen DOES from the screen. The Restore case now reads both halves — the row calls `onRestore(item.id)`, the screen passes `handleRestore` — because the split is the claim.

  0 new cases, 0 new keys; `components/archive-row.tsx` is named by a suite through the pins that moved onto it. `npm run verify` green: 8725 tests, 20/20 guards, 12.9 KiB of headroom. No DB migration, no creds.

## The archive screen restores a selection (2026-09-12)

<!-- Second bullet of "Bulk-outcome follow-ups" (2026-09-12), carried in every group since the bulk-archive round and the oldest unactioned item in that file. -->

- [DONE: 2026-09-12] [est: 9000] **One gesture puts thirty rows in the archive and thirty taps got them back** — `archiveItems` shipped on the bulk bar and the archive screen took its rows out one at a time. That is not a missing convenience: the whole argument for archiving over deleting is that the way back is cheap, and it stops being true at exactly the scale the other direction was built for.

  **`unarchiveItems` is the mirror, minus the clock.** `archiveItems` stamps ONE timestamp for a selection so a single act does not scatter across the top of the archive list; this clears a field, so there is nothing to stamp and nothing to order — every row it touches leaves the list. It clears with `null` and not `undefined`, which is the whole of `unarchiveItem`'s doc block at thirty times the size: `updateRemoteItem` writes a field only when the key is PRESENT in `updates`, so an `undefined` would clear thirty local flags, leave thirty cloud rows archived, and let the next sync put the selection straight back in the trash.

  **The interesting part of the screen half is what it does NOT pass.** `app/archive.tsx` has refused a "delete all" since it was written — a place people reach after a mistake is the worst place for a one-tap way to make a bigger one — and mounting a bar that can render one is the first thing that could have quietly retired that argument. So `<BulkBar>`'s actions became optional and the archive screen states its answer by omission: `onRestore`, `onCancel`, nothing else, with a case failing on any of the other three appearing at that call site.

  **`rowSelected` changes two colours and no geometry.** `getItemLayout` promises every row is exactly `ROW_HEIGHT`; a selection style that added a 2px border would make that promise false for precisely the rows a user has touched, which reads as a scroll bug and only after you interact. Its own case, listing the properties that would grow a row.

  **No confirm in front of the bulk restore**, for the reason the single-row restore has none: nothing it does is destructive, and the way to undo it is the action that filled this screen. The destructive resolution stays one row at a time behind the confirm it has always had.

  24 new cases in `__tests__/bulk-restore.test.ts` (8725 in the suite); 1 new key in all six locales. 3 existing pins restated: the archive screen's context destructure (now a membership check, since `unarchiveItems` joined a line that wraps) and its footer (a fragment now, so the case reads the button rather than the prop) — and `bulk-archive.test.ts`'s five body slices, which were `indexOf("archiveItems: async")` and would have silently grown to cover `unarchiveItems`, a name that CONTAINS that string. `npm run verify` green: 8725 tests, 20/20 guards, 13.6 KiB of headroom, +13.1 KiB drift of which +4.1 KiB is copy. No DB migration, no creds.

## The bulk toasts count the listings they withdrew (2026-09-12)

<!-- First bullet of "Archive-polish follow-ups" (2026-09-12), carried since the bulk-archive round. -->

- [DONE: 2026-09-12] [est: 7000] **Both confirms counted listings and both outcome toasts counted only rows** — so a seller who archived thirty items and took four listings down was told about the thirty. That is the half they least need to have seen, because the other half is the one other people can see: four standing offers withdrawn from every buyer's device, reported as nothing.

  **`bulkListingsRemoved`, composed onto the existing counted message** rather than a second pair of keys — the same shape the confirms already use, and one string instead of four. A case asserts it is a different sentence from `bulkListedWarning` in every locale: one is a confirm asking about something that has not happened and the other reports what did, and a locale that reused the warning would tell a seller their listings "will be taken down" after they already were.

  **Read before the selection is cleared.** `selectedOpenListings` derives from `selectedIds` and `exitSelectionMode` empties it two lines below the toast, so the outcome is composed into a local after the await and before the exit. Its own case, on both bulk paths, asserting the ordering rather than the presence.

  4 new cases (33 in the suite); 1 new key in all six locales, declined in the three Slavic ones. 2 existing counts restated. `npm run verify` green: 8698 tests, 20/20 guards, 17.1 KiB of headroom, +9.7 KiB drift of which +3.5 KiB is copy. No DB migration, no creds.

## The archive screen finishes its list (2026-09-12)

<!-- Third and fourth bullets of "Budget-history follow-ups" / "Archive-list follow-ups" (2026-09-12): the missing `getItemLayout` and the missing pull-to-refresh. -->

- [DONE: 2026-09-12] [est: 6000] **Two things every other list screen has** — `getItemLayout`, without which FlatList measures every row as it mounts and cannot skip to an offset; and pull-to-refresh, which this was the only list screen without, so reconciling an archive across two devices meant leaving the screen and coming back.

  **`ROW_HEIGHT` is declared once and read by both the stylesheet and the layout.** A `getItemLayout` whose number disagrees with the style places rows where they are not, which looks like a scroll bug and is a constant. A 56px thumbnail in 12px of padding, and the text column is shorter than the thumb — a 15px title and two 12px meta lines come to roughly 50 — so the thumb sets the height and nothing in the row can grow past it.

  **Which is the property that makes a fixed layout honest**, and it holds because both text fields are capped at one line and the date is a ten-character ISO prefix. Its own case, counting the caps: a row whose text could wrap would need the measuring back.

  3 new cases (41 in the suite). 0 new keys. 1 existing pin restated — `refresh` joined the context destructure, which is not what that case was claiming. `npm run verify` green: 8694 tests, 20/20 guards, 18.4 KiB of headroom. No DB migration, no creds.

## The budget keeps its own history (2026-09-12)

<!-- Fifth bullet of "Page-size follow-ups" (2026-09-12), carried eight times and the oldest unactioned item in that file. -->

- [DONE: 2026-09-12] [est: 10000] **Four raises, each argued against the one before it, in prose** — `lib/bundle-size.ts`'s doc block is four paragraphs doing a table's job, and a reader wanting the RATE had to reconstruct it by reading all four. Every build now prints `budget raised 3 times since 2026-09-10 (+61.4 KiB in total)`.

  **`BUDGET_HISTORY`, newest first, and the head IS the snapshot.** A raise adds a row rather than editing one, which is what makes the trend accumulate instead of being overwritten by the measurement meant to extend it. Each row carries the budget it justified — without that a row is half an argument, since how much was spent is only meaningful against how much was bought — plus the date and one line on what spent it, with a case rejecting a `because` under forty characters.

  **`translationsBytes` is `null` on the three rows that predate the copy measure.** The honest answer for a number nobody took, and better than back-filling it from today's file, which would describe a translations module nine keys larger. `formatCopyDriftLine` returns nothing for a null baseline rather than subtracting from zero, which would report the whole translations module as one round's growth.

  **The trend line says nothing for a one-row history**, because "raised once" is not a trend, and it points at the rows rather than restating them — four `because` lines in a build log would be the prose the history replaced, printed on every commit.

  **Two rules the repo already had, applied.** `plural(raises, "time", "times")` rather than a hand-rolled ternary, and `lib/bundle-size.ts` named in `lib/plural.ts`'s importer paragraph — which is worth the sentence it got: a build-log line nobody translates is still a sentence with a count in it, and the alternative is a second rule for the places the first was judged not to matter. The `if (history.length < 2)` guard is the FOURTH entry in `plural.test.ts`'s documented if-shaped hole, and the same kind as the other three: a bounds check on how many rows a claim needs, in a module that does import the rule for its actual inflection.

  11 new cases (17 in `budget-snapshot.test.ts`). 3 existing pins restated. `npm run verify` green: 8691 tests, 20/20 guards, 18.7 KiB of headroom. No DB migration, no creds.

## One page size stops doing five jobs (2026-09-12)

<!-- Second bullet of "Archive-list follow-ups" (2026-09-12), carried since the load-more rounds. -->

- [DONE: 2026-09-12] [est: 8000] **`DEFAULT_CHUNK_PAGE_SIZE` was 20 and every one of the five windows took it** — across rows that differ by an order of magnitude in what they mount: a collection card with a full-bleed remote cover against an archive row with a 56px thumbnail and two lines of text. The export existed to be overridden and nothing overrode it for as long as the hook existed.

  **`CHUNK_PAGE_SIZE_CARDS = 12` and `CHUNK_PAGE_SIZE_ROWS = 40`** — the two shapes the app actually has. Twelve collection cards is twelve remote images decoded before the user has scrolled anywhere, which is the cost the hook exists to bound and the one the shared default was never chosen against; forty compact rows stops a list of thirty asking for a Load-more press for no reason.

  **The numbers are argued, not measured, and both the module and the suite say so.** Nobody has profiled either row on a device; 12 and 40 come from what the rows CONTAIN. What is defensible is the split — one number could not be right for both — and the shape that makes a measured answer possible later. A case asserts that disclaimer is still in the doc block, because a reader who takes them for profiler output would be reluctant to change them, which is the opposite of the point.

  **The default stays, bracketed by the two.** A sixth window whose row shape nobody has decided about should get a working default rather than a guess dressed as a decision — and the bracketing is what says one number could not have been right.

  12 new cases in `__tests__/chunk-page-sizes.test.ts`, including one that fails on any future window taking the bare default. 8 existing pins restated across 6 suites: each spelled `useChunkedList(x)` with no size, which was never what any of them was claiming. `npm run verify` green: 8680 tests, 20/20 guards, 18.7 KiB of headroom. No DB migration, no creds.

## The archive screen recycles its rows (2026-09-12)

<!-- Fifth bullet of "Approximate-marker follow-ups" (2026-09-12), carried through six rounds since "Archive-window follow-ups". -->

- [DONE: 2026-09-12] [est: 7000] **The window bounded the start and nothing bounded the ceiling** — the screen mapped `visibleItems` into a `<View>` inside a `<ScrollView>`, so the chunked window capped how many rows were MOUNTED FIRST and nothing recycled them: five presses of Load more held a hundred rows and a hundred remote cover photos. On the one list in the app with no ceiling of its own — nothing prunes an archive, every sale adds to it, and one gesture on the bulk bar can put thirty rows in it.

  **`<Screen scroll={false}>` with the list owning the scroll**, the pattern `lib/flat-list-styles.ts` describes and collection detail established. The hero is the header, the empty state is `ListEmptyComponent` — the same claim as the `length === 0` ternary it replaces, made by the thing that actually knows whether the list is empty — and the Load-more button is the footer.

  **The window stays, and so do both ways of growing it.** `onEndReached` for a scroll that reaches the end, `<LoadMoreButton>` for the platforms and gestures where that never fires, and `remaining > 0` as the single gate for both.

  4 new/restated cases (38 in the suite). 0 new keys. `npm run verify` green: 8668 tests, 20/20 guards, 19.0 KiB of headroom — the change cost 0.3 KiB, which is what swapping a mapped View for a FlatList already in the bundle looks like. No DB migration, no creds.

## The app says what the PDF was taught to say (2026-09-12)

<!-- First and second bullets of "Total-caveat follow-ups" (2026-09-12): "`approximate` is rendered in one place" and "The stats screen renders `skipped` and not `approximate`". -->

- [DONE: 2026-09-12] [est: 11000] **Three in-app surfaces showed a raw sum of three currencies as though it were one number** — `approximate` shipped for the export, where the claim is permanent, and the collection cards, the collection-detail summary and the stats screen said nothing. The argument for their silence was that they re-render when the rates land, and that is an argument for a QUIETER marker than a sentence rather than for silence: a user reads the figure in the meantime, and "1,500 USD" for items priced in three currencies is a claim the app is making.

  **The cards take the `≈` they already print.** `<CostBadge>`'s item mode has marked a converted cost with that symbol since it was written, and the two questions are genuinely different — item mode asks whether a conversion changed the currency, raw mode whether there was a rate table at all — which is why the flag is a prop rather than a shared computation. They happen to print the same symbol because they mean the same thing to a reader.

  **The stats screen gets the words, and the worse claim wins.** It is the surface whose entire job is one total, and it was saying "excludes N items" about the smaller problem while staying silent about the larger. A figure summed with no rate table is in no single currency; being short of a few items is not the same thing, and a case asserts the branches are in that order.

  **`<CollectionCard>` takes one total instead of a pair.** Four call sites spread `amount` and `currency` by hand from the one object the provider hands them, and `approximate` would have made it three props — the prop-pair smell an earlier round flagged, collected by the feature that would have made it worse.

  **Both summary-card branches are marked.** Owners get a tap-to-swap `Pressable` and non-owners a plain `View`, with the badge written out in each; a case counts two, because the realistic slip is marking one.

  14 new cases in `__tests__/approximate-total-marker.test.ts`; 1 new key in all six locales. 2 existing pins restated — including a `<CostBadge>` adoption case that matched the prop list exactly, which made it a pin on the arguments rather than on the adoption it is about. `npm run verify` green: 8666 tests, 20/20 guards, 19.4 KiB of headroom, +7.4 KiB drift of which +2.5 KiB is copy.

## The exported total says when it is not what it looks like (2026-09-12)

<!-- First and second bullets of "Export-money follow-ups" (2026-09-12): "The export prints a partial total with nothing saying it is partial" and "The no-rates path prints raw amounts under a converted label, in the one artifact that is kept". -->

- [DONE: 2026-09-12] [est: 13000] **`approximate`, and a caveat under the PDF's total** — two ways a total can be less than it appears, and the document stated neither.

  **The first was on the shape and unrendered.** `skipped` counts items in a currency the rate table did not hold; they are simply absent from the sum, and nothing but the stats screen had ever rendered the field.

  **The second was invisible even to a caller that wanted to know.** With no rate table at all, `collectionTotalCost` summed raw amounts and reported them as fully `converted` — indistinguishable from a total that really had converted everything. That is a fine lie for a card which re-renders two seconds later, and it is not fine for the one output a user keeps, mails and files: a PDF exported during a cold start states a sum of three currencies as a figure in one, permanently, with nothing on it saying so.

  **So the flag exists for the consumers that cannot re-render**, and it is a genuinely different question from `skipped`: `approximate` is about the TABLE, `skipped` about the currencies in it. A total can be exact-but-partial, which is the commoner case. Its own case, both directions.

  **`emptyCollectionTotal` is not approximate.** Zero of zero items is exact in every currency, so there is nothing a rate table would have changed — and a case keeps it equal to what `collectionTotalCost([])` answers with rates and without.

  **The unconverted sentence wins over the partial one** when both could apply, because a total summed with no table at all is the worse claim. Resolved on the screen, since the builder takes strings and the counted sentence needs a number — and the screen reads the total ONCE, because two accessor calls could straddle a rate table landing and print a caveat about a figure that is no longer on the page.

  17 new cases in `__tests__/export-total-caveat.test.ts`; 2 new keys in all six locales. 23 existing expectations gained the field, and 2 case titles were restated: "sums raw amounts and reports them as converted" was true, and was the bug. `npm run verify` green: 8652 tests, 20/20 guards, 20.3 KiB of headroom, +6.4 KiB drift of which +1.9 KiB is copy — the first round in which the copy line has printed at all, and it says a third of the growth was sentences.

## Deleting a collection takes its listings with it (2026-09-12)

<!-- Second bullet of "Departure-path follow-ups" (2026-09-12): "`deleteCollection` deletes its items and is not in the record". -->

- [DONE: 2026-09-12] [est: 12000] **The record caught its own blind spot on the first day** — the previous round built a list of every path that touches an item and derived it from mutations whose PARAMETER was an item id. That is the shape of the paths that were already known, so it found eight and missed six — among them `deleteCollection`, which removes a collection AND every item in it and is the largest departure path in the app. Deleting a collection of thirty left thirty standing offers on every buyer's device, each pointing at an item that no longer exists anywhere.

  **The sweep had the same blind spot as the rounds it was written to catch, one level up**, which is the finding worth more than the bug. It asks `does this write setLocalItems?` now — a question about what a mutation DOES rather than what it takes, and one a parameter cannot dodge. Fourteen paths instead of eight, and the six it had been missing are their own case so a narrowing cannot pass quietly.

  **No new strings.** Deleting a collection IS the bulk delete with the selection implied, so its confirm takes `bulkListedWarning` — the same counted sentence, already declined in three Slavic locales. A separate key would have been the same warning translated twice.

  **The six new verdicts are mostly exemptions, and each says why.** `addItem` and `addWishlistItem` are arrivals; `reorderItemsInCollection` writes `sortOrder` and a listing has no notion of position; `transferItemToBuyer` is the buyer's copy of a thing they just bought. `deleteUserContent` is the interesting one: an admin dropping another user's content from THIS device, where the listings belong to that user's account and are not the viewer's to withdraw — one device deciding what another person is selling.

  3 new cases (13 in the suite), 6 new verdicts, 0 new keys. `npm run verify` green: 8635 tests, 20/20 guards, 23.2 KiB of headroom, +3.6 KiB drift and no copy line. No DB migration, no creds.

## Every path that touches an item has a listing verdict (2026-09-12)

<!-- First bullet of "Snapshot follow-ups" (2026-09-12): "`moveItems` is the one bulk resolution that still ignores listings, and four rounds have now declined to say why". -->

- [DONE: 2026-09-12] [est: 11000] **`LISTING_RULE_BY_ITEM_PATH` — the answer to a question four rounds carried, and the list that would have caught the class** — two bugs this week were the same bug: a path that touches an item and does not ask the question the others ask. The archive round taught `archiveItem` to retire an open listing and did not think about `deleteItem`; the round that fixed `deleteItem` did not think about the bulk pair. Each was found by writing a suggestion afterwards rather than by anything in the tree, because the failure is always a path nobody remembered — and a rule stated only in the paths that follow it cannot name the one that does not.

  **`moveItems` is EXEMPT, and that is a decision rather than an omission.** A listing carries `itemId` and nothing about the collection, and `app/listing/[id].tsx` resolves its item with `getItemById(listing.itemId)` alone — so moving an item between collections is invisible to the marketplace, and retiring a listing there would withdraw a live offer for no reason. Three cases hold that reasoning against the code it depends on: the listing screen's resolution, the absence of `collectionId` from the `MarketplaceListing` shape, and the verdict's own wording. Four rounds carried this as an open question and the answer turned out to be a sentence no amount of copying the archive path would have produced.

  **The expensive half is the case that finds a NINTH path.** The eight entries are derived from the provider's own context type rather than listed twice, so a mutation added to the type and not to the record is red until somebody decides which kind it is. That is what turns "somebody has to remember" into "the build says so".

  **Every verdict carries an argument, enforced.** A case rejects a reason under sixty characters, because "EXEMPT" alone is exactly the state the four `moveItems` rounds were already in.

  **A verdict of "retires" is a claim about a CALL SITE.** The providers cannot see each other, so the rule is enforced by screens — a real limitation, and the premise of the whole record. A case asserts the provider itself touches neither helper, so the record cannot quietly start meaning something else.

  10 new cases in `__tests__/item-departure-paths.test.ts`. `npm run verify` green: 8632 tests, 20/20 guards, 24.6 KiB of headroom, +2.2 KiB drift and no copy line — the round added no strings. No DB migration, no creds.

## The budget's two measurements become one (2026-09-12)

<!-- First bullet of "Copy-measure follow-ups" (2026-09-12): "Two recorded measurements now have to move together and nothing enforces it". -->

- [DONE: 2026-09-12] [est: 8000] **`BUDGET_SNAPSHOT` — a trap the previous round built, closed before anybody fell into it** — `LAST_MEASURED_BUNDLE_BYTES` in one module and `LAST_MEASURED_TRANSLATIONS_BYTES` in another, and the pair is a single measurement: the bundle at the commit that last moved the budget, and the translations module at that same commit. The report subtracts one from today's bundle, the other from today's copy, and prints the second as a share OF the first. A raise that updated one and forgot the other would report a copy delta measured against the wrong baseline — silently, with a plausible number, on the one output somebody reads when deciding whether to raise again.

  **A suite could not have caught it.** The tolerance that keeps a drift case from going red on ordinary work is exactly the size of the error it would need to catch. One object instead, so forgetting half is not expressible rather than merely detectable.

  **It imports nothing at all, and that is the point.** `lib/translations-footprint.ts` reaches the i18n parser and `lib/bundle-size.ts` must not; a module both import has to be one that cannot carry anything into either. The strongest form of the rule and the cheapest to read — the same argument `lib/translation-status.ts` makes, asserted the same way.

  **`takenOn` is on the object.** A raise argues about a rate — four moves in three days — and a rate needs the date. A case rejects one in the future, which is what a typed number rather than a measured one looks like.

  8 new cases in `__tests__/budget-snapshot.test.ts`. 2 existing pins restated: both shipped an hour earlier asserting the bundle figure was DECLARED in `lib/bundle-size.ts`, which was right for an hour — what they were claiming is that the number has one home, which is what they ask now. `npm run verify` green: 8622 tests, 20/20 guards, 26.6 KiB of headroom, +0.1 KiB drift. No DB migration, no creds.

## The drift says how much of it was sentences (2026-09-12)

<!-- First bullet of "Bundle-drift follow-ups" (2026-09-12): "The drift is one number and the doc block asked for two". -->

- [DONE: 2026-09-12] [est: 10000] **`translationsFootprint` — the other half of what `lib/bundle-size.ts` asked for** — the drift line shipped this morning saying how much of the last raise a build has spent, and the doc block's request was "KiB per round, split code from copy". That split is the distinction the four raises actually turned on and nobody could state: one round cost 17.9 KiB with four counted strings in six languages in it, the next cost 0.2 KiB with none.

  **It reads SOURCE bytes, and the line says so in its own words.** `lib/i18n-context.tsx`'s size is not that module's share of the minified bundle — the bundler renames locals, strips types and drops comments — and the line sits directly under a figure in bundle KiB, where the obvious misreading is that it is part of that total and the obvious next step is to subtract it. What survives minification almost untouched is the string literals themselves, so the DELTA between two measurements tracks what the bundle sees far better than either absolute number does. Its own case, on the wording rather than the arithmetic.

  **`declarations`, not just `baseKeys`.** A key translated into six languages is six strings in the bundle and a key only `en` declares is one, so a round adding four keys everywhere spends roughly six times what an English-only round does — invisible in a vocabulary count, and the number that actually costs bytes. A case separates the two by deleting one translation from a fixture.

  **Silent when the copy has not moved.** A build that added no sentences should not print a line about sentences: the drift line above it already says the growth was code. This round is its own first example — verify prints the drift and no copy line.

  **`translation-status.test.ts` gains an entry, with the reason.** The rule is that nothing the app loads reaches the i18n parser, and this module reaches it: it runs under tsx from the check script after the build. The reason recorded is that it is the likeliest of the three to be imported by mistake, because what it measures — the app's sentences — is a thing a screen could plausibly want to show.

  15 new cases in `__tests__/translations-footprint.test.ts`, including one asserting the module never touches the filesystem and one that the copy line prints before `process.exit(1)` rather than after. `npm run verify` green: 8614 tests, 20/20 guards, 26.6 KiB of headroom, +0.1 KiB drift. No DB migration, no creds.

## The budget says how much of itself has been spent (2026-09-12)

<!-- Fifth bullet of "Archive-window follow-ups" (2026-09-12), carried from "Bulk-archive follow-ups" and asked for by `lib/bundle-size.ts`'s own doc block. -->

- [DONE: 2026-09-12] [est: 9000] **Four raises in three days, each argued against the one before it** — 4.53 → 4.55 → 4.57 → 4.59 MiB, because nothing recorded what was buying the space. Every build prints it now: `+17.9 KiB since the last budget move, of the 26.8 KiB it bought`.

  **The measurement had three readers and was owned by the one that cannot print it.** `MEASURED_BUNDLE_BYTES` was declared inside `bundle-size.test.ts` while the doc block argued from it in every paragraph and the report could not reach it at all. It is `LAST_MEASURED_BUNDLE_BYTES` beside the budget now, with the reasoning for why it is a MEASUREMENT and never a re-measurement: reading `dist/` for it would make every claim depend on whether somebody had built, and turn a real regression into a number that re-derives its own expectation. Three cases pin that — including one asserting the module never reaches for the filesystem.

  **The zero case says "none of it spent" rather than printing `+0.0 KiB`.** A build at the recorded measurement is the one that just moved the budget, and reading that as growth is how a raise gets argued twice. A shrink signs with a minus, because deleting a screen did that twice this week and a drift that could only be positive would report it as nothing.

  **The second line is the rule the suite held and the tool never said.** Below 8 KiB of headroom the gate fails on ordinary feature work rather than on an accidental SDK, which is not what it is for — and a build sat at 3.7 KiB this morning reporting "OK": green, and one ordinary diff from a red CI that would have named the wrong cause. It now says so on the build that is actually close, and asks for an argument rather than a bigger number, which is the failure mode the budget's own doc block names. Not printed over budget, where there is a louder thing to say.

  16 new cases in `__tests__/bundle-drift.test.ts`, including the exclusive boundary at the floor itself. `npm run verify` green: 8599 tests, 20/20 guards, 26.6 KiB of headroom, +0.1 KiB of drift. No DB migration, no creds.

## The archive screen mounts a window (2026-09-12)

<!-- Fourth bullet of "Bulk-archive follow-ups" (2026-09-12), carried through six rounds since "Archive-screen follow-ups". -->

- [DONE: 2026-09-12] [est: 6000] **Six rounds carried this and the bulk archive is what made it real** — the screen mapped over `archivedItems` and mounted a remote cover photo per row, on the one list in the app with no ceiling: nothing prunes it, every sale adds to it, and one gesture on the bulk bar can now put thirty rows in it. The reason it shipped unwindowed was that it was empty for almost everybody, which was an argument about timing, and the timing changed.

  **`useChunkedList` and `<LoadMoreButton>`, both already used by four screens.** No new keys: the button owns its three, which is why it exists. `remaining` is the only number — the component renders nothing at zero, so the gate and the label are the same fact rather than a `hasMore` asked beside a subtraction. A case asserts `hasMore` appears nowhere on this screen.

  **The window takes the provider's memoised array, and that is what makes it work.** `useChunkedList` resets when the array's REFERENCE changes, so a screen that filtered inline would snap back to one page on every render and turn `loadMore` into a no-op. `archivedItems` is memoised on `localItems` alone; a case pins both halves — the memo in the provider, and the absence of a re-derivation on the screen.

  **A restore resetting the window is correct, not a bug.** The row left the list, so a window the user had grown is describing a list that no longer exists.

  3 new cases in `__tests__/archive-screen.test.ts`. 0 new keys. `npm run verify` green: 8583 tests, 20/20 guards, 26.6 KiB of headroom — the change cost 0.2 KiB, which is what reusing two components instead of writing a third looks like. No DB migration, no creds.

## The bulk bar gains a way back, and learns about listings (2026-09-12)

<!-- First and third bullets of "Open-listing follow-ups" (2026-09-12): "`deleteItems` still leaves every open listing standing" and "Nothing archives in bulk", fifth round carrying the latter. -->

- [DONE: 2026-09-12] [est: 17000] **`archiveItems` + `openListingsForItems` — one bug and one gap that are the same machinery** — `deleteItems` was the bar's only way to make a selection go away, so somebody retiring thirty sold items either deleted them, permanently, or opened thirty screens. And it asked nothing about listings, so those thirty rows left storage while thirty standing offers stayed on every buyer's device with the confirm saying nothing. The single-item archive and the single-item delete had each been fixed for exactly that this morning; the bulk path is where nobody would have noticed.

  **One `archivedAt` for the whole selection.** The archive screen sorts on that field, and thirty timestamps a millisecond apart would scatter a single act across the top of the list in an order nobody chose. Its own case, counting the `new Date()` calls.

  **`openListingsForItems` returns the listings, not a count or a boolean**, because both callers need all three answers: how many to warn about, which ids to remove, and whether to bother. It walks the SELECTION's order rather than the listing store's, so the warning a user reads and the removals that follow agree with the order the selection was made in — and it takes the first match per item, which is what `findListingByItemId` does, so a duplicate pair resolves the same way here as on the item screen.

  **The bulk archive asks only when a listing would go with it.** The single-item action takes no confirm for an unlisted item — archiving is reversible from the item's banner and from the archive screen — and the bulk version keeps that rule rather than inventing a stricter one for the same act done thirty times. The listing removal is the part that is not reversible, and that is exactly when it asks. Not styled destructive, and Archive sits between Move and Delete: a reversible action past the permanent one reads as the more serious of the two.

  **The three Slavic locales decline the counted nouns.** `i18n-agreement` caught the first draft, where the number was followed by "из них" / "з іх" / "z nich" and the declined noun came later — correct prose the guard cannot read, which is the same thing as a rule it cannot enforce. Rewritten so the agreed word sits beside the number, which is also where a bulk confirm's reader looks.

  **4.57 → 4.59 MiB, the FOURTH raise in three days, and the trend is now written into the doc block as the finding rather than a footnote.** Each raise buys about 25 KiB and four rounds of ordinary work spends it, so the number records how fast the app grows and has never said stop. The shape is still right — headroom is chosen against Clarity's ~30 KiB, so a statically-imported SDK trips on its own commit — and this round cost 17.9 KiB of which the copy is a real share. The paragraph asks for KiB-per-round, split code from copy, before a fifth raise.

  29 new cases in `__tests__/bulk-archive.test.ts`; 4 new keys in all six locales. 3 existing counts restated. `npm run verify` green: 8580 tests, 20/20 guards, 26.8 KiB of headroom. No DB migration, no creds.

## Deleting an item takes its listing too (2026-09-12)

<!-- First bullet of "Listing-retirement follow-ups" (2026-09-12): "`deleteItem` still leaves a live listing standing". -->

- [DONE: 2026-09-12] [est: 10000] **The archive round fixed one half of this and left the worse half standing** — an open listing is a standing offer to strangers, it lives on every buyer's device, and nothing the seller does to the item reaches it. Archiving at least left a row somebody could restore and look at; deleting removes it from storage entirely, so the buyer who claims the surviving listing gets a purchase pointing at nothing the seller can even open.

  **`shouldRetireListingOnArchive` is `isOpenListing` now.** The delete path is what made the narrower name wrong: the two callers differ in what they do NEXT — an archive is reversible and asks about the listing it is about to withdraw; a delete is reversible by nothing, so its existing confirm gains a sentence rather than a second dialog — and never in what they need to know. Named for the listing's state, with a case asserting the old name is gone.

  **A sold listing survives both.** A delete does not un-sell a thing that was sold, and the buyer's purchase list, the transfer log and "recently sold" all read that record.

  **The sentence appears only when there is a listing to lose.** A confirm naming only the item describes half of what the button does on the one action in this family nothing can undo; a confirm mentioning a listing for every item is noise on the overwhelming majority that have none. Both platforms take the same body — the web branch builds its own string and the native branch passes it to `Alert.alert`, and the realistic slip is changing one and leaving the other saying less. Its own case.

  **Removal before deletion**, for the same reason as the archive path, with the same ordering case — plus one asserting the navigation away still happens, because the realistic slip in adding a branch above a `router.replace` is to return early out of the whole function.

  13 new cases in `__tests__/delete-retires-listing.test.ts`; 1 new key in all six locales. 2 existing pins restated: the rename, and the "leaves delete exactly as it was" case that had pinned the confirm's exact arguments when what it was claiming is that delete still ASKS while archive does not. `npm run verify` green: 8551 tests, 20/20 guards, 12.5 KiB of headroom. No DB migration, no creds.

## Archiving takes the listing down with it (2026-09-12)

<!-- First bullet of "Archived-listing follow-ups" (2026-09-12): "Archiving an item that is currently listed leaves the listing standing". -->

- [DONE: 2026-09-12] [est: 12000] **`shouldRetireListingOnArchive` — the previous round locked the door the other way round** — it stopped a listing being CREATED for an archived item and said nothing about the reverse order, which is the likelier one: a seller agrees a sale off-platform and archives afterwards. `archiveItem` touched nothing in the marketplace, so the listing outlived the archive and stayed in `activeListings` for every buyer, while the item's own screen showed it as listed underneath a banner reading "this item is archived".

  **A SOLD listing stays.** The sold-listing prompt archives the item as the final step of a sale that already happened, and the listing is that sale's record — the buyer's purchase list, the transfer log and "recently sold" all read it. Removing it there would delete the history the archive exists to keep, which is the argument `archivedAt` itself is built on. So the question is exactly "is there a listing, and is it still open?", asked on `soldAt` — the same field `activeListings` filters on, with a case asserting the two agree.

  **A function, not an `if` on a screen.** The previous round's rule lives in a component, which the next caller (a bulk archive) would not inherit; this one is in `marketplace-helpers.ts` where it can be tested on values and reached.

  **The listed branch asks first and offers no undo.** Archiving an unlisted item is private; archiving a listed one withdraws something other people can see, and `addListing` mints a new id and a new `createdAt`, so nothing can put the listing back. An undo that silently restored half of what it took would be worse than none — so the confirm carries the warning instead, and a case asserts each locale's body actually says the listing does not come back rather than only asking "are you sure".

  **The removal goes before the archive.** An item archived first is one whose listing removal can be lost. Its own ordering case, as is the one on the earlier round's `addListing` guard.

  19 new cases in `__tests__/archive-retires-listing.test.ts`; 3 new keys in all six locales. 1 existing pin restated: last round's "takes no confirm" is now about the unlisted BRANCH, which is what it was always claiming. `npm run verify` green: 8538 tests, 20/20 guards, 14.1 KiB of headroom. No DB migration, no creds.

## An archived item cannot be put up for sale (2026-09-12)

<!-- Fourth bullet of "Item-archive follow-ups" (2026-09-12): "An archived item is still listable, tradeable and reactable". -->

- [DONE: 2026-09-12] [est: 11000] **The marketplace block under the archived banner was completely unchanged** — "List on marketplace" rendered as normal, the sheet opened, and `addListing` accepted the id. So a seller could offer an item the app itself was describing, two inches higher on the same screen, as one they no longer have. The archived items likeliest to be re-listed are exactly the ones that got there through the sold-listing prompt — rows whose entire history is "this was already bought".

  **This is the one affordance in the family worth closing.** The share sheet, the reaction bar and the edit form all act on the seller's own data. A listing creates an expectation in somebody ELSE, who claims it and then waits for a thing that was sold last month.

  **No CTA rather than a disabled one.** A greyed-out "List on marketplace" invites a press that explains nothing; the sentence says what to do instead, and Restore is the button directly above it.

  **Two locks, because hiding a button is not closing a door.** The sold-listing prompt fires on a realtime update, on whatever screen the seller is looking at — including this one with the listing sheet already open. `handleSubmitListing` checks before it calls `addListing`, and a case asserts the ordering rather than only the presence.

  **The feed is not where this could have been fixed.** Items are local-first: a buyer's device holds the listing and not the seller's `archivedAt`, so no filter on the browse page can see the state. The lock has to be on the device that knows.

  **Taking a listing DOWN still works on an archived item.** That is the direction that reduces the obligation rather than creating one, and an item archived while listed is exactly the case that needs it. Its own case.

  12 new cases in `__tests__/archived-not-listable.test.ts`; 2 new keys in all six locales, with a case that the standing hint and the refusal toast are not the same sentence — a hint that reads like an error alarms where nothing went wrong, and an error that reads like a hint does not say the press was rejected. 2 existing counts restated. `npm run verify` green: 8519 tests, 20/20 guards, 17.5 KiB of headroom. No DB migration, no creds.

## Archiving reaches the screen the item is on (2026-09-12)

<!-- Second and third bullets of "Archive-screen follow-ups" (2026-09-12): "Archiving is still reachable from exactly one place, and it is a marketplace prompt" and "Nothing tells a user an item IS archived". -->

- [DONE: 2026-09-12] [est: 12000] **An Archive action and an archived banner on `app/item/[id].tsx` — the archive family's last two holes** — `archiveItem` had exactly one caller, the sold-listing prompt, so the only route into the archive was selling something through the marketplace. A collector who wanted to retire an item they still own — broken, loaned, lent to a show — could only delete it, which is the one action in this family nothing can undo. And an archived item rendered on this screen exactly like a live one: no banner, no restore, nothing saying why it had disappeared from every list.

  **This screen is where an archived row is met by accident.** The deep link and the marketplace transfer log both land here, and `getItemById` does not filter archived rows — so it was already reachable and already silent. The banner answers the three questions somebody arriving that way actually has, which is why it is a title AND a hint: "Archived" alone says where it went, not whether it is gone or whether it can come back. A case asserts the hint is neither a copy of the title nor shorter than it, in each of the six locales.

  **No confirm on the archive, and an undo instead.** Archiving is reversible in two places now — this screen's own banner and `app/archive.tsx` — so a confirm would be an obstacle in front of a reversible action, and the toast covers the tap that was a mistake. The same shape the sold prompt uses, down to capturing the id before the await: `activeItem` is recomputed from `getItemById` and the row leaves that list the moment it is archived.

  **The action disappears once the item is archived.** A second archive would restamp `archivedAt` and move the row to the top of a list sorted by it — a confusing no-op rather than a destructive one — and a button offering it beside a banner saying "this is archived" reads as a contradiction either way.

  **`isArchived`, never `activeItem.archivedAt`.** The screen asks the shared predicate and a case asserts the field is not read directly, which is the gate that keeps a seventh spelling of "is this in the trash" from being written on the screen most likely to grow one.

  18 new cases in `__tests__/item-archive-actions.test.ts`; 4 new keys in all six locales. 2 existing counts restated — the item-detail family rose by SEVEN for a four-key round, because the screen also picked up three keys the archive screen had already paid for, which is exactly what a screen family counts and a per-key diff would not show. `npm run verify` green: 8507 tests, 20/20 guards, 20.0 KiB of headroom. No DB migration, no creds.

## The archive is a place you can walk into (2026-09-12)

<!-- First bullet of "Unarchive follow-ups" (2026-09-12): "The restore is reachable for about eight seconds and then never again". -->

- [DONE: 2026-09-12] [est: 16000] **`archivedItems` + `app/archive.tsx` — the other half of the morning's restore** — `unarchiveItem` shipped with one caller, the Undo on the sold-listing prompt's toast, which covers the accidental archive and nothing else: eight seconds after the fact there was still no way to see what had been archived or to get any of it back. The list, the screen, and a home-screen row that appears the moment something lands in the trash.

  **Sorted by `archivedAt`, not `createdAt`.** What somebody opens this screen looking for is the thing they archived a minute ago, which may well be the oldest item they own. Its own case, built so the two orderings disagree.

  **`localItems`, not the merged list.** An archive is a thing you did to your OWN rows; a friend's archived item is neither yours to restore nor yours to be shown. And `isArchived` alone, with no `!isWishlist`: an archived want is exactly as unreachable as an archived holding, and a filter that excluded it would leave it with no way back at all.

  **Restore has no confirm and Delete does.** Restoring is reversible by archiving again; deleting is reversible by nothing, and this is the screen people arrive at having already made one mistake. For the same reason there is no "empty the archive" — a screen whose whole purpose is recovering from a mistake is the worst possible place to put a one-tap way to make a bigger one. Both are their own cases, including the negative one.

  **The banner is conditional.** An archive is a recovery path, not a feature to advertise: a permanent row pointing at an empty screen is noise on the surface every session starts on, and a row that appears when the trash gains something is how somebody learns the trash exists at the one moment they need to. `dashboard-banner.test.ts` counts three rows now and asserts the third is the conditional one, because "bump the number to 3" would have been a different claim about the dashboard.

  **4.55 → 4.57 MiB, re-measured and re-argued as the doc block requires.** Four rounds today took the bundle from 4634.4 to 4655.5 KiB and the headroom from 24.8 to 3.7 — below the 8 KiB floor `bundle-size.test.ts` holds precisely so the gate fails on an SDK and not on a screen. The raise buys 24.2 KiB, still under Clarity's ~30 KiB, so a statically-imported SDK still trips on its own commit.

  33 new cases in `__tests__/archive-screen.test.ts`; 11 new keys in all six locales. 4 existing counts restated (the banner count, the home-screen family size, the coverage denominator, the measured bundle). `npm run verify` green: 8489 tests, 20/20 guards, 24.2 KiB of headroom. No DB migration, no creds.

## An archive you can come back from (2026-09-12)

<!-- First bullet of "Live-item-predicate follow-ups" (2026-09-11): "An archived want can be created and nothing can un-archive it". -->

- [DONE: 2026-09-12] [est: 15000] **`unarchiveItem` — the safe answer was the irreversible one** — the sold-listing prompt offers Archive beside Delete and labels it "keep it for stats and audit history", and an item that took it left every listing, total, count, recent-items strip and search in the app. No screen lists archived rows and there was no `unarchiveItem` anywhere in the tree, so the row sat in storage and kept syncing: not gone, not reachable, counted by nothing. The archive was one-way in both directions.

  **`null`, not `undefined`.** `updateRemoteItem` writes the column only when the key is PRESENT in `updates` (`"archivedAt" in updates`), and the row coercer reads `null` for "not archived". Clearing the field with `undefined` would restore the item locally and leave the cloud row archived, so the next sync would put it straight back in the trash — a worse bug than the one being fixed, and silent. Its own case, plus one pinning the `in updates` contract it depends on.

  **The undo goes where the archive happened.** `toast.show` rather than `toast.success` (a success toast has no action slot), and the id is captured before the await: `item` is recomputed from the head of the notification queue, which the next line dismisses, so an undo closing over it would restore whatever the NEXT notification is about. The restore shows no toast of its own, so it announces — same argument as the reorder undo.

  **A second bug, found while reading the first, in five places.** `archiveItem`, `updateItem`, `updateCollection`, `promoteWishlistItem` and `moveItems` learned what they had changed by assigning to a `let` from inside a `setLocalItems` updater and reading it on the next line. React computes a `useState` update eagerly only while the hook has no work queued, so the pattern holds right up to the moment something else has queued one — a landing realtime row, a second tap, a sync flush — and then the variable is still `null`, the `if` is skipped, and the cloud write never happens. Local state changes, the remote row does not, the next sync hands the old value back: an edit that reverts itself, only under load. `deleteCollection` in the same file had always read `localItems` directly; `resolveLocalItem` is that shape with a name on it.

  19 new cases in `__tests__/unarchive-item.test.ts`. 2 existing counts restated (the marketplace family's size, the coverage denominator). `npm run verify` green: 8456 tests, 20/20 guards, 15.1 KiB of headroom. No DB migration, no creds.

## The PDF prints money, not numbers (2026-09-12)

<!-- Third and fourth bullets of "Cost-gate follow-ups" (2026-09-11), carried through four rounds: "The export's total is summed but never converted or labelled" and "A collection's `currency` override reaches the totals and not the PDF". -->

- [DONE: 2026-09-12] [est: 13000] **`ExportMoney` — the one output a user keeps was the one place that converted nothing** — `buildCollectionExportHtml` summed the raw `cost` fields itself, printed the result as a bare number beside a "Total cost" label, and printed each item's cost as `${item.cost}`: no currency, no separator. A collection holding items in EUR and USD filed one figure in no unit at all, and a 1500-euro item printed "1500" beside a 1500-dollar one with nothing in the document able to tell them apart. `collection.currency` — the override that reaches every screen in the app — reached this document not at all, because the builder never read the field.

  **The builder takes the ANSWERS, not the rate table.** The conversion needs the USD rates, which live in the provider behind a fetch and a cache; taking them would make a pure module's signature about where rates come from instead of about the document, and would be a second conversion implementation to keep in step with the screen's. Taking `{ total, itemCost }` gets something better than two implementations that agree today: the PDF prints the figures the screen printed, because they are the same values — `getCollectionTotalCost(collection.id)` and the `convertItemCost(item, collection.currency)` every `<CostBadge>` on that screen already calls.

  **The `≈` is emitted by the builder rather than taken as a label**, because the marker is a symbol and not a word: all six locales spell `itemValueApprox` as exactly `≈ ${amount} ${currency}`. A case pins that equivalence rather than trusting the comment — if a locale ever writes it differently, the export has to start taking a label, and that case is what says so. What it marks is worth marking in a document somebody files: a figure derived from a rate table on the day it was printed is not the price that was paid.

  **The cost cell is escaped now.** The amount is a number, but the code beside it comes from `costCurrency`, which is stored text nothing validates on the way out of the database — and it was being interpolated into the document raw. Its own case.

  21 new cases in `__tests__/export-pdf-money.test.ts`. 10 existing call sites in two suites restated onto the new signature, one of them the assertion that the printed total is "42" — it is "42 USD" now, which is the round in one character. `npm run verify` green: 8437 tests, 20/20 guards, 16.1 KiB of headroom. No DB migration, no creds.

## The stats screen's total is a real total (2026-09-12)

<!-- First bullet of "Cost-gate follow-ups" (2026-09-11), carried through three rounds: "`app/stats.tsx` is the one screen whose totals never convert". -->

- [DONE: 2026-09-12] [est: 14000] **`portfolioTotalCost` + `ownedTotalCost` — the one total in the app that never converted and never said what it was in** — the stats screen reduced every owned item into `sum + i.cost` and rendered it with `toLocaleString()` under a label reading "Total value". A collector holding items priced in EUR, GBP and USD read the sum of three different units, printed with no currency beside it, on the same screen as collection cards that had converted those exact figures correctly. Three rounds carried this bullet.

  **The property worth the most is the one a user checks by hand: the portfolio total is the collection cards added up.** That is why `portfolioTotalCost` resolves an item with no `costCurrency` through its COLLECTION's override before falling back to the viewer's display currency — `collectionTotalCost` already does this, because for a card the target IS that collection's override. A plain `?? displayCurrency` passes every other case in the new suite and silently disagrees with the cards for exactly the collections that set one. Its own case, in both directions (50 with the override, 40 without).

  **One `sumEntries`, because the no-rates branch is a judgement call.** Summing raw amounts while the rate table loads is a deliberate lie of convenience the module header argues for; a second copy of a judgement call is how two screens end up disagreeing about what an unconvertible item is worth. Both totals delegate, and a case counts the delegations.

  **`skipped` is rendered for the first time since the field existed.** A single collection is usually one currency, so a missing rate there is pathological; a PORTFOLIO spans currencies, which makes it ordinary — and a headline figure that silently drops those items is wrong rather than approximate. `statsTotalValuePartial`, translated in all six locales: a hint saying a sum is incomplete, served in English under somebody else's flag, is the same defect one layer up.

  **Built from `itemsByCollection`, not by re-filtering `items`.** The index is already live-item filtered (the predicate the screen used to get via `selectOwnedActiveItems`), and walking owned collections skips every friend's, subscription's and share's items rather than testing each one. Memoised in the provider on the five things it depends on.

  30 new cases in `__tests__/portfolio-total.test.ts`. 1 existing pin re-stated: `has-finite-cost.test.ts` listed stats as a caller that reads the shared gate, and the right claim now is that the screen touches no `.cost` at all. `npm run verify` green: 8416 tests, 20/20 guards, 16.4 KiB of headroom. No DB migration, no creds.

