## The last unmounted provider, and the five keys a wrong gate would have leaked into (2026-08-30)

<!-- Fifth bullet of "Chat-mount follow-ups", filed earlier this run: "`social-context` and `collections-context` are the last two unmounted providers ... collections has five persist hooks behind one `persistEnabled`, which makes it the highest-value mount left in the tree and the one where a wrong gate costs the user's own collections rather than a cache". .tasks.md's three remaining tasks are all still blocked. -->

- [DONE: 2026-08-30] [est: 21000] **`CollectionsProvider` is mounted, and the two data-loss bugs its `persistEnabled` was bought with are now asserted by running it** — the seed overwrite (an unreadable store installed `seedCollections`/`seedItems`, and the persist effects wrote them over the real blobs the moment `ready` flipped) and the account switch (five keys already naming user B while `ready` and the gate still described user A). Both had been fixed for days and both were covered by reading the provider's source, which is the shape that let each of them ship.

  **Ten cases, and a refused write is the claim in six of them.** A rejected read writes NOTHING and leaves both real blobs on disk byte-for-byte; nothing containing the string `seed` is ever written; a blob that is not JSON refuses the whole session's writes rather than that one key; and the account switch writes none of A's five blobs under B's five keys. `ready` still flips in every one of them, because a broken store is not a reason to block the tree.

  **The first case in the file is the one that makes the other six mean something.** A successful hydrate writes all five keys — collections, items, followed ids, and both offline upsert queues — so "wrote nothing" is a refusal rather than a provider that never writes at all.

  **Mutation-checked on each half of the gate separately.** Dropping `hydrationSafeToPersist` from `persistEnabled` fails exactly the three refusal cases and none of the account cases; dropping `hydrationMatchesKey` fails exactly the account-switch case and none of the refusal cases. Two independent claims, not one restated twice, which is what the previous source-shape coverage could not distinguish.

  **Fourteen mocks, and the read set is the point of them.** The cloud surface, both realtime subscriptions, the tombstone/cursor/import stores and the currency stack are all inert, so the five reads the suite counts are exactly the five blobs `persistEnabled` is about — a case asserting what was read is asserting something. `isSupabaseConfigured` is false, which is a real state (a signed-in user on a device with no Supabase) rather than a convenience.

  **Six of six providers are mounted now.** `[needs-dev-dep]` has named jsdom as the blocker for this class of coverage since July, across every one of them; the count of providers that claim needs is zero.

  `npm run verify` green: typecheck, `lint:all`, 7074 tests (+10), build, bundle secrets, bundle size (28.3 KiB headroom), bundle smoke.

