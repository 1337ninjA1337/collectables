## Six families said du, five strings said Sie, and nobody had written it down (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Per-screen i18n follow-ups" — "German has now followed *du* for six consecutive families and the app-wide question is still open ... the sweep is now smaller than the argument". Filed by the run an hour earlier; taken because a decision six runs have deferred is one somebody eventually makes by accident. -->

- [DONE: 2026-08-21] [est: 12000] **Settle the German register, and make the decision a rule rather than a comment** — the empty states, wishlist, filters, search, marketplace and collection families all address the reader as *du*; `templatePickerHint`, `homeSubtitle`, `languageSubtitle`, `deleteAccountText` and `deleteAccountFailed` still said *Sie*. Six local calls in one direction is not a split, it is an unrecorded decision with five exceptions, and a German speaker reads the mix as two people having written the app.

  **The direction was chosen by precedent, not preference**, and the entry says so: converting five strings is a smaller change than converting six families back, and reverting is now the larger edit — which is the point of writing it as an enforceable rule instead of a note somebody has to find.

  **The suggestion said three strings; it is five.** Corrected on contact, the third time today a promoted entry's count was wrong in the direction of "smaller than it is". Worth noticing as a pattern in how these get filed rather than as three separate slips.

  **The string that shaped the rule is the one NOT changed.** `marketplaceListingNotFoundHint` reads `Sie wurde möglicherweise vom Eigentümer entfernt` — third-person feminine agreeing with *die Anzeige*, rendering "It may have been removed by its owner". A sweep flagging every capitalised `Sie` would have "fixed" that into nonsense. So the guard matches two constructions that are unambiguously formal ADDRESS — capitalised `Ihr…`/`Ihnen`, and `<verb>en Sie` — and leaves the pronoun alone. A case names that string explicitly, because it is what explains the rule's shape.

  **The mutation check caught a defect in the guard's own message.** Reverting one string to `Wählen Sie` failed correctly but quoted `hlen Sie`: JS `\w` is ASCII-only, so the match started mid-word at the umlaut. It caught the defect and misdescribed it — which is the half of a guard that decides whether the next reader trusts it. The class is now `[A-Za-zÄÖÜäöüß]`, and the message names `Wählen Sie` in full.

  A vacuity case asserts `de` says *du* at least twenty times, since every other assertion here is satisfied by a locale that addresses nobody at all.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6308 tests (+3), build.

## The last prefix family, and the number that says the unit is spent (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Quotation-guard follow-ups" — "`marketplace*` (12) and `collection*` (10) are the last two prefix families of any size". This is the second half of that entry and the end of the list it came from. -->

- [DONE: 2026-08-21] [est: 14000] **Translate the collection family — ten form labels a user meets before they own anything** — `collectionNameLabel`, `collectionNamePlaceholder`, `collectionDescriptionLabel`, `collectionCoverLabel` and six more: the create-collection form. Naming a collection is the first thing this app asks anybody to do, so an English form here is the first English a new speaker of any of these four locales sees, on the screen where they are least invested and most likely to leave.

  Low typographic hazard for once — no quoted spans, one interpolation (`Collection: ${name}`), and the placeholders are examples rather than instructions, so `Напрыклад, Вінтажныя кансолі` and `Na przykład Zabytkowe konsole` are translations rather than transliterations. German kept *du* (`Was du sammelst`), the sixth family running.

  **The finding is the number underneath.** 175 keys remain English in all four, and the largest remaining prefix group is SEVEN (`no*`, `share*`, `email*`, `stats*` — then `auth*` at six, `export*`/`ai*`/`delete*` at five). The prefix-family unit is spent: `FAMILIES` has no seventh entry worth the ceremony, and a rule whose entries cover seven keys each is more bookkeeping than guard. The per-SCREEN question three previous runs raised is no longer a nice refinement — it is the only unit left that groups the remaining work the way a user meets it. Filed as the top suggestion with the counts, so the next run starts from a measurement rather than re-deriving one.

  `collection` joins `FAMILIES` at 14. Coverage: be 65.1%, pl 63.3%, es 62.7%, de 62.5% — up ten each, and every locale has gained roughly five points across today's two i18n tasks. Mutation-checked by pasting the English placeholder into `be`: the verbatim-English case fails naming the locale, the family and the key.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6305 tests (+5), build.

## The dialog a seller meets at the worst possible moment (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Quotation-guard follow-ups" — "`marketplace*` (12) and `collection*` (10) are the last two prefix families of any size". Taken after four consecutive guard-infrastructure tasks, because the remaining i18n families are the work with a user on the other end of it. -->

- [DONE: 2026-08-21] [est: 16000] **Finish the marketplace family — the twelve `marketplaceSoldPrompt*` keys** — the suggestion counted `marketplace*` as a twelve-key family; it is a SEVENTY-THREE key family that was 61/73 done in four locales, and the twelve missing ones were the whole sold-prompt sub-family. Worth stating because it changes what the task was: not a small family nobody had started, but the last twelfth of the largest family in the map, left behind when the rest was translated.

  **They are the strings a seller reads the moment somebody claims their listing** — a dialog offering to archive, delete or keep the original in their collection, plus a destructive-action confirmation. Switching languages on somebody at the point they are being asked to permanently delete something is the same class of failure the filter task named about validation errors, one step worse: this one has a delete button on it.

  **The typographic hazard this file carries was handled per locale, not pasted.** English writes `"${title}"`; the four locales use their own pairs — `«…»` for `be` and `es`, `„…”` for `pl`, `„…“` for `de`. Mutation-checked by planting the English opening mark in the Polish body, which is exactly the paste that produced four real defects when the guard first ran: it fails naming the mark, the locale and its own pair, and the openers-vs-closers case fires alongside it.

  **German followed *du*, the fifth family running.** `deine Anzeige`, `möchtest du`, `deiner Sammlung`. The app-wide *du*/*Sie* question is still open and still filed; five consecutive local calls in the same direction is now most of an answer, and the entry saying so is more useful than a sixth.

  `marketplace` joins `FAMILIES` at size 73 — the largest entry there, and the first whose size is a real vacuity guard rather than a formality, since a rename inside seventy-three keys is easy to miss. Coverage: be 63.1%, pl 61.3%, es 60.7%, de 60.5%, each up twelve keys. No floor moved: the coverage floors had slack, which is what they are measured with.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6300 tests (+5), build.

## The record that was checked against itself (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Third-surface follow-ups" — "`SURFACE_RELATIONSHIPS` is asserted against the table and nothing asserts it against the SCREENS", plus the module-scope entry beside it and the `kind`-union question, which asked for a decision rather than a change. -->

- [DONE: 2026-08-21] [est: 14000] **Tie `SURFACE_RELATIONSHIPS` to the screens it is a claim about** — the record says `tab` is asked about exactly `friend`, `request_received` and `following` BECAUSE `app/friends.tsx` has three lists. Every case about it read the table, so it was a claim checked against itself: a fourth list, or one quietly re-pointed at a different id array, would leave the record wrong and every existing case green.

  **The friends screen is the one surface whose relationships are enumerable**, because it maps a tab through a literal record rather than asking the context. So the record's values are read out of the source and compared against `SURFACE_RELATIONSHIPS.tab`. Mutation-checked by re-pointing the request tab at `request_sent` — a relationship the tab surface does not serve — which fails naming both sides of the disagreement.

  **The counterpart case states the honest limit.** `detail` and `row` map no fixed set: they call `getRelationship`, which can answer any of the six, so all five non-`self` relationships are reachable there by construction rather than by a list somebody wrote. What CAN be checked is that they still ASK the context, since a screen deriving the relationship some other way is exactly the one that could quietly narrow what its surface serves. Comments stripped, for the reason the last four tasks kept re-learning.

  **`RELATIONSHIP_BY_TAB` moved to module scope.** A frozen three-entry record with no closure over props or state, rebuilt every render and reading as though it depended on something — the more expensive kind of wrong, since the cost is a reader's time rather than three string properties.

  **The `kind`-union question was answered NO, and the answer is written down.** `"friend" | "following" | "request"` and `ProfileRelationship` are two names for the same three values, and `renderProfileCard` could take the relationship directly, deleting both the union and the record. Deliberately not done: the record is where the getRelationship-vs-tab decision LIVES, and passing the relationship in would move it to three call sites, each one edit away from reaching for `getRelationship` because it is right there. One place to state a decision beats three to restate it.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6295 tests (+2), build.

## Two lists of the same roots, and a test to catch them drifting (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Scan-root follow-ups" — "`FLOOR_WALKS` and `count.roots` are two tables naming the same roots, kept in step by one test". The entry offered two shapes and called the bigger one cleaner; it is, and this is it. -->

- [DONE: 2026-08-21] [est: 22000] **Derive the walk list from the floors instead of listing it twice** — `FLOOR_WALKS` named the roots of each multi-root walk for the re-measure tool, `count.roots` named the same roots for the guard, and a test asserted they agreed. A list, a second list, and a check that they matched, for one fact.

  **The floor entry wins the tie because it is the one the GUARD asserts against.** A table that disagreed with it would document a property the binary does not enforce, which is the failure this module exists to refuse, one level up. `extensions` moved into `ScannedCountFloor` beside `roots` — a walk is completely described by those two — and `floorWalks()` is now a projection over `SCANNED_FLOORS`. A guard joins the list by declaring `count.roots`, which is also what makes it a multi-root walk, so the two can no longer drift: nothing to add when a guard adopts roots, nothing to remove when one stops.

  **`checkWalkPremise` took a whole `FloorWalk` and only ever read its extensions.** It takes the extension list now, which is what the pure cases were always really about — three of them were constructing a walk object to pass one field.

  **The vacuity guard had to be rewritten, and got stronger for it.** It asked `floorWalks()` which of its entries lacked roots, which is unanswerable-by-construction once that list IS the entries with roots — a case that cannot fail is the thing it was written to prevent. It now asks the WRAPPERS: a guard whose `SCANNED_DIRS` names more than one directory must declare `count.roots`. That catches the new multi-root guard that declares nothing — which the hand-kept table would simply have omitted, silently, since being absent from `FLOOR_WALKS` was indistinguishable from not being a walk. Mutation-checked by dropping `roots` from `check-inline-hex`: it fails naming the guard.

  The parity test is gone, replaced by one that pins the derivation — the walk's roots must be the SAME reference as the floor's, so a future edit that copies rather than projects is caught.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6293 tests, build. Run immediately before the commit this time, after the mutation check rather than before it.

## The gap my own change opened, and the comment written six times (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Scan-root follow-ups" — the top two entries, both filed an hour earlier by the run that finished the scan-root work. Taken together because they are one cleanup of one subsystem, and because the first closes a cost that run introduced deliberately and named on the way out. -->

- [DONE: 2026-08-21] [est: 20000] **Report a floor that has drifted loose, and give the six wrappers one call instead of two** — two loose ends from #433–#435, neither of them a defect and both of them the kind of thing that stops being fixable once somebody has read past it twice.

  **The drift line closes a gap that was opened on purpose.** Retiring the largest-root rule means nothing forces a re-measure any more — which also means nothing notices a floor going SLACK. A number measured at a quarter of a 700-file walk is 41% of a 900-file one without anybody touching it, and at that point the walk could lose two-fifths of its files, every root present so the per-root check is happy, and pass. `FLOOR_DRIFT = 0.4` is where the number stops doing much rather than where it becomes wrong, and the line it prints is ADVICE: the row stays `ok`, the tool still exits 0, and the sentence says "nothing is broken, and nothing will go red for this". A threshold that failed would have reintroduced the chore the whole change existed to remove.

  **Writing it found a real flaw in the formatter, one commit old.** The largest-root note and the slack notes were a single `else if` chain, so a `declared_roots` row sitting under its largest root could not also report drift — the note about the RETIRED rule swallowed the note about the live one, on exactly the row most likely to have drifted. They are two independent statements about one row and are now two independent branches, with a case that asserts both fire together.

  **`assertScannedWalk` deletes a decision that was written out six times.** Each multi-root wrapper carried two calls plus the same four-line comment explaining why roots come first. The ordering is the whole content — when a root vanishes both premises fail and they give opposite advice, one naming the directory and one saying "re-measure the floor" — and it is now true by construction instead of by six call sites remembering. The two halves stay exported for the single-root guards that only want the count, and the function REFUSES an entry that declares no roots rather than falling back, so "which premises did this guard assert?" stays answerable from the call site.

  Two pre-existing cases had to be corrected rather than accommodated, which is the honest half: `assert.doesNotMatch(roomy, /is deletable —/)` asserted that a floor with more room than the target says nothing, and that is precisely the behaviour this task changed; and the wrapper-shape case demanded the literal `assertScannedFloor(` from every guard. Both now say what is true and why.

  Mutation-checked both ways: splitting a wrapper back into two calls fails naming the guard, and disabling the drift branch fails three cases including the pre-existing tight-slack one.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6293 tests, build.

## Multi-root floors: state the property instead of approximating it with a number (decomposed 2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Action-row follow-ups" / "Relationship-table follow-ups" / "Quotation-guard follow-ups" — three consecutive runs have now filed "a multi-root floor expressed as a COUNT has to be re-measured every time the repo grows". Decomposed rather than taken whole: what looked like "divide by the walk size" turns out to be a design change to how six guards state their premise, and the obvious version of it is circular. -->

Context, so the next run does not re-derive it:

- **The chore.** `__tests__/lint-guard-partial-root.test.ts` enforces `minimum > largestRoot` for the six multi-root floors. It goes red whenever an ordinary file lands in the largest root — five times now, twice this afternoon from ONE new component — and the failure surfaces in a suite unrelated to the change that caused it.
- **The obvious fix is circular and must not be attempted.** A floor cannot be `fraction × walkSize` evaluated at guard time: the walk size IS the count being checked, so `count >= 0.75 × count` is always true. The fraction only means something to a tool that already knows what the tree SHOULD hold, which is the committed number again.
- **`remeasure-floors --write` is ruled out by design, not by effort.** `scripts/remeasure-floors.ts` says so in its own header: "a script that rewrote the table would be a script that erased the reasoning the table exists to hold." Do not add it.
- **The property already exists, one layer away.** `checkWalkPremise`'s `empty_root` code says "this root walked to zero files" — the direct, number-free form of "no scan root silently vanished". It is computed by `remeasure-floors` and by the test suite, and NOT by the guards at runtime, which is the only reason the total-count floor has to carry the property by arithmetic.

So the work is: give the guards the per-root property at runtime, then retire the arithmetic that was standing in for it.

- [DONE: 2026-08-21] [est: 45000] **1/3 — teach `SCANNED_FLOORS` a `roots` list and prove it on `check-inline-radius`** — add `ScannedCountFloor.roots?`, a `no_root_files` failure code, a pure `evaluateScannedRoots(files, roots)` and an `assertScannedRoots(checkName, files)` beside the existing count assertion; validate `roots` the way `inputs` is already validated (blank entry, duplicate). Adopt it in one guard, end to end, so the design is proved by a wrapper rather than by a unit test. The count floor stays exactly as it is — this slice adds a premise, it removes nothing.

  **Shipped, and the payoff showed up inside the slice.** `ScannedCountFloor.roots?`, two failure codes (`no_root_files` about the tree, `no_roots_floor` about a wrapper/entry mismatch), three entry problems validated the way `inputs` already is (`empty_roots`, `blank_root`, `duplicate_root`), a pure `evaluateScannedRoots` and an `assertScannedRoots`. `check-inline-radius` calls it, and a tree holding only `components/` — 46 files, the exact count that drew level with the floor of 46 twice this afternoon — now refuses with "scan root app/ contributed 0 of the 46 source file(s) this walk found" instead of "below the committed floor, re-measure". No number in it.

  **Roots are asserted BEFORE the count, deliberately.** When a root vanishes both fire and they give opposite advice: `no_root_files` names the directory that went quiet, `below_floor` says "if the tree legitimately shrank, re-measure the floor" — the wrong fix, and the easy one. The specific diagnosis should be the one the reader meets. That reorder is why `check-inline-radius`'s partial fixture changed from "all of app/" to "three files from each root": the dropped-root case moved to its own block, so both codes keep a real wrapper run instead of one shadowing the other.

  **`floor-walks.test.ts`'s largest-root case now skips entries that declare roots**, which is the whole point — that case is the one that has gone red five times on ordinary growth. `check-inline-radius` is out of it. A new parity case pins that a floor's declared roots equal its `FLOOR_WALKS` roots, since two tables now describe one walk and only one of them refuses.

  Every exhaustive record in the harness earned its keep: eleven compile errors across four suites, each naming a code with no fixture, no sentence or no wrapper run. `npm run verify` green: typecheck, `lint:all` 16/16, 6280 tests (+43), build.

- [DONE: 2026-08-21] [est: 25000] **2/3 — roll `roots` out to the other five multi-root guards** — `check-inline-hex`, `check-analytics-imports`, `check-clarity-input-mask`, `check-a11y-jsx`, `check-problem-phrasing-imports`. Mechanical once 1/3 lands: one field per entry, one call per wrapper. The partial-root fixture harness gains a red run per guard.

  **Shipped. All six now refuse a dropped root by name** — verified by spawning each guard against a tree holding only its first root: every one exits 1 with "scan root components/ contributed 0 of the 19 source file(s) this walk found" rather than a floor number.

  **The fixtures generalised instead of being copied five times.** Each guard's `PARTIAL_FIXTURES` spec used to hand over one whole root (`["app"]`), which was the shortest route to a walk that came up short — and stopped being that once the per-root assertion started running first, since a tree missing `components/` now refuses by NAME. `sliceOfEveryRoot(checkName)` reads `FLOOR_WALKS` for the roots AND the extensions (a markup-only walk handed `.ts` files counts zero of them and reproduces `no_files`, the code the empty-root harness already owns) and takes three files from each. `check-problem-phrasing-imports` went from all of `app/` to fifteen files; that does NOT make it affordable in the dropped-root block, which still copies a whole root per fixture.

  **Two things the migration broke, both caught by the harness rather than by review.** The `no_roots_floor` fixture pointed at `check-inline-hex`, which now declares roots — so it produced `no_root_files` and the exhaustive producer map failed on the mismatch. Repointed at `check-env-inlining`, a single-root walk where naming roots would be the floor and the root saying the same thing twice, with a comment saying why it cannot be one of the six.

  **The arithmetic case in `floor-walks.test.ts` now covers nothing, and says so.** With all six skipped it is a loop that passes exactly like a loop that checked everything — the failure mode this whole module exists to refuse. A new case publishes the count of walks still holding the property by a committed number (zero) and names them when it is not, so the migration finishing is visible rather than inferred. The case itself is kept as the safety net for the next multi-root guard, which will arrive without roots and be caught on its first red run.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6281 tests, build.

- [DONE: 2026-08-21] [est: 30000] **3/3 — retire `minimum > largestRoot` for the six, and say what the count floors are still for** — with the property enforced directly, the arithmetic is a second statement of it and the only thing that ever needs re-measuring. The count floors should NOT be deleted: they still catch a walk that returns implausibly few files (a glob that broke without emptying a root), which is a different failure. This slice is mostly the ARGUMENT — which suite keeps which claim, what `remeasure-floors` prints once `MOVE` is no longer a thing that can happen, and whether `FLOOR_SLACK` survives. Do not start it before 2/3 is green.

  **Shipped, and the argument came out as predicted with one correction.** The count floors are NOT deleted and `FLOOR_SLACK` survives. What changed is which question the number answers: the no-single-root property belongs to `roots`, and the count now owns only the other quiet failure — a walk that came back implausibly SMALL with every root present (a narrowed glob, an extension filter that stopped matching, a widened skip list). No per-root check can see that, and none should; it is a question about the size of the result, which is what a floor is.

  **`holds` and `actionable` are two fields on purpose.** `holds` stays the arithmetic FACT, computed identically for every row, so a `declared_roots` row can report `false` and be in perfect health. `actionable` is the policy and the only thing the tool exits 1 for. Collapsing them would have meant a number that means different things per row, which is the ambiguity this module refuses everywhere else.

  **The suggestion drops `largestRoot + 1` once roots are declared, and that is the point rather than a side effect.** That term pushed floors UP as the tree grew, and a floor pushed up trips on ordinary shrinkage. Under the slack reading alone the six numbers barely move — 174, 48, 48, 48, 48, 535 — because the notes were already doing ~25% arithmetic and the largest-root term mostly did not bind. What it did do was TRIGGER re-measures. Nothing now goes red on ordinary growth.

  **Two places tell the reader the durable fix before the recurring one.** `formatFloorMeasurement` and the arithmetic case's failure message both say "declare this guard's roots" before "re-measure", and a case asserts that ORDER — re-measuring is the fix that has to be done again next quarter, and a tool that names it first schedules a chore instead of ending one. A `declared_roots` row sitting under its largest root also says so out loud rather than printing a silent `ok`: a reader who remembers the old rule would otherwise read it as the tool having stopped checking.

  **All six notes rewritten** to say what their number is for now, plus the module header, which had justified the floor with the `app/`-lost-but-`lib/`-kept example that is now the roots' job.

  Mutation-checked by putting `check-inline-radius` back on arithmetic and dropping its floor under `components/`: `MOVE` returns, offers declaring roots first, and both the arithmetic case and the vacuity-count case go red. The safety net for the next multi-root guard is real.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6286 tests, build.

## The third copy, and the tab that was answering its own question (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Action-row follow-ups" — "`app/friends.tsx` is a THIRD copy of these styles and nobody had filed it", written an hour earlier by the run that extracted the component and found the third copy while scanning for related defects. Taken immediately because the entry names the behaviour-preserving version explicitly, and because a third copy left in place is how the first two came back. -->

- [DONE: 2026-08-21] [est: 24000] **Give the friends screen the third surface it had been improvising** — `app/friends.tsx` rendered the same pills from a three-way ternary on the TAB (`"request" | "friend" | "following"`) with its own copy of `actions`, `primaryAction`, `primaryActionText`, `secondaryAction` and `secondaryActionText`. Not a third instance of the table — it branches on a tab, not a relationship — which is exactly why it had survived two extractions.

  **Every difference it had was defensible, and none of it was written down.** The friends tab offers chat and unfriend but no unfollow; the following tab offers unfollow but no add-friend. Both omissions are the tab declining to restate its own heading — a friend listed under "Friends" is not there to be unfollowed, and the following list is a tidying screen, not a prompt to befriend everyone in it. Those are good decisions that existed only as the absence of JSX. `ProfileSurface` gained `"tab"` and they are entries, with `EVERYWHERE` / `BROWSING` / `DETAIL_ONLY` / `DETAIL_AND_TAB` named for what they mean rather than for their members. Not one button changed, and a case asserts that by reading the three deleted lists back out of the table.

  **`getRelationship` would have compiled, passed, and changed what the following tab shows.** It answers `friend` before it answers `following`, so a friend the user also follows renders as a friend — while the tab lists everyone in `following` regardless. The screen therefore maps `kind` through an explicit `RELATIONSHIP_BY_TAB` record: the tab is the question being asked, so the tab decides. A case pins that `getRelationship` is not called here, comments stripped — the record's own doc block names it to explain why it is absent, and the previous run learnt this same lesson twice in one afternoon.

  **`SURFACE_RELATIONSHIPS` is the new half worth keeping.** A surface's shorter button list is only defensible if it is shorter because the surface serves fewer RELATIONSHIPS, and until now nothing said which ones a surface sees. `tab` sees three; `detail` and `row` see five. It is checked in both directions — a relationship a surface names must render something, and one it does not name must render nothing — which is what stops `tab` quietly growing a `request_sent` badge the friends screen has no list to put it in. It also fixed a case written an hour earlier: "has something to render on every surface except self" was true for two surfaces and became false the moment a third arrived, which is the kind of assertion that gets weakened rather than re-derived. Mutation-checked both ways: giving the following tab an add-friend button fails two cases naming the button and the tab, and giving `tab` a `request_sent` badge fails with "tab renders request_sent without naming it".

  **The friends screen's dispatcher throws on `follow`, for the same reason the people list throws on `chat`.** Every list on that screen is of people the user already has an edge to, so `follow` is unreachable — and a table edit that made it reachable should surface as a throw rather than as a button that does nothing.

  Three registries let `app/friends.tsx` go: it leaves `radius-pill-adoption` (its only `RADIUS_PILL` consumers were these buttons), drops to `list: false` in `spacing-gap-adoption`, and gives up four tokens in `design-tokens`. All three follow the precedent `<HeroBanner>` set — the note says where the token went, not that it vanished. No floors moved: no new file.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6237 tests, build. No DB migration, no creds, no new i18n keys.

## The same button in two different fonts, for as long as there have been two screens (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Relationship-table follow-ups" — "`styles.primaryAction` and friends are still duplicated across the two screens", filed by the run that extracted the decision table and named the presentation as the layer it had left behind. Taken because closing your own seam beats starting a new one, and because it turned out to be hiding a live visual defect. -->

- [DONE: 2026-08-21] [est: 22000] **Extract `<RelationshipActionRow>` so the profile buttons are rendered once, not twice** — the previous run moved the DECISION into `lib/relationship-actions.ts` and left the PRESENTATION in two files: `app/people.tsx` and `app/profile/[id].tsx` each declared `actions`, `primaryAction`, `secondaryAction`, `statusBadge` and the three label styles with the same values, then wrote the same map-and-render block underneath. The `<SheetShell>` pattern one layer down.

  **The two copies had already parted, and the difference was a font.** `app/people.tsx` set `fontFamily: FONT_BODY_EXTRABOLD` on all three label styles; `app/profile/[id].tsx` set none. So "Add friend" rendered in DM Sans ExtraBold in the people list and in the platform's default face — San Francisco on iOS, Roboto on Android — on the profile screen, and had done for as long as both screens have existed. Nothing here could see it: both files typecheck, both pass `lint:hex` and `lint:radius`, and the defect is one property PRESENT in one file rather than a wrong value in either. `app/friends.tsx` turns out to carry a third copy of the same styles and it agrees with the people list, so the profile screen was the single outlier and the merged style keeps the named font. This is the second consecutive task where extracting a duplicate found a live defect in one of the copies, which is the argument for the extraction rather than a bonus from it.

  **`children` is what keeps the admin button out of the table.** The profile screen renders "Delete profile" inside the same wrapping row when the viewer is an admin. It shares the row's layout and nothing else — it is a capability, not a relationship — so it goes through `children` rather than becoming a ninth `RelationshipActionId` that `app/people.tsx`'s dispatcher would then have to refuse. A case asserts the component's CODE contains no mention of admin, which is also the case that needed `stripComments`: the component's doc comment explains why the admin button is elsewhere, and a rule that fires on prose about itself is a rule that gets exempted rather than fixed. Same lesson the quotation guard learnt on the i18n file, and the second time this week.

  **The structural case that matters is the negative one.** `relationship-actions.test.ts` used to assert each screen called `relationshipActions(relationship, "detail" | "row")`; they no longer call it at all, they name a `surface=` and the component asks. Rewriting that case to check only `surface="detail"` would have left it passing for a screen that named its surface AND went back to mapping the list itself — so it now also asserts neither screen calls `relationshipActions(`, and a second suite asserts neither declares `primaryAction:` nor reads `action.kind`. Mutation-checked the font case by dropping one `fontFamily`: it fails with `2 !== 3` and names which three labels need it.

  **Four scanned floors re-measured, and it is the fifth time.** One new `components/` file drew `check-inline-radius` and `check-analytics-imports` level with `components/`'s own count (46); `check-a11y-jsx` and `check-clarity-input-mask` were raised alongside them at 48 rather than left at 46, because their notes say the alignment is deliberate and two floors that used to be one number become two numbers to re-measure the moment one moves alone. All four notes now point at the `.suggestions.md` entry arguing these be stated as a FRACTION of the walk (`minimum ≈ 0.75 × walkSize`) — five re-measurements, two of them caused by a single new component, is the property failing to be expressed rather than the tree misbehaving.

  Sixteen `<Pressable>`s across two screens became one component. `npm run verify` green: typecheck, `lint:all` 16/16, 6236 tests (+16), build. No DB migration, no creds, no new i18n keys.

## The decision table written out twice, and the one place it disagreed with itself (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Pressable-role follow-ups" — "`app/people.tsx` and `app/profile/[id].tsx` both write the relationship-action buttons once per branch" and "`app/profile/[id].tsx` writes the same four buttons five times", filed 2026-08-20 by two different runs. Picked from screen code after four consecutive i18n tasks. -->

- [DONE: 2026-08-21] [est: 21000] **Extract `lib/relationship-actions.ts` so the profile buttons are a table rather than ten ternary branches** — `app/profile/[id].tsx` had five relationship branches holding sixteen `<Pressable>`s and `app/people.tsx` five more holding ten, every one of them the same four lines differing only in label key and handler. A decision table with six inputs, written twice, comparable only by reading both.

  **They already disagreed, and nothing recorded whether that was deliberate.** A friend's DETAIL screen offers chat, unfriend and unfollow; a friend's ROW in the people list offers unfriend alone. That is a good decision — a list row is not the place for three buttons — but until now the only evidence for it was two blocks of JSX, and both readings were available to whoever looked next: "the row is missing two buttons" or "the detail screen has two extra". It is now `surfaces: ["detail"]` on two entries, and a case asserts `friend` is the ONLY relationship where the two surfaces differ, so a second divergence has to be chosen on purpose.

  **Intents, not function names.** `reject_request`, `cancel_request` and `remove_friend` are three ids mapping to one `removeFriend` call, because the social graph has one edge-removal operation and three things a person can mean by it. That is what lets the labels differ ("Reject" / "Cancel invitation" / "Remove friend") without three call sites deciding independently which function that implies — and it is asserted, because it is exactly the duplication somebody would "notice" and collapse. Each screen switches on `ACTION_METHOD[id]` rather than on the intent, so the mapping lives in the table; the people list's dispatcher THROWS on `chat` rather than falling through, since a silent no-op is how a table edit that gave a row a chat button would go unnoticed.

  `TranslationKey` is now exported from `lib/i18n-context.tsx` so the table can name a key it does not render. The import is type-only and therefore erased — that module pulls react-native and cannot be loaded by `tsx --test` — and it is what makes a typo'd label key fail in the table rather than at every call site. Sixteen `<Pressable>`s became nine and ten became four.

  **Two scanned floors had to be re-measured, and the pattern is now the finding.** One new `lib/` file drew `check-inline-hex`'s floor level with `lib/`'s own count (168), and one new test file drew `check-problem-phrasing-imports`'s level with `__tests__/`'s (450). Both were raised with `npm run remeasure-floors` (174 and 535) and both notes now say what the fourth re-measurement for the same reason means: these floors track repository GROWTH rather than guard coverage, and a floor expressed as a fraction of the walk would state the property directly instead of restating a number. That guard behaved exactly as designed — it noticed the moment its own number stopped proving anything — which is why the observation belongs in the note rather than in a complaint.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6220 tests (+17), build. No DB migration, no creds, no new i18n keys.

## The quote that was opened in Polish and closed in English (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Filter-family follow-ups" — "Nothing checks that a locale's quotation marks are its own" and "`search*` (14) is the next family". One task, because the guard is what protects the quoting decisions the previous run made by hand, and writing it found six defects nobody had filed. -->

- [DONE: 2026-08-21] [est: 20000] **Translate the search family, and check that each locale quotes with its own marks** — the search overlay was the awkward half-translated case: four of its eighteen keys (`searchClose`, both in-collection strings, the overlay a11y label) were declared in every locale and the other fourteen in none, so the SAME overlay showed a translated placeholder above English result headings. 14 keys × 4 locales closes it.

  **The guard found six real defects on its first run, none of them in a family this task touched.** Two `chatsEmptyHint` strings used Russian-style guillemets in Polish and German — `kliknij «Wiadomość»`, `tippe auf «Nachricht»` — which is what happens when somebody translates from the `ru` string and carries its punctuation across. And four marketplace message templates opened with the correct locale mark and closed with an **ASCII `"`**: `` `…ogłoszenie „${title}"…` ``. Inside a template literal that is perfectly legal code, which is exactly why it survived — the compiler has no opinion, and the user sees `„Tytuł"`. All six fixed.

  **The rule cannot be "no English quotes", and that is the whole design.** `de` closes with `“` and `pl` closes with `”`, so both English marks are legitimate somewhere in this file. What is checkable is narrower and exact: every curly mark inside a locale's block must be one of THAT locale's two. It still catches the paste — an English `“…”` in `de` brings a `”` that `de` never uses, and in `pl` brings a `“` that `pl` never uses. A second case counts openers against closers per locale, which is what catches the half-converted string where both marks happen to be legal for that locale; it is the case that found the four ASCII closers.

  **The suite reported its own explanatory comment first.** The header inserted above each locale's filter block named all four pairs — `«…» for be, „…” for pl` — so every mark appeared in every block and four locales failed on prose about themselves. Fixed by blanking comments through `stripComments` before scanning, and the comment now points at the suite instead of repeating it. Worth recording because a rule that fires on documentation of itself is a rule that gets exempted rather than fixed.

  **Fourth family, and `be` is now at 60.7%** (pl 58.9%, es 58.3%, de 58.1%) — from 46.4%/44.6%/44.0%/43.8% this morning. Floors re-measured at the usual ~8% slack (be 279, pl 271, de 267, es 268). Mutation-checked both new rules: an English `“` planted in `pl` fails naming the mark and the locale's own pair, and a `de` string with an ASCII closer fails with 12 openers against 11 closers.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6203 tests (+20), build. No DB migration, no creds, no new base keys.

## The error message you read while already stuck (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Complete-family follow-ups" — "`filter*` is the next family and it is the one that will test the harness", written by the run before this one, which named both hazards in advance: a function-valued key and four strings built around typographic quotes. Both turned out to be real, and one of them was a hole in the harness rather than in the translation. -->

- [DONE: 2026-08-21] [est: 19000] **Translate the item-filter family, and teach the family rule to read function-valued keys** — 19 of the 20 `filter*` keys were English in all four partial locales (`filterClearSearch` was the lone exception). The filter sheet opens from every list screen, and **eight of the twenty strings are validation errors** — the messages a user reads while already stuck, which is the worst possible moment to switch languages on them.

  **The typographic quotes were the predicted hazard and they were handled per locale, not pasted.** English writes `“Price from” must be a number.`; `ru` already used `«…»`. The four new locales use their own: `«…»` for `be`, `„…”` for `pl`, `„…“` for `de`, `«…»` for `es`. Pasting the English pair would have been invisible to every check in this repo and wrong on four locales' terms.

  **The function-valued key was the predicted hazard that turned out to be a hole in the HARNESS.** `filterActive` is `(params) => \`Filters (${params?.count ?? 0})\`` — the first function value to land in a listed family. `valueOf` read string literals only and returned `null` for it, so the verbatim-English check silently skipped it: a locale could declare `` `Filters (${…})` `` with the English word sitting inside what looks like code and satisfy every count in the file. The suite would have gone green on exactly the mistake it exists to catch, on the one key most likely to be copied wholesale.

  So the reader now also takes a function's template body. The interpolation is deliberately left in rather than stripped — it is identical across locales, so it neither hides a difference nor invents one, and stripping it correctly would need a second brace-aware walk. **And a new case asserts that the function keys ARE being compared**, because a reader that quietly went back to returning `null` would make the verbatim check skip them and stay green — the failure this whole paragraph is about, one level up. Mutation-checked both ways: an English `filterActive` in `pl` fails naming the key, and sabotaging the template reader fails with "is function-valued and is not being compared".

  **Third family, and the coverage numbers keep moving for the right reason.** be 57.9%, pl 56.1%, es 55.5%, de 55.3% — all four gained 19 more. Floors re-measured at the usual ~8% slack (be 266, pl 258, de 254, es 255). The `publishes the honest number` case reads 0 below half for the second run running; its caveat about the count being published rather than ratcheted is unchanged and still the important part.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6183 tests (+7), build. No DB migration, no creds, no new base keys.

## The translated body under the English heading (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Empty-state translation follow-ups" — "the next family to take is `filter*` (19) or `wishlist*` (14)", written by the run an hour earlier. `wishlist*` chosen over the larger `filter*` because that entry names a seam the previous task OPENED, and closing your own seam beats starting a new one. -->

- [DONE: 2026-08-21] [est: 18000] **Translate the wishlist family into `be`, `pl`, `de` and `es`, and make the all-or-nothing rule take a LIST of families** — the empty-state task translated `emptyProfileWishlistTitle` into four locales as "Спіс жаданняў" / "Lista życzeń" / "Wunschliste" / "lista de deseos" and left `wishlist` itself English. So those screens read a translated body under an English heading — worse than either half alone, because it looks like a bug rather than like missing work. 14 keys × 4 locales, using the same nouns the empty states settled on rather than picking again, which is the whole point of taking this family second.

  **Every locale is now at or above half, for the first time.** be 54.1%, pl 52.3%, es 51.7%, de 51.5%. The `publishes the honest number` case asserted 4 below half this morning, 3 after the empty states, and 0 now. That number is PUBLISHED, not ratcheted, and the case now says so: forty-one new English keys with no new `be` translations would put `be` back under half without anybody losing a translation, which is ordinary feature churn and precisely the failure mode `TRANSLATION_FLOORS` documents for a ceiling. Written into the comment so the next person updates the number instead of deleting the case. Floors re-measured with the usual ~8% slack (be 249, pl 241, de 237, es 238).

  **The suite generalised rather than being copied, which is the durable half.** `i18n-empty-states.test.ts` became `i18n-complete-families.test.ts`: `FAMILIES` is a list of `{name, pattern, size, because}` and a family joins it when it is complete in all six locales — explicitly not before, because a half-translated family listed here is a red suite that gets exempted, which is how a rule stops being read. `filter*` is a one-entry addition once it is done. Three things improved in the generalisation:

  - The vacuity guard is now an EXACT size rather than a floor. A `>=` would let a 15th key arrive quietly; exact means adding one forces the choice between translating it and taking the family off the list. Mutation-checked by planting a `wishlistShareHint` — it fails naming 15 against 14 and telling the reader both remedies.
  - The verbatim-English check no longer keys off a `Hint` suffix. It takes every base string of 25 characters or more, which is what a copy-paste actually reaches for; short labels can legitimately coincide between related languages, and it means the rule works on a family whose long strings are not named `*Hint`.
  - A new case rejects overlapping patterns, because two entries matching one key would report it under both names and a pattern like `/^wish/` would quietly annex a future `wishGranted`. `wishlist` itself needed `/^wishlist([A-Z]|$)/` rather than the empty family's `/^empty[A-Z]/` — the bare key IS a family member, which the first pattern's shape would have missed.

  `valueOf` returns `null` for a function-valued key and the doc comment now names the one that exists (`filterActive`), because the next family added here contains it and a reader that guessed would compare `null` to `null` and call two locales different. Mutation-checked in both directions; tree clean after. `npm run verify` green: typecheck, `lint:all` 16/16, 6176 tests (+5), build. No DB migration, no creds, no new base keys.

## The first screen a new account sees, in a language they did not pick (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Locked-chip composed-key follow-ups" — "`visibilityPrivate` still ships in English for pl/de/es". That entry turned out to be STALE (the 2026-08-20 chip task translated it in all six), and checking it is what produced the real finding: 268 base keys are English in all four partial locales. Picked deliberately from user-facing i18n after two infrastructure tasks. -->

- [DONE: 2026-08-21] [est: 20000] **Translate the whole empty-state family into `be`, `pl`, `de` and `es`, and stop it half-regressing** — the suggestion said the visibility chip still read "Private" under a Polish sentence. It does not; that was fixed the day before it was filed. What the check found instead is bigger and had no entry: `be`, `pl`, `de` and `es` each declare fewer than half the base map, and **268 keys are English in all four at once**. `ru` is effectively complete (499 of 501; the two are `appName`, a brand name, and `emailPlaceholder`, which is `you@example.com` — correctly untranslated, and worth recording as such rather than being counted as a gap forever).

  **Which 268 to take is the whole decision, and `empty*` is the answer with the best argument.** An empty state is what a NEW account sees on every tab, and a new account is exactly the user most likely to be reading in the language they just picked — the moment where falling back to English is least excusable and most likely to be the user's first impression of the app. The family is also complete at 24 keys with no partial rows, which is what makes the enforcement below possible here and not for the map at large. 96 strings written, verified against each locale's own register and vocabulary rather than translated in isolation: `be` and `de` take the вы/Sie-style plural imperative their neighbouring strings use, `pl` and `es` the informal second person theirs use, and the item noun follows each locale's existing `itemsCount` (`прадметаў`, `przedmiotów`, `Objekte`, `objetos`) rather than a fresh choice.

  **`be` crossed half — the first time that number moved because something was translated.** 51.3%, up from 46.4%. Every previous move of the partial-locale percentages in this file came from DELETING dead English keys, which raises the ratio without a user seeing one more word in their own language. The `publishes the honest number` case asserted "four of the six locales sit below half" and now asserts three, with that distinction written into it. All four floors re-measured with the usual ~8% slack (be 236, pl 228, de 224, es 225); `en`/`ru`'s floor NOTES were also corrected from 502/500 to 501/499, which is not a floor change — the prose had gone stale against its own measurement.

  **`__tests__/i18n-empty-states.test.ts` gives this family the rule the general case cannot have.** `TRANSLATION_FLOORS` is deliberately a floor on the total and not a ceiling on what is inherited, because a new English string ships before its five translations and a ceiling would go red on every feature PR. That is right for the map and says nothing about any family: a locale can lose eight of these twenty-four and stay far above its floor, with no symptom except a Polish user reading English. So the family is pinned all-or-nothing — a count would let the next person add a 25th English key and leave it forever. Two failure modes, not one: a missing declaration, and a declaration carrying the English text verbatim, which satisfies the coverage counter exactly as well as a real translation does. A fourth case guards the second, which compares two reads and would pass if BOTH came back null. The family is located through `localeKeys` rather than a fresh slice of the file — that reader exists because the hand-rolled ones found `deleteItem:` inside templates that merely mentioned it.

  Mutation-checked both ways: deleting `be`'s `emptySubscribedCta` fails naming the locale and the key, and pasting the English `emptyPeopleHint` into `de` fails naming it as verbatim. `npm run verify` green: typecheck, `lint:all` 16/16, 6171 tests (+4), build. No DB migration, no creds, no new base keys — this makes copy that already existed readable by four of the six shipped languages.

## The keys were checked and the values were not (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Clamp-and-control follow-ups" — "`premium_upsell_shown` now has three props and nothing constrains their VALUES", written an hour earlier by the run that added the third prop. Picked deliberately from analytics rather than the visibility row a third time. -->

- [DONE: 2026-08-21] [est: 21000] **Give the closed-set analytics props a table of allowed values, and enforce it on the same policy the key check uses** — `assertValidProps` has always validated the payload's KEYS against `ANALYTICS_EVENTS[name].props`: dev throws, production warns and strips. The values under those keys were free strings, and the value is the more expensive of the two to get wrong. An unknown key makes a column the schema doc has never heard of and nobody has built a report on. `source: "collectionEdit"` makes a second bucket inside a column somebody HAS, splitting a funnel that is then read as a number that looks too low — the failure mode where nothing looks broken and the conclusion is wrong.

  **`lib/analytics-prop-values.ts` holds eleven sets, and none of them is a hand-written list.** Each is built by `closedSet<T>(members: Record<T, true>)`, which is exhaustive in BOTH directions: a member the union gained and the table did not is a missing property, and a member the table has and the union does not is an excess property. A plain `readonly T[]` would happily accept a table listing two of a union's five members, which is exactly the drift the module exists to prevent. `MarketplaceMode`, `CollectionVisibility`, `CurrencyValueError`, `AnalyticsRelationship`, `PremiumIntentSource`, `PremiumUpsellControl` and `AppLanguage` all bind this way — the last through a type-only import, because `lib/i18n-context.tsx` pulls react-native and cannot be LOADED by a node CLI, while its types cost nothing at runtime.

  **What is deliberately NOT constrained is the load-bearing half.** Ids (`collectionId`, `targetUserId`), counts (`photoCount`) and anything from outside the app (`provider`, whatever Supabase reports) have no set: a table that has to be exempted is a table that stops being read. Booleans have none for a different reason — `AnalyticsProps` already types them, and `[true, false]` would restate the compiler. A vacuity case guards the whole feature, because every other case here passes trivially if `values` is absent everywhere, which is what a bad merge leaves behind.

  **`source` is per-event, not per-prop-name, and that is the design.** `premium_activated.source` may be `server_sync` (the cloud restored an entitlement); `premium_upsell_shown.source` may not, because nobody SAW a gate. One global table keyed by prop name would have to allow it for both, so the sets live inside each event definition and a case states the difference by exercising both.

  **The production policy is deliberately asymmetric with the key check, and the reason is stated as a case.** An unknown key is dropped — nothing was built on it. An out-of-set VALUE is KEPT, because it sits under a key a report already reads and dropping it would delete what actually happened on the grounds that the taxonomy is out of date, leaving a breakdown whose buckets no longer add up to the event count. That is a harder thing to notice than one extra bar. Dev throws either way, so a kept value only ever means one that reached production without anybody running the dev build.

  **Three things the tables caught on the way in, none of which was a typo.** `signup_completed.method` has a legitimate THIRD value: `simulateSignupEvent()` fires `method: "manual_test"` to check the PostHog wire end-to-end, and it runs in dev — the runtime where an undeclared value throws. Declared, with the reason, rather than exempting the call site and leaving a `method` breakdown unexplained. `__tests__/analytics-validate.test.ts` had used `mode: "sale"` as its fixture in nine places — the wrong spelling of `"sell"`, in the suite that exists to check payloads. And `listing_created`'s description told the report builder `mode` = sale/trade/swap; two of those three values have never existed. The rendered set beside each key is now generated from the type instead of remembered, which is the durable half of that fix — `docs/powerbi-connection.md` shows `` `mode` (`sell` | `trade`) `` and the sets are sorted so reordering a union does not rewrite the doc.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6167 tests (+22), build. No DB migration, no creds, no i18n keys. Verified no live call site sends an out-of-set value — every one of them passes a typed union through (`listing.mode`, `relationshipForAnalytics(...)`, `useI18n().language`), which is why the dev throw ships without breaking a screen.

## The third spelling of one rule, and the two buttons the funnel could not tell apart (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Dead-branch follow-ups" — three entries from yesterday's run: "the same `visibility === "private"` shape guards the save-time clamp and nothing says whether THAT one is reachable", "`premium_upsell_shown` now fires from two controls per screen and the event cannot tell them apart", and "the locked hint is a `<Pressable>` with no pressed state and no hit-slop". One task because all three are the same row, and the first two are both about a decision that gets read off it. A fourth entry in the group — "three screens now decide is private locked and the third is the marketplace listing sheet" — was checked and is wrong: `app/item/[id].tsx` gates the LISTING CAP on premium, not visibility, and shares no rule with these two. -->

- [DONE: 2026-08-21] [est: 15000] **Route the save-time clamp through `isPrivateVisibilityLocked`, tag the upsell with which control fired it, and make the sentence feel like a button** — yesterday's task made one rule out of the two visibility ROWS and left the third copy where nobody was looking: `handleSaveEdit` in `app/collection/[id].tsx` wrote `!isPremium && editVisibility === "private" && (collection.visibility ?? "private") !== "private"` for itself. Same three clauses, a fourth screen away from the chip they have to agree with, and free to drift in exactly the way the two rows already had.

  **First, the question the suggestion said nobody could answer: the clamp is unreachable, deliberately.** `openEditModal` seeds `editVisibility` from the collection, and the sheet's locked chip calls the upsell and returns before changing it, so a free user cannot arrive at the save having selected "private" on a public collection. That makes it defence in depth rather than dead code, and it is kept — the day the chip is bypassed it is the only thing between that bypass and a private collection on a free account. What it is not is a place to restate the rule, so it is now `clampVisibilityForSave(isPremium, editVisibility, collection.visibility ?? "private")` in `lib/premium-helpers.ts`, one branch over `isPrivateVisibilityLocked`. Being unreachable at runtime is precisely why it needed pinning: nothing exercises it, so a regression to a fourth hand-written spelling would look like working code forever. An exhaustive case walks all four `(isPremium, saved)` combinations and requires the clamp and the chip's lock to agree, because the two disagreeing is the actual bug — a chip that refuses a change the save would have allowed reads as broken, and a chip that allows one the save silently reverts is worse. The argument order is asserted in both directions: `selected` and `savedVisibility` are both `CollectionVisibility`, so swapping them type-checks and clamps backwards in silence.

  **Second, the event that could not tell its two senders apart.** The chip and the sentence beside it are the same button, so they send the same `feature` and the same `source` — which meant `premium_upsell_shown` had no way to say which one a person pressed. That matters because the standing `[needs-data]` decision further down this file is specifically about the CHIP: with the two merged, taps on the SENTENCE would keep it alive forever, and hiding it would look like it cost traffic it never had. `control: "chip" | "hint"` is now a required argument of each screen's `showPrivateUpsell`, so a new call site cannot forget it, and it is registered in `ANALYTICS_EVENTS` — `assertValidProps` strips an unlisted key in production and throws on it in dev, so a payload the registry does not name is lost rather than wrong. A case asserts both literals appear in both files: forwarding the argument is worth nothing if both call sites pass `"chip"`, which is what a copy-paste lands on, and the funnel would then look exactly as it did before with a name on it.

  **Third, the sentence now behaves like the control it became yesterday.** It was a 20px-tall line of text with no hit-slop and no pressed state — the shape most likely to feel broken on a phone. `hitSlop={8}` is this tree's existing answer (eight other controls use it) and the pressed style is opacity rather than a colour swap, so the amber that marks it as pressable survives the press. Both halves are asserted, because either alone still leaves it feeling dead.

  `docs/powerbi-connection.md` is regenerated rather than edited — `lint:powerbi-doc` caught the drift the moment the registry gained a prop, which is the guard working. `npm run verify` green: typecheck, `lint:all` 16/16, 6145 tests (+9), build. No DB migration, no creds, no new i18n keys.

## The sentence explaining the padlock, which nobody could ever see (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Locked-chip composed-key follow-ups" — "The upsell fires on the CHIP but not on the visible hint below it". Investigating it found the hint was not merely inert, it was unreachable. Picked deliberately from a user-facing area after three infrastructure tasks. -->

- [DONE: 2026-08-21] [est: 17000] **Make the premium-only visibility hint reachable, and make it the same button as the padlocked chip** — the suggestion said a sighted user reads "Private collections are a premium feature" as body text under the visibility row and taps it, and nothing happens. The first half turned out to be wrong in a more interesting way: **the sentence never rendered at all**. Both screens chose the hint with `!isPremium && visibility === "private"`, and a free user can never reach that state — the locked chip calls the upsell and returns BEFORE `setVisibility`. So the branch that explains the padlock was dead in `app/create-collection.tsx` and in `components/edit-collection-modal.tsx`, in all six languages, and the only place the sentence ever appeared was a toast that has gone by the time somebody looks back at the row.

  **What ships:** the hint under the row now always describes the CURRENT selection, and when the private option is locked a second line appears beside it — amber and bold, so it reads as something to press next to a sentence that is not — carrying the premium explanation and firing the same upsell the chip does. Adding a line rather than swapping one: choosing a single sentence for both jobs is exactly what made the explanation unreachable, and replacing "public collections are visible to everyone" with "private is premium" would trade away the useful half for the user who is actually looking at a public collection. The a11y label is the one already written for the locked chip.

  **The rule moved to `lib/premium-helpers.ts`, which is where the drift showed.** Both rows had written the lock out for themselves and the copies already disagreed: create locked on `!isPremium` alone, edit on `!isPremium && (savedVisibility ?? "private") !== "private"`. `isPrivateVisibilityLocked(isPremium, savedVisibility)` is now one rule, and the part that genuinely differs stays at the call site as a decision each screen makes: create passes `"public"` (a collection being created has no private history to protect) and the sheet passes `savedVisibility ?? "private"` (the value can be momentarily unknown while the collection loads, and treating unknown as private errs toward not locking an owner out of their own sheet). Both defaults are asserted, because swapping them is silent — one locks every free user out of nothing, the other unlocks the gate entirely.

  `__tests__/premium-visibility-lock.test.ts` exercises the rule by CALLING it, including the lapsed-subscriber case that was the reason it takes the saved visibility rather than the selected one, and pins one thing the refactor could quietly break: moving the upsell body into a named `showPrivateUpsell()` per screen is exactly where two call sites become one analytics event, so the distinct `source: "create_collection"` / `"collection_edit"` is a case — the standing "hide the locked chip once PostHog shows nobody taps it" decision is waiting on that funnel. Three existing suites pinned the old inline expressions and now pin the new shape; `edit-collection-modal`'s string sweep checks the two per-selection keys one level down, in the helper that names them.

  `npm run verify` green: typecheck, `lint:all` 16/16, 6136 tests (+10), build. No DB migration, no creds, no new i18n keys — this makes copy that was already translated into six languages visible for the first time.

## The scanner that was copied without its careful half (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Pressable-role follow-ups" — "`interactivePressables` is the fourth open-tag scanner in this repository". Three of the four it named are gone (the sweep suites were deleted when the sweep hit zero), and the run an hour earlier wrote a new one, which is what made the entry actionable rather than historical. -->

- [DONE: 2026-08-21] [est: 15000] **Extract `lib/jsx-open-tag.ts` so there is one answer to "where does this tag end"** — the standing suggestion counted four copies of the same JSX open-tag walk and said three handled brace depth identically because they were copied from each other. The count is what changed: the sweep suites that held two of them were deleted when the a11y sweep reached zero, leaving `lib/check-a11y-jsx.ts`'s private version as the only careful one — and then the sheet-Escape task an hour ago wrote a fresh copy that had the brace counter and NOT the string-literal skipping. That is the whole argument for extracting, demonstrated rather than asserted: the reasoning was written down in one file's doc comment, the next person needed the same walk, and the half they left out is the half that is silent when it is wrong.

  **What moved and what deliberately did not.** `skipStringLiteral`, `openTagEnd` and `closeTagIndex` moved verbatim out of the guard into `lib/jsx-open-tag.ts`, keeping the reasoning that came with them — the naive `/<Pressable([\s\S]*?)>/` ends the tag at the first `>` in the file, which here is usually the one inside `onPress={() => x}`, and on the a11y sweep it reported ten offenders where the careful version reports three: seven false positives, every one a button that DID carry a label in attribute text the regex never reached. What stayed in the guard is its tag STACK — tracking ancestors so a node inside a hidden subtree is not asked for props of its own is a rule about that guard's domain, not a primitive. The module is explicitly not a parser: callers ask about a tag they have already located and get back text they still have to interpret.

  Two readers were promoted rather than moved, because the sheet suite needed the inverse question and its versions were the weaker ones. `openTagAt(source, at)` reads back from an attribute offset to the whole tag that carries it; `attributeValue(tag, name)` reads the expression inside `name={…}`, brace- AND string-aware, whitespace flattened so a prop the formatter wrapped across three lines compares equal to the same handler written inline (there is no prettier here, so both spellings genuinely exist). Two decisions in it are stated as cases rather than comments: the name is matched only where a name can START, so asking for `label` does not match `accessibilityLabel`; and the string form (`accessibilityRole="none"`) returns null rather than its text, because a caller that conflated the two would be comparing a value against the expression that produces it. The sheet suite also got more precise in the swap — it now reads the Modal's own open tag instead of its whole span, so an `onRequestClose` on a nested element can no longer stand in for a Modal that has none.

  `__tests__/jsx-open-tag.test.ts` states each mistake the module prevents as its own case, which is what the old arrangement could not do: covered only through the guard's findings, a case here was really a case about `<Pressable>` and `accessibilityLabel`, and the rule ("a `>` inside an attribute does not end the tag") was never said on its own. Eighteen cases: a `>` in an arrow, in a string, in a template; an unterminated tag and an unterminated literal returning the honest answer rather than a guess (these scanners read files mid-edit, and one that threw would turn a lint run into a crash); a nested same-name close tag; the truncated-arrow shape; a brace inside a string; both ends of the name-boundary rule.

  Verified behaviour-preserving where it counts: planting an unlabeled icon-only `<Pressable>` into `components/currency-sheet.tsx` still fails `lint:a11y-jsx` with all three of its codes (`no_role`, `unlabeled`, `undecided_icon`) at the right line, and the tree reports clean again on revert. Worth recording from that check: the first plant landed inside a doc comment and was correctly ignored — the guard blanks comments through `stripComments`, which is a feature working, not a miss. `npm run verify` green: typecheck, `lint:all` 16/16, 6126 tests (+18), build. No DB migration, no creds.

## The dependency the a11y argument rests on, and the key that was never compared (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Verified-negative follow-ups" — two entries from the run that wrote the sheet suite: "`react-native-web`'s modal behaviour is now load-bearing and unpinned" and "Nothing checks that a sheet's `onRequestClose` actually closes it". They are one task because the first is what makes the second's Escape key work at all. -->

- [DONE: 2026-08-21] [est: 14000] **Pin what `react-native-web`'s `<Modal>` provides, and check every sheet's Escape agrees with its backdrop** — `sheet-modal-semantics` argues that eleven sheets need no `accessibilityViewIsModal` because they sit inside a `<Modal>`. On the platform most of this app's users are on — the web build on GitHub Pages — "inside a Modal" means whatever `react-native-web` decides it means, and that was read out of `node_modules` by a person once and then written into a doc comment, which is where a fact about a dependency goes to stop being checked. An upgrade that dropped the focus trap would change none of the eleven files and leave nothing looking wrong; the sheets would simply stop being modal for a screen reader, and the suggestion the whole suite exists to refute would become correct at last.

  `__tests__/rnw-modal-premise.test.ts` is the cheap version of what `lint:sentry-version` does for the Sentry SDK: it reads the SHIPPED `exports/Modal/*.js` (0.21.2 today) and asserts the three separate things the argument uses — `"aria-modal": true` and `role: active ? 'dialog'` on the content, `ModalFocusTrap` both imported AND rendered around the content (a trap that ships but is no longer wired is the regression that leaves the file in place and the behaviour gone), and Escape asserted in two halves, the `e.key === 'Escape'` listener and the `onRequestClose()` call, because either alone is a different regression with the same symptom. Every message names the installed version, so a failure points at what to go and read rather than at itself. It fails CLOSED on a missing file — an upgrade that renames or bundles these away is the single likeliest way for the premise to change, and a `try`/`catch` returning `""` would turn all six cases into silent passes.

  **The second half is what that pinning makes assertable.** Escape calls `onRequestClose`; a backdrop tap calls the backdrop's `onPress`. Two props, the same intent, and nothing had ever compared them — a sheet whose keyboard dismissal does something other than its tap dismissal is a small, very confusing bug that ships because both halves look right on their own line. The new case reads both out of every Modal span that contains the sandwich and requires them equal. All eleven agree today, so this ships green; it is the drift that is being stopped. Two readers were needed and both are brace-aware for a concrete reason: `=>` appears in nearly every handler here, so `indexOf(">")` truncates `onPress={() => setOpen(false)}` at `setOpen(false` and would then compare a handler unequal to itself written inline. Whitespace is flattened so the formatter wrapping a prop across three lines does not read as a mismatch.

  Deliberately NOT reported: a backdrop with no `onPress` at all. The wishlist promote sheet is one — Escape closes it, a tap does not. That is an asymmetry rather than a contradiction, and a rule that fires on it would be exempted and then stopped being read. A vacuity case guards the pair, because the comparison passes trivially if both readers return null everywhere: at least ten files must yield BOTH handlers.

  Mutation-checked both ways rather than assumed. Pointing `components/currency-sheet.tsx`'s `onRequestClose` at a different function fails naming the file and both handlers; flipping `"aria-modal": true` to `false` inside the installed `ModalContent.js` fails naming the version and what it costs a screen reader. `npm run verify` green: typecheck, `lint:all` 16/16, 6108 tests (+8), build. No source change — this task's product is two premises that can no longer rot quietly. No DB migration, no creds.

## The public ID that changed every time the screen recomputed (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Slug-walk follow-ups" — two entries that turned out to be one finding: "the wall-clock suffix is still 13 digits and now has a seam to fix it behind" and "the uniqueness walks are O(n) per candidate". Chasing the first found a live bug the entry did not mention. -->

- [DONE: 2026-08-21] [est: 16000] **Seed the anonymous profile-ID suffix on the account ID instead of the wall clock, and make both uniqueness walks linear** — the suggestion asked for a shorter suffix because `collector-1755823419233` is an identifier the app asks a person to SHARE. The length is the smaller half. `String(Date.now())` returns a DIFFERENT value on every call, and both call sites that reach it sit inside the `profiles` memo in `lib/social-context.tsx` — which recomputes on a language change, on an arriving friend request, on any change to the profile list. So a user whose display name and email local part are both non-ASCII (a Cyrillic name with a Cyrillic address is not exotic here — `ru`, `be` and `pl` are three of the six shipped languages) was handed a **new public ID every recompute**. Nobody could find them by the ID on their own screen, and whichever value happened to be current at save time is what `upsertMyProfile` wrote into the cloud row.

  **The fix keeps the one property the clock had.** `slugSuffixFromSeed` is FNV-1a over a seed, truncated to 30 bits and rendered base-36, so it is exactly six characters and always inside `[a-z0-9]` — the alphabet `slugifyProfileId` would otherwise strip, and the suffix is appended AFTER slugification, so a `-`, `_` or `+` in the token would survive unstripped into a URL. `stableSuffix(user.id)` is passed at all three call sites (`buildFallbackProfile` and both walks in `updateMyProfile`), and `normalizeProfile` defaults its suffix to one seeded on the profile's own `id`, which is what makes the function deterministic for every profile the app holds rather than only for the signed-in one. Two accounts with equally unslugifiable names still get different IDs — that is the collision the clock existed to prevent — and `ensureUniquePublicId` resolves the ~1-in-1e9 case where they do not. The hash is one-way on purpose: the seed is the auth UUID and the output goes in a shared URL, so a case asserts no chunk of the account ID appears in the result.

  **Existing users are untouched**, which is why there is no migration: `normalizeProfile` prefers a stored `publicId`, so anybody who has ever saved a profile keeps theirs. The change only reaches profiles that had nothing to slugify — the ones whose ID was never stable in the first place.

  **The walks, in the same pass.** Both re-scanned the whole profile list on every iteration (`profiles.some(...)` inside a `while`), so a name shared by k people cost k full passes. `takenSlugs` reads the list once into a `Set` and `walkToFreeSlug` walks it — identical answers, because neither walk mutates the list. Three cases guard the rewrite where it could differ rather than where it is obvious: a 200-collision run (an off-by-one at scale), a profile whose slug is `""` (normal here — `coerceProfileRow` turns every NULL column into `""`), and a self id appearing on more than one row (the shape that catches a `find`-style rewrite of the exclusion).

  `__tests__/profile-slug-uniqueness.test.ts` grew four describes, including the regression stated as directly as it can be: normalize one profile twice, no injected suffix, assert the two public IDs are equal. With the clock underneath, that failed whenever the two calls landed in different milliseconds. The URL-safety case pins the constraint the two slug alphabets exist to satisfy — `encodeURIComponent(id) === id` over a battery of hostile names — where it can be checked, rather than as a comparison against `slugifyUsername`, which is a second function that could drift the same way. `__tests__/fallback-profile-locale.test.ts` had pinned the old call text exactly and now pins the new one, seed argument included, because omitting it is precisely how the wall clock comes back. `npm run verify` green: typecheck, `lint:all` 16/16, 6100 tests (+13), build. No DB migration, no creds.

## The suggestion that was wrong, and the invariant that made it wrong (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md — "Sheet-sandwich follow-ups" and "Pressable-role follow-ups", where the same claim was filed three times by three different runs. Picked deliberately from a different area than the last six tasks. The outcome is not the one the entry asked for. -->

- [DONE: 2026-08-21] [est: 12000] **Do NOT add `accessibilityViewIsModal` to the sheets — verify the claim first, then pin what makes it unnecessary** — the standing suggestion, filed three times, read: "`accessibilityViewIsModal` is set on none of the nine — a sheet is drawn over the screen and a screen reader can still walk out of it into the content behind. That is the actual modal bug; the role work does nothing about focus escaping." It is wrong, and the reason it is wrong is instructive: all three filings were written while sweeping `accessibilityRole` across the JSX of the sandwich, and none of them looked at what the sandwich is nested IN.

  Every sheet in this tree renders inside React Native's `<Modal>` — eleven files, checked one at a time. A `<Modal>` is not a positioned `<View>` on any of the three platforms: iOS presents a separate view controller, which UIKit scopes VoiceOver to; Android renders a Dialog, which is its own window for TalkBack; and `react-native-web` — read in `node_modules` rather than assumed, which is the step that settled it — renders `ModalContent` with `aria-modal: true` and `role="dialog"`, wraps the children in `ModalFocusTrap` (it loops Tab inside the modal and pulls focus back when it escapes), and closes on Escape. So the prop the suggestion asked for would have been dead weight in eleven files, and "the Android half" it called the harder problem does not exist either.

  **What shipped instead is the property that makes it unnecessary, asserted.** `__tests__/sheet-modal-semantics.test.ts` finds every sheet by its fingerprint — the two `accessibilityRole="none"` opt-outs no other shape in this tree uses twice in one file — and requires each to sit inside a `<Modal>` span. The day somebody builds a sheet as a bare absolutely-positioned overlay, which has no focus trap, no Escape-to-close and no `aria-modal`, the suggestion becomes correct and a named case says so rather than nobody noticing. Two more cases guard the guard: the opt-outs must come in PAIRS (a backdrop and a card; an odd count means one of them has quietly become a control again), and a case that fails if `accessibilityViewIsModal` is ever added to a sheet — which exists precisely so the suggestion is not implemented a fourth time by somebody who reads the entry and not this note. The `modalSpans` helper fails CLOSED on a nesting it does not expect: a too-small span reports a sandwich as outside a Modal rather than waving one through.

  Also pinned: the walk finds at least ten files. A sweep that matched nothing would make every per-file case vacuously true, which is the failure the whole repository's floors exist for and the easiest one to ship in a suite like this.

  Mutation-checked both ways. Rewriting `components/currency-sheet.tsx`'s `<Modal>` into a `<View>` fails naming the file and what a positioned overlay costs; adding the redundant prop to the same sheet fails the case written to stop it. `npm run verify` green: typecheck, `lint:all` 16/16, 6087 tests (+15), build. No source change at all — this task's product is a verified negative and a test. No DB migration, no creds.

## The rule for the leak, not just the fix for it (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Public-ID privacy follow-ups", top entry — filed an hour earlier by the run that fixed the leak itself, which is the right split: the fix was one removed term and belonged in its own review; the rule that stops the next one is a different change with its own registration surface. -->

- [DONE: 2026-08-21] [est: 19000] **`check-profile-id-pii`: fail when an email address reaches a public profile identifier** — the previous task removed one `|| profile.email` from `normalizeProfile`'s `publicId` chain, and nothing stopped the next one. This is the sixteenth guard, written the moment the count reached zero, which is the only time a rule like this can be written — the same pattern `check-a11y-jsx`'s `no_role` and `undecided_icon` followed, and the reason the standing suggestion said the orphan guard had to wait for the families to be removed first.

  **Which fields, and the one deliberately left out.** `publicId` / `public_id` and `username` are covered: both are identifiers a person shares, is searched by, and does not choose at sign-up, and both are written back to the cloud row so a bad value sticks. `displayName` is NOT covered, and that is the decision worth recording: deriving a display name from an email's local part is INTENDED here — somebody signing up as `ada@example.com` should be called "ada" rather than "Collector", and `resolveFallbackIdentity` does exactly that. A rule covering `displayName` would fire on live, reviewed code, and a rule that fires on correct code is one somebody exempts and then stops reading. The exclusion is a case in the suite rather than only a comment.

  **The seam, which is the interesting part of the rule.** It matches `email` as a WHOLE WORD, so `emailName` and `userEmail` pass. That is not a gap it fails to close — it is the line it is drawing. An address's local part does legitimately seed a username, and in this tree it does, via `emailName` → `slugSeed`: a named intermediate somebody had to write down and a reviewer can see. What the rule is actually about is a raw address reaching an identifier INLINE, where nobody has weighed which part of it survives — which is exactly the shape the bug had.

  Two implementation details that came from the bug rather than from taste. The value reader is brace- and quote-aware because the offending expression spanned three lines inside a call whose own arguments were comma-separated: both "read to the next comma" and "read to end of line" truncate it before the term that mattered, so a simpler scanner would have reported the tree clean. And the planted-offender fixture in `guard-report-paths` is multi-line for the same reason — a single-line plant would be found by a scanner this guard has already outgrown.

  Registered the full way rather than as a suite: `LINT_GUARDS`, `SCANNED_FLOORS` (floor 200 over the same five-root walk `check-orphan-i18n-keys` takes, 264 files, riding above the 165 `lib/` alone contributes — and `lib/` is where every profile-shaping function lives, so a walk that had quietly lost it would report a clean tree while reading none of the code the rule is about), `package.json`, plus the two registry meta-guards: a planted-offender case in `guard-report-paths` and a partial-root fixture in `lint-guard-partial-root`. CI needs no change because it runs `lint:all`.

  Verified end to end against the real thing, which is the only verification that means anything here: putting `|| profile.email` back into `lib/social-helpers.ts` exits 1 and prints `lib/social-helpers.ts:357  publicId: slugifyProfileId( profile.publicId || … || profile.email, suffix, )`, and the tree reports clean again when it is reverted. 13 cases in the guard's own suite, built around the two things the bug proved it has to get right. `npm run verify` green: typecheck, `lint:all` 16/16, 6072 tests (+30), build. No DB migration, no creds.

## The email address that became a public profile ID (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Slug-walk follow-ups" — the entry noted that `normalizeProfile` was the last pure function behind the React Native import wall AND that it had "a privacy-shaped decision sitting in a `||` chain". Extracting it was the task; the chain turned out to be a live leak. -->

- [DONE: 2026-08-21] [est: 18000] **Stop deriving a public profile ID from the email address, and extract the last function behind the wall** — `normalizeProfile`'s `publicId` fallback chain read `profile.publicId || profile.username || profile.displayName || profile.email`. That last term is the finding, and it is reachable rather than theoretical: `coerceProfileRow` in `lib/supabase-row-coerce.ts` turns every NULL column into `""`, so a cloud row with no `public_id`, no `username` and no `display_name` but a real address arrives as a profile whose only non-empty name is the email — and comes out with `ada-lovelace-example-com` as its public ID.

  What makes it worth a task rather than a note: `publicId` is not internal. It is the identifier the app invites people to SHARE so others can find them (the removed `profileIdHint` key read "Use this ID so other people can find your profile"), `lib/supabase-profiles-shapes.ts` writes it to the cloud row as `public_id` on every `upsertMyProfile`, and it is in `PROFILE_COLUMNS` on read. So an address-derived ID does not stay local — it is written back, shared, and searchable, and both the local part and the domain survive slugification. The chain now ends at the display name; a profile with nothing to name it gets the anonymous seed, which is what `slugifyProfileId` already does with an empty string, so the fix removed a term rather than adding a branch.

  `normalizeProfile` moved to `lib/social-helpers.ts` in the same pass, which is the reason the branch had never been exercised: the function sat behind React Native peers `tsx --test` cannot transform, so nothing had ever called it. It is the last of the four extracted this evening. Two behaviours were deliberately preserved and are pinned so a later tidy has to argue with them: the `username` fallback is the bare seed with NO suffix (several nameless profiles share the username `collector`; adding a suffix would rename profiles already stored under it, and `ensureUniqueUsername` is what resolves a clash when somebody actually picks a name), and the `publicId` preference order — chosen ID, then username, then display name — is asserted at each step rather than only at the end.

  **The remediation is a data fix and it is written down rather than done.** The code stops producing these IDs; rows written before it still carry whatever was derived earlier. `MANUAL-TASKS.md` gains a section with a read-only SELECT that finds affected rows by comparing each `public_id` against its own email slugified the same way the app did, and a deliberately unqualified-refusing UPDATE. It says out loud not to blank these silently: a cleared `public_id` breaks any profile link already handed out, so contacting the affected users and letting them pick a new ID is the preferred path and clearing is for unused rows.

  Mutation-checked on the leak itself, both shapes: restoring `|| profile.email` fails 2 cases naming the fragment that escaped ("'ada' from the email address reached the public profile ID: ada-lovelace-example-com"), and so does the careless partial fix that leaks only the local part — which is why that case asserts the fragments one at a time rather than comparing the whole string against the seed. Two wiring assertions from the previous two tasks pointed at `lib/social-context.tsx` and were repointed rather than deleted, since the functions have been migrating between the two files all evening; the "no hand-written copy of the fallback word" claim now checks BOTH files. `npm run verify` green: typecheck, `lint:all` 15/15, 6042 tests (+5), build. No schema migration; one data remediation, documented. No creds.

## Two loops nobody had ever run (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Pure-extraction follow-ups" — the top entry, filed by the run below after it extracted the first pure function out of the same file and noticed three more behind the same wall. -->

- [DONE: 2026-08-21] [est: 14000] **Extract `slugifyProfileId`, `ensureUniquePublicId` and `ensureUniqueUsername`, and give their collision paths a first test** — all three sat in `lib/social-context.tsx`, behind React Native peers `tsx --test` cannot transform, so nothing had ever executed them. That includes two `while` loops that walk `-2`, `-3`, … looking for a free slug — untested at any suffix — and a last-resort branch that could not be reached deterministically at all, because it stamped `Date.now()`.

  The clock is an injected `UniqueSuffix` now, defaulted to the wall clock so no call site changes, which gives the branch a fixed value to assert. It is not configuration anybody wants and the doc comment says so. The `profiles` argument became a structural `{ id, publicId?, username? }` rather than `UserProfile`, so a fixture is three fields instead of eight and the walks cannot quietly start depending on something else about a profile. `slugifyUsername` came out of `ensureUniqueUsername`, where it had been inlined — the two slugifiers stay separate functions rather than one with a separator parameter, because a public ID appears in a URL and a username does not, and a case pins that they disagree on both the separator and on what happens to an underscore that was already there.

  **The case worth having most** is the `selfId` guard: without it, re-saving a profile without changing its ID reads as a collision with itself, so editing a bio would walk somebody from `ada-lovelace` to `ada-lovelace-2` and again on every save. It had no test. It has two now — one asserting the guard holds, one asserting the same call WITHOUT `selfId` does renumber, so the case cannot pass by the guard being unreachable. A second deliberate behaviour is pinned because the obvious alternative is so tempting: the walk stops at the first FREE suffix rather than the highest taken one, so a deleted `-2` is handed out again.

  Related tidy in the same pass: `normalizeProfile`'s `|| "collector"` now reads the shared `FALLBACK_SLUG_SEED`. Four paths invent a name for a nameless profile and they agreed by three separate hand-written literals; the agreement is structural now, and the previous task's own wiring case — which pinned the two literals — was updated rather than deleted, since what it is really about is that the constant is imported and not re-typed. It caught this refactor, correctly, which is what it was for.

  Mutation-checked four ways, all of them things no previous test could have seen because the functions were unreachable: disabling the public-ID collision loop fails 5 cases; freezing its counter hangs the run (`timeout` exits 124 — an infinite loop IS the failure); pointing the username walk at `publicId`, the copy-paste bug the two near-identical loops invite, fails 3; dropping the `selfId` guard fails 1. `npm run verify` green: typecheck, `lint:all` 15/15, 6037 tests (+20), build. No DB migration, no creds.

## Tests that read the source, made to call the code (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Fallback-profile follow-ups" — filed by the run directly below against its own suite, which is the honest place for it: the suite it shipped was the best available at the time and it knew what was wrong with it. -->

- [DONE: 2026-08-21] [est: 13000] **Extract `resolveFallbackIdentity` into `lib/social-helpers.ts` so the fallback chain is tested by being CALLED** — `lib/social-context.tsx` pulls React Native peers, and `tsx --test` cannot transform them (`Unexpected "typeof"` out of `react-native/index.js`), so every assertion about `buildFallbackProfile` was a regex over its source text. That is a real weakness and not a stylistic one: a refactor that keeps the text and breaks the behaviour passes every one of them, and the suite would go green while a user with no email address got an empty name.

  The pure part moved: the `full_name` → email-local-part → translated-default chain, the ASCII slug seed, and the placeholder address, all in a function that takes a plain `{ email, fullName, userName }` and the localised default. `lib/social-helpers.ts` was already the right home — it imports nothing but types and is node-testable, which is what the suggestion pointed at. What deliberately did NOT move is `slugifyProfileId`: its last-resort branch stamps `Date.now()` so two profiles with unslugifiable names cannot collide, which makes it non-deterministic for exactly the inputs that reach it (a non-ASCII email local part), and dragging it into a pure module would have meant either threading a clock through or losing the uniqueness. The split is at the honest seam — the function returns `slugSeed` and the caller slugifies.

  **A bug found by writing the first real test.** An address with an empty local part — `"@example.com"` — split to `""`, which is falsy but was being passed through `??`, so it became an empty display name AND an empty username. `""` is not a name; it is now treated as no address at all and falls through to the translated default and the ASCII seed. Nine source-text assertions became calls with real inputs, and this is the case none of them could have expressed.

  Mutation-checked two ways, both chosen because the OLD suite would have passed them: seeding the slug from the translated display name (the exact regression the split exists to prevent) turns two cases red, and dropping the email local part out of the display-name chain turns two red. Both mutations live in `lib/social-helpers.ts`, a file the old suite never read at all — which is the argument for the extraction in one sentence.

  The source-text assertions that remain are the ones genuinely about WIRING rather than behaviour, and they are kept for that reason: that `buildFallbackProfile` actually calls the resolver, that it slugifies `identity.slugSeed`, that the bio sentinel is exported from the writer and imported by the screen, and that `?? "you"` has not come back. A pure function can be perfect and unused. `npm run verify` green: typecheck, `lint:all` 15/15, 6017 tests (+5), build. No DB migration, no creds.

## The English word under the dead key (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Key-position-rule follow-ups" — filed by the run directly below, which found it while deleting the dead `you` key and deliberately did NOT fix it in that change: a translation-key removal and a behaviour fix are two different reviews. -->

- [DONE: 2026-08-21] [est: 16000] **Translate the fallback profile's display name, and give the bio sentinel one definition instead of two** — deleting the orphan `you` yesterday was a cleanup; the literal underneath it was a live bug. `buildFallbackProfile` in `lib/social-context.tsx` builds the profile a person sees after sign-up and offline, and it fell back to the English word `"you"` as their NAME when the session carried no email address and no `full_name` metadata — in whatever language the app was in. It reads a new `defaultDisplayName` now, declared AND translated in all six locales (Collector / Коллекционер / Калекцыянер / Kolekcjoner / Sammler / Coleccionista), and the email's local part still wins ahead of it so somebody with an address is still called by the name in it.

  **The interesting half is what was deliberately NOT translated.** The old `baseName` fed three fields, and two of them are SLUGS: `username` and `publicId` go through `slugifyProfileId`, which strips everything outside `[a-z0-9]`. Translating the seed would have left a Russian or Belarusian user with an empty slug rescued by the timestamp fallback into `collector-1755...` — a worse profile ID than they get today, and a regression dressed as a fix. The slug seed is now an explicit `FALLBACK_SLUG_SEED = "collector"`, which is the same word `normalizeProfile` and `ensureUniqueUsername` already fall back to, so the three paths finally agree instead of each inventing their own. A case asserts the seed is a constant and not the translated name, so a future pass "finishing the job" by translating the other fields fails loudly.

  **Second finding, in the same function:** `bio` was a hardcoded English sentence — and it turned out to be a second copy of `DEFAULT_EN_PROFILE_BIO`, a private `const` in `app/profile/[id].tsx` that the screen compares the stored bio against in order to translate it on render. Two copies of one sentence in two files with nothing tying them together: rewording either would have left the screen comparing against a sentence nothing writes any more, and the symptom would have been a bio that quietly stopped translating rather than an error. The constant is exported from the writer and imported by the reader now. It is deliberately STILL written in English, which is the counter-intuitive half and the reason the two fields got different fixes: the bio is persisted — to AsyncStorage and to the cloud row other people read — so localising it at write time would freeze it in the author's language, and a Polish viewer would read Russian. The display name is safe to localise because it is recomputed on every render until the person edits their profile, at which point they have chosen a name of their own. Both halves are pinned, including a case asserting `bio: t("defaultProfileBio")` does NOT appear, with the reason in the message.

  A related trap the suite had to work around rather than solve: `lib/social-context.tsx` pulls React Native peers, so `tsx --test` cannot import it (`Unexpected "typeof"` out of `react-native/index.js`). The sentinel's value is read back out of the committed source instead — the same technique `i18n-translation-coverage` uses on the translations file, and it checks the text the profile screen actually compares against at runtime. A case pins that the `en` translation and the sentinel are byte-identical, because the comparison is `===` and one character of drift silently disables the render-time translation.

  Base count 501 (500 + `defaultDisplayName`) — the first key this file has GAINED in a week of removals, and it arrived because a removal uncovered the bug. `npm run verify` green: typecheck, `lint:all` 15/15, 6012 tests (+12), build. No DB migration, no creds.

## The guard's own blind spot, measured — and the key that was hiding in it (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Orphan-guard follow-ups" — a suggestion the previous run filed AGAINST ITS OWN guard, which is the reason it was worth doing next: an unmeasured limit in a rule that shipped an hour earlier is the cheapest thing in the file to be wrong about. -->

- [DONE: 2026-08-21] [est: 15000] **Measure the orphan guard's loose direction, close it, and delete the 37th key it was hiding** — `check-orphan-i18n-keys` shipped with the rule "a base key is read if it appears anywhere in the source tree as a string literal", and shipped with a follow-up saying that rule counted a key as read whenever its name collided with an unrelated literal, that nobody had measured how big that hole was, and that if it turned out small, naming the occupants would be cheaper than tightening the rule. Measured: **the blind spot had exactly one occupant, and it was a real orphan.**

  `you` — "You", and translated into `ru`, `be`, `pl`, `de` and `es` as well — is rendered by nothing. `t("you")` appears in no file in the repository. Its only mention anywhere in the source tree is `const baseName = user.email?.split("@")[0] ?? "you"` in `lib/social-context.tsx`, a default display name derived from an email local-part that has nothing whatever to do with the key. It is the only one of the 37 dead keys that somebody translated into all six languages, so five people's worth of work went into a string with no consumer — the "dead keys inflate every coverage row" point the earliest suggestion in this thread made, with the worst case of it attached to a name.

  **The measurement changed the answer.** The suggestion offered "name the occupants" as the cheap option; one occupant made tightening cheaper still, and the tightened rule is more honest besides. A mention now counts only when the literal sits in a POSITION a translation key is written in: after `(` or `,` (an argument — `t("k")`, `pick(t, "k")`), after `:` (a record value — `from_negative: "k"`, `labelKey: "k"`), after `as` or `|` (a union member). The last of those is load-bearing and was found by measuring rather than by reasoning: three of the four `condition*` keys are spelled as wrapped continuation lines (`| "conditionExcellent"`) in `components/item-card.tsx`, so a rule demanding `as "k"` on one line would have reported three live keys. A fallback value is preceded by `?`, and qualifies under none of the five. Over the whole tree the tightened rule reports one orphan and zero false positives; the loose rule reported none and missed it. Still no exemptions list.

  Counts: base 501 → 500, `ru` 500 → 498, and every partial locale drops one (be 233 → 232, pl 224 → 223, de 220 → 219, es 221 → 220) — the only one of the 37 to touch all six rows. All clear of the floors re-measured earlier today. `REMOVED_FAMILIES` gains a fourth entry, a family of one: `you` belongs to none of the three sweeps, and the note records that it is the key that proved the blind spot was worth measuring rather than merely documenting.

  Verified end to end on the real tree, which is what the fixtures cannot do: planting a `plantedValueKey` whose ONLY mention is `?? "plantedValueKey"` in `lib/social-context.tsx` exits 1 and names it — the case the shipped rule would have waved through. Six new cases in the guard's suite, one per branch of the position alternation plus the three non-positions (`=`, `===`, `return`) and the wrapped-union line. `npm run verify` green: typecheck, `lint:all` 15/15, 6000 tests (+5), build. No DB migration, no creds.

## The scan that found three dead families, made permanent — and the key it found that the scans missed (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Orphan-scan follow-ups", filed by the run directly below. The standing entry had said for three sessions that this guard should land only once the orphan count was near zero, because a rule floored at 35 findings means nothing. Removing the third family took the count to zero, which is what unblocked it. -->

- [DONE: 2026-08-21] [est: 22000] **`check-orphan-i18n-keys`: make the throwaway scan a guard, and delete the 36th orphan it found on its first run** — three families and 35 keys were removed this week by a tsx in scratchpad that died with the container each time, and nothing stopped the next dead key being added. The guard is `scripts/check-orphan-i18n-keys.ts` + `lib/check-orphan-i18n-keys.ts` (the repo's wrapper/pure-module split), registered in `LINT_GUARDS`, `SCANNED_FLOORS` and `package.json` as `lint:orphan-i18n` — 15 guards now, and CI needs no change because it runs `lint:all`.

  **The rule is not `t("…")`, and that is the whole design.** Matching the direct call is the obvious answer and it is measurably wrong here: 43 of the tree's 502 base keys are never written inside a `t(...)` call at all. They reach the translator through four different forms — the direct call; a key stored in a table (`{ mode: "name-asc", labelKey: "sortNameAsc" }` and `from_negative: "filterPriceNegative"` in `lib/item-filters.ts`, `empty: "currencyErrorEmpty"` in `lib/format-currency-input.ts`); a helper that takes the key (`pick(t, "crashFallbackTitle")` in `components/crash-fallback.tsx`); and the `as` clause of the one dynamic build. So the rule is "the key appears anywhere in the source tree as a bare string literal", which covers all four uniformly. That is also what makes the EXEMPTIONS LIST the original suggestion asked for unnecessary, and the reason is worth keeping: the dynamic build's `as` clause is itself a list of string literals, so the hard case the design worried about — a hand-maintained allowlist for dynamically-built keys, drifting out of date — does not arise. The rule is loose in one direction (a key colliding with an unrelated literal counts as read) and that is the stated price of having nothing to justify twice.

  **It found `profileId` immediately, and three hand scans had not.** The scratch scans matched `\bkey\b` over the tree, and `lib/social-context.tsx` declares six `(profileId: string)` PARAMETERS — bare-word matching cannot tell a parameter name from a key reference, so the key looked read. It is dead: it is the LABEL of the never-built "Save profile ID" field whose `profileIdHint` and `saveProfileId` went with the social/people family a day earlier, and the only sibling any screen renders is `profileIdPlaceholder`. Removed, `en`/`ru` only, base 502 → 501 and `ru` 500 → 499, both clear of the floors re-measured in the previous task. `REMOVED_FAMILIES` grows to 16 in the social family and 36 overall.

  **Knock-on fixed rather than worked around:** adding `profileId` to `dead-i18n-keys.test.ts` turned its own source-tree case red, because that case still used `\bkey\b` — the same looseness that hid the key. It now matches through `collectStringLiterals`, the guard's exported rule, so "the guard says this key is dead" and "this suite says it stays dead" can no longer be answered differently. One rule, two consumers.

  **Two registry meta-guards caught the incomplete registration and both were satisfied properly, not exempted.** `guard-report-paths` requires every `listSourceFiles` caller to have a planted-offender case; this guard reports no `path:line:column` at all, because its finding is an ABSENCE and there is no location to name. That is a categorically different exemption from the existing one (which is about fixture cost), so it gets its own `REPORTS_NO_PATH` list plus a case that asserts the REASON — the report must contain no path-shaped token — so the day the guard learns to name a file, the exemption fails instead of quietly covering it. `lint-guard-partial-root` requires a red run for every count floor; `["app"]` at 19 files against a floor of 200 is the fixture, and the note records why the order matters: `assertScannedFloor` runs BEFORE the guard reads `lib/i18n-context.tsx`, so a partial root refuses on `below_floor` rather than on a missing file.

  Floor 200 with the property the other multi-root notes name: it rides above the 163 `lib/` alone contributes, so no single root clears it. That matters more here than for most guards, because a key's only reader is often a table in `lib/` rather than a `t()` call in a screen — a walk that had quietly lost `app/` would report those keys live while missing every orphan a screen used to render.

  Verified end to end, both directions: a planted `plantedDeadKey` in the base map exits 1 naming it and its locales; a `plantedTableKey` read ONLY through a table entry in `lib/item-filters.ts` exits 0. 13 cases in `__tests__/check-orphan-i18n-keys.test.ts`, built around the four reference forms rather than the direct call. `npm run verify` green: typecheck, `lint:all` 15/15, 5995 tests (+31), build. No DB migration, no creds.

## The discover feed that was never built, and the floor the last family owed (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Dead-i18n-key follow-ups" — the third and last of the families the orphan scan named, and the one the standing entry said would own the `TRANSLATION_FLOORS` re-measure. -->

- [DONE: 2026-08-21] [est: 16000] **Delete the last ten dead i18n keys, re-measure all six translation floors, and take the orphan count to zero** — the feed/share family. THREE lost their call sites in the same 2026-04-12 / 2026-05-01 rewrites the first two families traced to (`tabDiscover`, `needMoreDataItemText`, `needTitleText`); SEVEN were never rendered in any commit — an empty discover feed with its hint and call to action (`emptyFeedTitle`, `emptyFeedHint`, `discoverCta`), a share-with-users sheet with its hint and empty state (`shareWithUsers`, `shareWithUsersHint`, `noUsersToShare`), and `conditionPlaceholder`. That last one is the only near-miss of the whole exercise and it was read at the call site rather than trusted to the scan: the three dynamic `t()` builds in the tree spell their union out — `as "conditionNew" | "conditionExcellent" | "conditionGood" | "conditionFair"` — and `conditionPlaceholder` is not in it. The reasoning is recorded beside the key in the table, because the next person to see a `condition*` name in a delete list will have the same doubt.

  **The floor re-measure, which is what made this family different.** All ten were declared in `en` and `ru` only, so the four partial locales lost nothing and `ru` went 510 → 500 — exactly its committed floor. The case that caught it is not the one the standing suggestion predicted: "'ru' has not lost translations" (`declared >= minimum`) PASSES at 500 >= 500, and the failure comes from the slack case instead, reading "'ru' floor sits at or above the current count, so ordinary churn — one deleted English key removing a translated one with it — turns it red". That is the better message of the two and it is exactly right, so the worry filed in the previous entry (that equality would read as "below the floor") was wrong and is corrected here. All six floors are re-measured against the current tree, each keeping the slack policy ITS OWN note states rather than being flattened to one number: `en` 500 → 466 and `ru` 500 → 465 at ~7% (the base map and the maintained locale, the tightest of the six), and be/pl/de/es 205/197/193/193 → 214/206/202/203 at ~8%, "roughly one feature's worth of keys". The four partial notes also record why their PERCENTAGES rose without a word being translated — the denominator shrank by 35 while they lost at most 4 declarations — which is the arithmetic that would otherwise read as progress. Mutation-checked by putting `ru` back to 500: the slack case goes red naming the locale.

  **Found by the guard I had just written, and fixed rather than exempted:** the first draft of the `es` floor note explained the locale's one-key lead by naming `openedForYou`, a key removed in the previous task. `TRANSLATION_FLOORS` notes are string literals, not comments, so `sourceCode` does not strip them and the "nothing in the source tree names a removed key" case reported `lib/i18n-coverage.ts: openedForYou` — correctly, since no scanner can tell prose in a string from a reader of the key. The note now says "the 'opened for you' header" and keeps its meaning. Loosening the rule to allow the mention was the other option and it was the wrong one: the rule is worth more sharp than the sentence was worth verbatim.

  `REMOVED_FAMILIES` is complete at three families and 35 keys, with a new case asserting both numbers together — the existing per-key count would still have passed on a table that lost a whole family, and 35 is the finding the three tasks share. **The orphan count is now zero: every one of the 502 base keys has a reader.** That is the precondition the standing suggestion set for the orphan GUARD (its floor starts at zero rather than at 35), so that task is now unblocked and filed at the top of the suggestions.

  `npm run verify` green: typecheck, `lint:all` 14/14, 5964 tests (+2), build. No DB migration, no creds.

## The collections list that was rewritten, and the "Opened for you" section that never shipped (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Dead-i18n-key follow-ups" — the second of the three families the orphan scan named, taken the same way as the first: trace each key before deleting, and pick the slice that does not cross the `ru` floor. Again no unblocked task remained in this file. -->

- [DONE: 2026-08-21] [est: 14000] **Delete the ten dead collections-list i18n keys, and turn the one-family suite into a table** — the orphan scan re-run against the merged tree reports 20 remaining, unchanged in membership from the pre-removal list minus the fifteen that shipped an hour earlier, which is the check that the last removal took nothing with it that a screen still reads. Same two-group split as the social family, traced per key with `git log -S` over the un-shallowed clone. SIX had call sites: `noOwnedCollections`, `noFriendCollections`, `noSubscribedCollections`, `yourCollection`, `sharedToYou` went in `8b35e1b` (2026-04-12, the same rewrite that took the people screens) and `loadingCollections` a fortnight later in `6bfbfd1` (2026-05-01, "implement new design") — the rewrite reaching the collections list rather than the people screens, which is why it is a different commit and worth naming. FOUR were never rendered in any commit: `openedForYou`, `openedForYouSubtitle`, `noSharedCollections` (the header, subtitle and empty state of an "Opened for you" section) and `statsItems`, a bare "items" stat label. The unbuilt four are the more interesting half here, because three of the family's keys reach past `en`/`ru` — `yourCollection` and `sharedToYou` were translated into be/pl/de/es and `openedForYou` into be/pl/de — so four locales carried translations for a section nobody ever built.

  Counts: base 522 → 512, `ru` 520 → 510, and the partial rows drop three each (two for `es`, which never had `openedForYou`): be 236 → 233, pl 227 → 224, de 223 → 220, es 223 → 221. Every one clear of its floor (500/500/205/197/193/193), so still no `TRANSLATION_FLOORS` re-measurement. That leaves the feed/share family, ten keys, as the one that owns it: removing them lands `ru` at 500 against a floor of 500, and the floor test asserts `minimum < declared` strictly, so it goes red at equality rather than below it.

  The suite became a table, and the second family is what earned it. `dead-social-i18n-keys.test.ts` shipped that morning with one family written out flat; a second copy of its three cases would have been the wrong answer to a second family arriving, and a third copy the wrong answer twice. `git mv`'d to `__tests__/dead-i18n-keys.test.ts`, so history follows, and it now holds `REMOVED_FAMILIES` — a name, the keys, and an `instead` clause saying where a contributor who trips it should go, since "this key does not exist" is not news when the key was deleted on purpose. Each family gets its own named absence case through `assertNoLocaleDeclares`, and the source-tree walk — the expensive half — moved OUT of the per-family loop and runs once for all of them. Two cases guard the table itself: no key may appear in two families (the mistake a table invites, since both families came out of one scan's output a day apart), and every family must carry a non-trivial `instead`. Mutation-checked: reviving `yourCollection` in the `pl` map turns the collections-list case red naming `pl: yourCollection`.

  `npm run verify` green: typecheck, `lint:all` 14/14, 5962 tests (+2 over the previous run's 5960 — the table adds a second per-family case and a table-integrity case, and folds three flat cases into two), build. No DB migration, no creds.

## Fifteen strings for screens that were rewritten, and six for a UI nobody built (2026-08-21)

<!-- Promoted from .tasks/.suggestions.md, "Dead-i18n-key follow-ups" — the top entry, and the family the suggestion's second bullet asked to trace before deleting. No unblocked task remained in this file: the three survivors are all `[needs-data]` / `[needs-dep-decision]` / `[needs-dev-dep]`. Taken family by family as the suggestion advised, and this family is the one that does NOT cross the `ru` floor, so it needs no floor re-measurement and cannot collide with the two that do. -->

- [DONE: 2026-08-21] [est: 18000] **Delete the fifteen dead social/people i18n keys, and give the third absence guard the helper the second one earned** — an orphan scan over the base map (rerun here, 35 orphans, matching the suggestion's count exactly) confirmed no reader in `app/`, `components/`, `lib/`, `scripts/`, `data/`, `__tests__` or `docs/`, and no dynamic `t(\`prefix${x}\`)` build spells them — the only dynamic constructor in the tree is the `condition*` one, which touches none of these. The suggestion's second bullet asked for `git log -S` per key before deleting, and it needed the clone un-shallowed (50 commits of 892) to answer; the answer splits the family in two and the split is the interesting part. NINE had real call sites, added at `init` and deleted in `8b35e1b` on 2026-04-12 when `app/people.tsx` was rewritten and `app/friends.tsx` was added — `peopleTitle`, `peopleSubtitle`, `noPeopleFound`, `loadingPeople`, `noFriendsYet`, `noFriendsYetTab`, `noRequestsYet`, `noFollowingYet`, `subscribeToSeeCollections` — ordinary leftovers. SIX never had a call site in any commit: `friendBadge`, `followingBadge`, `invitationSent`, `searchFilterByOwner`, `profileIdHint`, `saveProfileId` — copy for a friend/following badge on a profile row, a "By owner" search filter and a "Save profile ID" field with its hint, none of which was ever built. `invitationSent` is the one of the fifteen translated into all six locales, so four languages carried a translation for a string nobody ever rendered; the suggestion's "dead keys inflate every denominator" point, with a name on it. Filed as a suggestion below rather than left implicit, which is what the promoting entry asked for.

  Counts: base 537 → 522, `ru` 535 → 520 (both clear of their floor of 500 — `en` and `ru` declared all fifteen), and be/pl/de/es drop one each with `invitationSent` (237/228/224/224 → 236/227/223/223, floors 205/197/193/193). No `TRANSLATION_FLOORS` re-measurement, which is exactly why this family went first: the two still outstanding (collections-list 10, feed/share 10) land `ru` at 500 against a strictly-greater floor, so whichever is removed last owns that re-measure. `i18n-translation-coverage.test.ts`'s "publishes the honest number" moved to `en: 522/522` with the reason prepended to its note; `ru`'s inherited list stays at two (a brand and an email address) because `ru` DECLARED all fifteen, so removing them adds nothing to the inherited set.

  The guard is the third of its shape, which the suggestion named as the point to stop copying the loop: `assertNoLocaleDeclares(source, predicate, why)` now sits in `__tests__/helpers/i18n-locales.ts` beside `assertDeclaredInEveryLocale` as its mirror, and both hand-rolled loops moved onto it — `visibility-chip-a11y.test.ts`'s `visibilityShared` case and `runtime-config-i18n-forbidden.test.ts`'s `runtimeConfig*` case. A predicate rather than a key because the three claims are three shapes (an exact name, a prefix, a set of fifteen), and the part worth writing once is the ANSWER: every offending `locale: key` pair listed. The realistic regression is a removal that reaches `en` and misses `ru` — the inheriting locales serve English either way, so nothing at runtime shows it — and that is what the message names. Deliberately reads DECLARED keys rather than resolved ones: a key only `en` declares resolves for `ru` through its `...en` spread, and reporting `ru` for it would make the failure unfixable, because there is nothing in the `ru` map to delete. Pinned by a case.

  New `__tests__/dead-social-i18n-keys.test.ts` (3 cases) pins both halves of the removal, because re-adding the key is only half of a reintroduction: the absence in every locale map, AND that nothing in the source tree names one of the fifteen — through `sourceCode`, so a comment recording why a key went is not itself an offence, and over the source tree only, since this suite writes all fifteen names out. Mutation-checked both ways: reviving `peopleTitle` in the `ru` map turns both cases red naming `ru: peopleTitle`, and planting a `"peopleTitle"` reference in `app/people.tsx` with no declaration turns the second red naming `app/people.tsx:peopleTitle`. Five cases added to `i18n-locales-helper.test.ts` for the new helper, including the half-removed fixture and the mirror-of-the-declaration-check pair.

  `npm run verify` green: typecheck, `lint:all` 14/14, 5960 tests (5961 before — twelve per-locale cases collapsed into three helper calls whose messages name the locale, eleven added), build. No DB migration, no creds.

## The nine strings for a form CLAUDE.md forbids building (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, "Dead-i18n-key follow-ups": 44 base keys are rendered by nothing, in four families. This is the safest slice — the nine `runtimeConfig*` keys are the copy for a runtime Supabase-config form (title, URL/key labels + placeholders, save button, the "configured in this browser" badge, the clear button) that CLAUDE.md's top instruction explicitly forbids, so deleting them enforces that prohibition rather than dropping planned work. Done first because it needs no floor re-measurement and cannot collide with the `ru`-at-500 floor the other families do. -->

- [DONE: 2026-08-20] [est: 8000] **Delete the nine dead `runtimeConfig*` i18n keys** — declared in `en` and `ru` only, rendered by no screen (confirmed: the only non-source mention was a doc-comment example in `i18n-source.test.ts`, now rewritten to name a live key). They are the exact strings a reintroduction of the forbidden runtime-config form would need — `runtimeConfiguredBadge` ("Configured by you in this browser") and `runtimeConfigClear` ("Clear runtime credentials") are the badge + button `CLAUDE.md` names — so removing them enforces the standing prohibition at the i18n layer. Base count 546 → 537; `ru` 544 → 535, still clear of its floor of 500 (en 537, floor 500). `i18n-translation-coverage.test.ts`'s pinned line moved to `en: 537/537` with the reason in its note; no floor re-measurement needed (the four partial locales never declared these keys, and neither en nor ru crosses 500). New dedicated guard `__tests__/runtime-config-i18n-forbidden.test.ts` — one case per locale (6) asserting no map declares a `runtimeConfig*` key — so re-adding the form's copy (the first thing a reintroduction needs) turns a named case red before any component wires it up. Distinct from the `visibilityShared` guard because this one is about a whole forbidden feature's namespace, not a single contradictory key. `npm run verify` green: typecheck, `lint:all` 14/14, 5961 tests (+6), build. No DB migration, no creds. Remaining orphan families (social/people, collections-list, feed/share — ~35 keys, the ones that DO cross the `ru` floor) stay filed as the follow-up below.

## The visibility key that read "Shared" in one language and "Public" in another (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, top entry of "Visibility-text translation follow-ups": `visibilityShared` was mismatched at the source (en "Shared", ru "Публичная"/Public) with the four other locales inheriting the English one, so which meaning a user saw depended on where it was rendered. -->

- [DONE: 2026-08-20] [est: 7000] **Remove the dead, self-contradictory `visibilityShared` key** — the entry read "Shared" in `en` and "Публичная" (Public) in `ru`, a contradiction at the source, and the other four locales inherited the English one. The suggestion's first step was to read each call site and decide which of the two meanings `visibilityShared` was for — but there are no call sites: the key is rendered by nothing. `components/visibility-badge.tsx` reads `visibilityViewer` ("Shared with you") for a collection someone shared with me and `visibilityPublic` for a public one; `visibilityShared` is a third, unused name that duplicated `visibilityViewer` while disagreeing with itself across languages. Because nothing renders it, no runtime assertion could ever have caught the contradiction — `t("visibilityShared")` returns a non-empty string in all six locales — so the honest fix is to delete the two declarations rather than invent a meaning for a string with no consumer. Removing it drops the base count 547 → 546 (and the be/pl/de/es inherited denominators by one, floors untouched — those gate `declared`, which is unchanged). `i18n-translation-coverage.test.ts`'s "publishes the honest number" moved to `en: 546/546` with the reason recorded in its note; ru's inherited list (`emailPlaceholder`, `appName`) is unaffected because ru had DECLARED `visibilityShared`, so its removal adds nothing to the inherited set. New guard in `__tests__/visibility-chip-a11y.test.ts` — one case per locale (6) asserting no map re-declares `visibilityShared` — so a future edit reviving a contradictory duplicate of `visibilityViewer` by reflex turns a named case red instead of shipping. `npm run verify` green: typecheck, `lint:all` 14/14, 5955 tests (+6), build. No DB migration, no creds.

## The locked chip announced `disabled` and was not (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, "Sheet-sandwich follow-ups": the locked private-visibility chip declared `accessibilityState={{ disabled: locked }}` and did not disable — pressing it opens the premium upsell (create screen) or fires a toast (edit modal). A screen reader offering the button as "dimmed, private" gave no cue for the dim and none that the upsell would appear on press. -->

- [DONE: 2026-08-20] [est: 8000] **Stop lying about `disabled` on the locked visibility chip, and let it name the premium constraint out loud** — two adoption points, one shape: `app/create-collection.tsx` (chip opens `<PremiumUpsellSheet>`) and `components/edit-collection-modal.tsx` (chip fires a toast). Both chips now declare `accessibilityState={{ selected }}` and NO `disabled` — the button is live, and the state that used to sit there was the honest description of how it LOOKS and a lie about what it DOES. The `accessibilityLabel` is set only on the locked variant, composed from `t("visibilityPrivate")` and `t("visibilityPrivatePremiumOnly")` so a non-visual user hears "Private — Private collections are a premium feature." instead of "Private". Unlocked chips carry no `accessibilityLabel`, so the `<Text>` child's name still composes on iOS. The label is a template literal with two `t()` calls, which the untranslated-label rule in `check-a11y-jsx` (matching bare `"..."` only) ignores by construction, so the fix does not need a new key. Related type narrowing found while writing it: inside the ternary, `v === "public"` is unreachable when `locked` is truthy (because `locked = v === "private" && !isPremium`), so both labels hardcode `t("visibilityPrivate")` — the private-only side of the pair — rather than branching on `v` a second time. New `__tests__/visibility-chip-a11y.test.ts` (6 cases) pins both chips separately — the shape of `accessibilityState`, the absence of any `disabled` key inside it, and both halves of the composed label — because a fix that reached only one screen would leave the other silent. `npm run verify` green: typecheck, `lint:all` 14/14, 5944 tests (+6), build. No DB migration, no creds.

- [DONE: 2026-08-20] [est: 7000] **Translate the visibility chip's visible text and hints so the eye and the ear agree** — the composed spoken label went per-locale in the entry above, and the chip's own text (`visibilityPrivate` / `visibilityPublic`) plus the two hints below it (`visibilityPublicHint` / `visibilityPrivateHint`) still fell through `...en` in four of six locales, so a Polish user saw "Private" on the chip and heard the Polish composed sentence via the label — the visible name and the spoken one in different languages. Four keys times four locales (be/pl/de/es — ru already had them), sixteen strings, no code changes. Verified by extending `__tests__/visibility-chip-a11y.test.ts` with a per-key case that goes through `assertMatchesInEveryLocaleBody` — the four keys are pinned as declared per locale, so a locale losing one turns a named case red rather than reducing a translation-coverage floor by one alongside every other churn. The base count went from 547 to 547 (no new keys, just new declarations of existing ones), and ru's inherited list stays at two — brand + email. `npm run verify` green: typecheck, `lint:all` 14/14, 5949 tests (+4 new per-key cases), build. No DB migration, no creds.

- [DONE: 2026-08-20] [est: 9000] **Say the locked chip's premium sentence in every language, as ONE composed key rather than two joined at runtime** — the label above shipped as `${t("visibilityPrivate")} — ${t("visibilityPrivatePremiumOnly")}`, and only English declared the second half; five locales fell through the `...en` spread, so a Russian screen reader user hearing the chip got "Private — Private collections are a premium feature." in English inside a Russian UI. Two of the five (ru, be) also lacked `visibilityPrivate`, so the composed sentence would have been English on both halves. Both issues collapse into one key per locale, `visibilityPrivateLockedA11y`, that names the value and the premium constraint together — so each language answers for its own apposition punctuation (a colon rather than an em-dash in ru/be) and the run-time concatenation the guard cannot see through is gone. Six declarations of `visibilityPrivateLockedA11y` and five new declarations of `visibilityPrivatePremiumOnly` (in ru/be/pl/de/es), taking the base count from 546 to 547 — the two consumers now read one key each, `accessibilityLabel={locked ? t("visibilityPrivateLockedA11y") : undefined}`, on both chips. Related brittle-suite finding on the first pass, and fixed rather than routed around: the new suite counted `readI18nSource().match(/visibilityPrivateLockedA11y:/g).length === 6`, which is exactly the shape `i18n-locales-helper.test.ts`'s "the habit stays retired" case forbids — a whole-file count would pass on a base map declaring the key twice while a non-base map inherited the English string. It now goes through `assertMatchesInEveryLocaleBody`, which asks each locale on its own. `translation-coverage`'s "names the three keys Russian still inherits" (down to two now, both legitimate — a brand and an email address) and the "publishes the honest number" report (546 → 547) both moved along with the change. `npm run verify` green: typecheck, `lint:all` 14/14, 5945 tests (+1 new case), build. No DB migration, no creds.

## The tappable that never says it is a button (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, "Disabled-state follow-ups", and decomposed on arrival because it is four times the size of the icon sweep. Neither React Native's `<Pressable>` nor react-native-web's sets a default role (checked in both sources), so a Pressable with no `accessibilityRole` announces as a generic element on all three platforms: a screen reader user is told there is some text there, not that it can be pressed. 152 of this tree's 194 interactive Pressables are in that state; 42 already carry a role.

Not a codemod, unlike the disabled sweep: `button` is right for most and wrong for several. `imagebutton`, `checkbox`, `link` and `search` are each already correct somewhere in this tree, full-screen backdrop Pressables (`onPress={close}` behind a modal) arguably want no role and no focus at all, and `swipe-tabs` wants the tab treatment the nav bar settled on (`button` + `accessibilityState={{ selected }}`, because React Native has no `tab` role in the subset this app types against).

Split by ownership rather than by count, shared code first: a role added to a component is a role added to every screen that renders it. Each sub-task ships independently and the counts are what the sweep measured on 2026-08-20. -->

- [DONE: 2026-08-20] [est: 16000] **The shared components (26)** — a role added here is a role added to every screen that renders them, which is why this slice went first. Most are `button`; the interesting ones are the four that are not. `selectable-item-row` is a `checkbox` with `accessibilityState={{ checked: selected }}`: it is a row in a multi-select list, and "button" would announce the tap while saying nothing about whether the item is in the selection, which is the whole state the row exists to show. `swipe-tabs`' two rows are `button` + `{{ selected: isActive }}`, because `tab` is not in the `AccessibilityRole` union this app types against — the same answer `<NavTab>` settled on, so the app now has one story for tabs rather than two. Three chips that render an "active" style and said nothing about it (a currency chip, a quick filter chip, a reaction chip) now carry `{{ selected }}`. And four Pressables got `accessibilityRole="none"` rather than a role: the backdrop behind the filter sheet and the create sheet, and the wrapper inside each whose only job is `e.stopPropagation()`. `button` on a backdrop puts a full-screen focus stop in front of the sheet, and on the wrapper it announces the sheet itself as pressable — `none` is that decision written down, which an unset role is not. 152 → 126. The new `__tests__/pressable-roles.test.ts` counts ALL interactive Pressables per file rather than the ones this slice touched: five of `item-filters`' ten already carried a role, and a case counting only the five would go green on a file that lost the other five. `npm run verify` green: typecheck, `lint:all` 14/14, 5947 tests (+13), build.

- [DONE: 2026-08-20] [est: 16000] **The modals and sheets (32)** — and the group turned out to have exactly one shape repeated seven times. Every sheet in this tree is the same two-Pressable sandwich: a full-screen backdrop whose press dismisses, wrapping a card whose only `onPress` is `e.stopPropagation()` to stop the tap reaching it. Neither is a control — `button` on the backdrop puts a focus stop the size of the screen in front of the sheet, and on the wrapper it announces the sheet itself as pressable — so both take `accessibilityRole="none"`, which is that decision written down rather than the absence of one. That regularity is now its own case: all nine sheets in the tree (the seven here plus the filter sheet and the create sheet from the previous slice) must opt out EXACTLY twice, so a sheet with one is a sheet whose backdrop or wrapper has quietly become a control. The rest are `button`, plus four chips that draw a "chosen" style and said nothing about it — the search overlay's type filter and its two owner chips, and the edit modal's visibility chip, which draws TWO states (`selected` and premium-`locked`) and announced neither: a screen reader user pressing the locked one got a toast and no warning beforehand. 126 → 94. Two codemod artefacts fixed in the same pass rather than left: the role insertion put the new prop mid-line on single-line tags (reflowed, one prop per line) and left a whitespace-only line on tags that were already multi-line (stripped) — this repo has no prettier, so nothing else would have. `npm run verify` green: typecheck, `lint:all` 14/14, 5956 tests (+9), build.

- [DONE: 2026-08-20] [est: 15000] **The two big detail screens (32)** — the most per-file and, in the end, the least varied. `app/profile/[id].tsx`'s sixteen are all plain buttons: it is one screen of relationship actions (add friend, remove friend, follow, unfollow, message, admin delete) written out once per `ProfileRelationship`, so there is nothing to decide beyond the role. `app/item/[id].tsx` carries the sheet sandwich again — its listing sheet is the tenth backdrop-plus-`stopPropagation` pair in the tree and takes the same `accessibilityRole="none"` — and two chip rows that draw `conditionChipSelected` and said nothing about it: the condition picker in the edit form and the buy/trade mode picker in the listing sheet. Both share a style AND a local `selected`, which is why one assertion covers both and why a case matching the STYLE would have passed on either of them alone. 94 → 62. `npm run verify` green: typecheck, `lint:all` 14/14, 5959 tests (+3), build.

- [DONE: 2026-08-20] [est: 20000] **The remaining 62 screens, and the rule the tree earned** — the list-and-create screens (`people`, `create`, `collection`, `wishlist`) and everything else (`friends`, `_layout`, `create-collection`, `settings`, `chat`, `listing`, `chats`, `index`, `auth/callback`) shipped as one push because the payoff is entirely at zero. `people`'s ten are eight relationship actions across the same `ProfileRelationship` branches `profile/[id]` already covered plus two pagination buttons; `wishlist` and `create` each carry another sheet sandwich; the rest are plain buttons and one more `conditionChip` (in `create.tsx`). Every interactive `<Pressable>` in `app/` and `components/` now announces what it is, 194 of 194, and `no_role` is `check-a11y-jsx`'s fifth code — same pattern as `undecided_icon` and `silent_disabled`: written the moment the count reaches zero, with the tree as the proof it passes. `HAS_ROLE` accepts `accessibilityRole="none"` because that IS a role and a deliberate one — a sheet backdrop opts out with it — and `IS_INTERACTIVE` reads `onPress` because a Pressable without one is a layout wrapper. The temporary `pressable-roles.test.ts` is deleted rather than extended a fifth time (it was written as a holding pen for exactly this), and every fixture in `check-a11y-jsx.test.ts` that opens an interactive `<Pressable>` was given a role so it stops asserting two rules at once — the same pass the hiding fixtures got when `undecided_icon` shipped. Related bug found in the same pass and fixed with a `precise` edit: my role-insertion codemod left whitespace-only lines on already-multi-line tags, which every previous sweep this week has also paid for; this repo has no prettier, so the sweeps are their own formatting. Verified end to end: removing one role from `app/settings.tsx` exits 1 naming the file, the line and what to add, and the tree goes green when it is put back. `npm run verify` green: typecheck, `lint:all` 14/14, 5938 tests (5 down from 5959 — the deleted suite), build.



## Greyed out and saying nothing (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, "Sweep-complete follow-ups": the disabled state was found on one button while deciding the wishlist's icons, and the entry said others almost certainly had it. Swept properly this time before touching anything, which is the lesson the icon sweep's own follow-ups kept repeating. -->

- [DONE: 2026-08-20] [est: 15000] **Say when a button is disabled, in all 29 places that did not** — greyed-out is a colour, and a colour reaches nobody: without `accessibilityState={{ disabled }}` a screen reader offers the button as though pressing it would do something. 29 of this tree's 33 disabled `<Pressable>`s were silent, including both of the photo lightbox's chevrons at the first and last photo, every disabled button on the login screen, and both bulk-bar actions. Unlike the icon sweep this needed no judgement at all — a `<Pressable>` that takes `disabled` should say so, with no per-site decision — so it went in as one codemod over `app/` and `components/`, brace-depth aware and string-literal aware so an expression containing `>` could not end an open tag early, inserting the state next to the `disabled` prop it mirrors. The rule then went into `check-a11y-jsx` the same way `undecided_icon` did an hour earlier: written the moment the count reached zero, with the tree as the proof it passes. Two details it is easy to get wrong and both are pinned — `accessibilityState` has to NAME disabled rather than merely exist (a tab carrying `{{ selected }}` beside a `disabled` prop is exactly as silent as one carrying nothing, and this tree has such tabs), and the rule is scoped to `<Pressable>` because `<DangerSection disabled>` forwards it to a Pressable that announces it correctly — a rule that could not see inside the component would be wrong about both of its call sites. Verified end to end by deleting one of the 29 back out: the guard exits 1 naming `components/bulk-bar.tsx:41` and what to add, and goes green when it is put back. `npm run verify` green: typecheck, `lint:all` 14/14, 5934 tests (+7), build. No DB migration, no creds.

## The guard named for its first rule (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, where it was filed three times in one day — twice by earlier runs and once by the run directly below, whose fourth rule is what made the name wrong rather than merely narrow. The entries all said the same thing: the rename is safe and boring, and worth doing before it becomes misleading. -->

- [DONE: 2026-08-20] [est: 9000] **Rename `check-a11y-icon-labels` to `check-a11y-jsx`** — it shipped this morning with one rule about unnamed icon buttons and finished the day with four: untranslated labels, one-sided hiding, and icons nobody decided about. Two of the four are not about labels and one is not about icons either, so the name had stopped describing the file — which is what a contributor reads before deciding whether their change is in scope for it. `check-a11y-jsx` says what it reads instead of what it first checked. Six touch points, every one of them already pinned by a suite, which is what made this boring rather than risky: `LINT_GUARDS`, `SCANNED_FLOORS`, `FLOOR_WALKS`, the partial-root fixture, the report-paths table, the comment-stripping fixture list, plus `package.json` and the three files themselves (`git mv` for all three, so history follows). The registry description was rewritten in the same pass — it still named one rule, and a registry entry that lists a quarter of what a guard does is the next thing somebody reads. `npm run verify` green: typecheck, `lint:all` 14/14, 5927 tests, build. No behaviour change, no DB migration, no creds.

## The rule the tree had to earn (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md, top entry of "Sweep-complete follow-ups" — filed by the run that finished the sweep an hour earlier, which is what made this writable at all. -->

- [DONE: 2026-08-20] [est: 17000] **Make "every icon is hidden or named" a guard, now that the count is zero** — `check-a11y-icon-labels` reported a node hidden on SOME platforms and said nothing about one hidden on NONE, so the whole day's sweep was held by per-file counts in one suite: they catch a new icon in the ten files they name and not in a new file. `undecided_icon` closes that. It is a rule that could not have been written this morning for a reason worth recording — with 36 findings it gets floored at 36 and stops meaning anything; the day the count reaches zero it becomes a rule with the tree as its proof. Hiding is INHERITED, so the walk keeps a stack of open tags rather than scanning them flat: `item-card.tsx`'s photo-count badge is a glyph AND a bare number hidden together as a subtree, and a flat rule would report both icons and demand redundant props on them. That stack also closes a suggestion filed this morning against `half_hidden` — a child inside an already-hidden parent is no longer reported as partially hidden. Android is what makes the ancestry more than a detail and it has its own table: `importantForAccessibility="no"` hides the node and leaves its children announced, and only `"no-hide-descendants"` takes the subtree, so `HIDES_SUBTREE_BY` differs from `HIDDEN_BY` on exactly that one platform — pinned by a case, because the two tables looking almost identical is how they end up merged by someone tidying. The rule deliberately does NOT accept a label on an ancestor `<Pressable>` as the icon's decision: on iOS that label REPLACES the subtree, so the icon is silent there and announced on the web, which is the split this guard exists to catch. Verified end to end rather than by fixture alone: a planted `<Ionicons name="planted-offender" />` in `components/empty-state.tsx` exits 1 naming the file, the line and what to do, and the tree goes green again when it is removed. The guard's own suite grew a `HIDDEN` fixture const and moved its hiding cases onto `<Text>`, because a fixture built from a bare `<Ionicons>` now asserts two rules at once and stops saying which one it is for. `npm run verify` green: typecheck, `lint:all` 14/14, 5927 tests (+7), build. No DB migration, no creds.

## Decorative icons nobody has decided about (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Three-platform hide follow-ups", filed by the run directly below) and decomposed on arrival: 36 of this tree's 44 `<Ionicons>` carried neither a hide nor a label. A guard cannot close this — "decorative" is a judgement, and a rule demanding a hide on every icon would be wrong about the ones that carry meaning — so it is split by how the judgement gets made, easiest evidence first. Each sub-task ships independently; the count in each title is what was left when it was written. -->

- [DONE: 2026-08-20] [est: 11000] **Hide the six whose name is already written next to them** — the sites where "decorative" is not a judgement call because the file says so: `nav-tab.tsx`, `danger-icon-button.tsx`, `bottom-nav.tsx`'s plus button and `currency-input.tsx`'s more-chip sit inside a `<Pressable>` that carries an `accessibilityLabel` (a required prop, in `DangerIconButton`'s case), and `soft-destructive-chip.tsx` and `visibility-badge.tsx` each have a sibling `<Text>` saying the same thing in words. All six now carry the full triple, and `a11y-icon-labels.test.ts` pins each one WITH the sentence that makes it decorative, so a site whose reason stops holding is a named failure rather than a silently-kept hide. 36 → 30. `npm run verify` green.

- [DONE: 2026-08-20] [est: 12000] **Decide the two photo widgets (6 icons)** — all six turned out to be the same easy case as the first sub-task rather than the judgement call this entry expected: every one of the six sits inside a `<Pressable>` that already carries an `accessibilityLabel`, so the glyph was being announced next to a name the button already had. `photo-lightbox.tsx`'s close / previous / next carry `t("galleryClose")`, `t("galleryPrevious")` and `t("galleryNext")`; `photo-preview.tsx`'s delete carries `t("delete")` and its two reorder chevrons `t("photoMoveBack")` / `t("photoMoveForward")`. All six now carry the full triple. The shared case in `a11y-icon-labels.test.ts` grew a per-file COUNT alongside the reason, because the reason is a property of each site and the count is the thing no repo-wide rule can state — a seventh icon added to either file inherits nothing from these six and now reddens a named case. 30 → 24. `npm run verify` green: typecheck, `lint:all` 14/14, 5902 tests (+2), build.

- [DONE: 2026-08-20] [est: 14000] **Decide the sheet and filter icons (5)** — and this is the first group of the sweep where the answer was not "hide it" for every site. Three were decorative on the same evidence as the twelve before them: the filter button's `options-outline` sits beside a `<Text>` that names it, and the sort chip's two glyphs sit inside a `<Pressable>` carrying `t("sortChipClear", { sort })`. The other two were carrying meaning no `<Text>` beside them repeated, and hiding those without moving the meaning first would have DELETED the only copy — which is the mistake this sweep could most easily have made while looking mechanical. `item-filters`' has-photos toggle says on-or-off by swapping `checkbox` for `square-outline`, and a glyph reaches no screen reader: it is now `accessibilityRole="checkbox"` with `accessibilityState={{ checked: draft.hasPhotos }}`, which is the platform's own word for it, and the glyph is hidden after that. `currency-sheet`'s rows announce their code and name from their own `<Text>` children and said nothing about WHICH one was chosen — the checkmark is rendered only when selected, so an unselected row had nothing to announce at all; the row now carries `accessibilityState={{ selected: isSelected }}`. Both are pinned in their own suite, separately from the hide table, because what makes them safe is the state and not the hide. 24 → 19. `npm run verify` green: typecheck, `lint:all` 14/14, 5906 tests (+4), build.

- [DONE: 2026-08-20] [est: 15000] **Decide the chat-entry-point icons: `app/_layout.tsx` (3) and `app/friends.tsx` (2)** — split out of the screen-level group because they turned out not to be an icon problem at all. Both draw an unread count inside the element that names them, which is the bottom nav's bug from earlier today in two more places — and `_layout.tsx`'s header ships on EVERY screen, so it is the widest of the three. The two need opposite fixes and the reason is worth stating: the header's `<Pressable>` carries an `accessibilityLabel`, which on iOS replaces its children entirely, so the count goes INTO the label (`navChatsUnreadA11y`, the key written this morning for the nav bar — the third consumer and the first that did not need a new string) and the pill is hidden as a subtree; the friends row carries no label and is named by composing its two `<Text>` children, so its pill stays in the tree and is given a sentence instead of a bare "3". The header's button is written out twice under two mutually exclusive path conditions, so the label is one `const` used by both — the label was the only part of the duplicated blocks with any reason to differ, and it does not. The three icons (two chat glyphs, one magnifier) and the friends chevron are decorative on the usual evidence and hidden. 17 → 12. `npm run verify` green: typecheck, `lint:all` 14/14, 5913 tests (+4), build.

- [DONE: 2026-08-20] [est: 16000] **Decide the remaining screen-level icons (12), closing the sweep** — every `<Ionicons>` in `app/` and `components/` has now been decided about. This batch was expected to need i18n keys written in six languages and needed none: all twelve are decorative on the usual evidence (`create.tsx`'s folder glyphs and chevrons sit beside naming `<Text>`; `wishlist.tsx`'s add / promote / pick buttons each carry their own; `chat/[id].tsx`'s clear and send and `profile/[id].tsx`'s settings button sit in `<Pressable>`s that already carry `t("chatClear")`, `t("chatSend")` and `t("settings")`). Reading all twelve before starting — which the previous run's own suggestion said to do — is what made that visible in one pass instead of four. Three things that were NOT decorative came out of the same read and are fixed here: `create.tsx`'s collection sheet said which collection was chosen with a checkmark and nothing else, so the row now carries `accessibilityState={{ selected: isSelected }}` (the same seam as `components/currency-sheet.tsx`, and the second time this exact shape has turned up); `wishlist.tsx`'s promote button greys out when there is no collection to promote into and said so in colour alone, so it carries `accessibilityState={{ disabled }}`; and `create.tsx`'s collection selector claimed no role at all, so it announced as plain text rather than as something you can press. The scratch inventory now reports 2 remaining, both in `item-card.tsx` — which is the measurement artifact filed this morning rather than work left: those two are hidden by their PARENT badge and the script reads icon attributes, not ancestors. 12 → 0. `npm run verify` green: typecheck, `lint:all` 14/14, 5920 tests (+7), build.

- [DONE: 2026-08-20] [est: 13000] **Label `item-card.tsx`'s photo-count chip (2 icons)** — three things were wrong at once and the entry above had spotted one of them. The glyph was announced; the number beside it was announced as "5" with nothing saying what it counted; and the count — the only useful part — never reached the name of the button it sits inside. The plan this entry proposed (an `accessibilityLabel` on the wrapping `<View>`) would have fixed the first two and left the third, and it would have put a second announcement inside a `<Pressable>` that is already one accessibility element on iOS. What shipped instead moves the count INTO that button's name — `galleryOpenPhotos`, a new key in all six languages — and hides the badge as a SUBTREE with `no-hide-descendants`, so the icon and the bare number go together and the sentence is said once, by the thing you actually press. Both card layouts, compact and full, carry the same chip and the same button. Related bug found and fixed in the same pass: `photosCount` — rendered by this same file's footer — existed in `en` and `ru` only, so four locales had been reading "5 photos" in English since it was written; it is now translated in `be`, `pl`, `de` and `es`, which is why the four partial rows rose by two keys rather than one. 19 → 17. `npm run verify` green: typecheck, `lint:all` 14/14, 5909 tests (+3), build.

## The badge a screen reader could not hear (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the top entry of "Icon-label follow-ups" (2026-08-20). That entry named the cost as "a plural-aware string in six languages" and pointed at `photosCount` to copy; the copy turned out to be the answer to a different question — see below. -->

- [DONE: 2026-08-20] [est: 18000] **Say the nav badges out loud, since they are drawn inside the label that silences them** — the chats tab draws an unread count and the friends tab a pending-request dot, both inside the `<Pressable>` that carries the tab's `accessibilityLabel`. A screen reader announces the label and stops, so the bar said "Chats, button" at zero unread and at forty: the one thing each badge exists to convey was the one thing a non-visual user never got. Two shapes of badge, two shapes of label — the count badge shows a number so its label says the number, and the friends badge is a dot that shows only THAT something is waiting, so its label says that and no more. Announcing a count the screen does not show would be its own mismatch, in the other direction. The label is derived from the BADGE rather than from the counter behind it: `navTabLabelSpec` in `lib/chat-helpers.ts` maps `FriendsTabBadge` onto `plain | unread | pending`, exhaustively, so a fourth badge kind has to answer "and what does this one say out loud?" before it compiles — and reading `unreadTotal` a second time in `bottom-nav.tsx` would have been a second place for the pill and the spoken name to disagree. It also catches a case the counter would not: `formatBadgeCount(0)` is `""`, so a zero-valued count badge draws nothing, and a label reading "unread: 0" beside no pill is exactly the mismatch this routing exists to prevent. The label deliberately does NOT use `formatBadgeCount`: its "99+" cap exists because the pill is 18px wide, and a spoken label has no width, so 240 unread is announced as 240. On the plural problem the suggestion flagged: the fix was to not have it. `Chats, unread: ${count}` and its five translations put the number after a colon, which is a form Russian, Belarusian and Polish read correctly at 1, 3 and 5 — copying `photosCount`'s `${count} фото` would have needed three-way agreement in three of the six locales and gets it wrong at 2. Both keys are written in all six languages rather than inherited from `en`, which took the total to 545. Related bug found and fixed while writing the suite, and it is the repo's own guard finding it: the first draft of `__tests__/nav-badge-a11y.test.ts` asserted `bodies.length === 6` over the whole translations file, which `i18n-locales-helper.test.ts` retires repo-wide for a good reason — six hits somewhere in the file is satisfied by ONE locale declaring the key six times. It is now one pattern per locale body that pins the formatter shape and the `${params?.count ?? 0}` interpolation together, so a locale whose label is a constant sentence fails on that locale's name. `npm run verify` green: typecheck, `lint:all` 14/14, 5894 tests (+13), build. No DB migration, no creds.

## The two props that cover two of this app's three platforms (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the top entry of "Paired-prop follow-ups" (2026-08-20), filed by the run that shipped the pairing rule an hour earlier. That entry called this "a bigger hole than the one just closed" and it was right: the pair it had just pinned everywhere covers iOS and Android, and this app's primary surface is the browser. The entry left the approach open — a `Platform.OS === "web"` branch or a prop only react-native-web reads — and the answer turned out to be neither. -->

- [DONE: 2026-08-20] [est: 16000] **Hide the decorative icons from the browser too, which the paired rule never asked them to** — `accessibilityElementsHidden` is iOS and `importantForAccessibility="no"` is Android, and react-native-web reads neither. All seven decorative `<Ionicons>` in this tree carried both, passed the guard that shipped an hour earlier, and were announced by every screen reader on the web — the surface this app ships to first. The fix is `aria-hidden`, which is one prop rather than the `Platform.OS === "web"` branch the suggestion expected: react-native-web forwards it to the DOM attribute of the same name. It is an ADDITION to the native pair and not a replacement for it, which is the part worth writing down: React Native's `<View>` derives `accessibilityElementsHidden` + `importantForAccessibility="no-hide-descendants"` from `aria-hidden` (`View.js` does this explicitly), so on a `<View>` the one prop would have been enough — and `<Text>` does not, and `<Ionicons>` renders a `<Text>`. Every one of the seven sites is an icon, so dropping the native pair for the single prop would have silently un-hidden all of them on iOS and Android. The guard is now three-way rather than paired: `HIDE_PLATFORMS` is `["ios", "android", "web"]`, `HIDDEN_BY` maps each to the source shape that HIDES (still matching on the instruction and not the prop name — `importantForAccessibility="yes"` is the opposite instruction, and `aria-hidden={false}` joins `accessibilityElementsHidden={false}` as not-a-hide, with the `{ false }` spelling now accepted too), and a finding carries `missing: HidePlatform[]` so the report says "still announced on web — add aria-hidden (web)" rather than restating the rule. The web pattern needs a lookbehind rather than `\b`: `-` is already a word boundary, so `data-aria-hidden` would have satisfied a `\b` version, and there is a case for that. Both ends stay non-findings by construction — hidden everywhere is done, hidden nowhere never asked to be — which is what stops the rule turning every plain `<View>` into a finding, and is now pinned rather than implied. The two count assertions in `sheet-search-row.test.ts` (2 icons here, 3 in `search-overlay.tsx`) count the web prop alongside the other two, with the same lookbehind. Verified end to end rather than by source match: `aria-hidden` appears 17 times in the exported web bundle, so the prop survives Metro and the `<Text>` spread in `@expo/vector-icons`' `create-icon-set`, which is the step a source assertion cannot see. `npm run verify` green: typecheck, `lint:all` 14/14, 5881 tests (+8), build. No DB migration, no creds.

## The half-fix a person cannot see on their own machine (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the third entry of "Accessibility follow-ups" (2026-08-08) — pin the paired-platform-prop rule globally, not per file. Landed as a third code on the guard that shipped an hour earlier rather than as a fourth guard: it is the same walk over the same two roots, and the tag parser it needs is the one already written. The fourth entry of that section (accessibilityHint on the sheet's Apply/Reset) is left standing; the fifth needs a real screen reader on web. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 14000] **Check the paired platform props everywhere, not in the one file that remembered to** — hiding a decorative node from assistive technology takes `accessibilityElementsHidden` on iOS and `importantForAccessibility="no"` on Android, and shipping one is a fix that works on the platform its author tested and does nothing on the other. It is the only mistake in this guard's subject that a developer cannot catch on their own machine. Eight sites in this tree hide something and all eight carry both — asserted by one file, `sheet-search-row.test.ts`, counting its own occurrences, which is a rule the ninth site would not inherit. `half_hidden` is now the guard's third code, over every element rather than only `<Pressable>`, since the eight sites are icons, views and text. The pairing is on the INSTRUCTION and not the prop name: `importantForAccessibility="yes"` is the opposite instruction and wants no iOS partner, and `accessibilityElementsHidden={false}` is not a hide either — both would be false positives for a rule that matched names. Adding it meant generalising the tag walk to any JSX element, so the two label rules and this one now share one `openTags` generator: two scanners over one file would be two chances to disagree about where a tag ends, which is the part of this that was hard to get right. Red run checked by hand with a one-sided `<View>` dropped into `components/`; the run names the file, the line and both props. The per-file case keeps its COUNT assertion — two decorative icons in that component is a fact no repo-wide rule can state — with a comment saying which half moved. 4 files, 6 new cases. `npm run verify` green: typecheck, 14/14 guards, 5874 tests, build exported. No DB migration, no creds.

## Six unnamed buttons on the bar a screen reader meets first (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Accessibility follow-ups" (2026-08-08) — sweep the codebase for the other icon-only Pressables, since the filter sheet's clear button is unlikely to have been the only one. Decomposed in two, because the sweep found the work-list and the guard that would stop a fourth is a separate change: the fix is below and DONE, the guard is the open entry under it. The third and fourth entries of that section (the paired-platform-prop rule, the Apply/Reset hints) are left standing; the fifth needs a real screen reader on web. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 20000] **Add `lint:a11y-icon-labels`, so the fourth unnamed icon button cannot land** — the three fixes below are pinned by name, which left every OTHER file free to grow an unnamed icon button. `lib/check-a11y-icon-labels.ts` is now the fourteenth guard, over two codes: `unlabeled` (an icon-only `<Pressable>` with no `accessibilityLabel`) and `untranslated` (a label written as a bare string literal, which is the `<CurrencyInput>` bug — English spoken to speakers of all six languages, and a rule that only asks whether a label EXISTS passes it). The scanner is hand-written rather than a regex and the reason is measured: `/<Pressable([\s\S]*?)>/` reported ten offenders on this tree and seven carried a label, because the open tag it captured ended at the `>` inside `onPress={() => x}` and the attribute text it tested stopped before the label. So the open tag is walked with a brace counter that skips string literals, and the close tag with a depth counter so a button inside a pressable card does not steal its parent's body. All seven false positives are cases in `__tests__/check-a11y-icon-labels.test.ts`, alongside the self-closing tag, the `<PressableRow>` near-miss, the quoted `">"`, the icon-beside-`<Text>` exemption, and an unclosed tag that must report nothing rather than hang. Comments are blanked through `stripComments`, so this module's own doc block can show the forbidden shape — the fixture proving that is the third entry in `strip-comments.test.ts`'s quoted-source list, with its reason. Wired the way the other thirteen are: `LINT_GUARDS` (one edit, which is what puts it in `lint:all`, `lint:ci` and ci.yml), a `SCANNED_FLOORS` entry sharing `check-clarity-input-mask`'s 46 over the identical `.tsx` walk, a `FLOOR_WALKS` row so the single-root property is enforced, a partial-root fixture, and a planted-offender case whose plant carries an arrow in its attribute — a plant without one would be caught by a scanner this guard has already outgrown. One shared rule widened: guard ids now allow digits, because spelling out "a11y" to satisfy `/^lint:[a-z-]+$/` would make the script harder to find than the check is worth. 8 files, 20 new cases. `npm run verify` green: typecheck, 14/14 guards, 5868 tests, build exported. No DB migration, no creds.

- [DONE: 2026-08-20] [est: 22000] **Name the icon-only buttons a screen reader could not** — a `<Pressable>` whose only child is an `<Ionicons>` has no text to announce, so without an `accessibilityLabel` it reads as "button" and nothing else. A brace-aware sweep of `app/` and `components/` found 17 icon-only buttons and three unnamed ones, and the expensive one was the whole bottom navigation bar: six tabs, six identical unnamed buttons, on the surface a non-visual user meets first and uses most often. `NavItem` now takes a REQUIRED `label` — optional is what let six tabs be added without one — and `<NavTab>` renders it with `accessibilityRole="button"` and `accessibilityState={{ selected: item.active }}`, since which tab you are standing on is said today by a coloured icon and a dot, neither of which reaches a screen reader. `<BottomNav>` supplies all six from existing keys (`goHome`, `searchTitle`, `marketplaceTitle`, `chatsTitle`, `friends`, `profile`), so `t()` stays in the screen and the leaf stays presentational. The other two were `<PhotoPreview>`'s reorder chevrons, which get separate labels because two buttons sharing one is worse than none — it says there are two and nothing about which is which. Related bug found in the same sweep and fixed here: `<CurrencyInput>`'s more-currencies chip HAD a label, hardcoded in English, so a Polish speaker's screen reader announced "More currencies" — it and the two new photo labels are now `t()` calls with all six locales written by hand (`currencyMore`, `photoMoveBack`, `photoMoveForward`), which moves the base map from 540 to 543 keys with every row rising together. `currencies.test.ts` pinned the English string and now pins the call, which is what makes the hardcoding a regression rather than a preference. 8 files, 6 new cases. `npm run verify` green: typecheck, 13/13 guards, 5832 tests, build exported. No DB migration, no creds.

## The last hand-written copy of the soft destructive recipe (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the third entry of "Danger-icon-button follow-ups" (2026-08-16) — `app/collection/[id].tsx` is the lone holdout importing CARD_BG_10 + DANGER_SOFT_2 directly, and the cheapest resolution may be a tone/radius seam on <DangerSection> rather than leaving one screen permanently outside the table. The fourth entry of that section shipped as the required-key rule directly below; the first two are design decisions with nobody assigned, the fifth shipped as the remeasure tool. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 15000] **Give `<DangerSection>` a shape seam, and bring the last holdout inside** — `lib/danger-surface.ts` owns the reversible-destructive trio (`CARD_BG_10` + `DANGER_SOFT_2` + `DANGER_DEEP_4`) so the two widgets that wear it cannot drift a channel apart, and `soft-destructive-chip.test.ts` swept `app/` and `components/` for anyone still holding it by hand. It found exactly one: the delete button on `app/collection/[id].tsx`, which was left alone on purpose because its geometry is the block form at `RADIUS_CARD` and adopting the widget would have rounded its corners — a design decision, not a refactor, so the suite pinned it as a known exception and the suggestion asked for a seam. The seam is `shape?: "pill" | "block"`, defaulting to the capsule. Tone entries now carry colour and alignment, shape entries carry radius and padding, and every button spreads one of each — split that way because the pair that must not drift is the colours, and a shape that restated them would be a third home for the trio; a case asserts neither shape entry mentions a colour. The screen renders `<DangerSection shape="block">` and drops two style entries plus all three token imports, so the sweep now asserts an EMPTY list: the rule is the whole rule rather than a rule with an exception attached. The block numbers (`RADIUS_CARD`, 18/16) are the screen's own, so the button is pixel-identical to what shipped — this repaints nothing, which was the objection that kept the holdout out for a year. Settings is untouched: its two call sites take the default and the pill entry carries the exact 16/14 they had. Also updated `design-tokens.test.ts`, which pinned the screen's token list and named the three that left. 4 files, 3 new cases. `npm run verify` green: typecheck, 13/13 guards, 5826 tests, build exported. No DB migration, no creds.

## The four keys that cannot wait for the next translation pass (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the fourth entry of "Partial-language follow-ups" (two translation keys landed in six locales by hand and nothing checks that a seventh locale gets them). The other four of that section are left standing — one is a decision with nobody assigned to it, one wants a per-screen coverage signal the app does not have. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 18000] **Say out loud which keys no locale may inherit** — every locale map in `lib/i18n-context.tsx` opens with `...en`, so an untranslated key renders as English under another flag, and `lib/i18n-coverage.ts` measures that as a percentage per language with a floor under it. That shape is right for the map as a whole and it deliberately says nothing about WHICH keys, because a new English string ships ahead of its five translations and a rule against that would redden every feature PR. Four keys are not like the other 538. `languagePartial` is the badge that tells a reader their language is incomplete: served from the base map it is a warning about the English fallback, written in English, on the chip of the language it is warning about. `languagePartialHint` is the same warning as the chip's accessibility label, which is the only form of it a screen-reader user gets. `language` and `languageSubtitle` head the one screen a reader opens BECAUSE the interface is not in a language they chose. All six locales declared all four already — by hand, and by nobody checking, which is exactly what the suggestion filed against the badge's own ship-run said. `lib/i18n-required-keys.ts` now carries `REQUIRED_IN_EVERY_LOCALE`, a reason per key in the shape `NEVER_WALKED_REASONS` and `UNCOUNTED_MODULE_REASONS` use, plus `findRequiredKeyGaps` over coverage rows (grouped by key, not by language: one key missing from three locales is one translation task, three keys missing from one locale is three), `describeRequiredKeyGap` carrying the reason into the failure so the person meeting it does not have to go read a constant to find the argument, and `unknownRequiredKeys` for the quiet direction — a typo'd entry can never be reported as a gap, so it would read as six languages in perfect order. The list is audited from both ends: every entry must name a key the base map declares AND a key `app/settings.tsx` still renders, and every reason must actually argue about the fallback rather than about the key being important — a bar the suite enforces by matching the sentence, which sent two of the four reasons back to be rewritten. `translationKeysUsed` reads literal `t("key")` calls only, with a word boundary so `format("x")` is not mistaken for the helper. Mutation-checked by deleting `de`'s `languagePartial`: the run names the language, the key and the reason. 2 files, 12 new cases. `npm run verify` green: typecheck, 13/13 guards, 5823 tests, build exported. No DB migration, no creds.

## A premise that says each root walked to the RIGHT something (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Counted-property follow-ups" (the premise checks each root walked to SOMETHING and not that it walked to the right something) together with the third of the same section (three things now check one property and the division of labour lives in a code comment inside one of them). Both filed by the change directly below. One change: the premise is what the division-of-labour paragraph is about, so the paragraph lands where all three consumers already look. The other three of that section are left standing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 26000] **Give the counting case a whole premise, and say once who checks what** — the case shipped a run ago asserts every multi-root floor still sits above its largest root, and guarded that claim with half a premise: each root walked to more than zero files. The half it was missing is the one that fails quietly. A root whose extension filter has silently stopped matching part of its tree — `.tsx` renamed away, a `.mts` file landing in `lib/` — still returns a healthy count from the files it does match, so the floor still clears, the case still passes, and the guard under it has stopped reading part of its own subject. `checkWalkPremise` in `lib/floor-walks.ts` now states the whole thing over four codes: `empty_root` (the old half), `count_mismatch` (the guard's filtered walk and an unfiltered walk of the same root disagree over one directory, which is what a drifting skip list looks like from the outside), `unused_extension` (an extension the walk declares that matches nothing under any of its roots) and `uncounted_extension` (a root holds module code under an extension no declared set covers and no entry excuses). Pure, with the sentences in a private table exhaustive over the code union and `describeWalkPremiseProblem` the only way out of it, so no caller can print half a sentence — the same shape `lib/scanned-floor.ts` uses for `ScannedFloorProblem`. The universe it subtracts from is a new `MODULE_EXTENSIONS` in `lib/source-dirs.ts`, deliberately wider than anything this repository holds (`.jsx`, `.cjs`, `.mts`, `.cts` appear in no file here) so the first one to arrive lands in a check rather than in nothing; `UNCOUNTED_MODULE_REASONS` excuses `.js` and `.mjs` by name with the boundary they sit the right side of — `scripts/serve-dist.js` is run by node, the three `.mjs` stubs are resolved by node's loader — and `unusedUncountedExcuses` fails if an excuse ever names an extension the scan roots no longer hold. A markup walk's uncounted `.ts` is read as a narrowing rather than a hole, because a declared set does count those, and the nesting that shorthand rests on (MARKUP ⊆ SOURCE ⊆ MODULE) is now asserted instead of assumed. `scripts/guard-io.ts` grew `tallyExtensions`, the unfiltered half of `listSourceFiles`, so the suite and the script take one walk rather than each writing their own. `npm run remeasure-floors` runs the premise too and prints each problem under the row it undermines, exiting 1 for them: a tool that only ever saw the filtered walk would answer an eroded root by suggesting a SMALLER floor, writing the erosion into the table as the new normal. Red path checked by dropping one `.mts` file into `lib/` — two walks report it by name and the run exits 1. The header of `lib/floor-walks.ts` now names all three enforcements and what each is for (cheap counting case, spawning fixture proof, human advisory), which is the question the second suggestion asked and the answer that had been living in a comment inside one of the three. 5 files, 11 new cases. `npm run verify` green: typecheck, 13/13 guards, 5811 tests, build exported. No DB migration, no creds.

## The biggest floor, and the property nothing in CI was checking (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Remeasure-tool follow-ups" (the partial-root suite skips `check-problem-phrasing-imports` on cost, leaving the largest floor with no run behind it) together with the second (two things now check one property and nothing says which is the advisory and which is the proof). Both filed by the change directly below. One change: the answer to the second is what makes the first affordable. The other three of that section are left standing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 7000] **Enforce the single-root property by COUNTING, for every walk, not just the four a fixture can afford** — the change below gave `FLOOR_WALKS` five entries and CI checked the property for four of them. The fifth is `check-problem-phrasing-imports`, which is the LARGEST floor in the table (450) with its biggest root at 430 — 96% of the way there, the entry likeliest to move next — and it is the one `lint-guard-partial-root.test.ts` skips, because each of its five fixtures would copy a 400-file `__tests__/` tree. So the floor with the least headroom had its property enforced by nothing: the fixture suite could not pay for it, and `npm run remeasure-floors` computes the same verdict but a tool nobody is required to run is not enforcement. One case in `__tests__/floor-walks.test.ts` closes it — `measureFloorWalk` over counts from `listSourceFiles`, no spawn, all five walks — and asserts each root walked to something before comparing, since a root that walked to zero would satisfy the claim for a reason that has nothing to do with floors. The failure names the root, the count, the floor, and the number `remeasure-floors` would suggest. That also settles the question the second suggestion asked, in a comment on the case: this is the CHEAP enforcement and covers every walk; the fixture suite is the PROOF that the guard binary really refuses and covers the four whose trees are small enough to copy; the script is the human-facing report. Mutation-checked by lowering the phrasing floor to `__tests__/`'s own count — the new case turns red alone and prints `431 … clears its floor of 430 … it suggests 513`. 2 files, 1 new case. `npm run verify` green: 5798 tests, 1313 suites, 13/13 guards, build exported. No DB migration, no creds.

## The four manual steps that ran four times (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Partial-language follow-ups" (`check-inline-hex`'s floor re-measured three times for the same event, filed by the change below) together with the fifth entry of "Danger-icon-button follow-ups" (the same request against `check-inline-radius`, filed four days earlier). Two independent askers for one script, which is usually where a script gets written. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 14000] **`npm run remeasure-floors`, and the arithmetic behind it in `lib/`** — a multi-root floor carries a property its own number cannot state: no single scan root may clear it alone, or a walk that lost every other root still reports green. `__tests__/lint-guard-partial-root.test.ts` enforces it and goes red the day an ordinary file lands in the largest root — three times now for `check-inline-hex` (twice by `lib/`, the third being the change directly below) and once for `check-inline-radius` — and the failure arrives in a suite with nothing to do with the change that caused it. The fix was the same four steps every time: count the roots, find the largest, pick a number above it with slack, rewrite a note. This does the counting and prints the diff: declared minimum, what the walk holds now, the per-root breakdown, the largest single root, and the suggested new number when the current one no longer clears it. It DECIDES nothing — a script that rewrote `SCANNED_FLOORS` would erase the reasoning the table exists to hold, so the output ends `the note is the half this tool cannot write`. `lib/floor-walks.ts` is the shared statement of which roots each floor is measured over, taken from the suite that had the only copy, and it is its OWN module rather than a second table in `lib/scanned-floor.ts` for a reason found the hard way: it is keyed by the same check names, and `entryPatch` locates an entry by `indexOf('  "name": {')` — two tables keyed alike in one file means the fixture that breaks an entry silently targets whichever comes first. The arithmetic (`measureFloorWalk`, `formatFloorMeasurement`) is pure and in `lib/` for the reason every guard here splits that way, and here it is load-bearing: the cases worth testing are a floor LEVEL with its largest root and a tree so lopsided that the property and the slack disagree, and no checkout of this repository produces either — a test that could only run against the real tree would be a test of today's numbers. So `>` and not `>=` is pinned at the boundary, the lopsided tree proves the property beats the slack (98 of 100 files in one root suggests 99, not the 75 a quarter-slack would give), an empty root list throws rather than inventing a zero floor, and the report's two branches are pinned in both directions — including that a floor with MORE room than the target says nothing, since a tool that tells its reader to tighten a working floor is one nobody runs twice. Proved end to end by lowering `check-inline-hex` to `lib/`'s own count: the script says `MOVE`, names the root, and suggests exactly the 168 that was picked by hand below. The `check-inline-hex` note is corrected to the post-change tree (225 files, `lib/` 161) and now names the command. `npm run verify` green: 5797 tests, 1313 suites, 13/13 guards, build exported. No DB migration, no creds.

## The English string under a Polish flag, said in the picker that offers it (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Translation-coverage follow-ups" (the number is measured and nothing acts on it — four locales below half, offered at parity) together with the fourth entry of "Danger-icon-button follow-ups" (a silent `...en` fallback means "six languages supported" is not the honest number to publish). One change: the measurement existed, the picker was the surface that contradicted it. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 15000] **Badge the partly-translated languages in the settings picker** — `lib/i18n-coverage.ts` has measured the gap since it shipped and nothing acted on it: every locale map opens with `...en`, so an untranslated key renders as an English string under a Polish flag, and four of the six locales sit below half (`be` 41%, `pl` 40%, `de` 39%, `es` 39%) against `ru` at 99%. The settings picker listed all six in the same chip, the same weight and the same order — a promise of parity the app cannot keep, and a reader who picked one of the four met a mostly-English interface with no way to know that was expected. Badged rather than hidden or reordered: hiding takes a language from the reader who wants it half-translated and knows it, and sorting by coverage rewrites a familiar list every time a translation lands; a badge changes what the picker CLAIMS without changing what it offers. `lib/translation-status.ts` is the verdict the picker can import — `lib/i18n-coverage.ts` PARSES the source of `lib/i18n-context.tsx`, which is a thing a test does and an app cannot. It commits the CLASSIFICATION and not the percentages, for the reason `TRANSLATION_FLOORS` gives next door: "de is at 38.8%" goes red on every feature PR that adds an English string, because the denominator moves and every row falls, while "de is one of the partial ones" is stable under exactly that churn and still catches both events worth catching — a locale finished and still badged, and one that collapsed into the set with nothing said. `__tests__/translation-status.test.ts` derives the set from the real measurement and asserts equality (premise first: fewer than six rows and the comparison proves nothing), pins that the base language and `ru` stay unqualified, that every badged code is one the picker actually offers, that the two groups are ≥20 points apart so the 90% threshold separates two genuinely different things, and that an unmeasured code answers false rather than throwing inside a render. The badge sits UNDER the label rather than beside it — `Беларуская` plus a badge on one line pushes the wrapped row to three lines on a narrow phone — and the qualification goes on the Pressable's `accessibilityLabel`, because a badge announced after the language name is one a reader meets after they have already chosen. Two new keys, translated into all six locales rather than left to inherit, which is why every coverage row ROSE by two. Two unrelated fixes on the way: `check-inline-hex`'s floor was drawn level with `lib/`'s own count by the new module (160 = 160, so a walk that lost both `app/` and `components/` would pass) and is re-measured to 168 of 224 — the third time for that reason and the second time by `lib/`; and `__tests__/lint-guard-invalid-floor.test.ts` hardcoded `minimum: 160` in the rewrite it patches the table with, so on the run that raised the floor the mutation silently matched nothing, the "broken" copy stayed valid and two cases failed claiming the guard would not refuse — it reads the number from `SCANNED_FLOORS` now, since a fixture that hardcodes the value it mutates breaks on exactly the maintenance it exists to survive. Mutation-checked two ways: dropping `es` from the list turns the equality and the gap cases, adding `ru` turns those two plus the unqualified-locale case. 7 files, 5 new cases. `npm run verify` green: 5786 tests, 1310 suites, 13/13 guards, build exported. No DB migration, no creds.

## The silence five suggestions asked about, and the two clauses nobody saw together (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the same observation filed FIVE times across five sections — "check-bundle-secrets walks `dist/`, which git ignores entirely, and now reads as the odd one out" (Committable-set), "the bundle scan is the other reader of the working tree and it does not honour the convention" (Local-only module), "check-bundle-secrets walks `dist/`, and a build could put a symlink in it" (Regular-file), "check-bundle-secrets walks and therefore has no unread clause, which now reads as a gap rather than a decision" (Unread-clause), and "the verdict lives in a table the second secret guard cannot reach" (Unread-fail, filed by the change below). A suggestion filed five times over two days is a reader failing to find something. Combined with "Nothing asserts the two clauses coexist on one line" (Unread-clause) and "Fail beats pass-clause, and no case shows the two candidates side by side" (Unread-fail): both are the sentence a reader meets on a strange checkout, pinned by nothing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 11000] **Say why the bundle scan walks, pin it against git, and run the two-oddity checkout** — `check-secrets` takes its candidates from `git ls-files` and NAMES the set in its report; `check-bundle-secrets` walks and said nothing about what it looked at, so a reader meeting the two side by side could not tell "not applicable" from "not thought about". Five suggestions in five sections asked for the same sentence. It is written now, and it is not a fallback story: `.gitignore` covers `dist/` whole, so git's listing names NOTHING under it and asking would hand the scan an empty candidate set over a directory that exists and is full — and the set this guard reports on is not what git would take but what the browser was SENT, which is exactly what is on disk. Three properties ride on that walk and each is free only because it walks: the regular-file question (`entry.isFile()` on every entry, which is the hole `partitionRegularFiles` exists to close on the git path), the unread clause (a walk only ever names what it just met), and the `.local.` convention (nothing generates such a name into `dist/`, and a build that emitted one would have SHIPPED it, so honouring the rule here would skip the one copy that matters). Prose nothing can break is how the `NEVER_WALKED` drift started, so `__tests__/bundle-walk-premise.test.ts` turns each half red on a change: git's listing holds no path under `dist/` (asserting its own premise first — git must have listed something under `scripts/`, or the case passes over a listing that came back empty for its own reasons), `.gitignore` still covers `dist`, `BUNDLE_SKIP_DIRS` is still empty against a source list that is not, and the guard's code — through `stripComments`, because the header EXPLAINS the omission and a raw match reads that explanation as the thing it forbids — still does not reach for `isLocalOnlyPath`. Second half is the sentence a reader actually meets: the local-only skip clause and the unread clause are appended by one `map`, each proven alone and never together, so one fixture now plants BOTH (an ignore file that deliberately does not cover `*.local.*`, since the repository's own would drop the file before the scan saw it) and asserts the one line, both commas, git's verdict still on the skip clause, and the clauses following the subject they qualify. And the precedence the two single-reason cases cannot show: a checkout holding an unread symlink AND a committed file the working tree lacks must FAIL, name the missing file, and leave the link out of a refusal that tells its reader to restore things — a link that is exactly where it should be is the wrong instruction. Mutation-checked two ways, each turning exactly two cases red and leaving the other pair green: dropping the unread clause turns the two clause cases, making every unread kind benign turns the two refusal cases. 3 files, 7 new cases. `npm run verify` green: 5781 tests, 1309 suites, 13/13 guards, build exported. No DB migration, no creds.

## The committed file the scan vouched for without reading (2026-08-20)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Unread-clause follow-ups" (a committed file the working tree does not hold is REPORTED and still passes) together with the third of that section (the `kind` strings are prose and compared as data — a code plus a rendering table). One change: the fail/pass decision the first needs is exactly the code-vs-prose separation the third asks for, so the guard branches on a stable code and the report prints the sentence. The other three of that section are left standing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-20] [est: 13000] **Fail the run over a committed file git listed and the working tree did not hold** — the change directly below told the two unread reasons apart and then gave them the same outcome: both a tracked symlink and a path git lists that is missing from the working tree landed on the pass line and the run exited 0. A symlink is a repository that uses links, and passing is right — following it would report a file the repository does not contain. A path in `git ls-files` that `lstat` cannot find is the opposite: a COMMITTED file this scan did not read, inside a run whose last words are `no committed secrets`. Vouching for a file it never opened is the one thing a secret guard may not do, so this fails the run — the same outcome an unopened archive already gets, and for the same reason. `IrregularKind` in `scripts/guard-io.ts` stops being the sentence a report prints and becomes a stable CODE (`not-regular-file` / `missing-from-worktree`), with `describeIrregular` rendering the prose and `isUnreadCommitted` carrying the fail/pass verdict off one `IRREGULAR_KINDS` table — so the wrapper branches on the code and the report reads the table, the way `SecretMatch` already separates `ruleId` from `description`, and a report reword is a table edit rather than a comparison changing under it. `formatUnreadCommitted` in `lib/secret-scan.ts` is the refusal, naming the files and prescribing `git checkout -- <path>` rather than `git rm --cached` (the file is meant to be there; this is an incomplete checkout, not a leak). The pass-line clause keeps only the benign symlinks, rendered through `describeIrregular`. The run case that used to assert this state passes now asserts it FAILS with the refusal, names the file and the fix, and shows the "scanned N file(s)" pass line is gone. Unit cases pin the table (`describeIrregular` for both codes, `isUnreadCommitted` true only for the missing one) and the new formatter (empty at zero, counts, names, sorts, `git checkout` not `git rm`). `partitionRegularFiles`'s two assertions moved to the codes. `npm run verify` green: 5774 tests, 1308 suites, 13/13 guards, build exported. No DB migration, no creds.

## The number that told a reader something had happened and not what (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Regular-file follow-ups" (the count saying how many and never which) together with the fourth (a symlink and a path missing from the working tree arriving under one number, when one is normal and the other means a committed file went unscanned). Both were filed by the change directly below, and both are about the same clause. The first, third and fifth of that section are left standing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 12000] **Name the listed candidates the scan did not read, and tell the two reasons apart** — the change below landed `1 listed path(s) not regular files` in the subject line, which is enough to notice a change and not enough to act on one: a tracked symlink with a scanned extension is an odd thing for a repository to hold, and the run reporting one handed its reader no name to go and look at. Worse, it summed two events that are not alike. A symlink is a repository that uses links — normal, and unread because following it would report a file the repository does not contain. A path git lists that the working tree does not hold is a checkout disagreeing with its own index: a COMMITTED file this scan did not read, inside a run whose last words are `no committed secrets`. One count gave them the same weight. `partitionRegularFiles` carries a `kind` per path now — `lstat` returning a non-file is one answer, `lstat` throwing is a different one, and every errno lands in the second because they all mean the same thing to a scan. `formatUnreadCandidates` in `lib/secret-scan.ts` renders them in the shape the local-only clause next door already uses: count, up to five names each with its reason, `+N more` for the tail, empty at zero so a clean checkout's pass line is unchanged. It is a CLAUSE rather than more parenthesis on the subject, because "which set did you read" and "what in it did you not read" are two sentences, and a bracket that itemises its own exceptions is a line nobody finishes. Sorted by code unit and not `localeCompare`, which is the comparison whose entire job is to differ between machines and which put `docs/link.md` before `PRIVACY.md` on the first run of the case that caught it. The second reason gets its own run: a fixture that commits a file and then removes it from the working tree alone, which is what a half-finished checkout leaves and what nothing had ever handed this guard. Mutation-checked by collapsing the clause back to the count it replaced, which turns exactly the three new cases red and no others. 5 files, 3 new cases. `npm run verify` green: 5771 tests, 1307 suites, 13/13 guards, build exported. No DB migration, no creds.

## The candidate the walk never returned and the listing always did (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Conflicted-index follow-ups" (the index stages being able to disagree about what KIND of thing a path is, which is what sent this looking at what the guard does with a non-file) together with the third entry of "Committable-set follow-ups" (`selectPaths` and `listFilesUnder` sharing their filters by convention rather than by construction, compared only over this repository's own tracked files). One change: both are about the two candidate sources answering the same question, and the answer they did not share was not a filter at all. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 13000] **Ask the listed candidates what KIND of thing they are, the way the walk always did** — `selectPaths` was written so the git listing and the walk could not drift on which candidates they take, and it can only apply the filters that are decidable from a string: extension, skip-list segment. The walk applies a third that is not in the string. `entry.isFile()` is false for a symlink, and `git ls-files` lists one, because git tracks it — so the two sources agreed on every name and disagreed on one whole class of entry, in the direction that makes the scan WIDER than the tree it was pointed at. `readFileSync` follows a link: a tracked `docs/link.md` was read as whatever it pointed at, and a target outside the scan root (`../../secrets/real.env`, an absolute path, anything at all) came back reported at the LINK's path as a credential this repository committed. It did not commit it; git holds forty bytes of target path. Proved before it was fixed — the run case fails against the old code with `[aws-access-key-id]` at `linked.md`, over a file in a temp directory the guard was never given. `partitionRegularFiles` in `scripts/guard-io.ts` puts the walk's question to the listed candidates, `lstat` rather than `stat` since not following the link is the entire point, and anything that cannot be stat'd at all is irregular too — a path the index holds and the working tree does not is not a file this scan can read, and not one it should be silent about. Which is the second half: the count goes in the pass line beside the one that was already there, `git's committable set (4 working-tree file(s) not in it, 1 listed path(s) not regular files)`, both silent at zero and joined inside ONE parenthesis so a clean checkout's line is unchanged and the line never grows a second bracket. The `notListed` count is built from the picked lists rather than the scanned ones on purpose: a symlink is in git's list and never in the walk, so counting it in both would report one entry twice under two names. Mutation-checked: replacing the partition with a pass-through turns exactly the new run case red and prints the out-of-root credential, which is the bug reproduced rather than described. 5 files, 5 new cases (three unit over the partition, including an equality asserting the listing's answer comes back to the walk's over the same tree, one over the two-clause subject line, one run). `npm run verify` green: 5769 tests, 1307 suites, 13/13 guards, build exported. No DB migration, no creds.

## The name the refusal printed, which was not the name on disk (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Conflicted-index follow-ups" (two callers of `git ls-files` deduplicating independently, with the note that they split on different characters "for reasons that are themselves load-bearing") together with the fourth (the refusal's count asserted at one and its message built for many). Writing the shared reader is what showed the second split was not load-bearing but a defect: it was the reason to look. The second, third and fifth of that section are left standing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 12000] **Read git's listing NUL-separated in both callers, and read it once** — the suggestion asked for a shared reader and answered itself with "the two split on different characters (`\0` and `\n`) for reasons that are themselves load-bearing". They were not. `git ls-files` ESCAPES a name it considers unusual unless it is asked for `-z` — `café.local.m` comes back as `"caf\303\251.local.m"`, quotes and octal included, because `core.quotePath` is on by default — and `trackedAmong` was the caller reading lines. That answer goes straight into `formatTrackedLocalOnly`, a refusal that NAMES files and tells its reader to `git rm --cached` them, so the guard's response to a genuinely committed credential was an instruction that fails when followed: `git rm --cached '"docs/powerbi/caf\303\251.local.m"'` matches nothing, over a file that really is a leak. The line reader had a second edge the escaping would never have covered: `.trim()` on each line, and a leading space is a legal filename git does NOT quote, so ` lead.local.m` was reported as `lead.local.m` — a path that does not exist, named alongside ones that do. Both callers ask for `-z` now and both go through one `namesFrom`, which splits on NUL, drops the trailing empty field and deduplicates; the three paragraphs that were spread across two functions (why `-z`, why the dedup, why no trimming) are written once beside it, and `listCommittable` keeps its `.sort()` at the call site because it is the only one of the two a report counts through. One unrelated placement fixed on the way: `listCommittable`'s doc comment sat above `CommittableAnswer`, so the type had the prose and the function had none. Three cases at the unit level and one at the run level, and the run case is what proves the product: two force-added `.local.` copies, one of them accented, asserting the refusal counts `2 file(s)`, prints both names as they are spelled on disk, sorts them, and contains no `\303` anywhere — which also gives the PLURAL path of that formatter its first spawn, having been exercised only by unit cases over the string. Each case asserts its own premise before its claim, so a checkout with `core.quotePath=false` or a git that stopped quoting fails loudly instead of passing over a bug it never met. Mutation-checked two ways, each turning exactly the expected cases red: restoring the exact pre-change reader (line split, trim, no `-z`) turns the three unit cases and the run case, and printing the escaped spelling in the report; adding the trim back INSIDE the shared reader turns only the two that are about trimming, which is what separates the two defects. 4 files, 4 new cases. `npm run verify` green: 5765 tests, 1306 suites, 13/13 guards, build exported. No DB migration, no creds.

## The sentence in the doc comment that turned out to be half true (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Index-state follow-ups" (nothing exercising a path at three index stages, which is the case `listCommittable`'s dedup exists for) together with the second (what "tracked" MEANS being a property of git this repository leans on and never states). Building the fixture the fifth asked for is what turned this from a test into a bug fix. The first, third and fourth of that section are left standing. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 11000] **Build the conflicted index, and dedup the OTHER caller of `git ls-files`** — `listCommittable` has said since it shipped that `--cached` lists a path once per stage during a merge and that the answer is deduplicated for exactly that reason. Nothing ran it: a conflicted index was a state no fixture could produce, so the sentence guarded a count that feeds the scanned floor and rested on somebody's reading of a man page. `stageConflicted` writes the three stages through `update-index --index-info` — a separate function rather than a fourth array on `GitRootOptions`, since the other two options say WHICH paths get a state and this one needs three blobs per path, and built directly rather than by provoking a real merge because a merge needs two branches, a shared ancestor and a genuine textual conflict, every one of which is a way for the fixture to end up NOT conflicted while still looking like it worked. Writing the case is what found the bug: `trackedAmong` asks `git ls-files -- <paths>`, which prints a conflicted path three times, and it did not deduplicate — so `formatTrackedLocalOnly`, whose whole first line is a COUNT, reports `3 file(s) are tracked by git AND skipped` over one file listed three times. A guard overstating its own finding in the report a reader acts on, and sending them after two files that do not exist. It deduplicates now, with the reason written beside it rather than in a commit message. The premise is asserted before the dedup is: `git ls-files -u` must return three lines, or the case passes over a repository that was never conflicted and proves a dedup that had nothing to remove. Second half is one paragraph in `lib/local-only.ts`: "tracked" here means IN THE INDEX and not in HEAD, which is what makes the refusal cover a file force-added a minute ago and not yet committed — a property of git this repository depends on rather than one it chose, so it is pinned against a fixture in that state instead of being left to the reading of a command name. Mutation-checked three ways, each turning exactly the expected cases red: dropping the new dedup turns 2 (the unit answer and the guard's report), dropping the OLD one turns 1, and a fixture that writes one stage instead of three turns the premise assertion alone — which is the case that stops the other two passing vacuously. 5 files, 3 new cases. `npm run verify` green: 5761 tests, 1306 suites, 13/13 guards, build exported. No DB migration, no creds.

## The state between the two the fixtures could build (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Force-add follow-ups" (every caller of `forceAdd` wanted a file that is merely STAGED, and that state had no fixture) together with the fifth (a scratch root can now become a work tree and `makePartialRoot`'s doc does not say so). One change: both are about what a fixture can express and whether a reader can find out. The fourth entry of that section is dropped rather than left standing — it asks for the count clause's silence at zero to be pinned "for the right reason", and `__tests__/secret-scan.test.ts` already asserts `formatScanSubject` at 0 AND at 4 directly, so the hole it describes is closed by a case it did not know about. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 9000] **Build the file that is in the index and not in HEAD, and refuse it** — `forceAdd` shipped a run earlier and committed what it added, which was free: every question these guards ask git (`ls-files`, `ls-files --cached`) answers a staged file exactly as it answers a committed one. Free is not the same as proven, and the state that had no fixture is the one a developer is ACTUALLY in — a minute after `git add -f`, before any commit, which is when a mistake is still cheap to undo and exactly when `npm run lint:secrets` should be refusing. `stage` force-adds AFTER the commit, so the paths land in the index alone, and the two lists may not name the same path: committing something and then re-adding it is the committed state, so accepting both would hand a case a fixture it did not ask for. Three cases pin the shape from three sides — `trackedAmong` calls a staged file tracked, `git ls-tree HEAD` holds only `.gitignore` (which is what makes the first case about the INDEX rather than a second spelling of `forceAdd`), and `listCommittable` keeps it in the candidate set — and one run proves the guard still refuses it, with the same message the committed case gets. The refusal's whole premise is that being in the index is enough to be a committed credential; until now nothing could produce the state that premise is about. Second half is one paragraph: `makePartialRoot` builds a root that is NOT a work tree, which is the shape half these suites need and the reason the walk fallback has coverage at all, and a reader had to find `initGitRoot` to learn the other half exists. The doc says so now, so the two helpers are read together. Mutation-checked two ways: staging BEFORE the commit turns exactly the HEAD case red, and redefining "tracked" as "in HEAD" turns exactly the two new git-state cases red and leaves the committed ones green — which is the evidence that this is a distinct behaviour rather than a second name for one already covered. 3 files, 4 new cases. `npm run verify` green: 5758 tests, 1306 suites, 13/13 guards, build exported. No DB migration, no creds.

## The half of the fixture that was still copied, and the equality nobody asserted (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the first, second, third and fifth entries of "Fixture-helper follow-ups", filed by the change directly below. One change: all four are about the git fixture — what it can build, how it fails, and the case its two halves were missing. The fourth (splitting `helpers/git-fixture.ts` out of the 1300-line guard-fixture module) is left standing: it is a decision about two homes for "what a scratch root is", which is the drift the change below just removed, and it wants its own answer. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 13000] **Let `initGitRoot` build the interesting half, throw when it cannot, and pin that the two candidate sources agree** — the helper landed a run earlier covering the easy half of a git fixture and left the half it existed for copied: `git add -f` past `.gitignore` is the ONLY way a file the `.local.` convention skips can also be a committed credential, so it is the shape every suite asserting that refusal needs, and two of the three call sites went straight back to spawning git by hand for it. `forceAdd` takes it now, `-f` and in the same commit as `.gitignore`, because a plain `add` would silently take none of them and leave a fixture proving the opposite of what it was built for. Missing paths are refused BEFORE `git init` runs, with the name in the message — `git add -f missing.local.m` otherwise fails in git's words, inside a fixture, several frames from the case that asked for it. Second half, and the reason the first is readable at all: the helper asserted on git's exit status, so "the scratch repository could not be built" arrived in the output as an assertion inside whichever case happened to build it first, indistinguishable at a glance from the guard under test answering wrongly. `GitFixtureError` says which of the two happened, and it separates `run.error` from a non-zero status — a missing binary or an unusable `cwd` leaves `status` null, which the old check rendered as `null !== 0` and explained nothing. Third half is the case the whole line of work had not written: the run suites prove the DIFFERENCES between git's listing and the walk (an ignored file dropped, an untracked one kept) and a filter applied on one path and not the other would leave every one of them green. So the same fixture content runs twice — once as a plain directory, once as a work tree whose ignore file covers nothing — and the two must report the same file, entry and archive counts, over a subject line that DIFFERS, with the `not in it` clause silent because git left nothing out. Fourth: the tracked-probe case exited 1 and matched the refusal's wording, and nothing said the run never reached the matcher — a force-added `.local.` file is in git's listing, so a refactor that kept the message and carried on scanning would print both and stay green. It now asserts the credential is NOT reported, which is what makes the case about the probe. One unrelated bug fixed on the way: `scripts/git-io.ts` carried `trackedAmong`'s doc comment directly above `listCommittable`'s, so the function had no documentation and the type below it had two — moved to the declaration it describes. Mutation-checked three ways, each turning exactly the expected cases red: narrowing the git listing by one directory turns 2 (the equality case and the count clause), printing the tracked refusal without exiting turns 1 and ONLY the new assertion, dropping `-f` from the force-add turns 5 across both suites. 5 files, 4 new cases. `npm run verify` green: 5754 tests, 1306 suites, 13/13 guards, build exported. No DB migration, no creds.

## One way to make a fixture a repository, and the prose that had nothing behind it (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Subject-count follow-ups" (three suites initialising a git repo into a fixture root within a day of each other) together with the fourth (the `.local.` documentation explaining where a rule is load-bearing with nothing enforcing the explanation). One change: both are about the git-mode fixtures — one makes them cheap to write, the other spends that on the case the prose was standing in for. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 8000] **`initGitRoot` beside `makePartialRoot`, and a run for the claim the comment makes** — "is this fixture a repository?" became a property a case chooses the moment `check-secrets` started taking its candidates from git, and three suites answered it by writing the same six lines within a day of each other. That is the count at which a shape stops being a coincidence, and the copies were already free to differ on the one thing that decides the outcome: whether `.gitignore` is COMMITTED, because `--exclude-standard` in a repository with no HEAD is a shape none of these guards has ever been run against. The helper commits it, sets `user.email`/`user.name` explicitly (a machine without them fails `git commit` outright, and that failure would arrive as a mystery inside an unrelated assertion), and pins `init.defaultBranch` so git's hint stays off the stderr these suites grep. The second half is the case the documentation was standing in for: `lib/local-only.ts` now says the `.local.` rule carries its whole weight in the WALK fallback and removes nothing under the git listing, which is a claim about behaviour that only prose asserted — so a run inside a work tree with an ignored `.local.` copy asserts the guard reports `from git's committable set` and prints no skip clause at all, while the existing non-git run still asserts the clause names the file. Two runs, two halves of the guard, and the comment now describes something a change can break.

## The number an ignore rule moves, and where the old rule still carries weight (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the fourth entry of "Committable-set follow-ups" (nothing measuring what the git listing dropped) together with the second (the `.local.` rule reading as dead code under the git listing). One change: both are about a reader being able to see what the new scan subject did, from the run and from the module. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 9000] **Count the working-tree files git's list left out, and say which rule is load-bearing where** — the run below justified the new subject with "a scratch file is not committable" and then reported nothing about how many candidates that removed, which makes the claim unfalsifiable from the run: a `.gitignore` line that starts covering a real directory produces a report identical to the one before it. So the walk runs a second time under git purely to COUNT the difference (one traversal of a tree the skip list has already narrowed), and `formatScanSubject` in `lib/secret-scan.ts` prints it — `git's committable set (4 working-tree file(s) not in it)`, silent at zero, because a clean checkout should not carry a clause about nothing. The subject moves out of the wrapper into the module beside `formatSecretReport` for the usual reason: a report assembled by string concatenation at the call site is one nothing can read. The other half is documentation, and it is not optional: git's listing already drops everything `.gitignore` covers, so `partitionLocalOnly` removes nothing on a normal checkout and its pass-line clause never appears there — a reader now finds a rule that looks dead and a clause that looks unreachable. `lib/local-only.ts` states where each half carries weight: the `.local.` rule is the whole guard in the WALK fallback (tarball export, scratch fixture root, no git binary), and `formatTrackedLocalOnly` matters MORE under the git listing, because a force-added `.local.` file IS in that listing and this rule is the only thing taking it back out.

## The set the report has always claimed to be about (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the third entry of "Local-copy follow-ups" (the scan reading untracked files for every name that is not `.local.`) together with the first entry of "Guard-asks-git follow-ups", filed by the change directly below, which observed that the "ask git inside a work tree, walk otherwise" shape now exists as working code and the fixture question was all that remained. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 15000] **Take the candidates from git when git can answer, and say which set was read** — the report ends "no committed secrets" and the guard was walking the working tree, which is that set only on a clean checkout. The `.local.` rule closed the hole in one direction; this closes the noise in the other, and the noise has a cost: a scratch `notes.md` with a pasted key, an editor backup, a half-finished migration are all read and all reported at the severity of a committed credential, and a guard that fails over files nobody can commit is one people learn to run with their eyes closed. So `listCommittable` runs `git ls-files --cached --others --exclude-standard -z` — `--others` deliberately kept, because a file created a minute ago and not yet added is one `git add -A` from being committed and dropping it would mean a pasted credential passes every local run until the moment it lands; `-z` because git QUOTES unusual names otherwise and a scan that opens the quoted spelling reads nothing; deduplicated because `--cached` lists a path once per stage during a merge conflict. The fixture question that blocked this is answered by keeping the walk: every guard fixture is a temp directory with no `.git` above it, so the fallback is the normal path for the harness rather than an exotic one, and the pass line NAMES which of the two it read (`from git's committable set` / `from the working tree (not checked against git: …)`) — two scans reporting identically over different sets is the failure this whole line of work is about. Both candidate sources go through one `pick`, and `selectPaths` in `scripts/guard-io.ts` applies the walk's own two filters (case-folded extension, skip-list by path SEGMENT) to a list nobody walked, so the two cannot drift on which candidates they take. Proved by a run inside a work tree with an ignored offender (passes), one with an untracked offender (fails), one outside git at all (walks, and says so), and a case asserting the git listing loses no tracked file the walk finds.

## The claim the skip rests on, asked by the guard instead of the suite (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Local-copy follow-ups" (the two git-backed assertions living in the suite rather than in the guard) together with the second entry of "Local-only module follow-ups", filed by the change directly below — the wrapper accumulating skipped names into module-level state from inside a `filter` predicate. One change: both are about the walk's two halves and what the guard can establish about them on its own. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 14000] **Ask git about the files the `.local.` rule removed, in the guard, when it removes one** — the convention rests on a claim about git: these files cannot be committed, so not reading them costs nothing. `git add -f` beats `.gitignore`, which makes a tracked `.local.` file a COMMITTED credential sitting outside the scan — the one way this rule can hide a real leak — and the check for it lived in `__tests__/local-only.test.ts`. So `npm run lint:secrets` on its own would walk straight past one in any checkout where the suite was not run, which is every checkout where somebody is debugging a guard. The guard asks now: `scripts/git-io.ts` (the git half of the premise, deliberately outside `lib/` for the same reason `guard-io.ts` is — `lib/local-only.ts` stays pure and spawns nothing) runs one `git ls-files --` over the skipped names, and ONLY when a name was skipped, so a normal checkout still spawns no child process at all. A tracked one fails the run and is named, with `git rm --cached` as the fix. Not being inside a work tree is a normal answer rather than an error — every guard fixture is a temp directory — so git's refusal comes back as a one-line REASON that the pass line prints: `(none tracked by git)` or `(not checked against git: …)`, because a reader cannot otherwise tell a verified exemption from an assumed one. The second half is the accumulator that made the first awkward: the wrapper was pushing skipped names into module-level state from inside a `filter` predicate, which is right exactly once per process, so `partitionLocalOnly` hands back both halves of the walk and the guard's output is a function of its input again.

## The convention with one home, and the skip nothing announced (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the fourth entry of "Local-copy follow-ups" (`isLocalOnlyPath` living in `lib/secret-scan.ts` and not being about secrets) together with the fifth (a guard that skips a file and says nothing about it). They are one change: the rule and the report of the rule, and splitting them would have moved the marker in one run and taught the wrapper to name it in the next. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 12000] **Give `*.local.*` its own module, and make the guard say what it passed over** — the run below made `.local.` a repository-wide rule and left it declared inside `lib/secret-scan.ts`, which is where it was first needed and not what it is about: a naming convention for files git will never take is wanted by the bundle scan, by a census, by the next guard somebody writes, and one rule with one home is exactly the move `NEVER_WALKED` made into `lib/source-dirs.ts` after having been written out twice. So `LOCAL_ONLY_MARKER` and `isLocalOnlyPath` move to `lib/local-only.ts` with the two git-backed assertions that make the exemption safe (that `.gitignore` still covers the pattern, and that nothing tracked matches it anyway), and `lib/secret-scan.ts` keeps a comment saying it HONOURS the convention rather than owning it — asserted, so a second definition cannot quietly reappear. The other half is the failure nobody could read: `filter` threw away the names it removed, so a reader who followed the instruction and misspelled the copy (`queries-local.m`, `queries.local`) got a red run naming their own pasted password with no hint that the NAME is why it was read. The wrapper now records what the rule took out and prints both halves — a clause on the pass line naming up to five skipped files and COUNTING the rest (a scan that quietly stops naming what it skipped is the silence this ends), and on failure the rule itself plus `Skipped none this run`, which is the sentence that tells the misnamer their file was never covered. Proved by spawning the guard at three scratch roots: a credential in `queries.local.m` passes and is named, the same credential in `queries-local.m` is reported with the rule under it, and a root holding both produces exactly one finding.

## The instruction that asks for a real password, pointed at a file git will not take (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Archive-scan follow-ups" (the README telling its reader to export a filled-in `.pbit` and saying nothing about not committing it) together with the third entry of "Extension-partition follow-ups", filed a day earlier, which asked the same of `queries.m`. They are one change: both documents instruct a paste, and neither could be pointed at an untracked file while the scan read the working tree. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 11000] **Say where the credential goes, and make the scan's subject the committed set** — the last two runs made the guard catch a filled-in Power BI template. What produces one is still the instruction: `docs/powerbi/queries.m` names four literals in its own header and tells its reader to replace them with their Supabase session pooler host and `service_role` database password, and the README's step 2 says the same about the parameter prompt an export writes back into the file. Catching that after the fact is second best; the file the reader edits should be one git will never take. That could not be said while the scan walked the working tree, because a gitignored `queries.local.m` with a real password in it turns the reader's own `npm run verify` permanently red — and a guard that is always red is a guard that gets deleted, which is the argument this repository already makes about floors. So the report's own words become true: it says "no committed secrets", and it now skips the `.local.` names `.gitignore` covers. `isLocalOnlyPath` tests the BASENAME, because the marker is about what a file is CALLED — a substring test over the path would take `docs/local/` out of the scan, and a directory nobody reads is worse than a file nobody reads. The exemption is checked in the two directions that make it safe rather than convenient, both by asking git: `git check-ignore` confirms the pattern really is ignored (if it stopped, this rule would be hiding committable files), and `git ls-files` confirms nothing tracked matches it, since `git add -f` beats `.gitignore` and a force-added `.local.` file is the one way this rule could hide a real leak. `queries.m` and the README now name `queries.local.m` and `*.local.pbit`, with a note on the export path saying what a Desktop-written template carries in its `DataModelSchema` part and why saving back over the committed one is the mistake. Verified end to end rather than reasoned about: the same AWS-shaped key in `docs/powerbi/queries.local.m` is scanned past, and in `docs/powerbi/queries.tracked-probe.m` is reported — one file, two names. 6 files, 3 new cases. `npm run verify` green: 5720 tests, 1298 suites, 13/13 guards, build exported. No DB migration, no creds.

## The archive branch that four regexes stood in for (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Archive-scan follow-ups", filed an hour earlier by the change directly below — the wrapper's whole archive path being pinned by regexes over its own source. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-19] [est: 9000] **Spawn the guard at a planted `.pbit` instead of reading its source** — the run below taught `check-secrets` to open archives and left the wiring asserted the way `SKIP_FILES` used to be, by matching four lines of the wrapper's own text. That is the weakest check in the guard, because the archive path has more ways to do nothing quietly than the text path does: the walk can stop matching the extension, the reader can hand back an entry nobody decoded, the report can name a path with no file at the end of it — and every one of those prints "no committed secrets" and exits 0, which is the sentence this guard exists to make trustworthy. `__tests__/secret-scan-archive-run.test.ts` runs it for real against a scratch root with a `docs/powerbi/planted.pbit` in it, DEFLATED, streamed and UTF-16LE, carrying a service_role JWT inside a `Sql.Database(...)` expression — the shape Power BI Desktop writes, which is the only shape that can carry a real one. Five things it asserts and the last two are the point: the guard exits 1 and names the rule; the report names the archive AND the entry (`…planted.pbit!DataModelSchema`, since the archive alone sends a reader to a seven-part zip with no idea which part matched); the half before the `!` joins back onto the root it scanned and opens, which is the property the whole `guard-report-paths` suite exists for and the one shape of report it had never seen; a TRUNCATED archive exits 1 saying it could not be opened rather than being skipped like a vanished text file, because one unopenable archive out of one is news; and a clean archive still says how many entries it read, which is the number that silently becomes zero when a walk stops finding them. The floor shapes the fixture rather than blocking it: 500 scanned files means the suite directory plus `lib` (584 between them), copied once per KIND of plant and shared by the three cases that use the same one. `makePartialRoot` takes a `Buffer` value now — an archive fixture written through `writeFileSync(…, "utf8")` is not an archive. Only one source regex survives, and its comment says why it has to: no fixture can hold 500 archives, so an archive dropped from the floor's count is invisible to a run. Mutation-checked three ways: never walking archives turns 5 cases red, skipping an unopenable one turns 1, dropping the entry name from the report turns 2. 4 files, 6 new cases. `npm run verify` green: 5717 tests, 1298 suites, 13/13 guards, build exported. No DB migration, no creds.

## The archive the extension list called unreadable (2026-08-19)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Extension-partition follow-ups", filed by the run directly below — `.pbit` being excused as "a zip, so its text is compressed" while sitting in the same directory as the M file that tells its reader to paste a connection string. Opening the file before writing the case turned the excuse into two wrong statements and a coverage hole. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. Pruned: the six sections completed on 2026-08-12, a week old today. -->

- [DONE: 2026-08-19] [est: 16000] **Open the one committed archive and scan what is inside it** — the suggestion asked for a case asserting the Power BI template does not embed a plaintext connection block. Reading the file first is what made it worth more than a case: the reason it was excused from the scan said "a zip, so its text is compressed and unreadable to a line scanner", and both halves were false. Every part of the committed `.pbit` is STORED, not compressed; what actually defeats the scanner is that they are UTF-16LE, so a NUL sits between every character and no rule can match across one — the excuse was right about the outcome and wrong about the cause, which is how it survived being read. And the file the scan should fear is not the committed one at all: `docs/powerbi/queries.m` instructs its reader, in its header, to replace four literals with their Supabase session pooler host and service_role database password, and the natural next step — Home → Manage Parameters, then export — writes a `.pbit` that carries the whole M expression as text and drops into the same path under the same name. That copy comes out of Power BI Desktop DEFLATED, which the repository's only zip reader (`lib/powerbi-template.ts`, store-only, written to read back its own deterministic output) refused with `unexpected compression method 8`. So `.pbit` moves out of `NOT_SCANNED_EXTENSION_REASONS` and into a new `ARCHIVE_SCAN_EXTENSIONS`: `check-secrets` opens each archive, decodes each entry by its byte-order mark, and runs the same eight rules, reporting `docs/powerbi/Collectables-Starter.pbit!DataModelSchema:12:34`. `lib/zip-archive.ts` is the reader both halves share now, and it is a rewrite rather than a move for two reasons a fixture proves rather than a comment claims: it inflates (method 8), and it reads sizes from the CENTRAL DIRECTORY, because a streamed entry's local header carries zeroes with the real values in a trailing data descriptor — the shape that makes a local-header reader return an empty entry, which in a secret scan is a pass. Everything it cannot open is a throw and the wrapper exits 1 on it, since one unopenable archive out of one is news where one text file out of 772 vanishing mid-walk is not; CRC is verified on the way out, which the round-trip case one module over already claimed in its name and did not do. The partition case gains the third bucket and the disjointness that goes with it — an extension may be text, or a container, or excused, and not two of those. Mutation-checked four ways, each turning exactly the expected cases red: refusing deflate again (3), dropping the UTF-16 decode (5), emptying `ARCHIVE_SCAN_EXTENSIONS` (1), and trusting the local header's sizes (8). 7 files, 18 new cases. `npm run verify` green: 5711 tests, 1297 suites, 13/13 guards, build exported. `check-secrets` now reports 772 files plus 7 archive entries. No DB migration, no creds.

## The file that documents every credential, and nothing read it (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Exemption-honesty follow-ups", filed by the run directly below — the two secret guards' extension sets being the last part of their policy with nothing checking it. Auditing the list against the tree turned a consolidation into four holes. The second entry of that section (a sweep over `secret-scan-ignore` markers) is dropped rather than left standing: no file in the tree carries the marker, so the sweep would assert over an empty set, which is the shape this repo refuses elsewhere. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 14000] **Read the four committed files a credential was likeliest to land in** — the suggestion asked for the two secret scans' extension sets to move beside the rules, since they were the last of that policy checked by nothing. Auditing the fourteen extensions against the tree before moving them is what mattered: four kinds of file were outside the scan, and the first two are the ones that sting. `.env.example` is the file whose entire subject is the SHAPE of every credential this app takes, which is exactly where a real one gets pasted while somebody debugs their env — `path.extname` answers `.example` and nothing read it. `docs/powerbi/queries.m` instructs its reader, in a comment, to replace four literals with their Supabase session pooler values — a database password, in a committed file — and `measures.dax` sits beside it; both are text and neither was read. `PrivacyInfo.xcprivacy` is config XML, and the five `PRIVACY.md.<lang>` translations each read as their own format because the extension of a doubly-suffixed name is the language code. All nine files are scanned now (761 → 770). Fourth hole, and the one with a cause worth removing: the extension test was case-SENSITIVE, so a stray `MANUAL-TASKS.MD` — an uppercase duplicate of the manual DB-steps document, every section of it already present verbatim in `MANUAL-TASKS.md`, referenced by nothing and unreadable on a case-insensitive checkout — sat at the root outside the scan for as long as it existed. `listFilesUnder` folds case before the membership test now, the suite-side walk's regex is `/\.tsx?$/i` to match, the stray file is deleted, and CLAUDE.md's one uppercase spelling of the filename (the thing that produced it) is corrected, since that instruction is what a future run reads. The check that keeps this closed is the partition the source directories already have: every extension in the tree is either in `SOURCE_SCAN_EXTENSIONS` or in `NOT_SCANNED_EXTENSION_REASONS` with a reason, and the three excused ones are binary (a font, a Power BI template zip, and the extensionless case `path.extname` answers `""` for). It needed one small thing from the walk — `extensions` is optional now, and omitting it returns every file, which is how the census asks the question. Mutation-checked: removing the case fold turns one case red, dropping `.example` turns two. 11 files, 3 new cases. `npm run verify` green: 5693 tests, 1295 suites, 13/13 guards, build exported. No DB migration, no creds.

## Four exemptions that had stopped exempting anything (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first and second entries of "Never-walked follow-ups", filed by the run directly below — the bundle scan's missing skip argument, and the file-level half of the secret scan's policy still being a wrapper constant checked by a regex. Probing the second one first turned it from a tidy-up into a coverage hole. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 9000] **Scan the three files a pasted credential is likeliest to land in** — the suggestion asked for `check-secrets`' `SKIP_FILES` to move beside `SECRET_SKIP_DIRS` so a suite could compare it by value instead of matching the guard's source with a regex. Measuring the list before moving it is what made this worth doing: four of its five entries were dead. The stated reason — "the scanner's own sources and tests embed the patterns and sample strings by definition" — is true of the fixtures and false of the code, because a rule carries a REGEXP and `/\bAKIA[0-9A-Z]{16}\b/` does not match itself. `lib/secret-scan.ts`, `scripts/check-secrets.ts` and `scripts/check-bundle-secrets.ts` produce zero matches each, so the exemption bought nothing and cost the coverage of the three files a sample credential is most likely to be pasted into while somebody debugs a rule; `package-lock.json` produced zero too, and reading it costs 23ms of a run that already reads 760 files. All four are scanned now (755 → 761 files) and the list is one entry — the matcher's own fixtures, where two sample secrets really do match. `SECRET_SKIP_FILE_REASONS` carries the reason per entry with the list derived from it, the same shape `NEVER_WALKED_REASONS` took an hour earlier, and the wrapper keeps only the `path.normalize` — a fact about the walk's output rather than about the policy. The case that would have caught this is the one the convention one directory over already states: an exemption must still be LOAD-BEARING, so every entry is scanned and must still produce a match, and the four retired names are asserted both ways — out of the list, and clean under the scanner, so dropping them cannot have turned the guard red for a reason nobody checked. The way to earn a new entry is a demonstrated match; the way to silence one line is the `secret-scan-ignore` marker, which says so where it happens. Second half: `check-bundle-secrets` passed no `skipDirs` at all, which is right and read as an omission beside a sibling naming six. `BUNDLE_SKIP_DIRS` is empty, exported beside `SECRET_SKIP_DIRS` and passed, with the doc saying why it may never be otherwise — a vendored tree under `dist/` is a tree that went to the browser, which is the one thing that scan must not skip. Mutation-checked: re-adding `lib/secret-scan.ts` to the exemptions turns two cases red, and dropping the bundle scan's skip argument turns two more. 5 files, 3 new cases. `npm run verify` green: 5690 tests, 1295 suites, 13/13 guards, build exported. No DB migration, no creds.

## One vocabulary for what a walk refuses to enter (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first, second, third and fifth entries of "Skip-list follow-ups", filed an hour earlier against the change directly below. The fourth (the suite-side cache key being order-sensitive while the guard-side walk has none) is left standing — it is the same caching decision that has been open for two runs and wants its own answer. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. Pruned: the eight tasks completed on 2026-08-11, a week old today. -->

- [DONE: 2026-08-18] [est: 11000] **Say what a walk never descends into once, with a reason per name** — sharing `NEVER_WALKED` between the two walks yesterday closed the drift and left three questions about the list itself, each of which is a way for a skip list to stop being reviewable. (1) `check-secrets` carried its own six-name `SKIP_DIRS`, three of which were `NEVER_WALKED` spelled again — an overlap invisible from either end, so a reader could not tell which entries are the shared "never holds source" rule and which are that scan's own. It is `[...NEVER_WALKED, ".git", "coverage", "web-build"]` now, which says both halves in one line, and it lives in `lib/secret-scan.ts` rather than the wrapper for a reason that improves the check: the wrapper runs `main()` at import and cannot be loaded from a suite, so the only test available was a regex over the guard's own source, and the case compares the VALUE now. (2) The list meant two things at once — only `node_modules` realistically appears under a source directory, so is this "never inside source" (one entry) or "never in any scan" (three)? Decided and written down: the second, because `assertSourceDirsCoverTheTree` walks the repository ROOT, and a list narrowed to `node_modules` would send it through the whole of `dist/` on every run and then report `dist` as an unaccounted-for directory of TypeScript. Each entry now carries its own reason in `NEVER_WALKED_REASONS`, with the list derived from it rather than restated beside it — the same one-authority argument the keyword `Set` in `lib/js-tokens.ts` paid for — so a fourth entry cannot arrive without saying why it is there. (3) The root walk had two mechanisms for one question and only one of them had a name: the shared list, and an inline `entry.name.startsWith(".")`, so `.expo` was excluded twice and `.github` by a rule nothing pointed at. `outsideSourceTree(name)` states both, and its doc says why they stay two rules rather than one merged list — the dot rule is about tooling directories AT THE ROOT and deliberately does not apply inside a source directory, where a dot-named directory is unusual enough that a scan should report what is in it. Fourth half, and the one that makes the two implementations worth keeping: the "the two walks agree" case ran once over `app`/`components`/`lib` with the default extensions, which is one configuration of ten, so a disagreement reached through `MARKUP_EXTENSIONS` or over `scripts/` had nothing looking at it. It is a loop over all five source directories in both extension sets now, with a non-empty assertion per directory and one for the markup union, because `data/` and `scripts/` hold no `.tsx` and two empty lists compare equal. Mutation-checked three ways, each turning exactly the expected cases red: making the guard walk ignore its `extensions` argument (2), adding `lib` to the secret scan's skip list (2), and writing the dot rule back out inline (1). 5 files, 3 new cases plus the parameterised agreement. `npm run verify` green: 5687 tests, 1294 suites, 13/13 guards, build exported. No DB migration, no creds.

## The skip list the agreement case was passing without (2026-08-18)

<!-- Found while reviewing the widest-walk pass rather than promoted from a suggestion: `guard-io-walk.test.ts` asserts the guard-side and suite-side walks answer the same over one tree, and the two carried separate lists of what never to descend into — so the case could only have held by luck. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 5000] **Share the never-walked list between the two walks** — the consolidation left one thing restated: `__tests__/helpers/source-files.ts` skipped `node_modules`, `dist` and `.expo`, and `scripts/guard-io.ts`'s `listSourceFiles` skipped nothing. Both are defensible in isolation and they cannot both be right, because the suite written an hour earlier asserts the two walks return the SAME list over `app`/`components`/`lib` — a case that was passing because no stray install happens to exist under a source directory today, not because the two agreed. `NEVER_WALKED` is in `lib/source-dirs.ts` beside the directory and extension lists now, taken by both. The consequence it guards is not theoretical: one `npm install` inside `components/` turns a 64-file scan into a several-thousand-file one, the floor sails past, and the guard reports findings in vendored code. Two cases came with it, and the second is the one worth having — the skip applies to a directory FOUND during the walk and not to a root a caller NAMES, so `listSourceFiles(root, ["node_modules"])` still walks it, because a caller that names it has said what it means. Writing the first case surfaced that distinction by getting it wrong: the original assertion asked for `node_modules` as a scan root and expected nothing back, which would have been a walk that ignores its own arguments. 4 files, 2 new cases. `npm run verify` green: 5684 tests, 1293 suites, 13/13 guards, build exported. No DB migration, no creds.

## The path in the report that nobody could open (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Widest-walk follow-ups", filed against the change made an hour earlier — the shared walk started returning paths relative to the root it was GIVEN, and nothing checked that the base a guard reports is the base a reader can join. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 12000] **Check that a guard's report names a file its reader can open** — the walk consolidation earlier today introduced a class of bug it could not have had before. While each guard did its own `path.relative(repoRoot, absolute)` there was one base and no way to pick the wrong one; `listFilesUnder` returns paths relative to the root it was GIVEN, which is `dist/` for the bundle scan and the repository for the rest, and the two look equally plausible in a report — `_expo/static/js/web/entry-….js` versus `dist/_expo/…`. Neither the type system nor a green run can tell them apart, because a guard finds nothing on a clean tree and a report nobody reads is a report nobody checks. So each of the four cheap guards is now run over a scratch tree with a PLANTED offender, and the path it prints is joined back onto the root it was pointed at and opened — present, readable, and not absolute, since a report that names `/home/runner/work/collectables/collectables/app/…` is unreadable without knowing where CI checked out. The offenders are the smallest thing each matcher rejects, written out rather than borrowed, because an offender that already exists would let the case pass on a walk that ignored the plant. Two details the suite had to get right or it would have passed for the wrong reason: the reported path is matched by the PLANT's own basename rather than "the first path-shaped thing in the output", since `check-inline-hex`'s report tells the reader to route the value through `lib/design-tokens.ts` — a real file, present in the fixture, resolving happily and saying nothing about the finding; and the three guards print the location three ways (bare, `:line`, `:line:column`), so the line and column are deliberately not part of the match, because what is under test is the BASE. Mutation-checked rather than assumed: repointing `check-inline-hex` at an absolute path turns exactly one case red and leaves the other seven green. `check-problem-phrasing-imports` is asserted at the source instead, with the reason stated — its floor of 450 spans five roots including `__tests__/`, so a fixture that gets far enough to print a report has to copy four hundred files per run to prove three lines the other four already prove — and the coverage case requires every `listSourceFiles` caller to be in one list or the other, so a new walking guard cannot arrive without one. `check-bundle-secrets` gets the same treatment for the same reason (the bundle premise refuses without a real `dist/`), and `check-secrets` gets it for a sharper one: its `SKIP_FILES` list is written in repo-relative paths, so a walk whose base moved would silently stop skipping the scanner's own sources and start reporting them. 1 file, 8 cases. `npm run verify` green: 5683 tests, 1293 suites, 13/13 guards, build exported. No DB migration, no creds.

## The two widest walks, and the fixture where the copies disagree (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Guard-walk follow-ups", filed an hour earlier — the two secret scanners left out of the consolidation because SOURCE_DIRS is not the list they narrow, which made the guards with the widest blast radius the two with no check on what they cover. The fourth entry (the "one call per process, so no cache" note) came along with it, since it is one sentence in the same doc. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 15000] **One walk for the two widest guards too** — the source-scan consolidation left `check-secrets` and `check-bundle-secrets` out, correctly at the time: `SOURCE_DIRS` is not the list they narrow, since one covers the whole repository across fourteen text extensions and the other covers `dist/` across five artifact ones. The consequence was that the two guards with the largest blast radius were the two with nothing checking what they cover, and one of them carried its own `try`/`catch` around `readdirSync` — the exact silent-pass hazard `listDirEntries` exists to hold in one place, written out again in the guard where "scanned nothing" means "no committed secrets". `listFilesUnder(root, { extensions, skipDirs })` in `scripts/guard-io.ts` is the walk now, with `listSourceFiles` built on top of it rather than beside it, so there is one recursion in the repository instead of seven. Paths come back relative to the ROOT they were given, not to the repository: `check-bundle-secrets` walks `dist/` and reports `_expo/static/js/web/entry-….js`, which is the name a reader of that guard wants. `skipDirs` matches a directory's own NAME rather than a path, so `node_modules` is skipped wherever it turns up, and `check-secrets`' file-level skip list stays OUTSIDE the walk — it names four sources and a lockfile, which is a rule about files, and a recursion with a second rule hidden in it is how a walk stops being reviewable. Verified as a migration rather than assumed: the old walk and the new one return byte-identical 756-entry lists over this repository, and the two guards still report 755 and 12 files. The new suite is the part worth keeping: the seven retired copies differed on four things nobody chose (the extension test, whether a non-directory counted as a file, whether the caller or the walk sorted, whether an unreadable directory returned nothing or threw), and every one of those differences agreed on every tree this repository has ever had — which is exactly why they were free to keep drifting. `__tests__/guard-io-walk.test.ts` builds a `mkdtemp` tree where they do NOT agree: a directory named `weird.ts` (returned as a source file by the two copies that took "not a directory"), a symlink, a file with no extension, a nested `node_modules`. Plus the case that says the guard-side and suite-side walks answer the same over `app`/`components`/`lib`, which is the one thing two implementations of one walk have to keep proving. And `check-secrets`' skip list is now asserted to hold only generated or vendored directories — a skip list that grows a SOURCE directory is how a committed credential stops being scanned, silently. 4 files, 12 cases in the new suite. `npm run verify` green: 5675 tests, 1292 suites, 13/13 guards, build exported. No DB migration, no creds.

## The keyword reached through a dot, one module further along (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Tokeniser follow-ups" — the stripper's prev/prevWord tracking being a second, smaller copy of what the i18n scanner does properly. It asked for one of two things (share the token loop, or document what the approximation gets wrong); probing first turned up a third answer, which is that one of the differences was a bug. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 13000] **Stop reading a keyword reached through a dot as a keyword** — the suggestion said the two scanners' previous-token tracking had "differences exactly in the class of input `js-tokens.ts` says the rule cannot resolve", and offered documenting it as the cheap option. Probing first was the right order: one of the differences is not an approximation, it is a bug. `lib/i18n-source.ts` clears the recorded word for a name reached through a `.` — `p.return` is a PROPERTY NAME that ends an expression, so `p.return / 2` divides — and has since its scanner shipped, with a paragraph explaining why. `lib/strip-comments.ts` shared the RULE with it and not the tracking, so it recorded `return`, opened a pattern at the `/`, and blanked to the end of the line; a comment below then survived into what four lint guards and eight suites read as code, which is the single failure stripping exists to prevent. Five spellings hit it (`a.return`, `q.delete`, `a?.return`, `a . return`, `o.typeof`) and a repo-wide sweep found zero live triggers, so it was a hazard standing rather than a failure running — stated that way rather than dressed up, and the sweep is the reason the claim can be made at all. The fix reads the member-dot flag at the START of an identifier run and holds it, because recomputing it per character answers `prev === "."` only for the first one, which is the same bug one character narrower. Whitespace deliberately does not break a run: it does not move `prev`, and `p . return` is still a member access. Numbers come out right for the same reason they did before — the `.` in `1.5` breaks the run, so the digits after it start one that looks like a member access and still divides, because every literal form ends in a character `DIVISION_FOLLOWS` covers. Second half, which is what stops this recurring: the rule itself was written out twice (`!DIVISION_FOLLOWS.test(prev) || followsRegExpKeyword(prevWord)`, in both modules that exist so it is written once) and is `opensRegExp(prev, prevWord)` in `lib/js-tokens.ts` now, with its doc stating the part the function cannot enforce — that `prevWord` must be EMPTY for a name after a `.`, because only the caller knows where a token began. A case asserts neither module spells the rule out any more. 5 files, 8 new cases pinning BOTH directions, since clearing the word too eagerly makes `return /}/` a division and blanks real code instead — which would be the guards going silently blind rather than loudly wrong. `npm run verify` green: 5663 tests, 1289 suites, 13/13 guards, build exported. No DB migration, no creds.

## The list five guards each remembered (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Source-walk follow-ups", filed an hour earlier — the guards under scripts/ still naming their own scan roots, and disagreeing. The second (the order-sensitive walk cache) and third (the exemption predicate that cannot see a string) are left standing as small decisions; the fourth and fifth are documentation. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 17000] **One list of source directories, and one walk for the guards** — the suite side got this earlier today; `scripts/` was the same duplication with a worse consequence, because a guard that quietly stops covering a directory reports green in the same words as one that covered everything. Five wrappers had each written the walk out, under three names (`listSourceFiles` ×3, `walkSources`, `collectTsxFiles`) with four differences nobody chose: three returned a sorted array and two appended to an out-param the caller sorted afterwards, three matched `/\.tsx?$/` and one `.endsWith(".tsx")`, and three checked `entry.isFile()` where two took anything that was not a directory — which counts a symlink or a fifo as source. And the scan lists were spelled three ways too (`SCANNED_DIRS`, `SCAN_ROOTS`, `SCAN_DIRS`), so a reader comparing two guards had to notice the rename before noticing the difference. `lib/source-dirs.ts` is the list now — pure, in `lib/` beside `scanned-floor.ts` and `guard-root.ts`, because the guards cannot import from `__tests__/` and the suite walk should not own a fact about the repository. It carries `SOURCE_DIRS`, `NON_APP_TS_DIRS` and the two extension sets, named for what they mean rather than left inline: a markup rule that also walked `.ts` would report findings in files that render nothing, and a code rule that walked only `.tsx` would miss every helper. `scripts/guard-io.ts` owns the walk, since that is the file that touches disk, and it returns repo-relative sorted paths because four of the five callers ran `path.relative(repoRoot, file)` on the very next line to report a finding and the fifth keyed a record by it. Built on `listDirEntries`, so a missing scan root stays a count of zero the floor refuses rather than an `ENOENT` stack. The narrowings stay — a rule about rendered markup has no business in `scripts/` — but they are recorded now: `__tests__/guard-scan-dirs.test.ts` pins each guard's list, requires every source directory it does NOT scan to carry a sentence saying why, and refuses a reason shorter than a reason. So `SOURCE_DIRS` gaining an entry turns five guards red instead of leaving five scans quietly narrower, which is the failure the suggestion was about. Mutation-checked rather than assumed: a hex appended to `app/index.tsx` is still reported at the right line and column, and the five guards scan 221 / 64 / 63 / 667 / 64 files. 12 files, 8 cases in the new suite. `npm run verify` green: 5655 tests, 1288 suites, 13/13 guards, build exported. No DB migration, no creds.

## The audit that outlived its suite, and the last per-line scan (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Tokeniser follow-ups" (the ECMA-262 audit stranded in the parser's suite) and the third of "Scanner-agreement follow-ups" (check-inline-hex still splitting by line after it started stripping). The second tokeniser entry — moving the scanner's whole token loop into js-tokens.ts so the stripper stops approximating — is left standing: it changes what the stripper reads, not where a case lives, and wants its own decision. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 14000] **Audit the token table where it lives, and scan the hex in one pass** — `lib/js-tokens.ts` shipped this morning carrying the regex-versus-division rule out of `lib/i18n-source.ts` so `stripComments` could share it. The rule moved; the ~250 lines auditing it did not. The exhaustive ECMA-262 `ReservedWord` partition, the nineteen-row table check, the per-row separator rule and the reserved-word pin all stayed in `__tests__/i18n-source.test.ts`, which left a module with four exports and no suite named after it — a reader opening `js-tokens.ts` found nothing, and a reader deleting the i18n parser would have taken the audit for BOTH callers with it. `__tests__/js-tokens.test.ts` is that audit now. The split is on what each case is a fact ABOUT: the table's own shape moved, and the two cases that drive `parseObjectLiteral` over every row stayed behind, because "this scanner obeys the table" is a fact about the scanner — a table nobody consults is documentation, and each caller should say so in its own suite. The move surfaced that `DIVISION_FOLLOWS` had no cases anywhere at all: every claim about it had been made through a parse, in the suite it was about to be separated from, and two callers now branch on it directly. It gets three of its own — one assertion per reason a character is IN the set (identifier, numeric literal, the trailing-dot form, the three closing brackets, the three quote styles), the other side spelled out because a character wrongly added there makes a scanner read a pattern as structure, and the single-character contract the callers rely on. `followsRegExpKeyword` gets the `Object.hasOwn` property directly rather than only through the parser fixture, including `__proto__`, which the old membership case did not name. Second half: `scripts/check-inline-hex.ts` was the last scanner in the repo splitting by line and building a fresh `RegExp` per line — a shape it kept from before it stripped comments, and the reason the two patterns' results had to be sorted per line and appended. It is one pass over the stripped text now with offsets converted by binary search, which is how the four `stripComments` guards already report, and the ordering is one comparison instead of a merge. `matchAll` on a per-call clone answers the module-scope `g` hazard the per-line reset existed for. 3 new cases pin what the arithmetic can get wrong: both ends of the search plus the boundary just past a newline, a match at offset 0 where the lookup has nothing before it, and the cross-line ordering the old walk got right by construction and a sort can get wrong. 5 files, 12 new cases (9 in the new `js-tokens.test.ts`, 3 in `check-inline-hex.test.ts`), 275 lines deleted against 110 added. `npm run verify` green: 5647 tests, 1286 suites, 13/13 guards, build exported. No DB migration, no creds.

## One walk over the source tree instead of eighteen (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Scanner-agreement follow-ups" (the double-walked sweeps and the four hand-rolled ones it named), the second (the four-directory literal), and the first (the three stripping shapes, said in one place). The third and fourth are left standing — one is a rewrite of check-inline-hex's per-line loop, the other is a measurement probe. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. Pruned: the eight tasks completed on or before 2026-08-10, a week old today. -->

- [DONE: 2026-08-18] [est: 21000] **One walk over the source tree instead of eighteen** — `suite-files.ts` did this for `__tests__` this morning and stopped at its own doorstep; the other half of the same habit was still standing, and it was bigger. Eighteen suites walked `app/`, `components/`, `lib/`, `data/` and `scripts/` recursively with the walk written out, and the copies had drifted in every way a copy can: nine returned absolute paths and nine repo-relative ones, nine matched `/\.tsx?$/` and nine `.endsWith(".tsx")`, two skipped `node_modules` and sixteen did not, one sorted and seventeen did not. Two of the differences are not cosmetic and are the reason this is a bug fix rather than a tidy-up: `analytics-call-site-parity` wrapped its `readdirSync` in a `try`/`catch` that returned early, and `link-aschild-style-flatten` guarded with `existsSync` and returned `[]` — so a renamed directory would have turned each of those sweeps into a silent pass reporting a clean tree, which is the one failure mode a guard must not have. `__tests__/helpers/source-files.ts` is the one walk now: repo-relative forward-slash paths, sorted, memoised per process like the suite walk beside it, with `sourceFiles(...dirs)`, the `tsxFiles` subset for the nine markup rules that wanted only `.tsx`, and three readers (`readSource`, `sourceCode` with offsets intact for the sweeps that print `file:line`, `sourceCodeFlat` for the ones matching across a rewrap). Fully recursive where the suite walk deliberately stops at one level, and the doc says why: `app/` is a router tree that nests as deep as the URL space, so a one-level walk there would report a clean `app/` while missing every screen. Second half: the widened strip-comments sweep from this morning named its four directories as a literal, which is the failure mode a hardcoded scan list has — a new top-level source directory joins the tree and the sweep silently stops covering it. `SOURCE_DIRS` is still a literal (deriving it from `tsconfig.json` does not work: its `include` is every `.ts` in the tree, which says nothing about which directories are app code) but it is now CHECKED against the tree: `assertSourceDirsCoverTheTree` walks the repository root and fails on a top-level directory of TypeScript in neither `SOURCE_DIRS` nor `NON_APP_TS_DIRS`, and on a `SOURCE_DIRS` entry that no longer holds any. The sweep picked up `data/` on the way, which nobody had noticed it was missing. Third half: the eleven `stripComments` callers each decided separately whether to also flatten whitespace, and nothing stated the three shapes together — `lib/strip-comments.ts` now names all three, which one a new scanner should reach for, and why getting it wrong (reporting offsets from flattened text) still passes on a clean tree. The new guard bans the CONJUNCTION rather than `readdirSync` alone: banning the call outright took thirty exemptions, which is a list and not a rule, while "walks a directory AND names a source directory" is the shape that was written eighteen times and leaves eleven exemptions that each walk something genuinely different (`dist/`, `supabase/migrations`, the `mkdtemp` fixture trees, `.ttf` assets). 21 files, 368 lines deleted against 138 added, 17 cases in the new `source-files-helper.test.ts`. `npm run verify` green: 5638 tests, 1283 suites, 13/13 guards, build exported. No DB migration, no creds.

## The last hundred and twenty, and the walks over them (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the second and fifth entries of "Inline-read follow-ups", plus the first and fourth as the small halves that came with them. The third (segments vs slash-joined) is settled by the same change and no longer stands. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 9000] **Make the last scanner agree with the rest about what "code" means, and widen the sweep past `__tests__`** — filed as the fifth suggestion of the run above and true: `check-inline-hex.ts` was the one guard not stripping comments, and its doc said why — the quote requirement on the SHORTHAND pattern keeps `#fff` in prose from being flagged. What the doc did not say is that the 6-DIGIT pattern has no quotes to require, so a comment reading "was #1a2b3c before the token migration" would have failed CI on prose. It strips now; the quote requirement stays, because an unquoted hex in real code is still not a style value, but it is no longer the only thing standing between a doc block and a red build. `stripComments` blanks rather than deletes, so the line and column the guard reports still point at the original source — pinned in a case, along with the failure in the other direction (a hex on a line that ALSO carries a comment must still be flagged, or the guard goes quiet rather than loud). Second half: this morning's regression sweep walked only `__tests__`, and half the twenty leaks it was written for were in `lib/` — a suite-only sweep would have documented the bug rather than found it. It walks `lib/`, `app/`, `components/` and `scripts/` now, and caught something immediately: `lib/privacy-page.ts` was on the quoted-source exception list and no longer belongs there, because its survivor was a real comment the regex fix resolved rather than string content. The "each exception still holds one" assertion is what surfaced it. 3 files, 5 new cases. `npm run verify` green: 5622 tests, 1278 suites, 13/13 guards, build exported.

- [DONE: 2026-08-18] [est: 16000] **Fix `stripComments` going blind on a regex literal — a live bug, not a tidy-up** — filed as a suggestion an hour earlier ("the stripper does not understand regex literals and four guards feed it source full of them"), probed, and it was real. A single unpaired backtick or quote inside a pattern — `` /`msg-${x}/ `` in `chat-context-uuid.test.ts`, `/["']key["']/` about thirty times across the analytics suites — put the stripper into string mode, and TEMPLATE mode does not end at a newline. Everything below survived into what a guard reads as code: twenty comment lines across four files (`chat-context-uuid.test.ts`, `i18n-source.test.ts`, `lib/privacy-page.ts`, `lib/spa-fallback.ts`), each downstream of one character. The consequence is a latent false positive in six sweeps and four lint guards — a doc comment quoting a shape a rule forbids would be reported as an offender, which is exactly what stripping exists to prevent, and the reason the "said once" guards can state their rule as "not at all". `lib/i18n-source.ts` had already paid for the regex-versus-division rule, including its exhaustive ECMA-262 `ReservedWord` audit and the nineteen-row keyword table; that rule is `lib/js-tokens.ts` now, imported by both, with no re-export left behind. The stripper gains a `regex` mode that tracks the previous token approximately (last non-space character, last identifier word) — what the shared rule takes — and handles a character class holding an unescaped `/`. Sweep-verified both directions: the twenty leaks are zero, and no division is misread as a pattern (which would blank real code and make a guard go silently blind rather than loudly wrong). The eleven `//` lines that still survive are string content — JavaScript inside template literals, the service-worker source and the parser's own fixtures — which the stripper is right to leave alone, and the regression case asserts that exception still applies rather than assuming it. 5 files, 3 new cases including the repo-wide sweep that found it. `npm run verify` green: 5619 tests, 1278 suites, 13/13 guards, build exported.

- [DONE: 2026-08-18] [est: 13000] **Give `stripComments` its own module, and the repo one implementation of it** — it lived in `lib/env-inlining.ts`, a module whose subject is Metro's `process.env.EXPO_PUBLIC_*` transform, and had grown ten importers with nothing to do with bundling: four lint guards (analytics imports, inline radius, empty-state wrappers, problem-phrasing imports) and six suites. A reader deleting env-inlining's last bundle caller would not have expected six guards to go red. `lib/strip-comments.ts` now, with `env-inlining` importing it like everybody else — no re-export, because a second door is the shape this repo keeps closing. The move surfaced an eleventh caller nobody had counted: `bundle-premise.test.ts` carried its own two-regex version that DELETED rather than blanked (so a reported offset would move) and would have eaten a string containing `/*`; it is the shared one now. And the function had no cases of its own — the env scan's tests exercised it end to end, which covers the one caller and says nothing about the ten that arrived since. Eight cases now, one per property a caller actually depends on: offsets and newlines survive (the four guards report `file:line` from stripped text and print the snippet from the original), a comment marker inside a string literal does NOT get stripped (a guard's list of forbidden shapes is an array of strings, several containing a slash), an escaped quote does not end the string, and an unterminated quote ends at the newline rather than blinding every rule below it. Plus `__resetSuiteFilesCacheForTests` — the two-line escape hatch for the premise yesterday's memoisation rests on, which is a premise and not a law. 15 files, 9 new cases. `npm run verify` green: 5616 tests, 1278 suites, 13/13 guards, build exported.

- [DONE: 2026-08-18] [est: 12000] **Hold an exemption list to four things, in one place, and answer the walk once per process** — every sweep in this family needs exemptions (the helper a rule is about has to name the shape; so does the guard that quotes it to search for it), and four guards had each written the same check around theirs in the same order and nearly the same words: pin the contents, confirm each entry is still in the walk, confirm each still NEEDS the exemption. The third part was written twice TODAY, independently. `assertExemptionsHonest` in `helpers/suite-files.ts` is that convention as one call, and it takes `expected` separately from `exemptions` on purpose — the list lives at module scope and the call sits in a case a hundred lines down, so widening the hole still means finding both; folding them into one literal would make the check agree with itself. It also catches two things none of the four copies did: an empty list (delete it rather than leave it) and a repeated entry (which hides how wide the hole is). Six failure modes exercised rather than assumed, because a checker that cannot fail is the same as no checker. `i18n-locales-helper.test.ts` gains the check it never had at all — its `EXEMPT` array had no assertion around it, which is exactly the hole the convention closes. Second half: the walk and the three readers are memoised, where `readRepoFile` deliberately is not — a hit there can only land within one process and the suites already hold a `const`, but a SWEEP reads every file and six of them now run across four guards, one of which strips comments from ~290 files character by character. Identity-checked in a case, so removing the cache is a red run rather than a silent re-derive. 6 files, 2 new cases. `npm run verify` green: 5607 tests, 1277 suites, 13/13 guards, build exported.

- [DONE: 2026-08-18] [est: 15000] **One walk over the suite directory instead of seven** — a guard that says "no suite does X" cannot be written against a list, because the list is the thing that goes stale, so seven suites here sweep their siblings and each had the readdir-one-level-deep walk written out. Four of the copies were byte-identical and three of those were added TODAY, by the two runs above that removed exactly this duplication one level down — four copies in one day is a rate, not a count. `__tests__/helpers/suite-files.ts` carries both shapes the sweeps genuinely want, and the difference is not cosmetic: `suiteFiles()` is every `.ts` one level deep, which a rule about how suites are WRITTEN needs so it reaches `helpers/` too, and `topLevelSuites()` is the `*.test.ts` set `npm test`'s glob actually RUNS, which a rule about runtime behaviour needs — a suite parked in a subdirectory typechecks and never executes. It also carries the three readers (`readSuite`, `suiteText`, `suiteCode`), because how source is normalised before a compare IS the rule rather than a detail: `suiteCode` strips comments first so a doc comment may still explain the shape being retired, `suiteText` only flattens so a prettier rewrap cannot hide one, and two guards had written both under two names. Every other namer of the directory took `SUITES_REL` — the bundler exclusion list, the stub directory, the fixture's path to `guard-fixture.ts`, and `test-typecheck-prelude.test.ts`, which keeps its own RECURSION on purpose because its subject is the files a one-level walk would miss. Exemption list is two entries, each asserted to still need it. 11 files, 8 cases in the new `suite-files-helper.test.ts` (including the one-level-and-no-further property and the written/run distinction pinned against `package.json`'s glob). `npm run verify` green: 5603 tests, 1277 suites, 13/13 guards, build exported.

- [DONE: 2026-08-18] [est: 18000] **Close the `__dirname` half of the same rule, which the sweep above had waved through** — found while filing the suggestions for the entry below: banning `process.cwd()` left exactly the near-miss the rule existed to end, because forty-three suites derived the same root as `path.join(__dirname, "..")` (or `"..", ".."` from `helpers/`) and the sweep did not look for it. `const ROOT = join(__dirname, "..")` was the single most common line in the suite directory after the imports. All forty-three go through `repoPath`/`readRepoFile`/`repoRelative` now, and the guard loops over BOTH derivations instead of naming one. The two agree because `tsx --test` starts every suite from the repository root — a premise that was implicit in forty-three files and is now pinned in one case, so if it ever stops holding the failure says so instead of arriving as forty-three ENOENTs. `__dirname` also stops meaning the root the moment a suite moves into a subdirectory, which the two `helpers/` files had already had to write around. Exemption list gains `lint-guard-empty-root.test.ts`, on it for a different reason than the other three: it matches `path.join(__dirname, "..")` inside the guard SCRIPTS under `scripts/`, where that shape is the sanctioned default root and a second occurrence is the bug it looks for — so the list is now documented per-entry rather than as one rule. 43 files, 1 new case (the cwd/`__dirname` premise), the root sweep now a loop over both. `npm run verify` green: 5595 tests, 1276 suites, 13/13 guards, build exported.

- [DONE: 2026-08-18] [est: 24000] **Route every remaining `process.cwd()` in the suites through `repoPath`/`repoRelative`, so the guard can ban the root outright** — the previous run took the READS through one helper and left the root itself spelled 120 times across 89 suites, for the things a reader cannot do: anchoring a `readdirSync` walk, turning an absolute hit back into a reportable name with `path.relative`, handing a `cwd` to a spawned guard. `repoPath(...segments)` and `repoRelative(absolute)` sit beside the reader now, and both they and `readRepoFile` take segments OR a slash-joined string — `path.join` normalises, so they are one path on both platforms Node supports, which settles the convention question the last migration left open. With the root gone the guard states the rule that has no next near-miss: a suite does not say `process.cwd()`, matched against source with COMMENTS STRIPPED so prose can still explain it. Its two predecessors each enumerated a shape and each had a miss waiting (`fs.readFileSync`, a `const cwd =` assigned first, the ~120 calls that never open a file at all). Three files stay exempt — the helper, its guard, and `i18n-source-file.test.ts`, which quotes the retired shape as a string — and each is now asserted to still NEED the exemption, so one that stops using the root is dropped rather than left standing open. Two smaller halves came with it: `supabase/functions/<name>/index.ts` was rendered by hand in three adoption suites, so it is `readEdgeFunction(name)` behind `__tests__/helpers/edge-function-source.ts` with a sweep of its own (5 cases, including the layout checked against every function directory in the tree rather than against one example); and both "said once" guards now carry a note that they check each other's suites, which is intended and reads as a confusing unrelated failure the first time. 91 files, 4 new cases in `repo-file-helper.test.ts` (the root sweep, the two-spellings equivalence, the walk round-trip, and the exemption-still-needed assertion) and 5 in the new suite; floor raised 120 → 250 readers with a comment saying it catches a broken walk rather than tracking the count. `npm run verify` green: 5594 tests, 1276 suites, 13/13 guards, build exported.

## The other hundred and fourteen (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the second and fifth entries of "Repo-file follow-ups", filed on the previous run. The first (renaming the call-site aliases) is a large diff with no behaviour in it and is left standing as its own decision; the third and fourth are the standing floor-maintenance question, unchanged. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 12000] **Route the inline literal reads through the helper too, and layer the i18n reader on it** — the previous run consolidated the duplicated `read(rel)` HELPERS and left the other half of the same habit standing, because the guard was written to ignore it: `readFileSync(path.join(process.cwd(), "app", "index.tsx"), "utf8")` written out at the top of a suite is not a duplicated helper, and it is the root spelled again — 114 times, across 72 files. All of them are `readRepoFile("app/index.tsx")` now, along with four stragglers the earlier sweep's shape list did not cover (two `supabase/functions/${name}/index.ts` readers, a `__tests__/${file}` one, and `helpers/sql-migration-asserts.ts`, which had two of its own). With the literals gone the guard can state the simple rule instead of enumerating shapes: a suite does not open a literal repo path of its own. Second half: `readI18nSource` reached for `node:fs` directly when it shipped this morning, which made two independent openers of one tree — the duplication one level up from the one it removed. It is `readRepoFile(I18N_SOURCE_REL)` now, with the location as a repo-relative constant and `I18N_SOURCE_PATH` derived from it, so the path is still said once and the two modules' subjects are separated: which file, versus how the suites reach the tree. 3 new cases in `__tests__/repo-file-helper.test.ts` (7 there): the inline-literal sweep, and the layering asserted two ways — the helper's source imports `./repo-file` and NOT `node:fs`, and the two readers return the same text. The layering case caught its own regression on the way: written with the filename inline it made `repo-file-helper.test.ts` the second file to spell the translations module's path, which turned this morning's "said once" guard red — the two guards checking each other, which is what they are for. It reads through the module's own exported constant instead. `npm run verify` green: typecheck + lint:all 13/13 + 5587 tests (+2 net) + build. No DB migration, no creds.

## One hundred and fifty-two copies of two lines (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Helper-naming follow-ups", which is the same argument that carried `readI18nSource` this morning, at fifty times the scale. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 19000] **Read a repo file through one helper instead of a hundred and fifty-two local copies** — most structural tests here read source TEXT rather than importing modules that pull React Native peers, and a hundred and fifty-two suites declared their own `readFileSync(path.join(process.cwd(), rel), "utf8")` to do it. Nobody wrote it wrong; they wrote it a hundred and fifty-two times, and the copies had already started to differ in ways nobody chose. `dev-globals-declaration.test.ts` took an ABSOLUTE path where every other copy took a repo-relative one — the same two lines, one `path.join` short, and nothing at a call site said which convention it was looking at. Two went through `fs.` rather than a named import. The root was spelled four ways (`process.cwd()` inline, `ROOT`, `REPO_ROOT`, `repoRoot`). New `__tests__/helpers/repo-file.ts` exports `REPO_ROOT` and `readRepoFile(rel)`; all 152 migrated, and the local names are kept by importing under them (`import { readRepoFile as read }`) so the change is a definition swap rather than churn across every call site. The odd one out was converted rather than exempted: its two path constants are repo-relative now and its inline `path.join(process.cwd(), "tsconfig.json")` argument went with them — the suite went red on the way through, which is the migration finding the inconsistency rather than preserving it. `process.cwd()` and not a path derived from `import.meta.url`, said in the doc because the two agree today and answer different questions: `tsx --test` starts every suite with the repo root as cwd, and cwd is what a repo-relative path in a test means — a suite run from elsewhere should fail loudly rather than resolve quietly against a source directory. Not memoised, for the reason `readI18nSource` is not: each suite is its own process. Pinned by `__tests__/repo-file-helper.test.ts`, 5 cases — the real read, the ENOENT (a suite asserting things about a file it could not open should stop, not report every assertion as a finding about the code), a directory sweep for the four retired shapes with whitespace flattened first so a reformatted copy is caught the same as a one-liner, an asserted two-entry exemption list (the helper, and the suite that has to quote the shapes to search for them), and a floor of >200 walked files / ≥120 readers, because a walk that finds no offenders is indistinguishable from a walk that is broken. The sweep deliberately matches the parameterised `(rel: string)` form only: plenty of suites still open one specific file inline for one assertion, which is a different thing from carrying a general reader. 154 files, 722 lines deleted against 172 added. `npm run verify` green: typecheck + lint:all 13/13 + 5585 tests (+5) + build. No DB migration, no creds.

## Saying which of the seven to reach for (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first, third, fourth and fifth entries of "Value-span follow-ups", filed earlier today. The second entry is left standing — it asks for the keys/values agreement to be enforced at the point of return rather than asserted, which is a change to the parser's shape and not a cleanup. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 11000] **Name the locale helpers for how wrong they can be, and pin the two invariants the value work leaned on** — four findings on code shipped hours earlier. (1) `i18n-locales.ts` grew to seven exports and the one easiest to reach for is the one to reach for LAST: `assertMatchesInEveryLocale` matched a whole map, so `key:[\s\S]*?SHAPE` through it is satisfied by SHAPE under any later key — the mistake the value spans exist to prevent, still available under the most general name in the module. Both body-matching helpers now end in `Body` (`assertMatchesInEveryLocaleBody`, `assertMatchesInEveryNonBaseLocaleBody`), which says at the CALL SITE what is being matched, and the module doc orders all seven by what to try first, ending with the two Body forms and why. (2) `assertValueInEveryLocale` printed the rejected value whole, which is the improvement over "not matched in de" and is also six multi-line templates inlined into one assertion message. Values are collapsed to one line and cut at 72 characters with an ellipsis, one locale per line. (3) `localeStrings` required a double-quoted literal, which holds because prettier normalises quotes — a fact about the config, not the language, and exactly the kind of dependency the move off the two-space indent was about. It takes either quote now. (4) The paren counter added with the value spans changed what `atTopLevel()` means for KEYS as well, and nothing said so: a parenthesised annotation's colon was already not read as a key because `expectKey` is false once a value has begun — an accident of where the arrow sits — and it is now true structurally. Pinned rather than left to the suite's silence, alongside a sweep asserting `keys` and `values` agree across the shapes that put a terminator inside a value (a trailing entry with no comma, a comma in an argument list, a comma in a regex, a nested object, a `}` inside a template, a spread) — a key with no span reaches a caller as "this locale does not declare that key", a translation finding rather than a parser bug. 4 new cases (2 in `__tests__/i18n-source.test.ts`, 57 there; 2 in `__tests__/i18n-locales-helper.test.ts`, 20 there), the message one asserting the ru line stays under 120 characters and carries an ellipsis rather than asserting the exact text. `npm run verify` green: typecheck + lint:all 13/13 + 5580 tests (+4) + build. No DB migration, no creds.

## The value the pattern was allowed to borrow from the next key (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Per-locale-assertion follow-ups", which named the remaining hole in the assertions migrated earlier today and said the fix was a value span the scanner could already reach. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 20000] **Teach the scanner to return each key's VALUE, and match value claims against it instead of against the map** — the per-locale assertions migrated this morning scoped their patterns to one locale's body, which closed the cross-map hole and left the same mistake one level down: `key:[\s\S]*?SHAPE` starts at its own key and strides through however many later entries it takes to find the shape, so `loadMoreItems routes count through a ?? 0` was satisfied by ANY later key in that map carrying a `?? 0`. `ParsedObjectLiteral` now carries `values` — a `ReadonlyMap` of each `name:` key to its exact source span, colon to the comma that ends the entry (or to the end of the body, which is the ordinary case for the last one, not the defensive one). Shorthand keys (`{ en, ru }`) are in `keys` and not in `values`: there is no value written at that level and an empty string would be a claim about one. A real scanner bug surfaced the moment spans were collected: `(` and `)` were not counted at all, so a comma inside an argument list read as a top-level separator — brace depth alone had been enough while only KEYS were collected (the identifier after such a comma is not followed by a `:`, so nothing was recorded and the mistake cost nothing), and with spans it truncated `(p) => String(p.name).replace(/}/g, "")` mid-call. Parens are counted separately from `depth` — the two closers are not interchangeable, and a `}` may not close the literal from inside an open call either, so `atTopLevel()` reads both — saved and restored across template `${}` frames alongside the brace depth, and floored at zero so a stray `)` cannot make every key below it invisible. Three new test helpers on top: `localeValuesOf` (one key's value per locale, throwing on a locale that does not declare it), `assertValueInEveryLocale` (the exact-span counterpart of `assertMatchesInEveryLocale`, whose failure names the locale AND prints the value it rejected), and `localeStrings` (the unquoted copy, refusing a value that is not a plain string literal so a key that became a formatter in one language is a named failure rather than a silently skipped comparison). Nine value-shape call sites migrated across eight suites; `localeValues` — the pattern-and-capture-group form added this morning — is deleted rather than left beside them, since every caller now asks by key. Pinned by 5 new cases in `__tests__/i18n-source.test.ts` (55 in that suite: the span itself with both ends anchored, the argument-list comma, shorthand keys having no value, a value's own braces/parens/regex staying inside it, and a sweep asserting every key of every real locale map has a non-empty span — a key without one would reach callers as "this locale does not declare that key", a translation finding rather than a parser bug) and 5 in `__tests__/i18n-locales-helper.test.ts` (18 there), built on a fixture where `greeting` is the string "Hello" and a later key carries the count shape: the body-wide rule is asserted to PASS on it before the exact rule is shown to fail, so the strengthening is demonstrated rather than claimed. Mutation-checked against the real file: dropping `de`'s `?? 0` reports `wrong in de ((params?: TranslationParams) => \`Mehr laden (noch ${params?.count})\`)`, where the body-wide form would have found a `?? 0` under another key and stayed green. `npm run verify` green: typecheck + lint:all 13/13 + 5576 tests (+10) + build. No DB migration, no creds.

## The slice that did not slice (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Per-locale-assertion follow-ups", filed earlier today. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 15000] **Retire the per-locale slice, which was checking a locale against the whole rest of the file** — eight cases in six suites asserted "ru / be / pl / de / es each override this key rather than falling back to en" with `const ${lang}: TranslationMap = \{[\s\S]*?KEY:[\s\S]*?\};`. It does not slice. `[\s\S]*?` crosses `};` like any other character, so the match ran from the named map's opening brace, through however many LATER maps it took to find the key, and out to the next `};` after that — the key had only to exist somewhere at or below the named locale. For `ru`, the first map in the file, that is the whole file: the assertion was very nearly vacuous, and which locale it could actually speak for was decided by declaration order. Replaced with `assertMatchesInEveryNonBaseLocale`, which tests each map's own body and skips the base (a map cannot fall back to itself). Three more whole-file counters surfaced on the way, and the way they surfaced is the finding: the sweep guard shipped this morning used `/\.(length|size), 6\b/` with a literal space, so `assert.equal(\n  matches.length,\n  6,\n)` — prettier's wrapping of the same expression — went unseen, and the guard reported three suites clean while they were still counting. `collection-detail-sort-gate`, `item-filters-search-ui` and `settings-events-taxonomy` are migrated and the guard now reads `\s*`. Pinned by 3 new cases in `__tests__/i18n-locales-helper.test.ts` (13 in that suite) built on a fixture where `ru` is declared BEFORE `es` and lacks the key `es` carries: the old slice pattern is asserted to MATCH that text — so the hole is demonstrated rather than described — while the new helper names `ru`; plus a case showing the base map is skipped (`en` written as a formatter, the others as strings, so the string pattern is false of `en` and true of every inheriting locale, which is exactly the difference between the two helpers), and the `/g` refusal the sibling helpers carry. Mutation-checked against the real file: deleting `ru`'s own `sortNameAsc` makes `collection-detail-sort-gate.test.ts` report "sortNameAsc is overridden — not overridden in ru", where the slice would have matched `be`'s copy two maps down and stayed green. `npm run verify` green: typecheck + lint:all 13/13 + 5566 tests (+3) + build. No DB migration, no creds.

## The parity check that was a sum (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "One-path follow-ups", filed on the previous run. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 18000] **Ask each locale map whether it declares the key, instead of counting six hits across the file** — twenty-two cases in eleven suites asked "is this key translated everywhere?" by matching `key:` against the whole of `lib/i18n-context.tsx`, counting, and asserting 6. It reads as a parity check and it is a SUM: any arrangement adding to six satisfies it, so a key declared twice in one locale and missing from another is green — which is the exact state the check existed to find — and when it does fail it names no language ("expected 6, got 5"). Three more faults rode along. Several variants matched a bare `key:` ANYWHERE, so a key named inside a template counted as a declaration. Several anchored on `^  key:`, i.e. on prettier's two-space indent, which is the formatting dependency `lib/i18n-source.ts` exists to end. And the six was a literal, so a seventh language would turn all twenty-two red at once with "expected 6, got 7" — reading as the new locale being wrong rather than as twenty-two numbers needing a bump. New `__tests__/helpers/i18n-locales.ts` answers the question through the parser: `locales()` (the picker's codes, so a seventh language is CHECKED rather than counted), `assertDeclaredInEveryLocale` (via `localeKeys`, and an inherited key counts as undeclared — which is the whole distinction, since a key served through `...en` renders, fits the layout and returns a non-empty `t()`), `assertMatchesInEveryLocale` for value-shape claims, and `localeValues` for the "six distinct strings" cases. Two hazards closed in the helpers themselves: both pattern-taking helpers REFUSE a `/g` regex, because a global pattern carries `lastIndex` between locales and would pass for one and fail for the next depending on walk order; and `localeValues` throws on a locale that does not match rather than returning a short map, because five values from six locales are trivially distinct. A real bug found while migrating `item-filters-search-ui.test.ts`: the label-vs-placeholder case zipped two separate whole-file sweeps by INDEX, so one locale missing either key shifted every later pair by one and compared a Polish label against a German placeholder — a pass or a failure decided by declaration order. It pairs by locale code now. Pinned by `__tests__/i18n-locales-helper.test.ts`, 10 cases, built around two fixtures that are GREEN under the old rule and RED under the new one — the sum-but-no-cover arrangement is asserted to satisfy the old count first, so the strengthening is shown rather than claimed — plus the inherited-key case, the mentioned-in-a-value case, the two `/g` refusals, the by-code pairing, and a directory sweep (with `bundle-smoke.test.ts` exempted and the reason written down: its six counts privacy pages, not locales) that keeps the habit retired. Mutation-checked against the real file: deleting `de`'s own `searchClose` makes `sheet-search-row.test.ts` fail with "'searchClose' is not declared in de", where the old count would have needed the total to move. `npm run verify` green: typecheck + lint:all 13/13 + 5563 tests (+10) + build. No DB migration, no creds.

## Fifty spellings of one path (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the third entry of "One-parser follow-ups" (`readI18nSource()`), which was still open and is the largest purely-mechanical item left in the file. Every .tasks.md task proper remains blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 16000] **Read `lib/i18n-context.tsx` through one helper instead of fifty copies of its path** — the suggestion said sixteen suites; the sweep found fifty, in two habits copied file to file: `readFileSync(path.join(process.cwd(), "lib", "i18n-context.tsx"), "utf8")` written out in fourteen (one of them reaching for an inline `require("node:path").join`), and `read("lib/i18n-context.tsx")` through a local `read(rel)` in thirty-six more. Fifty statements of one fact, all correct, none of them the place a rename would be made — and a rename is the case that costs: that file is where every locale map lives, so moving it turns fifty suites red at once with fifty identical ENOENTs and nothing saying they are one edit. New `__tests__/helpers/i18n-source-file.ts` exports `I18N_SOURCE_PATH` and `readI18nSource()`; all fifty suites migrated, along with the four local wrappers the consolidation made pointless (two `readI18nSrc()` one-liners inlined, two dead `read(rel)` helpers deleted with the `node:fs`/`node:path` imports that were left holding nothing). **Deliberately not "beside the parser", which is where the suggestion put it**: `lib/i18n-source.ts` takes source TEXT and `lib/i18n-coverage.ts` is written the same way and says so in its own doc — neither touches the filesystem, which is what lets a bundler treat them as ordinary modules. Putting `node:fs` into `lib/` to tidy fifty test lines trades a property of the shipped tree for neatness in the suites, so the path lives on the suites' side of that line and the helper's doc says why. The migration is not self-defending — nothing stopped the next suite from writing the join again, which is how fifty accumulated — so `__tests__/i18n-source-file.test.ts` sweeps the suite directory rather than a list of files: 5 cases covering the real read (`localeKeys(source, "en")` non-empty and the six picker codes, so a wrong path fails as a missing block rather than as a silently empty string), the resolved value, the sweep itself, an asserted two-entry exemption list (the helper, and the suite that pins the value — a third entry means someone widened the hole instead of importing), and a floor of >200 walked files / ≥40 readers, because a directory walk that finds no offenders is indistinguishable from one whose walk is broken. Two things the sweep deliberately does not do, both found by running it: it does not match the bare filename (`security-review-wiring.test.ts` lists it as one path among several in a fixture for a workflow's glob filter — a string about CI, not a file being opened), and it flattens whitespace before matching, so a prettier-wrapped join is caught the same as a one-liner. One bug fixed on the way, caught by the suite rather than by the typechecker: the mechanical pass rewrote the inner half of `require("node:fs").readFileSync(require("node:path").join(…), "utf8")` and left `require("node:fs").readI18nSource()`, which types as `any` and fails at run time — `item-filters-price-range.test.ts` went red on it, which is the one place in this change where a `require` call's untyped return was the difference between a red case and a silent one. `npm run verify` green: typecheck + lint:all 13/13 + 5553 tests (+5) + build. No DB migration, no creds.

## What stands between the word and the slash, and the number that was six tokens (2026-08-18)

<!-- Promoted from .tasks/.suggestions.md: the second, third, fourth and fifth entries of "Reserved-word-partition follow-ups". The first entry of that section is deferred with the standing `lib/js-scan.ts` move it depends on, "Keyword-table follow-ups" says outright its one entry is not worth doing for a single caller, and "Member-access follow-ups" is explicitly parked until a third lookbehind rule exists. Every .tasks.md task proper is still blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-18] [est: 13000] **Give every keyword row the separator it uses, read a number as one token, and say which productions the contextual list was drawn from** — four findings from the partition audit, and one of them turned out to be a live bug rather than an invariant. (1) The row-shape case had quietly stopped checking the thing that matters. It matched `word` + one space + regex literally until the three ASI rows (`break`, `continue`, `debugger`) arrived, then loosened to "any whitespace" for ALL nineteen rows at once — so a `return` row rewritten across a line break would still have passed, and whether a `/` can stand on the word's own line (i.e. whether there is a division for the scanner to get wrong) was no longer stated anywhere. Rows are `KeywordRegExpRow` now — `{ example, separator }` with `separator: "inline" | "newline"` — and each row is checked against the separator it DECLARES: inline rows must write the regex one space after the word, newline rows a line break after it. Three cases: the per-row match, an assertion that the newline set is exactly the three statement-enders (so a future row cannot reach for `"newline"` to dodge writing a same-line example), and a mutation case that reformats every inline example across a line break and requires the strict pattern to reject it. (2) `consumed()` was documented as one call per token and numbers rode the loop's per-CHARACTER tail — `1_000n` was six calls — which was filed as harmless because only the last character survives. It was not harmless: the tail resynced INSIDE the literal, so a numeric property name like `0x2f:` was consumed as the digit `0` and then `x2f` hit the identifier branch with `expectKey` still true, found the colon a lookahead away, and was pushed as a key. Not a key the scanner declined to read — a key that is in no file at all, invented from the middle of a number and handed to every caller counting keys. Every form carrying a letter or an underscore had it (radix prefixes, exponents, BigInt suffixes, separators); plain `1:` never did, which is why nothing caught it. Numbers now go through `NUMERIC_LITERAL` (anchored `y`, radix prefixes ordered FIRST so `0x1f` is not read as `0` + `x1f`) and `startsNumericLiteral`, which admits a leading `.` only when a digit follows so the member-access dot the identifier branch reads back is untouched. (3) `CONTEXTUAL_KEYWORDS` is eight words chosen by judgement and cannot be made exhaustive the way `RESERVED_WORDS` was — it is not a spec production — so the doc now names the productions that WERE consulted (`ForInOfStatement`, accessor `MethodDefinition` names, import/export clauses, `AsyncFunctionDefinitions`, `ClassElement` modifiers, `LexicalDeclaration`) and the ones deliberately skipped because no `/` can follow the word in them (the strict-mode `FutureReservedWord` modifiers, `accessor`), which makes an omission reviewable instead of invisible. (4) The file now has three load-bearing claims nothing executable can check — the `.` in `DIVISION_FOLLOWS` (its counter-example `p./re/` is a syntax error and cannot be written into a fixture that compiles), the ASI rows, and `skipRegExpLiteral`'s newline bound — and they are named together beside `DIVISION_FOLLOWS` so the next reader reads their untestability as "this is a claim about JavaScript" rather than as an excuse to delete them. Pinned by 8 new cases in `__tests__/i18n-source.test.ts` (50 in that suite): the three separator cases above, a sweep dividing after hex/octal/binary/BigInt/exponent/signed-exponent/separator/leading-dot literals in one fixture, a case proving the leading-dot gate does not disturb `p.return / 2` or `...p`, and the phantom-key case, which is red against the old scanner and green against the new one. `npm run verify` green: typecheck + lint:all 13/13 + 5548 tests (+8) + build. No DB migration, no creds.

## The word in neither list, and the number that ends in a dot (2026-08-17)

<!-- Promoted from .tasks/.suggestions.md: the second and third entries of "Keyword-table follow-ups" (the `Set` derived at import with nothing pinning the agreement, and the hand-enumerated exclusion lists) and the first entry of "Member-access follow-ups" (the decimal point the member-dot clause was said to be accidentally right about). The first entry of "Keyword-table follow-ups" says outright it is not worth doing for one caller, and the second entry of "Member-access follow-ups" is explicitly deferred until a third lookbehind rule exists. Every .tasks.md task proper is still blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions. -->

- [DONE: 2026-08-17] [est: 7000] **Make the keyword partition exhaustive against ECMA-262, drop the set derived from it, and divide after a trailing-dot number** — three findings, two of them from auditing the suggestion rather than from taking it at its word. (1) `REGEXP_KEYWORDS` was a `Set` rebuilt from the table's keys at import — two structures that could not drift the day they were written, which is exactly the pair a later hand-edit drifts: the membership cases read the RECORD and the scanner read the SET, so a hand-written set would have changed what the scanner did with every case green. There is one structure now (`Object.hasOwn` on the table), so there is nothing left to agree, and `hasOwn` rather than `in` is itself a fix — `constructor` and `toString` are `in` any object literal, so a value named `constructor` followed by a `/` would have opened a regex. (2) The two exclusion lists were hand-typed, which is the recall problem the table escaped one level up: a word in NEITHER the table nor an exclusion is invisible — no case names it, so nothing says which way a `/` after it goes. Both sides are derived from one authority now, ECMA-262 §12.7.2's `ReservedWord` production (38 words, pinned by count and by content so the partition cannot be made exhaustive by deleting the word that broke it) plus the eight contextual keywords that can stand before a `/` — `of` is a table row and is not reserved, which is why the spec's list alone is not enough. The partition is asserted exhaustive AND disjoint, and each of the 27 exclusions carries its reason ("takes a `(`", "ends an expression") rather than "is not a keyword", which is not a reason when half of them are keywords. Running the audit added three rows: `break`, `continue` and `debugger` end a statement, so ASI closes it at the line break and the `/re/` below opens a new one — a regex CAN follow them, no valid program divides there, so a row costs nothing and their absence was the invisible case the exhaustiveness check exists to catch. (3) The suggestion said the member-dot clause was accidentally right about `1.5 / 2` too. It never ran for it — a digit is not an identifier start, so `prev === "."` is unreachable from a number and `1.5` divided because it ends in a DIGIT. Which left the shape the coincidence was hiding: `2.` is a legal numeric literal, `.` was outside `DIVISION_FOLLOWS`, and `2. / 2` opened a regex that swallowed the `}` closing its `${}` slot, the template, the `,` and every key below — the keyword hole again, reached through a number instead of a word. A `.` cannot precede a `/` in any other valid program (`p./re/` is a syntax error, no statement begins with a dot), so the class gets it unconditionally. 5 new cases (the trailing-dot and decimal divisions, the inherited-property-name division, the exhaustive/disjoint partition, and the reserved-word list pinned), replacing the two hand-enumerated exclusion cases with one derived from the record. Mutation-checked three ways, each turning exactly one case red: removing the `.` from the class, spelling membership `in` instead of `Object.hasOwn`, and dropping the `break` row. `npm run verify` green: typecheck + lint:all 13/13 + 5543 tests (+3 net) + build. No DB migration, no creds.

## The list recalled instead of derived, and the keyword reached through a dot (2026-08-17)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Keyword-rule follow-ups" (the fourteen-entry list, one row exercised, unaudited against ECMA-262) and the second entry of "Token-recorder follow-ups" (the `consumed()` invariant, whose one remaining hole was a keyword arriving as a property name). Every .tasks.md task proper is still blocked — [needs-data] on a 30-day PostHog window, two on dependency decisions — so this run took the top actionable suggestions again. -->

- [DONE: 2026-08-17] [est: 6000] **Audit the regex/division keyword list against the rule it approximates, and divide after a property named like a keyword** — two findings about the same one-token lookbehind. (1) `REGEXP_FOLLOWS_KEYWORD` was a fourteen-entry `Set` written from memory, and one row (`return`) was ever exercised — the exact shape of list that is wrong in the row nobody checks. It is a `Record<word, example>` now, each row carrying the shape that puts a regex after its own word, and the audit is against the RULE rather than against the same memory: ECMA-262 chooses `InputElementRegExp` vs `InputElementDiv` by whether an expression may begin next, so membership is "can an expression start immediately after this word", not "is this a keyword". That moved the list from fourteen to sixteen — `default` (`export default /re/`) and `extends` (`class R extends /re/ {}`) were missing — and the cases now check the boundary in both directions: the words that END an expression (`this`, `super`, `true`, `null`) must stay OUT because `DIVISION_FOLLOWS` is right about them, and the words a punctuator always follows (`if`/`while`/`for` take `(`, `const` a name) must stay out because none can be the last word before a `/`. Every row is driven through the scanner (with the word, `/}/` is a pattern and both keys survive; the control word `returnValue` loses the literal), so the table is a rule the code obeys, not prose beside it. (2) The one hole left in the `consumed()` invariant was a keyword reached through a MEMBER ACCESS: `p.return` ends an expression, so `p.return / 2` divides, but the word rule recorded `return` and opened a regex — the stale-word failure arriving through `.` rather than through a branch that forgot to clear. A `prev === "."` check clears the word for a property name, and `p?.of / 2` rides the same `.`. 8 new cases (the member-dot division, the row-shows-a-regex and alphabetical-lowercase table checks, the two exclusion lists, every row through the scanner with a control, and the three parser-accepts/runtime-rejects rows pinned). Mutation-checked: dropping the member-dot clause turns 1 red, dropping the keyword clause turns 3, and a row whose example omits its own word is caught by the table check. `npm run verify` green: typecheck + lint:all 13/13 + 5540 tests (+7) + build. No DB migration, no creds.

## The pair that had to move together (2026-08-17)

<!-- Promoted from .tasks/.suggestions.md: the second, third and fourth entries of "Keyword-rule follow-ups", filed by the run directly below — two variables cleared by hand in eleven branches, the whitespace rule nothing pinned, and a scan result whose element field is always empty for half its callers. The first and fifth entries are a keyword-list audit and a three-table design decision, neither of which fits in this run's remainder. -->

- [DONE: 2026-08-17] [est: 5000] **Give the `/` rule's two variables one recorder, and stop the scan result offering a field it never fills** — `prev` and `prevWord` were assigned by hand in eleven branches, and the failure mode is a branch that records the character and forgets the word: the keyword survives one token too long and a division opens a regex. `consumed(token, word?)` is now the single call per token, so a new branch cannot record half of the pair. The two properties nothing had asserted are the ones prettier's output depends on: the word survives WHITESPACE (`return  /}/`) and survives a COMMENT (`return /* keep */ /}/`), both of which the character rule leaves alone and neither of which had a case — mutating either turns the new row red. The opposite direction is honestly out of reach and the case says so: a stale word misreads a division as a regex, and `skipRegExpLiteral` stops at the line end, so the scan resyncs on the next line with the key count unchanged. That the pair moves together is now a property of the code shape rather than of an assertion. `scanLiteral` also stopped promising element spans to readers that get none: it collects them only for an array scan, and `ParsedObjectLiteral & ScanEnd` (via `scanObjectBody`) no longer carries the field at all, so a future reader asking an object scan for its elements gets a compile error instead of an empty list. 1 new case, and 5533 tests green. Mutation-checked three ways: clearing the word on a comment, clearing it on whitespace, and dropping the keyword clause each turn a row red. `npm run verify` green: typecheck + lint:all 13/13 + 5533 tests (+1) + build. No DB migration, no creds.

## The keyword the one-character rule got wrong (2026-08-17)

<!-- Promoted from .tasks/.suggestions.md: the first, second and fourth entries of "One-parser follow-ups", filed by the run directly below — the two failure shapes chosen per call site with nothing saying why, the regex heuristic's known-wrong input, and the element spans collected for every scan and read by one. The third entry is corrected in place rather than implemented: each suite is its own process, so memoising the read saves nothing. -->

- [DONE: 2026-08-17] [est: 6000] **Fix the regex/division rule where a keyword breaks it, and say which readers throw** — the suggestion asked for a case pinning the known-wrong input with a doc line admitting it. It is five lines to fix instead. `DIVISION_FOLLOWS` reads the one character before a `/`, which is the standard disambiguation and is wrong after a KEYWORD: `return` ends in `n`, so `return /}/.test(s)` was read as a division and the `}` as structure — closing the function body early and dropping every key below it, which is the exact failure the regex branch was added to prevent, one keyword away. The scan now carries `prevWord` beside `prev` (the last identifier, cleared by any other token) and opens a regex when that word is one of fourteen keywords. `p.returnValue / 2` stays a division, which is the point of keeping both halves: what decides is the whole word before the slash, not its last letter. The module header now names the two failure shapes and the question each belongs to — the `find*` readers return null because "there is no such declaration" is an answer a caller acts on, `localeKeys` throws because its callers are asserting parity and an empty set would report a renamed map as 538 untranslated keys — so the sixteen suites that now use both pick by intent rather than by which import was copied. And the element spans `scanLiteral` collected on every call are collected only for an array scan now: `findArrayLiteral` is their one reader, and a locale-map scan was building a list of ~40 nested-value spans that nothing ever looked at. 1 new case (a `return`-opened regex and a `returnValue` division in one literal). Mutation-checked: dropping the keyword clause turns it red, and the earlier division case stays green either way, which is what makes the two halves independent. `npm run verify` green: typecheck + lint:all 13/13 + 5532 tests (+1) + build. No DB migration, no creds.

## Four readers of one file (2026-08-17)

<!-- Promoted from .tasks/.suggestions.md: the third, fourth and fifth entries of "Translation-coverage follow-ups" — the second statement exported under a utility's name, the scanner with no regex branch, and three suites locating the same blocks three ways. Counting the copies found sixteen, not three. The first two entries are a picker-policy decision and a delta-reporting choice, neither of which a run can make on its own. -->

- [DONE: 2026-08-17] [est: 14000] **Give `lib/i18n-context.tsx` one reader, and the scanner the branch it was missing** — the suggestion counted three suites parsing that file three ways; the grep found sixteen. Every structural claim about the translations is made against the SOURCE (the module pulls React Native peers, so a node test cannot import it), and each suite had brought its own habit: two copies of `= \[([\s\S]*?)\];` for `languageOptions` (`i18n-locale-map-parity`, `privacy-page-i18n`), a `([^}]+)` for the `translations` record, ten copies of `const en = {([\s\S]*?)\n} as const;` paired with a `\n};` variant for the other five locales, and three copies of a `languageBlock()` that sliced from one declaration to the NEXT one — so a map moved below another would have taken its keys with it, and every assertion would have stayed green. They agreed because prettier writes the file the way all sixteen expected. `lib/i18n-source.ts` is now the one reader: `findObjectLiteral`/`findArrayLiteral`/`findLocaleBlock` locate a top-level `const` by name (line-anchored, so a declaration quoted in a doc comment is not one, and `\b` so `enabled` is not `en`), and `localeKeys(source, language)` answers the question a dozen of those suites were really asking — does this locale DECLARE this key — which a substring test could not: `block.includes("deleteItem:")` is true of a template that merely mentions it. Two shapes the scanner could not read are now read: SHORTHAND properties, which is how `const translations = { en, ru, be, pl, de, es }` registers six languages without writing a colon (the `([^}]+)` regex it replaces read to the first `}` in the file), and REGEX LITERALS, the gap the suggestion named — no value holds one today, and the first `.replace(/}/g, "")` in a translation would have ended the literal early and dropped every key below it silently. The two shapes it still refuses (quoted and computed keys) are a compile error against `TranslationMap`, and their absence from the real file is a case now rather than a sentence in a doc comment. `declaredKeysByIndentation` → `declaredKeysByFormatting`, with a doc line saying it exists to DISAGREE and is not a parser: it was exported for one comparison under a name that reads like a utility, and the next reader looking for "the keys of this literal" would have picked the shorter function and got a reader that is wrong about multi-line templates. 17 new cases in `__tests__/i18n-source.test.ts` (the regex brace, a character class holding its own delimiter, division after an identifier and after a `)`, shorthand, the long-name and doc-comment near-misses, a picker label containing `];` — with the old regex's truncated output asserted beside it — a nested object's `code:` not read as a row, the two throws that stop a vacuous parity check, `localeKeys` against a key mentioned in a template, and the quoted/computed absence). Mutation-checked four ways: dropping the regex branch turns 2 red, treating every `/` as a regex opener turns 1, dropping shorthand turns 2, and un-anchoring the declaration turns 1. `npm run verify` green: typecheck + lint:all 13/13 + 5531 tests (+17) + build. No DB migration, no creds.

## The English string under a Polish flag (2026-08-17)

<!-- Promoted from .tasks/.suggestions.md: the fourth entry of "Danger-icon-button follow-ups" — `deleteItem` existed in three of six languages and nobody noticed for as long as it was unused. The three entries above it are design decisions (repaint a shipped row, pick a second adopter, round a screen's corners) rather than work a run can do on its own, so this was the top actionable one. -->

- [DONE: 2026-08-17] [est: 9000] **Count the keys each language does NOT translate, so the silent fallback becomes a number** — every locale map opens with `...en`, so an untranslated key is not a missing key: it is an English string served under a Polish flag. It renders, it fits, `t()` returns a non-empty value, and every assertion that a label appears on screen passes — which is why `deleteItem` sat in three of six languages until a new accessibility label reached for it, and why nothing in a 5476-case suite could have said so. `lib/i18n-coverage.ts` turns the fallback into a measurement: per language, how many keys it declares for itself, which base keys it inherits (by name, not by count), and the share that leaves. The numbers are en 538/538, ru 535 (99.4%), be 222 (41.3%), pl 213 (39.6%), de and es 209 (38.8%) — four of the six locales below half, which is the honest thing to publish beside "six languages supported". It is a PARSER rather than an import because `lib/i18n-context.tsx` pulls React Native peers (the two existing i18n suites read its source the same way), and it is a scanner rather than a regex because the values are strings and template-literal arrow functions: `{`, `}` and `:` all appear inside text a line-oriented reader takes for structure, and a ternary in a `${}` slot is a colon at what looks like the top level. The formatting-based reading survives as `declaredKeysByIndentation`, exported and compared against the scanner for all six maps — two statements of "what does this literal declare", derived from things that can disagree, so a scanner bug shows up as a disagreement rather than as a smaller number that still looks plausible. The floors go on DECLARED keys and deliberately not on inherited ones: a new English string ships before its five translations, so a ceiling on the fallback would go red on every feature PR and be deleted within a week, which is the failure mode `lib/scanned-floor.ts` names for a floor that trips on churn. What must never happen is the other direction — a locale losing translations to a bad merge — and that is what `TRANSLATION_FLOORS` catches, each a dated measurement with ~8% slack, exhaustive over the six so a seventh language cannot arrive in the picker without one (asserted against `languageOptions` in both directions). Fixed on the way, since the suggestion named it: `deleteItemTitle`/`deleteItemText` existed only in en and ru, so four locales showed a Russian-or-English delete confirmation over a translated button — be/pl/de/es now declare both, which is what moved their counts. 38 new cases in `__tests__/i18n-translation-coverage.test.ts` (the scanner against braces in strings, a ternary colon in a template slot, a function body's own keys, a URL that is not a comment, escaped quotes, the literal's closing brace with a second map behind it; the two derivations agreeing per language and a fixture proving they can disagree in both directions; every map spreading the base and the base spreading nothing; no duplicate and no unknown keys; ru's three inherited keys pinned by name; the delete-item family in all six; the floors and their dated notes; and the published report). Mutation-checked three ways: dropping the template-slot frame turns 1 case red, dropping string context turns 6, removing one locale's `deleteItemTitle` turns 1, and removing a ru key turns 2. `npm run verify` green: typecheck + lint:all 13/13 + 5514 tests (+38) + build. No DB migration, no creds.

## The share named `node_modules` (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the first and second entries of "Second-statement follow-ups", filed by the run directly below — the assumption with two spellings, and the platform premise pinned only for the platform the suite boots on. Writing the second found a hang. -->

- [DONE: 2026-08-16] [est: 8000] **Stop the nearest-link climb hanging on a UNC root, and pin the premise on the platforms it claims** — the suggestion was housekeeping: `path.win32` and `path.posix` need no filesystem, so a premise stated as "every platform node runs on" should be asserted against both rather than against whichever one the runner booted with. Asserting it is how it fell over. `path.win32.basename("\\\\server\\share\\")` is `"share"`, not `""` — a UNC root's basename is the share name — and a share named `node_modules` is legal. `nearestChainLink` climbed while `basename(dir) === "node_modules"` on the premise that a root would always stop it; for `\\\\server\\node_modules\\` the basename IS `node_modules` and `dirname` returns the root itself, so the climb had nowhere to go and went there forever. Not a wrong answer — a hang, in a helper five suites spawn through. It stops at the FIXED POINT of `dirname` now, which is what "there is nothing above this" means on every platform and is the same test the chain walk already stopped on, so the two still agree about such a root instead of one of them not returning. `nodeModulesChain` survived the same premise by accident that turned out to be design: its root link is appended unconditionally OUTSIDE the loop, which is what made non-emptiness structural a run ago, and is why a chain rooted at that share is still one link long. The shared test is `isNodeModulesDir` now, exported: both derivations spelled `path.basename(dir) === "node_modules"` for themselves, so the case comparing them looked like it checked the rule and could only ever check that the two copies had not drifted — with one home, the comparison is left checking the part worth checking. Both take an optional path namespace, defaulted, which is the seam that makes the win32 roots rows instead of prose. The termination case is a CHILD with a timeout, and that is the finding inside the finding: the loop is synchronous, so `node:test`'s own per-case timeout never gets a turn — the first draft carried `{ timeout: 5000 }`, and mutating the fix away took the entire run down rather than reddening one row, which is the decoration this fixture's loader spawn was built to avoid. The in-process comparison deliberately excludes that root and says so. 8 new cases (the predicate at three depths and against two near-miss names, the fixed-point premise across four roots in both namespaces, the basename premise pinned where it holds and where it fails, the hazard, the child, and the chain staying one link long for a root that is itself a `node_modules`). Mutation-checked: restoring the unbounded climb turns the child case red in ~1s instead of hanging the suite; dropping the climb entirely still turns 3 cases red. `npm run verify` green: typecheck + lint:all 13/13 + 5458 tests (+8) + build. No DB migration, no creds.

## The rule checked against itself (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the third and fifth entries of "Trailing-rest follow-ups", filed by the run directly below — `nearestChainLink` walks the whole chain for one entry, and three blocks compute the filesystem root for themselves. The second entry, the unpinned platform premise, ships with them because both fixes now rest on it. -->

- [DONE: 2026-08-16] [est: 5000] **Give the nearest-link rule a second statement, so the case comparing the two stops being a tautology** — `nearestChainLink` was `return nodeModulesChain(root)[0]`, and the case defending it asserted `nearestChainLink(root) === nodeModulesChain(root)[0]`: the same expression twice, run four ways, green by construction. The function was extracted BECAUSE three call sites spelled `chain[0]` with no statement of why that is the nearest link, and a definition that just re-spells it leaves the rule exactly as defended as the doc comment it replaced — while looking, in a passing suite, like it had been checked. It derives the answer now: climb out of a root that is itself a `node_modules` (node never searches inside one, so `/a/node_modules` is nearest to itself rather than to a `node_modules` one level deeper) and join `node_modules` onto what is left. Two derivations that CAN disagree, which is what makes the comparison worth running — a change to the chain walk that moved the nearest link fails here now instead of quietly redefining "nearest" for both readers at once — and it walks two directories instead of walking a scratch root to the filesystem root to discard all but one entry. The premise both functions rest on is the third piece: the chain is non-empty because the filesystem root's link is appended unconditionally, and this climb terminates because that root's basename is never `node_modules`. Same fact, asserted in prose in two places and nowhere in code, so it is a case — `basename` is `""`, is not `node_modules`, and `dirname` of it is itself. Both claims read as being about node's resolution and are really about `path`, so the platform is where they should fail if they ever do. Finally the fixture that needs no plant: three blocks were computing `path.parse(path.resolve("/")).root` for themselves, with the link derived in one and re-derived inline in the others; `FS_ROOT` and `FS_ROOT_LINK` are file-level constants beside `REPO_ROOT` now, so the fourth block cannot invent a fourth spelling. 3 new cases (the platform premise, the climb at two depths, and a relative root resolved before it is answered about), plus the comparison widened to five roots. Mutation-checked: dropping the climb turns 3 cases red, which is the tautology's whole point — before this it turned none. `npm run verify` green: typecheck + lint:all 13/13 + 5450 tests (+3) + build. No DB migration, no creds.

## The guarantee re-established after the fact (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the first, third and fifth entries of "Chain-invariant follow-ups", filed by the run directly below — the non-empty tuple enforced by an unreachable throw, the probe reused on a string match, and the list rebuilt one entry at a time to persuade the compiler. All three are the same shape and the first two fixes remove the third. -->

- [DONE: 2026-08-16] [est: 5000] **Make the chain non-empty by construction rather than by a check nothing can reach** — the run below deleted an unreachable branch and then added one: `nodeModulesChain` pushed the filesystem root's link inside its loop like any other, so the non-emptiness the type promised had to be re-established afterwards, by destructuring the result and refusing an empty list with a sentence no reader will ever see. That is the exact shape this file has now deleted three times for rotting, and the reasoning that produced it was sound — the alternative on offer was a cast, which would let a future edit to the walk hand every caller an undefined nearest link with the types still green. The third option is to build the list so the compiler can see the guarantee: the loop now stops one directory SHORT of the filesystem root and the root's own link is appended outside it, where the argument for it can be read, and `ChainLinks` becomes `readonly [...string[], string]` — some links, then the one the walk always ends with. That is the shape of the argument rather than a shape chosen for convenience: what makes a chain non-empty is not that its first entry exists but that its LAST one is guaranteed, and writing it the other way round is what forced the check. Readers lose nothing — a trailing-rest tuple still types `chain[0]` as a string, destructuring and iteration are unchanged — and the rebuild that existed only to persuade the compiler goes with the branch. The other half is the walk's probe reuse, which matched the nearest link by STRING (`dir === nearestPath`): correct only while no link appears in the chain twice, which is true because every entry is `join(dir, "node_modules")` for a strictly ascending `dir`, and was stated nowhere — precisely the property a normalisation or de-dup pass would be added to guarantee and would then quietly invalidate. It is `index === 0` now, which is what was meant and cannot come apart, and the no-duplicates property it used to rely on is a case in its own right. 1 new case (four roots, no chain repeating a link), and the deleted branch's coverage is replaced by the compiler: dropping the appended root link is a type error at the return, which is stronger than the test it had. Mutation-checked three ways: returning the loop's list alone fails to compile, reusing the probe at index 1 turns 3 cases red, and reversing the chain still turns 14. `npm run verify` green: typecheck + lint:all 13/13 + 5447 tests (+1) + build. No DB migration, no creds.

## The empty chain no root can have (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the first and fourth entries of "Probed-link follow-ups" — `ChainProvenance.nearest` is filled on the first iteration of a loop and nothing says it is the NEAREST, and `probeNearestLink`'s only own branch is asserted through `emptyLinkFrom(null)` instead. The fifth entry, the narrowly-typed `readdir` seam, rides along. -->

- [DONE: 2026-08-16] [est: 7000] **Prove the chain is never empty, so the nearest link stops being a maybe** — the suggestion asked for a case pinning that `probeNearestLink` returns null for a root whose chain is empty, naming the filesystem root as the fixture that needs no plant. Writing it is how the finding turned around: `/` HAS a chain, and it is `["/node_modules"]`. `nodeModulesChain` climbs until `dirname` stops moving, and that last directory is the filesystem root, whose `basename` is `""` on every platform node runs on — never `node_modules` — so its own link is pushed by every chain, including the chain of a root that IS a `node_modules`. The empty-chain branch three functions carried was for a state no root can be in, and its doc comment named as its example the one root that disproves it. So the branch is gone rather than covered: `nodeModulesChain` returns a non-empty tuple (`readonly [string, ...string[]]`) with the argument written where the readers are, `probeNearestLink` returns a `ProbedLink` instead of a maybe, `ChainProvenance.nearest` is non-nullable, and `emptyLinkFrom` takes a `ProbedLink` — "there is a link and it answered nothing" survives as the `real === null` line it always was. The other half is the ordering the same three readers depended on: `chain[0]` spelled three times, correct only because a sentence in a doc comment said the chain comes back nearest-first, so a sort or a de-duplication there would have made all three wrong at once with every assertion still green — the fixtures that reach them have one-link chains. The type says it now, `nearestChainLink(root)` is the one name for it, and the walk's `nearest ??= …` — right only because the loop happened to run nearest-first — became an explicit first-entry probe reused by the loop, so the link that DECIDES is the link the message reports at no extra syscall. Two duplications went with it: the global-folder refusal built a second chain to name `chain[0]` beside a clause reporting on the same link (it reads `provenance.nearest.path` now, so the two halves of one sentence cannot disagree), and the `readdir` seam, typed `(path: string) => string[]`, moved to the `fs.PathLike` overload it actually calls. 11 new cases: the chain's ordering as the property the readers use (each link's directory strictly contains the next one's), non-emptiness across five roots including `/` and a root inside `node_modules`, the filesystem root's one-link chain as the row the deleted branch's doc comment claimed the opposite of, the `node_modules` skip, relative-root resolution, `nearestChainLink` against the chain for four shapes of root, and `probeNearestLink` planted three ways — a real directory, an absent link, and the empty one whose finding the absent-install refusal is built on. Mutation-checked both ways: reversing the chain turns 14 cases red, and taking the walk's nearest from the FAR end turns the global-folder refusal and both empty-link message cases red. `npm run verify` green: typecheck + lint:all 13/13 + 5446 tests (+11) + build. No DB migration, no creds.

## The link probed twice, and the refusal that could describe neither probe (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the first entry of "Empty-link follow-ups" — `nearestEmptyLink` re-probes a chain link the provenance walk has already probed, one syscall later and with no memory of the answer. -->

- [DONE: 2026-08-16] [est: 6000] **Carry the nearest link's probe out of the walk, so the refusal reports the walk it was decided by** — `resolvedFromCheckout` probes the nearest chain link to decide whether the checkout answered, and the clause builder probed the same path again while assembling the message. Two observations, one syscall apart, with no memory between them: they can disagree, and the shape that makes them disagree is not exotic — a slow `npm ci` running in another terminal lands an install between the walk and the sentence. The refusal then describes a state that had already stopped being true when the decision was made, which is the one thing a diagnosis cannot afford, because a reader who checks the claim and finds it false stops believing the paragraph that names their real problem. `ChainProvenance` now carries a `ProbedLink` for the nearest link exactly as the walk saw it, and `nearestEmptyLink(root)` splits into `emptyLinkFrom(nearest, readdir)` — pure but for one listing, taking the walk's answer — and `probeNearestLink(root)`, kept for the ONE refusal that has no walk behind it: when `require.resolve` itself throws there is no provenance to carry anything out of, so that message still has to ask. The `readdir` seam covers the branch that had none: a listing that FAILS is not this probe's finding, because the link is then unreadable and that has its own path, its own sentence and its own list, and answering here too would send the reader to fix one directory for two different reasons. 4 new cases (no chain at all, a link the walk could not resolve, the realpath read while the reader's own path is reported — the split that matters for a symlinked install — and the failed listing declining to speak). Mutation-checked both ways: reading the asked-about path instead of the resolved one turns the symlink row red, and reporting the failed listing as an emptiness finding turns the declining row red. `npm run verify` green: typecheck + lint:all 13/13 + 5435 tests (+4) + build. No DB migration, no creds.

## The table row nothing prints, and the one-row table that cost two child processes (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the first and fifth entries of "Obstruction-table follow-ups" — nothing ties a kind to a probe that can emit it, and `obstructionClause` is pure, unexported, and reachable only through a spawned refusal. The first entry of "Phrase-rulebook follow-ups" filed hours earlier is the same finding generalised to all four tables, so it ships here. -->

- [DONE: 2026-08-16] [est: 8000] **Require every phrasing row to be RENDERED by something, and make the three clause builders pure and exported** — the rulebook pinned how a row reads and said nothing about whether anything prints it, which is the gap all four tables carried: a kind added by someone sketching ahead (`"socket"`, `"no-execute-bit"`, a third host) satisfies every assertion, ships as dead prose, and reads to the next person as a finding the walk knows how to make. `assertPhraseTable` now takes an optional `renderedIn` corpus and requires each row to appear in one of its strings — a row no renderer can produce is a promise the code does not keep. What made that affordable is the other half: the clause builders were pure functions reachable only THROUGH a refusal, so every row of them cost a planted directory and a spawned child, which is why "no findings in, no clause out" — a one-row table — was being proved with two `NODE_PATH` plants and a symlink cycle. `obstructionClause` and `unreadableList` are exported as they were; `emptyLinkClause` took the bigger change, from `(root, host)` to `(EmptyLink | null, host)`, moving the `nearestEmptyLink` probe out to the two call sites so the renderer is pure and the filesystem stays with the half that needs one. The unreadable-link rendering became `unreadableList` in the process, which was inline at the throw site and was the one of the three that had never been a function. The spawns stay exactly where their claim needs them — that a clause reaches a message a REAL walk produced — and nowhere else. 6 new cases (the empty clause for each of the three builders, the singular/plural agreement across four slots in one sentence with both paths listed, the path named exactly once per host, and the separator that has to stay a separator), plus `renderedIn` wired into all four tables. Mutation-checked both ways: rendering one obstruction kind for both turns the table's coverage rule AND the loader check red, and hard-coding the singular agreement turns the builder rows red. `npm run verify` green: typecheck + lint:all 13/13 + 5431 tests (+6) + build. No DB migration, no creds.

## The errno blamed on the call that succeeded (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the second, third and fourth entries of "Obstruction-table follow-ups" — the structurally-typed `lstat` seam, the `statSync` failure caught by the `realpath` handler, and the `UnreadableLink.detail` that renders identically for two different directories. All three are the same report pointing at the wrong place, so they ship together. -->

- [DONE: 2026-08-16] [est: 8000] **Name the syscall on an unreadable link, split the `stat` out of the `realpath` catch, and make the injected `lstat` a real one** — the unreadable-link report prints `<path> (<errno>)` and the path it prints is the chain link, whichever call produced the errno, which made it wrong in two cases out of three. An `EACCES` from `realpath` means the link cannot be traversed and the reader should look at the link; the SAME code from `lstat` means its PARENT could not be read, so the link itself may be perfectly fine and the `chmod` they were sent to run changes nothing. `UnreadableLink` now carries a `LinkSyscall`, the wording of what each one implicates lives in an exported `SYSCALL_IMPLICATION` under the phrase rulebook shipped an hour earlier, and the sentence reads `<path> (<detail> on <syscall> — <implication>)`. The second half is the misattribution that made the third row possible: `realpathSync` and `statSync` sat in one `try`, so a `stat` that failed landed in the handler written to classify the FIRST call — a target removed between the two, or a filesystem that answers `realpath` and then refuses `stat`, produced an errno attributed to a syscall that had already succeeded. The window is a race and the message is its only artifact, so `probeResolvedLink(real, stat = fs.statSync)` is split out with its own catch and its own syscall tag; a `stat` reporting plain ABSENCE gets no finding at all, because the path is genuinely gone as of the second observation and that is what every ancestor of every checkout looks like. Third, the injected `lstat` was typed `{ isSymbolicLink(): boolean }` — satisfied by the real syscall AND by a two-line stub, which is the seam working, but also by nothing else the probe might grow: an `.isFile()` or `.mode` check added later compiles against `fs` and throws `is not a function` on every injected row at once. It asks for `fs.Stats` now, and the rows supply real ones taken from a planted symlink and a planted file, which is a more honest fixture than an object that answers one question. 6 new cases (the stat race in three errnos, the vanished target in two, the default stat against a real directory and a real file, plus the table's three rows and the assertion that its rows send the reader to three different places). Mutation-checked both ways: dropping the syscall from the rendered message turns 2 cases red, and re-attributing the `stat` failure to `realpath` turns the race rows red. `npm run verify` green: typecheck + lint:all 13/13 + 5425 tests (+6) + build. No DB migration, no creds.

## The rule with one home, the tables with one rulebook, and the tail that was only true once (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the second, third and fifth entries of "Package-counting follow-ups", filed by the run directly below — the undotted-name rule enforced inline, the two phrasing tables disagreeing about periods for a reason recorded nowhere, and the clause tail carried into a refusal it is false about. -->

- [DONE: 2026-08-16] [est: 9000] **Name the package-entry rule, put the phrase tables under one rulebook, and split the clause tail per refusal** — three findings about the same clause, all of the form "a decision was made and not written down". (1) The rule the emptiness probe turns on — a `node_modules` entry is loadable iff it is UNDOTTED — was a doc comment over an inline `!entry.startsWith(".")` at one call site: a claim about how node resolves, stated in prose and enforced in an expression, with no way to read the two against each other. It ships as an exported `looksLikePackageEntry(entry)`, whose interesting rows are now a table rather than a directory plant and a spawned child per shape, which is why only two of them were ever covered. 9 rows: the plain name, the hyphenated one, the `@scope` the rule has to survive, npm's four bookkeeping shapes including pnpm's `.modules.yaml`, and the two that pin WHERE the line is — a dotted name nobody has seen yet is excluded because it is dotted rather than because it was enumerated, and an undotted `bin` is a package rather than npm's shim directory. (2) `OBSTRUCTION_PHRASE` forbade a period anywhere and `EMPTY_LINK_PHRASE` forbade one only at the end, which is a real difference for a real reason — the second legitimately names `.package-lock.json`, because the reader recognises their own listing in it — recorded nowhere, so the third table inherits a rule by coin flip. And the containment check, which stops an `includes` pin on a shorter row being satisfied by a longer one, existed on the second table only, for no reason beyond the order they were written in. Both now go through one `assertPhraseTable(table, rules)` in a new `__tests__/helpers/phrase-table.ts`: the punctuation rule is a parameter named after its reason (`"never"` / `"not-at-the-end"`), the template each row is dropped into is quoted back in every failure message, and distinctness and containment come free — so a table that wants to differ has to say which rule and roughly why. (3) The clause's tail — "this refusal reads exactly like one for a checkout with no `node_modules` at all" — was written for the absent-install message and carried into the global-folder one by the same interpolation, where the reader can disprove it in the paragraph directly above: that message opens by naming the path `$NODE_PATH` answered with, and reads nothing like the absent case. A message that makes a checkable claim and gets it wrong costs the rest of the message its credit, which is more than the sentence was buying. `emptyLinkClause(root, host)` now takes a closed `EmptyLinkHost`, and the two readings live in an exported `EMPTY_LINK_TAIL` under the same table rules as the others — the global-folder row says the global answer won by default rather than on merit, which is what the finding actually means there. 12 new cases (the 9 predicate rows, both refusals asserted to carry their own tail AND to stay clear of the other's, and the tail table's own two rows). Mutation-checked three ways: restoring the shared tail turns 2 cases red, narrowing the predicate to plain names turns the scoped fixture and the table red, and a tail row that punctuates itself is caught by the shared rulebook. `npm run verify` green: typecheck + lint:all 13/13 + 5419 tests (+9) + build. No DB migration, no creds.

## The install directory that is not empty and still holds nothing (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the third and fifth entries of "Empty-link follow-ups", filed by the run directly below — `readdirSync().length` misses the bookkeeping-only directory, and the clause's wording was prose the cases retyped. -->

- [DONE: 2026-08-16] [est: 7000] **Count packages rather than entries, and put the empty-link wording in the table the cases read** — the finding shipped an hour earlier asked `readdirSync(...).length === 0`, and that is not the shape an interrupted install usually leaves: npm writes `.package-lock.json` BEFORE it restores the store, and a prune leaves `.bin` and `.cache` behind, so the common ending is a `node_modules` that is not empty by any count and still holds nothing node can load. That reader was sent straight back to the refusal this finding exists to replace, with the extra confusion of a message that had explicitly declined to call their directory empty. The probe now reports two kinds — `empty` and `no-packages` — split on whether any entry is UNDOTTED, which is node's own line rather than a guess: a package directory is the specifier, and every specifier node resolves from here is a plain name or a `@scope`, never a leading dot. The scoped case is the one the rule has to survive and it gets its own negative, because "no plain names" and "no undotted names" agree on every fixture except a workspace whose only installed dependencies are scoped — which would be told it holds no packages at all. The second half is the treatment `OBSTRUCTION_PHRASE` got one commit earlier, applied to the clause that was written since: the wording moved into an exported `EMPTY_LINK_PHRASE`, and all four existing cases now read the phrase from there instead of retyping `/is empty/` — which was the failure that extraction exists to stop, since the two NEGATIVE cases assert an absence and a reworded clause would have made them vacuous rather than red. Its table rows are pinned the same way (verb phrase, no self-punctuation, distinct), plus one the obstruction table did not need: no phrase may CONTAIN another, or an `includes` pin on the shorter is satisfied by the longer. 5 new cases (bookkeeping-only in two shapes, the scoped package, the three table rows). Mutation-checked both ways: dropping the undotted test turns the bookkeeping case red, and narrowing it to plain names turns the scoped one red. `npm run verify` green: typecheck + lint:all 13/13 + 5410 tests (+5) + build. No DB migration, no creds.

## The `node_modules` that is there and holds nothing (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the second entry of "Obstruction-reporting follow-ups" — the walk asks only whether `tsx` is under each chain link, so an empty one produces no finding at all. -->

- [DONE: 2026-08-16] [est: 6000] **Name the empty `node_modules`, instead of printing the refusal for a checkout that has none** — an interrupted `npm ci`, a pruned store and a cleaned cache all leave the directory behind with nothing in it, which is the most common real version of "the install is not where you think", and it was the one shape the chain walk could not see: the walk asks only whether `tsx` is under each link, and an empty directory answers that with the same silence as a healthy one that lost a single package. So the reader got the absent-install refusal BYTE FOR BYTE — sent looking for a `node_modules` that is sitting right there in their own listing, which is the same contradiction the file-in-the-way finding was added to remove. `nearestEmptyLink` is one `readdirSync` on the nearest chain link, and the three ways it can decline are each a decision: an absent or obstructed link has `real === null` and is somebody else's finding; a `readdirSync` that THROWS is the unreadable-link finding, which already owns that path and that sentence, so answering here too would name one path twice with two explanations; and only the nearest link is asked, because an ancestor's empty `node_modules` is another project's business and one line per ancestor is the noise this classification keeps refusing to make. It gets its own clause rather than a row in `OBSTRUCTION_PHRASE` because the two end in opposite advice — something standing in a chain path must be REMOVED before anything can be installed there, and an empty directory is precisely what `npm ci` fills, so folding it into that table would tell the reader to delete the one directory they must keep. Appended to the absent-install and global-folder refusals, and deliberately not to the unreadable-link one, whose whole claim is that the question could not be decided about that very path. 4 new cases: the finding itself, the same finding carried into the global-folder message (an empty local install plus a loadable `$NODE_PATH` resolves, so that is the refusal a reader actually gets), a populated `node_modules` staying silent, and the two refusals this exists to separate — absent versus empty — required to differ with the roots normalised out. Mutation-checked both ways: dropping the emptiness test turns the populated case red, and suppressing the finding turns the other three red. `npm run verify` green: typecheck + lint:all 13/13 + 5405 tests (+4) + build. No DB migration, no creds.

## The phrasing that lives in one table, and the lstat that must not lie (2026-08-16)

<!-- Promoted from .tasks/.suggestions.md: the first, third and fifth entries of "Obstruction-reporting follow-ups", filed by the run directly below — the free-text obstruction fragment, the `lstat` catch that swallowed every failure, and the negative case that read only one of the three refusals. -->

- [DONE: 2026-08-16] [est: 8000] **Close the obstruction kinds into a table, stop swallowing every `lstat` failure as absence, and read the negative case across all three refusals** — three halves of the same report. (1) `ChainObstruction.what` was a free-text fragment written at the two throw-adjacent sites, and it parsed only because both were typed to fit the one template that renders them: `<path> (<phrase>)`, then "those paths are in the chain node searches". A third shape added by someone reading only the probe — a socket, a FIFO, a directory with no execute bit — would have produced a sentence that does not. It is now a closed `ObstructionKind` union with the wording in an exported `OBSTRUCTION_PHRASE` table, so adding a kind is a compile error until the phrase is written beside the two it has to read like, and the two message cases assert the phrase FROM the table rather than retyping it (a reworded row can no longer leave a green suite pinning prose the fixture stopped printing). (2) `danglingLink` reported ordinary absence for every `lstat` failure, and `lstat` reads the PARENT directory — so an `EACCES` there answered "the link is not there" for a link that may well exist, which is exactly the collapse the unreadable finding was created to stop, one level down. The catch now goes back through `describeResolveFailure`: `ENOENT`/`ENOTDIR` stay silent, everything else is routed into the unreadable list where "cannot tell" already has a sentence and deliberately does not prescribe `npm ci`. It ships as `probeAbsentLink(target, lstat = fs.lstatSync)` with the syscall injectable, because the rows that matter cannot be arranged here: these suites run as root in CI containers (root traverses an unreadable directory), and every state a non-root reader could arrange that `realpath` reports with a non-absence code is answered by the branch above this one. A case pins the default against a real dangling symlink, so the seam cannot quietly become a test of the test. (3) The clause's negative case asserted on the absent-install refusal only — the ONE of the three messages that never carries the clause at all — so a regression making it unconditional in the global-folder or unreadable-link message would have left it green while every reader of those two got a paragraph about paths that are fine. It now renders the trio and requires "Note also" and every table phrase to be absent from each. Mutation-checked three ways: swallowing the `lstat` failure turns 2 cases red, an unconditional clause turns the negative case red, and a clause hard-coded into the global-folder message alone is caught by the extension rather than by the case it replaced. `npm run verify` green: typecheck + lint:all 13/13 + 5401 tests (+9) + build. No DB migration, no creds.

## The link that points at nothing, and the clause that must stay quiet (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md: the second and fifth entries of "Chain-walk follow-ups", filed by the run directly below — the dangling-symlink chain link, and the missing negative case for the clause that run added. -->

- [DONE: 2026-08-15] [est: 6000] **Tell a dangling chain link apart from an absent one, and pin the clause as conditional** — `realpath` on a symlink whose target is gone fails with `ENOENT`, and `ENOENT` is exactly what the ancestors of every checkout report, so the classification stays silent for it on purpose. For a link a PERSON made — `ln -s ../shared/node_modules`, target since moved or renamed — that silence is the wrong answer to the only question they have: the path exists, they can see it, and it is the entire reason the install they believe they linked is not being found. One `lstatSync`, in the branch that has already established the target is not there, tells the two `ENOENT`s apart. It ships as a generalisation rather than a second list: `notDirectories: string[]` became `obstructed: ChainObstruction[]`, since "a file where a directory belongs" and "a link pointing at nothing" are one finding with two shapes — something is standing in a chain path, node skips it, and the refusal above it is right while the reason for it was missing. `probeChainLink` returns the obstruction instead of a boolean, so the walk has one branch for the class rather than one per shape. The second half is the case the previous run should have written with the first: every assertion about the clause said it APPEARS, and none said it stays away. A clause that fires when nothing is wrong is worse than one that never fires — it teaches the reader to skip the tail of every message, including the one naming their actual problem — so an ordinary absent-install refusal is now required to be one sentence shorter, by name and by symptom. `npm run verify` green: typecheck + lint:all + 5392 tests (+2) + build. No DB migration, no creds.

## The link that is a file, and the list that was only ever one (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md: the fifth entry of "Distinctness-and-detail follow-ups" (nothing asserts the report names EVERY unreadable link) and the second of "Errno-reporting follow-ups" (a file where node expects a directory is silently plain absence). Both are about the same report and the same walk, so they ship together. The `plantPackage` helper asked for by the third entry of "Containment-check follow-ups" fell out of writing the two cases and went with them. -->

- [DONE: 2026-08-15] [est: 9000] **Report a chain link that is a file, and pin the unreadable-link list as a list** — the walk behind the loader check collects findings all the way up the `node_modules` chain, and two of them were unreachable from any test. The first is a real gap in what the reader is told: `realpath` reports `ENOTDIR` only for a path component SHORT of the last one, and every chain link is an ancestor directory plus the name `node_modules` — so a stray FILE under that name (a download that landed wrong, a botched `npm ci`) resolves cleanly, matches nothing, and the walk moved on without a word. node skips it too, so the refusal that follows is right; what was missing is the reason, and its absence is visible to the reader as a contradiction — they are told their checkout resolves through a global folder while a `node_modules` sits right there in their own `ls`. `probeChainLink` now asks what the thing IS (one `statSync` on the already-realpathed answer, so no second resolution and no symlink question), the provenance carries `notDirectories` beside `unreadable`, and the finding is APPENDED to whichever refusal fires rather than replacing it: a file where a directory belongs does not make the answer uncertain the way an unreadable link does — it explains why the install is not where it looks like it is, which is a different sentence from "could not be decided" and belongs next to it, not instead of it. The second half is the list that had never been a list: every case until now planted exactly one unreadable link, so a `[0]` in place of the join, a de-dup that kept the first, or a `break` on the first finding would all have passed while telling the reader to fix one of two paths — and the second is found only after the re-run they were told would work. The new case plants a cycle at the nearest link AND one an ancestor up, then requires both paths in the message and requires the COUNT in the sentence to agree with them, since a count rendered from a different expression than the list is the next way the two drift apart. Writing the two cases turned the fake global package — `mkdirSync`, a manifest, an `index.js`, spelled out at four call sites — into one `plantGlobalTsx(dir)` helper, so a case now says what it is testing instead of restating how a package is shaped; it is loadable rather than empty on purpose, and the comment says why (an empty directory is skipped by the resolver, so the case would assert the absent-install refusal while looking like it tested a global one). `npm run verify` green: typecheck + lint:all + 5390 tests (+2) + build. No DB migration, no creds.

## The value that renders as `[object Object]`, and the opening two refusals could share (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (first and third entries of "Distinctness-and-detail follow-ups", filed by the run directly below). The other three entries of that section stay standing: two of them (a purity guard on `describeResolveFailure`, a `loaderRefusal(kind, facts)` renderer) are structural rules that want a run of their own, and the third (naming every unreadable link) is a second fixture plant rather than a message change. -->

- [DONE: 2026-08-15] [est: 11000] **Render a thrown value instead of naming the language's fallback, and compare the refusals by their opening sentence** — two halves, both about a message being read by someone who has just lost an hour to whatever produced it. The first: `catch` binds whatever was thrown, and two places in this repository interpolate that binding straight into a diagnosis — the guard fixture's unreadable-link detail and the unreadable-input reason twelve guard wrappers hand to `assertParsedInputs`. Each owned its own `String(error)`, and both copies failed on the same two values. A plain object renders as `[object Object]`, which says strictly less than the word "unknown" would — it names the fallback every object shares, not the value — and an object with NO prototype (`Object.create(null)`, the shape a JSON-ish payload arrives in) has no `toString` at all, so `String(...)` raises `TypeError: Cannot convert object to primitive value` INSIDE the catch that was reporting a different failure, replacing the diagnosis with a worse one. The rendering is now `describeThrown` in a new `lib/thrown-value.ts` — pure, `node:util` and nothing else — and both call sites import it, so neither owns a copy of the edge cases: an Error keeps its first line (checked for being a string, because `instanceof` says nothing about the shape of the fields), everything else goes through `util.inspect` at depth 1 with `breakLength: Infinity` and a 200-character cap, so a thrown config tree is one clause rather than a screen. The quotes `inspect` adds are the point rather than noise: `no errno: 'ENOENT'` is a thrown string and `no errno: ENOENT` is an Error whose message was an errno, and those are different bugs. `scripts/guard-io.ts`'s `reasonOf` carried a second defect the move fixed — it returned any string `code`, INCLUDING `""`, so an error with a blank code produced "could not be read ()", a message that names a file and then declines to say what happened to it; `describeThrownReason` treats a blank code as no code. The second half is the distinctness case shipped the run below: it compared the three loader refusals as whole strings, which is the weakest form of the property it is named for and not the shape the drift it was written for actually had — the `npm ci` repeat arrived as one clause, in messages still differing by a path further down. Two refusals that have converged on the same OPENING are two refusals a reader stops distinguishing, whatever their tails do, because the first sentence is what gets read, quoted into an issue and searched for. It now compares leading sentences too, and asserts each opening is SHORTER than its message so the stronger check cannot quietly decay into the whole-string one when a refusal loses its paragraph. Pinned by the new `__tests__/thrown-value.test.ts` (12 cases: the error first line, a non-string message, a thrown object, the null-prototype value with an `assert.throws` on `String` beside it so the case cannot outlive the reason it exists, a thrown string against an Error message, the primitives, the size cap, a no-newlines sweep, and the four `describeThrownReason` rows including the blank code and a numeric one) plus one more in `guard-fixture-refusals.test.ts` for the `[object Object]` slot. `npm run verify` green: typecheck + lint:all + 5388 tests (+13) + build. No DB migration, no creds.

## Three refusals that must stay three, and the errno nobody had (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (second and third entries of "Errno-reporting follow-ups", filed by the run directly below): `UNKNOWN` was printed in the slot where every other case prints a real errno, and nothing said the three loader refusals were DISTINCT. The first entry of that section was left standing: it proposes reversing a decision shipped an hour earlier, which wants a run of its own rather than same-day churn. -->

- [DONE: 2026-08-15] [est: 7000] **Say "no errno" in words, and pin the three loader refusals as three** — the classification behind the unreadable-link report printed `UNKNOWN` for an error carrying no `code`, in the slot where every other row prints `EACCES`/`ELOOP`/`ENAMETOOLONG`. That reads like a real filesystem code, and the reader spends the next minute looking one up that does not exist; the error's own message is the only thing actually known there. It is now `no errno: <first line>`, and the classification moved into an exported `describeResolveFailure(error)` — a pure function of one argument, which is what makes its interesting rows a TABLE instead of three arranged kernel states: the two absence codes stay silent, every other code is passed through, an error with no `code` is described in words, and a thrown non-Error survives (`catch` binds whatever was thrown, and this value is rendered straight into the diagnosis that exists to explain the failure — one `String(...)` away from a TypeError inside it). `UnreadableLink.code` became `.detail` for the same reason: the field no longer always holds an errno and a name that says it does would be a lie the next reader has to discover. The second half is the drift the individual `assert.match`es cannot see: the three refusals — does-not-resolve, GLOBAL_FOLDERS, unreadable-link — are the reader's whole map of what went wrong, and each was pinned only by its own regex in its own case, so nothing said they were DIFFERENT. The first draft of the third message repeated `npm ci` from the second, which is exactly that drift caught by hand. One case now renders all three with the mkdtemp roots normalised out and asserts three distinct strings. Non-vacuity checked by short-circuiting the unreadable branch so its case falls through to the global-folder sentence: the two collapse into one after normalisation and the case goes red, alongside a red for the `UNKNOWN` regression. 5 new cases. `npm run verify` green: typecheck, `lint:all` 13/13, 5375 tests (+5), build. No DB migration, no creds.

## The link that could not be read, told apart from the one that is not there (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Containment-check follow-ups", filed by the run directly below): realpathOrNull collapsed every failure reason into null, so three unrelated errnos arrived as the GLOBAL_FOLDERS refusal. -->

- [DONE: 2026-08-15] [est: 9000] **Keep the errno instead of collapsing every unresolvable chain link into `null`** — `realpathOrNull` answered `null` for `ENOENT` (the ordinary state of nearly every ancestor's `node_modules`) and for `EACCES` on a directory the runner cannot traverse, `ELOOP` on a symlink cycle and `ENAMETOOLONG` alike, and the caller reads `null` as "this chain link does not exist". So a checkout whose own install this process merely cannot LOOK at got the GLOBAL_FOLDERS sentence — a diagnosis naming a resolution rule that had nothing to do with it, ending in `npm ci`, which fixes none of the three. Worse than unhelpful: the unreadable link may be exactly the install the resolver loaded, so "not from the checkout" was an answer the check was not entitled to. `probeChainLink` now returns the realpath or the errno, treating only `ENOENT`/`ENOTDIR` as absence, and `resolvedFromCheckout` returns `{fromCheckout, unreadable}` — collected along the whole walk, and returned even on success so the CALLER decides, because a chain that contains the resolution has nothing left to explain. The third refusal names each unreadable path with its code, says in as many words that this is not "your checkout has no install", and deliberately does not print `npm ci`. The entry inside an unreadable directory is no longer probed: it is reached through the same path, so it can only repeat the errno under a longer name. Two new cases, both mutation-checked: a scratch checkout whose `node_modules` is a self-referential symlink plus a loader on `$NODE_PATH` (run in a CHILD, since `NODE_PATH` is read once at startup) gets a refusal that names `ELOOP` and the link and mentions neither GLOBAL_FOLDERS nor `npm ci`; and the same cycle at the NEAREST link with the real hoisted install one directory up is ACCEPTED, since an unreadable link is a reason to withhold the answer, not to refuse an install the spawn goes on to load. A symlink cycle rather than a `chmod`, because these suites run as root in CI containers and root traverses an `EACCES` directory anyway — the permissions case would have passed without ever producing an errno. `npm run verify` green: typecheck, `lint:all` 13/13, 5370 tests (+2), build. No DB migration, no creds.

## The chain entry that is there and does not answer (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Provenance-check follow-ups", filed by the run directly below), and the fixture it asks for found a hole rather than confirming a comment: the second arm of the provenance check accepted an install that had NOT answered. -->

- [DONE: 2026-08-15] [est: 8000] **Prove the chain entry ANSWERED, instead of trusting that it exists** — `resolvedFromCheckout` accepted on either of two grounds, and the second was `existsSync(<chain>/node_modules/tsx)`, defended by an argument in a comment: the chain is searched before the global folders, so an entry that is there must have won. The argument has a gap the comment did not, and the fixture the suggestion asked for walked straight into it — the chain is searched first, but an entry the resolver cannot LOAD is skipped rather than fatal, so an empty `node_modules/tsx` (an interrupted `npm ci`, a pruned store), a broken symlink or a manifest whose `main` was never written all leave the resolver falling through to `$NODE_PATH` and succeeding there. Existence then reads that as "the checkout answered", clears the check, and hands five suites the spawn that exits 1 with both pipes empty — a half-written install plus a good global one being precisely the case this check was added for a commit earlier. Reproduced before it was fixed: an empty `node_modules/tsx` under the root and a loadable `tsx` on `NODE_PATH` resolves to `/tmp/…/g/tsx/index.js` while `existsSync` on the chain entry returns true. The existence arm is gone; both arms are now containment, REALPATHED on both sides — the chain's `node_modules` and the package entry inside it — which keeps every case the existence arm was protecting (a symlinked or store-backed install lives at the entry's realpath; a root reached through a symlinked ancestor realpaths to the same place) while refusing every unloadable one, since a path that does not resolve has no realpath to compare. One new case, red under the shipped `existsSync` and green now. `npm run verify` green: typecheck, `lint:all` 13/13, 5368 tests (+1), build. No DB migration, no creds.

## The install proved to be the checkout's own (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Loader-resolution follow-ups", filed by the run directly below): `require.resolve` keeps GLOBAL_FOLDERS even when `paths` is passed, so a globally installed loader answered for a checkout that has none. -->

- [DONE: 2026-08-15] [est: 11000] **Refuse a loader that only a global folder can see** — the resolution that replaced `existsSync` yesterday asks node's own question, and node's answer is wider than the question: `paths` replaces the resolution roots "with the exception of GLOBAL_FOLDERS", so `$NODE_PATH`, `~/.node_modules` and `$PREFIX/lib/node` still answer. A developer with a global `tsx` therefore got a GREEN loader check for a checkout with no install — and then the spawn the check clears is `node --import tsx`, an ESM resolution, which consults no global folder at all. That is the empty-pipes failure the whole check exists to prevent (exit 1, both pipes empty, byte-for-byte a guard REFUSING), arriving through the check itself, with the one message that would have explained it suppressed. `tsxLoaderIn` now asks a second question after the resolution succeeds: did the answer come out of the checkout's own dependency chain? `nodeModulesChain(root)` is `root/node_modules` plus every ancestor's — precisely what CJS walks and what the ESM resolver walks, and precisely NOT the global folders — and `resolvedFromCheckout` accepts on either of two grounds, because a resolved path is REALPATHED: the file lies under a chain entry, or a `node_modules/tsx` entry exists on the chain. The second arm is what keeps a symlinked or store-backed install (pnpm, `npm link`, a workspace) accepted — the chain is searched BEFORE the global folders, so if such an entry exists the successful resolution necessarily came through it — and it is also what keeps a root reached through a symlinked ancestor (`/var` → `/private/var` on macOS) from being read as a global answer. The refusal names the resolved file, the `node_modules` that should have answered, the GLOBAL_FOLDERS rule that let it through, and `npm ci`. Three new cases: the global-folder refusal, run in a CHILD process carrying `NODE_PATH` (it is read once when node builds its global paths at startup, so assigning it in-process changes nothing — a call could not have tested this); a symlinked install accepted; a hoisted install one directory up accepted, in a temp directory made inside this repository so the ancestor chain is real. The first is non-vacuous by construction: the same fixture under the previous code resolves `/tmp/…/tsx/index.js` and answers `"tsx"`. `npm run verify` green: typecheck, `lint:all` 13/13, 5367 tests (+3), build. No DB migration, no creds.

## The install proved by resolving it, and the last spawn through the bin (2026-08-15)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Fixture-refusal follow-ups", filed by the run directly below): the loader check proved a DIRECTORY named `node_modules/tsx` exists, which is not the thing the spawn depends on. -->

- [DONE: 2026-08-15] [est: 9000] **Resolve `tsx` the way node will, instead of asking whether a directory shares its name** — `tsxLoaderIn` is the one check standing between five suites and their worst failure mode: an absent loader exits non-zero with BOTH pipes empty, byte-for-byte what a guard REFUSING looks like, so every "this run failed" assertion passes for the wrong reason and every "this run passed" fails with a bare `1 !== 0` and nothing to explain it. It proved that with `existsSync` on `node_modules/tsx`, which is a claim about a directory NAME rather than about the thing the spawn needs: an interrupted `npm ci`, a pruned store or a half-restored cache all leave the directory in place with the package's entry points missing, so the check passes and the spawn produces exactly the empty-pipes signature the check exists to prevent — the one failure it must not let through. `require.resolve("tsx", {paths: [root]})` asks node's own question, through a `createRequire` bound to the helper only so `resolve` exists (every call passes `paths`, which replaces the resolution roots entirely, so the answer is about the checkout in the argument). The refusal now names the checkout, the `node_modules` it looked under, AND the resolver's own first line, because a half-written install and an absent one want different fixes and the sentence is the only place a reader can tell them apart — pinned by a case that renders both with the root normalised out and requires them to DIFFER, or "it says why" is satisfied by two identical sentences. It still answers the bare specifier rather than the file the resolver named: `--import /abs/node_modules/tsx` is an `ERR_UNSUPPORTED_DIR_IMPORT`, and the CJS resolution may name a different entry than the `import` condition the spawn gets. Three new cases (the empty `node_modules/tsx` directory, the manifest whose declared entry point is missing, the two-sentence comparison), all three red under the `existsSync` this replaces. **Related bug found by the same sweep and fixed in the same pass:** `__tests__/lint-guard-premise.test.ts` — the file that runs all thirteen guards for real — still carried its own `execFileSync(node_modules/.bin/tsx)`, the last one in the repository. That is the spawn shape that sat in CI's "Run tests" step past 25 minutes twice: the bin is a WRAPPER around its own node child, so killing it leaves the grandchild holding the write end and the parent's read never sees EOF, which makes a `timeout` decoration — and this copy had no `timeout`, no `maxBuffer` and no loader check at all, so on a checkout with no install it produced thirteen bare `1 !== 0` failures with empty output. Migrated onto `runGuardWith`, deleting ~25 lines of private spawn and a second `checkNameOf`. The migration got a drift guard rather than a comment: a sweep over every `__tests__/*.test.ts` (comments stripped, since two files explain this in prose) refusing the bin spawn, with the pattern assembled from two halves on separate lines because written as one literal it matches ITSELF and would report only its own file forever. The sweep carries its own positive control — both spellings the repository has actually used must match, and a `.bin/tsc` must not — so a pattern that stopped matching anything could not pass as a clean sweep. Mutation-checked both ways: restoring the `existsSync` turns 3 loader cases red, and planting a bin spawn in a sibling suite makes the sweep name the file. `npm run verify` green: typecheck, `lint:all` 13/13, 5364 tests (+5), build. No DB migration, no creds.

## The diagnosis a fresh checkout gets (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (first entry of "Fixture-refusal follow-ups", filed by the run directly below): tsxLoader's refusal was the one throw in the helper still without a run, and it fired for real during that run. -->

- [DONE: 2026-08-14] [est: 5000] **Make `tsxLoader`'s refusal reachable from a test, and give it two** — the last untested throw in `__tests__/helpers/guard-fixture.ts`, and the one with the strongest claim on being tested: an absent loader exits non-zero with BOTH pipes empty, which is byte-for-byte what a guard REFUSING looks like to these suites, so without it every "this run failed" assertion in five files would pass for the wrong reason. It was unreachable only because `TSX_MODULE` was a `const` resolved against `REPO_ROOT` at import, which is the one thing a test cannot vary. Now `tsxLoaderIn(root)` takes the checkout to look in and `tsxLoader()` is it against `REPO_ROOT` — the same shape `makePartialRoot` already uses for the tree it copies out of. Two cases: an empty scratch directory gets a refusal that names `npm ci` AND the path it looked at (a diagnosis that omits either is one the reader cannot act on), and the real checkout answers with the BARE specifier rather than the directory it checked, which is the `ERR_UNSUPPORTED_DIR_IMPORT` the doc comment warns about stated as an assertion. Not hypothetical: the check fired for real on the checkout this was written on. `npm run verify` green: typecheck, `lint:all` 13/13, 5359 tests (+2), build. No DB migration, no creds.

## The fixture's own refusals, seen firing (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fifth entry of "Shared-fixture follow-ups"): makeSharedPatchedRepo's marker injection is asserted per case and the injection itself is untested. Widened to every refusal in the helper, since the same argument covered all nine. -->

- [DONE: 2026-08-14] [est: 16000] **Give every refusal in `__tests__/helpers/guard-fixture.ts` a red run** — the helper is the floor under five suites and a few hundred assertions, and nearly every claim those suites make is really a claim about it: that the scratch root holds what the spec asked for, that the patched copy is the thing that ran, that the patch landed. It defends each with a throw, and until now only one of the nine had ever been seen to fire (`makePartialRoot([], {})`, next door in the empty-input suite). A guard that cannot fire is indistinguishable from one that fires correctly — `makeSharedPatchedRepo`'s marker check is the ONE thing standing between thirteen provenance assertions and being vacuous, and `entryPatch`'s brace counter is the one thing standing between a patched table and a rewrite that landed in the wrong half. New `__tests__/guard-fixture-refusals.test.ts`, 22 cases: the absolute entry, the entry this repository does not have, the marker module missing from the copied entries, the patch aimed at a file the copy does not hold, the patch that changed nothing, `entryPatch`'s missing key / unclosed entry / no-op rewrite, plus the positive halves those refusals only make sense against (a nested entry sliced WHOLE rather than stopped at its inner brace, the neighbouring entry left alone, and the same parse run against the committed `SCANNED_FLOORS` so the fixture's idea of an entry is checked against a real one). `assertNoStackTrace` — cited by four suites and never tested — got four: prose containing the word "at" passes, a frame on line ONE is caught (a bare `/\n\s+at/` would not be), the nameless `at /path:1:2` shape is caught, and the context prefix names the run. **Two real bugs found and fixed in the helper, both mutation-checked.** (1) `makePartialRoot` refuses AFTER `mkdtempSync`, so every bad-spec assertion leaked a scratch directory into `os.tmpdir()` — invisible precisely because the throw it rides on is the expected outcome. The population is now in a `try` that `rmSync`s and rethrows, and the three cases compare the set of `lint-guard-partial-*` directories before and after rather than trusting the sentence. (2) `runPatched` called `applyPatches` OUTSIDE its `try`, and `applyPatches` writes file by file — so a multi-file patch whose second anchor had drifted threw with the first file already rewritten and the `finally` never reached, handing the next case a copy broken in a way its own patch never mentions. Moved inside the `try`; `pristine` is captured per file as it is touched, so a partially applied patch restores exactly as far as it got. Mutation-checked by putting both back: 5 cases go red, including the cascade where the leftover patch makes the NEXT case's restore assertion fail — which is the failure shape the bug would have produced in the two patched-table suites. `npm run verify` green: typecheck, `lint:all` 13/13, 5357 tests (+22), build. No DB migration, no creds.

## One parser for the table, in the file both harnesses read it from (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fourth entry of "Lookup-code follow-ups", filed by the run directly below): that run wrote a second copy of the entry-slicing brace counter because the first lived in a *.test.ts file rather than in helpers/. -->

- [DONE: 2026-08-14] [est: 4000] **Move `entryPatch` into `helpers/guard-fixture.ts` so one brace counter serves both patched-table harnesses** — `lint-guard-invalid-floor.test.ts` (ten malformed-declaration cases) and `lint-guard-missing-floor.test.ts` (three unanswerable-lookup cases) patch the SAME table the same way: find `  "<key>": {`, count braces from the opening one, rewrite inside the slice, refuse a rewrite that matched nothing. The second copy existed only because the first was declared in a file the `__tests__/*.test.ts` runner glob owns and therefore could not be imported from. It now sits beside `makeSharedPatchedRepo`, which is the function it exists to feed, and both harnesses import it — so the assumption the parse rests on (an entry that ever NESTS must be sliced whole, or the rewrite lands in the wrong half and `runPatched`'s no-op check waves it through because the patch did change something) is written down once instead of being right twice by hand. Pure move: no behaviour change, both harnesses green unchanged at 108 cases. `npm run verify` green: typecheck, `lint:all` 13/13, 5335 tests, build. No DB migration, no creds.

## The refusal that arrived as a stack trace (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fifth entry of "Blank-key follow-ups"): scannedFloorFor threw a bare Error for an unknown check name beside a ScannedFloorError for an unusable one. -->

- [DONE: 2026-08-14] [est: 14000] **Give the three floor LOOKUPS a failure code instead of a bare `Error`** — `scannedFloorFor` (no entry under this name), `countFloorFor` (entry is not count-shaped) and `assertParsedInputs` (entry declares no inputs) each threw a plain `Error`, and every wrapper's handler catches `ScannedFloorError || GuardRootError` and re-throws anything else. So the three most likely consequences of a RENAME printed as node stacks naming `scannedFloorFor`, while the ten ways a malformed entry can be unusable printed as one line headed by the guard's own name — the refusal a reader is likeliest to meet was the one that looked like a crash. `no_floor_declared`, `no_count_floor` and `no_inputs_floor` are the seventh, eighth and ninth `ScannedFloorFailureCode`, so all nine now render out of `FAILURE_MESSAGE` and the module owns every word it prints. A private `lookupFailure(code)` builds them with zeroed `count`/`minimum`, and their sentences deliberately read NEITHER: these are findings about a lookup, so quoting a number would invent a measurement nobody took, and the remeasure hint would send the reader to change a number that is not the problem (both pinned by a test). `SHAPE_MISMATCH_REASON` is exported for the same reason `FLOOR_BELOW_ONE_REASON` is: the two shape sentences are one disagreement stated from either side, so the shared half lives once and the halves that differ are the assertion the wrapper should have called instead (`assertParsedInputs` vs `assertScannedFloor`). New `__tests__/lint-guard-missing-floor.test.ts` gives each of the three a red run through a real wrapper on the shared patched copy — the key renamed out from under `check-inline-hex`, its `count` swapped for `inputs`, and `check-appstore-config`'s `inputs` swapped for a `count` (with a date added to the note in the same patch, because an undated count floor would refuse FIRST on `undated_note` and the case would silently stop testing the code it names). Each case asserts exit 1, the patched-copy marker, the WHOLE sentence rendered by `formatScannedFloorFailure` rather than a re-typed fragment, no stack trace, no `scanned-floor: ` prefix (the bare errors' shape), and no clean-scan line. The excuse list is mechanical rather than prose-only: `EVALUATOR_PRODUCED` names, per excused code, the harness that produces it, and a test requires each named `__tests__/…` path to EXIST — so `LookupFailureCode = Exclude<…>` makes a new failure code fail to compile until it has a case here or a harness elsewhere. Mutation-checked by putting the bare `Error` back on `countFloorFor`: three cases go red, including the shared-reason one. `npm run verify` green: typecheck, `lint:all` 13/13, 5335 tests (+34), build. No DB migration, no creds.

## The rule stated where it applies, not where it was written (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (third entry of "Subject-id follow-ups", which is the standing "repo-wide guard for PROBLEM_DETAIL readers" suggestion restated for the second table): one regex over one file was the whole defence. -->

- [DONE: 2026-08-14] [est: 26000] **Add `check-problem-phrasing-imports`: a phrasing PART may not be read without a sentence JOINER in scope** — `lib/scanned-floor.ts` renders one problem as one sentence, a subject joined to a clause in exactly one function. Both halves are exported on purpose (a test asserting what a refusal says should read the words rather than re-type a fragment), and that is also the hole: any module can import `scannedFloorProblemDetail` and `scannedFloorProblemSubject` and build its own `"<subject> <clause>"`. The only thing standing against it was the `one problem, one phrasing` drift guard — a regex over `lib/scanned-floor.ts` that sees a second reader IN THAT FILE and nothing anywhere else — which is the same gap the suggestion named twice, once per private table. The rule now lives where it applies: `lib/check-problem-phrasing-imports.ts` (pure matcher) + `scripts/check-problem-phrasing-imports.ts` (walker), registered as the thirteenth entry of `LINT_GUARDS` and `npm run lint:problem-phrasing`, in the shape `check-analytics-imports` already uses. It gathers the named bindings of every `import … from "…/scanned-floor"` in a file and refuses when a part is present and no joiner is — `describeScannedFloorProblem`, `describeScannedFloorProblemRef`, `formatScannedFloorProblem` or `formatScannedFloorFailure`, the last because a failure carries a problem ref and the message table renders it through the same path. Namespace imports are followed rather than left as a one-line walk-around: `import * as floor` plus `floor.scannedFloorProblemDetail(…)` reads a part and is reported, while a file whose namespace reads reach a joiner passes. Comments are stripped first, so the guard does not refuse on its own doc block naming the accessors. `lib/scanned-floor.ts` needs no exemption and gets none: it declares these functions and imports none of them, which a test pins by asserting the file contains no self-import — an exemption list that cannot fall out of date because there is none. Scan roots are `app/`, `components/`, `lib/`, `scripts/` and `__tests__/`; tests are deliberately IN scope, since a test asserting what a refusal says is precisely the caller that wants both halves. Its `SCANNED_FLOORS` entry commits 450 against 638 files measured today, above the 396 that `__tests__/` alone contributes, so no single scan root clears the floor by itself — and the partial-root harness got a fixture for it, since a count floor with no red run is a number nobody has ever seen fail (the suite refuses a new count-shaped guard without one, which is how the omission surfaced). 20 new unit cases cover both parts, all four joiners, two-statement imports, type-only and aliased bindings, a longer neighbouring module name, prose in comments, and all three namespace shapes; five repository files are asserted clean under the rule they are subject to. Mutation-checked by planting a two-part hand-joined sentence in `lib/`: the guard exits 1 and names both reads, the file, the line and the way out. `npm run verify` green: typecheck, `lint:all` 13/13, 5301 tests (+36), build. No DB migration, no creds.

## The key nobody validated, and the sentence that opened with a space (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (fifth entry of "Subject-id follow-ups"): the validator checked everything under a SCANNED_FLOORS key and never the key itself. -->

- [DONE: 2026-08-14] [est: 12000] **Report a blank `SCANNED_FLOORS` key as its own problem code instead of rendering a headless sentence** — `validateScannedFloorEntry` checked ten properties of an entry's DECLARATION and never the entry's own name, so a blank key was invisible to it: every shape check passes, `formatScannedFloorProblem` renders the key as the sentence's subject, and the reader gets `" declares no premise…"` — a line that opens with a space where the guard's name belongs. The subject-id refactor a day earlier closed the ref path (both ids resolve to non-empty exported constants) and left this one open, because it is not a rendering bug at all: it is an unvalidated input reaching a renderer that cannot defend itself. `empty_check_name` is the eleventh `ScannedFloorProblemCode` and the only one about the NAME rather than the declaration under it. It is added FIRST in the validator, deliberately: `scannedFloorFor` raises the first problem, so a blank key reported second would print as whatever else the entry got wrong, under a subject that is a space. Its clause in `PROBLEM_DETAIL` says what the defect is rather than describing a different entry — "is keyed by a blank name, so no wrapper can look its floor up and every sentence about it opens with a space where the guard's name belongs" — so the one sentence in the table that genuinely renders with a missing first word is the sentence explaining that the missing first word is the finding. `describeScannedFloorProblem`'s doc now says where a blank subject is defended (at the source, not in the renderer) rather than leaving the next reader to conclude it is not defended anywhere. The pure suite's `BROKEN` fixture map grew a `BROKEN_UNDER` companion — the check name a fixture is validated under, needed by exactly one code — so the three loops that read `BROKEN` (produces-its-code, has-a-fixture, has-a-sentence) cover the new code without the map itself changing shape; new cases pin that a sound floor under a blank key reports `empty_check_name` and NOTHING else, that it is reported first, that `validateScannedFloors({"": floor})` catches it (the route it actually travels, since no wrapper asks for a floor under a blank literal), and that every real `SCANNED_FLOORS` key is non-blank. The wrapper harness in `lint-guard-invalid-floor.test.ts` could not gain an eleventh case honestly: a guard asks for its floor under a string literal in its own source, so a blank KEY is not a row any wrapper can request. Rather than fake one, `ProblemCodeKey` became `Exclude<ScannedFloorProblemCode, WrapperUnreachableCode>` beside a `WRAPPER_UNREACHABLE` record that costs a written reason per excused code, with `Extract<…>` on the excuse type so a code cannot be excused under a name the module does not have — a new code still fails to COMPILE until it has a wrapper run or an argument about reachability. A runtime case asserts the excuse is non-empty and that no code is listed on both sides. `npm run verify` green: typecheck, `lint:all` 12/12, 5265 tests (+6), build. No DB migration, no creds.

## The other type's words field, and the branch nobody could reach (2026-08-14)

<!-- Promoted from .tasks/.suggestions.md (top entry of "Two-field-problem follow-ups", plus the second entry of that section and the third of "Single-phrasing follow-ups", which are the same finding stated three ways): the PROBLEM type lost its rendered-sentence field yesterday and the FAILURE type kept one. -->

- [DONE: 2026-08-14] [est: 11000] **Carry a problem code on an `invalid_floor` failure, not a sentence someone else rendered** — `ScannedFloorProblem` dropped its `detail` field yesterday because every renderer derived the words from `code`; `ScannedFloorFailure` still carried `detail?: string`, and `FAILURE_MESSAGE.invalid_floor` printed it verbatim, which made it the ONE of six codes whose message came from outside the message table. So the sentence a guard showed for a bogus declaration was chosen by whichever of three call sites built the failure — exactly the state the problem type was in a day earlier, on the type that actually crosses the boundary from the pure function to the wrapper. `detail` is gone and `problem?: ScannedFloorProblemRef` replaces it: `{ subject, code }`, two enums and no prose. `ScannedFloorProblemSubject` is `"entry" | "caller"` — the only part that genuinely varies between producers — resolved to words by `scannedFloorProblemSubject`, the single reader of a `PROBLEM_SUBJECT` record that is exhaustive over the union and points at the two already-exported subject constants, so the ids and the constants cannot drift into two vocabularies. `describeScannedFloorProblemRef` joins the pair through the existing `describeScannedFloorProblem`, so a failure printed by a wrapper and a problem listed by a test say the same thing about the same code by construction rather than by convention. The `?? NUMERIC_FLOOR_DETAIL(f.minimum)` fallback stopped being a fallback: `evaluateScannedFloor`'s bogus-minimum branch no longer fills a detail, so that arm is now the REACHED renderer for the one `invalid_floor` that is not about a table entry at all (a number handed straight to the pure function — no entry to send the reader to, and no problem code that honestly fits), and the optionality of `problem` is documented as that either/or rather than left looking like a default. Both producers stopped building strings: `evaluateParsedInputs` reports `{subject: "caller", code: "empty_inputs"}` and `scannedFloorFor` reports `{subject: "entry", code: problem.code}`. New assertions run over the failure type's WHOLE surface instead of one fixture, which is the second half of the promoted suggestion: an exhaustive `Record<ScannedFloorFailureCode, () => ScannedFloorFailure>` gives every code a producer, and each produced failure must (a) carry no string field containing any of the ten sentences from the table, (b) declare no field outside a `Record<keyof ScannedFloorFailure, true>` — a `keyof` claim, so a re-added words field fails to COMPILE where a runtime key check cannot see a declared-but-never-populated one — and (c) carry a `problem` only when the code is `invalid_floor`. The same `keyof` treatment went onto `ScannedFloorProblem`, replacing the single-fixture `Object.keys` check. All ten problem codes are now rendered through the failure path and asserted to contain the table's own clause in the entry's voice, and the numeric branch is pinned to name the number AND to not sound like a finding about the table. Mutation-checked by flipping `scannedFloorFor`'s subject to `"caller"`: `scanned-floor.test.ts` stays green (it builds its own refs) and all ten wrapper runs in `lint-guard-invalid-floor.test.ts` go red, which is the honest division of labour — the pure suite pins the rendering, the wrapper suite pins which subject a guard actually reports. `EVERY_PROBLEM_CODE` moved to module scope in the test file so the phrasing block and the new failure block read one exhaustive list rather than two. `npm run verify` green: typecheck, `lint:all` 12/12, 5259 tests (+9), build. No DB migration, no creds.

## "Mark as received" buyer confirmation (decomposed from "Persist a Mark as received button on each purchase card")

Context: lets a buyer confirm a marketplace purchase arrived via an `arrivedAt` timestamp, unlocking dispute handling and a "delivery confirmed" signal on the seller's sales card. Decomposed bottom-up — pure data layer first, then local persistence, then buyer UI, seller UI, then cloud sync + migration. Each sub-task ships independently.

<!-- Decomposed 2026-07-04 from "Hide the visibilityChip for private entirely on free accounts once we have analytics confirming nobody clicks the locked chip": the precondition was unverifiable because the locked-chip tap was never instrumented — no event, so "nobody clicks" can never be confirmed. Split into (a) instrumentation, actionable now, and (b) the hide itself, blocked on collected data. -->

- [needs-data] Hide the `visibilityChip` for "private" entirely on free accounts (instead of locking it) once PostHog shows ~zero `premium_upsell_shown` events with `feature = "private_collection"` over a meaningful window (say 30 days after the instrumentation ships) — a cleaner flow at the cost of a less discoverable upgrade prompt. Blocked until that data exists; do not implement before checking the dashboard.

<!-- Decomposed 2026-07-11 from "Add a __tests__/analytics-provider-strict-mode.test.ts that mounts <AnalyticsProvider> under <React.StrictMode>": the repo has no React mounting harness (no jsdom, no react-test-renderer — only the fake-dom stub), so a real StrictMode mount is blocked on a dev-dep decision. Decomposed bottom-up: pure scheduler first (node-testable now, mock timers), provider wiring second, the actual mount test last (blocked). Note the 500ms identify debounce already shipped — the double-mount invariant is now "two rapid schedules collapse into one identify". -->

### ItemCard design-tokens migration (2026-05-25)

- [needs-dep-decision] Migrate `LazyPhoto` onto `expo-image` for real offscreen deferral (`priority="low"`, `recyclingKey`, built-in transition) — blocked on adding the `expo-image` dependency (new package + lockfile churn; decide alongside the jsdom/react-test-renderer dev-dep decision the `[needs-dev-dep]` tasks are waiting on). The `LazyPhoto` component shipped 2026-07-16 is the single adoption point, so the migration is a one-file swap when the dep lands.

### EmptyState design-tokens migration (2026-05-25)

- [needs-dev-dep] **Migrate the `borderStyle: "dashed"` rendering test** [blocked 2026-07-17: a headless-browser screenshot test needs a browser-automation dev-dep (`@playwright/test` or similar) that `npm test`'s plain `tsx --test` harness doesn't have — decide it alongside the jsdom/react-test-renderer dev-dep the `[needs-dev-dep]` analytics tasks wait on] — React Native Web renders dashed borders inconsistently across browsers (Safari + Firefox honor it; Chrome on Android sometimes drops it). A visual regression test using a headless browser screenshot would catch silent drops, especially since `EmptyState` is the only component in the design-tokens-migrated set that uses `borderStyle: "dashed"`. Without it, a future React Native Web upgrade could mute the visual without anyone noticing.

### Item-filters design-tokens migration (2026-05-23)

- [DONE: 2026-08-16] [est: 0] **Add `keyboardType="numeric"` enforcement + min/max validation to the `priceFrom`/`priceTo` `TextInput`s** — today the inputs accept any string and the apply step calls `parseFloat` which can return NaN or negative numbers. A pure `validatePriceRange(from, to)` helper in `lib/filter-helpers.ts` would let the apply button gray out on invalid input (e.g. `from > to` or non-numeric) and surface a `t("filterInvalidPrice")` hint below the row, mirroring the existing `inputInvalid` style from `create-collection.tsx`. **Closed as already shipped** — duplicate of the 2026-08-09 `validatePriceRange` entry filed from the item-filters section; `components/item-filters.tsx` carries `keyboardType="numeric"` on both bounds and gates Apply on `validatePriceRange`, so both halves of this entry are live and pinned by `__tests__/item-filters-price-range.test.ts` + `__tests__/item-filters-apply-guard.test.ts`.

### Wishlist design-tokens migration (2026-05-23)

- [DONE: 2026-08-16] [est: 0] **Batch-migrate the new `CARD_BG_9` (`#fff4e5`) across the 6 other inline consumers** — the value lives inline in `app/index.tsx` (2 occurrences), `app/collection/[id].tsx` (2 occurrences), and `app/item/[id].tsx` (2 occurrences). All seven sites use it as the warm-cream "pick / image-picker / amber-cta" button background; rolling them in a single PR (rather than waiting for the per-file sub-tasks 3+ tasks away) would lock in its canonical role as "warm cream secondary-button surface" and prevent the value from drifting while someone edits one of those screens in isolation. ~3 minutes per file, fully mechanical. **Closed as already shipped** — no inline `#fff4e5` survives anywhere outside `lib/design-tokens.ts`; all seven sites (`app/index.tsx`, `app/collection/[id].tsx`, `app/wishlist.tsx`, `components/item-card.tsx`, `components/use-app-theme.ts`) import `CARD_BG_9` by name, and `__tests__/design-tokens.test.ts` pins each file's import plus a repo-wide no-inline-hex assertion.

- [DONE: 2026-08-16] [est: 0] **Promote `CARD_BG_7` (`#f4ecdf`) into the page-background gradient declaration in `components/screen.tsx`** — that component still declares `colors={["#f4ecdf", "#fffaf4", "#f4f1ea"]}` inline. The first stop now has a named token; replacing it would let a future "warm vs. cool theme" toggle flip the gradient through token re-export rather than a hardcoded edit, and would close the gradient's first inline literal. The `"#fffaf4"` mid-stop is already `PAGE_BG_2`, and `"#f4f1ea"` would join `CARD_BG_*` as a sibling once we touch it. **Closed as moot** — `components/screen.tsx` no longer declares a gradient at all: the page background became `theme.page` from `useAppTheme()` during the `redesign-secondary` pass, so the three-stop literal this entry was about is gone rather than tokenised. `#f4ecdf` now appears only in `lib/design-tokens.ts` as `CARD_BG_7`.

- [DONE: 2026-08-16] [est: 0] **Replace the 3 remaining `backgroundColor: "#fff"` 3-digit shorthands with a new `WHITE_INPUT_BG` token** — wishlist's input / cancelButton / collectionRow each render pure-white surfaces on the cream page. Today the 3-digit shorthand sidesteps the 6-digit regex, but a named token (which could simply re-export `PURE_WHITE`) would make the intent visible and let a future "subtle off-white inputs" rebrand be a one-token swap. Same pattern would apply to the `chatsBadgeText` color call-out from prior migrations. **Closed as already shipped** — the three `backgroundColor: "#fff"` shorthands are `PURE_WHITE` now, which is the re-export this entry proposed `WHITE_INPUT_BG` would be; a second name aliasing the same value would be a token with no distinct meaning, so the alias was not added. No 3-digit background shorthand remains under `app/` or `components/`.

- [DONE: 2026-08-16] [est: 9000] **Add a `<DangerIconButton />` extraction so the trash/delete row gets its own token-aware widget** — wishlist's `deleteButton` style (borderRadius 999 + DANGER_SOFT border + CARD_BG_8 bg + DANGER_DEEP_3 icon) is a recipe that will recur on every list view that supports per-row delete (item card, collection card, listing card). Extracting it would let those screens use one component instead of re-implementing the 4-line stylesheet, and gives the danger family its first composite "destructive action" UI primitive. **Shipped** as `components/danger-icon-button.tsx` with the `icon onPress accessibilityLabel disabled` API — `accessibilityLabel` REQUIRED rather than optional-with-fallback, which is the one place it departs from `<SoftDestructiveChip>`: an icon-only control renders no text, so the fallback that widget uses would announce nothing, and the wishlist row it was lifted from shipped with no label at all. The recipe moved verbatim (pill radius, 10/8 padding) and its colours became `DESTRUCTIVE_ICON_SURFACE` / `DESTRUCTIVE_ICON_FOREGROUND` in `lib/danger-surface.ts`, beside the soft pair, with the same colours-here/geometry-there split the family already agreed on. The two surfaces were deliberately NOT merged: they are one channel apart on all three slots (`CARD_BG_8`/`CARD_BG_10`, `DANGER_SOFT`/`DANGER_SOFT_2`, `DANGER_DEEP_3`/`DANGER_DEEP_4`), which is exactly the drift `<SoftDestructiveChip>`'s doc comment warns about caught a shade further along — but repainting a shipped row is a design decision and not a refactor, so each gets a name and the merge is filed as a suggestion instead of smuggled in under an extraction. A case asserts they differ on every slot, so a future "tidy-up" that collapses one into the other has to say so out loud. `app/wishlist.tsx` is the single adoption point and drops three token imports with the inline stylesheet entry. Two things fixed on the way: the delete button had no accessibility label (`t("deleteItem")` now), and `deleteItem` existed in only 3 of 6 languages — `pl`/`de`/`es` inherit `...en`, so the label rendered English in three locales and no test could see it. 18 new cases in `__tests__/danger-icon-button.test.ts`, colours asserted at RUNTIME through the render harness rather than by source regex (the component mounts, and the rendered style is compared against the imported constants), plus the surface's shape, the distinctness of the two trios, the disabled treatment, the mandatory-label typing, and an adoption sweep requiring no screen to import `CARD_BG_8` + `DANGER_SOFT` together again. Mutation-checked three ways: repointing the button at the soft surface turns 4 cases red, drifting the padding turns 1, hardcoding the wishlist a11y label turns 1. One knock-on the change forced: the new component file made `components/` a 45th file and drew level with `check-inline-radius` / `check-analytics-imports`' floor of 45, which `__tests__/lint-guard-partial-root.test.ts` reads as "one scan root now clears this guard alone" — both floors re-measured to 46 against the 64-file walk, `check-clarity-input-mask` raised with them to keep the aligned pair aligned, and three stale note counts refreshed. `npm run verify` green: typecheck + lint:all 13/13 + 5476 tests (+18) + build. No DB migration, no creds.

