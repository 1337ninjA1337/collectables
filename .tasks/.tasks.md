- [DONE] [est: 22000] there is still same error that says Supabase needs to be connected Add EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY, then enable Email, Google, and Apple in Auth settings.
- (decomposed from the original [~200k tokens] cross-device chat task)
- [DONE] [est: 18000] chat-cloud-1: add Supabase migration for `chat_messages` table (id uuid pk, chat_id text, from_user_id uuid, to_user_id uuid, text text, created_at timestamptz default now()) with indexes on (chat_id, created_at) and (to_user_id, created_at); enable RLS; add SELECT policy "either participant can read" and INSERT policy "sender must equal auth.uid() AND must be a friend of recipient (lookup against existing friends table)". Document policy SQL inside `supabase/migrations/`.
- [DONE] [est: 14000] chat-cloud-2: add `lib/supabase-chat.ts` with REST helpers (`fetchMessagesForChat`, `sendMessage`, `markRead`) that talk to the new table via the existing `supabaseRest` pattern in `supabase-profiles.ts`. Falls back to no-op when `!isSupabaseConfigured` so local-only mode keeps working.
- [DONE] [est: 16000] chat-cloud-3: refactor `lib/chat-context.tsx` to load+send through `supabase-chat.ts` first, mirror writes into AsyncStorage as a local cache, and merge cloud results on top of cached results so offline reads still work. Keep the `collectables-chats-v1-{userId}` cache key.
- [DONE] [est: 17000] chat-cloud-4: subscribe to a Supabase realtime channel filtered to `to_user_id = current user` inside `chat-context.tsx`, push incoming messages into state immediately, and unsubscribe on sign-out / unmount.
- [DONE] [est: 19000] chat-cloud-5: add a "typing..." indicator using a Supabase Presence channel keyed by `chat_id`; broadcast on `onChangeText` (debounced 1s) and clear on send; render below the message list in `app/chat/[id].tsx`.
- [DONE] [est: 13000] chat-cloud-6: add an unread-message badge on the bottom-nav friends tab driven by chat-context state (count of cloud messages where `to_user_id = me` AND `created_at > last_seen_at`); update `last_seen_at` whenever the user opens that chat screen.
- [DONE] [est: 11000] chat-cloud-7: add tests for `supabase-chat.ts` request shaping (URL, headers, body), the friend-check helper, and the unread-counter selector.

- (decomposed from the original ~150k-token marketplace task)
- [DONE] [est: 21000] marketplace-1: add `MarketplaceListing` type to `lib/types.ts` (id, itemId, ownerUserId, mode: "trade" | "sell", askingPrice nullable, currency, notes, createdAt, soldAt nullable). Add a `lib/marketplace-context.tsx` with AsyncStorage cache (`collectables-marketplace-v1`) exposing `listings`, `myListings`, `addListing`, `removeListing`, `findListingByItemId`. Wire the provider into `app/_layout.tsx` after `ChatProvider`. Pure helpers (filtering, dedup) in a separate `lib/marketplace-helpers.ts` so they're unit-testable. No UI.
- [DONE] [est: 18000] marketplace-2: add `app/marketplace.tsx` route that renders the merged `listings` as a card grid (item title, photo, owner display name, mode badge, price). Add a "marketplace" tab to `components/bottom-nav.tsx` next to friends (use `storefront-outline` / `storefront` Ionicons) and reshuffle the layout so the plus-button stays centered. New `marketplaceTitle` / `marketplaceEmpty` translations across all six languages.
- [DONE] [est: 22000] marketplace-3: add a "List on marketplace" button to `app/item/[id].tsx` (only for items the current user owns). Opens a small sheet to pick `mode` (trade vs sell), `askingPrice` (only when sell), `notes`. Calls `addListing` on submit. Enforces the 1-listing-per-user cap by reading `myListings.length`; if over the cap, show a translated "upgrade to premium" hint and disable submit. Show a "Remove listing" button on the same screen when the item is already listed.
- [DONE] [est: 19000] marketplace-4: add `app/listing/[id].tsx` showing the listing's item details, owner profile chip, mode/price, notes, and a "Message owner" CTA. The CTA calls `ensureChatWith(ownerUserId)` from `useChat()` and routes to `/chat/{ownerUserId}`. If the viewer is not friends with the owner, show the same "friends only" hint pattern as the existing chat screen.
- [DONE] [est: 17000] marketplace-5: in `app/listing/[id].tsx`, render a "Recent prices for similar items" section. Pure helper in `lib/marketplace-helpers.ts` does fuzzy name match (≥0.9 similarity via Dice-coefficient or normalized Levenshtein-ratio over lowercased titles) over all completed listings (`soldAt != null` OR mode === "sell" with a recorded `askingPrice`) and returns up to 10 most-recent prices. Add unit tests for the similarity threshold edge cases and the price-history selector.
- [DONE] [est: 6000] marketplace-6: end-to-end smoke - verify build, run all tests, manually confirm marketplace tab renders, listing creation respects 1-item cap, and "Message owner" opens the existing chat flow. Fix any regressions found.

- (decomposed from the original ~120k-token premium-account task)
- premium-1: data layer - add `isPremium: boolean` to `UserProfile` in `lib/types.ts` (default false). Add `lib/premium-context.tsx` backed by AsyncStorage key `collectables-premium-v1-{userId}` exposing `isPremium`, `activatePremium()`, `cancelPremium()`. Wire the provider into `app/_layout.tsx` after `MarketplaceProvider`. Pure helpers (`isPremiumActive`, `mergePremiumState`) in a separate `lib/premium-helpers.ts` for unit testing. No UI changes.
- premium-2: marketplace integration - thread `isPremium` from the new `usePremium()` hook into `app/item/[id].tsx` so the listing CTA calls `canCreateListing(isPremium)` and the upgrade hint stays hidden when premium. Update `MarketplaceContext.canCreateListing` callers and remove the hardcoded `false` default in the context. Add unit tests asserting cap is bypassed when premium=true and respected when premium=false.
- premium-3: settings UI - add a "Premium" section to `app/settings.tsx` with a benefits list (no listing cap, future perks), an "Activate premium" CTA when free, a "Premium active" badge + "Cancel premium" link when premium. Translations (`premiumTitle`, `premiumBenefit1`, `premiumBenefit2`, `premiumActivate`, `premiumActive`, `premiumCancel`, `premiumCanceled`, `premiumActivated`) across all 6 languages.
- premium-4: tests + smoke - add `__tests__/premium-helpers.test.ts` covering the new pure helpers, update marketplace-helpers tests if needed, run full test suite, run `npm run build`, verify settings screen renders the premium section. Fix any regressions.

- post-premium polish (track separately once premium-1..4 land):
- track/log a `premium_activated_at` timestamp so we can later wire renewal/expiry without changing the storage shape.
