[DONE] [est: 2000] - now on first screen there are buttons that cannot be pressed. so i cant login into any account. fix it and use github secrets.
- 
[DONE] [est: 8000] - check previous pipeline issues (there are several tests failed) and fix them
[DONE] [est: 5000] - add chats icon on mobile navbar so it becomes home, search, market, plus, chats, friend, profile.
[DONE] [est: 40000] - design/1: Load Syne+DM Sans fonts in _layout.tsx (useFonts, SplashScreen) and apply fontFamily across all StyleSheets in the 16 listed files
[DONE] [est: 8000] - design/2: Replace plain #261b14 hero Views with LinearGradient in app/index.tsx, components/login-screen.tsx, app/settings.tsx
[DONE] [est: 6000] - design/3: Replace ActivityIndicator loading states with Skeleton component in app/index.tsx, app/collection/[id].tsx, app/item/[id].tsx, app/people.tsx
[DONE] [est: 8000] - design/4: 2-column masonry grid in app/collection/[id].tsx + compact prop on ItemCard component
[DONE] [est: 5000] - design/5: Bottom nav polish — icon size 22, inactive color #bbb0a6, active dot indicator, plus button 50px/border 3px
[DONE] [est: 3000] - remove a small "configured by you in this browser" badge plus a "Clear runtime credentials" button next to the new runtime Supabase config form, so a user who pasted the wrong URL/key can reset without digging into devtools `localStorage`. The clear helper (`clearRuntimeSupabaseConfig`) already exists; this is purely UI plumbing. All credentials should come from github secrets
[DONE] [est: 0] - Wire `supabase/migrations/` into a `supabase db push` step in the deploy workflow (gated on a `SUPABASE_DB_URL` secret). Right now migrations like the new `20260424_chat_messages.sql` need to be applied by hand against the project, which is easy to forget and creates drift between the SQL in git and what's actually live.
[DONE] [est: 0] - Promote the shape-helper pattern used by the new `lib/supabase-chat-shapes.ts` (pure URL/body/header builders kept separate from the fetch wrapper) to the older `lib/supabase-profiles.ts`. Today `supabase-profiles.ts` builds URLs inline inside async functions, which means the only way to verify shape is to mock `fetch`. Splitting out shape helpers would let tests assert the URL/body shape for `upsertCollection`, `fetchProfiles`, and the friend-request endpoints without spinning up a fake fetch.
[DONE] [est: 0] - Add an outbox queue for offline-sent chat messages: when `cloudSendMessage` returns null in `chat-context.tsx`, the locally-generated message stays only on the originating device and is never replayed once the network comes back. A small `pendingByChatId` slice in the existing AsyncStorage cache plus a flush-on-reconnect effect would close that gap without adding a new storage key.
[DONE] [est: 0] - Surface the realtime channel state from `subscribeToInbox` (a `connected` boolean) and render a tiny "offline / reconnecting" pill in `app/chat/[id].tsx`. Today realtime-js silently auto-reconnects, so users can stare at a stale conversation with no signal that pushed messages are paused. Plumbing the SUBSCRIBED / CHANNEL_ERROR status up through chat-context costs a few lines and dramatically improves debuggability.
[DONE] [est: 0] - Auto-clear stale typing presence on tab/window blur (web) and `AppState` background (native) by calling `setTyping(false)` from a single hook. Today the 1s debounce in `app/chat/[id].tsx` clears typing on idle, but if the user backgrounds the app mid-keystroke the other side keeps seeing "is typing..." until the realtime presence channel times them out (~30s). Listening for visibility/AppState changes inside `subscribeToTyping`'s caller is a few lines and removes the visual lag.
[DONE] [est: 0] - Persist `lastReadByChat` to a server-side `chat_reads` table so the unread badge stays consistent across devices. Today the count is read off `lastReadByChat` in AsyncStorage, so opening a chat on phone A still leaves the dot lit on phone B. A small `chat_reads(user_id, chat_id, last_read_at)` table with an UPSERT on `markRead` plus a SELECT on hydrate would close that gap and reuse the existing supabase-chat REST scaffolding.
[DONE] [est: 0] - Refactor the runtime wrappers in `lib/supabase-chat.ts` to take an injected `fetcher`/`tokenProvider` so they can be exercised under `node --test` without the React Native peers. Today the wiring is covered by the regex/structural tests in `supabase-chat-wiring.test.ts`, which do catch missing helper imports but won't catch e.g. a swapped `body`/`headers` argument. A 10-line dependency-injection refactor would let us assert real `fetch.calls[0].arguments` shapes.
[DONE] [est: 4000] - check previous pipeline issues (there are several tests failed) and fix them
[DONE] [est: 3000] - add chats icon on mobile navbar so it becomes home, search, market, plus, chats, friend, profile.
[DONE] [est: 62000] - implement new design from ./design.md throught all application
[DONE] [est: 5000] - Surface a small "configured by you in this browser" badge plus a "Clear runtime credentials" button next to the new runtime Supabase config form, so a user who pasted the wrong URL/key can reset without digging into devtools `localStorage`. The clear helper (`clearRuntimeSupabaseConfig`) already exists; this is purely UI plumbing.
[DONE] [est: 2000] - Wire `supabase/migrations/` into a `supabase db push` step in the deploy workflow (gated on a `SUPABASE_DB_URL` secret). Right now migrations like the new `20260424_chat_messages.sql` need to be applied by hand against the project, which is easy to forget and creates drift between the SQL in git and what's actually live.
[DONE] [est: 8000] - Promote the shape-helper pattern used by the new `lib/supabase-chat-shapes.ts` (pure URL/body/header builders kept separate from the fetch wrapper) to the older `lib/supabase-profiles.ts`. Today `supabase-profiles.ts` builds URLs inline inside async functions, which means the only way to verify shape is to mock `fetch`. Splitting out shape helpers would let tests assert the URL/body shape for `upsertCollection`, `fetchProfiles`, and the friend-request endpoints without spinning up a fake fetch.
[DONE] [est: 5000] - Add an outbox queue for offline-sent chat messages: when `cloudSendMessage` returns null in `chat-context.tsx`, the locally-generated message stays only on the originating device and is never replayed once the network comes back. A small `pendingByChatId` slice in the existing AsyncStorage cache plus a flush-on-reconnect effect would close that gap without adding a new storage key.
[DONE] [est: 4000] - Surface the realtime channel state from `subscribeToInbox` (a `connected` boolean) and render a tiny "offline / reconnecting" pill in `app/chat/[id].tsx`. Today realtime-js silently auto-reconnects, so users can stare at a stale conversation with no signal that pushed messages are paused. Plumbing the SUBSCRIBED / CHANNEL_ERROR status up through chat-context costs a few lines and dramatically improves debuggability.
[DONE] [est: 3000] - Auto-clear stale typing presence on tab/window blur (web) and `AppState` background (native) by calling `setTyping(false)` from a single hook. Today the 1s debounce in `app/chat/[id].tsx` clears typing on idle, but if the user backgrounds the app mid-keystroke the other side keeps seeing "is typing..." until the realtime presence channel times them out (~30s). Listening for visibility/AppState changes inside `subscribeToTyping`'s caller is a few lines and removes the visual lag.
[DONE] [est: 6000] - Persist `lastReadByChat` to a server-side `chat_reads` table so the unread badge stays consistent across devices. Today the count is read off `lastReadByChat` in AsyncStorage, so opening a chat on phone A still leaves the dot lit on phone B. A small `chat_reads(user_id, chat_id, last_read_at)` table with an UPSERT on `markRead` plus a SELECT on hydrate would close that gap and reuse the existing supabase-chat REST scaffolding.
[DONE] [est: 3000] - Refactor the runtime wrappers in `lib/supabase-chat.ts` to take an injected `fetcher`/`tokenProvider` so they can be exercised under `node --test` without the React Native peers. Today the wiring is covered by the regex/structural tests in `supabase-chat-wiring.test.ts`, which do catch missing helper imports but won't catch e.g. a swapped `body`/`headers` argument. A 10-line dependency-injection refactor would let us assert real `fetch.calls[0].arguments` shapes.
[DONE] [est: 8000] Move marketplace listings off AsyncStorage and into a Supabase `marketplace_listings` table once the chat-cloud pattern stabilises. The current `lib/marketplace-context.tsx` is a single-device store, which means a listing made on phone A is invisible to phone B and to other users — the entire point of a marketplace. Mirror the shape-helpers + REST wrapper pattern from `lib/supabase-chat.ts` so the cloud migration is a drop-in that keeps the existing pure helpers testable.
[DONE] [est: 3000] Lift the bottom-nav "balanced layout" trick (a hidden flex spacer cell so the centered plus button keeps its position when the number of tabs grows) into a small `<NavRow>` helper that takes `leftItems` / `rightItems` arrays and pads automatically. Today the spacer in `components/bottom-nav.tsx` is hard-coded and will silently re-skew the next time someone adds another tab; a 30-line helper that always pads the shorter side closes that gap and makes future tabs (e.g. notifications, stats) cheap to add.
[DONE] [est: 4000] Replace the bare `<TextInput keyboardType="numeric">` price field in the marketplace listing sheet (`app/item/[id].tsx`) with a small `<CurrencyInput>` helper that strips non-numeric characters as you type, normalises commas to dots, and surfaces a `currency` selector matching the user's locale. Today the user can paste arbitrary text into the price field and the only validation is `Number(...)` at submit time, which silently drops to a null price when the input doesn't parse — the listing then publishes as "trade" with no warning. Centralising this also unlocks consistent behaviour for any future cost/price field.
[DONE] [est: 4000] Hydrate the listing detail screen (`app/listing/[id].tsx`) from a remote `fetchListingById` helper so deep-link visits work for users whose AsyncStorage hasn't seen the listing yet. Today `getListingById` only sees the local cache, so opening a `/listing/{id}` link from another device renders the not-found EmptyState even when the listing exists on the owner's machine. Once the marketplace moves to Supabase (separate suggestion), wiring an additional REST fetch into the existing `useEffect` will close the gap with one new helper in `lib/supabase-profiles.ts`.
[DONE] [est: 3000] Tighten the `priceHistoryForTitle` similarity heuristic by also stripping common collectible-stopwords (e.g. "holo", "rare", "edition", "limited", "1st") before computing the Dice coefficient. Today "Charizard Holo 1999" and "Charizard 1st Edition 1999" land at ~0.71 similarity and never meet the 0.9 threshold, even though a human would call them comparable comps. A 20-line stopword filter inside `normalizeTitle` (gated by a constant so tests remain deterministic) would dramatically widen the comp pool without false positives because the stopwords are generic.
[DONE] [est: 2000] Add a CI smoke step that boots `npx expo export --platform web` and parses the resulting `dist/_expo/static/js/web/*.js` for known regression markers (e.g. an `marketplaceListOnMarketplace` translation key that fails to resolve, or a `_layout.tsx` provider that wasn't wrapped). Today regression detection for the marketplace flow lives only in `__tests__/marketplace-*.test.ts`, which exercise pure helpers but cannot catch screen-level wiring breakage (a missing provider, a stale i18n key) — those slip past `tsc --noEmit` because they're string-keyed lookups. A 30-line bundle-grep step in CI catches the long tail of "build green, screen blank" regressions for free.
[DONE] [est: 3000] - Centralise per-user AsyncStorage keys (chat, social, premium) into a single `lib/storage-keys.ts` module that exports `chatCacheKey(userId)`, `socialCacheKey(userId)`, `premiumKey(userId)`. Today each context hardcodes its own template (`collectables-chats-v1-${userId}`, `collectables-social-v1-${userId}`, `collectables-premium-v1-${userId}`) which means renaming a key requires grepping multiple files and risks leaving stale data behind. A 20-line key-builder module also makes it trivial to add a `clearAllUserData(userId)` helper for the existing "Delete account" flow which today only clears auth + cloud and leaves these caches behind.
[DONE] [est: 2000] - Render a small "Premium" badge next to the user's avatar in `components/bottom-nav.tsx` (and on profile chips) by threading `isPremium` from `usePremium()` into the existing avatar rendering. Today the only signal that premium is active lives on the settings screen, so users have no ambient confirmation that their upgrade took effect — a tiny gold dot or star overlay closes that gap and surfaces the social value of the feature without adding a new screen.
[DONE] [est: 3000] - Add a `formatRelativeDate(iso, locale)` helper in `lib/i18n-context.tsx` and reuse it for the new `premiumActiveSince` string instead of the bare `activatedAt.slice(0, 10)`. Today the settings screen renders raw ISO date prefixes ("Premium since 2026-04-25") which look out of place next to the rest of the UI which speaks human ("just now", "yesterday"). A 30-line `Intl.RelativeTimeFormat`-based helper would also unblock similar polish for the chat preview "last seen" line and the listing "added X ago" hint.
[DONE] [est: 0] - Hide the "Activate premium" CTA in `app/settings.tsx` while `usePremium().ready === false` to avoid a brief flash of the free-tier UI for premium users on cold start. Today the settings screen reads `isPremium` immediately, but the AsyncStorage hydration is async — for a fraction of a second a premium user sees the "Activate" button before the cached state lands. A small `if (!ready) return null` over the CTA (or a `Skeleton` placeholder) closes the gap and matches how `_layout.tsx` already gates rendering on `ready` for auth/i18n.
[DONE] [est: 0] - Wire the new `premiumActivatedAt` log into a `premiumExpiresAt(state, periodDays)` helper in `lib/premium-helpers.ts` and surface a "renews on YYYY-MM-DD" line in `app/settings.tsx`. The storage shape now persists the activation log across cancellations, so the helper only needs to add `periodDays * 86400000` and format — no migration. Pair it with a tiny `isPremiumExpired(state, now, periodDays)` selector and an effect in `lib/premium-context.tsx` that auto-calls `cancelPremium()` when expired, so the existing `isPremium` boolean stays the single source of truth for downstream callers (marketplace cap, UI badges).
[DONE] [est: 2000] - Apply the same `ready`-gate pattern just landed in `app/settings.tsx` to every other screen that branches on `usePremium().isPremium` (today: `app/item/[id].tsx` listing CTA, plus any new premium badges as they roll out). Today only the settings card avoids the cold-start flash; on item screens a premium user can still briefly see the "upgrade for unlimited listings" hint while AsyncStorage hydrates. A reusable `<PremiumGate fallback={...}>` wrapper around the existing `usePremium()` call would close the gap consistently across screens for ~30 lines.
[DONE: 2026-05-03] [est: 2000] - Add a TTL to the new `viewerProfiles` cache in `SocialProvider`. Today an entry stays in memory for the entire session, so a name/avatar change made elsewhere only shows up after the user signs out and back in. Stamping each cache entry with `cachedAt` and refetching anything older than ~10 minutes inside `ensureProfilesLoaded` would close the staleness window without giving up the dedup wins from this task.

[DONE: 2026-05-03] [est: 1000] - Cache `node_modules` and the Expo Metro cache (`~/.expo`, `node_modules/.cache`) between CI runs in both `ci.yml` and `release.yml`. The new web-build step adds ~1min per PR push that's mostly Metro re-bundling unchanged dependencies; an `actions/cache` step keyed on `package-lock.json` would cut that close to zero on warm runs without changing semantics.

[DONE: 2026-05-03] [est: 1500] - Add a small `lib/cloudinary-config.ts` (pure, no react-native import) that reads the three `EXPO_PUBLIC_CLOUDINARY_*` env vars once and exports a single `cloudinaryConfig` object (apiBase, cloudName, uploadPreset). Today `lib/cloudinary.ts` reads `process.env` at module top-level — fine for production but invisible in tests. A pure config module with a `resolveCloudinaryConfig(env)` helper that takes a record makes it trivial to add coverage for the priority rules (override URL wins over cloud name, etc.) and lets future modules share the same parsed config without re-reading env in three places.

[DONE: 2026-05-03] [est: 2000] - Replace the dual fixed-interval polls landed in `app/chat/[id].tsx` (8s) and `app/chats.tsx` (15s) with a single visibility-aware scheduler that pauses when the tab is hidden / app is backgrounded and resumes (with one immediate refresh) on return. Today both intervals run continuously regardless of focus, which wastes Supabase REST quota on idle tabs and adds avoidable battery drain on mobile. A small `useVisibilityRefresh(refreshFn, intervalMs)` hook in `lib/chat-context.tsx` (listening to `document.visibilitychange` on web and `AppState` change on native) would centralise the cadence and let future cloud-backed screens (premium, marketplace) reuse the same primitive.
[DONE: 2026-05-03] [est: 1500] - Surface a "connection / sync" indicator next to the new web header chats button so users know whether realtime is online (cheap green dot) or whether their messages will land via the on-mount REST refetch (yellow). The plumbing exists: `subscribeToInbox` already receives `SUBSCRIBED` / `CHANNEL_ERROR` status updates but ignores them. Threading that boolean through `ChatContext` (a `realtimeOnline` flag) and rendering an 8px dot on the header icon would close the visibility gap without adding a new screen, and pairs naturally with the new `refreshFromCloud` fallback.

- Extract font constants (`FONT_DISPLAY`, `FONT_BODY`, `FONT_BODY_BOLD`, `FONT_BODY_SEMIBOLD`) into a shared `lib/fonts.ts` module so the string literals `"Syne-ExtraBold"`, `"DMSans-Bold"` etc. don't need to be copy-pasted across 16 files. The current approach works but a typo in a fontFamily string silently falls back to the system font on native; a constants file + a TypeScript union type would catch mismatches at compile time.

- Extract a `<NavTab>` component from the repeated `<Pressable>` + `<Ionicons>` + `renderBadge` block in `components/bottom-nav.tsx`. The three-cell repetition (left map, plus, right map) now renders the same 6-line icon/badge pattern twice; a 20-line sub-component with `icon`, `iconActive`, `active`, `badge`, and `onPress` props would shrink the render output and make it easier to add future tabs (e.g. notifications) without copy-pasting.

- Add a `postinstall` npm script that runs `npx expo install --check` to validate peer dependencies after `npm install`, and commit a `package-lock.json` to CI so `npm ci` can be used instead of `npm install`. Today the pipeline fails silently when `node_modules` is absent because `tsx` falls back to the globally-cached version which lacks tsconfig path-alias support; a lock-file + `npm ci` step would make installs deterministic and surface peer-dep drift before tests run.
- Add a `lint:ci` script in `package.json` that runs `npx tsc --noEmit && npm test` together as a single pre-push gate. Today CI catches TypeScript errors and test failures separately, but a developer can accidentally push code that passes tests yet has TS errors (as happened with the duplicate `mergeCloudMessages`). A combined gate at the bottom of the CI job list would surface both classes of failures in the same place and make the pipeline failure reason obvious at a glance.
- Expose `clearRuntimeSupabaseConfig` from `lib/supabase.ts` as a dev-only escape hatch via the Expo DevMenu (registered with `addDevMenuItems`) instead of a persistent UI button. Today the function is dead code since the runtime config form was removed; surfacing it only in dev builds via the DevMenu would let engineers flush a bad local override without polluting the production login screen with credentials-reset UI.
- Add a realtime subscription to `marketplace_listings` inserts in `lib/supabase-marketplace.ts` so the marketplace screen auto-refreshes when another user posts a new listing — mirroring the `subscribeToInbox` pattern from `lib/supabase-chat.ts`. Today the context only fetches on mount, so a listing posted by another user is invisible until the viewer hard-reloads. Wiring a Supabase realtime channel on `marketplace_listings` (filtered to `sold_at=is.null`) closes the gap with ~30 lines using the existing `RealtimeClient` setup.
- Extract `NavTab` from `components/bottom-nav.tsx` into a standalone `components/nav-tab.tsx` file so it can be reused in a future desktop/tablet sidebar nav without pulling in the full `BottomNav` component. Today `NavTab` is a local helper inside `bottom-nav.tsx`; the 20-line extraction costs nothing and makes the component graph explicit.
- Extend `CurrencyInput` with a locale-aware default currency by reading `useI18n().language` in the component and mapping it to a suggested currency (e.g. `ru`→RUB, `de`→EUR, `pl`→PLN). Today the component always defaults to USD on first open, so a Russian user sees USD pre-selected even though RUB is more natural. A 10-line lookup map inside the component closes that gap without adding a prop.
- Show a `<SkeletonItemDetail>` placeholder in the listing detail screen while `fetchingRemote === true` instead of the empty-state "🪧" icon with "..." title. Today deep-link visitors see a brief flash of the not-found empty state while the remote fetch resolves; a skeleton placeholder would communicate loading intent and match the pattern used in `app/item/[id].tsx`.
- Make `COLLECTIBLE_STOPWORDS` configurable per-collection via a `Collection.stopwords?: string[]` field so users who collect stamps (where "rare" IS a meaningful distinction) can opt out of the default word list. Today the filter applies globally, which may over-strip for some categories. A per-collection override stored in AsyncStorage requires no migration and keeps the default fast.
- Expand the bundle smoke check to also grep for the six i18n language codes (`"ru"`, `"en"`, `"be"`, `"pl"`, `"de"`, `"es"`) to catch cases where a tree-shaking regression strips the non-default translation maps. Today the check only verifies key names in the English map and provider names; a full-locale grep is an extra 6-line addition to the shell script.
- Add a CI step that checks all `EXPO_PUBLIC_*` secrets are non-empty before the build starts and fails early with a clear message (e.g. "EXPO_PUBLIC_SUPABASE_URL is required"). Today an unset secret produces an empty string that silently disables login buttons in the deployed app; a pre-build validation step converts a silent UX bug into an obvious CI failure.
- Add a `migrateStorageKey(oldKey, newKey)` utility in `lib/storage-keys.ts` that reads the old key, writes the value under the new key, then deletes the old key. Today renaming a versioned key (e.g. `collectables-chats-v1` → `v2`) silently loses all cached data for existing users because the new key starts empty. A 10-line migrate helper used once at context boot would let any future versioned rename be lossless.
- Extend the premium dot in `components/bottom-nav.tsx` with a `useEffect` that fires a one-time `ToastProvider` "Premium active" toast on the session when `isPremium` transitions from false to true. Today activating premium from the settings screen navigates back without any ambient confirmation visible on the home tab; a single toast keyed on the transition closes the feedback gap without adding a new screen.
- Reuse `formatRelativeDate` for the chat preview "last seen" timestamp and the marketplace listing "added X ago" hint. Both currently render raw ISO slices; swapping them to `formatRelativeDate` (already available via `useI18n()`) would make those timestamps locale-aware for free.
- Use `<PremiumGate>` in `app/marketplace.tsx` wherever the listing cap UI branches on `isPremium`, so the ready-gate pattern is consistent across all premium-branching screens without manual `premiumReady` destructuring.
- Add a `lib/design-tokens.ts` module that exports the color palette (`HERO_DARK`, `AMBER_ACCENT`, `CARD_BG`, `BORDER`) as typed constants. Today 16+ files reference hex strings like `#261b14` and `#d89c5b` directly, so a brand colour change requires a grep-and-replace across the entire codebase with no compile-time safety net. A 20-line constants file would let TypeScript catch typos and make palette-wide changes a one-line edit.