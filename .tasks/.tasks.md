## The helper four runs had filed, and the bug in its first draft (2026-08-30)

<!-- First bullet of "Collections-mount follow-ups", filed minutes earlier this run and in the three suggestion blocks before it: "six suites now hand-write the same harness and the sixth needed fourteen `mockModule` calls ... the helper has been filed after each of the last three mounts and the cost of not having it just tripled". .tasks.md's three remaining tasks are all still blocked. -->

- [DONE: 2026-08-30] [est: 27000] **`__tests__/helpers/mount-provider.ts` exists, four suites are on it, and writing its own test found that its drain returned one macrotask early** — 262 lines of duplicated harness came out of `chat`, `premium`, `social` and `collections`, and the copies had already diverged (two supported a `writeError`, two did not). What the extraction was actually for is the second half.

  **Each suite drained its provider's async hydrate with a HAND-COUNTED number of `settle()`/`rerender()` pairs** — two for chat, four for collections, each arrived at by watching a case fail and adding one. That number is a claim about how many awaits a hydrate holds, written nowhere near the hydrate, and it fails silently in the worst direction: a sixth await added to a provider turns "nothing was written under user B's key" into "nothing had run yet", and the leak case passes. `RenderResult` exposes `dirty` now and `drain()` loops on it, throwing if the tree never settles.

  **The first `drain()` read `dirty` AFTER the re-render, and that is wrong by one whole step.** A hydrate's `setState` calls arrive from a promise chain BETWEEN two builds; a build resets the flag and then renders, so a flag read afterwards describes the render that just consumed the change rather than the change itself. Against a three-await fixture it stopped at step two — the exact early exit the fixed counts had, reproduced by the helper meant to remove it. Reading `dirty` first asks the right question: has anything moved since the last render.

  **Ten cases for the harness, and the mutation check is the one that matters:** restoring the after-render read fails the three-await case and nothing else. A runaway effect that sets its own input throws with a message naming that shape rather than timing the suite out somewhere less informative; `providerHarness` imports its provider exactly once across mounts, refuses `value()` before a render, and resets the storage-failure budget per case.

  **Two suites keep their own store double and the guard says why.** `i18n` and `diagnostics` read ONE key, so their doubles answer every `getItem` with the same string and their cases set that string rather than a map entry; the exemption goes through `assertExemptionsHonest`, so an entry that stops needing the hole fails rather than leaving one open. Two sweeps keep a seventh suite from re-hand-rolling either piece.

  `npm run verify` green: typecheck, `lint:all`, 7084 tests (+10), build, bundle secrets, bundle size (28.3 KiB headroom), bundle smoke.

